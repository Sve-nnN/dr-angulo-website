{
  "name": "seo-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tooling de datos del milestone v1.2. Paquete Node independiente: NO es un workspace del package.json de la raiz y el constructor de la imagen de produccion no lo instala.",
  "scripts": {
    "cli": "tsx --env-file=../.secrets/.env src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@googleapis/sheets": "^14.0.0",
    "p-limit": "^7.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
