---
phase: 04-contenido-seo-legal-y-publicaci-n
plan: 04-02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [SEO-04, INFRA-03]
user_setup:
  - service: vercel
    why: "Publicar el sitio en una URL pública requiere login/autorización de Juan en su cuenta de Vercel — un agente no puede autenticarse en su nombre"
    env_vars:
      - name: NEXT_PUBLIC_GA_ID
        source: "Google Analytics 4 → Admin → Data streams → Measurement ID (opcional; el sitio funciona sin él, AnalyticsScripts simplemente no renderiza)"
      - name: NEXT_PUBLIC_META_PIXEL_ID
        source: "Meta Events Manager → Data sources → Pixel ID (opcional, mismo criterio)"
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys (opcional hasta tener dominio verificado; el formulario cae a WhatsApp mientras tanto)"
    dashboard_config:
      - task: "Importar el repo en Vercel (Add New → Project) o correr vercel login && vercel deploy desde la raíz del proyecto"
        location: "https://vercel.com/new"
        details: "Framework preset: Next.js (autodetectado). Sin configuración adicional necesaria — el build ya pasa limpio."
    local_dev: []

must_haves:
  truths:
    - "El sitio responde correctamente en mobile y desktop, verificado visualmente en navegador"
    - "Las imágenes (next/image) y fuentes (next/font) no generan layout shift visible"
    - "El sitio está desplegado y accesible en una URL pública de Vercel"
  artifacts: []
  key_links: []
---

<objective>
QA visual/responsive/performance de todo el sitio (incluyendo blog y privacidad agregados en 04-01) y publicación en una URL pública de Vercel.

Purpose: confirmar que el sitio se ve y funciona bien en mobile/desktop antes de publicarlo (SEO-04), y hacerlo accesible públicamente (INFRA-03).
Output: sitio verificado visualmente en navegador, build de producción listo. Deploy a Vercel — PENDIENTE, requiere acción de Juan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: QA visual/responsive de todo el sitio</name>
  <files></files>
  <action>Revisión visual en navegador (desktop + mobile) de todas las rutas del sitio, incluyendo /blog, /blog/[slug] y /privacidad agregadas en 04-01: header/menú móvil, botón flotante de WhatsApp, banner de cookies, formulario de contacto, imágenes (next/image) y fuentes (next/font self-hosted, `display: swap` desde Phase 1) sin layout shift visible. Sin archivos de código dedicados — QA transversal sobre lo ya construido en fases 1-4.</action>
  <verify>npm run build limpio + inspección visual en navegador (desktop y viewport mobile) de cada ruta</verify>
  <acceptance_criteria>
    - "npm run build termina sin errores ni warnings"
    - "Las 18 rutas generadas (14 páginas + 4 artículos de blog) renderizan sin contenido roto ni placeholder"
  </acceptance_criteria>
  <done>Sitio verificado visualmente en desktop y mobile, sin layout shift visible</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Sitio completo, build de producción limpio (`npm run build`), listo para desplegar — el deploy en sí NO se ejecutó todavía.</what-built>
  <how-to-verify>
    Juan debe: 1) Correr `vercel login` seguido de `vercel deploy` desde la raíz del proyecto, o conectar el repositorio desde https://vercel.com/new (framework Next.js se autodetecta, sin configuración adicional). 2) Confirmar la URL pública `*.vercel.app` que devuelve Vercel. 3) Opcionalmente, antes o después del deploy, configurar en el dashboard de Vercel (Project → Settings → Environment Variables) las variables `NEXT_PUBLIC_GA_ID`, `NEXT_PUBLIC_META_PIXEL_ID` y `RESEND_API_KEY`/`EMAIL_FROM` cuando existan esas cuentas — el sitio funciona sin ellas.
  </how-to-verify>
  <resume-signal>Pegar la URL de Vercel una vez desplegado, o confirmar que el deploy se hizo</resume-signal>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npm run build limpio
- [x] QA visual/responsive verificado en navegador (desktop + mobile)
- [ ] Deploy a Vercel ejecutado y URL pública confirmada — **PENDIENTE, requiere login/autorización de Juan**
</verification>

<success_criteria>
- Sitio verificado visualmente en desktop y mobile, sin layout shift
- Build de producción limpio y listo para desplegar
- Deploy a Vercel: PENDIENTE — acción humana de Juan requerida (login/autorización en Vercel)
</success_criteria>

<output>
Ver `.planning/phases/04-contenido-seo-legal-y-publicaci-n/04-SUMMARY.md` (summary combinado con 04-01).
</output>

---
**Estado:** Parcial — QA completado (commit `88b397d`), deploy a Vercel pendiente. Ver `04-SUMMARY.md`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
