---
phase: 04-contenido-seo-legal-y-publicaci-n
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified: [src/content/blog.ts, src/app/blog/page.tsx, "src/app/blog/[slug]/page.tsx", src/app/privacidad/page.tsx, src/components/cookie-consent-banner.tsx, src/components/analytics/consent.ts, src/components/analytics/analytics-scripts.tsx, src/app/sitemap.ts, src/app/layout.tsx, src/components/layout/footer.tsx]
autonomous: true
requirements: [BLOG-01, LEGAL-01, LEGAL-02]
user_setup: []

must_haves:
  truths:
    - "Existen entre 4 y 6 artículos de blog basados en contenido educativo real del doctor, cada uno con su propia URL y metadata"
    - "Aparece un aviso de cookies antes de cargar GA4/Meta Pixel, y estos solo cargan si el visitante acepta explícitamente"
    - "Existe una página de Política de Privacidad enlazada desde el footer en todas las páginas"
  artifacts:
    - path: "src/content/blog.ts"
      provides: "4 posts (slug/title/description/date/paragraphs) — contenido basado en el Instagram real del doctor"
    - path: "src/app/blog/page.tsx"
      provides: "Listado de blog con metadata propia y canonical"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Página de artículo individual con generateStaticParams + generateMetadata por post (OG, canonical)"
    - path: "src/app/privacidad/page.tsx"
      provides: "Política de privacidad (datos recopilados, WhatsApp, cookies/analítica, derechos Ley 29733, contacto), noindex"
    - path: "src/components/cookie-consent-banner.tsx"
      provides: "Banner de consentimiento (Aceptar/Rechazar) con link a /privacidad"
    - path: "src/components/analytics/consent.ts"
      provides: "Store de consentimiento en localStorage vía useSyncExternalStore, compartido entre banner y analytics"
  key_links:
    - from: "src/components/cookie-consent-banner.tsx"
      to: "src/components/analytics/consent.ts"
      via: "useSyncExternalStore(subscribeConsent, getStoredConsent, ...) — el banner se oculta cuando ya hay una decisión guardada"
      pattern: "useSyncExternalStore"
    - from: "src/components/analytics/analytics-scripts.tsx"
      to: "src/components/analytics/consent.ts"
      via: "mismo store de consentimiento — GA4/Meta Pixel solo se renderizan si consent === \"granted\""
      pattern: "consent !== \"granted\""
    - from: "src/app/layout.tsx"
      to: "src/components/cookie-consent-banner.tsx"
      via: "render directo en el body, junto a AnalyticsScripts"
      pattern: "CookieConsentBanner"
    - from: "src/content/blog.ts"
      to: "src/app/sitemap.ts"
      via: "blogPosts.map() genera una entrada de sitemap por artículo"
      pattern: "blogPosts.map"
---

<objective>
Blog propio con artículos educativos basados en el Instagram real del doctor, página de Política de Privacidad y banner de consentimiento de cookies que gatea GA4/Meta Pixel.

Purpose: cumplir BLOG-01 (contenido SEO propio) y LEGAL-01/LEGAL-02 (privacidad + cookies) antes de publicar el sitio.
Output: `/blog`, `/blog/[slug]` (4 artículos), `/privacidad`, banner de cookies conectado al gate de analytics existente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/SEO-TRACKING.md
@src/components/analytics/analytics-scripts.tsx
@src/lib/site-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contenido de blog (listado + artículos)</name>
  <files>src/content/blog.ts, src/app/blog/page.tsx, src/app/blog/[slug]/page.tsx, src/app/sitemap.ts</files>
  <read_first>.planning/PROJECT.md, .planning/research/SEO-TRACKING.md</read_first>
  <action>4 artículos en `src/content/blog.ts` (tipo `BlogPost`: slug/title/description/date/paragraphs[]) adaptados del contenido educativo real del Instagram del doctor (dolor de espalda, hernia discal vs. dolor muscular, miedo a la cirugía de columna, estenosis espinal). Listado en `/blog` con metadata propia y canonical. Artículo individual en `/blog/[slug]` con `generateStaticParams` (SSG para los 4 posts) y `generateMetadata` por post (title/description/OG/canonical). Cada artículo cierra con un CTA de WhatsApp (`WhatsAppCta location="services"`). `sitemap.ts` mapea `blogPosts` a una entrada por artículo.</action>
  <verify>npm run build — /blog y las 4 rutas /blog/[slug] se generan como SSG</verify>
  <acceptance_criteria>
    - "blog.ts exporta blogPosts con 4 posts, cada uno con slug/title/description/date/paragraphs"
    - "blog/[slug]/page.tsx exporta generateStaticParams y generateMetadata"
    - "sitemap.ts incluye blogPosts.map(...)"
  </acceptance_criteria>
  <done>4 artículos publicados y navegables, indexables individualmente</done>
</task>

<task type="auto">
  <name>Task 2: Política de privacidad</name>
  <files>src/app/privacidad/page.tsx, src/components/layout/footer.tsx</files>
  <read_first>src/lib/site-config.ts, .planning/PROJECT.md</read_first>
  <action>Página `/privacidad` con 5 secciones: datos que se recopilan (formulario), contacto por WhatsApp, cookies/analítica (explica el gate de consentimiento), derechos del usuario (Ley N.º 29733, contacto por WhatsApp para ejercerlos) y contacto general. Metadata con `robots: { index: false, follow: true }` — página legal, no compite por keywords. Link "Política de privacidad" agregado al footer, visible en todas las páginas.</action>
  <verify>Visita /privacidad — 5 secciones visibles; link "Política de privacidad" visible en el footer de cualquier página</verify>
  <acceptance_criteria>
    - "privacidad/page.tsx exporta metadata con robots index: false"
    - "footer.tsx contiene Link href=\"/privacidad\""
  </acceptance_criteria>
  <done>Política de privacidad publicada y enlazada desde el footer</done>
</task>

<task type="auto">
  <name>Task 3: Banner de cookies + gate de consentimiento sobre analytics</name>
  <files>src/components/cookie-consent-banner.tsx, src/components/analytics/consent.ts, src/components/analytics/analytics-scripts.tsx, src/app/layout.tsx</files>
  <read_first>src/components/analytics/analytics-scripts.tsx</read_first>
  <action>Store de consentimiento (`consent.ts`) en localStorage (clave `cookie_consent`, valores `granted`/`denied`), expuesto vía `useSyncExternalStore` (`getStoredConsent`/`setStoredConsent`/`subscribeConsent`/`getConsentServerSnapshot`) para evitar mismatches de hidratación SSR. Banner (`cookie-consent-banner.tsx`) se muestra solo si `consent === null` (sin decisión previa), con botones Aceptar/Rechazar y link a `/privacidad`. `analytics-scripts.tsx` (existente de Phase 3) se modifica para no montar `<GoogleAnalytics>`/Meta Pixel salvo `consent === "granted"`, además de las env vars ya requeridas. Ambos componentes renderizados en `layout.tsx`.</action>
  <verify>En navegador: limpiar localStorage → banner visible; GA4/Meta Pixel NO se cargan hasta hacer clic en "Aceptar" (Network tab); clic en "Rechazar" oculta el banner sin cargar analytics</verify>
  <acceptance_criteria>
    - "consent.ts expone getStoredConsent/setStoredConsent/subscribeConsent"
    - "analytics-scripts.tsx contiene consent !== \"granted\" como guard antes del return"
    - "layout.tsx renderiza CookieConsentBanner y AnalyticsScripts"
  </acceptance_criteria>
  <done>GA4/Meta Pixel nunca se montan sin consentimiento explícito</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npm run build limpio (4 artículos de blog generados como SSG)
- [x] Banner de cookies verificado en navegador — gatea la carga de analytics
- [x] /privacidad accesible y enlazada desde el footer
</verification>

<success_criteria>
- 4 artículos de blog con metadata propia, navegables desde /blog
- Banner de cookies gatea GA4/Meta Pixel; política de privacidad publicada y enlazada
- Sin errores ni warnings de build
</success_criteria>

<output>
Ver `.planning/phases/04-contenido-seo-legal-y-publicaci-n/04-SUMMARY.md` (summary combinado con 04-02).
</output>

---
**Estado:** Completado — ver `04-SUMMARY.md` y commit `88b397d`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
