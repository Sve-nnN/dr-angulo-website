---
phase: 01-fundaci-n-t-cnica-y-de-marca
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/layout.tsx, src/components/layout/header.tsx, src/components/layout/footer.tsx, src/components/layout/whatsapp-float-button.tsx, src/lib/site-config.ts, src/app/icon.png, src/app/apple-icon.png]
autonomous: true
requirements: [INFRA-02, BRAND-03, BRAND-04]
user_setup: []

must_haves:
  truths:
    - "Header, footer y botón flotante de WhatsApp visibles en todas las páginas"
    - "Logo/wordmark del doctor visible en header y como favicon"
    - "Header responsive: nav desktop + menú móvil funcional"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Metadata base, fuentes, JSON-LD gate, analytics gate, wraps Header/Footer/WhatsAppFloatButton"
    - path: "src/components/layout/header.tsx"
      provides: "Nav desktop + menú móvil + CTA WhatsApp"
    - path: "src/components/layout/footer.tsx"
      provides: "NAP (nombre/dirección/teléfono) + redes"
    - path: "src/components/layout/whatsapp-float-button.tsx"
      provides: "Botón flotante con tracking whatsapp_click"
    - path: "src/lib/site-config.ts"
      provides: "Config centralizada (nombre, teléfono, dirección, redes)"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "import + render en el body"
      pattern: "Header"
---

<objective>
Layout global (header, nav, footer, botón flotante de WhatsApp) presente en todas las páginas, con el logo real del doctor y favicon.

Purpose: esqueleto compartido que envuelve todas las páginas de contenido de Phase 2 en adelante.
Output: `layout.tsx` + componentes de layout + favicon.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout + site config</name>
  <files>src/app/layout.tsx, src/lib/site-config.ts</files>
  <action>Root layout con metadata base, fuentes (Poppins/Inter), gate de analytics/JSON-LD, y config centralizada del sitio (nombre, teléfono, dirección, redes) en `site-config.ts` para no repetir datos hardcodeados en cada componente.</action>
  <verify>npm run dev — layout visible en todas las rutas</verify>
  <acceptance_criteria>
    - "layout.tsx importa y renderiza Header, Footer, WhatsAppFloatButton"
  </acceptance_criteria>
  <done>Layout envuelve todas las páginas</done>
</task>

<task type="auto">
  <name>Task 2: Header, Footer, WhatsApp flotante</name>
  <files>src/components/layout/header.tsx, src/components/layout/footer.tsx, src/components/layout/whatsapp-float-button.tsx</files>
  <action>Header con nav desktop + menú móvil + CTA WhatsApp contextual. Footer con NAP (nombre/dirección/teléfono) y redes. Botón flotante de WhatsApp con mensaje prellenado y tracking `whatsapp_click` (cta_location).</action>
  <verify>Verificado visualmente en navegador (desktop + mobile)</verify>
  <acceptance_criteria>
    - "whatsapp-float-button.tsx dispara evento whatsapp_click"
    - "header.tsx tiene menú móvil funcional"
  </acceptance_criteria>
  <done>Header/Footer/WhatsApp flotante funcionales en todas las páginas</done>
</task>

<task type="auto">
  <name>Task 3: Logo real + favicon</name>
  <files>src/app/icon.png, src/app/apple-icon.png</files>
  <action>Logo del doctor (recorte de captura de Instagram, mejor fuente disponible) integrado en Header y como favicon/apple-icon.</action>
  <verify>Favicon visible en pestaña del navegador; logo visible en header</verify>
  <acceptance_criteria>
    - "src/app/icon.png y apple-icon.png existen"
  </acceptance_criteria>
  <done>Logo y favicon visibles</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] Layout visible en todas las páginas (verificado en navegador)
- [x] Logo y favicon reales integrados
- [x] Botón WhatsApp flotante con tracking
</verification>

<success_criteria>
- Header/Footer/WhatsApp flotante presentes en todas las rutas
- Logo real del doctor visible en header y favicon
</success_criteria>

---
**Estado:** Completado — ver `01-SUMMARY.md` y commit `88b397d`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
