---
phase: 03-conversi-n-tracking-y-seo-t-cnico
plan: 03-02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/analytics/analytics-scripts.tsx, src/components/analytics/consent.ts, src/lib/tracking.ts, src/components/ui/whatsapp-cta.tsx, src/components/layout/whatsapp-float-button.tsx]
autonomous: true
requirements: [CONTACT-01, CONTACT-02, TRACK-01, TRACK-02, TRACK-03, TRACK-04]
user_setup:
  - service: "Google Analytics 4"
    why: "TRACK-01 requiere un Measurement ID real; el código ya está gateado y listo, solo falta la cuenta"
    env_vars:
      - name: NEXT_PUBLIC_GA_ID
        source: "Google Analytics → Admin → Flujos de datos → Measurement ID (formato G-XXXXXXX)"
  - service: "Meta Pixel"
    why: "TRACK-02 requiere un Pixel ID real; el código ya está gateado y listo, solo falta la cuenta"
    env_vars:
      - name: NEXT_PUBLIC_META_PIXEL_ID
        source: "Meta Events Manager → Orígenes de datos → Pixel → ID del píxel"

must_haves:
  truths:
    - "El botón flotante de WhatsApp dispara whatsapp_click con cta_location='floating_button'"
    - "Los WhatsAppCta de header, hero, servicios, footer y contacto disparan whatsapp_click con su propio cta_location"
    - "Enviar el formulario de contacto dispara generate_lead (GA4) y Lead (Meta)"
    - "GA4 y Meta Pixel no se montan si falta la env var correspondiente, sin importar el consentimiento"
    - "GA4 y Meta Pixel no se montan si el visitante no otorgó consentimiento de cookies, aunque la env var esté configurada"
  artifacts:
    - path: "src/lib/tracking.ts"
      provides: "trackWhatsAppClick(location) y trackFormSubmit(outcome) — wrappers tipados sobre window.gtag/window.fbq"
    - path: "src/components/ui/whatsapp-cta.tsx"
      provides: "CTA de WhatsApp reutilizable (3 variantes visuales) con prop location: CtaLocation que dispara tracking en el onClick"
    - path: "src/components/layout/whatsapp-float-button.tsx"
      provides: "Botón flotante fixed bottom-right con tracking whatsapp_click cta_location='floating_button'"
    - path: "src/components/analytics/analytics-scripts.tsx"
      provides: "Monta GoogleAnalytics (@next/third-parties/google) y el snippet de Meta Pixel solo con env var + consentimiento"
    - path: "src/components/analytics/consent.ts"
      provides: "Store de consentimiento sobre localStorage vía useSyncExternalStore, consumido por AnalyticsScripts y por el banner de cookies de Phase 4"
  key_links:
    - from: "src/components/ui/whatsapp-cta.tsx"
      to: "src/lib/tracking.ts"
      via: "onClick={() => trackWhatsAppClick(location)}"
      pattern: "trackWhatsAppClick\\(location\\)"
    - from: "src/components/layout/whatsapp-float-button.tsx"
      to: "src/lib/tracking.ts"
      via: "onClick={() => trackWhatsAppClick(\"floating_button\")}"
      pattern: "trackWhatsAppClick\\(\"floating_button\"\\)"
    - from: "src/components/analytics/analytics-scripts.tsx"
      to: "src/components/analytics/consent.ts"
      via: "useSyncExternalStore(subscribeConsent, getStoredConsent, getConsentServerSnapshot) — gate de consentimiento, además del gate de env var"
      pattern: "useSyncExternalStore"
---

<objective>
Cada botón de WhatsApp del sitio mide su clic por ubicación, y el envío del formulario de contacto mide su conversión — todo gateado para no cargar ni un script de analítica sin env var configurada y consentimiento otorgado.

Purpose: sin esto, "agenda por WhatsApp en menos de 2 clics" (core value del proyecto) no se puede medir ni optimizar — el doctor necesita saber qué CTA convierte más.
Output: `lib/tracking.ts` con los dos eventos, `WhatsAppCta`/`WhatsAppFloatButton` instrumentados, `AnalyticsScripts` con doble gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/site-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/tracking.ts — eventos GA4 + Meta Pixel</name>
  <files>src/lib/tracking.ts</files>
  <read_first>src/lib/site-config.ts</read_first>
  <action>Declarar `window.gtag`/`window.fbq` en un `declare global`. Exportar `trackWhatsAppClick(location: CtaLocation)` que llama `window.gtag?.("event", "whatsapp_click", { cta_location: location, event_category: "engagement" })` y `window.fbq?.("track", "Contact", { cta_location: location })`. Exportar `trackFormSubmit(outcome: "email" | "whatsapp_fallback")` que llama `window.gtag?.("event", "generate_lead", { cta_location: "contact_form", outcome })` y `window.fbq?.("track", "Lead", { outcome })`. Usar `?.` en cada llamada para que no truene si gtag/fbq no están cargados (env var ausente o sin consentimiento).</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - "tracking.ts exporta trackWhatsAppClick y trackFormSubmit"
    - "Ambas funciones usan window.gtag?. y window.fbq?. (optional chaining, no truena sin analytics cargado)"
  </acceptance_criteria>
  <done>Ambos eventos definidos y tipados con CtaLocation</done>
</task>

<task type="auto">
  <name>Task 2: Instrumentar todos los CTAs de WhatsApp</name>
  <files>src/components/ui/whatsapp-cta.tsx, src/components/layout/whatsapp-float-button.tsx</files>
  <read_first>src/lib/tracking.ts, src/lib/site-config.ts</read_first>
  <action>`WhatsAppCta` recibe `location: CtaLocation` como prop obligatoria y llama `trackWhatsAppClick(location)` en `onClick`, antes de que el navegador siga el link `wa.me`. Cada page.tsx que ya usa `WhatsAppCta` (header, home hero, servicios, testimonios, FAQ, sobre-el-doctor, footer, contacto, blog) le pasa su propio `location` de `CtaLocation` (`header`, `hero`, `services`, `footer`, `contact_page`). `WhatsAppFloatButton` llama `trackWhatsAppClick("floating_button")` en su propio `onClick`.</action>
  <verify>Verificado visualmente en navegador — clic en cada ubicación dispara el evento (Network tab / GA4 DebugView cuando hay env var)</verify>
  <acceptance_criteria>
    - "whatsapp-cta.tsx tiene onClick={() => trackWhatsAppClick(location)}"
    - "whatsapp-float-button.tsx tiene onClick={() => trackWhatsAppClick(\"floating_button\")}"
  </acceptance_criteria>
  <done>Los 6 valores de CtaLocation están cubiertos por al menos un CTA real en el sitio</done>
</task>

<task type="auto">
  <name>Task 3: AnalyticsScripts con doble gate (env var + consentimiento)</name>
  <files>src/components/analytics/analytics-scripts.tsx, src/components/analytics/consent.ts</files>
  <read_first>src/app/layout.tsx</read_first>
  <action>`consent.ts`: store minimalista sobre `localStorage` (`getStoredConsent`, `setStoredConsent`, `subscribeConsent`, `getConsentServerSnapshot` para SSR) pensado para `useSyncExternalStore`. `analytics-scripts.tsx` (client component): lee `NEXT_PUBLIC_GA_ID`/`NEXT_PUBLIC_META_PIXEL_ID` a nivel de módulo; con `useSyncExternalStore` obtiene el consentimiento actual; si `consent !== "granted"` retorna `null` de inmediato (no monta nada); si hay consentimiento, monta `<GoogleAnalytics gaId={GA_ID} />` solo si `GA_ID` existe, y el snippet de Meta Pixel (`next/script`, `strategy="afterInteractive"`) solo si `META_PIXEL_ID` existe. Se monta una vez en `layout.tsx`.</action>
  <verify>npm run build — confirmar que sin NEXT_PUBLIC_GA_ID/NEXT_PUBLIC_META_PIXEL_ID en el entorno, AnalyticsScripts no inyecta ningún script</verify>
  <acceptance_criteria>
    - "analytics-scripts.tsx retorna null cuando consent !== 'granted', antes de evaluar GA_ID/META_PIXEL_ID"
    - "GoogleAnalytics y el Script de Meta Pixel están cada uno detrás de su propio && con la env var"
  </acceptance_criteria>
  <done>Sin env var configurada en este entorno, ningún script de analítica se monta — confirmado leyendo el código y el build</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npx tsc --noEmit limpio
- [x] npm run build limpio
- [x] Verificado en navegador que cada botón de WhatsApp abre wa.me con el mensaje correcto por ubicación
- [x] Confirmado en el código que AnalyticsScripts no monta scripts sin env var (NEXT_PUBLIC_GA_ID/NEXT_PUBLIC_META_PIXEL_ID no están configuradas todavía en este proyecto — pendiente de que Juan cree las cuentas)
</verification>

<success_criteria>
- Cada botón de WhatsApp del sitio (header, hero, flotante, footer, y el resto de ubicaciones) dispara whatsapp_click con su cta_location
- El formulario de contacto dispara generate_lead/Lead al enviarse
- GA4/Meta Pixel están completamente implementados y gateados — activarlos en producción es solo cuestión de configurar las env vars, sin tocar código
</success_criteria>

---
**Estado:** Completado — ver `03-SUMMARY.md`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
