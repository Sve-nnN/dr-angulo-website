---
phase: 03-conversi-n-tracking-y-seo-t-cnico
plan: 03-01
type: execute
wave: 2
depends_on: ["03-02"]
files_modified: [src/app/contacto/page.tsx, src/components/contact-form.tsx, src/app/actions/contact.ts, src/lib/resend.ts]
autonomous: true
requirements: [CONTENT-05, CONTACT-03, CONTACT-04]
user_setup:
  - service: "Resend"
    why: "CONTACT-04 requiere una API key real y (idealmente) un dominio verificado para enviar email; el código ya está gateado y listo, solo falta la cuenta"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys → Create API Key"
      - name: EMAIL_FROM
        source: "Resend Dashboard → Domains (dominio verificado); mientras tanto el código cae al remitente de pruebas onboarding@resend.dev"
      - name: CONTACT_EMAIL_TO
        source: "Definir con Juan: el correo real donde deben llegar las notificaciones de nuevas consultas"

must_haves:
  truths:
    - "La página /contacto muestra nombre, dirección y mapa embebido de Clínica Montefiori, más el teléfono de WhatsApp"
    - "El formulario rechaza envíos sin nombre, teléfono o motivo con un mensaje de error server-side (Zod)"
    - "Al enviar el formulario con éxito, el usuario ve confirmación y un botón para continuar por WhatsApp con un mensaje prellenado con sus propios datos"
    - "Si RESEND_API_KEY no está configurada, el envío no falla: se omite el email y se ofrece igual el fallback a WhatsApp"
  artifacts:
    - path: "src/app/contacto/page.tsx"
      provides: "Página de Contacto: dirección/mapa embebido de Clínica Montefiori, teléfono, y formulario"
    - path: "src/components/contact-form.tsx"
      provides: "Client component del formulario — useActionState, honeypot anti-spam, pantalla de éxito con CTA a WhatsApp"
    - path: "src/app/actions/contact.ts"
      provides: "Server Action submitContactForm — validación Zod, envío de email condicionado a Resend, arma el mensaje de WhatsApp"
    - path: "src/lib/resend.ts"
      provides: "Cliente Resend gateado por RESEND_API_KEY (null si no está configurada)"
  key_links:
    - from: "src/components/contact-form.tsx"
      to: "src/app/actions/contact.ts"
      via: "useActionState(submitContactForm, initialState) en <form action={formAction}>"
      pattern: "useActionState\\(submitContactForm"
    - from: "src/app/actions/contact.ts"
      to: "src/lib/resend.ts"
      via: "if (resend) { resend.emails.send(...) } — solo intenta enviar si el cliente existe"
      pattern: "if \\(resend\\)"
    - from: "src/components/contact-form.tsx"
      to: "src/lib/tracking.ts"
      via: "trackFormSubmit(\"email\") en useEffect cuando state.status === \"success\""
      pattern: "trackFormSubmit"
---

<objective>
Página de Contacto con dirección/mapa de Clínica Montefiori y un formulario funcional que valida server-side, envía email por Resend cuando está configurado, y siempre ofrece continuar por WhatsApp con los datos ya escritos.

Purpose: dar una segunda vía de conversión (además de WhatsApp directo) para el paciente que prefiere dejar sus datos y que lo llamen, sin perder la conversión si Resend todavía no está configurado.
Output: `/contacto` con mapa + formulario, Server Action con validación y envío condicionado, fallback a WhatsApp garantizado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/site-config.ts
@src/lib/tracking.ts
@src/components/ui/whatsapp-cta.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action + cliente Resend</name>
  <files>src/app/actions/contact.ts, src/lib/resend.ts</files>
  <read_first>src/lib/site-config.ts</read_first>
  <action>Server Action `submitContactForm` con schema Zod (`name` min 2, `phone` min 6, `email` opcional pero válido si se llena, `reason` min 5, `company` honeypot opcional). Arma `whatsappMessage` con nombre/teléfono/motivo. Si el honeypot viene lleno, responde éxito sin enviar nada. `src/lib/resend.ts` exporta `resend` (instancia de `Resend` o `null` si falta `RESEND_API_KEY`) y `EMAIL_FROM`. Si `resend` existe, envía dos correos (notificación a `siteConfig.email` + auto-respuesta al paciente si dejó email) dentro de un `try/catch` que nunca revienta la Server Action.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - "contact.ts usa z.object con name/phone/email/reason/company"
    - "resend.ts exporta resend como null cuando no hay RESEND_API_KEY"
    - "submitContactForm siempre retorna status: success cuando la validación pasa, incluso sin Resend configurado"
  </acceptance_criteria>
  <done>Server Action valida, intenta el email si corresponde, y nunca bloquea el flujo de éxito</done>
</task>

<task type="auto">
  <name>Task 2: ContactForm (client component)</name>
  <files>src/components/contact-form.tsx</files>
  <read_first>src/app/actions/contact.ts</read_first>
  <action>Formulario client component con `useActionState(submitContactForm, initialState)` y `useFormStatus` para el botón de envío. Honeypot oculto (`name="company"`, `tabIndex={-1}`, posicionado fuera de pantalla, no `display:none`). Al llegar a `status === "success"`, reemplaza el formulario por una pantalla de confirmación con un link `wa.me` armado con `state.whatsappMessage`, y dispara `trackFormSubmit("email")` una sola vez vía `useEffect` + `useRef` (evita doble tracking en re-renders).</action>
  <verify>Verificado visualmente en navegador — envío de prueba end-to-end</verify>
  <acceptance_criteria>
    - "contact-form.tsx importa trackFormSubmit de @/lib/tracking"
    - "El campo honeypot tiene aria-hidden y está posicionado fuera de pantalla, no display:none"
  </acceptance_criteria>
  <done>Formulario envía, muestra error server-side si falta un campo, y pantalla de éxito con WhatsApp si todo va bien</done>
</task>

<task type="auto">
  <name>Task 3: Página /contacto</name>
  <files>src/app/contacto/page.tsx</files>
  <read_first>src/lib/site-config.ts, src/components/contact-form.tsx</read_first>
  <action>Página con metadata propia (title/description/canonical), dirección y mapa embebido (`iframe` de Google Maps con query armado desde `siteConfig.clinic`), teléfono, horario referencial, botón `WhatsAppCta` (`location="contact_page"`) y `<ContactForm />` en un grid de dos columnas.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - "contacto/page.tsx exporta metadata con alternates.canonical: '/contacto'"
    - "contacto/page.tsx renderiza un iframe de Google Maps con la dirección de siteConfig.clinic"
  </acceptance_criteria>
  <done>/contacto muestra mapa, dirección y formulario funcional</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npm run build limpio
- [x] npx tsc --noEmit limpio
- [x] Formulario probado end-to-end en navegador (envío real, pantalla de éxito, link de WhatsApp con datos prellenados)
</verification>

<success_criteria>
- Página de Contacto muestra dirección/mapa de Clínica Montefiori y formulario funcional
- El formulario valida server-side y nunca deja al paciente sin salida (Resend si está configurado, WhatsApp siempre)
</success_criteria>

---
**Estado:** Completado — ver `03-SUMMARY.md`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
