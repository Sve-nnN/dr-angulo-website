---
phase: 13-clusters-competencia-y-las-10-de-oro
workstream: seo-keywords
plan: 05
type: execute
wave: 4
depends_on: ["13-04"]
autonomous: false
requirements: [KWR-06]
files_modified:
  - seo-tools/src/phase13/golden.ts
  - seo-tools/src/phase13/golden.test.ts
  - seo-tools/src/phase13/golden-run.ts
  - seo-tools/data/golden-10.json
  - .planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-DIEZ-DE-ORO.md

must_haves:
  truths:
    - "Existen diez keywords elegidas por valor de negocio primero y alcanzabilidad después (KWR-06, D-10)."
    - "Juan puede leer por qué esas diez y no simplemente las diez de mayor volumen."
    - "Juan aprobó la lista antes de que se convierta en el criterio de desempate de la fase 14."
  artifacts:
    - seo-tools/data/golden-10.json
    - .planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-DIEZ-DE-ORO.md
  key_links:
    - "Las 10 de Oro son la entrada del criterio de priorización de la fase 14: cuando dos URLs se peleen la misma keyword, gana la que sirve a una keyword de oro. Una lista sin aprobar arrastra ese sesgo a todo el mapa."
---

<objective>
El cierre de la fase: de las 4.766 keywords del universo objetivo salen diez por las que vale la
pena pelear primero, con su justificación escrita, y Juan las aprueba antes de que se conviertan en
el criterio de desempate del mapa de la fase 14.

Propósito: una lista de diez sin justificación es una opinión disfrazada de entregable. Una
condición que el doctor efectivamente opera, con SERP ganable, vale más que una keyword genérica de
volumen alto, y eso hay que poder leerlo, no deducirlo.

Salida: la lista, el documento que la explica y la aprobación.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-PUNTO-DULCE.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-CLUSTERS.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-COMPETIDORES.md
@.planning/PROJECT.md
@.planning/workstreams/seo-keywords/ROADMAP.md
</context>

<constraints>
Rigen las mismas reglas duras del plan 13-01: nada bajo `src/` de la raíz, nada en los archivos de
planificación de la raíz ni del workstream `milestone`, `seo-tools/src/cli.ts` cerrado, `git add`
por archivo nombrado y globs entre comillas.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Elegir las diez y escribir por qué</name>

  <read_first>
    - `seo-tools/data/sweet-spot.jsonl` — la alcanzabilidad de cada cabeza con sus razones en texto,
      del plan 13-04.
    - `seo-tools/data/clusters.json` — los clusters con su tipo de página dominante.
    - `seo-tools/data/keyword-gap.json` — dónde posicionan los competidores y el doctor no.
    - `seo-tools/data/ahrefs-keywords.jsonl` — KD, traffic potential y el volumen de Ahrefs.
    - `.planning/PROJECT.md` — qué opera realmente el doctor: traumatología, cirugía de columna,
      ortopedia infantil, y las cuatro sedes de Lima.
    - `.planning/workstreams/seo-keywords/ROADMAP.md`, fase 14 — las 18 URLs mapeables y las nueve
      que v1.1 todavía no escribió. Una keyword de oro sin URL que la pueda ganar es una decisión
      para MAP-04, no un error de esta lista.
  </read_first>

  <files>
    seo-tools/src/phase13/golden.ts,
    seo-tools/src/phase13/golden.test.ts,
    seo-tools/src/phase13/golden-run.ts,
    seo-tools/data/golden-10.json,
    .planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-DIEZ-DE-ORO.md
  </files>

  <behavior>
    - El valor de negocio se evalúa antes que la alcanzabilidad, no al revés. Una keyword de valor
      alto y alcanzabilidad media entra antes que una de valor bajo y alcanzabilidad alta.
    - Ninguna keyword de marca ajena entra en la lista, sin importar su volumen.
    - Ninguna keyword de alcance distinto de objetivo entra en la lista.
    - Una condición que el doctor opera y que no tiene volumen medido puede entrar: el hueco es de
      la fuente, no del mercado, y está documentado.
    - Cada una de las diez trae su justificación en texto, y esa justificación nombra el dato
      concreto que la sostiene: su cluster, su tipo de página, su KD, quién ocupa su top 10 y si hay
      competidor posicionando ahí.
    - El documento incluye las keywords de mayor volumen que **no** entraron, con la razón de cada
      exclusión. Sin esa sección, la lista no se puede discutir.
    - La selección es determinista y reproducible: dos corridas producen la misma lista en el mismo
      orden.
  </behavior>

  <action>
    Construir `seo-tools/src/phase13/golden.ts`, que combina lo que produjeron los planes anteriores
    y ordena las cabezas por dos ejes en el orden que fija la decisión (D-10): **primero valor de
    negocio, después alcanzabilidad**.

    El valor de negocio se apoya en lo que el consultorio efectivamente atiende y en lo que el sitio
    va a tener: las cuatro condiciones núcleo que v1.1 ya publicó, las cuatro sedes, la especialidad
    diferencial de ortopedia infantil —que ninguno de los tres competidores de v1.0 cubre— y los
    procedimientos que el doctor realiza. La alcanzabilidad ya viene calculada del plan 13-04, con
    el KD cruzado contra la ocupación real del top 10.

    La forma concreta de ordenar y de presentar queda a criterio propio; es lo que el CONTEXT delega.
    Lo que no queda a criterio es que se pueda leer. `13-DIEZ-DE-ORO.md` tiene que traer, en este
    orden:

    1. **Las diez, una por bloque**, con: la keyword, su cluster, su tipo de página, su intención y
       etapa, sus métricas de las dos fuentes con la fuente marcada, quién ocupa hoy su top 10 y qué
       URL del sitio —existente o planificada— es la candidata a ganarla.
    2. **La justificación de cada una en prosa**, nombrando el dato que la sostiene. No "alto valor
       comercial", sino qué se opera, quién está adelante y por qué se le puede ganar.
    3. **Las que no entraron y por qué.** Las de mayor volumen del universo primero. Las dos de
       marca ajena van acá con su motivo explícito. Las de codificación clínica —`artrosis cie 10`
       con 2.400 y `cie-10 hernia discal` con 1.600, que encabezan cualquier orden por volumen—
       también, porque son búsquedas de personal administrativo y no de pacientes.
    4. **Las advertencias que la lista arrastra:** que el universo de 5.716 es una muestra truncada
       y no un censo; que 90 de las 114 búsquedas disponibles se gastaron midiendo la SERP y que hay
       cabezas sin medir; y que Ahrefs y DinoRank discrepan fuerte en volumen para las condiciones
       núcleo, con las dos cifras a la vista.

    Escribir `data/golden-10.json` con la misma información en forma estructurada, para que la fase
    14 la consuma sin parsear prosa.

    El documento va humanizado: es para que lo lea Juan, no para que lo procese una máquina.
  </action>

  <verify>
    <automated>
cd seo-tools
npm run typecheck
env -u DINORANK_API_KEY -u SERPAPI_API_KEY npm test
./node_modules/.bin/tsx src/phase13/golden-run.ts --out /tmp/g1.json
./node_modules/.bin/tsx src/phase13/golden-run.ts --out /tmp/g2.json
test "$(shasum -a 256 /tmp/g1.json | cut -d' ' -f1)" = "$(shasum -a 256 /tmp/g2.json | cut -d' ' -f1)"
node -e "
const fs=require('fs');
const g=JSON.parse(fs.readFileSync('data/golden-10.json','utf8'));
if(g.keywords.length!==10) throw new Error('no son diez: '+g.keywords.length);
const ks=fs.readFileSync('data/keywords.jsonl','utf8').trim().split('\n').map(l=>JSON.parse(l));
const porClave=Object.fromEntries(ks.map(o=>[o.keywordKey,o]));
for(const k of g.keywords){
  const base=porClave[k.keywordKey];
  if(!base) throw new Error('no está en el universo: '+k.keywordKey);
  if(base.alcance!=='objetivo') throw new Error('fuera de alcance: '+k.keywordKey+' ('+base.motivoAlcance+')');
  if(!k.justificacion || k.justificacion.length<80) throw new Error('justificación insuficiente: '+k.keywordKey);
  if(!k.cluster) throw new Error('sin cluster: '+k.keywordKey);
  if(!k.tipoDePagina) throw new Error('sin tipo de página: '+k.keywordKey);
}
if(!Array.isArray(g.descartadasDeMayorVolumen) || g.descartadasDeMayorVolumen.length<5) throw new Error('faltan las descartadas de mayor volumen');
for(const d of g.descartadasDeMayorVolumen){ if(!d.motivo) throw new Error('descartada sin motivo: '+d.keyword); }
console.log('las diez:'); g.keywords.forEach((k,i)=>console.log('  '+(i+1)+'.', k.keyword, '['+k.cluster+']'));
"
DOC=../.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-DIEZ-DE-ORO.md
test -f "$DOC"
test "$(grep -c 'muestra truncada' "$DOC")" -ge 1
test "$(grep -ciE 'no entraron|descartadas|por qué no' "$DOC")" -ge 1
    </automated>
  </verify>

  <acceptance_criteria>
    - `data/golden-10.json` tiene exactamente diez keywords, todas de alcance objetivo y todas
      presentes en el universo.
    - Cada una trae cluster, tipo de página y una justificación de al menos ochenta caracteres que
      nombra el dato que la sostiene.
    - El bloque de descartadas de mayor volumen tiene al menos cinco entradas, cada una con su
      motivo, e incluye las dos de marca ajena y las dos de codificación clínica.
    - Dos corridas producen el mismo archivo con el mismo orden.
    - `13-DIEZ-DE-ORO.md` existe, tiene las cuatro secciones y declara que el universo es una
      muestra truncada.
    - Hay una prueba nombrada por cada comportamiento del bloque `<behavior>`, incluidas la que
      afirma que una keyword de marca ajena con volumen alto nunca entra y la que afirma que una
      condición sin volumen medido sí puede entrar.
  </acceptance_criteria>

  <done>
    Existen las diez con su justificación legible y con la lista de las que se quedaron afuera y por
    qué.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 2: Juan aprueba las 10 de Oro antes de que la fase 14 las use</name>

  <what-built>
    La fase 13 completa. En concreto, lo que hay para revisar:

    - **Las 10 de Oro** con su justificación, en
      `.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-DIEZ-DE-ORO.md`.
    - **Los clusters**, en `13-CLUSTERS.md`, con el tipo de página que Google premia en cada uno y
      cuántas keywords de cada cluster están validadas contra la SERP y cuántas se infirieron por
      texto.
    - **Los cinco competidores**, en `13-COMPETIDORES.md`, con DR, referring domains, tráfico
      orgánico, keywords en top 100, blog y páginas más enlazadas, más la línea de base del propio
      dominio.
    - **El punto dulce**, en `13-PUNTO-DULCE.md`, y sobre todo su sección de keywords de volumen
      alto que quedaron fuera de alcance.
    - **El Sheet**: el tab `Keyword Research` con `Cluster`, `Top Result` y `Keyword Difficulty`
      llenos donde hay dato, y el tab `Competitor Analysis` con los cinco competidores escrito por
      primera vez.
  </what-built>

  <how-to-verify>
    1. Abrir `13-DIEZ-DE-ORO.md` y leer las diez con su justificación. La pregunta que hay que poder
       contestar es la del CONTEXT: **por qué esas diez y no las diez de mayor volumen.** Si alguna
       justificación no lo contesta, decirlo.
    2. Leer la sección de las que no entraron. Comprobar que ninguna de esas exclusiones sorprende,
       en particular las dos de marca de competidores y las dos de codificación clínica.
    3. Contrastar contra la práctica real: ¿las diez cubren lo que el doctor efectivamente opera?
       ¿Falta alguna condición o algún procedimiento que importe y no esté? ¿Sobra alguna?
    4. Abrir el Sheet (`Dr Juan Carlos Angulo - SEO Master`):
       - tab `Keyword Research`: la columna `Cluster` está llena, `Top Result` tiene URL solo en las
         cabezas, `Keyword Difficulty` muestra número en las cabezas y el literal en el resto, y las
         columnas `URL` y `Suggested H1` siguen vacías porque son de las fases 14 y 15;
       - tab `Competitor Analysis`: los cinco competidores en columnas, con sus métricas alineadas a
         las etiquetas, y sin rastro del competidor de ejemplo de la plantilla.
    5. Revisar en `13-PUNTO-DULCE.md` la comparación de volumen entre Ahrefs y DinoRank para las
       cuatro condiciones núcleo. Las dos cifras están publicadas a propósito y no se promedian:
       confirmar que eso es lo que se quiere entregar al cliente.

    **Lo que esta aprobación decide:** las 10 de Oro son el criterio de desempate de la fase 14.
    Cuando dos URLs se peleen la misma keyword, gana la que sirve a una keyword de oro. Aprobar acá
    es aprobar ese sesgo para todo el mapa de keyword a URL.
  </how-to-verify>

  <resume-signal>
    Escribir "aprobado" para cerrar la fase, o describir qué cambiar de la lista: qué keyword sacar,
    cuál poner en su lugar y por qué. Si el cambio es de criterio y no de una keyword suelta,
    decirlo así y se recalcula la lista entera.
  </resume-signal>

  <verify>
    <automated>
test -f "/Users/juan/Documents/Codigo/Clientes/Juan Angulo/website/.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-DIEZ-DE-ORO.md"
test -f "/Users/juan/Documents/Codigo/Clientes/Juan Angulo/website/.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-CLUSTERS.md"
test -f "/Users/juan/Documents/Codigo/Clientes/Juan Angulo/website/.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-COMPETIDORES.md"
test -f "/Users/juan/Documents/Codigo/Clientes/Juan Angulo/website/.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-PUNTO-DULCE.md"
    </automated>
  </verify>

  <acceptance_criteria>
    - Los cuatro entregables legibles de la fase existen antes de pedir la revisión.
    - La respuesta de Juan queda registrada literal en el SUMMARY, con su fecha, igual que se
      registraron las decisiones J-1 a J-6 de la fase 12.
    - Si Juan pide cambios, se aplican y se vuelve a pedir la aprobación: la fase no cierra con la
      lista sin aprobar.
  </acceptance_criteria>

  <done>
    Juan aprobó las 10 de Oro, o pidió cambios y la lista corregida quedó aprobada. La fase 14 puede
    arrancar con un criterio de priorización que no se inventó sola.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datos de las fases anteriores → decisión de negocio | Una lista de diez que sesga todo el mapa de la fase 14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-40 | Repudiation | las 10 de Oro | high | mitigate | Cada keyword trae su justificación con el dato que la sostiene, y el documento incluye las descartadas con su motivo. Checkpoint bloqueante antes de que la lista se use |
| T-13-41 | Tampering | criterio de la fase 14 | high | mitigate | La aprobación de Juan queda registrada literal en el SUMMARY con su fecha |
| T-13-42 | Information disclosure | entregables legibles | low | accept | Los cuatro documentos viven dentro del workstream y contienen datos públicos de competencia más métricas propias |
| T-13-SC | Tampering | instalación de paquetes | high | accept | Esta fase no instala paquetes. Criterio: área de preparación vacía sobre `package.json` y `package-lock.json` |
</threat_model>

<verification>
1. `cd seo-tools && npm run typecheck && env -u DINORANK_API_KEY -u SERPAPI_API_KEY npm test` en verde.
2. Los cuatro entregables legibles de la fase existen y están commiteados.
3. `.cache/_quota.json`: `sources.serpapi.calls` en 102 o menos. La reserva de 24 búsquedas sigue
   disponible al cerrar la fase.
4. `data/keywords.jsonl` conserva el SHA-256 con que lo dejó la fase 12.
5. `test "$(git diff --cached --name-only -- src/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/workstreams/milestone/ .secrets seo-tools/src/cli.ts | wc -l | tr -d ' ')" -eq 0`
   antes de cada commit.
6. La respuesta de Juan al checkpoint está transcrita en el SUMMARY.
</verification>

<success_criteria>
- Existen las 10 de Oro con justificación legible y con las descartadas explicadas.
- Juan las aprobó, o pidió cambios y la versión corregida quedó aprobada.
- Los nueve requisitos de la fase 13 quedan cerrados: KWR-04 a KWR-06, COMP-01 a COMP-04, SHEET-01
  y SHEET-03.
</success_criteria>

<artifacts>
## Artefactos que produce este plan

| Archivo | Qué es | Lo consume |
|---|---|---|
| `seo-tools/data/golden-10.json` | Las diez en forma estructurada | Fase 14 (criterio de desempate de MAP-01 y MAP-02) |
| `13-DIEZ-DE-ORO.md` | El entregable legible de KWR-06 | Juan, fase 14, fase 15 |
</artifacts>

<handoff>
## Lo que la fase 14 se lleva

- **`data/golden-10.json`** es el criterio de desempate de MAP-01 y MAP-02: cuando dos URLs se
  peleen la misma keyword, gana la que sirve a una keyword de oro.
- **`data/keywords-13.jsonl`** trae cluster, procedencia del cluster, top result, KD y traffic
  potential por keyword. Es el dataset de entrada del mapa.
- **`data/clusters.json`** trae el tipo de página que la SERP premia por cluster, que es
  directamente lo que MAP-04 tiene que declarar por URL.
- **`data/keyword-gap.json`** dice dónde posicionan los competidores y el doctor no, con la
  cobertura de la medición declarada.
- **Quedan 24 búsquedas de SerpApi de reserva** hasta el reset del 2026-08-21, y capturas sin hacer
  para el resto del universo. Si la fase 14 necesita una SERP puntual, ahí está el margen.
- **Sigue pendiente, y no depende de este workstream:** dar de alta `drangulocolumna.com` como
  proyecto en el panel de DinoRank y conectarle Search Console. Es la condición de MAP-02.
</handoff>

<output>
Crear `.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-05-SUMMARY.md`
al terminar, con las diez keywords listadas, la respuesta literal de Juan al checkpoint con su fecha
y el balance final de cuota de las tres fuentes.
</output>
