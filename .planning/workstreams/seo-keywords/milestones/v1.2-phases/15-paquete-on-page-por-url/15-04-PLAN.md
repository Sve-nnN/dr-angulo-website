---
phase: 15-paquete-on-page-por-url
workstream: seo-keywords
plan: 04
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - seo-tools/data/copy-servicios.json
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/home.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-ortopedia-infantil.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-cirugia-minimamente-invasiva.md
autonomous: true
requirements: [ONPAGE-03, ONPAGE-04, ONPAGE-06]
must_haves:
  truths:
    - "La home, el hub de servicios y las dos páginas de servicio transaccionales están redactadas de punta a punta."
    - "La home y el hub no se pisan: cada una pelea la consulta que el mapa le asignó y el texto lo sostiene."
    - "Cada bloque médico está marcado como pendiente de aprobación del doctor."
    - "Las entidades obligatorias derivadas del top 10 están usadas en el texto de cada página."
    - "El copy deja lugar a los enlaces que la matriz de la fase 14 propuso, sin escribirlos: los enlaces los implementa v1.1."
  artifacts:
    - seo-tools/data/copy-servicios.json
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/home.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios.md
  key_links:
    - "El copy se escribe en `data/copy-servicios.json` y el Markdown se genera desde ahí (D-12)."
    - "`ymyl.ts` del plan 15-02 es la compuerta; este plan no define criterios propios."
    - "El title, la meta y el H1 vienen de `data/onpage.json` y no se reescriben acá."
---

<objective>
Redactar las cuatro páginas transaccionales de la fase: la home, el hub de servicios y las dos
páginas de servicio cuya SERP sí premia formato comercial.

Propósito: son las URLs por las que entra el paciente que ya decidió consultar. La medición de la
fase 13 dice que los cinco competidores rankean sin enlaces en sus páginas interiores, así que acá
la palanca es el contenido y no el perfil de enlaces, que este dominio no tiene.

Salida: `data/copy-servicios.json` completo y cuatro documentos en `paquetes/`.

Decisiones que gobiernan este plan: D-05, D-08, D-09, D-10, D-11, D-12, D-14, D-16. El registro
completo está en el `<objective>` de `15-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-01-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-02-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-ENLAZADO.md
@seo-tools/data/onpage-serp.json
@seo-tools/data/onpage.json
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 15-01.
**Presupuesto de SerpApi de este plan: cero.** `url-map.jsonl` sigue de solo lectura y `src/` de
la aplicación no se toca.

**Alcance exacto de este plan, cuatro URLs:**

| URL | Keyword primaria | Formato | Mínimo | Acción |
|---|---|---|---|---|
| `/` | traumatología lima | página de servicio | 600 | Reescribir |
| `/servicios` | cirujano de columna lima | página de servicio | 700 | Reescribir |
| `/servicios/ortopedia-infantil` | ortopedia infantil lima | página de servicio | 700 | Reescribir |
| `/servicios/cirugia-minimamente-invasiva` | cirugía mínimamente invasiva en lima | página de servicio | 700 | Crear |

**Hechos medidos, ya verificados, no hay que redescubrirlos:**

- La home y el hub **no se pisan, y está medido**: `traumatología lima` y `cirujano de columna
  lima` comparten CERO URLs en su top 10. Son dos páginas distintas medidas de a pares, aunque el
  cluster de 41 cabezas las junte por transitividad. El texto tiene que sostener esa separación:
  la home se queda con las variantes de traumatología y el hub con las de columna.
- `ortopedia infantil lima` es la única de las cuatro páginas de servicio publicadas que ya está en
  el formato que la SERP premia —5 de 9 posiciones son página de servicio— y es justo la que la
  fase 13 marcó como la más ganable. Acá no hay que cambiar de formato, hay que competir mejor.
- `/servicios/cirugia-minimamente-invasiva` no existe todavía. Sale de una keyword de oro que
  ninguna URL existente podía ganar. La ruta, el layout y el sitemap son de v1.1; el copy es de
  este plan.
- `/sedes` quedó como hub de navegación sin keyword primaria, por decisión de Juan del 2026-08-11,
  para no canibalizar a las cuatro sedes. El copy de estas cuatro páginas no le devuelve un rol de
  posicionamiento por la puerta de atrás.
- Las cuatro sedes vigentes son consultorio Surco, Ricardo Palma, Sanna La Molina y Padre Luis
  Tezza. Montefiori no existe en este sitio (D-11).
</context>

<tasks>

<task type="auto">
  <name>Task 1: La home y el hub de servicios, separados por el dato</name>
  <files>seo-tools/data/copy-servicios.json</files>
  <read_first>
    - `seo-tools/data/onpage-serp.json` y `data/onpage.json`, filas `/` y `/servicios`.
    - `seo-tools/data/url-map.jsonl`, las mismas dos filas: la `justificacion` de la home explica
      con el dato por qué se queda con traumatología y no con columna.
    - `src/app/page.tsx` y `src/content/services.ts`, en solo lectura: lo que hay publicado hoy.
    - `.../paquetes/servicios-hernia-discal.md`: el molde aprobado por Juan.
    - `.../14-ENLAZADO.md`, las filas de origen `/` y `/servicios`: qué enlaces salientes se
      propusieron, para que el copy tenga dónde alojarlos.
  </read_first>
  <behavior>
    - La home cubre las cuatro preguntas de la SERP de su keyword como secciones propias.
    - El hub cubre las suyas.
    - Ninguna sección de la home usa como título una keyword secundaria del hub, ni al revés.
    - Al menos el 80 % de las entidades obligatorias de cada URL aparecen en su texto.
    - Cada secundaria del mapa queda cubierta por alguna sección de su URL.
    - Las sedes que se nombran son las cuatro vigentes.
    - Cada sección clínica va marcada como pendiente de aprobación del doctor.
    - Las dos páginas pasan la compuerta de humanización sin hallazgos.
  </behavior>
  <action>
    Redactar las dos páginas dentro de `data/copy-servicios.json`.

    **`/`**, mínimo 600 palabras. Es la puerta del sitio y su keyword es la de especialidad entera.
    Su trabajo es decir qué hace el doctor, para quién, dónde atiende y cómo se agenda, y repartir
    hacia las guías y hacia las sedes. Lo que no puede hacer es explicar las condiciones en
    profundidad: eso es de las guías y duplicarlo las pone a competir.

    **`/servicios`**, mínimo 700 palabras. Es el hub reformulado y pelea la consulta de cirujano de
    columna. Su trabajo es presentar los servicios de columna con el criterio con el que un
    paciente elige, y mandar a cada guía. Igual que la home, no explica cada condición: la nombra
    y deriva.

    La separación entre las dos no es una preferencia de redacción, es lo que el dato ya midió. Se
    midieron par a par y comparten cero URLs en su top 10. Si el texto de la home se llena de
    columna y el del hub de traumatología general, esa separación se pierde donde único importa,
    que es en la página. Al escribir, mantener el reparto: traumatología y ortopedia general en la
    home, columna en el hub.

    Los enlaces que la matriz de la fase 14 propuso no se escriben acá, se alojan: el copy deja la
    frase donde el enlace va a entrar y el paquete registra cuál era, con su anchor. Los enlaces
    los implementa v1.1 en el código, y este workstream no toca `src/`.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-servicios.json --url / && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-servicios.json --url /servicios</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'),s=require('./data/onpage-serp.json'); const mal=['/','/servicios'].filter(u=>{const p=(d.paginas||d).find(x=>x.url===u); const min=s.urls.find(x=>x.url===u).minimoDePalabras; return p.secciones.flatMap(x=>x.parrafos).join(' ').split(/\s+/).filter(Boolean).length<min;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'),s=require('./data/onpage-serp.json'); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const mal=['/','/servicios'].filter(u=>{const p=(d.paginas||d).find(x=>x.url===u); const e=s.urls.find(x=>x.url===u).entidades; const t=n(p.secciones.flatMap(x=>x.parrafos.concat([x.titulo])).join(' ')); return 100*e.filter(x=>t.includes(n(x.termino))).length/e.length<80;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'); const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const sec=u=>new Set((m.find(r=>r.url===u).secundarias||[]).map(n)); const home=(d.paginas||d).find(x=>x.url==='/'), hub=(d.paginas||d).find(x=>x.url==='/servicios'); const cruz=home.secciones.map(x=>n(x.titulo)).filter(t=>[...sec('/servicios')].some(k=>t.includes(k))).length + hub.secciones.map(x=>n(x.titulo)).filter(t=>[...sec('/')].some(k=>t.includes(k))).length; process.stdout.write(String(cruz))")" -eq 0`
      — ninguna de las dos se lleva por título una secundaria de la otra: la separación medida se
      sostiene en la página y no solo en el mapa.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'); const t=(d.paginas||d).filter(p=>['/','/servicios'].includes(p.url)).flatMap(p=>p.secciones.flatMap(s=>s.parrafos)).join(' '); process.stdout.write(String(/montefiori/i.test(t)?1:0))")" -eq 0`
    - Las dos compuertas devuelven cero hallazgos y salen con estado 0.
    - `test "$(shasum -a 256 seo-tools/data/url-map.jsonl | cut -d' ' -f1)" = "ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5"`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    La home y el hub están redactados dentro de contrato, cada uno con su consulta, y el texto
    sostiene la separación que el solape par a par ya había medido.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ortopedia infantil y cirugía mínimamente invasiva</name>
  <files>seo-tools/data/copy-servicios.json</files>
  <read_first>
    - `seo-tools/data/onpage-serp.json` y `data/onpage.json`, filas
      `/servicios/ortopedia-infantil` y `/servicios/cirugia-minimamente-invasiva`.
    - `src/content/service-pages.ts`, entrada `ortopedia-infantil` desde la línea 639, en solo
      lectura: es lo que hay publicado y lo que esta página mejora.
    - `seo-tools/data/golden-10.json`, la entrada de cirugía mínimamente invasiva: de ahí sale por
      qué esta URL existe.
  </read_first>
  <behavior>
    - Ortopedia infantil habla a quien decide por el paciente, que es el padre o la madre, y no al
      paciente pediátrico.
    - Cirugía mínimamente invasiva explica qué es el procedimiento y en qué se diferencia de la
      cirugía abierta, sin prometer tiempos de recuperación ni resultados que nadie verificó.
    - Al menos el 80 % de las entidades obligatorias de cada URL aparecen en su texto.
    - Cada secundaria del mapa queda cubierta por alguna sección de su URL.
    - Cada sección clínica va marcada como pendiente de aprobación del doctor.
    - Las dos páginas pasan la compuerta de humanización sin hallazgos.
  </behavior>
  <action>
    Redactar las dos páginas dentro de `data/copy-servicios.json`, mínimo 700 palabras cada una.

    **`/servicios/ortopedia-infantil`** es la única de las cuatro páginas de servicio publicadas
    que ya enfrenta la SERP correcta: 5 de 9 posiciones de su top 10 son página de servicio. Acá no
    se cambia de formato, se compite mejor. La pregunta que hay que responder mejor que los cinco
    competidores es la de un padre que no sabe si lo que ve en su hijo necesita consulta.

    **`/servicios/cirugia-minimamente-invasiva`** se crea desde cero. Es un procedimiento, no una
    condición, así que el orden interno cambia: qué es, para qué condiciones se usa, cómo se decide
    si un paciente es candidato, cómo es el postoperatorio en términos generales y cómo se agenda
    una evaluación.

    El riesgo propio de esta página es el que la restricción del proyecto nombra con todas las
    letras: es la página donde más tienta escribir cuántos días de recuperación, qué porcentaje de
    éxito o cuántas se llevan hechas. Nada de eso está verificado y nada de eso se escribe (D-10).
    Lo que sí se puede decir es qué se evalúa en consulta para decidir, y eso es más útil para el
    paciente que una cifra que nadie respalda.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-servicios.json --url /servicios/ortopedia-infantil && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-servicios.json --url /servicios/cirugia-minimamente-invasiva</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'),s=require('./data/onpage-serp.json'); const u=['/servicios/ortopedia-infantil','/servicios/cirugia-minimamente-invasiva']; const mal=u.filter(x=>{const p=(d.paginas||d).find(y=>y.url===x); const min=s.urls.find(y=>y.url===x).minimoDePalabras; return p.secciones.flatMap(y=>y.parrafos).join(' ').split(/\s+/).filter(Boolean).length<min;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'),s=require('./data/onpage-serp.json'); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const u=['/servicios/ortopedia-infantil','/servicios/cirugia-minimamente-invasiva']; const mal=u.filter(x=>{const p=(d.paginas||d).find(y=>y.url===x); const e=s.urls.find(y=>y.url===x).entidades; const t=n(p.secciones.flatMap(y=>y.parrafos.concat([y.titulo])).join(' ')); return 100*e.filter(y=>t.includes(n(y.termino))).length/e.length<80;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-servicios.json'); const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); let mal=0; for(const x of ['/servicios/ortopedia-infantil','/servicios/cirugia-minimamente-invasiva']){const p=(d.paginas||d).find(y=>y.url===x); const cub=new Set(p.secciones.flatMap(y=>y.keywordsCubiertas||[])); mal+=(m.find(r=>r.url===x).secundarias||[]).filter(k=>!cub.has(k)).length;} process.stdout.write(String(mal))")" -eq 0`
    - Las dos compuertas devuelven cero hallazgos y salen con estado 0.
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Las dos páginas de servicio están redactadas dentro de contrato, con las entidades usadas y sin
    una sola cifra de recuperación, de éxito ni de volumen que nadie haya verificado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Los cuatro documentos de la familia, generados y verificados</name>
  <files>.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/home.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-ortopedia-infantil.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-cirugia-minimamente-invasiva.md</files>
  <read_first>
    - `seo-tools/src/phase15/paquete.ts` tal como lo dejó el plan 15-02.
    - `.../14-ENLAZADO.md`, las cuatro filas de origen de estas URLs.
  </read_first>
  <action>
    Generar los cuatro documentos con `paquete.ts` y no a mano (D-12), y correr la compuerta sobre
    la familia entera.

    Cada documento lleva, además de lo que el generador ya arma, la lista de enlaces salientes que
    la matriz de la fase 14 propuso para esa URL, con su anchor y su destino, bajo un rótulo que
    diga con todas las letras que **se proponen y los implementa v1.1**. Así quien abra el paquete
    de `/servicios` encuentra ahí el enlazado que le toca y no tiene que ir a buscar
    `14-ENLAZADO.md`, que es justamente lo que D-14 pide.
  </action>
  <verify>
    <automated>cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-servicios.json && ./node_modules/.bin/tsx src/phase15/paquete.ts --data data/copy-servicios.json && ./node_modules/.bin/tsx src/phase15/paquete.ts --data data/copy-servicios.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['home','servicios','servicios-ortopedia-infantil','servicios-cirugia-minimamente-invasiva']; process.stdout.write(String(f.filter(x=>!fs.existsSync(d+x+'.md')).length))")" -eq 0`
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['home','servicios','servicios-ortopedia-infantil','servicios-cirugia-minimamente-invasiva']; let mal=0; for(const x of f){const t=fs.readFileSync(d+x+'.md','utf8'); const c=((t.split('<!-- copy:inicio -->')[1])||'').split('<!-- copy:fin -->')[0]; if((c.match(/[—–“”‘’]/g)||[]).length>0)mal++;} process.stdout.write(String(mal))")" -eq 0`
      — cero rayas largas y cero comillas tipográficas dentro de la región de copy. Fuera de la
      región no se mira: la prosa heredada de la fase 14 no es nuestra.
    - `test "$(node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios.md','utf8'); process.stdout.write(String(/implementa v1\.1/i.test(t)?1:0))")" -eq 1`
      — el bloque de enlaces dice que se propone y no se implementa desde este workstream.
    - `test "$(cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-servicios.json >/dev/null 2>&1 && echo 1 || echo 0)" -eq 1`
    - Determinismo: la segunda corrida del generador deja los cuatro documentos con el mismo
      SHA-256 que la primera.
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
  </acceptance_criteria>
  <done>
    Los cuatro documentos de la familia transaccional existen, traen su enlazado propuesto adentro,
    pasan la compuerta y se regeneran idénticos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copy clínico → paciente | Contenido YMYL. Una promesa de recuperación o de éxito sin verificar es daño real. |
| Home ↔ hub de servicios | Dos URLs con cero solape medido pueden volver a pisarse si el texto mezcla los dos temarios. |
| Copy → matriz de enlazado | El paquete lista los enlaces propuestos; escribirlos sería implementar código ajeno. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-18 | Tampering | Cifras de recuperación o de éxito en cirugía mínimamente invasiva | critical | mitigate | La compuerta `ymyl.ts` prohíbe cifras sobre el propio doctor y exige fuente en toda afirmación con dígito. |
| T-15-19 | Tampering | Canibalización entre la home y el hub | high | mitigate | Criterio que prueba que ninguna se lleva por título una secundaria de la otra. |
| T-15-20 | Tampering | Copy clínico sin sello del doctor | critical | mitigate | La compuerta corre sobre la familia entera y sale con estado distinto de cero si falta un sello. |
| T-15-21 | Elevation of Privilege | Implementar los enlaces internos | high | mitigate | El paquete los lista como propuesta; criterio de staging vacío para `src/`. |
| T-15-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si algún paso lo requiriera, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- SHA-256 de `seo-tools/data/url-map.jsonl` y de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
</verification>

<success_criteria>
- Las cuatro páginas transaccionales están redactadas de punta a punta, dentro de su mínimo y con
  las entidades obligatorias usadas en el texto.
- La separación medida entre la home y el hub se sostiene en la página y no solo en el mapa.
- Los cuatro documentos se abren solos, traen su enlazado propuesto y pasan la compuerta.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 15-04)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Copy de las cuatro páginas transaccionales | `seo-tools/data/copy-servicios.json` | Dato |
| Cuatro documentos de implementación | `.../paquetes/home.md`, `servicios.md`, `servicios-ortopedia-infantil.md`, `servicios-cirugia-minimamente-invasiva.md` | Entregable |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-04-SUMMARY.md` al terminar.
</output>