---
phase: 15-paquete-on-page-por-url
workstream: seo-keywords
plan: 07
type: execute
wave: 4
depends_on: ["15-03", "15-04", "15-05", "15-06"]
files_modified:
  - seo-tools/src/phase15/revision.ts
  - seo-tools/src/phase15/kr-h1-push.ts
  - seo-tools/src/phase15/kr-h1-push.test.ts
  - seo-tools/data/sheet-columns.json
  - seo-tools/data/onpage-audit.json
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/agendar.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/blog.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/contacto.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sobre-el-doctor.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/testimonios.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-PAQUETE.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-REVISION-DOCTOR.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
autonomous: true
requirements: [ONPAGE-02, ONPAGE-05, ONPAGE-06]
must_haves:
  truths:
    - "Las 24 URLs del mapa tienen su documento propio: quien implementa cualquiera abre un archivo y lo sigue de corrido."
    - "La revisión del doctor está agrupada en una sola ronda y ordenada por lo que de verdad necesita su ojo clínico."
    - "La auditoría de duplicados vuelve a correr sobre el paquete completo y sigue dando cero."
    - "Las columnas `Suggested H1` y `URL` del tab `Keyword Research` quedan llenas para las 16 keywords primarias, sin tocar ninguna otra celda del documento del cliente."
    - "La columna `URL`, que la fase 14 reservó y cerró sin llenar, deja de estar huérfana: se cierra acá porque este es el único cargador que vuelve a tocar ese tab."
    - "Existe un handoff autocontenido que las fases 8 y 10 de v1.1 pueden leer solas."
  artifacts:
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-PAQUETE.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-REVISION-DOCTOR.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
    - seo-tools/src/phase15/kr-h1-push.ts
  key_links:
    - "`kr-h1-push.ts` corre con `estadosPropios: [\"fase-15\"]`, así que solo puede escribir la columna de esta fase y ninguna de las fases 12, 13 o 14."
    - "`omitirCamposAusentes` en verdadero y `addMissingColumns` en falso: las 5.700 filas que no son primaria quedan intactas."
    - "El índice y el handoff se generan desde los datasets; ningún dato se transcribe a mano."
---

<objective>
Cerrar la fase y el milestone v1.2 entregando el paquete: los 24 documentos completos, el índice
que los ordena, la ronda única de revisión del doctor, la auditoría corrida sobre el paquete
terminado, la columna `Suggested H1` publicada en el documento del cliente y el handoff
autocontenido hacia las fases 8 y 10 de v1.1.

Propósito: el criterio de terminado de esta fase no es que el copy esté escrito, es que quien lo
implemente no tenga que volver a preguntar. Este plan es el que convierte cinco datasets en algo
que se abre y se sigue.

Salida: `paquetes/` con 24 archivos, `15-PAQUETE.md`, `15-REVISION-DOCTOR.md`, `15-AUDITORIA.md`
actualizado, `15-HANDOFF-V11-ONPAGE.md` y el tab `Keyword Research` con su columna llena.

Decisiones que gobiernan este plan: D-03, D-06, D-07, D-08, D-09, D-12, D-13, D-14. El registro
completo está en el `<objective>` de `15-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-02-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-03-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-04-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-05-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-06-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-HANDOFF-V11.md
@seo-tools/data/sheet-columns.json
@seo-tools/src/phase13/kw-push.ts
@seo-tools/src/sheets/upsert.ts

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 15-01.
**Presupuesto de SerpApi de este plan: cero.** `url-map.jsonl` sigue de solo lectura y `src/` de
la aplicación no se toca.

**Hechos medidos del tab `Keyword Research`, ya verificados, no hay que redescubrirlos:**

- Encabezados en la fila **3**, `mapBy` por **nombre**, `keyField` en `keyword` y `keyHeader` en
  `Suggested Keyword`. La columna `Suggested H1` existe en el modelo con `field: null` y
  `status: "fase-15"`: **esta fase es la que le pone campo**, tal como el plan 13-01 dejó
  declarado.
- El tab tiene **5716 filas** de datos, cargadas por el plan 13-04. Las **16 keywords primarias
  del mapa están todas presentes** en `data/keywords-13.jsonl`, así que las 16 se resuelven como
  actualización y ninguna se inserta.
- `estadosPropios` es un parámetro de `upsertRows` y su valor por defecto NO incluye `fase-15`.
  Pasarle exactamente `["fase-15"]` es lo que impide que esta carga pueda tocar una columna de la
  fase 12, 13 o 14 aunque quedara contigua en el rango.
- `TabSchema.columns` es la fuente de verdad de qué columnas hay; `byHeader` colapsa encabezados
  repetidos y no sirve para decidir qué escribir.
- Las columnas de seguimiento que responde el cliente se siembran una sola vez y después no se
  pisan.

**Alcance del paquete completo, 24 documentos:**

| Grupo | Cuántos | Los produjo |
|---|---|---|
| Guías clínicas y preguntas frecuentes | 4 | 15-03 |
| Documentos de redirección | 2 | 15-03 |
| Transaccionales: home, hub y dos servicios | 4 | 15-04 |
| Fichas de sede | 4 | 15-05 |
| Posts del blog | 4 | 15-06 |
| URLs que solo llevan title y meta | 6 | **este plan** |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Los seis documentos cortos y el índice del paquete</name>
  <files>.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/agendar.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/blog.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/contacto.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sobre-el-doctor.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/testimonios.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-PAQUETE.md</files>
  <read_first>
    - `seo-tools/data/onpage.json`, las seis filas de acción `dejar` con su title, su meta y su H1
      transcrito.
    - `seo-tools/data/url-map.jsonl`, las mismas seis filas: su `motivoSinPrimaria` es lo que el
      documento tiene que explicar.
    - `seo-tools/src/phase15/paquete.ts`, el tipo de documento corto.
    - `.../14-HANDOFF-V11.md`, la sección 4: por qué estas URLs se enlazan con anchor de navegación
      y no de keyword.
  </read_first>
  <behavior>
    - Cada uno de los seis documentos trae title y meta nuevos, el H1 publicado sin cambios, y el
      motivo escrito de por qué esa URL no pelea ninguna keyword.
    - Ninguno de los seis propone cuerpo de texto: proponerlo les inventaría una intención que el
      mapa decidió que no tienen.
    - Cada uno dice que se enlaza con anchor de navegación y cuál es.
    - El índice lista las 24 URLs con su acción, su formato y el archivo que le corresponde.
    - El índice se genera desde los datasets y no se escribe a mano.
  </behavior>
  <action>
    Generar los seis documentos cortos de las URLs de acción `dejar` (D-06) y después regenerar los
    24 de una sola pasada, para dejar probado que el paquete entero sale de los datasets y es
    reproducible (D-12).

    Escribir `15-PAQUETE.md`, que es la puerta de entrada del entregable. Tres cosas y en este
    orden: la tabla de las 24 URLs con acción, formato y archivo; el resumen de qué recibe cada
    grupo, que son página completa las 16, title y meta las 6, y redirección las 2; y la nota de
    que cada archivo se abre solo, porque quien implementa una URL no debería leer las otras
    veintitrés (D-14).

    El índice se genera. Transcribir 24 filas a mano es exactamente el tipo de trabajo que se
    desincroniza en la primera corrección y deja al entregable diciendo algo que los datasets ya no
    dicen.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/paquete.ts --todos && ./node_modules/.bin/tsx src/phase15/paquete.ts --todos --indice</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(ls .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/*.md | wc -l | tr -d ' ')" -eq 24`
    - `test "$(cd seo-tools && node -e "const fs=require('fs'); const d='../.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const m=fs.readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); const slug=u=>u==='/'?'home':u.replace(/^\//,'').replace(/\//g,'-'); process.stdout.write(String(m.filter(r=>!fs.existsSync(d+slug(r.url)+'.md')).length))")" -eq 0`
      — cada una de las 24 URLs del mapa tiene su archivo, sin faltar ninguna.
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['agendar','blog','contacto','sedes','sobre-el-doctor','testimonios']; let mal=0; for(const x of f){const t=fs.readFileSync(d+x+'.md','utf8'); if(t.includes('<!-- copy:inicio -->'))mal++;} process.stdout.write(String(mal))")" -eq 0`
      — a las seis que no compiten no se les propone cuerpo de texto.
    - `test "$(node -e "const t=require('fs').readFileSync('.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-PAQUETE.md','utf8'); const m=require('fs').readFileSync('seo-tools/data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); process.stdout.write(String(m.filter(r=>!t.includes(r.url)).length))")" -eq 0`
      — el índice nombra las 24 URLs.
    - Determinismo: correr `--todos` dos veces deja los 24 documentos con los mismos SHA-256.
    - `test "$(shasum -a 256 seo-tools/data/url-map.jsonl | cut -d' ' -f1)" = "ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5"`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Las 24 URLs del mapa tienen su documento, los seis cortos explican por qué no compiten, y el
    índice los ordena y se regenera desde los datasets.
  </done>
</task>

<task type="auto">
  <name>Task 2: La ronda única de revisión del doctor y la auditoría sobre el paquete terminado</name>
  <files>seo-tools/src/phase15/revision.ts, seo-tools/data/onpage-audit.json, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-REVISION-DOCTOR.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md</files>
  <read_first>
    - Los cuatro datasets de copy: `data/copy-guias.json`, `copy-servicios.json`, `copy-sedes.json`
      y `copy-blog.json`.
    - `seo-tools/src/phase15/auditoria.ts` y `ymyl.ts` del plan 15-02.
    - `15-CONTEXT.md`, el párrafo sobre agrupar toda la revisión en una sola ronda.
  </read_first>
  <behavior>
    - El documento de revisión agrupa todos los bloques clínicos de las 16 páginas en una sola
      ronda.
    - Los bloques van ordenados por lo que de verdad necesita el ojo del doctor primero, no por
      orden de URL.
    - Cada bloque llega con su URL, su sección, el texto exacto y una casilla para aprobar o
      corregir.
    - Los datos operativos de sede que quedaron pendientes de confirmación salen en su propia
      sección, separados de lo clínico.
    - La auditoría de duplicados vuelve a correr sobre el paquete completo y sigue dando cero.
    - La compuerta de YMYL corre sobre los cuatro datasets a la vez y devuelve cero hallazgos.
  </behavior>
  <action>
    Implementar `revision.ts` para emitir `15-REVISION-DOCTOR.md` desde los cuatro datasets de
    copy. El documento es lo que se le pone al doctor delante, así que su forma decide si la ronda
    dura una hora o tres.

    Cómo se ordena, que es la parte discrecional y por eso se declara acá:

    1. **Primero, lo que puede hacer daño si está mal.** Afirmaciones sobre cuándo consultar de
       urgencia, sobre qué no hacer, y sobre qué significa un síntoma. Es lo que un paciente puede
       accionar solo.
    2. **Segundo, lo que decide una conducta médica.** Criterios de cuándo se opera y cuándo no,
       qué se evalúa, qué alternativas hay.
    3. **Tercero, lo descriptivo.** Qué es cada condición, anatomía, terminología.
    4. **Cuarto, lo operativo.** Sedes, horarios y datos que el doctor o la clínica confirman, no
       revisa clínicamente.

    Dentro de cada bloque, el texto exacto con su URL y su sección, y una casilla. Un doctor que
    tiene que abrir dieciséis archivos para revisar deja la revisión para después, y esta fase se
    queda entregada a medias por un problema de formato.

    Volver a correr la auditoría de duplicados y la compuerta de YMYL, ahora sobre el paquete
    terminado. La del plan 15-02 corrió sobre metadata sin copy; esta corre sobre todo. Actualizar
    `15-AUDITORIA.md` con el resultado final y con la fecha, y dejar escrito que dio limpia
    **antes** de que v1.1 publique, que es lo que ONPAGE-05 pide y el momento en que sirve.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --todos && ./node_modules/.bin/tsx src/phase15/auditoria.ts --data data/onpage.json --out data/onpage-audit.json && ./node_modules/.bin/tsx src/phase15/revision.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const a=require('./data/onpage-audit.json'); process.stdout.write(String(a.titlesDuplicados.length+a.h1Duplicados.length+a.metasFaltantes.length+a.titlesFueraDeRango.length+a.metasFueraDeRango.length))")" -eq 0`
      — ONPAGE-05 sigue cerrado sobre el paquete completo.
    - `test "$(cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --todos >/dev/null 2>&1 && echo 1 || echo 0)" -eq 1`
      — las 16 páginas pasan la compuerta a la vez.
    - `test "$(cd seo-tools && node -e "const fs=require('fs'); const t=fs.readFileSync('../.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-REVISION-DOCTOR.md','utf8'); const ds=['copy-guias','copy-servicios','copy-sedes','copy-blog'].map(x=>require('./data/'+x+'.json')); const cl=ds.flatMap(d=>(d.paginas||d)).flatMap(p=>p.secciones.filter(s=>s.tipo==='clinico').map(s=>({u:p.url,t:s.titulo}))); process.stdout.write(String(cl.filter(x=>!t.includes(x.titulo||x.t)).length))")" -eq 0`
      — todos los bloques clínicos de las 16 páginas están en la ronda; ninguno se queda afuera.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const pend=(d.paginas||d).flatMap(p=>(p.datosOperativos||[]).filter(o=>/pendiente/i.test(o.fuente))); const t=require('fs').readFileSync('../.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-REVISION-DOCTOR.md','utf8'); process.stdout.write(String(pend.filter(o=>!t.includes(o.dato)).length))")" -eq 0`
      — los datos operativos pendientes están en la ronda, en su sección aparte.
    - `test "$(grep -c 'antes de que v1.1 publique' '.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md')" -ge 1`
    - Determinismo: dos corridas de `revision.ts` producen el mismo SHA-256 de
      `15-REVISION-DOCTOR.md`.
  </acceptance_criteria>
  <done>
    Existe una sola ronda de revisión, ordenada por riesgo clínico y no por URL, con cada bloque
    localizable, y la auditoría sobre el paquete terminado sigue dando cero.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `Suggested H1` en el documento del cliente y el handoff hacia v1.1</name>
  <precondition>`.secrets/.env` trae la service account de Google con permiso de edición sobre el Sheet `1aowectbAJhyyZWhwQ6N_re-ENeSENvNN-5DebqCIls0`. Sin eso la carga no corre y hay que escalarle a Juan antes de seguir.</precondition>
  <files>seo-tools/src/phase15/kr-h1-push.ts, seo-tools/src/phase15/kr-h1-push.test.ts, seo-tools/data/sheet-columns.json, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md</files>
  <read_first>
    - `seo-tools/data/sheet-columns.json`, bloque `Keyword Research` completo: `headerRow` 3,
      `mapBy` por nombre, `keyField` y `keyHeader`, y la columna `Suggested H1` con `field: null` y
      `status: "fase-15"`.
    - `seo-tools/src/sheets/upsert.ts`, líneas 207 a 300: `UpsertOptions`, `estadosPropios`,
      `ESTADOS_PROPIOS_POR_DEFECTO` y la forma de `UpsertSummary`.
    - `seo-tools/src/phase13/kw-push.ts` entero: el patrón de cargador de este mismo tab.
    - `seo-tools/src/phase14/ca-push.ts`: el patrón más chico de cargador de fase.
    - `.../14-HANDOFF-V11.md` entero: es el modelo de handoff autocontenido que este documento
      continúa.
  </read_first>
  <behavior>
    - El registro de una keyword primaria trae dos campos, el H1 propuesto y la URL que la sirve;
      ninguna otra columna viaja en la petición.
    - Una keyword que no está en el tab se reporta por nombre y no se inserta como fila nueva.
    - La URL que se escribe es la del mapa de la fase 14, no una derivada nueva: el cargador la lee
      de `url-map.jsonl` y no la recalcula.
    - La carga corre en ensayo por defecto y solo escribe con `--yes`.
    - Una segunda carga con `--yes` reporta cero inserciones y cero columnas agregadas.
    - El cargador se niega a correr si el modelo no declara `field` para `Suggested H1`.
    - El cargador se niega a correr con los estados propios por defecto: exige `fase-15`.
  </behavior>
  <action>
    Habilitar la columna `Suggested H1` en `data/sheet-columns.json` poniéndole `field:
    "suggestedH1"`. El `status` se queda en `fase-15`, que es lo que el plan 13-01 ya había
    declarado cuando la reservó para esta fase.

    **Y cerrar de paso la columna `URL` del mismo tab, que quedó huérfana.** Está declarada
    `status: "fase-14"` con `field: null`: la fase 14 la reservó, produjo el dato que la llena y
    cerró sin escribirla. No es de esta fase por requisito —ningún ONPAGE la nombra— pero sí es de
    esta fase por oportunidad: este es el único cargador que vuelve a tocar ese tab, el dato ya
    existe en `url-map.jsonl`, y dejar vacía una columna del entregable del cliente teniendo el
    dato a mano es exactamente el hueco que la verificación de la fase 14 encontró en las columnas
    de Ahrefs. Ponerle `field: "url"` y dejar el `status` en `fase-14`, que es de donde viene el
    dato; el cargador declara `estadosPropios: ["fase-14", "fase-15"]` por ese motivo y no por
    conveniencia. Registrarlo en el SUMMARY como cierre de un residuo de la fase anterior, no como
    alcance nuevo de esta.

    Implementar `kr-h1-push.ts` con la disciplina de los cargadores de las fases 13 y 14: ensayo por
    defecto, `--yes` para escribir, `omitirCamposAusentes` en verdadero y `addMissingColumns` en
    falso. Y una defensa que este cargador necesita más que los otros: **`estadosPropios` en
    exactamente `["fase-14", "fase-15"]`**. El valor por defecto de `upsertRows` habilita fase 12,
    fase 13, `nueva` y `no-consultado`, y este tab tiene columnas de las tres primeras llenas con
    5716 filas de datos. Con el permiso acotado a esos dos estados, esta carga no puede tocar nada
    que no sea suyo aunque una columna ajena quedara en medio del rango. Los dos estados son
    exactamente las dos columnas que este cargador escribe y ninguna más: comprobarlo con un
    criterio que cuente las columnas de esos estados en el modelo y exija que sean dos.

    Los registros son 16, uno por keyword primaria del mapa, cada uno con la keyword como clave, el
    H1 propuesto que ya vive en `data/onpage.json` y la URL que lo sirve, leída de
    `url-map.jsonl`. Ninguno trae otro campo. Las 5.700 filas restantes del tab no entran en la
    petición y quedan intactas.

    Escribir `15-HANDOFF-V11-ONPAGE.md`, autocontenido, para las fases 8 y 10 de v1.1. Lo que tiene
    que decir, en este orden:

    1. **Qué se entrega y dónde**: 24 documentos en `paquetes/`, uno por URL, y cómo se abre el que
       corresponde.
    2. **Para la fase 10**: la tabla de las 22 URLs con title y meta, con su conteo de caracteres,
       dentro del contrato de 60 y 155 que esa fase ya verifica.
    3. **Para la fase 8**: qué páginas reciben copy completo, cuáles solo metadata y cuáles se
       apagan. Las 3 URLs por crear y las 3 redirecciones 301, incluida la del renombre de
       escoliosis que la fase 14 ya había avisado.
    4. **El orden que no se puede invertir**: las dos guías absorben el contenido de sus posts, así
       que la guía se publica antes de que el 301 se ponga. Redirigir primero tira el contenido.
    5. **La restricción que sigue viva**: ninguna línea de texto clínico se publica sin la
       aprobación del doctor. La ronda está armada en `15-REVISION-DOCTOR.md` y **es bloqueante para
       la fase 8**.
    6. **Lo que este handoff NO resuelve**: los datos operativos de sede que quedaron pendientes de
       confirmación, y los enlaces internos, que se propusieron en la fase 14 y los implementa v1.1.

    Correr el ensayo, leerlo, escribir, y volver a escribir para dejar probada la idempotencia sobre
    este tab.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=kr-h1 && npm run typecheck && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase15/kr-h1-push.ts --data data/onpage.json --dry-run && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase15/kr-h1-push.ts --data data/onpage.json --yes && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase15/kr-h1-push.ts --data data/onpage.json --yes</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const c=require('./data/sheet-columns.json'); const col=c['Keyword Research'].columns.find(x=>x.header==='Suggested H1'); process.stdout.write(String(col.field==='suggestedH1'&&col.status==='fase-15'?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const c=require('./data/sheet-columns.json'); const col=c['Keyword Research'].columns.find(x=>x.header==='URL'); process.stdout.write(String(col.field==='url'&&col.status==='fase-14'?1:0))")" -eq 1`
      — la columna huérfana de la fase 14 queda con campo, conservando su estado de origen.
    - `test "$(cd seo-tools && node -e "const c=require('./data/sheet-columns.json'); const n=c['Keyword Research'].columns.filter(x=>x.status==='fase-14'||x.status==='fase-15').length; process.stdout.write(String(n))")" -eq 2`
      — el permiso ampliado a dos estados habilita exactamente dos columnas y ninguna más. Si
      alguien agrega una tercera con uno de esos estados, este criterio falla antes de que la carga
      la escriba sin querer.
    - `test "$(cd seo-tools && node -e "const s=require('fs').readFileSync('src/phase15/kr-h1-push.ts','utf8'); process.stdout.write(String(/estadosPropios/.test(s)&&/fase-15/.test(s)&&/fase-14/.test(s)&&/addMissingColumns:\s*false/.test(s)&&/omitirCamposAusentes/.test(s)?1:0))")" -eq 1`
      — las defensas del cargador están en el código y no en la intención.
    - Las dos corridas finales con `--yes` reportan `actualizadas: 16`, `insertadas: 0` y
      `columnasAgregadas: 0`. Los tres números quedan copiados en el SUMMARY.
    - Lectura de solo lectura del documento vivo sobre dos de las 16 keywords elegidas al azar: las
      celdas `Suggested H1` y `URL` traen el H1 de `data/onpage.json` y la URL de `url-map.jsonl`,
      y las columnas vecinas de las fases 12 y 13 siguen con el valor que tenían antes de esta
      carga.
    - `test "$(cd seo-tools && node -e "const fs=require('fs'); const m=fs.readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse).filter(r=>r.keywordPrimaria); process.stdout.write(String(m.length))")" -eq 16`
      — hay exactamente una URL por keyword primaria para escribir en la columna.
    - `test "$(cd seo-tools && node -e "const o=require('./data/onpage.json').filas; process.stdout.write(String(o.filter(r=>r.keywordPrimaria&&r.h1).length))")" -eq 16`
    - `test "$(node -e "const t=require('fs').readFileSync('.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md','utf8'); const o=require('./seo-tools/data/onpage.json').filas.filter(r=>r.accion!=='redirigir'); process.stdout.write(String(o.filter(r=>!t.includes(r.url)).length))")" -eq 0`
      — el handoff nombra las 22 URLs con metadata; ninguna se queda sin instrucción.
    - `test "$(grep -c 'escoliosis-y-deformidades' '.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md')" -ge 1`
      — el renombre de slug que arrastra 301, sitemap y enlaces sigue avisado.
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
    - Las seis conductas declaradas tienen prueba propia y la suite completa queda en verde.
  </acceptance_criteria>
  <done>
    Las columnas `Suggested H1` y `URL` están llenas en el documento del cliente para las 16
    keywords primarias, recargarlas no inserta ni agrega nada, y existe un handoff autocontenido
    que las fases 8 y 10 de v1.1 pueden leer solas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `kr-h1-push.ts` → tab con 5716 filas de tres fases anteriores | Una escritura con permisos amplios puede pisar columnas de las fases 12, 13 y 14 sin lanzar ninguna excepción. |
| Paquete → fases 8 y 10 de v1.1 | Si el handoff obliga a interpretar, el trabajo se decide dos veces y la fase no cumplió su razón de existir. |
| Copy sin aprobar → publicación | Si el sello no llega hasta v1.1 como instrucción bloqueante, la restricción YMYL se pierde en el traspaso. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-31 | Tampering | Escritura sobre columnas de fases anteriores | critical | mitigate | `estadosPropios: ["fase-15"]` en la carga; criterio que lo prueba en el código y lectura del documento vivo comparando columnas vecinas. |
| T-15-32 | Tampering | Filas insertadas de más en un tab de 5716 | high | mitigate | Criterio que exige `insertadas: 0` en las dos corridas; una keyword no encontrada se reporta y no se inserta. |
| T-15-33 | Elevation of Privilege | Publicar copy clínico sin aprobación | critical | mitigate | El handoff declara la ronda del doctor como bloqueante para la fase 8; cada bloque clínico llega sellado desde el dataset. |
| T-15-34 | Denial of Service | Contenido perdido por redirigir antes de fundir | high | mitigate | El handoff fija el orden con todas las letras y nombra las dos guías que absorben. |
| T-15-35 | Repudiation | Índice o handoff desincronizado del dataset | medium | mitigate | Los dos se generan desde los datasets; criterios que cruzan el documento contra `url-map.jsonl` y `onpage.json`. |
| T-15-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si algún paso lo requiriera, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- `npx tsc --noEmit` en la raíz con estado 0.
- SHA-256 de `seo-tools/data/url-map.jsonl` y de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96: la fase entera, siete planes, cerró sin gastar una búsqueda.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
- El tab `Keyword Research` verificado por lectura del documento vivo.
</verification>

<success_criteria>
- ONPAGE-06 cerrado: existe un paquete por URL que quien ejecute las fases 8 y 10 de v1.1 puede
  abrir e implementar de corrido, con su índice y su handoff autocontenido.
- ONPAGE-02 cerrado del lado del Sheet: `Suggested H1` lleno para las 16 keywords primarias, con
  cero inserciones y cero columnas agregadas.
- ONPAGE-05 sigue cerrado sobre el paquete terminado y no solo sobre la metadata.
- La revisión del doctor está armada en una sola ronda, ordenada por riesgo clínico.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 15-07)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Seis documentos cortos | `.../paquetes/{agendar,blog,contacto,sedes,sobre-el-doctor,testimonios}.md` | Entregable |
| Índice del paquete | `.../15-PAQUETE.md` | Entregable |
| Ronda de revisión del doctor | `.../15-REVISION-DOCTOR.md` | Entregable |
| Auditoría final | `.../15-AUDITORIA.md` | Entregable |
| Handoff hacia v1.1 | `.../15-HANDOFF-V11-ONPAGE.md` | Handoff |
| Cargador de `Suggested H1` | `seo-tools/src/phase15/kr-h1-push.ts` | Código |
| Columna `Suggested H1` | Tab `Keyword Research` del Sheet | Entrega externa |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-07-SUMMARY.md` al terminar.
</output>
