---
phase: 12-instrumentaci-n-de-datos-y-universo-de-keywords
workstream: seo-keywords
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - seo-tools/src/keywords/seeds.ts
  - seo-tools/src/keywords/seeds.test.ts
  - seo-tools/src/keywords/permute.ts
  - seo-tools/src/keywords/permute.test.ts
  - seo-tools/src/keywords/expand.ts
  - seo-tools/src/keywords/expand.test.ts
  - seo-tools/src/sources/serpapi.ts
  - seo-tools/src/sources/serpapi.test.ts
  - seo-tools/src/commands/kw-seeds.ts
  - seo-tools/src/commands/kw-expand.ts
  - seo-tools/data/seeds.json
  - seo-tools/data/modifiers.json
  - seo-tools/data/candidates.jsonl
  - seo-tools/README.md
autonomous: true
requirements: [KWR-01, INFRA-03]

user_setup:
  - service: serpapi
    why: "Las capas 3 y 4 de la expansion traen el long tail real de Lima que la permutacion no inventa. Sin la clave el plan igual cierra por la ruta de relleno de cache desde un agente."
    env_vars:
      - name: SERPAPI_API_KEY
        source: "Panel de SerpApi, seccion de la clave de API. Agregar a .secrets/.env. Opcional: si no llega, se usa la ruta de relleno de cache con la herramienta MCP."
    dashboard_config:
      - task: "Confirmar el plan contratado de SerpApi. La expansion consume cerca de 100 busquedas, que entra incluso en el plan gratuito de 250 al mes, pero conviene saberlo antes de correr"
        location: "serpapi.com, seccion de plan y consumo"

must_haves:
  truths:
    - "Existe un snapshot commiteado de las semillas del negocio extraidas del contenido de v1.0, de la competencia investigada y del CV del doctor, con 55 o mas semillas unicas."
    - "El CLI no importa modulos de la aplicacion en tiempo de ejecucion: lee el snapshot, asi que un cambio de v1.1 en su contenido no puede romper el tooling."
    - "Solo con permutacion determinista, sin una sola llamada de red, ya se superan las 400 keywords candidatas."
    - "La expansion con datos reales de Google funciona por dos caminos que producen exactamente el mismo resultado: llamada directa con clave propia, o relleno de la cache desde un agente con herramienta MCP."
    - "Volver a correr la expansion en modo offline reproduce el mismo archivo de candidatos sin una sola llamada de red."
    - "Las keywords sin datos no se descartan: quedan marcadas, porque el long tail geolocalizado sin volumen medible es justo donde un dominio nuevo puede ganar."
    - "Ninguna keyword de otra ciudad del Peru entra al universo."
  artifacts:
    - "seo-tools/data/seeds.json con el snapshot de semillas y su procedencia"
    - "seo-tools/data/modifiers.json con las familias de modificadores y de geo"
    - "seo-tools/data/candidates.jsonl con el universo candidato normalizado y deduplicado"
    - "seo-tools/src/keywords/seeds.ts, permute.ts, expand.ts"
    - "seo-tools/src/sources/serpapi.ts pasando siempre por el seam de cache"
    - "seo-tools/src/commands/kw-seeds.ts y kw-expand.ts implementados"
  key_links:
    - "seeds.ts hacia src/content/: lectura de una sola vez que produce un snapshot; ningun otro modulo vuelve a mirar la aplicacion"
    - "serpapi.ts hacia el seam de cache del plan 01: ninguna llamada de red esquiva el seam, o el modo offline deja de ser verificable"
    - "kw:expand con la bandera de solo planificar hacia cache:put: el CLI calcula la clave y el agente la usa, nunca al reves"
    - "candidates.jsonl hacia el plan 04: la clasificacion y el dataset consolidado se construyen sobre este archivo"
---

<objective>
Levantar el universo candidato de keywords del negocio completo del doctor, con el camino barato y determinista primero y el long tail real de Google despues.

Purpose: KWR-01 pide 400 o mas keywords expandidas desde semillas por condicion, procedimiento, sintoma, especialidad y sede. El research midio el inventario real del repositorio y el conteo cierra: la permutacion sola produce alrededor de 432 candidatos sin gastar una sola llamada. Pero la permutacion inventa frases que nadie busca, asi que las capas con datos reales de Google no son opcionales: aportan el long tail geolocalizado y sirven de validacion. Ese es tambien el terreno donde un dominio de agosto de 2026 sin historial puede competir.

Output: el archivo de candidatos normalizado y deduplicado que el plan 04 clasifica y consolida, con las capturas de resultados de busqueda cacheadas y reutilizables por la fase 13.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/seo-keywords/ROADMAP.md
@.planning/workstreams/seo-keywords/REQUIREMENTS.md
@.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-RESEARCH.md
@.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-01-SUMMARY.md
@.planning/research/COMPETITORS.md
</context>

<constraints>
1. Nada se escribe bajo `src/`. Este plan lee `src/content/` una sola vez para extraer semillas y nunca lo modifica.
2. Nada se escribe en `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` ni bajo `.planning/workstreams/milestone/`.
3. No se toca `seo-tools/src/cli.ts`: el plan 02 corre en la misma ola y el despacho ya quedo completo en el plan 01.
4. No se toca `seo-tools/src/sheets/`: es propiedad del plan 02 en esta misma ola.
5. Ninguna funcion de este plan llama a la red sin pasar por el seam de cache del plan 01.
6. La herramienta de dificultad y potencial de trafico queda fuera: cero cliente, cero clave, cero llamadas.
7. Keywords fuera de Lima quedan fuera de alcance. El negocio es presencial y no hay sede que respalde otra ciudad.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Extraccion de semillas a un snapshot commiteado</name>

  <files>seo-tools/src/keywords/seeds.ts, seo-tools/src/keywords/seeds.test.ts, seo-tools/src/commands/kw-seeds.ts, seo-tools/data/seeds.json</files>

  <read_first>
    - `src/content/services.ts` — condiciones por categoria y ejemplos de abordajes de procedimiento
    - `src/content/service-pages.ts` — paginas de servicio que v1.1 esta construyendo
    - `src/content/locations.ts` — cuatro sedes, distritos y clinicas
    - `src/content/blog.ts` — titulos de los cuatro posts
    - `src/content/faq.ts` — once preguntas de pacientes
    - `src/content/cv.ts` — especialidades del doctor
    - `.planning/research/COMPETITORS.md` — terminos que la competencia cubre
    - `.planning/PROJECT.md` — seccion de la auditoria del 2026-08-10, con la SERP real y el local pack
    - `12-RESEARCH.md`, *Pattern 5* capa 1 con el inventario ya contado, y *Pitfall 10*
    - `seo-tools/src/keywords/normalize.ts` del plan 01
  </read_first>

  <behavior>
    - La extraccion produce 55 o mas semillas unicas despues de normalizar y deduplicar.
    - Cada semilla lleva su procedencia: de que archivo o documento salio y de que tipo es (condicion, procedimiento, especialidad, sede, sintoma o pregunta).
    - Dos semillas que solo difieren en tildes, mayusculas o espacios sobrantes colapsan en una sola.
    - El texto visible de la semilla conserva sus tildes; la forma normalizada solo se usa para deduplicar.
    - Volver a ejecutar la extraccion sobre el mismo contenido produce un archivo identico byte por byte: el orden de salida es determinista, no depende del recorrido del sistema de archivos.
    - Los demas subcomandos leen el snapshot y no importan ningun modulo de la aplicacion.
  </behavior>

  <action>
    Implementar el subcomando de extraccion de semillas y el modulo que lo respalda.

    Leer los seis archivos de contenido de la aplicacion, la investigacion de competencia y la auditoria del proyecto, y extraer las cadenas de dominio que sirven de semilla: condiciones tratadas por categoria, ejemplos y abordajes de procedimiento, sedes con sus distritos y sus clinicas, titulos de blog reducidos a su termino nuclear, preguntas frecuentes reducidas a su termino nuclear, especialidades del doctor, y los terminos que la competencia cubre y el sitio no. El research ya conto el inventario real y estimo entre 55 y 70 semillas unicas: si el conteo cae por debajo de 55, la extraccion se quedo corta y hay que revisar que archivo se paso por alto antes de continuar.

    El punto clave de diseno es el aislamiento entre los dos workstreams. Esta lectura ocurre una sola vez y produce `seo-tools/data/seeds.json`, que se commitea. Ningun otro subcomando puede importar modulos de la aplicacion en tiempo de ejecucion: si v1.1 agrega un import de framework a uno de esos archivos, el tooling de v1.2 dejaria de arrancar en medio de la fase 14. Regenerar el snapshot es una decision explicita mediante la bandera de refresco, nunca un efecto colateral de otra corrida.

    Cada entrada del snapshot lleva el texto visible, la forma normalizada, el tipo de semilla y la procedencia. La procedencia importa porque la fase 13 va a querer saber si un cluster nace del contenido propio o del hueco de la competencia. Ordenar la salida de forma determinista para que el diff de git sea legible y para que reejecutar no produzca ruido.

    Escribir las pruebas antes que la implementacion, sobre fragmentos de contenido en memoria: no pueden depender de que los archivos de la aplicacion tengan hoy un valor concreto, porque el otro workstream los esta editando.
  </action>

  <verify>
    <automated>cd seo-tools && npm run typecheck && npm test && npm run cli -- kw:seeds && node -e "const s=JSON.parse(require('node:fs').readFileSync('data/seeds.json','utf8')); const n=Array.isArray(s)?s.length:s.seeds.length; if(n<55) throw new Error('semillas insuficientes: '+n); console.log('semillas '+n);" && H1=$(node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('data/seeds.json')).digest('hex'))") && npm run cli -- kw:seeds && H2=$(node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('data/seeds.json')).digest('hex'))") && test "$H1" = "$H2" && echo "determinista OK ($H1)" && test "$(grep -rl 'src/content' src --include='*.ts' | wc -l | tr -d ' ')" -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `cd seo-tools && npm run cli -- kw:seeds` sale con codigo 0 y `seo-tools/data/seeds.json` contiene 55 o mas entradas unicas.
    - Ejecutar el subcomando dos veces seguidas deja el archivo identico. La comparacion se hace con el hash SHA-256 del archivo antes y despues de la segunda corrida, no con `git diff`: en la primera ejecucion el archivo todavia no esta versionado y `git diff` sobre un archivo sin seguimiento sale con codigo 0 aunque el contenido haya cambiado, asi que pasaria en falso.
    - Cada entrada declara tipo y procedencia, y estan representados los seis tipos de semilla.
    - `test "$(cd seo-tools && grep -rl 'src/content' src --include='*.ts' | wc -l | tr -d ' ')" -eq 1` sale con codigo 0: un unico archivo del tooling mira la aplicacion. El conteo es de archivos y no de lineas, porque el modulo de extraccion referencia los seis archivos de contenido y contar lineas daria seis o mas.
    - `cd seo-tools && grep -rn "from \"\\.\\./\\.\\./\\.\\./src/" src --include='*.ts' | wc -l` imprime `0`: ningun modulo importa la aplicacion de forma estatica en el resto del arbol.
    - `git status --porcelain -- src/` no reporta ninguna modificacion: la extraccion solo leyo.
    - `cd seo-tools && npm test` sale con codigo 0 y las pruebas de este modulo corren sobre contenido en memoria.
    - `git diff --cached --name-only -- src/ .secrets seo-tools/src/cli.ts seo-tools/src/sheets | wc -l` imprime `0` antes de commitear.
  </acceptance_criteria>

  <done>
    Existe un snapshot commiteado de las semillas del negocio completo con su procedencia, la extraccion es determinista, y el tooling quedo desacoplado de lo que el otro workstream haga con el contenido de la aplicacion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Permutacion determinista, filtro de relevancia y archivo de candidatos</name>

  <files>seo-tools/src/keywords/permute.ts, seo-tools/src/keywords/permute.test.ts, seo-tools/src/keywords/expand.ts, seo-tools/src/keywords/expand.test.ts, seo-tools/src/commands/kw-expand.ts, seo-tools/data/modifiers.json, seo-tools/data/candidates.jsonl</files>

  <read_first>
    - `seo-tools/data/seeds.json` producido en la tarea 1
    - `12-RESEARCH.md`, *Pattern 5* capa 2 con la tabla de combinaciones y sus conteos, y el total realista tras normalizar, deduplicar y filtrar
    - `12-CONTEXT.md`, seccion *Universo de keywords*
    - `seo-tools/src/keywords/normalize.ts` del plan 01
  </read_first>

  <behavior>
    - La permutacion de condiciones por modificadores informacionales, condiciones por modificadores comerciales, especialidades por geo y procedimientos por modificadores produce 400 o mas candidatos brutos.
    - La combinacion respeta el orden natural del espanol: el modificador precede a la semilla cuando corresponde y el geo va al final.
    - Dos candidatos que colapsan a la misma forma normalizada se cuentan una sola vez y se conserva el primero por orden determinista.
    - Un candidato que no contiene ningun termino del dominio medico ni de especialidad queda descartado por el filtro de relevancia.
    - Un candidato que nombra una ciudad del Peru distinta de Lima o sus distritos queda descartado.
    - Cada candidato conserva la semilla de la que nacio y la capa de expansion que lo produjo.
    - Volver a ejecutar la expansion sin llamadas de red produce un archivo de candidatos identico byte por byte.
  </behavior>

  <action>
    Escribir `data/modifiers.json` con las cuatro familias que el research ya midio: modificadores informacionales, modificadores comerciales, geo de Lima con sus distritos y sus clinicas, y modificadores de procedimiento. Que vivan en un archivo de datos y no en el codigo es lo que permite ampliarlos en la fase 13 sin tocar la logica.

    Implementar la permutacion como funcion pura: recibe semillas y modificadores, devuelve candidatos, sin efectos ni red. Es la unica capa de expansion con costo cero y reproducibilidad total, asi que es la que sostiene el umbral de KWR-01 aunque todo lo demas falle.

    Implementar el orquestador de expansion con dos responsabilidades: normalizar y deduplicar contra la forma normalizada, y aplicar el filtro de relevancia. El filtro exige que el candidato contenga al menos un termino del dominio medico o de especialidad, y descarta los que nombren otra ciudad del pais. Ese segundo criterio no es una preferencia: el negocio es presencial en Lima y no hay sede que respalde otra ciudad.

    Escribir `data/candidates.jsonl` con una linea por candidato. El formato de una linea por registro se eligio por sobre el separado por comas porque las keywords en espanol llevan comas y porque el diff de git queda legible sin entrecomillado. Cada linea trae el texto visible, la forma normalizada que sirve de clave, la semilla de origen, la capa que lo produjo y un estado que arranca marcando que todavia no tiene datos de ninguna fuente. Las keywords sin datos no se descartan nunca: el long tail geolocalizado sin volumen medible es justo el terreno donde un dominio nuevo de agosto de 2026 puede ganar.

    Implementar `commands/kw-expand.ts` sin tocar el archivo de despacho del CLI. Al terminar imprime el conteo por capa, el conteo tras deduplicar, el conteo tras filtrar y el total final, para que el criterio de las 400 se verifique leyendo la salida y no confiando en ella.

    **Que hacer si el neto queda por debajo de 400.** La tabla del *Pattern 5* proyecta alrededor de 432 candidatos brutos de permutacion, pero el umbral de KWR-01 se mide despues de deduplicar y filtrar, y hay solape real entre familias: `cirugia de hernia discal` sale tanto de la familia comercial como de la de procedimiento. Si el conteo post filtro queda debajo de 400, la respuesta correcta es ampliar `data/modifiers.json` primero, que es determinista y de costo cero, y recien despues considerar las capas de red. Hay holgura de sobra sin tocar codigo: la capa 1 produce entre 55 y 70 semillas y la tabla de permutacion solo usa 23 condiciones, 5 especialidades y 10 procedimientos, asi que quedan semillas sin combinar y familias de modificadores que se pueden extender. No reducir el umbral ni relajar el filtro de relevancia o de geografia para llegar al numero: eso entregaria un universo inflado con frases que nadie busca, que es exactamente lo que el filtro existe para evitar.

    Escribir las pruebas antes que la implementacion, sobre conjuntos pequenos de semillas y modificadores en memoria.
  </action>

  <verify>
    <automated>cd seo-tools && npm run typecheck && npm test && npm run cli -- kw:expand --offline && node -e "const L=require('node:fs').readFileSync('data/candidates.jsonl','utf8').trim().split('\n').filter(Boolean); if(L.length<400) throw new Error('universo insuficiente tras deduplicar y filtrar: '+L.length+'. Ampliar data/modifiers.json antes de recurrir a las capas de red.'); const k=new Set(); for(const l of L){const o=JSON.parse(l); if(k.has(o.keywordKey)) throw new Error('duplicado: '+o.keywordKey); k.add(o.keywordKey);} console.log('candidatos '+L.length);" && H1=$(node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('data/candidates.jsonl')).digest('hex'))") && npm run cli -- kw:expand --offline && H2=$(node -e "console.log(require('node:crypto').createHash('sha256').update(require('node:fs').readFileSync('data/candidates.jsonl')).digest('hex'))") && test "$H1" = "$H2" && echo "determinista OK ($H1)"</automated>
  </verify>

  <acceptance_criteria>
    - `seo-tools/data/candidates.jsonl` tiene 400 o mas lineas despues de deduplicar y filtrar, y ninguna forma normalizada se repite. Si la primera corrida queda debajo, se amplia `data/modifiers.json` y se vuelve a correr; el SUMMARY registra que familias se ampliaron.
    - Cada linea es JSON valido y declara texto visible, forma normalizada, semilla de origen, capa y estado.
    - Ejecutar la expansion dos veces seguidas deja el archivo identico, comparado por hash SHA-256 antes y despues de la segunda corrida. No se usa `git diff`: en la primera ejecucion el archivo esta sin seguimiento y `git diff` saldria con codigo 0 aunque el contenido cambiara.
    - `cd seo-tools && npm run cli -- kw:expand --offline` sale con codigo 0 sin ninguna clave de API definida en el entorno.
    - Existe una prueba que afirma que un candidato que nombra otra ciudad del pais queda descartado, y otra que afirma que un candidato sin ningun termino del dominio queda descartado.
    - La salida del subcomando imprime los cuatro conteos: por capa, tras deduplicar, tras filtrar y total.
    - `seo-tools/data/modifiers.json` es JSON valido y declara las cuatro familias.
    - `git diff --cached --name-only -- src/ .secrets seo-tools/src/cli.ts seo-tools/src/sheets | wc -l` imprime `0` antes de commitear.
  </acceptance_criteria>

  <done>
    El umbral de KWR-01 queda superado con permutacion determinista y sin gastar una sola llamada de red, con el filtro de relevancia y de geografia aplicados y el archivo de candidatos reproducible.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Fuente de datos reales de Google por los dos caminos, con la misma cache</name>

  <files>seo-tools/src/sources/serpapi.ts, seo-tools/src/sources/serpapi.test.ts, seo-tools/src/keywords/expand.ts, seo-tools/src/commands/kw-expand.ts, seo-tools/data/candidates.jsonl</files>

  <read_first>
    - `seo-tools/src/cache.ts`, `seo-tools/src/http.ts` y `seo-tools/src/commands/cache-put.ts` del plan 01
    - `12-RESEARCH.md`, *Pattern 3* completo (la frontera de herramientas de agente y el seam de relleno), *Pattern 5* capas 3 y 4, *Pattern 2* (tabla de que se persiste), el ejemplo *C8* y la seccion *Open Questions* punto 4
    - `12-CONTEXT.md`, seccion *Universo de keywords*
    - `seo-tools/data/candidates.jsonl` producido en la tarea 2
  </read_first>

  <precondition>
    Una de estas dos cosas es cierta antes de correr la expansion con datos reales: o `SERPAPI_API_KEY` esta definida en `.secrets/.env`, o hay un agente con la herramienta MCP de SerpApi disponible para rellenar la cache. Si no se cumple ninguna, la tarea igual se implementa y se prueba, y la expansion queda con las capas deterministas, que ya superan el umbral de KWR-01; registrar esa situacion en el SUMMARY.
  </precondition>

  <reversibility rating="reversible">
    Todo lo que produce esta tarea son archivos de cache y lineas agregadas al archivo de candidatos. Borrar la cache y volver a expandir reconstruye el estado.
  </reversibility>

  <behavior>
    - Una consulta de sugerencias de autocompletado devuelve el arreglo de sugerencias parseado y lo persiste en la cache con la clave que calcula el seam.
    - Una consulta de resultados de busqueda geolocalizada en Lima devuelve busquedas relacionadas y preguntas relacionadas parseadas, y persiste la respuesta completa, porque la fase 13 la necesita para el agrupamiento por solape y el perfil de competencia.
    - La segunda ejecucion de la misma consulta no llama a la red: resuelve desde la cache.
    - Con la bandera de solo planificar, no se hace ninguna llamada y se escribe un archivo con la lista de consultas pendientes, cada una con su clave calculada, su fuente, su endpoint y sus parametros.
    - Rellenar la cache con el subcomando correspondiente usando una de esas claves y un cuerpo de respuesta hace que la siguiente expansion resuelva esa consulta como acierto.
    - El resultado de la expansion es identico haya llegado el dato por llamada directa o por relleno externo: el codigo aguas abajo no puede distinguir el origen.
    - Sin clave y sin cache, la expansion con datos reales falla con un mensaje que nombra las dos rutas disponibles y no deja el archivo de candidatos a medio escribir.
    - Antes de gastar busquedas, el subcomando imprime la cantidad estimada y exige confirmacion explicita.
  </behavior>

  <action>
    Implementar `sources/serpapi.ts` con dos funciones, sugerencias de autocompletado y resultados de busqueda geolocalizada, ambas segun el ejemplo *C8*: idioma espanol, pais Peru, dominio de Google de Peru y ubicacion Lima para la busqueda. Las dos pasan por el seam de cache del plan 01 sin excepcion. La clave de la API viaja como parametro de la peticion tal como documenta el proveedor, pero nunca se escribe en el envelope de cache: el envelope guarda los parametros de la consulta, jamas las credenciales.

    Conectar las dos capas restantes de la expansion. La capa de autocompletado corre una consulta por semilla principal con sufijos dirigidos, no expansion alfabetica: recorrer el abecedario multiplicaria por veintisiete el consumo, superaria de largo el plan gratuito y aporta poco frente a los sufijos que ya se sabe que importan en este negocio. La capa de resultados de busqueda corre sobre los terminos cabecera y cosecha busquedas relacionadas y preguntas relacionadas. Estas mismas capturas son las que la fase 13 necesita para agrupar por solape y para perfilar competencia, asi que se guardan en cache con el mismo esquema y no se repite la pasada el mes que viene.

    Implementar los dos caminos que el plan tiene que soportar, con la llamada directa como preferida:

    - Camino directo: la clave se lee del archivo de secretos y la llamada sale del propio CLI, que queda sin necesidad de agente y reejecutable.
    - Camino de relleno: con la bandera de solo planificar, el CLI no llama a nadie y escribe la lista de consultas pendientes con la clave ya calculada para cada una. Un agente lee ese archivo, invoca la herramienta MCP con esos parametros y devuelve el cuerpo crudo mediante el subcomando de relleno de cache. Al volver a correr la expansion, todas las consultas pegan en cache, no hay red y el resultado es identico.

    La propiedad critica es que la funcion de hash vive en un solo lugar. Quien rellena nunca calcula el nombre del archivo: se lo pregunta al CLI. Si el agente inventara la ruta, la cache se desincroniza y nadie se entera hasta el reprocesamiento del mes siguiente. Documentar el procedimiento de relleno en el README del paquete, con el ejemplo completo de las tres ordenes.

    Antes de gastar busquedas, imprimir la cantidad estimada y exigir confirmacion explicita: nadie sabe todavia que plan tiene contratado Juan y una corrida a ciegas puede agotarlo.

    Escribir las pruebas antes que la implementacion, con una doble de la funcion de red. Incluir la prueba que compara el resultado de una expansion alimentada por llamada directa contra el de una expansion alimentada por relleno externo con el mismo cuerpo: tienen que ser iguales.
  </action>

  <verify>
    <automated>cd seo-tools && npm run typecheck && env -u SERPAPI_API_KEY npm test && env -u SERPAPI_API_KEY npm run cli -- kw:expand --offline && test "$(grep -rn 'fetch(' src/sources --include='*.ts' | grep -v '//' | wc -l | tr -d ' ')" -eq 0 && node -e "const L=require('node:fs').readFileSync('data/candidates.jsonl','utf8').trim().split('\n').filter(Boolean); if(L.length<400) throw new Error('universo insuficiente: '+L.length); console.log('candidatos '+L.length);"</automated>
  </verify>

  <acceptance_criteria>
    - `cd seo-tools && env -u SERPAPI_API_KEY npm test` sale con codigo 0: la suite completa corre sin la clave.
    - Existe una prueba nombrada de forma reconocible que afirma que una expansion alimentada por llamada directa y una alimentada por relleno de cache producen candidatos identicos.
    - `cd seo-tools && env -u SERPAPI_API_KEY npm run cli -- kw:expand --offline` sale con codigo 0. Ese codigo de salida es la prueba completa de que no hubo ningun fallo de cache, porque el modo offline aborta duro ante el primero. No se cruza con la salida de `cache:stats`: los contadores de aciertos y fallos que define el plan 01 son de la ejecucion en curso y no sobreviven al fin del proceso, asi que un segundo proceso no puede medir lo que hizo el primero.
    - Una consulta deliberadamente ausente de la cache ejecutada en modo offline sale con codigo distinto de cero y su mensaje nombra fuente, endpoint y clave calculada.
    - `cd seo-tools && npm run cli -- kw:expand --plan-only` no realiza ninguna llamada de red y escribe un archivo de consultas pendientes donde cada entrada trae clave, fuente, endpoint y parametros.
    - `cd seo-tools && grep -rn "fetch(" src/sources --include='*.ts' | grep -v "//" | wc -l` imprime `0`: las fuentes llaman a la red a traves del envoltorio del plan 01, nunca de forma directa.
    - `cd seo-tools && grep -rniE "serpapi_api_key|api_key" .cache -r 2>/dev/null | wc -l` imprime `0`: ningun archivo de cache contiene la credencial.
    - `git check-ignore -q seo-tools/.cache` sale con codigo 0 y `git diff --cached --name-only -- seo-tools/.cache | wc -l` imprime `0`.
    - `git diff --cached --name-only -- src/ .secrets seo-tools/src/cli.ts seo-tools/src/sheets | wc -l` imprime `0` antes de commitear.
    - Si la expansion con datos reales llego a correr, el archivo de candidatos supera las 450 lineas y el SUMMARY reporta el rendimiento real por capa. Si no llego a correr por falta de clave y de agente, el archivo mantiene sus 400 o mas lineas deterministas y el SUMMARY declara la situacion y que consultas quedaron pendientes.
  </acceptance_criteria>

  <done>
    La expansion con datos reales de Google funciona por los dos caminos, produce el mismo resultado por cualquiera de ellos, y todo lo consultado queda cacheado de forma que reprocesar no cuesta una sola llamada. Las capturas de resultados de busqueda quedan disponibles para la fase 13 sin repetir la pasada.
  </done>
</task>

</tasks>

<artifacts>
## Artefactos que produce este plan

**Modulos:**
- `seo-tools/src/keywords/seeds.ts` — exporta la extraccion de semillas desde el contenido de la aplicacion, la competencia y el CV
- `seo-tools/src/keywords/permute.ts` — exporta la permutacion pura de semilla por modificador por geo
- `seo-tools/src/keywords/expand.ts` — exporta la orquestacion de las cuatro capas, la deduplicacion y el filtro de relevancia
- `seo-tools/src/sources/serpapi.ts` — exporta sugerencias de autocompletado y resultados de busqueda geolocalizada, ambas a traves del seam de cache
- `seo-tools/src/commands/kw-seeds.ts` y `seo-tools/src/commands/kw-expand.ts` — implementados

**Subcomandos del CLI que quedan funcionales:**
- `kw:seeds`, con bandera de refresco
- `kw:expand`, con banderas de offline, refresco, solo planificar y confirmacion

**Datos commiteados:**
- `seo-tools/data/seeds.json` — snapshot de semillas con procedencia
- `seo-tools/data/modifiers.json` — cuatro familias de modificadores y geo
- `seo-tools/data/candidates.jsonl` — universo candidato normalizado y deduplicado

**Datos no commiteados pero reutilizables:**
- Capturas de resultados de busqueda de Lima en la cache, insumo directo de KWR-04 y COMP-03 en la fase 13

**Pruebas:**
- `seeds.test.ts`, `permute.test.ts`, `expand.test.ts`, `serpapi.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contenido de la aplicacion hacia tooling | Lectura de una sola vez de archivos que otro workstream esta editando en paralelo |
| API externa hacia disco | Respuestas de un proveedor de terceros que se persisten y despues se leen como datos confiables |
| Agente con herramienta MCP hacia cache | Un actor externo al proceso escribe archivos que el CLI va a leer como si fueran respuestas propias |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-21 | Information Disclosure | credencial de SerpApi en archivos de cache | high | mitigate | La credencial viaja como parametro de la peticion pero se excluye del envelope antes de persistir; criterio de aceptacion que busca la credencial dentro del directorio de cache y exige cero coincidencias; el directorio esta ignorado por git |
| T-12-22 | Tampering | relleno de cache desde un agente | high | mitigate | La clave la calcula siempre el CLI y el agente solo la consume; el subcomando de relleno valida que el cuerpo sea JSON y deduce el resultado del contenido; una clave que no corresponde a ninguna consulta pendiente se reporta |
| T-12-23 | Tampering | respuestas de la API tratadas como datos confiables | medium | mitigate | Validar la forma de la respuesta antes de indexar campos; una respuesta sin el arreglo esperado se persiste como vacio y no rompe la expansion |
| T-12-24 | Denial of Service | cuota de busquedas del proveedor | medium | mitigate | Cantidad estimada impresa y confirmacion explicita antes de gastar; sufijos dirigidos en lugar de expansion alfabetica, que multiplicaria el consumo por veintisiete; cache sin expiracion para que nada se consulte dos veces |
| T-12-25 | Tampering | lectura del contenido de la aplicacion | low | mitigate | Lectura de una sola vez a un snapshot commiteado; ningun otro modulo importa la aplicacion; criterio de aceptacion que cuenta las referencias y exige exactamente una |
| T-12-26 | Information Disclosure | mensajes de error de la fuente externa | medium | mitigate | Los errores nombran endpoint y parametros, nunca el valor de la credencial; como maximo su longitud y su prefijo |
</threat_model>

<verification>
Al cerrar el plan, todo lo siguiente debe ser cierto:

1. `cd seo-tools && npm run typecheck && env -u SERPAPI_API_KEY -u DINORANK_API_KEY npm test` sale con codigo 0.
2. `cd seo-tools && env -u SERPAPI_API_KEY npm run cli -- kw:expand --offline` sale con codigo 0, que es lo que prueba que no hubo ningun fallo de cache.
3. `seo-tools/data/candidates.jsonl` tiene 400 o mas lineas sin formas normalizadas repetidas.
4. `cd seo-tools && grep -rn "fetch(" src/sources --include='*.ts' | grep -v "//" | wc -l` imprime `0`.
5. `git status --porcelain -- src/` no reporta modificaciones.
6. `git diff --cached --name-only -- .secrets seo-tools/.cache seo-tools/src/cli.ts seo-tools/src/sheets .planning/workstreams/milestone | wc -l` imprime `0`.
7. `grep -rniE "ahrefs" seo-tools/src --include='*.ts' | grep -v "//" | wc -l` imprime `0`.
8. Ejecutar dos veces seguidas la extraccion de semillas y la expansion no cambia ningun archivo commiteado.
</verification>

<success_criteria>
- KWR-01 cumplido en su parte de universo: existen 400 o mas keywords candidatas del negocio completo del doctor, expandidas desde semillas por condicion, procedimiento, sintoma, especialidad y sede. Las metricas por keyword las cierra el plan 05.
- INFRA-03 verificado de punta a punta en una fuente real: reprocesar la expansion completa no consume ni una llamada.
- La fase queda desacoplada del workstream `milestone`: el tooling lee un snapshot, no la aplicacion.
- Las capturas de resultados de busqueda de Lima quedan cacheadas y disponibles para la fase 13 sin repetir la pasada.
</success_criteria>

<output>
Crear `.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-03-SUMMARY.md` al terminar.

Incluir de forma explicita:
- El conteo final por capa: permutacion, autocompletado y resultados de busqueda, antes y despues de deduplicar y filtrar.
- Cual de los dos caminos se uso para las capas con datos reales, y cuantas busquedas se gastaron.
- Si la expansion con datos reales no llego a correr, que consultas quedaron pendientes y con que clave, para retomarlas sin recalcular nada.
</output>
