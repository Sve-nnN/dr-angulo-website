---
phase: 12-instrumentaci-n-de-datos-y-universo-de-keywords
workstream: seo-keywords
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - seo-tools/src/sources/dinorank.ts
  - seo-tools/src/sources/dinorank.test.ts
  - seo-tools/src/keywords/enrich.ts
  - seo-tools/src/keywords/enrich.test.ts
  - seo-tools/src/commands/dino-probe.ts
  - seo-tools/src/commands/kw-enrich.ts
  - seo-tools/data/fixtures/dinorank-keyword-research-pe.json
  - seo-tools/data/fixtures/dinorank-auditoria-pe.json
  - seo-tools/data/fixtures/dinorank-canibalizaciones-pe.json
  - seo-tools/data/fixtures/dinorank-tfidf-pe.json
  - seo-tools/data/keywords.jsonl
  - seo-tools/README.md
autonomous: false
requirements: [INFRA-02, KWR-02]

user_setup:
  - service: dinorank
    why: "INFRA-02 y KWR-02 no se pueden cerrar sin una clave que la API acepte. La clave actual devuelve 401 en todos los endpoints probados y el problema esta del lado del proveedor, no del formato."
    env_vars:
      - name: DINORANK_API_KEY
        source: "Panel de DinoRank, seccion de API. Regenerar la clave y reemplazar el valor en .secrets/.env. Verificado el 2026-08-10 que no es error de formato: son 64 caracteres hexadecimales, la cabecera es la documentada, y una cabecera mal escrita devuelve un mensaje distinto."
    dashboard_config:
      - task: "Confirmar que el plan contratado incluye acceso a la API y cual es el techo de consumo mensual del endpoint de investigacion de keywords"
        location: "Panel de DinoRank, seccion de plan y consumo"

must_haves:
  truths:
    - "Existe una fixture commiteada con la respuesta real del endpoint de investigacion de keywords para Peru, y el parser esta escrito contra ella, no contra una suposicion."
    - "Las pruebas del parser corren sin ninguna clave, porque trabajan sobre las fixtures grabadas."
    - "El cliente cubre los cuatro endpoints que INFRA-02 exige, no solo el que esta fase consume."
    - "Cada keyword del universo trae volumen, CPC y competencia, o queda marcada de forma explicita como sin datos en la fuente, con su columna de origen declarandolo."
    - "Un rechazo de credencial nunca queda guardado en cache, asi que regenerar la clave y volver a correr funciona sin borrar nada."
    - "Recargar el Sheet despues del enriquecimiento actualiza las filas existentes y no agrega ninguna."
  artifacts:
    - "seo-tools/src/sources/dinorank.ts con los cuatro endpoints"
    - "seo-tools/data/fixtures/ con la respuesta real de cada endpoint probado"
    - "seo-tools/src/keywords/enrich.ts con el enriquecimiento por lotes y limite de concurrencia"
    - "seo-tools/src/commands/dino-probe.ts y kw-enrich.ts implementados"
    - "seo-tools/data/keywords.jsonl con las tres metricas rellenas"
    - "Tab Keyword Research del Sheet del cliente actualizado con las metricas"
  key_links:
    - "dino:probe hacia el parser: la fixture se graba antes de escribir una sola linea de parseo, porque la forma de la respuesta no esta documentada en ninguna parte"
    - "El sondeo con pais Peru y no con otro: la doc dice que Espana y Mexico se resuelven con un backend propio y el resto con un proveedor externo, asi que una fixture de otro pais daria un parser que falla en produccion"
    - "enrich.ts hacia el seam de cache del plan 01: una interrupcion a mitad de las 400 llamadas no obliga a repetir nada"
    - "keywords.jsonl hacia sheet:push del plan 02: el enriquecimiento rellena campos del esquema que el plan 04 ya dejo completo, sin migrar nada"
---

<objective>
Conectar la fuente primaria de metricas y cerrar el universo con volumen, CPC y competencia por keyword para Peru en espanol.

Purpose: este es el unico plan de la fase que depende de una credencial que hoy no funciona, y por eso esta aislado al final. La clave actual devuelve un rechazo en todos los endpoints probados y ya se descarto que sea error de forma. Todo lo demas de la fase cierra sin esta pieza: el guardarrail de build, el seam de cache, el escritor idempotente, el universo de 400 o mas y la clasificacion ya estan entregados por los planes 01 a 04. Ademas hay un problema de contrato: la documentacion del proveedor lista los parametros de entrada pero no publica ni un solo ejemplo de respuesta, y para Peru la consulta la resuelve un backend distinto del que atiende a Espana y Mexico. Escribir un parser contra una suposicion seria construir sobre arena, asi que el sondeo que graba la respuesta real es tarea obligatoria y previa.

Output: el cliente de los cuatro endpoints que INFRA-02 exige, las fixtures reales commiteadas, el universo enriquecido y el Sheet del cliente actualizado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/ROADMAP.md
@.planning/workstreams/seo-keywords/REQUIREMENTS.md
@.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-RESEARCH.md
@.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-04-SUMMARY.md
@seo-tools/data/keywords.jsonl
</context>

<constraints>
1. Nada se escribe bajo `src/`.
2. Nada se escribe en `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` ni bajo `.planning/workstreams/milestone/`.
3. No se toca `seo-tools/src/cli.ts`, ni `seo-tools/src/sheets/`, ni `seo-tools/src/cache.ts`, ni `seo-tools/src/keywords/classify.ts`: son propiedad de planes ya cerrados.
4. Ninguna llamada de red esquiva el seam de cache del plan 01.
5. El sondeo se hace con pais Peru. Nunca con otro pais: la doc dice que Espana y Mexico se resuelven con el servidor propio del proveedor y el resto de los paises con un proveedor externo, asi que la forma de la respuesta puede diferir y una fixture del pais equivocado produce un parser que falla en produccion.
6. Ninguna fixture commiteada puede contener la clave de API ni ningun token de sesion.
7. La herramienta de dificultad y potencial de trafico sigue fuera de la fase. Este plan no la reincorpora: las dos columnas siguen con el valor literal que declara que no se consultaron.
</constraints>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 1: Clave de DinoRank funcional</name>

  <files>.secrets/.env</files>

  <read_first>
    - `12-CONTEXT.md`, seccion *Estado de credenciales al cerrar el discuss*, con el detalle de todo lo que ya se descarto
    - `12-RESEARCH.md`, seccion *Standard Stack* subseccion de DinoRank, con la tabla de parametros de los cuatro endpoints, y *Pitfall 5*
    - `seo-tools/src/config.ts` del plan 01
  </read_first>

  <what-built>
    Los planes 01 a 04 dejaron entregada toda la fase menos las metricas por keyword: el guardarrail de build, el seam de cache con modo offline, el escritor idempotente verificado contra el Sheet real, el universo de 400 o mas keywords y su clasificacion por intencion y etapa. El tab `Keyword Research` ya esta cargado. Lo unico que falta es volumen, CPC y competencia, y eso necesita una clave que la API acepte.
  </what-built>

  <how-to-verify>
    Explicar a Juan el estado exacto, sin ambiguedad:

    La clave que hoy esta en el archivo de secretos devuelve un rechazo de autorizacion con mensaje de clave invalida en todos los endpoints probados. Ya se descarto que sea un error de forma: son 64 caracteres hexadecimales, la cabecera usada es la documentada, se probaron ademas la variante de portador y el paso por parametro de consulta, y una cabecera mal escrita devuelve un mensaje distinto, de clave ausente en vez de invalida. El problema esta del lado del proveedor: clave sin activar, plan sin acceso a la API, o clave de otra cuenta.

    Pedir dos cosas:

    1. Regenerar la clave desde el panel de DinoRank y reemplazar el valor en `.secrets/.env`.
    2. Confirmar que el plan contratado incluye acceso a la API y cual es el techo mensual de consumo del endpoint de investigacion de keywords. La documentacion aclara que los endpoints de auditoria y de canibalizaciones no consumen cuota, pero no dice cuanto consume el de investigacion ni sobre que techo, y esta fase necesita cerca de 400 llamadas.

    Verificar la clave nueva con una sola llamada antes de seguir, y confirmar que responde algo distinto de un rechazo de autorizacion.

    **Si la clave sigue rechazada:** no bloquear la fase entera. Detener este plan, dejarlo registrado como pendiente en el SUMMARY, y reportar que el resto de la fase esta entregado. El dataset ya tiene el esquema completo con las tres metricas marcadas como pendientes, asi que retomar este plan mas adelante no obliga a rehacer nada ni a migrar el Sheet.
  </how-to-verify>

  <resume-signal>
    Responder "clave lista" cuando el valor nuevo este en `.secrets/.env`, o "sigue rechazada" para cerrar la fase sin las metricas y dejar este plan pendiente.
  </resume-signal>

  <acceptance_criteria>
    - Una llamada de prueba al endpoint de investigacion de keywords con la clave nueva devuelve un codigo distinto de 401 y de 403.
    - `git check-ignore -q .secrets/.env` sale con codigo 0: el archivo sigue fuera del control de versiones.
    - `git diff --cached --name-only -- .secrets | wc -l` imprime `0`.
    - El valor de la clave no aparece en ningun log, mensaje ni archivo commiteado. Como maximo se reporta su longitud y su prefijo.
    - Si la clave sigue rechazada, el SUMMARY declara el plan como pendiente y confirma que los planes 01 a 04 estan cerrados.
  </acceptance_criteria>

  <done>
    Existe una clave que la API acepta, o la decision explicita de cerrar la fase sin las metricas y dejar este plan pendiente, con el esquema del dataset ya preparado para retomarlo.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: Sondeo de los endpoints y grabacion de las fixtures reales</name>

  <files>seo-tools/src/commands/dino-probe.ts, seo-tools/data/fixtures/dinorank-keyword-research-pe.json, seo-tools/data/fixtures/dinorank-auditoria-pe.json, seo-tools/data/fixtures/dinorank-canibalizaciones-pe.json, seo-tools/data/fixtures/dinorank-tfidf-pe.json</files>

  <read_first>
    - `12-RESEARCH.md`, seccion *Standard Stack* subseccion de DinoRank con la tabla de parametros de los cuatro endpoints, *Pitfall 4*, el ejemplo *C6* y la seccion *Open Questions* punto 1
    - `seo-tools/src/cache.ts`, `seo-tools/src/http.ts` y `seo-tools/src/config.ts` del plan 01
    - `12-CONTEXT.md`, parrafo sobre construir el cliente contra el contrato documentado
  </read_first>

  <precondition>
    La clave nueva esta en `.secrets/.env` y la tarea 1 confirmo que la API responde algo distinto de un rechazo de autorizacion. Si no se cumple, detenerse: sin respuesta real no hay fixture, y sin fixture el parser de la tarea 3 seria una suposicion.
  </precondition>

  <reversibility rating="reversible">
    Las fixtures son archivos de datos grabados. Volver a sondear con la bandera de refresco las regenera.
  </reversibility>

  <action>
    Implementar el subcomando de sondeo y correrlo antes de escribir una sola linea de parseo. Este orden no es una preferencia de estilo: la documentacion del proveedor lista los parametros de los cuatro endpoints pero no publica ni un ejemplo de respuesta, no expone especificacion de la API en ninguna de las rutas habituales, y solo describe el bloque de datos como el analisis, sin enumerar sus campos.

    El subcomando recibe el endpoint y los parametros, hace una unica llamada a traves del seam de cache y graba la respuesta completa en `data/fixtures/` con nombre que incluya endpoint y pais. Ademas imprime un inventario de la respuesta: las claves de primer nivel, el tipo de cada una y, para el bloque de datos, las claves de segundo nivel con su tipo y un valor de ejemplo. Ese inventario es lo que permite escribir el parser de la tarea 3 mirando lo que hay, en lugar de adivinar.

    Sondear con pais Peru y idioma espanol, con una keyword cabecera real del negocio. Nunca con otro pais. La doc dice que Espana y Mexico se resuelven con el servidor de visibilidad propio del proveedor y que el resto de los paises usa un proveedor externo, asi que la latencia y probablemente la forma del bloque de datos difieren. Si el campo que declara la fuente en la respuesta de Peru resulta distinto del que devolveria otro pais, dejarlo anotado: puede que haga falta una rama de parseo por origen.

    Sondear los cuatro endpoints que INFRA-02 exige, no solo el que esta fase consume. Los de auditoria y canibalizaciones son especialmente baratos de sondear porque la doc dice explicitamente que no consumen cuota, y las fases 14 y 15 los van a necesitar: la deteccion de canibalizacion de MAP-02 y la auditoria de duplicados de ONPAGE-05 salen de ahi. Grabar tambien esas fixtures ahora, mientras la clave funciona, ahorra descubrir su contrato en medio de una fase posterior.

    Antes de commitear cualquier fixture, revisarla campo por campo y quitar cualquier token, identificador de sesion o eco de la credencial que el proveedor pueda haber incluido en la respuesta. Estas fixtures se commitean porque son la unica documentacion real del contrato, y quedan a la vista de todo el repositorio.

    Actualizar la seccion correspondiente del README del paquete con el contrato real descubierto: los campos del bloque de datos, sus tipos, y cual de ellos corresponde a volumen, cual a CPC y cual a competencia.
  </action>

  <verify>
    <automated>cd seo-tools && npm run typecheck && npm run cli -- dino:probe --endpoint keyword-research --country pe && node -e "const f=JSON.parse(require('node:fs').readFileSync('data/fixtures/dinorank-keyword-research-pe.json','utf8')); const s=JSON.stringify(f); if(!/\"country\"\s*:\s*\"pe\"/i.test(s)) throw new Error('la fixture no es de Peru'); if(process.env.DINORANK_API_KEY && s.includes(process.env.DINORANK_API_KEY)) throw new Error('la fixture contiene la credencial'); console.log('fixture OK, claves de primer nivel: '+Object.keys(f.response||f).join(', '));"</automated>
  </verify>

  <acceptance_criteria>
    - `cd seo-tools && npm run cli -- dino:probe --endpoint keyword-research --country pe` sale con codigo 0 y graba la fixture.
    - La fixture del endpoint de investigacion de keywords existe, esta commiteada, y sus parametros de peticion declaran pais Peru e idioma espanol.
    - El inventario impreso por el sondeo identifica de forma explicita que campo del bloque de datos corresponde a volumen, cual a CPC y cual a competencia. Si alguno no existe en la respuesta real, queda registrado en el SUMMARY como hallazgo, porque KWR-02 depende de los tres.
    - Existen las fixtures de los otros tres endpoints, o el SUMMARY explica cual no se pudo sondear y por que.
    - Ninguna fixture contiene la clave de API: `grep -rl "$DINORANK_API_KEY" seo-tools/data/fixtures 2>/dev/null | wc -l` imprime `0` con la variable definida.
    - El README del paquete documenta el contrato real descubierto, con los nombres de campo tal como llegan.
    - `git diff --cached --name-only -- src/ .secrets seo-tools/src/cli.ts seo-tools/src/sheets | wc -l` imprime `0` antes de commitear.
  </acceptance_criteria>

  <done>
    La forma real de la respuesta de los endpoints, para Peru, esta grabada en fixtures commiteadas y documentada, de modo que el parser de la tarea 3 se escribe contra un contrato conocido y sus pruebas corren sin clave.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Cliente de los cuatro endpoints, enriquecimiento del universo y recarga del Sheet</name>

  <files>seo-tools/src/sources/dinorank.ts, seo-tools/src/sources/dinorank.test.ts, seo-tools/src/keywords/enrich.ts, seo-tools/src/keywords/enrich.test.ts, seo-tools/src/commands/kw-enrich.ts, seo-tools/data/keywords.jsonl</files>

  <read_first>
    - Las fixtures grabadas en la tarea 2 y el inventario que imprimio el sondeo
    - `12-RESEARCH.md`, subseccion de DinoRank con la tabla de los cuatro endpoints, *Pitfall 4*, *Pitfall 5*, *Pitfall 6*, el ejemplo *C6* y la seccion *Open Questions* punto 3
    - `seo-tools/src/cache.ts` y `seo-tools/src/http.ts` del plan 01, en particular la tabla de que se persiste
    - `seo-tools/data/keywords.jsonl` y `seo-tools/data/sheet-columns.json`
  </read_first>

  <precondition>
    Las fixtures de la tarea 2 existen y el inventario identifico los campos de volumen, CPC y competencia. Sin eso el parser no tiene contra que escribirse.
  </precondition>

  <reversibility rating="costly">
    El enriquecimiento consume cuota del proveedor, que es un recurso limitado y desconocido en su techo. Por eso la primera corrida va acotada con la bandera de limite para medir el consumo real, y la cache convierte esa prudencia en gratis: lo consultado queda consultado. La recarga del Sheet es reversible volviendo a cargar.
  </reversibility>

  <behavior>
    - El parser aplicado a la fixture real devuelve volumen, CPC y competencia con los tipos correctos.
    - El parser aplicado a una respuesta con el bloque de datos vacio devuelve los tres campos marcados como sin datos en la fuente, sin lanzar excepcion.
    - El parser aplicado a una respuesta con un campo faltante marca solo ese campo como sin datos y conserva los demas.
    - Un rechazo de credencial produce un mensaje accionable que nombra el codigo, dice que hacer y aclara que subcomandos siguen funcionando sin esa clave, y no deja nada escrito en cache.
    - Una respuesta de limite de tasa o de error de servidor se reintenta con retroceso exponencial y no se persiste.
    - El enriquecimiento respeta el limite de concurrencia configurado y nunca lanza las 400 llamadas a la vez.
    - Una interrupcion a mitad del enriquecimiento no obliga a repetir las keywords ya consultadas: la siguiente corrida las resuelve desde cache.
    - Con la bandera de limite, se consultan solo las primeras keywords indicadas y el resto queda intacto.
    - Una keyword sin datos en la fuente conserva su fila y queda marcada, no se descarta.
    - Las dos columnas de metricas diferidas conservan su valor literal de no consultado despues del enriquecimiento.
  </behavior>

  <action>
    Implementar `sources/dinorank.ts` con los cuatro endpoints que INFRA-02 exige: investigacion de keywords, TF-IDF semantico, auditoria on-page y canibalizaciones. Los dos ultimos no se consumen en esta fase pero las fases 14 y 15 dependen de ellos, y escribirlos ahora, con las fixtures frescas y la clave funcionando, evita descubrir su contrato en medio de una fase posterior. Todos pasan por el seam de cache del plan 01, sin excepcion.

    El parseo se escribe contra las fixtures de la tarea 2 y es tolerante por diseno: un campo ausente marca ese campo como sin datos en la fuente y no rompe la fila. La distincion entre no consultado y sin datos en la fuente importa para la fase 13, porque una keyword geolocalizada sin volumen medible es senal positiva de oportunidad para un dominio nuevo, no un dato faltante.

    El manejo del rechazo de credencial se implementa tal como lo describe el ejemplo *C6*: mensaje accionable en espanol neutro que nombra el codigo, indica regenerar la clave desde el panel, y aclara que subcomandos siguen funcionando sin ella. Nada de trazas crudas. Y nunca se persiste: la tabla del seam del plan 01 ya lo prohibe, y es lo que evita que un rechazo quede congelado y siga fallando despues de que Juan arregle la clave.

    Implementar `keywords/enrich.ts`. El endpoint de investigacion recibe una keyword por llamada, asi que el universo son alrededor de 400 peticiones. Limitar la concurrencia a tres o cuatro con el limitador ya instalado, con retroceso exponencial ante limite de tasa y ante errores de servidor, y tiempo limite por peticion. Cachear por keyword para que una interrupcion no obligue a repetir nada.

    Empezar acotado. Nadie sabe el techo de consumo del plan de Juan, asi que la primera corrida usa la bandera de limite con unas veinte keywords para medir el consumo real, se revisa el saldo en el panel, y recien despues se corre el universo completo. La cache hace que esa prudencia no cueste nada: lo consultado queda consultado.

    Al terminar, actualizar `data/keywords.jsonl` rellenando volumen, CPC y competencia con su columna de origen cada uno, y recargar el tab `Keyword Research` con el subcomando del plan 02. Esa recarga es la prueba real de SHEET-06 en su caso mas exigente: las filas existen, cambian de valor, y la cantidad de filas no puede moverse ni una.
  </action>

  <verify>
    <automated>cd seo-tools && npm run typecheck && env -u DINORANK_API_KEY npm test && env -u DINORANK_API_KEY npm run cli -- kw:enrich --offline && node -e "const L=require('node:fs').readFileSync('data/keywords.jsonl','utf8').trim().split('\n').filter(Boolean); let con=0; for(const l of L){const o=JSON.parse(l); if(o.kd!=='no_consultado'||o.trafficPotential!=='no_consultado') throw new Error('metrica diferida alterada en '+o.keywordKey); if(o.volumeSource==='dinorank'||o.volumeSource==='sin_datos') con++;} const pct=Math.round(con*100/L.length); if(pct<95) throw new Error('cobertura de metricas insuficiente: '+pct+'%'); console.log('cobertura '+pct+'% sobre '+L.length+' keywords');" && npm run cli -- sheet:push --tab "Keyword Research" && npm run cli -- sheet:push --tab "Keyword Research"</automated>
  </verify>

  <acceptance_criteria>
    - `cd seo-tools && env -u DINORANK_API_KEY npm test` sale con codigo 0: todas las pruebas del parser corren sobre las fixtures, sin clave.
    - Existe una prueba por cada uno de los diez comportamientos declarados.
    - Los cuatro endpoints estan implementados y exportados, y cada uno tiene al menos una prueba sobre su fixture.
    - `cd seo-tools && grep -rn "fetch(" src/sources/dinorank.ts | grep -v "//" | wc -l` imprime `0`: el cliente llama a la red a traves del envoltorio del plan 01.
    - El 95 por ciento o mas de las lineas del dataset tienen la columna de origen del volumen resuelta, ya sea con la fuente o con la marca de sin datos. Ninguna queda pendiente.
    - Ninguna linea del dataset tiene alteradas las dos columnas de metricas diferidas: siguen con su valor literal de no consultado.
    - La primera corrida acotada esta registrada en el SUMMARY con el consumo medido, y la corrida completa se hizo despues de revisar ese numero.
    - La recarga del Sheet reporta cero inserciones y la cantidad de filas del tab no cambia, medido contra el documento real y registrado en el SUMMARY con los dos numeros.
    - Juan puede abrir el Sheet y ver volumen, CPC y competencia con su columna de origen al lado.
    - `git diff --cached --name-only -- src/ .secrets seo-tools/.cache seo-tools/src/cli.ts seo-tools/src/sheets | wc -l` imprime `0` antes de commitear.
  </acceptance_criteria>

  <done>
    Cada keyword del universo trae volumen, CPC y competencia para Peru en espanol con la fuente marcada por columna, o queda marcada de forma explicita como sin datos en la fuente; el cliente cubre los cuatro endpoints que las fases 14 y 15 van a necesitar; y la recarga del Sheet no duplico ni una fila.
  </done>
</task>

</tasks>

<artifacts>
## Artefactos que produce este plan

**Modulos:**
- `seo-tools/src/sources/dinorank.ts` — exporta investigacion de keywords, TF-IDF, auditoria on-page y canibalizaciones, todos a traves del seam de cache
- `seo-tools/src/keywords/enrich.ts` — exporta el enriquecimiento por lotes con limite de concurrencia, reintento y reanudacion desde cache
- `seo-tools/src/commands/dino-probe.ts` y `seo-tools/src/commands/kw-enrich.ts` — implementados

**Subcomandos del CLI que quedan funcionales:**
- `dino:probe`, con banderas de endpoint, pais y refresco
- `kw:enrich`, con banderas de limite, offline, refresco y solo planificar

**Datos commiteados:**
- `seo-tools/data/fixtures/dinorank-keyword-research-pe.json` y las fixtures de los otros tres endpoints
- `seo-tools/data/keywords.jsonl` actualizado con las tres metricas y sus columnas de origen

**Entregable externo:**
- Tab `Keyword Research` del Sheet del cliente actualizado con volumen, CPC y competencia

**Pruebas:**
- `seo-tools/src/sources/dinorank.test.ts`, `seo-tools/src/keywords/enrich.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Credencial hacia proveedor externo | Una clave de API cruza en cada una de las 400 peticiones y no puede aparecer en log, cache ni fixture |
| Respuesta de contrato desconocido hacia dataset | La forma de la respuesta no esta documentada; todo campo es entrada no confiable hasta validarse |
| Fixture commiteada hacia repositorio | Una respuesta real del proveedor queda a la vista de todo el repositorio |
| Dataset enriquecido hacia documento del cliente | Segunda escritura masiva sobre un entregable que el cliente abre |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-41 | Information Disclosure | credencial en fixtures commiteadas | critical | mitigate | Revision campo por campo antes de commitear, mas criterio de aceptacion que busca el valor de la credencial dentro del directorio de fixtures y exige cero coincidencias |
| T-12-42 | Information Disclosure | mensajes de error del cliente | high | mitigate | El mensaje de rechazo se construye con texto fijo y no interpola la clave; como maximo reporta longitud y prefijo |
| T-12-43 | Tampering | rechazo de credencial congelado en cache | high | mitigate | Heredada del plan 01: los rechazos de autorizacion nunca se persisten, con prueba dedicada; es lo que permite que regenerar la clave arregle el problema sin borrar nada |
| T-12-44 | Denial of Service | cuota del proveedor agotada por una corrida a ciegas | high | mitigate | Primera corrida acotada con bandera de limite para medir consumo real, revision del saldo antes de la corrida completa, limite de concurrencia de tres o cuatro, y cache sin expiracion |
| T-12-45 | Tampering | respuesta de contrato desconocido indexada sin validar | high | mitigate | Parser escrito contra fixtures reales, tolerante a campos ausentes, con prueba por cada forma degradada; un campo faltante marca ese campo y no rompe la fila |
| T-12-46 | Denial of Service | limite de tasa del proveedor | medium | mitigate | Retroceso exponencial ante limite de tasa y errores de servidor, tiempo limite por peticion, y ningun reintento sobre errores de credencial |
| T-12-47 | Tampering | recarga masiva sobre el documento del cliente | medium | mitigate | El escritor del plan 02 actualiza en su posicion y no inserta; criterio de aceptacion que compara la cantidad de filas antes y despues contra el documento real |
</threat_model>

<verification>
Al cerrar el plan, todo lo siguiente debe ser cierto:

1. `cd seo-tools && npm run typecheck && env -u DINORANK_API_KEY npm test` sale con codigo 0.
2. Las fixtures de los cuatro endpoints existen y estan commiteadas, o el SUMMARY explica cual falta y por que.
3. Ninguna fixture contiene la credencial.
4. `seo-tools/data/keywords.jsonl` tiene el 95 por ciento o mas de sus lineas con la columna de origen del volumen resuelta.
5. Las dos columnas de metricas diferidas conservan su valor literal de no consultado en el cien por ciento de las lineas.
6. Dos cargas seguidas sobre el tab `Keyword Research` dejan la misma cantidad de filas.
7. `cd seo-tools && grep -rn "fetch(" src/sources --include=*.ts | grep -v "//" | wc -l` imprime `0`.
8. `grep -rniE "ahrefs" seo-tools/src --include=*.ts | grep -v "//" | wc -l` imprime `0`.
9. `git diff --cached --name-only -- src/ .secrets seo-tools/.cache .planning/workstreams/milestone | wc -l` imprime `0`.
</verification>

<success_criteria>
- INFRA-02 cumplido: existe un cliente de la fuente primaria que consulta los cuatro endpoints exigidos, con la clave fuera del control de versiones y las pruebas corriendo sin ella.
- KWR-02 cumplido en su redaccion vigente: cada keyword del universo trae volumen, CPC y competencia para Peru en espanol con la fuente marcada por columna, y las dos metricas diferidas existen en el dataset y en el Sheet con el valor que declara que no se consultaron.
- INFRA-03 verificado en la fuente mas cara: reprocesar el universo completo no consume ni una llamada.
- SHEET-06 verificado en su caso mas exigente: filas que cambian de valor se actualizan en su posicion y la cantidad de filas no se mueve.
- Las fixtures de auditoria y canibalizaciones quedan grabadas, lo que adelanta trabajo de MAP-02 en la fase 14 y de ONPAGE-05 en la fase 15.
</success_criteria>

<output>
Crear `.planning/workstreams/seo-keywords/phases/12-instrumentaci-n-de-datos-y-universo-de-keywords/12-05-SUMMARY.md` al terminar.

Incluir de forma explicita:
- El contrato real descubierto: campos del bloque de datos con su tipo y cual corresponde a cada metrica.
- Si la respuesta para Peru difiere de lo que la documentacion sugiere para otros paises, y si hizo falta una rama de parseo distinta.
- El consumo medido en la corrida acotada y el consumo total del universo.
- Cuantas keywords quedaron marcadas como sin datos en la fuente, que para la fase 13 es senal de oportunidad y no de dato faltante.
- Los dos conteos de filas del tab, antes y despues de la recarga.
- Si el plan quedo pendiente por credencial, que exactamente falta y que ya esta entregado.
</output>
