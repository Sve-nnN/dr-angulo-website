---
phase: 15-paquete-on-page-por-url
workstream: seo-keywords
plan: 05
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - seo-tools/data/copy-sedes.json
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-clinica-ricardo-palma.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-clinica-tezza.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-consultorio-privado.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-sanna-la-molina.md
autonomous: true
requirements: [ONPAGE-03, ONPAGE-04, ONPAGE-06]
must_haves:
  truths:
    - "Las cuatro sedes vigentes tienen su ficha redactada de punta a punta, cada una peleando la consulta que el mapa le asignó."
    - "Ningún dato de dirección, horario o teléfono se inventa: sale del contenido publicado o queda marcado como pendiente de confirmación."
    - "Ninguna página afirma una relación con la clínica que no sea la real: el doctor atiende ahí."
    - "Montefiori no aparece en ninguna de las cuatro páginas."
    - "Las cuatro fichas no se canibalizan entre sí ni le devuelven a `/sedes` un rol de posicionamiento que la fase 14 le quitó a propósito."
  artifacts:
    - seo-tools/data/copy-sedes.json
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-clinica-ricardo-palma.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-clinica-tezza.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-consultorio-privado.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-sanna-la-molina.md
  key_links:
    - "Los datos operativos de cada sede se LEEN de `src/content/locations.ts` y `location-pages.ts`; ninguno se compone."
    - "`ymyl.ts` del plan 15-02 es la compuerta; este plan no define criterios propios."
    - "El title, la meta y el H1 vienen de `data/onpage.json` y no se reescriben acá."
---

<objective>
Redactar las fichas de las cuatro sedes vigentes: consultorio Surco, Ricardo Palma, Sanna La Molina
y Padre Luis Tezza.

Propósito: son las URLs que responden la consulta de un paciente que ya sabe a qué clínica quiere
ir, y hoy ninguna de las cuatro está escrita para eso. La sede de Ricardo Palma además tiene una
búsqueda relacionada verificada en la SERP que hoy no responde ninguna URL del sitio.

Salida: `data/copy-sedes.json` completo y cuatro documentos en `paquetes/`.

Decisiones que gobiernan este plan: D-05, D-08, D-09, D-10, D-11, D-12, D-14, D-16. El registro
completo está en el `<objective>` de `15-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-01-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-02-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-HANDOFF-V11.md
@seo-tools/data/onpage-serp.json
@seo-tools/data/onpage.json
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 15-01.
**Presupuesto de SerpApi de este plan: cero.** `url-map.jsonl` sigue de solo lectura y `src/` de
la aplicación no se toca.

**Alcance exacto de este plan, cuatro URLs, todas con mínimo de 500 palabras:**

| URL | Keyword primaria | Intención medida | Tipo que exige la SERP |
|---|---|---|---|
| `/sedes/clinica-ricardo-palma` | cirujano de columna clínica ricardo palma | Transaccional | Página de servicio |
| `/sedes/clinica-tezza` | ortopedia infantil clínica tezza | Transaccional | Página de servicio |
| `/sedes/sanna-la-molina` | cirujano de columna clínica sanna | Comercial | Perfil o ficha |
| `/sedes/consultorio-privado` | cirugía de columna surco | Comercial | Sin formato dominante |

**Hechos medidos, ya verificados, no hay que redescubrirlos:**

- **Las cuatro sedes vigentes son consultorio Surco, Ricardo Palma, Sanna La Molina y Padre Luis
  Tezza. Montefiori no existe en este sitio: el doctor ya no atiende ahí** (D-11).
- La sede de Ricardo Palma tiene prioridad de datos: la búsqueda relacionada
  `traumatologo especialista en columna clínica ricardo palma` está verificada en la SERP de Lima
  y hoy no la responde ninguna URL del sitio.
- `/sedes` quedó como hub de navegación **sin keyword primaria a propósito**, por decisión de Juan
  del 2026-08-11, precisamente para dejar de canibalizar a estas cuatro. Se enlaza con anchor de
  navegación y no de keyword.
- El tipo de página de `sanna la molina` lo domina una red social, y el de `consultorio privado`
  no lo domina nadie. Eso no es un hueco del dato: es lo que la SERP devolvió, y las dos fichas se
  escriben sabiéndolo en vez de forzarles el formato de las otras dos.
- Los datos operativos de cada sede están en `src/content/locations.ts` y
  `src/content/location-pages.ts`, con los slugs `consultorio-privado`, `clinica-ricardo-palma`,
  `sanna-la-molina` y `clinica-tezza`. Los dos archivos en SOLO LECTURA.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ricardo Palma y Padre Luis Tezza, las dos fichas transaccionales</name>
  <files>seo-tools/data/copy-sedes.json</files>
  <read_first>
    - `seo-tools/data/onpage-serp.json` y `data/onpage.json`, filas
      `/sedes/clinica-ricardo-palma` y `/sedes/clinica-tezza`.
    - `src/content/locations.ts` y `src/content/location-pages.ts` completos, en solo lectura: de
      ahí salen dirección, referencias de cómo llegar y todo dato operativo.
    - `seo-tools/data/url-map.jsonl`, las dos filas con sus cinco secundarias cada una.
    - `.../paquetes/servicios-hernia-discal.md`: el molde aprobado por Juan.
  </read_first>
  <behavior>
    - La ficha de Ricardo Palma responde explícitamente la consulta de traumatólogo especialista en
      columna en esa clínica, que hoy ninguna URL del sitio responde.
    - La ficha de Tezza queda centrada en ortopedia infantil, que es su primaria, sin volverse una
      segunda página de ortopedia infantil general.
    - Todo dato de dirección, horario, teléfono o cómo llegar sale del contenido publicado y trae
      su fuente declarada; lo que no esté publicado se marca como pendiente de confirmación en vez
      de completarse.
    - Ninguna de las dos afirma una relación institucional que no sea la real: el doctor atiende
      ahí, la clínica no es suya.
    - Al menos el 80 % de las entidades obligatorias de cada URL aparecen en su texto.
    - Cada secundaria del mapa queda cubierta por alguna sección de su URL.
    - Cada sección clínica va marcada como pendiente de aprobación del doctor.
    - Las dos páginas pasan la compuerta de humanización sin hallazgos.
  </behavior>
  <action>
    Redactar las dos fichas dentro de `data/copy-sedes.json`, mínimo 500 palabras cada una,
    siguiendo la jerarquía de ficha de sede que `data/onpage-serp.json` ya fijó: dónde queda, cómo
    llegar, qué se atiende ahí, horarios y cómo se agenda.

    **Ricardo Palma** es la que más datos tiene a favor y la que más hay que cuidar. Su búsqueda
    relacionada verificada es la de un paciente que ya eligió la clínica y busca al especialista de
    columna adentro. La ficha tiene que responder eso en el primer párrafo, no en el quinto.

    **Padre Luis Tezza** pelea ortopedia infantil en esa clínica. El riesgo propio es que se le
    escape el foco y termine siendo una segunda página de ortopedia infantil general, compitiendo
    contra `/servicios/ortopedia-infantil`. La diferencia es el terreno: la página de servicio
    explica la especialidad y la ficha explica atenderse de esa especialidad **ahí**.

    Sobre los datos operativos hay una regla que no se negocia y que en una ficha de sede pesa más
    que en cualquier otra página de la fase: **una dirección inventada es peor que una afirmación
    médica inflada, porque manda a un paciente a un lugar equivocado**. Todo dato sale del
    contenido publicado, con su fuente declarada en el dataset. Lo que no esté publicado se marca
    como pendiente de confirmación y v1.1 lo completa antes de publicar, con el dato confirmado por
    el doctor o por la clínica.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-sedes.json --url /sedes/clinica-ricardo-palma && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-sedes.json --url /sedes/clinica-tezza</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'),s=require('./data/onpage-serp.json'); const u=['/sedes/clinica-ricardo-palma','/sedes/clinica-tezza']; const mal=u.filter(x=>{const p=(d.paginas||d).find(y=>y.url===x); const min=s.urls.find(y=>y.url===x).minimoDePalabras; return p.secciones.flatMap(y=>y.parrafos).join(' ').split(/\s+/).filter(Boolean).length<min;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const p=(d.paginas||d).filter(x=>['/sedes/clinica-ricardo-palma','/sedes/clinica-tezza'].includes(x.url)); const mal=p.flatMap(x=>x.datosOperativos||[]).filter(o=>!o.fuente||o.fuente.trim()===''); process.stdout.write(String(mal.length))")" -eq 0`
      — todo dato operativo declara de dónde salió o queda marcado como pendiente. Ninguno se
      compone.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const t=(d.paginas||d).flatMap(p=>p.secciones.flatMap(s=>s.parrafos.concat([s.titulo]))).join(' '); process.stdout.write(String(/montefiori/i.test(t)?1:0))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'),s=require('./data/onpage-serp.json'); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const u=['/sedes/clinica-ricardo-palma','/sedes/clinica-tezza']; const mal=u.filter(x=>{const p=(d.paginas||d).find(y=>y.url===x); const e=s.urls.find(y=>y.url===x).entidades; const t=n(p.secciones.flatMap(y=>y.parrafos.concat([y.titulo])).join(' ')); return 100*e.filter(y=>t.includes(n(y.termino))).length/e.length<80;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); let mal=0; for(const x of ['/sedes/clinica-ricardo-palma','/sedes/clinica-tezza']){const p=(d.paginas||d).find(y=>y.url===x); const cub=new Set(p.secciones.flatMap(y=>y.keywordsCubiertas||[])); mal+=(m.find(r=>r.url===x).secundarias||[]).filter(k=>!cub.has(k)).length;} process.stdout.write(String(mal))")" -eq 0`
    - Las dos compuertas devuelven cero hallazgos y salen con estado 0.
    - `test "$(shasum -a 256 seo-tools/data/url-map.jsonl | cut -d' ' -f1)" = "ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5"`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Las dos fichas transaccionales están redactadas dentro de contrato, con todo dato operativo
    respaldado o marcado como pendiente, y Ricardo Palma responde en el primer párrafo la búsqueda
    que hoy no responde nadie.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sanna La Molina y el consultorio de Surco, con la SERP que les tocó</name>
  <files>seo-tools/data/copy-sedes.json</files>
  <read_first>
    - `seo-tools/data/onpage-serp.json` y `data/onpage.json`, filas `/sedes/sanna-la-molina` y
      `/sedes/consultorio-privado`.
    - `seo-tools/data/page-type-map.json`, las dos cabezas: el reparto completo de su top 10 y su
      nivel de confianza.
    - `src/content/locations.ts` y `src/content/location-pages.ts`, en solo lectura.
  </read_first>
  <behavior>
    - La ficha de Sanna está escrita como perfil, con datos concretos arriba, porque su top 10 lo
      domina una red social y ese es el formato con el que compite.
    - La ficha del consultorio de Surco declara en el paquete que su SERP no tiene formato
      dominante y con qué criterio se eligió el formato de ficha.
    - Todo dato operativo sale del contenido publicado con su fuente, o queda pendiente.
    - Al menos el 80 % de las entidades obligatorias de cada URL aparecen en su texto.
    - Cada secundaria del mapa queda cubierta por alguna sección de su URL.
    - Ninguna de las cuatro fichas repite el título de sección de otra: cuatro páginas del mismo
      tipo se canibalizan si se escriben con la misma plantilla rellenada.
    - Las dos páginas pasan la compuerta de humanización sin hallazgos.
  </behavior>
  <action>
    Redactar las dos fichas restantes dentro de `data/copy-sedes.json`, mínimo 500 palabras cada
    una.

    **Sanna La Molina** enfrenta una SERP donde el resultado dominante es una red social. Eso dice
    algo concreto sobre qué está premiando Google ahí: perfil, no artículo. La ficha se escribe con
    esa forma, los datos concretos primero y la prosa después.

    **Consultorio de Surco** es el caso donde la SERP no resolvió: ningún tipo domina su top 10.
    Cuando el dato no alcanza, la salida correcta es declararlo y elegir con criterio explícito, no
    disimularlo eligiendo en silencio. El paquete de esta URL lleva escrito que el formato de ficha
    se eligió por coherencia con las otras tres sedes y no porque la SERP lo exigiera, para que
    quien lo lea sepa qué parte es medición y qué parte es decisión.

    El riesgo que comparten las cuatro fichas y que se cierra en esta tarea: escribirlas con la
    misma plantilla rellenada las pone a competir entre sí, que es exactamente lo que la fase 14
    resolvió sacándole la keyword a `/sedes`. Cada ficha tiene que decir algo propio de su sede
    —qué se atiende ahí y no en las otras, cómo se llega desde su distrito, qué tipo de paciente
    va a esa— y no las mismas cinco secciones con el nombre cambiado.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-sedes.json --url /sedes/sanna-la-molina && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-sedes.json --url /sedes/consultorio-privado</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'),s=require('./data/onpage-serp.json'); const u=['/sedes/sanna-la-molina','/sedes/consultorio-privado']; const mal=u.filter(x=>{const p=(d.paginas||d).find(y=>y.url===x); const min=s.urls.find(y=>y.url===x).minimoDePalabras; return p.secciones.flatMap(y=>y.parrafos).join(' ').split(/\s+/).filter(Boolean).length<min;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const ps=(d.paginas||d).filter(p=>p.url.startsWith('/sedes/')); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const t=ps.flatMap(p=>p.secciones.map(s=>n(s.titulo))); process.stdout.write(String(t.length-new Set(t).size))")" -eq 0`
      — ningún título de sección se repite entre las cuatro fichas: no son una plantilla rellenada.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const p=(d.paginas||d).find(x=>x.url==='/sedes/consultorio-privado'); process.stdout.write(String(p.notaDeFormato&&p.notaDeFormato.length>40?1:0))")" -eq 1`
      — el caso donde la SERP no resolvió queda declarado y no disimulado.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'),s=require('./data/onpage-serp.json'); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const u=['/sedes/sanna-la-molina','/sedes/consultorio-privado']; const mal=u.filter(x=>{const p=(d.paginas||d).find(y=>y.url===x); const e=s.urls.find(y=>y.url===x).entidades; const t=n(p.secciones.flatMap(y=>y.parrafos.concat([y.titulo])).join(' ')); return 100*e.filter(y=>t.includes(n(y.termino))).length/e.length<80;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-sedes.json'); const mal=(d.paginas||d).flatMap(p=>p.datosOperativos||[]).filter(o=>!o.fuente||o.fuente.trim()===''); process.stdout.write(String(mal.length))")" -eq 0`
    - Las dos compuertas devuelven cero hallazgos y salen con estado 0.
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Las cuatro fichas de sede están redactadas, cada una con la forma que su SERP medida pide, y el
    caso sin formato dominante queda declarado en su paquete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Los cuatro documentos de sede, generados y verificados</name>
  <files>.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-clinica-ricardo-palma.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-clinica-tezza.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-consultorio-privado.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/sedes-sanna-la-molina.md</files>
  <read_first>
    - `seo-tools/src/phase15/paquete.ts` tal como lo dejó el plan 15-02.
    - `.../14-ENLAZADO.md`, las cuatro filas de origen de las sedes.
  </read_first>
  <action>
    Generar los cuatro documentos con `paquete.ts` y no a mano (D-12), y correr la compuerta sobre
    la familia entera.

    Cada documento lleva un bloque propio de las sedes que las otras familias no necesitan: la
    lista de datos operativos con su fuente, y aparte la lista de los que quedaron pendientes de
    confirmación. Ese segundo bloque es lo que v1.1 tiene que resolver antes de publicar, y tenerlo
    separado evita que se publique una ficha con un horario que nadie confirmó.
  </action>
  <verify>
    <automated>cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-sedes.json && ./node_modules/.bin/tsx src/phase15/paquete.ts --data data/copy-sedes.json && ./node_modules/.bin/tsx src/phase15/paquete.ts --data data/copy-sedes.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['sedes-clinica-ricardo-palma','sedes-clinica-tezza','sedes-consultorio-privado','sedes-sanna-la-molina']; process.stdout.write(String(f.filter(x=>!fs.existsSync(d+x+'.md')).length))")" -eq 0`
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['sedes-clinica-ricardo-palma','sedes-clinica-tezza','sedes-consultorio-privado','sedes-sanna-la-molina']; let mal=0; for(const x of f){const t=fs.readFileSync(d+x+'.md','utf8'); const c=((t.split('<!-- copy:inicio -->')[1])||'').split('<!-- copy:fin -->')[0]; if((c.match(/[—–“”‘’]/g)||[]).length>0)mal++;} process.stdout.write(String(mal))")" -eq 0`
      — cero rayas largas y cero comillas tipográficas dentro de la región de copy.
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['sedes-clinica-ricardo-palma','sedes-clinica-tezza','sedes-consultorio-privado','sedes-sanna-la-molina']; process.stdout.write(String(f.filter(x=>/montefiori/i.test(fs.readFileSync(d+x+'.md','utf8'))).length))")" -eq 0`
    - `test "$(cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-sedes.json >/dev/null 2>&1 && echo 1 || echo 0)" -eq 1`
    - Determinismo: la segunda corrida del generador deja los cuatro documentos con el mismo
      SHA-256 que la primera.
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
  </acceptance_criteria>
  <done>
    Los cuatro documentos de sede existen, separan los datos respaldados de los pendientes de
    confirmación, pasan la compuerta y se regeneran idénticos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dato operativo → paciente que viaja | Una dirección o un horario inventado manda a un paciente a un lugar equivocado. Es el daño más concreto que puede hacer esta fase. |
| Ficha de sede → clínica que no es del doctor | Afirmar una relación institucional que no existe es un problema con un tercero, no solo con el paciente. |
| Las cuatro fichas entre sí y contra `/sedes` | Cuatro páginas del mismo tipo se canibalizan si se escriben con la misma plantilla. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-22 | Tampering | Dato operativo compuesto | critical | mitigate | Todo dato operativo exige `fuente`; criterio con conteo cero de datos sin respaldo, y bloque separado de pendientes en el paquete. |
| T-15-23 | Spoofing | Relación institucional afirmada de más | high | mitigate | Conducta declarada y revisada en la ronda del doctor; el copy dice que el doctor atiende ahí. |
| T-15-24 | Tampering | Sede dada de baja que reaparece | high | mitigate | La compuerta rechaza Montefiori; criterio sobre el dataset y sobre los cuatro documentos. |
| T-15-25 | Tampering | Canibalización entre las cuatro fichas | medium | mitigate | Criterio que exige cero títulos de sección repetidos entre ellas. |
| T-15-26 | Repudiation | Formato elegido sin declarar que la SERP no resolvió | medium | mitigate | `notaDeFormato` obligatoria en el consultorio de Surco; criterio de longitud mínima. |
| T-15-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si algún paso lo requiriera, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- SHA-256 de `seo-tools/data/url-map.jsonl` y de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
</verification>

<success_criteria>
- Las cuatro sedes vigentes tienen su ficha redactada dentro de contrato, con las entidades
  obligatorias usadas y sin un solo dato operativo sin respaldo.
- Las cuatro se distinguen entre sí y ninguna le devuelve a `/sedes` el rol que la fase 14 le
  quitó.
- Los cuatro documentos se abren solos y separan lo respaldado de lo pendiente.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 15-05)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Copy de las cuatro sedes | `seo-tools/data/copy-sedes.json` | Dato |
| Cuatro documentos de implementación | `.../paquetes/sedes-*.md` | Entregable |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-05-SUMMARY.md` al terminar.
</output>
