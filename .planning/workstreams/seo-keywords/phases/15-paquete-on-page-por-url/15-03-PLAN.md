---
phase: 15-paquete-on-page-por-url
workstream: seo-keywords
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - seo-tools/data/copy-guias.json
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-estenosis-espinal.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-escoliosis-y-deformidades.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/preguntas-frecuentes.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/blog-estenosis-espinal-que-es.md
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/blog-hernia-discal-o-dolor-de-espalda-como-diferenciarlos.md
autonomous: true
requirements: [ONPAGE-03, ONPAGE-04, ONPAGE-06]
must_haves:
  truths:
    - "Las tres guías clínicas de columna están redactadas de punta a punta, con la extensión que la SERP de su keyword exige."
    - "El contenido de los dos posts que se apagan vive dentro de su guía antes de que v1.1 ponga el 301: primero se funde y después se redirige."
    - "Cada bloque médico de las cuatro páginas está marcado como pendiente de aprobación del doctor."
    - "Las entidades obligatorias derivadas del top 10 están efectivamente usadas en el texto, no solo listadas al lado."
    - "Quien implemente cualquiera de estas seis URLs abre un solo archivo y lo sigue de corrido."
  artifacts:
    - seo-tools/data/copy-guias.json
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-estenosis-espinal.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-escoliosis-y-deformidades.md
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/preguntas-frecuentes.md
  key_links:
    - "El copy se escribe en `data/copy-guias.json` y el Markdown se genera; el documento no se edita a mano (D-12)."
    - "`ymyl.ts` del plan 15-02 es la compuerta: este plan no define criterios propios de humanización ni de sello médico."
    - "La jerarquía y las entidades salen de `data/onpage-serp.json`; el title, la meta y el H1 salen de `data/onpage.json`. Ninguno de los dos se recalcula acá."
---

<objective>
Redactar las cuatro páginas informativas de la fase: las tres guías clínicas de columna y
preguntas frecuentes. Es el bloque donde la reescritura vale más, porque son las URLs cuya SERP
medida premia contenido informativo y no página comercial, y las que hoy están publicadas en el
formato equivocado.

Propósito: `estenosis espinal` tiene 8 de 8 resultados de contenido informativo en su top 10 de
Lima. Una página comercial no compite ahí por bien hecha que esté. Estas cuatro páginas son la
corrección de eso, y además son las que absorben los dos posts del blog que se apagan.

Salida: `data/copy-guias.json` completo y seis documentos en `paquetes/`, cuatro con página
redactada y dos con la instrucción de redirección.

Decisiones que gobiernan este plan: D-05, D-07, D-08, D-09, D-10, D-12, D-14, D-16. El registro
completo está en el `<objective>` de `15-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-01-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-02-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-HANDOFF-V11.md
@seo-tools/data/onpage-serp.json
@seo-tools/data/onpage.json
@seo-tools/data/copy-guias.json
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 15-01.
**Presupuesto de SerpApi de este plan: cero.** `url-map.jsonl` sigue de solo lectura y `src/` de
la aplicación no se toca.

**Alcance exacto de este plan, seis URLs:**

| URL | Formato | Mínimo de palabras | Qué es |
|---|---|---|---|
| `/servicios/hernia-discal` | guía clínica | 1400 | Ya redactada por el tracer. Acá pasa la compuerta y se regenera |
| `/servicios/estenosis-espinal` | guía clínica | 1400 | Nueva. Absorbe `/blog/estenosis-espinal-que-es` |
| `/servicios/escoliosis-y-deformidades` | guía clínica | 1200 | Nueva. Bajo el slug renombrado |
| `/preguntas-frecuentes` | guía clínica | 800 | Reescritura |
| `/blog/estenosis-espinal-que-es` | redirección | 0 | Documento corto: 301 hacia la guía |
| `/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos` | redirección | 0 | Documento corto: 301 hacia la guía |

**Hechos medidos, ya verificados, no hay que redescubrirlos:**

- El reparto real del top 10 de cada keyword, que es el motivo del formato:
  `estenosis espinal` da 8 de 8 contenido informativo internacional; `hernia discal`, 6
  informativo y 1 de página de servicio; `escoliosis`, 5 informativo, 1 red social y 1 guía.
- `escoliosis` sola vale 18.000 de volumen y 3.400 de potencial de tráfico. `cifosis` tiene 3.600
  de volumen con KD 3, el doctor la opera, y quedó fuera de las 10 de Oro por un punto: es la
  razón por la que el slug pasó a `/servicios/escoliosis-y-deformidades` y el paraguas es donde
  entra sin necesidad de URL propia.
- Los dos posts que se apagan ya remiten por escrito a la guía: `estenosis-espinal-que-es` la
  nombra dos veces, y el ángulo diferencial de `hernia-discal-o-dolor-de-espalda-como-diferenciarlos`
  ya vive dentro de la guía de hernia.
- El contenido publicado de las tres páginas de servicio está en `src/content/service-pages.ts`,
  entradas `hernia-discal` (línea 87), `estenosis-espinal` (línea 276) y `escoliosis` (línea 463).
  Los cuatro posts están en `src/content/blog.ts`. Todo en SOLO LECTURA.
</context>

<tasks>

<task type="auto">
  <name>Task 1: La guía de estenosis espinal, que absorbe el post que se apaga</name>
  <files>seo-tools/data/copy-guias.json</files>
  <read_first>
    - `seo-tools/data/onpage-serp.json`, fila `/servicios/estenosis-espinal`: la jerarquía completa
      con las cuatro preguntas de la SERP y la lista de entidades obligatorias con su procedencia.
    - `seo-tools/data/onpage.json`, misma fila: title, meta y H1 ya decididos. No se reescriben.
    - `src/content/service-pages.ts`, entrada `estenosis-espinal` desde la línea 276, en solo
      lectura: es lo que hay publicado y lo que esta guía reemplaza.
    - `src/content/blog.ts`, entrada `estenosis-espinal-que-es` desde la línea 242, en solo
      lectura: es el material que la guía absorbe.
    - `.../paquetes/servicios-hernia-discal.md`: el molde aprobado por Juan. El tono, la
      profundidad y el orden interno de esta guía son los de ese documento.
  </read_first>
  <behavior>
    - La guía cubre las cuatro preguntas de la SERP de `estenosis espinal` como secciones propias.
    - Las cinco keywords secundarias del mapa quedan cubiertas por alguna sección.
    - Al menos el 80 % de las entidades obligatorias de la URL aparecen en el texto.
    - Todo lo que el post `estenosis-espinal-que-es` explicaba tiene su lugar dentro de la guía:
      ninguna idea del post queda sin destino.
    - Cada sección clínica va marcada como pendiente de aprobación del doctor.
    - Ninguna afirmación con cifra viaja sin fuente.
    - El texto pasa la compuerta de humanización del plan 15-02 sin hallazgos.
  </behavior>
  <action>
    Redactar la guía completa de `/servicios/estenosis-espinal` dentro de `data/copy-guias.json`,
    siguiendo la jerarquía que `data/onpage-serp.json` ya fijó y el molde del paquete aprobado.
    Mínimo 1400 palabras de cuerpo, y el mínimo es piso y no objetivo: la SERP de esta keyword es
    8 de 8 contenido informativo internacional, o sea Mayo Clinic y MedlinePlus, y la pelea se gana
    con una guía que responda mejor y en el terreno del paciente de Lima.

    La absorción del post no es un copiar y pegar. El post se llama `estenosis-espinal-que-es` y
    su tema, qué es la estenosis, es una sección de la guía, no la guía entera. Lo que hay que
    hacer es recorrer el post entero, decidir sección por sección dónde entra dentro de la guía
    nueva, y anotar en el dataset el destino de cada bloque. Redirigir sin fundir tira el
    contenido a la basura, y el orden acordado con v1.1 es fundir primero y redirigir después.

    Registrar en la fila un campo `absorbe` con la lista de bloques del post y a qué sección de la
    guía fue cada uno. Ese campo es lo que le permite a quien implemente comprobar que no se perdió
    nada, sin volver a abrir el post.

    Sobre el contenido médico: la guía explica la condición, no promete resultados. La diferencia
    entre "el tratamiento conservador mejora los síntomas en la mayoría de los casos" con su
    fuente, y "el doctor tiene 95 % de éxito", es la diferencia entre una guía y un problema
    legal. Ante la duda entre una frase con más fuerza comercial y una verificable, va la
    verificable (D-10).
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-guias.json --url /servicios/estenosis-espinal</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'); const p=(d.paginas||d).find(x=>x.url==='/servicios/estenosis-espinal'); const t=p.secciones.flatMap(s=>s.parrafos).join(' '); process.stdout.write(String(t.split(/\s+/).filter(Boolean).length))")" -ge 1400`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'),s=require('./data/onpage-serp.json'); const p=(d.paginas||d).find(x=>x.url==='/servicios/estenosis-espinal'); const u=s.urls.find(x=>x.url==='/servicios/estenosis-espinal'); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const t=n(p.secciones.flatMap(x=>x.parrafos.concat([x.titulo])).join(' ')); const hit=u.entidades.filter(e=>t.includes(n(e.termino))).length; process.stdout.write(String(Math.round(100*hit/u.entidades.length)))")" -ge 80`
      — las entidades obligatorias están usadas en el texto y no solo listadas (ONPAGE-03).
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'),s=require('./data/onpage-serp.json'); const p=(d.paginas||d).find(x=>x.url==='/servicios/estenosis-espinal'); const u=s.urls.find(x=>x.url==='/servicios/estenosis-espinal'); const tit=p.secciones.map(x=>x.titulo).join(' | '); const faltan=u.jerarquia.filter(h=>h.origen==='pregunta-serp').filter(h=>!tit.includes(h.texto)); process.stdout.write(String(faltan.length))")" -eq 0`
      — las preguntas reales de la SERP son secciones de la página.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'); const p=(d.paginas||d).find(x=>x.url==='/servicios/estenosis-espinal'); process.stdout.write(String(Array.isArray(p.absorbe)&&p.absorbe.length>0&&p.absorbe.every(b=>b.origen&&b.destino)?1:0))")" -eq 1`
      — cada bloque del post que se apaga tiene destino nombrado dentro de la guía.
    - La compuerta `ymyl.ts` sobre esta URL devuelve cero hallazgos y sale con estado 0.
    - `test "$(shasum -a 256 seo-tools/data/url-map.jsonl | cut -d' ' -f1)" = "ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5"`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    La guía de estenosis espinal está redactada con 1400 palabras o más, absorbe el post que se
    apaga con destino declarado por bloque, y pasa la compuerta sin hallazgos.
  </done>
</task>

<task type="auto">
  <name>Task 2: La guía de escoliosis y deformidades, y preguntas frecuentes</name>
  <files>seo-tools/data/copy-guias.json</files>
  <read_first>
    - `seo-tools/data/onpage-serp.json` y `data/onpage.json`, filas
      `/servicios/escoliosis-y-deformidades` y `/preguntas-frecuentes`.
    - `src/content/service-pages.ts`, entrada `escoliosis` desde la línea 463, y `src/content/faq.ts`
      completo. Los dos en solo lectura.
    - `.../14-HANDOFF-V11.md`, la sección del renombre de slug: qué arrastra y por qué el paraguas
      cubre más que escoliosis sola.
  </read_first>
  <behavior>
    - La guía de escoliosis cubre el paraguas completo de deformidades y no solo escoliosis, con
      cifosis nombrada dentro.
    - Las secundarias de cada URL quedan cubiertas por alguna sección.
    - Al menos el 80 % de las entidades obligatorias de cada URL aparecen en su texto.
    - `/preguntas-frecuentes` responde preguntas reales y no repite el cuerpo de las guías: enlaza
      hacia ellas conceptualmente en vez de duplicar el temario.
    - Cada sección clínica va marcada como pendiente de aprobación del doctor.
    - Las dos páginas pasan la compuerta de humanización sin hallazgos.
  </behavior>
  <action>
    Redactar las dos páginas dentro de `data/copy-guias.json`.

    **`/servicios/escoliosis-y-deformidades`**, mínimo 1200 palabras. El slug se renombró a
    propósito para cubrir el paraguas entero de deformidades de columna y no escoliosis sola, y la
    guía tiene que sostener ese paraguas: escoliosis con sus tipos, cifosis, y el resto de
    deformidades que el doctor opera. Escribir solo sobre escoliosis dejaría el renombre sin
    respaldo de contenido y a `cifosis`, con sus 3.600 de volumen, sin página que la reciba.

    **`/preguntas-frecuentes`**, mínimo 800 palabras. Esta página tiene un riesgo propio y hay que
    esquivarlo: si repite el temario de las guías compite contra ellas, que es exactamente lo que
    la fase 14 pasó tres planes resolviendo. Su trabajo es responder las dudas transversales que
    ninguna guía cubre por ser de una sola condición, como cuánto dura una consulta, qué llevar,
    cómo se decide operar o no, y qué pasa después. Su primaria es `reumatólogo o traumatólogo`,
    que es literalmente una pregunta de a quién consultar, así que esa distinción es el corazón de
    la página y no una nota al pie.

    Las dos con el mismo estándar de la tarea 1: sello por sección clínica, fuente en toda
    afirmación con cifra, cuatro sedes vigentes y ninguna otra.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-guias.json --url /servicios/escoliosis-y-deformidades && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-guias.json --url /preguntas-frecuentes</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'),s=require('./data/onpage-serp.json'); const ps=(d.paginas||d).filter(p=>['/servicios/escoliosis-y-deformidades','/preguntas-frecuentes'].includes(p.url)); const mal=ps.filter(p=>{const min=s.urls.find(u=>u.url===p.url).minimoDePalabras; return p.secciones.flatMap(x=>x.parrafos).join(' ').split(/\s+/).filter(Boolean).length<min;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'); const p=(d.paginas||d).find(x=>x.url==='/servicios/escoliosis-y-deformidades'); const t=p.secciones.flatMap(x=>x.parrafos.concat([x.titulo])).join(' ').toLowerCase(); process.stdout.write(String(/cifosis/.test(t)?1:0))")" -eq 1`
      — el paraguas del renombre tiene respaldo de contenido.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'),s=require('./data/onpage-serp.json'); const n=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const urls=['/servicios/escoliosis-y-deformidades','/preguntas-frecuentes']; const mal=urls.filter(u=>{const p=(d.paginas||d).find(x=>x.url===u); const e=s.urls.find(x=>x.url===u).entidades; const t=n(p.secciones.flatMap(x=>x.parrafos.concat([x.titulo])).join(' ')); return 100*e.filter(x=>t.includes(n(x.termino))).length/e.length<80;}); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'),s=require('./data/onpage-serp.json'); const urls=['/servicios/escoliosis-y-deformidades','/preguntas-frecuentes']; let mal=0; for(const u of urls){const p=(d.paginas||d).find(x=>x.url===u); const cub=new Set(p.secciones.flatMap(x=>x.keywordsCubiertas||[])); const sec=s.urls.find(x=>x.url===u).coberturaDeSecundarias.map(c=>c.secundaria); mal+=sec.filter(k=>!cub.has(k)).length;} process.stdout.write(String(mal))")" -eq 0`
      — ninguna secundaria del mapa queda sin sección que la cubra (D-15).
    - Las dos compuertas devuelven cero hallazgos y salen con estado 0.
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Las dos páginas están redactadas dentro de contrato, la de escoliosis sostiene el paraguas del
    renombre y preguntas frecuentes no compite contra las guías.
  </done>
</task>

<task type="auto">
  <name>Task 3: Los seis documentos de la familia, generados y verificados</name>
  <files>.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-estenosis-espinal.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-escoliosis-y-deformidades.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/preguntas-frecuentes.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/blog-estenosis-espinal-que-es.md, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/blog-hernia-discal-o-dolor-de-espalda-como-diferenciarlos.md</files>
  <read_first>
    - `seo-tools/src/phase15/paquete.ts` tal como lo dejó el plan 15-02, con sus cuatro tipos de
      documento.
    - `seo-tools/data/url-map.jsonl`, las dos filas `redirigir` con su `redirigeA` y su
      `motivoSinPrimaria`.
  </read_first>
  <action>
    Generar los seis documentos con `paquete.ts` y no a mano (D-12). Los cuatro de página completa
    salen de `data/copy-guias.json`; los dos de redirección salen del mapa y dicen tres cosas:
    hacia dónde va el 301, que el contenido ya fue absorbido y en qué sección de qué guía quedó, y
    que el orden correcto es publicar la guía antes de poner la redirección.

    Correr la compuerta sobre la familia entera, incluida la guía de hernia discal que redactó el
    tracer, porque las reglas ejecutables aparecieron después de ella y todavía no la habían
    tocado. Si aparece algún hallazgo en la guía del tracer, se corrige el texto en el dataset y se
    regenera. Lo que no se hace es aflojar la regla.
  </action>
  <verify>
    <automated>cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-guias.json && ./node_modules/.bin/tsx src/phase15/paquete.ts --data data/copy-guias.json && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /blog/estenosis-espinal-que-es && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(ls .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/ | wc -l | tr -d ' ')" -ge 6`
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const f=['servicios-hernia-discal','servicios-estenosis-espinal','servicios-escoliosis-y-deformidades','preguntas-frecuentes']; let mal=0; for(const x of f){const t=fs.readFileSync(d+x+'.md','utf8'); const c=((t.split('<!-- copy:inicio -->')[1])||'').split('<!-- copy:fin -->')[0]; if((c.match(/[—–“”‘’]/g)||[]).length>0)mal++;} process.stdout.write(String(mal))")" -eq 0`
      — cero rayas largas y cero comillas tipográficas dentro de la región de copy de los cuatro
      documentos. Fuera de la región no se mira: la prosa heredada de la fase 14 no es nuestra.
    - `test "$(node -e "const fs=require('fs'); const d='.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/'; const t=fs.readFileSync(d+'blog-estenosis-espinal-que-es.md','utf8'); process.stdout.write(String(t.includes('/servicios/estenosis-espinal')?1:0))")" -eq 1`
      — el documento de la URL que se apaga dice hacia dónde va (D-07, D-14).
    - `test "$(cd seo-tools && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-guias.json >/dev/null 2>&1 && echo 1 || echo 0)" -eq 1`
      — la familia entera, incluida la guía del tracer, pasa la compuerta.
    - Determinismo: regenerar los seis documentos deja los mismos SHA-256 que la corrida anterior.
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
  </acceptance_criteria>
  <done>
    Los seis documentos de la familia informativa existen, se abren solos, pasan la compuerta y se
    regeneran idénticos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copy clínico → paciente | Contenido YMYL sobre condiciones de columna. Una afirmación sin verificar es daño real. |
| Post que se apaga → guía que lo absorbe | Si el contenido no se funde antes del 301, se pierde y el 301 lo entierra. |
| Guías entre sí y contra preguntas frecuentes | Cuatro páginas del mismo tema pueden canibalizarse si repiten temario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-13 | Tampering | Copy clínico sin sello del doctor | critical | mitigate | La compuerta `ymyl.ts` corre sobre la familia entera y sale con estado distinto de cero si falta un sello. |
| T-15-14 | Denial of Service | Contenido perdido en el 301 | high | mitigate | Campo `absorbe` obligatorio con origen y destino por bloque; criterio que lo exige no vacío. |
| T-15-15 | Repudiation | Cifra médica sin fuente | high | mitigate | La compuerta exige `fuente` en toda afirmación con dígito. |
| T-15-16 | Tampering | Canibalización entre las guías y preguntas frecuentes | medium | mitigate | Preguntas frecuentes responde lo transversal; la auditoría de duplicados del plan 15-02 ya corrió sobre title y H1. |
| T-15-17 | Elevation of Privilege | Escritura en `src/` | critical | mitigate | Lectura únicamente; criterio de staging vacío para `src/`. |
| T-15-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si algún paso lo requiriera, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- SHA-256 de `seo-tools/data/url-map.jsonl` y de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
</verification>

<success_criteria>
- Las cuatro páginas informativas están redactadas de punta a punta, dentro de su mínimo de
  palabras y con las entidades obligatorias usadas en el texto.
- El contenido de los dos posts que se apagan vive dentro de su guía con destino declarado por
  bloque.
- Los seis documentos de la familia se abren solos y pasan la compuerta de YMYL y humanización.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 15-03)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Copy de las cuatro páginas informativas | `seo-tools/data/copy-guias.json` | Dato |
| Seis documentos de implementación | `.../paquetes/servicios-*.md`, `preguntas-frecuentes.md`, `blog-*.md` | Entregable |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-03-SUMMARY.md` al terminar.
</output>
