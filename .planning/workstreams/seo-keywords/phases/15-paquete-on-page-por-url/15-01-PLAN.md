---
phase: 15-paquete-on-page-por-url
workstream: seo-keywords
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seo-tools/src/phase15/model.ts
  - seo-tools/src/phase15/entidades.ts
  - seo-tools/src/phase15/entidades.test.ts
  - seo-tools/src/phase15/serp-onpage.ts
  - seo-tools/src/phase15/serp-onpage.test.ts
  - seo-tools/src/phase15/metadatos.ts
  - seo-tools/src/phase15/metadatos.test.ts
  - seo-tools/src/phase15/paquete.ts
  - seo-tools/src/phase15/paquete.test.ts
  - seo-tools/data/onpage-serp.json
  - seo-tools/data/onpage.json
  - seo-tools/data/copy-guias.json
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md
autonomous: false
requirements: [ONPAGE-02, ONPAGE-03, ONPAGE-06]
must_haves:
  truths:
    - "Una URL real recorre la fase entera: de las capturas de SERP ya pagadas a un paquete Markdown que quien implementa puede abrir y seguir de corrido."
    - "Las 16 URLs con keyword primaria tienen su jerarquía H2/H3 derivada de las preguntas reales que la SERP de Lima muestra para su keyword."
    - "Las 16 URLs tienen su lista de entidades obligatorias derivada del top 10 que ya posiciona, con la procedencia de cada término al lado."
    - "Todo sale de las 96 capturas en caché: la fase no gasta ni una búsqueda de SerpApi."
    - "Ninguna línea de texto clínico del paquete se entrega sin el sello de pendiente de aprobación del doctor."
    - "Juan aprueba tono, formato y estructura sobre una página real antes de que se escriban las otras quince."
  artifacts:
    - seo-tools/data/onpage-serp.json
    - seo-tools/src/phase15/entidades.ts
    - seo-tools/src/phase15/serp-onpage.ts
    - seo-tools/src/phase15/paquete.ts
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md
  key_links:
    - "`serp-onpage.ts` lee `.cache/serpapi/` con `leerSerp(keyword, { offline: true })`: una captura ausente es error de lectura y jamás una búsqueda nueva."
    - "`url-map.jsonl` es de SOLO LECTURA en toda la fase: sus secundarias sostienen los anchors de la matriz de enlazado del plan 14-04."
    - "`paquete.ts` genera el Markdown desde el dataset; el documento no se edita a mano."
---

<objective>
Fundar la fase probando la tubería completa sobre UNA página real antes de escribir dieciséis: de
las capturas de SERP ya pagadas a la jerarquía, las entidades, el title, la meta, el H1, el copy
clínico y el documento Markdown que v1.1 abre e implementa.

Propósito: si el formato del paquete está mal, se descubre después de una página y no después de
las dieciséis. Y lo que el resto de la fase consume —la jerarquía y las entidades de las 16 URLs—
queda medido acá, desde caché y con coste de cuota cero.

Salida: `data/onpage-serp.json` con la jerarquía y las entidades de las 16, el paquete completo de
`/servicios/hernia-discal`, y la aprobación de Juan sobre tono y formato.

## Registro de decisiones de la fase (`15-CONTEXT.md`)

Estos identificadores se usan en los siete planes. Salen de `15-CONTEXT.md`, sección
`<decisions>`, y son inamovibles.

| ID | Decisión |
|---|---|
| D-01 | ONPAGE-02: la jerarquía H2/H3 sale de las 96 capturas de SERP ya cacheadas. Presupuesto de SerpApi de la fase: cero. |
| D-02 | ONPAGE-03: las entidades obligatorias se derivan del top 10 capturado y NO del TF-IDF de DinoRank, cuyo corpus de comparación vino vacío (`totalUrls: 0`, `wdfdf.media: []`, `df: null`). |
| D-03 | ONPAGE-05: la auditoría de duplicados es una comprobación local sobre el paquete propuesto, corrida antes de que v1.1 publique. |
| D-04 | `/auditoria` de DinoRank no se usa en esta fase: responde HTTP 200 con los datos de `soumahotel.com`, el proyecto de otro cliente. |
| D-05 | Las 16 URLs con keyword primaria reciben la página redactada de punta a punta. Decisión de Juan del 2026-08-12, sobre la recomendación contraria de acotar a esquema más secciones clave. |
| D-06 | Las 6 URLs de acción `dejar` reciben solo title y meta. No compiten por una keyword y ya declararon por escrito por qué. |
| D-07 | Las 2 URLs de acción `redirigir` no reciben copy. Necesitan que el paquete de su destino exista y la instrucción de redirección. |
| D-08 | Ninguna línea de texto clínico se entrega como lista para publicar: cada bloque médico va marcado como pendiente de aprobación del doctor. |
| D-09 | Toda la revisión del doctor se agrupa en una sola ronda, y el paquete tiene que hacerla navegable. |
| D-10 | Nada de credenciales, cifras de cirugías, tasas de éxito ni resultados inventados. Solo lo verificado o lo que el doctor confirme por escrito. |
| D-11 | Montefiori no existe en este sitio. Las cuatro sedes vigentes son consultorio Surco, Ricardo Palma, Sanna La Molina y Padre Luis Tezza. |
| D-12 | Un Markdown por URL bajo `paquetes/`, generado desde el dataset JSON, que es la fuente de verdad. Los documentos se regeneran, no se editan. |
| D-13 | Title ≤60 caracteres y description ≤155 son contrato con la fase 10 de v1.1. |
| D-14 | El paquete se abre por URL suelta: quien implementa una no debería leer las otras quince. |
| D-15 | Los anchors de la matriz de enlazado salen de las secundarias del destino. Si esta fase cambiara las secundarias, los anchors del plan 14-04 dejarían de tener respaldo. |
| D-16 | Lo que las fases anteriores ya decidieron y esta no reabre: tres páginas de servicio se reescriben como guía clínica, `/servicios/escoliosis` pasa a `/servicios/escoliosis-y-deformidades`, y `/sedes` queda como hub sin primaria. |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/seo-keywords/ROADMAP.md
@.planning/workstreams/seo-keywords/STATE.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-HANDOFF-V11.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md
@seo-tools/data/url-map.jsonl
@seo-tools/src/phase14/pagetype-map.ts
@seo-tools/src/phase13/args.ts

**Restricciones duras del workstream, sin excepción:**

- Ningún archivo bajo `src/` de la aplicación se crea, se modifica ni se borra. Leerlo es
  necesario y correcto; escribirlo rompe el workstream paralelo que despliega desde ahí.
- No se escribe nada en `.planning/workstreams/milestone/`, ni en `.planning/STATE.md`,
  `.planning/ROADMAP.md` o `.planning/REQUIREMENTS.md` de la raíz.
- **Presupuesto de SerpApi de este plan y de la fase entera: CERO búsquedas (D-01).** Quedan 6
  hasta el reset del 2026-08-21 y no son de esta fase. Si algún paso pareciera necesitarlas, se
  detiene y se le pregunta a Juan.
- `seo-tools/data/url-map.jsonl` es de SOLO LECTURA en toda la fase (D-15). SHA-256 esperado:
  `ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5`.
- `seo-tools/data/keywords.jsonl` sigue de solo lectura. SHA-256:
  `c59dad2ddaeaeff47395a8844a813b2f04a43f1a3f3ad490b7b43b98bbcd7eac`.
- `src/cli.ts` de `seo-tools` está CERRADO desde la fase 12. Los ejecutables van bajo
  `src/phase15/` con su `main()` guardado al final del archivo, igual que `pagetype-map.ts`.
- No se instala ningún paquete de npm. Si algo pareciera pedirlo, se detiene y escala a Juan.

**Trampas ya conocidas, medidas en las fases 12, 13 y 14:**

- `FORCE_COLOR` está activo: `console.log` de un número emite códigos ANSI y rompe `test -eq`.
  En los criterios usar `process.stdout.write(String(n))`.
- El shell es zsh: todo glob va entre comillas.
- `grep -c` sale con estado 1 cuando cuenta cero. Envolverlo en `test "$(...)" -eq N`.
- Después de `cd seo-tools`, las rutas relativas ya no llevan el prefijo `seo-tools/`.

**Hechos medidos el 2026-08-12, ya verificados, no hay que redescubrirlos:**

- `url-map.jsonl` tiene **24 filas**: 13 `reescribir`, 3 `crear`, 6 `dejar`, 2 `redirigir`. Las
  13 + 3 = **16 con `keywordPrimaria`** son exactamente las que reciben página completa.
- El caché de SerpApi tiene **96 capturas** bajo `seo-tools/.cache/serpapi/`. El sobre es
  `{schema, source, endpoint, request, fetchedAt, outcome, httpStatus, response}` y la consulta
  está en `request.params.q`. **Las 16 keywords primarias tienen captura**, cada una con 4
  `related_questions`, 8 `related_searches` y entre 7 y 10 `organic_results`. Releerlas cuesta
  cero.
- `seo-tools/.cache/_quota.json` marca `sources.serpapi.calls: 96`. Ese número no se mueve.
- La captura NO trae el cuerpo de las páginas que rankean: trae `title` y `snippet` de cada
  orgánico. El corpus de entidades es ese y hay que declararlo como tal, no venderlo como un
  TF-IDF sobre el texto completo.
- `url-map.jsonl` contiene 12 rayas largas dentro de sus campos de prosa. Cualquier regla de
  humanización que prohíba la raya larga tiene que acotarse a la región de copy del paquete, o
  falla por texto heredado que este plan no escribió.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: De la captura al paquete, por `/servicios/hernia-discal` y una sola vez</name>
  <files>seo-tools/src/phase15/model.ts, seo-tools/src/phase15/entidades.ts, seo-tools/src/phase15/entidades.test.ts, seo-tools/src/phase15/serp-onpage.ts, seo-tools/src/phase15/metadatos.ts, seo-tools/src/phase15/paquete.ts, seo-tools/src/phase15/paquete.test.ts, seo-tools/data/copy-guias.json, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md</files>
  <read_first>
    - `seo-tools/src/phase14/pagetype-map.ts` entero: es el patrón de módulo de fase que hay que
      replicar, incluido `leerSerp(keyword, { offline: true })` y el guardado de `main()` al final.
    - `seo-tools/src/phase13/serp.ts`, firma de `leerSerp` y `OpcionesSerp`.
    - `seo-tools/src/sources/serpapi.ts`, tipos `SerpCompleta`, `ResultadoOrganico`: de ahí salen
      `organicos`, `preguntas` y `relacionadas`.
    - `seo-tools/src/phase13/args.ts`: `parseBanderas`, `texto`, `booleana`, `ejecutar`.
    - `seo-tools/data/url-map.jsonl`, la fila de `/servicios/hernia-discal`: primaria, cinco
      secundarias, `tipoExigidoPorSerp`, `intent`, `cluster` y `motivoDeAccion`.
    - `src/content/service-pages.ts`, entrada `slug: "hernia-discal"` (línea 87 en adelante), y
      `src/content/blog.ts`, entrada `slug: "hernia-discal-o-dolor-de-espalda-como-diferenciarlos"`
      (línea 105 en adelante). Las dos en SOLO LECTURA: son el material que la guía absorbe.
    - `.../14-HANDOFF-V11.md`, la sección que explica por qué esta URL va como guía clínica.
  </read_first>
  <behavior>
    - `entidadesDelTop10` sobre una captura con siete orgánicos devuelve términos con su
      frecuencia documental: en cuántos de los siete aparece y en qué posiciones.
    - Un término que aparece en un solo resultado del top 10 queda fuera del umbral por defecto.
    - Los nombres de dominio, las marcas de los competidores y el ruido de navegación no salen
      como entidad clínica.
    - `jerarquiaDe` sobre la captura de `hernia discal` devuelve las cuatro preguntas de la SERP
      como encabezados, o nombra la que descartó con su motivo.
    - Cada una de las cinco secundarias del mapa queda cubierta por al menos un encabezado.
    - `tituloYMeta` devuelve un title de 60 caracteres o menos y una meta de 155 o menos, con
      todos los tokens de la keyword primaria presentes y el primero dentro de los primeros 12
      caracteres.
    - `renderPaquete` sobre una sección marcada como clínica emite el sello de pendiente de
      aprobación del doctor; sobre una sección operativa, no.
    - Dos llamadas seguidas a `renderPaquete` con el mismo dataset producen el mismo texto.
  </behavior>
  <action>
    Recorrer la tubería completa por UNA sola URL: `/servicios/hernia-discal`. Se elige porque es
    el caso más caro de la fase —guía clínica larga, SERP de contenido informativo, y además
    absorbe el post del blog que después redirige—, así que si el formato del paquete aguanta acá,
    aguanta en las otras quince.

    En `model.ts`, definir los tipos que usan los siete planes: `FilaOnPage` (url, accion,
    keywordPrimaria, title, metaDescription, h1, h1Origen, redirigeA), `JerarquiaDeUrl` (nivel,
    texto, origen, literal), `EntidadObligatoria` (termino, documentos, de, posiciones, clase),
    `SeccionDeCopy` (nivel, titulo, tipo, aprobacion, parrafos, afirmaciones, keywordsCubiertas,
    entidadesCubiertas) y `PaqueteDeUrl`. Un solo juego de tipos para toda la fase.

    En `entidades.ts`, implementar la derivación de entidades obligatorias sobre los títulos y
    fragmentos del top 10 (D-02). El criterio es discrecional y se declara acá para que se pueda
    auditar: n-gramas de una a tres palabras, con lista de vacías en español; se descartan el
    dominio y la marca de cada resultado, la marca propia del sitio y el vocabulario de
    navegación; un término entra si aparece en el 40 % o más de los orgánicos medidos; el
    resultado se corta en 20 términos, ordenados por frecuencia documental y, a igualdad, por la
    posición más alta en la que aparecieron. Si con ese umbral quedaran menos de 8 términos, se
    baja el umbral por pasos de 0,1 y **se registra el umbral aplicado en la fila**. Si aun al
    mínimo no llega a 8, la fila declara `entidadesInsuficientes: true` en vez de inventar
    términos. Cada entidad viaja con su procedencia: cuántos documentos y qué posiciones.

    En `serp-onpage.ts`, implementar la jerarquía (D-01) para esta URL: las cuatro preguntas de
    `related_questions` se vuelven encabezados de nivel 2 con su literal al lado; las ocho
    `related_searches` se acomodan como nivel 2 o 3 según si son subtema de un encabezado ya
    puesto; y las cinco secundarias del mapa se cruzan contra el resultado para que ninguna quede
    sin cubrir. El esqueleto de la guía clínica ordena los encabezados en el recorrido que hace un
    paciente: qué es, cómo se siente, por qué pasa, cómo se confirma, qué se puede hacer sin
    operar, cuándo sí hay que operar, y cuándo consultar. Una pregunta que no entre se nombra en
    `preguntasSinUsar` con el motivo escrito.

    En `metadatos.ts`, implementar `tituloYMeta` para esta URL, con el contrato de D-13 y con la
    keyword primaria al frente. La comprobación de "al frente" es por tokens y sin tildes, porque
    en español la keyword se parte con preposiciones al escribirla natural.

    En `paquete.ts`, implementar el generador de Markdown (D-12, D-14). El documento se abre
    solo y trae, en este orden: qué URL es y qué hay que hacer con ella, el title y la meta con su
    conteo de caracteres, el H1, la jerarquía completa de encabezados, las entidades obligatorias
    con su procedencia, el copy redactado, y al final el bloque de qué queda pendiente del doctor.
    El copy va delimitado por dos marcas de comentario HTML que abren y cierran la región, porque
    las reglas de humanización se aplican a lo que escribimos y no a la prosa heredada de los
    datasets de la fase 14.

    Redactar el copy real de la guía en `data/copy-guias.json`. Guía clínica de 1400 palabras o
    más, que absorbe el material del post `hernia-discal-o-dolor-de-espalda-como-diferenciarlos`
    antes de que v1.1 lo redirija: fundir primero y redirigir después, o el contenido se tira. Cada
    sección declara si es clínica, y toda sección clínica va marcada como pendiente de aprobación
    del doctor (D-08). Cada afirmación que lleve una cifra viaja con su fuente. Nada de
    credenciales, número de cirugías, tasas de éxito ni resultados (D-10). Las sedes que se
    nombren son las cuatro vigentes (D-11).

    El texto se escribe humanizado desde el principio y no se "humaniza después": rayas largas
    fuera, comillas rectas, sin emojis, encabezados en mayúscula solo la inicial, ritmo de oración
    variado, y sin las muletillas de redacción automática. La regla completa vive en
    `~/.claude/skills/humanizer/SKILL.md` y el plan 15-02 la convierte en compuerta ejecutable;
    acá se cumple a mano y Juan la juzga en el checkpoint.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern="entidades|paquete" && npm run typecheck && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /servicios/hernia-discal && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /servicios/hernia-discal</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ".planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md"`
    - `test "$(node -e "const t=require('fs').readFileSync('.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md','utf8'); const m=t.split('<!-- copy:inicio -->')[1]; const c=(m||'').split('<!-- copy:fin -->')[0]; process.stdout.write(String(c.trim().split(/\s+/).filter(Boolean).length))")" -ge 1400`
      — la guía es una página completa y no un esquema (D-05).
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'); const f=(d.paginas||d).find(p=>p.url==='/servicios/hernia-discal'); const mal=f.secciones.filter(s=>s.tipo==='clinico'&&s.aprobacion!=='pendiente-doctor'); process.stdout.write(String(mal.length))")" -eq 0`
      — ninguna sección clínica sin sello (D-08).
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'); const f=(d.paginas||d).find(p=>p.url==='/servicios/hernia-discal'); const af=f.secciones.flatMap(s=>s.afirmaciones||[]); const mal=af.filter(a=>/\d/.test(a.texto)&&(!a.fuente||a.fuente.trim()==='')); process.stdout.write(String(mal.length))")" -eq 0`
      — toda afirmación con cifra declara de dónde salió (D-10).
    - `test "$(node -e "const t=require('fs').readFileSync('.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md','utf8'); const c=((t.split('<!-- copy:inicio -->')[1])||'').split('<!-- copy:fin -->')[0]; process.stdout.write(String((c.match(/[—–“”‘’]/g)||[]).length))")" -eq 0`
      — dentro de la región de copy no hay rayas largas ni comillas tipográficas. Fuera de esa
      región la prosa heredada de la fase 14 sí las tiene y no se toca.
    - `test "$(cd seo-tools && node -e "const d=require('./data/copy-guias.json'); const f=(d.paginas||d).find(p=>p.url==='/servicios/hernia-discal'); const t=f.secciones.flatMap(s=>s.parrafos).join(' '); process.stdout.write(String(/montefiori/i.test(t)?1:0))")" -eq 0`
      — Montefiori no aparece (D-11).
    - Determinismo, corrido desde la raíz del repositorio:
      `PAQ=.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md; a=$(shasum -a 256 "$PAQ" | cut -d' ' -f1); (cd seo-tools && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /servicios/hernia-discal >/dev/null); b=$(shasum -a 256 "$PAQ" | cut -d' ' -f1); test "$a" = "$b"`
      — regenerar da el mismo documento byte a byte (D-12).
    - `test "$(shasum -a 256 seo-tools/data/url-map.jsonl | cut -d' ' -f1)" = "ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5"`
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
    - Las ocho conductas declaradas tienen prueba propia y la suite completa queda en verde.
  </acceptance_criteria>
  <done>
    Existe el paquete completo de `/servicios/hernia-discal`: title, meta, H1, jerarquía, entidades
    con procedencia y guía clínica de 1400 palabras o más, con cada bloque médico marcado como
    pendiente del doctor, y regenerarlo produce el mismo archivo.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Jerarquía y entidades obligatorias de las 16 URLs, desde caché</name>
  <files>seo-tools/src/phase15/serp-onpage.ts, seo-tools/src/phase15/serp-onpage.test.ts, seo-tools/src/phase15/entidades.ts, seo-tools/src/phase15/entidades.test.ts, seo-tools/data/onpage-serp.json</files>
  <read_first>
    - `seo-tools/data/url-map.jsonl` completo: las 16 filas con `keywordPrimaria`, con su
      `tipoExigidoPorSerp`, `intent`, `secundarias` y `accion`.
    - `seo-tools/data/page-type-map.json`: el tipo de página que la SERP exige por cabeza, con su
      reparto y su nivel de confianza. Ya está medido; no se recalcula.
    - `seo-tools/data/serp-page-types.json`, el orden de precedencia de los siete tipos.
    - `seo-tools/data/golden-10.json`: qué URLs sirven keyword de oro, para saber cuáles no
      admiten una jerarquía floja.
  </read_first>
  <behavior>
    - Las 16 URLs con primaria salen en el artefacto; las 8 sin primaria no.
    - Cada URL declara su formato: guía clínica, página de servicio o ficha de sede, derivado del
      tipo que exige la SERP y no del nombre de la carpeta.
    - Cada URL declara su mínimo de palabras, que es el contrato que los planes de copy tienen que
      cumplir.
    - Las cuatro preguntas de cada captura o entran a la jerarquía o quedan en `preguntasSinUsar`
      con motivo: ninguna desaparece en silencio.
    - Ninguna secundaria del mapa queda sin encabezado que la cubra.
    - Una URL cuyo umbral de entidades tuvo que bajarse registra el umbral aplicado.
    - `/servicios/estenosis-espinal`, cuya SERP es 8 de 8 contenido informativo, sale con formato
      de guía clínica y no de página de servicio.
    - Correr el generador dos veces produce el mismo archivo.
  </behavior>
  <action>
    Extender `serp-onpage.ts` y `entidades.ts` de una URL a las 16 y emitir
    `data/onpage-serp.json` con, por cada URL: `url`, `keywordPrimaria`, `keywordPrimariaKey`,
    `formato`, `minimoDePalabras`, `posicionesMedidas`, `jerarquia`, `entidades`,
    `coberturaDeSecundarias`, `preguntasSinUsar`, `umbralAplicado` y `fuente`. Arriba, un bloque
    `umbrales` y un bloque `resumen`.

    Los tres esqueletos de jerarquía, uno por formato:

    - **Guía clínica**: qué es, cómo se siente, por qué pasa, cómo se confirma, qué se puede hacer
      sin operar, cuándo sí hay que operar, preguntas frecuentes y cuándo consultar. Es el formato
      que la SERP exige donde el top 10 es contenido informativo.
    - **Página de servicio**: qué se atiende, cómo es la consulta, qué condiciones entran, dónde
      se atiende y cómo se agenda.
    - **Ficha de sede**: dónde queda, cómo llegar, qué se atiende ahí, horarios y cómo se agenda.

    El mínimo de palabras por URL, que es el contrato de D-05 contra la tentación de entregar
    esquema en vez de página:

    | URL | Formato | Mínimo |
    |---|---|---|
    | `/servicios/hernia-discal` | guía clínica | 1400 |
    | `/servicios/estenosis-espinal` | guía clínica | 1400 |
    | `/servicios/escoliosis-y-deformidades` | guía clínica | 1200 |
    | `/preguntas-frecuentes` | guía clínica | 800 |
    | `/` | página de servicio | 600 |
    | `/servicios` | página de servicio | 700 |
    | `/servicios/ortopedia-infantil` | página de servicio | 700 |
    | `/servicios/cirugia-minimamente-invasiva` | página de servicio | 700 |
    | `/sedes/clinica-ricardo-palma` | ficha de sede | 500 |
    | `/sedes/clinica-tezza` | ficha de sede | 500 |
    | `/sedes/consultorio-privado` | ficha de sede | 500 |
    | `/sedes/sanna-la-molina` | ficha de sede | 500 |
    | `/blog/artrosis` | guía clínica | 900 |
    | `/blog/lumbalgia` | guía clínica | 900 |
    | `/blog/5-sintomas-de-columna-que-no-debes-ignorar` | guía clínica | 900 |
    | `/blog/miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber` | guía clínica | 900 |

    Las dos guías que absorben un post del blog llevan 1400 y no 1200 justamente porque el
    material fundido tiene que caber adentro (D-07).

    El generador lee siempre en modo offline. Una captura ausente hace fallar la fila con el
    nombre de la keyword, nunca una llamada a la red (D-01).
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern="serp-onpage|entidades" && npm run typecheck && ./node_modules/.bin/tsx src/phase15/serp-onpage.ts --out data/onpage-serp.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); process.stdout.write(String(d.urls.length))")" -eq 16`
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const mal=d.urls.filter(u=>!Array.isArray(u.jerarquia)||u.jerarquia.length<5||!u.formato||!(u.minimoDePalabras>0)); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const mal=d.urls.filter(u=>(u.jerarquia.filter(h=>h.origen==='pregunta-serp').length+(u.preguntasSinUsar||[]).length)!==4); process.stdout.write(String(mal.length))")" -eq 0`
      — las cuatro preguntas de cada SERP están usadas o nombradas con motivo, ninguna se pierde.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const mal=(d.urls||[]).flatMap(u=>(u.preguntasSinUsar||[]).filter(p=>!p.motivo||p.motivo.length<20)); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const mal=d.urls.flatMap(u=>(u.coberturaDeSecundarias||[]).filter(c=>!c.cubiertaPor)); process.stdout.write(String(mal.length))")" -eq 0`
      — ninguna secundaria del mapa queda sin encabezado (D-15).
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const mal=d.urls.filter(u=>u.entidadesInsuficientes!==true&&(u.entidades.length<8||u.entidades.length>20)); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const mal=d.urls.flatMap(u=>u.entidades.filter(e=>!(e.documentos>=1)||!Array.isArray(e.posiciones)||e.posiciones.length!==e.documentos||!(e.de>0))); process.stdout.write(String(mal.length))")" -eq 0`
      — cada entidad viaja con su procedencia y el conteo coincide con las posiciones.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage-serp.json'); const u=d.urls.find(x=>x.url==='/servicios/estenosis-espinal'); process.stdout.write(String(u&&u.formato==='guia-clinica'?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
    - Determinismo: dos corridas seguidas producen el mismo SHA-256 de `data/onpage-serp.json`.
    - Las ocho conductas declaradas tienen prueba propia y la suite completa queda en verde.
  </acceptance_criteria>
  <done>
    Las 16 URLs tienen su jerarquía H2/H3 derivada de las preguntas reales de su SERP y su lista de
    entidades obligatorias con procedencia, todo desde caché y con cero búsquedas gastadas.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Juan aprueba tono, formato y extensión sobre una página real</name>
  <what-built>
    Un paquete completo, el de `/servicios/hernia-discal`, y la jerarquía y las entidades de las
    otras quince. El paquete es el molde: las quince páginas que faltan se escriben con este
    formato, este tono y este nivel de detalle.
  </what-built>
  <how-to-verify>
    Abrir
    `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/paquetes/servicios-hernia-discal.md`
    y leerlo entero como si fueras quien va a implementarlo. Cuatro preguntas concretas:

    1. **Tono.** ¿El texto suena a alguien escribiendo o suena a máquina? Es lo único que ningún
       criterio automático decide bien y es lo caro de arreglar después de dieciséis páginas.
    2. **Formato.** ¿El documento se abre solo? ¿Podrías implementar esta URL sin abrir ningún
       otro archivo del workstream?
    3. **Profundidad.** Son 1400 palabras o más de guía clínica. ¿Es la extensión que querías
       cuando elegiste página completa por encima de esquema más secciones clave, o te quedó
       corta o larga?
    4. **Sello YMYL.** ¿Cada bloque médico está marcado como pendiente de tu aprobación y del
       doctor, y no hay ninguna cifra ni credencial que nadie haya verificado?

    Después, abrir `seo-tools/data/onpage-serp.json` y mirar dos filas al azar: la jerarquía tiene
    que leerse como el índice de una página real, no como una lista de keywords pegadas.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" o describir qué cambiar del tono, del formato o de la extensión antes de que se escriban las otras quince páginas</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.cache/serpapi/` → generadores de la fase | Datos ya pagados. Una lectura que falle y salga a la red gasta una cuota que no se repone hasta el 2026-08-21. |
| Copy clínico → paciente | Contenido YMYL. Una afirmación médica sin verificar publicada es daño real, no un bug de formato. |
| Este workstream → `src/` de la aplicación | v1.1 despliega desde ahí. Cualquier escritura rompe producción ajena. |
| Esta fase → `url-map.jsonl` | Sus secundarias sostienen los 135 anchors de la matriz del plan 14-04. Cambiarlas los deja sin respaldo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Denial of Service | Cuota de SerpApi | critical | mitigate | Los generadores leen con `offline: true`; criterio que fija la cuota en 96 después de correr. |
| T-15-02 | Tampering | Copy clínico sin sello | critical | mitigate | Toda sección de tipo clínico exige `aprobacion: pendiente-doctor`; criterio que cuenta violaciones y exige cero. |
| T-15-03 | Repudiation | Afirmación médica sin fuente | high | mitigate | Toda afirmación con cifra exige `fuente` no vacía; criterio con conteo cero. |
| T-15-04 | Elevation of Privilege | Escritura en `src/` de la aplicación | critical | mitigate | Lectura únicamente; criterio de staging vacío para `src/` antes de commitear. |
| T-15-05 | Tampering | Modificación de las secundarias del mapa | high | mitigate | `url-map.jsonl` de solo lectura; criterio de SHA-256 fijo en los siete planes. |
| T-15-06 | Information Disclosure | Datos de otro cliente vía `/auditoria` de DinoRank | high | transfer | El endpoint no se usa en esta fase (D-04). Queda registrado en `15-AUDITORIA.md` para que nadie lo reintente creyendo que es un 500 transitorio. |
| T-15-07 | Repudiation | Entidad obligatoria sin procedencia | medium | mitigate | Cada entidad lleva documentos y posiciones; criterio que exige que coincidan. |
| T-15-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes: usa el `seo-tools` existente. Si algún paso lo requiriera, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- Guardarraíl de v1.1 intacto: `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- La app raíz sigue compilando: `npx tsc --noEmit` con estado 0.
- SHA-256 de `seo-tools/data/url-map.jsonl` y de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96: el plan cerró sin gastar una búsqueda.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
- Juan aprobó el paquete de muestra antes de que se escriba una página más.
</verification>

<success_criteria>
- Existe `data/onpage-serp.json` con la jerarquía H2/H3 y las entidades obligatorias de las 16
  URLs, con procedencia por término y cero búsquedas gastadas.
- Existe el paquete completo de `/servicios/hernia-discal`, con guía clínica de 1400 palabras o
  más y cada bloque médico sellado.
- El formato del paquete está aprobado por Juan y las quince páginas restantes tienen molde.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 15-01)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Jerarquía y entidades por URL | `seo-tools/data/onpage-serp.json` | Dato medido desde caché |
| Copy de la guía de hernia discal | `seo-tools/data/copy-guias.json` | Dato, se completa en 15-03 |
| Módulos de la fase | `seo-tools/src/phase15/{model,entidades,serp-onpage,metadatos,paquete}.ts` | Código |
| Paquete de muestra | `.../paquetes/servicios-hernia-discal.md` | Entregable |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-01-SUMMARY.md` al terminar.
</output>
