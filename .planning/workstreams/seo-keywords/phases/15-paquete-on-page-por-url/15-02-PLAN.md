---
phase: 15-paquete-on-page-por-url
workstream: seo-keywords
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - seo-tools/src/phase15/metadatos.ts
  - seo-tools/src/phase15/metadatos.test.ts
  - seo-tools/src/phase15/auditoria.ts
  - seo-tools/src/phase15/auditoria.test.ts
  - seo-tools/src/phase15/ymyl.ts
  - seo-tools/src/phase15/ymyl.test.ts
  - seo-tools/src/phase15/paquete.ts
  - seo-tools/src/phase15/paquete.test.ts
  - seo-tools/data/onpage.json
  - seo-tools/data/onpage-audit.json
  - seo-tools/data/tells-ia.json
  - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md
autonomous: true
requirements: [ONPAGE-01, ONPAGE-02, ONPAGE-05]
must_haves:
  truths:
    - "Las 22 URLs que quedan vivas tienen title y meta description reescritos dentro del límite de caracteres, y las 16 que compiten llevan la keyword primaria al frente."
    - "Las 16 URLs que compiten tienen H1 propuesto; las 6 que no compiten declaran el H1 que ya está publicado, sin cambiarlo."
    - "La auditoría sobre el mapa propuesto devuelve cero titles duplicados, cero H1 duplicados y cero metas faltantes, y lo hace ANTES de que v1.1 publique."
    - "Ninguna de las 6 URLs que declararon no competir toma en su title la keyword primaria de otra URL."
    - "Las compuertas de humanización y de YMYL existen como código ejecutable, no como recomendación: los cuatro planes de copy de la wave 3 tienen que pasarlas."
  artifacts:
    - seo-tools/data/onpage.json
    - seo-tools/data/onpage-audit.json
    - seo-tools/data/tells-ia.json
    - .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md
  key_links:
    - "`auditoria.ts` corre contra `data/onpage.json`, el paquete propuesto, y nunca contra el sitio vivo ni contra `/auditoria` de DinoRank (D-03, D-04)."
    - "`ymyl.ts` es la compuerta que consumen los cuatro planes de copy: sin ella cada plan inventaría su propio criterio de humanización."
    - "El H1 de las 6 URLs sin primaria se LEE de `src/` y no se propone: cambiarlo sería reabrir una decisión de la fase 14."
---

<objective>
Cerrar la metadata de las 22 URLs que quedan vivas y dejar armadas las dos compuertas que la fase
entera necesita: la auditoría de duplicados sobre el paquete propuesto, y el juego de reglas
ejecutables de humanización y de YMYL que los cuatro planes de copy tienen que pasar.

Propósito: ONPAGE-01 y ONPAGE-05 se cierran acá, antes de que se escriba una sola palabra más de
copy. Y las cuatro familias de la wave 3 corren en paralelo sin inventar cada una su propio
criterio de qué cuenta como texto humanizado y qué cuenta como afirmación médica sellada.

Salida: `data/onpage.json` con title, meta y H1 de las 24 filas del mapa, `15-AUDITORIA.md` con el
resultado de la auditoría local, y `ymyl.ts` como compuerta.

Decisiones que gobiernan este plan: D-03, D-04, D-06, D-07, D-08, D-10, D-11, D-13, D-15. El
registro completo está en el `<objective>` de `15-01-PLAN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-01-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CANIBALIZACION.md
@seo-tools/data/url-map.jsonl
@seo-tools/data/onpage-serp.json
@seo-tools/src/phase15/model.ts

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 15-01.
**Presupuesto de SerpApi de este plan: cero.** `url-map.jsonl` sigue de solo lectura.

**Hechos medidos, ya verificados, no hay que redescubrirlos:**

- Las 24 filas del mapa se reparten en 13 `reescribir`, 3 `crear`, 6 `dejar` y 2 `redirigir`.
- Las 6 de acción `dejar` son `/agendar`, `/blog`, `/contacto`, `/sedes`, `/sobre-el-doctor` y
  `/testimonios`. Cada una ya declara por escrito en `motivoSinPrimaria` por qué no compite, y en
  varios casos el motivo es que competir le quitaría la SERP a la home.
- Las 2 de acción `redirigir` son `/blog/estenosis-espinal-que-es` hacia
  `/servicios/estenosis-espinal`, y `/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos`
  hacia `/servicios/hernia-discal`. Su campo `redirigeA` ya trae el destino.
- El contenido publicado vive en `src/content/service-pages.ts` (`servicePages`, con los slugs
  `hernia-discal`, `estenosis-espinal`, `escoliosis`, `ortopedia-infantil`),
  `src/content/location-pages.ts` (`locationPages`, con `consultorio-privado`,
  `clinica-ricardo-palma`, `sanna-la-molina`, `clinica-tezza`) y `src/content/blog.ts`
  (`blogPosts`, con los cuatro slugs del blog). Las rutas estáticas están bajo `src/app/`. Todo en
  SOLO LECTURA.
- `/auditoria` de DinoRank responde HTTP 200 con `site.domain: soumahotel.com`, el proyecto de
  otro cliente dado de alta en esa cuenta. No es un 500 transitorio y no se reintenta (D-04).
- `/tfidf` de DinoRank lee nuestra URL pero devuelve el corpus de comparación vacío
  (`totalUrls: 0`, `wdfdf.media: []`, `df: null`), así que no sirve para comparar contra las
  páginas que ya posicionan (D-02).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Title, meta y H1 de las 24 filas del mapa</name>
  <files>seo-tools/src/phase15/metadatos.ts, seo-tools/src/phase15/metadatos.test.ts, seo-tools/data/onpage.json</files>
  <read_first>
    - `seo-tools/data/url-map.jsonl` completo, con especial atención a `motivoSinPrimaria` de las
      seis filas `dejar` y a `redirigeA` de las dos que redirigen.
    - `seo-tools/data/onpage-serp.json`: el H1 tiene que ser coherente con la jerarquía y con el
      formato que ya quedaron decididos.
    - `.../14-CANIBALIZACION.md`: qué pares se midieron y por qué ocho URLs declararon no competir.
    - `src/app/*/page.tsx` y los tres módulos de `src/content/`, en solo lectura, para localizar el
      H1 publicado de las seis URLs que no compiten.
  </read_first>
  <behavior>
    - Las 22 URLs que quedan vivas tienen title y meta; las 2 que redirigen salen con `title: null`
      y `redirigeA` cargado.
    - Ningún title pasa de 60 caracteres y ninguna meta de 155.
    - En las 16 que compiten, todos los tokens de tres letras o más de la keyword primaria están
      en el title y el primero arranca dentro de los primeros 12 caracteres.
    - Ninguna de las 6 URLs sin primaria toma en su title la keyword primaria completa de otra URL.
    - El H1 de las 16 que compiten es propuesto y sale marcado `h1Origen: "propuesto"`.
    - El H1 de las 6 sin primaria se lee del sitio publicado y sale marcado `h1Origen: "publicado"`
      con el archivo y la línea de donde salió.
    - Una URL sin H1 localizable en el sitio sale con `h1Origen: "ausente-en-el-sitio"` y el motivo
      escrito, en vez de recibir un H1 inventado.
    - Correr el generador dos veces produce el mismo archivo.
  </behavior>
  <action>
    Extender `metadatos.ts` de la URL del tracer a las 24 filas del mapa y emitir `data/onpage.json`
    con, por fila: `url`, `accion`, `keywordPrimaria`, `title`, `titleLargo`, `metaDescription`,
    `metaLargo`, `h1`, `h1Origen`, `origenDelH1`, `keywordAlFrente`, `redirigeA` y `formato`.
    Arriba, un bloque `resumen` con el reparto por acción.

    Cómo se escribe cada grupo:

    - **Las 16 que compiten (D-05).** Title con la keyword primaria al frente, dentro de 60. Meta
      dentro de 155, que diga qué resuelve la página y termine en una razón para entrar, sin
      prometer resultados (D-10). H1 propuesto, coherente con la jerarquía de
      `data/onpage-serp.json`: el H1 no repite el title palabra por palabra, porque son dos piezas
      con trabajos distintos.
    - **Las 6 que no compiten (D-06).** Title y meta descriptivos de lo que la página es, sin
      keyword de posicionamiento. Esto no es una omisión: el mapa midió que si `/sedes` o
      `/contacto` pelearan un término se lo quitarían a la home o a las cuatro sedes, y ese es
      justamente el motivo escrito en `motivoSinPrimaria`. El H1 no se propone: se lee del sitio y
      se transcribe. Cambiarlo desde acá reabriría una decisión de la fase 14 sin dato nuevo.
    - **Las 2 que redirigen (D-07).** No reciben title ni meta. La fila existe igual porque quien
      abra esa URL tiene que encontrar la instrucción de redirección, y el destino del 301 ya está
      en el mapa.

    La comprobación de "keyword al frente" es por tokens y sin tildes. Comparar la cadena entera
    fallaría con títulos escritos en español natural, donde la keyword se parte con preposiciones:
    `cirujano de columna en Clínica Ricardo Palma` contiene la primaria aunque no la contenga como
    subcadena literal.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=metadatos && npm run typecheck && ./node_modules/.bin/tsx src/phase15/metadatos.ts --out data/onpage.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; process.stdout.write(String(d.length))")" -eq 24`
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; const v=d.filter(r=>r.accion!=='redirigir'); const mal=v.filter(r=>!r.title||!r.metaDescription||r.title.length>60||r.metaDescription.length>155); process.stdout.write(String(mal.length))")" -eq 0`
      — el contrato de D-13 con la fase 10 de v1.1 se cumple en las 22.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; const n=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const mal=d.filter(r=>r.keywordPrimaria).filter(r=>{const t=n(r.title); const tk=n(r.keywordPrimaria).split(/\s+/).filter(x=>x.length>=3); if(!tk.every(x=>t.includes(x)))return true; return t.indexOf(tk[0])>12;}); process.stdout.write(String(mal.length))")" -eq 0`
      — las 16 llevan la keyword primaria al frente, medido y no afirmado.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; const n=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase(); const prim=d.filter(r=>r.keywordPrimaria).map(r=>n(r.keywordPrimaria)); const sin=d.filter(r=>r.accion==='dejar'); const mal=sin.filter(r=>prim.some(p=>n(r.title).includes(p))); process.stdout.write(String(mal.length))")" -eq 0`
      — ninguna URL que declaró no competir se lleva por el title la primaria de otra.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; const mal=d.filter(r=>r.accion==='dejar'&&r.h1Origen==='propuesto'); process.stdout.write(String(mal.length))")" -eq 0`
      — a las seis que no compiten no se les propone H1 nuevo.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; const mal=d.filter(r=>r.h1Origen==='publicado'&&(!r.origenDelH1||!/:\d+$/.test(r.origenDelH1))); process.stdout.write(String(mal.length))")" -eq 0`
      — todo H1 transcrito declara archivo y línea de donde salió.
    - `test "$(cd seo-tools && node -e "const d=require('./data/onpage.json').filas; const mal=d.filter(r=>r.accion==='redirigir'&&(r.title!==null||!r.redirigeA)); process.stdout.write(String(mal.length))")" -eq 0`
    - `test "$(shasum -a 256 seo-tools/data/url-map.jsonl | cut -d' ' -f1)" = "ada0a4a1fe6c0d149de428352ed07260c1e48a88d4b897d2d15e6c1f6ea951b5"`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
    - Determinismo: dos corridas producen el mismo SHA-256 de `data/onpage.json`.
    - Las ocho conductas declaradas tienen prueba propia y la suite completa queda en verde.
  </acceptance_criteria>
  <done>
    Las 22 URLs vivas tienen title y meta dentro de contrato, las 16 que compiten llevan la keyword
    al frente y su H1 propuesto, y las 6 que no compiten transcriben el H1 publicado con su
    procedencia.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: La auditoría de duplicados sobre el mapa propuesto</name>
  <files>seo-tools/src/phase15/auditoria.ts, seo-tools/src/phase15/auditoria.test.ts, seo-tools/data/onpage-audit.json, .planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md</files>
  <read_first>
    - `seo-tools/data/onpage.json` de la tarea 1.
    - `.../14-CANIBALIZACION.md`: el precedente de cómo se documenta un cruce del mapa contra sí
      mismo, incluida la forma de declarar qué queda fuera del alcance y por qué.
    - `15-CONTEXT.md`, sección `<decisions>`, los dos párrafos sobre ONPAGE-05 y sobre
      `/auditoria`.
  </read_first>
  <behavior>
    - Dos URLs con el mismo title salen listadas como duplicado con las dos URLs nombradas.
    - Dos URLs con el mismo H1 salen listadas como duplicado.
    - Una URL viva sin meta description sale listada como meta faltante.
    - La comparación de duplicados ignora mayúsculas, tildes y espacios repetidos: `Hernia discal`
      y `hernia  discal` son el mismo title.
    - Las 2 URLs que redirigen quedan fuera del alcance de la auditoría, nombradas y con motivo.
    - Un title de 61 caracteres sale listado como fuera de rango aunque no esté duplicado.
    - La auditoría no hace ninguna llamada de red.
  </behavior>
  <action>
    Implementar `auditoria.ts` como función pura sobre `data/onpage.json` que emite
    `data/onpage-audit.json` con seis listas —`titlesDuplicados`, `h1Duplicados`, `metasFaltantes`,
    `titlesFueraDeRango`, `metasFueraDeRango`, `keywordNoAlFrente`—, un bloque `alcance` que dice
    cuántas URLs se auditaron y cuáles quedaron fuera con su motivo, y un bloque `resumen`.

    Escribir `15-AUDITORIA.md` con el resultado legible y, arriba de todo, la razón por la que esta
    auditoría es local y no sale de DinoRank. Va con el dato crudo, no como excusa: `/auditoria`
    audita un sitio **publicado**, y acá se está auditando un paquete que todavía no se publicó, que
    es exactamente el momento en que la auditoría sirve. Y hay un segundo motivo, más grave que un
    endpoint caído: pidiéndole `drangulocolumna.com` responde HTTP 200 con `site.domain:
    soumahotel.com`, el proyecto de otro cliente que hay dado de alta en esa cuenta. Un fallo
    silencioso que entrega datos ajenos es peor que uno ruidoso, y queda escrito para que nadie lo
    reintente creyendo que fue un 500 transitorio (D-04).

    El documento cierra diciendo con todas las letras qué queda habilitado: la auditoría dio limpia
    **antes** de que v1.1 publique, que es lo que ONPAGE-05 pide.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=auditoria && npm run typecheck && ./node_modules/.bin/tsx src/phase15/auditoria.ts --data data/onpage.json --out data/onpage-audit.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const a=require('./data/onpage-audit.json'); process.stdout.write(String(a.titlesDuplicados.length+a.h1Duplicados.length+a.metasFaltantes.length))")" -eq 0`
      — cero titles duplicados, cero H1 duplicados y cero metas faltantes. Es ONPAGE-05 entero.
    - `test "$(cd seo-tools && node -e "const a=require('./data/onpage-audit.json'); process.stdout.write(String(a.titlesFueraDeRango.length+a.metasFueraDeRango.length+a.keywordNoAlFrente.length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const a=require('./data/onpage-audit.json'); process.stdout.write(String(a.alcance.auditadas))")" -eq 22`
    - `test "$(cd seo-tools && node -e "const a=require('./data/onpage-audit.json'); const f=a.alcance.fuera||[]; process.stdout.write(String(f.length===2&&f.every(x=>x.motivo&&x.motivo.length>20)?1:0))")" -eq 1`
      — las dos URLs que redirigen quedan fuera nombradas y con motivo, no desaparecidas.
    - `test "$(grep -c 'soumahotel' '.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-AUDITORIA.md')" -ge 1`
      — el motivo por el que no se usó `/auditoria` queda escrito con el dato, no como una nota
      vaga de que el endpoint no estaba disponible.
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
    - Determinismo: dos corridas producen el mismo SHA-256 de `data/onpage-audit.json`.
    - Las siete conductas declaradas tienen prueba propia y la suite completa queda en verde.
  </acceptance_criteria>
  <done>
    ONPAGE-05 está cerrado sobre el mapa propuesto: cero titles duplicados, cero H1 duplicados y
    cero metas faltantes, con el alcance y las exclusiones declaradas en `15-AUDITORIA.md`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Las compuertas de YMYL y de humanización, y el generador para los cuatro tipos de documento</name>
  <files>seo-tools/src/phase15/ymyl.ts, seo-tools/src/phase15/ymyl.test.ts, seo-tools/src/phase15/paquete.ts, seo-tools/src/phase15/paquete.test.ts, seo-tools/data/tells-ia.json</files>
  <read_first>
    - `~/.claude/skills/humanizer/SKILL.md` completo. Las reglas que se vuelven código salen de
      ahí, no de la intuición de quien ejecuta.
    - `seo-tools/src/phase15/paquete.ts` tal como lo dejó el tracer, y
      `.../paquetes/servicios-hernia-discal.md`, que es el formato aprobado por Juan.
    - `seo-tools/data/copy-guias.json`: la forma del dataset de copy que las cuatro familias van a
      llenar.
    - `seo-tools/data/onpage-serp.json`, campo `minimoDePalabras` por URL.
  </read_first>
  <behavior>
    - Una sección de tipo clínico sin `aprobacion: "pendiente-doctor"` hace fallar la compuerta.
    - Una afirmación cuyo texto tiene una cifra y no trae `fuente` hace fallar la compuerta.
    - Un texto que dice cuántas cirugías hizo el doctor, cuántos años lleva ejerciendo o qué tasa
      de éxito tiene hace fallar la compuerta, aunque traiga fuente: nadie verificó esas cifras.
    - Un texto que nombra Montefiori hace fallar la compuerta; los cuatro nombres de sede vigentes
      pasan.
    - Una raya larga, una raya corta o una comilla tipográfica dentro de la región de copy hace
      fallar la compuerta. Fuera de esa región no se mira nada: la prosa heredada de la fase 14
      tiene doce rayas largas y no es nuestra.
    - Un emoji dentro de la región de copy hace fallar la compuerta.
    - Una muletilla del diccionario de tells hace fallar la compuerta, con la muletilla y la URL
      nombradas.
    - Una página cuyo cuerpo no llega a su `minimoDePalabras` hace fallar la compuerta.
    - Una página cuya desviación estándar de longitud de oración es menor a 6 palabras hace fallar
      la compuerta.
    - Un encabezado de cuatro palabras o más donde todas las palabras de cuatro letras o más
      arrancan en mayúscula hace fallar la compuerta.
  </behavior>
  <action>
    Implementar `ymyl.ts` como la compuerta única que los cuatro planes de copy de la wave 3 van a
    correr. Recibe un dataset de copy y su URL, y devuelve la lista de hallazgos con la URL, la
    sección y el texto exacto que falló. No devuelve un booleano: devuelve evidencia, porque quien
    tiene que corregir necesita saber dónde.

    Las reglas se agrupan en dos familias.

    **YMYL (D-08, D-10, D-11).** Toda sección clínica sellada como pendiente del doctor. Toda
    afirmación con cifra con su fuente declarada. Prohibición dura de cifras sobre el propio
    doctor, porque ninguna está verificada y la restricción del proyecto es explícita: ante la
    duda entre una frase con más fuerza comercial y una verificable, va la verificable. Y las
    sedes que se nombren son las cuatro vigentes.

    **Humanización.** Las reglas salen del skill y se implementan sobre la región de copy
    delimitada por las dos marcas de comentario que el tracer ya dejó puestas. Acotar la región no
    es una concesión: `url-map.jsonl` trae doce rayas largas en campos de prosa que esta fase no
    escribió, así que una regla global fallaría por texto heredado y obligaría a aflojarla.

    Sembrar `data/tells-ia.json` con el diccionario de muletillas en español que la compuerta
    busca. El diccionario arranca con, al menos: cabe destacar, es importante señalar, en la
    actualidad, juega un papel fundamental, un pilar fundamental, en el corazón de, no solo sino
    también, en definitiva, sumérgete, descubre cómo, en resumen, a día de hoy, se erige como, un
    abanico de, de la mano de, la clave está en, sin lugar a dudas, es fundamental destacar, en
    este sentido, cabe mencionar. La lista es ampliable y vive en datos, no en el código, para que
    agregar una muletilla no sea un cambio de programa.

    Generalizar `paquete.ts` a los cuatro tipos de documento que la fase entrega: guía clínica,
    página de servicio, ficha de sede, y el documento corto de las URLs que solo llevan title y
    meta o que se apagan con un 301. Los cuatro comparten cabecera, y el corto dice en dos líneas
    qué hacer con esa URL para que quien la busque encuentre la respuesta y no un hueco (D-14).

    El generador tiene una obligación propia: no emitir rayas largas ni comillas tipográficas
    dentro de la región de copy, ni siquiera en los rótulos que él mismo escribe. Si necesita
    separar dos ideas, usa coma o punto.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern="ymyl|paquete" && npm run typecheck && ./node_modules/.bin/tsx src/phase15/ymyl.ts --data data/copy-guias.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const t=require('./data/tells-ia.json'); const l=t.tells||t; process.stdout.write(String(Array.isArray(l)&&l.length>=20?1:0))")" -eq 1`
    - La compuerta corrida sobre `data/copy-guias.json`, que ya tiene la guía aprobada por Juan,
      devuelve cero hallazgos y sale con estado 0.
    - `test "$(cd seo-tools && node -e "const s=require('fs').readFileSync('src/phase15/ymyl.ts','utf8'); process.stdout.write(String(/copy:inicio/.test(s)&&/copy:fin/.test(s)?1:0))")" -eq 1`
      — las reglas de humanización están acotadas a la región de copy y no al documento entero.
    - Las once conductas declaradas tienen prueba propia, cada una con un caso que falla y uno que
      pasa. La prueba de la raya larga incluye el caso de prosa heredada fuera de la región, que
      **tiene** que pasar.
    - `test "$(cd seo-tools && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /agendar >/dev/null 2>&1 && echo 1 || echo 0)" -eq 1`
      — el documento corto de una URL sin copy se genera sin error.
    - `test "$(cd seo-tools && ./node_modules/.bin/tsx src/phase15/paquete.ts --url /blog/estenosis-espinal-que-es >/dev/null 2>&1 && echo 1 || echo 0)" -eq 1`
      — el documento de una URL que se apaga con 301 también existe y dice hacia dónde va (D-07).
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Existe una compuerta ejecutable de YMYL y de humanización con evidencia por hallazgo, y el
    generador cubre los cuatro tipos de documento que la fase entrega.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copy clínico → paciente | Contenido YMYL. Una afirmación médica sin verificar publicada es daño real. |
| `metadatos.ts` → H1 publicado en `src/` | Se lee y se transcribe. Proponerle uno nuevo reabriría una decisión de la fase 14 sin dato nuevo. |
| Auditoría local → auditoría de DinoRank | El endpoint del proveedor devuelve datos de otro cliente sin marcar error. Usarlo sería auditar el hotel de otro y creer que se auditó este sitio. |
| Compuerta de humanización → prosa heredada | Una regla sin región falla por texto de la fase 14 y presiona para aflojarla. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-08 | Spoofing | Auditoría con datos de otro dominio | critical | transfer | `/auditoria` de DinoRank no se usa; la auditoría corre local sobre el paquete propuesto y el motivo queda escrito con el dominio real que devolvió. |
| T-15-09 | Tampering | H1 reescrito en una URL que declaró no competir | high | mitigate | `h1Origen: "publicado"` obligatorio en las seis; criterio que exige cero H1 propuestos ahí. |
| T-15-10 | Tampering | Canibalización reintroducida por el title | high | mitigate | Criterio que prueba que ningún title de las seis contiene la primaria de otra URL. |
| T-15-11 | Repudiation | Compuerta que dice sí o no sin evidencia | medium | mitigate | `ymyl.ts` devuelve hallazgos con URL, sección y texto, no un booleano. |
| T-15-12 | Denial of Service | Regla de humanización sin región | medium | mitigate | Las reglas se acotan entre las dos marcas de copy; prueba explícita con prosa heredada que tiene que pasar. |
| T-15-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si algún paso lo requiriera, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- `npx tsc --noEmit` en la raíz con estado 0.
- SHA-256 de `seo-tools/data/url-map.jsonl` y de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
</verification>

<success_criteria>
- ONPAGE-01 cerrado: las 22 URLs vivas con title y meta dentro de límite y con la keyword al
  frente donde corresponde.
- ONPAGE-05 cerrado: la auditoría sobre el mapa propuesto da cero duplicados y cero metas
  faltantes, corrida antes de que v1.1 publique.
- Existe la compuerta ejecutable que los cuatro planes de copy de la wave 3 tienen que pasar.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 15-02)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Metadata por URL | `seo-tools/data/onpage.json` | Dato |
| Resultado de la auditoría | `seo-tools/data/onpage-audit.json` | Dato |
| Diccionario de muletillas | `seo-tools/data/tells-ia.json` | Dato |
| Auditoría legible | `.../15-AUDITORIA.md` | Entregable |
| Compuerta de YMYL y humanización | `seo-tools/src/phase15/ymyl.ts` | Código |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/15-paquete-on-page-por-url/15-02-SUMMARY.md` al terminar.
</output>
