---
phase: 13-clusters-competencia-y-las-10-de-oro
workstream: seo-keywords
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [KWR-04, COMP-03, SHEET-01, SHEET-03]
files_modified:
  - seo-tools/src/sources/serpapi.ts
  - seo-tools/src/sources/serpapi.test.ts
  - seo-tools/src/phase13/args.ts
  - seo-tools/src/phase13/serp.ts
  - seo-tools/src/phase13/serp.test.ts
  - seo-tools/src/phase13/serp-show.ts
  - seo-tools/src/phase13/pagetype.ts
  - seo-tools/src/phase13/pagetype.test.ts
  - seo-tools/src/phase13/tab-recon.ts
  - seo-tools/src/sheets/schema.ts
  - seo-tools/src/sheets/upsert.ts
  - seo-tools/src/sheets/upsert.test.ts
  - seo-tools/data/serp-page-types.json
  - seo-tools/data/sheet-columns.json
  - seo-tools/docs/sheet-model.md
  - .planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md

must_haves:
  truths:
    - "Una SERP ya cacheada se lee como registro tipado con posiciones, URLs y dominios, sin salir a la red y sin consumir una búsqueda (D-06, D-15)."
    - "Cada resultado orgánico de las 12 SERP cacheadas queda clasificado por tipo de página con reglas deterministas (COMP-03)."
    - "El escritor del Sheet puede escribir las columnas de la fase 13 y sigue sin tocar las de las fases 14 y 15."
    - "La forma real del tab transpuesto `Competitor Analysis` está medida y escrita, no supuesta (SHEET-03)."
  artifacts:
    - seo-tools/src/phase13/serp.ts
    - seo-tools/src/phase13/pagetype.ts
    - seo-tools/data/serp-page-types.json
    - .planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md
  key_links:
    - "`serpCompleta()` reusa `parametrosBusqueda()` sin tocar un parámetro: si la clave de caché cambia, las 12 capturas de la fase 12 se pierden y la fase arranca con 12 búsquedas menos."
    - "`sheet-columns.json` es lo que decide qué columnas escribe el upsert: `Cluster` y `Top Result` tienen que quedar escribibles sin arrastrar `URL` ni `Suggested H1`."
---

<objective>
Esta fase convierte un universo plano en clusters validados contra la SERP real de Lima. Antes de
gastar una sola de las 90 búsquedas del presupuesto, este plan prueba el camino completo de punta a
punta sobre una keyword que **ya está capturada**: leer la SERP del caché, tipificarla, y dejar al
Sheet listo para recibir las columnas de la fase.

Propósito: si el camino falla, falla con coste cero. Descubrir después de gastar 90 búsquedas que la
clave de caché cambió, o que el escritor no alcanza la columna `Cluster`, sería irreparable dentro
del horizonte del proyecto: la cuota de SerpApi no se repone hasta el 2026-08-21.

Salida: el parser de SERP completa, el clasificador de tipo de página, el modelo del Sheet con las
columnas de la fase 13 habilitadas y el reconocimiento medido del tab transpuesto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/seo-keywords/ROADMAP.md
@.planning/workstreams/seo-keywords/STATE.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-CONTEXT.md
@.planning/workstreams/seo-keywords/data/serpapi-validacion-2026-08-10.md
@.planning/workstreams/seo-keywords/data/sheet-recon-2026-08-10.md
@seo-tools/src/sources/serpapi.ts
@seo-tools/src/cache.ts
@seo-tools/src/sheets/schema.ts
@seo-tools/src/sheets/upsert.ts
</context>

<decisions_map>
## Mapa de decisiones de la fase

`13-CONTEXT.md` registra las decisiones en prosa y sin identificadores. Los cinco planes las citan
con estas etiquetas, que valen para toda la fase 13:

| ID | Decisión | Dónde vive en el CONTEXT | Planes que la implementan |
|---|---|---|---|
| D-01 | 90 búsquedas de SerpApi para la fase, 24 de reserva | El recurso escaso | 13-02 |
| D-02 | Los campos de posición y URL de DinoRank están vacíos: la SERP exige SerpApi de verdad | El recurso escaso | 13-02 |
| D-03 | La captura se concentra en unas 90 candidatas a keyword primaria | Clustering | 13-02 |
| D-04 | El resto del universo se agrupa por texto y se marca como tal | Clustering | 13-02 |
| D-05 | Umbral de solape: tres URLs compartidas en el top 10 | Clustering | 13-02 |
| D-06 | Las 12 capturas de la fase 12 ya cuentan como hechas | Clustering | 13-01, 13-02 |
| D-07 | Ahrefs vuelve, acotado a las mismas candidatas y con el `select` recortado | Dificultad | 13-03, 13-04 |
| D-08 | Cuando Ahrefs y DinoRank discrepan, se publican las dos con la fuente marcada | Dificultad | 13-03, 13-04 |
| D-09 | El punto dulce cruza el KD con quién ocupa realmente el top 10 | Dificultad | 13-01, 13-04 |
| D-10 | Las 10 de Oro: valor de negocio primero, alcanzabilidad después, con justificación legible | Dificultad | 13-05 |
| D-11 | Los cinco competidores quedan definidos | Competencia | 13-03 |
| D-12 | DR y referring domains salen de Ahrefs, cinco dominios | Competencia | 13-03 |
| D-13 | El gap se deriva de las SERP ya capturadas, con coste adicional cero | Competencia | 13-04 |
| D-14 | Las keywords de marca ajena son inteligencia, nunca objetivo | Competencia | 13-02, 13-04 |
| D-15 | Capturar una vez y reutilizar: KWR-04 y COMP-03 comparten la captura | Specific Ideas | 13-01, 13-02 |

**Ideas diferidas del CONTEXT, que ningún plan de esta fase implementa:** vincular Search Console
al proyecto de DinoRank (es de la fase 14), esperar el primer rastreo de DinoRank sobre el sitio
(fase 15), subir de plan en SerpApi, y resolver la discrepancia de volumen entre Ahrefs y
DataForSEO, que esta fase expone publicando las dos fuentes pero no investiga.

**Queda a criterio propio, delegado explícitamente:** el umbral numérico exacto de candidatas dentro
del orden de 90, el algoritmo concreto de similitud de texto, la forma del archivo de clusters y de
su volcado al Sheet, y cómo se ordena y se presenta la justificación de las 10 de Oro.
</decisions_map>

<constraints>
## Reglas duras de este workstream, sin excepción

1. **Nada se escribe bajo `src/` de la raíz.** El milestone v1.1 corre en paralelo y despliega a
   producción desde ahí. Leer `src/` está permitido; escribir, no. Todo el código de esta fase vive
   bajo `seo-tools/`.
2. **Nada se escribe en `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`
   de la raíz ni bajo `.planning/workstreams/milestone/`.** Los artefactos de planificación de este
   workstream viven solo en `.planning/workstreams/seo-keywords/`.
3. **`seo-tools/src/cli.ts` está cerrado desde la fase 12 y este plan no lo edita.** Los módulos
   ejecutables de la fase 13 son puntos de entrada propios bajo `seo-tools/src/phase13/`, y se
   invocan así:
   ```
   cd seo-tools && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase13/<modulo>.ts <banderas>
   ```
   Verificado el 2026-08-11: el binario existe y `--env-file` carga las credenciales.
   **Ningún archivo de `src/phase13/` puede importar el despachador `src/cli.ts`**: ese módulo
   invoca `main()` al cargarse, así que importarlo desde otro punto de entrada ejecuta el
   despachador con los argumentos equivocados. Por eso este plan crea `src/phase13/args.ts`.
4. **El `exclude: ["node_modules","seo-tools"]` del `tsconfig.json` de la raíz no se toca.** Es el
   guardarraíl que protege el build de producción del otro workstream.
5. **`git add` por archivo nombrado, nunca `git add -A`.**
6. **La shell es zsh:** todo glob va entre comillas o muere con `no matches found`.
</constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarea 1: Una SERP cacheada de punta a punta, con coste cero</name>

  <read_first>
    - `seo-tools/src/sources/serpapi.ts` — `parametrosBusqueda`, `consultar`, `OpcionesConsulta`,
      `SERPAPI_FUENTE`, `SERPAPI_ENDPOINT`. `consultar` es privada: hay que exportarla o construir
      la nueva función dentro del mismo archivo.
    - `seo-tools/src/cache.ts` — `fetchThroughCache`, `cacheKey`, `readEnvelope`, el modo `offline`.
    - `seo-tools/src/config.ts` — `CACHE_DIR`, `SEO_TOOLS_ROOT`, `CliError`.
    - `.planning/workstreams/seo-keywords/data/serpapi-validacion-2026-08-10.md` — qué bloques
      devuelve la fuente y para qué sirve cada uno.
  </read_first>

  <files>
    seo-tools/src/sources/serpapi.ts,
    seo-tools/src/sources/serpapi.test.ts,
    seo-tools/src/phase13/args.ts,
    seo-tools/src/phase13/serp.ts,
    seo-tools/src/phase13/serp.test.ts,
    seo-tools/src/phase13/serp-show.ts
  </files>

  <behavior>
    - `serpCompleta("hernia discal", {cacheDir, offline:true})` devuelve el registro tipado sin
      emitir ninguna llamada de red y sin necesitar credencial.
    - La clave de caché que calcula `serpCompleta` es **idéntica** a la que ya existe en disco para
      esa keyword: `cacheKey(SERPAPI_FUENTE, SERPAPI_ENDPOINT, parametrosBusqueda(q))` resuelve a un
      archivo existente bajo `.cache/serpapi/`.
    - Los orgánicos salen ordenados por posición ascendente, con `dominio` en minúsculas y sin el
      prefijo de subdominio genérico de web.
    - Una respuesta sin `local_results`, sin `answer_box` o sin `ai_overview` produce listas vacías
      y `null`, nunca una excepción: 0 de las 12 capturas traen `answer_box` y solo 3 traen pack
      local, así que la ausencia es el caso normal y no un error.
    - Un enlace que no parsea como URL se descarta de la lista de dominios y no tumba la captura.
    - `args.ts` parsea pares `--bandera` y `--bandera=valor` y expone lectores de texto, booleano y
      número, sin importar nada del despachador.
  </behavior>

  <action>
    Agregar a `seo-tools/src/sources/serpapi.ts` la lectura completa de la SERP, que es lo que
    KWR-04 y COMP-03 comparten (D-15). Exportar una interfaz `SerpCompleta` y una función
    `serpCompleta(q, opciones)` que pase por el mismo camino de caché que `busquedaGeolocalizada`.

    El punto crítico: la nueva función **tiene que llamar a `parametrosBusqueda(q)` sin agregar,
    quitar ni renombrar un solo parámetro**. Si los parámetros cambian, cambia la clave de caché y
    las 12 capturas de Lima de la fase 12 dejan de valer (D-06). La ubicación, el dominio de Google,
    el país y el idioma ya están fijados en ese archivo por la validación del 2026-08-10 y no se
    tocan.

    Campos reales de la respuesta, medidos el 2026-08-11 sobre las 12 capturas en caché. Escribir el
    parser contra estos nombres y no contra los que uno esperaría:

    | Bloque | Ruta | Campos que interesan |
    |---|---|---|
    | Orgánicos | `organic_results[]` | `position`, `link`, `title`, `displayed_link`, `snippet`, `source` |
    | Pack local | `local_results.places[]` | presente en 3 de 12 |
    | Preguntas | `related_questions[]` | `question`, `type` |
    | Relacionadas | `related_searches[]` | `query` |
    | Resumen de IA | `ai_overview` | `text_blocks`, `references` |
    | Destacado | `answer_box` | **ausente en las 12** |
    | Vídeos | `inline_videos` | presente en 8 de 12 |

    Forma de `SerpCompleta`: la keyword consultada, la lista de orgánicos con posición, URL, dominio
    normalizado, título y fragmento, la lista del pack local con nombre, calificación y cantidad de
    reseñas, las relacionadas, las preguntas, el bloque destacado como objeto o `null`, y dos
    banderas para el resumen de IA y para el carrusel de vídeos. Agregar la marca de tiempo del
    envelope, no la del reloj, para que dos lecturas del mismo archivo den lo mismo.

    Crear `seo-tools/src/phase13/args.ts` con el parseo de banderas propio de la fase. Es
    deliberadamente una copia funcional del parser de la fase 12 y no una importación: importar el
    despachador ejecuta su `main()` al cargarse. Dejar esa razón escrita en la cabecera del archivo.

    Crear `seo-tools/src/phase13/serp.ts` como capa fina sobre la fuente: recibe una keyword,
    devuelve el registro, y sabe leer el caché sin credencial. Y crear
    `seo-tools/src/phase13/serp-show.ts` como punto de entrada que imprime el registro de una
    keyword en JSON, con `--offline` para prohibir la red.

    Correr el tracer sobre `hernia discal`, que ya está en caché. Es una de las cuatro condiciones
    núcleo del negocio y su SERP genérica está dominada por contenido internacional
    (mayoclinic.org, medlineplus.gov, quironsalud.com): eso es exactamente la señal que la fase
    necesita ver desde el primer minuto.
  </action>

  <verify>
    <automated>
cd seo-tools
ANTES=$(node -e "console.log(JSON.parse(require('fs').readFileSync('.cache/_quota.json','utf8')).sources.serpapi.calls)")
npm run typecheck
npm test
./node_modules/.bin/tsx src/phase13/serp-show.ts --keyword "hernia discal" --offline > /tmp/serp-tracer.json
node -e "
const s=JSON.parse(require('fs').readFileSync('/tmp/serp-tracer.json','utf8'));
if(s.organicos.length<7) throw new Error('orgánicos insuficientes: '+s.organicos.length);
if(s.organicos[0].posicion!==1) throw new Error('el primero no es la posición 1');
if(!s.organicos.every(o=>typeof o.dominio==='string' && o.dominio===o.dominio.toLowerCase())) throw new Error('dominio sin normalizar');
if(s.preguntas.length!==4) throw new Error('preguntas: '+s.preguntas.length);
if(s.destacado!==null) throw new Error('esta captura no trae bloque destacado');
console.log('tracer ok:', s.organicos.length, 'orgánicos,', s.relacionadas.length, 'relacionadas');
"
DESPUES=$(node -e "console.log(JSON.parse(require('fs').readFileSync('.cache/_quota.json','utf8')).sources.serpapi.calls)")
test "$ANTES" -eq "$DESPUES"
test "$DESPUES" -eq 12
test "$(grep -rl 'cli\.js' src/phase13/ | wc -l | tr -d ' ')" -eq 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` sale con código 0 y `npm test` sale con código 0 sin ninguna credencial
      en el entorno (`env -u SERPAPI_API_KEY -u DINORANK_API_KEY npm test`).
    - `serp-show.ts --offline` sobre `hernia discal` sale con código 0 e imprime 7 orgánicos, 8
      relacionadas y 4 preguntas.
    - El contador de SerpApi del libro de cuota vale **12 antes y 12 después**. La tarea no gasta
      ni una búsqueda.
    - Hay una prueba nombrada que afirma que la clave de caché de `serpCompleta` coincide con la de
      `busquedaGeolocalizada` para la misma keyword.
    - Hay una prueba por cada rama tolerante: sin pack local, sin bloque destacado, sin resumen de
      IA y con un enlace que no parsea.
    - Ningún archivo de `src/phase13/` importa el módulo del despachador.
  </acceptance_criteria>

  <done>
    Una keyword real recorre el camino completo —caché, parser, registro tipado, salida legible—
    con coste cero de cuota, y las 12 capturas de la fase 12 quedan confirmadas como reutilizables.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Qué tipo de página premia Google, con reglas en archivo de datos</name>

  <read_first>
    - `seo-tools/data/intent-rules.json` — el patrón que estableció la fase 12: los patrones viven
      en un archivo de datos y afinarlos no obliga a tocar código ni pruebas.
    - `seo-tools/src/keywords/classify.ts` — cómo se lee ese archivo y cómo se declara la
      precedencia entre niveles.
    - `.planning/workstreams/seo-keywords/data/serpapi-validacion-2026-08-10.md`, sección
      "Consecuencias para la fase 13" — el top 10 real de `cirujano de columna` en Lima, que es el
      caso de referencia.
    - `seo-tools/src/phase13/serp.ts` — el registro que produjo la tarea 1.
  </read_first>

  <files>
    seo-tools/src/phase13/pagetype.ts,
    seo-tools/src/phase13/pagetype.test.ts,
    seo-tools/data/serp-page-types.json,
    seo-tools/src/phase13/serp-show.ts
  </files>

  <behavior>
    - Cada resultado orgánico recibe exactamente un tipo, y el nivel por defecto nunca deja un
      resultado sin clasificar.
    - `doctoralia.pe` y cualquier agregador de fichas resuelven a directorio.
    - `mayoclinic.org`, `medlineplus.gov` y `quironsalud.com` resuelven a contenido internacional,
      que es un tipo aparte de guía: son páginas que un consultorio de Lima no disputa.
    - `facebook.com`, `instagram.com`, `youtube.com` y `pe.linkedin.com` resuelven a red social.
    - Un dominio de médico individual con una URL construida sobre la keyword —el patrón de
      `clinicarthromeds.pe/traumatologo-especialista-en-columna-lima-peru/`— resuelve a página de
      servicio, no a ficha de clínica.
    - El tipo del cluster es el tipo con más presencia en el top 10, con desempate declarado y
      estable: dos corridas sobre la misma SERP dan el mismo tipo.
    - La clasificación es determinista y no llama a nadie: cero red, cero modelo.
  </behavior>

  <action>
    Crear `seo-tools/data/serp-page-types.json` con los tipos y sus patrones. Los cuatro que pide el
    ROADMAP son guía, página de servicio, ficha de clínica y directorio; la medición sobre las 12
    capturas obliga a tres más para no forzar resultados dentro de una caja que no les corresponde:
    contenido internacional, red social y otro. La lista de dominios repetidos medida el 2026-08-11
    sobre las 12 SERP es el punto de partida y está en el `<read_first>` de esta tarea.

    Declarar la precedencia entre tipos igual que hace `intent-rules.json`: un orden explícito en el
    archivo, no el orden de iteración de un objeto.

    Crear `seo-tools/src/phase13/pagetype.ts` que aplique esas reglas sobre un `SerpCompleta` y
    devuelva dos cosas: el tipo de cada resultado orgánico y el tipo dominante del top 10 con su
    reparto. Ese reparto es lo que COMP-03 pide registrar por cluster, y es también la mitad de la
    evidencia del punto dulce de KWR-05 (D-09): saber que la posición 3 la ocupa una clínica con
    marca fuerte es lo que el KD promedio no sabe.

    Extender `serp-show.ts` para que imprima también el tipo por resultado y el reparto del top 10.
  </action>

  <verify>
    <automated>
cd seo-tools
npm run typecheck
npm test
./node_modules/.bin/tsx src/phase13/serp-show.ts --keyword "hernia discal" --offline --with-types > /tmp/pt1.json
./node_modules/.bin/tsx src/phase13/serp-show.ts --keyword "hernia discal" --offline --with-types > /tmp/pt2.json
test "$(shasum -a 256 /tmp/pt1.json | cut -d' ' -f1)" = "$(shasum -a 256 /tmp/pt2.json | cut -d' ' -f1)"
node -e "
const fs=require('fs');const p='.cache/serpapi';
const {serpCompletaDesdeEnvelope}=require('node:module');
" 2>/dev/null || true
./node_modules/.bin/tsx -e "
import {readdirSync,readFileSync} from 'node:fs';
import {clasificarSerp} from './src/phase13/pagetype.ts';
import {serpDesdeCuerpo} from './src/phase13/serp.ts';
let total=0, sinTipo=0;
for (const f of readdirSync('.cache/serpapi')) {
  if (!f.endsWith('.json')) continue;
  const e = JSON.parse(readFileSync('.cache/serpapi/'+f,'utf8'));
  const serp = serpDesdeCuerpo(String(e.request.q), e);
  const r = clasificarSerp(serp);
  for (const o of r.resultados) { total++; if (!o.tipo) sinTipo++; }
  if (!r.tipoDominante) throw new Error('cluster sin tipo dominante: '+e.request.q);
}
console.log('resultados clasificados:', total, 'sin tipo:', sinTipo);
if (sinTipo !== 0) throw new Error('quedaron resultados sin tipo');
if (total < 90) throw new Error('se clasificaron menos resultados de los que hay: '+total);
"
    </automated>
  </verify>

  <acceptance_criteria>
    - Los **95 resultados orgánicos** de las 12 SERP cacheadas quedan clasificados: cero sin tipo.
    - Las 12 SERP producen un tipo dominante; ninguna queda sin él.
    - Dos ejecuciones consecutivas producen archivos con el mismo SHA-256.
    - Hay una prueba nombrada por cada comportamiento del bloque `<behavior>`, con dominios reales
      de las capturas y no inventados.
    - `test "$(grep -rniE 'fetch\(|https?://|openai|anthropic' seo-tools/src/phase13/pagetype.ts | wc -l | tr -d ' ')" -eq 0`
      confirma que el clasificador no consulta a nadie.
    - Los patrones viven en `data/serp-page-types.json`: cambiar un dominio de categoría no obliga a
      editar `pagetype.ts`.
  </acceptance_criteria>

  <done>
    Cada resultado de cada SERP capturada tiene un tipo de página, cada SERP tiene un tipo dominante
    con su reparto, y afinar la clasificación es editar un JSON.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: El Sheet aprende la fase 13 y el tab transpuesto queda medido</name>

  <read_first>
    - `seo-tools/data/sheet-columns.json` — el modelo declarativo completo, incluida la entrada de
      `Competitor Analysis` con `orientation: "columnas"`, `columns: []` y su nota.
    - `seo-tools/src/sheets/schema.ts` líneas 28-60 — `COLUMN_STATUSES` (ya incluye `fase-13`) y
      `REQUIRED_STATUSES`.
    - `seo-tools/src/sheets/upsert.ts` líneas 303-345 — el filtro de columnas propias y cómo se
      resuelve el valor de cada celda (`literal`, `source`, `readFieldPath`).
    - `seo-tools/src/sheets/client.ts` — `getSheetsSession`, `quoteTab`.
    - `.planning/workstreams/seo-keywords/data/sheet-recon-2026-08-10.md` secciones 4, 6 y 7.
    - `.planning/workstreams/seo-keywords/data/sheet-headers.json` — el volcado del 2026-08-10:
      `Competitor Analysis` figura con `headerRow: null`, `headers: []` y grilla `36 x 21`.
  </read_first>

  <files>
    seo-tools/src/sheets/schema.ts,
    seo-tools/src/sheets/upsert.ts,
    seo-tools/src/sheets/upsert.test.ts,
    seo-tools/data/sheet-columns.json,
    seo-tools/src/phase13/tab-recon.ts,
    seo-tools/docs/sheet-model.md,
    .planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md
  </files>

  <action>
    **Parte A — habilitar las columnas de la fase 13.** En `data/sheet-columns.json`, dentro de
    `Keyword Research`, dar campo a dos columnas que hoy están declaradas y vacías:

    | Encabezado real | Columna | `field` nuevo | `status` |
    |---|---|---|---|
    | `Cluster` | B | `cluster` | `fase-13` |
    | `Top Result` | M | `topResult` | `fase-13` |

    `Top Result` estaba marcada `fase-15` por el modelo de la fase 12. Se reasigna a la 13 con
    fundamento: es el resultado que Google pone primero, o sea dato de SERP, y la SERP la captura
    esta fase. Dejar esa razón en el campo `note` de la columna. **`Suggested H1` sigue siendo de la
    fase 15 y no se toca**, igual que `URL`, que es de la 14.

    En `upsert.ts`, agregar `"fase-13"` al filtro de columnas propias (línea 309). En `schema.ts`,
    agregar `"fase-13"` a `REQUIRED_STATUSES`, para que si alguien renombra `Cluster` o
    `Top Result` en el documento del cliente la carga falle ruidosa en vez de escribir en el vacío.

    **Parte B — medir el tab transpuesto antes de escribir una línea de escritor.** Es el mismo
    patrón que salvó a la fase 12: sondear y grabar la respuesta real antes de parsear. Crear
    `seo-tools/src/phase13/tab-recon.ts`, un punto de entrada **de solo lectura** que baje el
    rectángulo `A1:U36` de `Competitor Analysis` y vuelque:

    - la etiqueta de cada fila de la columna A, con su número de fila y su texto literal, espacios
      incluidos;
    - qué columnas están ocupadas y por qué competidor;
    - dónde vive el residuo de plantilla (`pera` / `pera.com` / DR 20 / AR 13);
    - qué filas corresponden a stats, cuáles a keywords, cuáles a snippets y cuáles a páginas.

    Guardar el volcado legible en
    `.planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md` y, con esas
    etiquetas ya medidas, completar el arreglo `columns` de `Competitor Analysis` en
    `sheet-columns.json`: para un tab transpuesto cada entrada describe una **fila de métrica** con
    su número de fila real, su etiqueta literal y el campo del registro de competidor que la llena.
    Declarar la clave de idempotencia como el dominio, tal como ya dice la nota del modelo.

    **Nada de escritura en este plan.** El escritor orientado a columnas lo construye el plan 13-03.
    Aquí solo se mide y se declara, con una sesión de solo lectura.

    Actualizar `docs/sheet-model.md` con las dos columnas reasignadas y con la forma del tab
    transpuesto.
  </action>

  <verify>
    <automated>
cd seo-tools
npm run typecheck
npm test
node -e "
const m=JSON.parse(require('fs').readFileSync('data/sheet-columns.json','utf8'));
const kr=m['Keyword Research'].columns;
const byHeader=Object.fromEntries(kr.map(c=>[c.header.trim(),c]));
if(byHeader['Cluster'].field!=='cluster') throw new Error('Cluster sin campo');
if(byHeader['Cluster'].status!=='fase-13') throw new Error('Cluster con estado equivocado');
if(byHeader['Top Result'].field!=='topResult') throw new Error('Top Result sin campo');
if(byHeader['URL'].status!=='fase-14') throw new Error('URL cambió de fase');
if(byHeader['Suggested H1'].status!=='fase-15') throw new Error('Suggested H1 cambió de fase');
const ca=m['Competitor Analysis'];
if(ca.orientation!=='columnas') throw new Error('el tab dejó de estar transpuesto');
if(ca.columns.length<8) throw new Error('el modelo del tab transpuesto quedó vacío: '+ca.columns.length);
if(ca.keyField!=='domain') throw new Error('clave de idempotencia equivocada');
console.log('modelo ok:', kr.length, 'columnas en Keyword Research,', ca.columns.length, 'filas de métrica en Competitor Analysis');
"
test -f ../.planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md
test "$(grep -c 'Domain Rating' ../.planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md)" -ge 1
    </automated>
    <human-check>
      Abrir el Sheet y confirmar que el tab `Competitor Analysis` sigue exactamente como estaba:
      esta tarea solo lee.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `Cluster` y `Top Result` quedan con campo y estado `fase-13`; `URL` sigue en `fase-14` y
      `Suggested H1` en `fase-15`.
    - Hay una prueba nombrada en `upsert.test.ts` que afirma que, con el modelo real, las columnas
      propias incluyen `Cluster` y `Top Result` y **no** incluyen `URL` ni `Suggested H1`.
    - Hay una prueba que afirma que actualizar una fila existente no escribe nada en las columnas de
      las fases 14 y 15.
    - `sheet:push --tab "Keyword Research" --data data/keywords.jsonl --dry-run` contra el documento
      real sale con código 0, reporta `insertadas: 0` y `columnas a agregar: 0`.
    - El volcado del tab transpuesto existe, nombra las filas reales de métrica con su número y
      registra dónde está el residuo `pera`.
    - El modelo de `Competitor Analysis` declara al menos ocho filas de métrica con su fila real y
      su etiqueta literal, y su clave de idempotencia es el dominio.
    - Ninguna escritura contra el documento del cliente: el reconocimiento corre en solo lectura.
  </acceptance_criteria>

  <done>
    El escritor puede alcanzar `Cluster` y `Top Result` sin rozar las columnas de las fases 14 y 15,
    y la forma del tab transpuesto está medida contra el documento en vivo en vez de supuesta.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SerpApi → `seo-tools` | Respuesta de un tercero que se parsea y se persiste en disco |
| `seo-tools` → Google Sheets | Documento del cliente; toda escritura es irreversible sin historial |
| `.secrets/.env` → proceso | Credenciales que nunca deben tocar caché, log ni commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information disclosure | `sources/serpapi.ts` | high | mitigate | La función nueva no construye la URL: reusa `consultar()`, que ya excluye `api_key` de la clave y del envelope. Criterio: `grep -rn "api_key" src/phase13/` imprime 0 |
| T-13-02 | Tampering | `sheets/upsert.ts` | high | mitigate | Ampliar el filtro de columnas propias podría hacer que la carga pise `URL` y `Suggested H1`. Prueba nombrada que afirma que esas dos no entran en `propias` |
| T-13-03 | Denial of service | cuota de SerpApi | critical | mitigate | Este plan corre entero en modo caché; criterio mecánico que compara el contador del libro de cuota antes y después y exige 12 en los dos |
| T-13-04 | Tampering | `data/sheet-columns.json` | medium | mitigate | Un cambio de estado mal hecho rompe la carga de la fase 12. Criterio que revalida el modelo contra el documento real en ensayo |
| T-13-05 | Information disclosure | volcado del tab transpuesto | low | accept | El documento es del propio cliente y el residuo de plantilla (`pera`) no es dato de terceros; el volcado se commitea dentro del workstream |
| T-13-SC | Tampering | instalación de paquetes | high | accept | Esta fase **no instala ningún paquete**: `seo-tools/package.json` no se modifica. Criterio: `git diff --cached --name-only -- seo-tools/package.json seo-tools/package-lock.json` vacío en cada commit |
</threat_model>

<verification>
Al cerrar el plan, desde la raíz del repositorio:

```bash
cd seo-tools && npm run typecheck && env -u SERPAPI_API_KEY -u DINORANK_API_KEY npm test
```

1. Typecheck en 0 y la suite completa en verde sin ninguna credencial.
2. El contador de SerpApi del libro de cuota sigue en **12**: este plan no gasta cuota.
3. `test "$(git diff --cached --name-only -- src/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/workstreams/milestone/ .secrets seo-tools/src/cli.ts seo-tools/package.json | wc -l | tr -d ' ')" -eq 0`
   antes de cada commit, ejecutado desde la raíz del repositorio.
4. `test "$(node -e "console.log(require('fs').readFileSync('../tsconfig.json','utf8').includes('\"seo-tools\"'))")" = "true"` —
   el guardarraíl del build sigue en su sitio.
5. `test "$(shasum -a 256 seo-tools/data/keywords.jsonl | cut -d' ' -f1)"` idéntico al valor tomado
   al empezar el plan: la fase 13 todavía no toca el dataset de la fase 12.
</verification>

<success_criteria>
- Una SERP cacheada se lee tipada, con coste cero de cuota y sin credencial.
- Los 95 resultados orgánicos de las 12 capturas quedan clasificados por tipo de página.
- El modelo del Sheet habilita `Cluster` y `Top Result` sin exponer las columnas de las fases 14 y 15.
- El tab transpuesto está medido contra el documento en vivo y declarado en el modelo.
- Cero búsquedas de SerpApi consumidas. Cero escrituras en el Sheet.
</success_criteria>

<artifacts>
## Artefactos que produce este plan

| Archivo | Qué es | Lo consume |
|---|---|---|
| `seo-tools/src/sources/serpapi.ts` (extendido) | `serpCompleta()` con la clave de caché intacta | 13-02 |
| `seo-tools/src/phase13/args.ts` | Parseo de banderas propio de la fase | 13-02, 13-03, 13-04, 13-05 |
| `seo-tools/src/phase13/serp.ts` | Registro tipado de SERP | 13-02, 13-04 |
| `seo-tools/src/phase13/pagetype.ts` + `data/serp-page-types.json` | Tipo de página por resultado y por cluster | 13-02, 13-04 |
| `seo-tools/data/sheet-columns.json` (extendido) | `Cluster` y `Top Result` escribibles; tab transpuesto modelado | 13-03, 13-04 |
| `.planning/.../data/competitor-tab-recon-2026-08-11.md` | Forma real del tab transpuesto | 13-03 |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-01-SUMMARY.md`
al terminar, con la lista de dominios por tipo de página medida sobre las 12 capturas y con las
etiquetas reales del tab transpuesto.
</output>
