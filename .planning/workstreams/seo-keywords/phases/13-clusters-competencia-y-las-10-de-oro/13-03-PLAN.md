---
phase: 13-clusters-competencia-y-las-10-de-oro
workstream: seo-keywords
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
autonomous: true
requirements: [COMP-01, COMP-04, SHEET-03]
files_modified:
  - seo-tools/src/phase13/ahrefs.ts
  - seo-tools/src/phase13/ahrefs.test.ts
  - seo-tools/src/phase13/ahrefs-plan.ts
  - seo-tools/src/phase13/ahrefs-ingest.ts
  - seo-tools/src/phase13/competitors.ts
  - seo-tools/src/phase13/competitors.test.ts
  - seo-tools/src/phase13/comp-profile.ts
  - seo-tools/src/sheets/column-upsert.ts
  - seo-tools/src/sheets/column-upsert.test.ts
  - seo-tools/src/phase13/comp-push.ts
  - seo-tools/data/competitors.json
  - seo-tools/data/ahrefs-usage.json
  - seo-tools/data/fixtures/ahrefs-domain-overview.json
  - seo-tools/data/fixtures/ahrefs-top-pages.json
  - .planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-COMPETIDORES.md

must_haves:
  truths:
    - "Los cinco competidores de Lima tienen DR, referring domains, tráfico orgánico estimado, keywords en top 100 y presencia de blog (COMP-01)."
    - "De cada competidor están registradas sus páginas más enlazadas (COMP-04)."
    - "El tab `Competitor Analysis`, que está transpuesto, se escribe por columna y recargarlo no duplica competidores (SHEET-03)."
    - "Toda respuesta cruda de Ahrefs queda en el caché del repositorio y su consumo queda contado."
  artifacts:
    - seo-tools/data/competitors.json
    - seo-tools/src/sheets/column-upsert.ts
    - .planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-COMPETIDORES.md
  key_links:
    - "Ahrefs es la única fuente que tiene DR y referring domains: si su camino no queda instrumentado, COMP-01 no se puede cerrar con ninguna otra."
    - "El tab transpuesto es el único del alcance donde la idempotencia es por columna y no por fila; el escritor de la fase 12 se niega a tocarlo por diseño."
---

<objective>
Los cinco competidores de Lima quedan perfilados con las métricas que solo Ahrefs tiene, y el tab
`Competitor Analysis` del Sheet —que está transpuesto y por eso ningún escritor del milestone podía
tocar— pasa a escribirse por columna, de forma idempotente.

Propósito: el punto dulce de la fase se calcula contra el perfil de enlaces real del dominio del
doctor, que es de agosto de 2026 y tiene historial casi nulo. Sin saber qué DR tienen los que hoy
ocupan el top 10, "alcanzable" es una opinión.

Salida: el camino instrumentado hacia Ahrefs con su caché y su contador, el perfil de los cinco, y
el tab del cliente escrito por primera vez.

Este plan corre en paralelo al 13-02: no necesita clusters ni capturas nuevas de SerpApi, y sus
cinco dominios son una decisión ya cerrada (D-11).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-01-SUMMARY.md
@.planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md
@.planning/workstreams/seo-keywords/data/sheet-recon-2026-08-10.md
@.planning/research/COMPETITORS.md
@seo-tools/src/cache.ts
@seo-tools/src/quota.ts
@seo-tools/src/sheets/schema.ts
@seo-tools/src/sheets/upsert.ts
</context>

<constraints>
Rigen las mismas reglas duras del plan 13-01. Dos aclaraciones propias de este plan:

- **`seo-tools/src/sheets/upsert.ts` no se edita aquí.** El escritor orientado a columnas es un
  archivo nuevo, `column-upsert.ts`. El plan 13-01 ya hizo el único cambio que ese archivo
  necesitaba y este plan corre en paralelo al 13-02: dos planes editando el mismo archivo en la
  misma wave es exactamente lo que la estructura de waves existe para evitar.
- **La verificación de la fase 12 que exigía cero menciones de Ahrefs en `seo-tools/src/` queda
  derogada.** Ahrefs vuelve al milestone por decisión de Juan del 2026-08-10 (D-07). Cualquier
  criterio heredado con esa forma se ignora.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Instrumentar Ahrefs sin API key propia: sondear, grabar, cachear y contar</name>

  <precondition>
    El servidor MCP de Ahrefs tiene que estar disponible en la sesión que ejecuta este plan.
    Verificado el 2026-08-11: `.secrets/.env` contiene `DINORANK_API_KEY`,
    `GOOGLE_SERVICE_ACCOUNT_FILE`, `SEO_SHEET_ID` y `SERPAPI_API_KEY`, y **no** contiene ninguna
    credencial de Ahrefs. Si el MCP no responde y tampoco hay clave en el entorno, detener el plan
    y avisar: sin esa fuente, COMP-01 no se puede cerrar y la decisión hay que devolvérsela a Juan.
  </precondition>

  <read_first>
    - `seo-tools/src/cache.ts` — `cacheKey`, `writeEnvelope`, `readEnvelope`, la forma de
      `CacheEnvelope` y la tabla de qué se persiste y qué no.
    - `seo-tools/src/quota.ts` — `QuotaBook.open`, `record`, `total`. El libro ya cuenta `dinorank`
      y `serpapi`; se le suma una tercera fuente.
    - `seo-tools/src/commands/cache-put.ts` — el camino de relleno que la fase 12 dejó armado para
      exactamente este caso: obtener el cuerpo crudo por fuera y dejarlo en la caché.
    - `seo-tools/src/commands/dino-probe.ts` — el patrón de sondeo que salvó a la fase 12: grabar la
      respuesta real **antes** de escribir una línea de parseo.
    - La documentación del propio MCP de Ahrefs: invocar su herramienta `doc` antes del primer
      llamado, para obtener los nombres exactos de herramientas y parámetros. No asumirlos.
  </read_first>

  <files>
    seo-tools/src/phase13/ahrefs.ts,
    seo-tools/src/phase13/ahrefs.test.ts,
    seo-tools/src/phase13/ahrefs-plan.ts,
    seo-tools/src/phase13/ahrefs-ingest.ts,
    seo-tools/data/fixtures/ahrefs-domain-overview.json,
    seo-tools/data/fixtures/ahrefs-top-pages.json,
    seo-tools/data/ahrefs-usage.json
  </files>

  <behavior>
    - `ahrefs-plan.ts` imprime, por cada consulta pendiente, su etiqueta de endpoint, sus parámetros
      y su clave de caché de 64 caracteres hexadecimales. Nunca inventa la clave: la calcula con la
      misma función que usa el resto del proyecto.
    - `ahrefs-ingest.ts` toma un cuerpo crudo ya obtenido, escribe el envelope bajo la fuente
      `ahrefs` y registra la llamada en el libro de cuota antes de devolver.
    - Reingerir el mismo cuerpo dos veces produce el mismo archivo y **no** vuelve a contar cuota.
    - Un cuerpo que no es JSON válido aborta sin escribir nada.
    - Una respuesta vacía se persiste igual, con resultado declarado: para Ahrefs, vacío significa
      "la fuente no conoce este dato", que es distinto de cero. Ya está medido que devuelve vacío
      para el geo long tail de Lima.
    - El parser lee por nombre de campo tomado de la fixture real, nunca por posición.
  </behavior>

  <action>
    Ahrefs no tiene credencial en el entorno y sí tiene servidor MCP. El camino, entonces, es el que
    la fase 12 ya dejó preparado: obtener el cuerpo por fuera y dejarlo en la caché, de modo que
    reprocesar el análisis no vuelva a consumir unidades (INFRA-03).

    **Paso 1, sondear.** Antes de escribir parseo, invocar la herramienta `doc` del MCP para obtener
    los nombres exactos de las herramientas y sus parámetros. Después, hacer **una** consulta de
    perfil de dominio sobre `drcarranzacolumna.com` y **una** de páginas principales sobre el mismo
    dominio. Grabar las dos respuestas crudas como fixtures commiteadas:
    `data/fixtures/ahrefs-domain-overview.json` y `data/fixtures/ahrefs-top-pages.json`. Los datos
    de un competidor público no necesitan seudonimizarse; sí hay que confirmar que la respuesta no
    trae ninguna credencial ni identificador de cuenta antes de commitear.

    **Paso 2, escribir el contrato en el README del paquete** con los nombres de campo tal como
    llegan, igual que hizo el plan 12-05 con los cuatro endpoints de DinoRank. Ese documento es lo
    único que va a existir cuando alguien vuelva dentro de tres meses.

    **Paso 3, el módulo.** `seo-tools/src/phase13/ahrefs.ts` declara las etiquetas de endpoint
    estables que este proyecto usa —una para el perfil de dominio, otra para páginas principales,
    otra para métricas de keyword, que la usa el plan 13-04— y expone:
    - `claveDeConsulta(endpoint, params)`, que envuelve a `cacheKey` con la fuente `ahrefs`;
    - `leerConsulta(endpoint, params)`, que lee el envelope de disco y devuelve `null` si no está;
    - los parsers de cada respuesta, escritos contra la fixture.

    **Paso 4, los dos puntos de entrada.** `ahrefs-plan.ts` recibe la lista de dominios o keywords y
    emite el plan de consultas con sus claves. `ahrefs-ingest.ts` recibe `--endpoint`, `--params`
    con el JSON de parámetros y `--file` con el cuerpo crudo, escribe el envelope con
    `writeEnvelope` y llama a `QuotaBook.record("ahrefs", etiqueta)`. Un solo comando: computa la
    clave, persiste y cuenta. Que la clave la calcule el proyecto y no la persona es lo que evita
    que un dato quede guardado bajo una clave que después nadie encuentra.

    **Paso 5, el contador de unidades.** El archivo lleva una clave `corridas`, que es un **arreglo** al que cada plan le agrega una entrada en vez de sobrescribir. Es contrato con el plan `13-04`, cuyo verify exige dos entradas acumuladas: si este plan escribe un objeto plano o pisa lo anterior, ese verify falla y el fallo aparece en el plan equivocado. Escribir `data/ahrefs-usage.json` con la fecha, la cantidad
    de consultas por endpoint y la estimación de unidades. La cuenta es Lite con unas 66.000 libres
    y este plan gasta cinco consultas de dominio más cinco de páginas; el plan 13-04 gasta el grueso
    (D-07). Sin este archivo, nadie sabe cuánto queda.
  </action>

  <verify>
    <automated>
cd seo-tools
npm run typecheck
env -u DINORANK_API_KEY -u SERPAPI_API_KEY npm test
test -f data/fixtures/ahrefs-domain-overview.json
test -f data/fixtures/ahrefs-top-pages.json
node -e "
const fs=require('fs');
for (const f of ['data/fixtures/ahrefs-domain-overview.json','data/fixtures/ahrefs-top-pages.json']) {
  const raw = fs.readFileSync(f,'utf8');
  JSON.parse(raw);
  if (/api[_-]?key|authorization|bearer |account_id/i.test(raw)) throw new Error('la fixture '+f+' trae algo que parece credencial');
}
const q=JSON.parse(fs.readFileSync('.cache/_quota.json','utf8'));
if(!q.sources.ahrefs) throw new Error('el libro de cuota no registró ninguna consulta de Ahrefs');
console.log('consultas de Ahrefs contadas:', q.sources.ahrefs.calls);
const u=JSON.parse(fs.readFileSync('data/ahrefs-usage.json','utf8'));
if(typeof u.unidadesEstimadas!=='number') throw new Error('falta la estimación de unidades');
console.log('unidades estimadas:', u.unidadesEstimadas);
"
./node_modules/.bin/tsx src/phase13/ahrefs-plan.ts --domains drcarranzacolumna.com | grep -qE '[0-9a-f]{64}'
test "$(node -e "const u=require('./data/ahrefs-usage.json'); if(!Array.isArray(u.corridas)) throw new Error('falta el arreglo corridas'); console.log(u.corridas.length)")" -ge 1
    </automated>
  </verify>

  <acceptance_criteria>
    - Las dos fixtures existen, son JSON válido y no contienen nada con forma de credencial ni de
      identificador de cuenta.
    - El libro de cuota tiene una tercera fuente contada, con al menos dos consultas registradas.
    - `ahrefs-plan.ts` imprime claves de caché de 64 caracteres hexadecimales.
    - Hay una prueba nombrada por cada comportamiento del bloque `<behavior>`, corriendo contra las
      fixtures y **sin ninguna credencial en el entorno**.
    - Hay una prueba nombrada que afirma que reingerir el mismo cuerpo no incrementa el contador.
    - `data/ahrefs-usage.json` declara consultas por endpoint y unidades estimadas.
    - El README del paquete documenta los nombres de campo tal como llegan de la fuente.
  </acceptance_criteria>

  <done>
    Ahrefs es una fuente más del proyecto: pasa por el caché, cuenta su consumo y su contrato está
    escrito contra respuestas reales, no contra suposiciones.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: El perfil de los cinco competidores de Lima</name>

  <read_first>
    - `.planning/research/COMPETITORS.md` — los tres investigados en v1.0, con sus secciones, sus
      CTAs y el dato de que solo uno de los tres tiene blog.
    - `.planning/workstreams/seo-keywords/data/serpapi-validacion-2026-08-10.md` — de dónde salieron
      los dos que faltaban y por qué.
    - `seo-tools/src/phase13/ahrefs.ts` — los parsers de la tarea 1.
    - `13-CONTEXT.md`, sección "Competencia" — los cinco dominios cerrados y qué se pide de cada uno.
  </read_first>

  <files>
    seo-tools/src/phase13/competitors.ts,
    seo-tools/src/phase13/competitors.test.ts,
    seo-tools/src/phase13/comp-profile.ts,
    seo-tools/data/competitors.json,
    .planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-COMPETIDORES.md
  </files>

  <behavior>
    - Los cinco dominios están fijos en un archivo de datos y el módulo no acepta otros: la lista es
      una decisión cerrada, no un parámetro de la corrida.
    - Una métrica que la fuente no devuelve queda declarada como no conocida, nunca como cero. Es la
      misma regla que la fase 12 aplicó al volumen y que existe porque un cero afirma algo que nadie
      midió.
    - La presencia de blog se deduce de las páginas principales: si alguna URL del top por enlaces
      cuelga de una ruta de blog o de artículos, el competidor tiene blog. Si no hay evidencia, la
      respuesta es que no se sabe, no que no tiene.
    - Las páginas más enlazadas salen ordenadas de mayor a menor y con su cantidad de dominios de
      referencia.
    - Dos ejecuciones sobre la misma caché producen el mismo archivo byte a byte y **cero** consultas
      nuevas.
  </behavior>

  <action>
    Fijar los cinco competidores (D-11), tres heredados de la investigación de v1.0 y dos que
    aparecieron solos en la validación de SerpApi del 2026-08-10:

    | Dominio | Quién es | De dónde salió |
    |---|---|---|
    | `drcarranzacolumna.com` | Dr. Paul Carranza, neurocirujano, cirugía endoscópica | v1.0 |
    | `drciezatraumatologia.com` | Dr. Ramiro Cieza, traumatología de columna | v1.0 |
    | `cirujanocolumna-elaos.com` | Dr. Eduardo Laos, neurocirujano, con blog | v1.0 |
    | `doctormunguia.com` | Dr. Gunter Munguía, neurocirujano endoscopista, Surco. Pack local posición 2, 4.8 con 24 reseñas | SerpApi, 2026-08-10 |
    | `clinicarthromeds.pe` | Orgánico posición 5, con la URL construida sobre la keyword geo `/traumatologo-especialista-en-columna-lima-peru/` | SerpApi, 2026-08-10 |

    Emitir el plan de consultas con `ahrefs-plan.ts`, obtener las respuestas por el MCP e ingerirlas
    una por una con `ahrefs-ingest.ts`. Diez consultas en total: perfil de dominio y páginas
    principales por cada uno de los cinco (D-12).

    `seo-tools/src/phase13/competitors.ts` arma el perfil leyendo **solo de la caché**, así que
    reconstruirlo cuesta cero unidades. Por competidor:

    - **COMP-01:** DR, referring domains, tráfico orgánico estimado, keywords en top 100 y presencia
      de blog.
    - **COMP-04, primera mitad:** las páginas más enlazadas, con su URL, su título si viene y su
      cantidad de dominios de referencia. La otra mitad de COMP-04, los featured snippets, se mide
      sobre las SERP capturadas y la cierra el plan 13-04: pedirle eso a Ahrefs sería caro y este
      proyecto ya va a tener el dato gratis.

    Agregar como sexta columna el propio `drangulocolumna.com`. Sin la línea de base, DR 5 contra DR
    30 no dice nada; con ella, es exactamente la distancia que el punto dulce tiene que respetar. Si
    Ahrefs no conoce el dominio —comprado el 2026-08-09— eso también es un dato y se registra como
    tal.

    Escribir `data/competitors.json` con un registro por dominio y con el bloque de procedencia por
    métrica, que por decisión **J-3** vive en el repositorio y no en el Sheet.

    Escribir `13-COMPETIDORES.md` como entregable legible: una tabla comparativa con los seis
    dominios, y debajo, por competidor, sus páginas más enlazadas. Incluir la referencia de reseñas
    del pack local que ya está medida, marcada como insumo para GBP-03 del otro workstream: Clinica
    De La Columna 3.5 con 34, Dr. Gunter Munguía 4.8 con 24, Centro de Columna Vertebral 5.0 con 13,
    el doctor 5.0 con 6.
  </action>

  <verify>
    <automated>
cd seo-tools
npm run typecheck
env -u DINORANK_API_KEY -u SERPAPI_API_KEY npm test
ANTES=$(node -e "const q=JSON.parse(require('fs').readFileSync('.cache/_quota.json','utf8'));console.log(q.sources.ahrefs.calls)")
./node_modules/.bin/tsx src/phase13/comp-profile.ts --out /tmp/comp1.json
./node_modules/.bin/tsx src/phase13/comp-profile.ts --out /tmp/comp2.json
DESPUES=$(node -e "const q=JSON.parse(require('fs').readFileSync('.cache/_quota.json','utf8'));console.log(q.sources.ahrefs.calls)")
test "$ANTES" -eq "$DESPUES"
test "$(shasum -a 256 /tmp/comp1.json | cut -d' ' -f1)" = "$(shasum -a 256 /tmp/comp2.json | cut -d' ' -f1)"
node -e "
const c=JSON.parse(require('fs').readFileSync('data/competitors.json','utf8'));
const esperados=['drcarranzacolumna.com','drciezatraumatologia.com','cirujanocolumna-elaos.com','doctormunguia.com','clinicarthromeds.pe'];
for(const d of esperados){ if(!c.competidores.some(x=>x.domain===d)) throw new Error('falta '+d); }
if(!c.competidores.some(x=>x.domain==='drangulocolumna.com')) throw new Error('falta la línea de base del propio dominio');
for(const x of c.competidores){
  for(const campo of ['domainRating','referringDomains','organicTraffic','organicKeywords','blog']){
    if(!(campo in x)) throw new Error(x.domain+' no declara '+campo);
    if(x[campo]===0 && x[campo+'Fuente']!=='ahrefs') throw new Error(x.domain+': '+campo+' en cero sin fuente que lo respalde');
  }
  if(!Array.isArray(x.paginasMasEnlazadas)) throw new Error(x.domain+' sin páginas más enlazadas');
}
console.log('perfiles:', c.competidores.length);
"
test -f ../.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-COMPETIDORES.md
    </automated>
  </verify>

  <acceptance_criteria>
    - Los cinco dominios de D-11 más `drangulocolumna.com` están en `data/competitors.json`.
    - Cada perfil declara DR, referring domains, tráfico orgánico, keywords en top 100 y presencia de
      blog, cada uno con su campo de procedencia.
    - Ninguna métrica queda en cero sin que la fuente lo haya devuelto: lo desconocido se declara
      desconocido.
    - Cada competidor trae su lista de páginas más enlazadas ordenada de mayor a menor.
    - Reconstruir el perfil dos veces no incrementa el contador de Ahrefs ni cambia el archivo.
    - `13-COMPETIDORES.md` existe, compara los seis dominios y lista las páginas más enlazadas por
      competidor.
    - Hay una prueba nombrada por cada comportamiento, incluida una que afirma que una métrica
      ausente no se convierte en cero.
  </acceptance_criteria>

  <done>
    Los cinco competidores están perfilados con las métricas que solo Ahrefs tiene, y hay una línea
    de base contra la cual medir qué es alcanzable para este dominio.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Escribir un tab transpuesto sin duplicar competidores</name>

  <read_first>
    - `.planning/workstreams/seo-keywords/data/competitor-tab-recon-2026-08-11.md` — las etiquetas
      reales medidas por el plan 13-01, con su número de fila.
    - `seo-tools/data/sheet-columns.json`, entrada `Competitor Analysis` — el modelo declarado por
      el plan 13-01: sin fila de encabezados, orientación por columnas, clave por dominio.
    - `seo-tools/src/sheets/upsert.ts` — el escritor orientado a filas: qué hace bien (tramos
      contiguos, borrado guardado, ensayo por defecto) y por qué `assertRowOriented` se niega a
      tocar este tab.
    - `seo-tools/src/sheets/client.ts` — `getSheetsSession`, `quoteTab`, `columnLetter`.
    - `.planning/workstreams/seo-keywords/data/sheet-recon-2026-08-10.md`, sección 6 y 7 — la forma
      del tab y dónde vive el residuo `pera`.
  </read_first>

  <files>
    seo-tools/src/sheets/column-upsert.ts,
    seo-tools/src/sheets/column-upsert.test.ts,
    seo-tools/src/phase13/comp-push.ts
  </files>

  <behavior>
    - La clave de idempotencia es el dominio leído de la fila de sitio web: cargar dos veces los
      mismos cinco competidores no agrega ni una columna.
    - Un competidor que ya está en el documento se actualiza en su columna; uno nuevo ocupa la
      siguiente ranura libre.
    - El escritor toca **solo las filas que declara suyas**. Cualquier fila del tab que el modelo no
      declare queda intacta, incluso si está vacía.
    - La columna del competidor de ejemplo de la plantilla se reutiliza como primera ranura: se
      sobrescribe con datos reales, no se borra por separado.
    - Ninguna celda se escribe de forma que el documento la evalúe como fórmula.
    - En modo ensayo no se emite ninguna petición de escritura, y el ensayo es el modo por defecto.
    - Si el documento cambió de forma respecto de lo que declara el modelo —una etiqueta movida de
      fila— el escritor aborta con el diff y no escribe nada.
  </behavior>

  <action>
    Construir `seo-tools/src/sheets/column-upsert.ts`, el segundo modo de escritura que el
    reconocimiento del 2026-08-10 anticipó y que la fase 12 dejó declarado pero sin implementar. Es
    un archivo nuevo: `upsert.ts` no se toca.

    El algoritmo es el espejo del de filas. Leer el rectángulo del tab. Localizar la fila de sitio
    web que declara el modelo y leerla entera: eso da el mapa de dominio a columna. Para cada
    competidor, resolver su columna —la que ya ocupa o la siguiente ranura libre— y escribir hacia
    abajo solo las filas declaradas, agrupadas en tramos contiguos igual que hace el escritor de
    filas, para no pisar filas de otras secciones.

    Antes de escribir, validar la forma: cada etiqueta declarada tiene que estar en la fila que dice
    el modelo, comparando con recorte en los dos extremos. Si alguna no calza, abortar con el diff
    de las cuatro listas —esperadas, reales, faltantes, sobrantes— que es el mismo mensaje que ya
    produce el escritor de filas y que es lo que hace depurable un fallo contra un documento vivo.

    El punto de entrada `seo-tools/src/phase13/comp-push.ts` acepta `--data`, `--dry-run` y `--yes`.
    Ensayo por defecto, como toda operación que modifica el documento del cliente.

    Correr primero el ensayo y copiar su salida al SUMMARY **antes** de ejecutar la carga real. El
    residuo de plantilla (`pera` / `pera.com` / DR 20 / AR 13) tiene que aparecer en ese listado y
    quedar sobrescrito por el primer competidor real; Juan ya autorizó eliminarlo el 2026-08-10.

    Después de la carga real, correrla otra vez sin cambiar nada y verificar que reporta cero
    columnas nuevas. Eso es SHEET-06 aplicado al segundo modo de escritura, y hay que medirlo contra
    el documento y no contra un cálculo.
  </action>

  <verify>
    <automated>
cd seo-tools
npm run typecheck
env -u DINORANK_API_KEY -u SERPAPI_API_KEY npm test
./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase13/comp-push.ts --data data/competitors.json --dry-run
./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase13/comp-push.ts --data data/competitors.json --yes
./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase13/comp-push.ts --data data/competitors.json --yes > /tmp/segunda-carga.txt
grep -qE 'columnas (nuevas|insertadas): 0' /tmp/segunda-carga.txt
node -e "
const m=JSON.parse(require('fs').readFileSync('data/sheet-columns.json','utf8'));
const ca=m['Competitor Analysis'];
if(ca.orientation!=='columnas') throw new Error('el modelo dejó de declarar el tab como transpuesto');
console.log('filas de métrica declaradas:', ca.columns.length);
"
    </automated>
    <human-check>
      Abrir el tab `Competitor Analysis` del Sheet: los cinco competidores aparecen en columnas, con
      sus métricas alineadas a las etiquetas de la columna A, y `pera` ya no está.
    </human-check>
  </verify>

  <acceptance_criteria>
    - La suite pasa sin ninguna credencial en el entorno; el escritor se prueba contra una doble en
      memoria, igual que hizo el plan 12-02.
    - Hay una prueba nombrada por cada comportamiento del bloque `<behavior>`, incluidas la de
      idempotencia por dominio y la del aborto por deriva de forma.
    - Hay una prueba nombrada que afirma que una fila del tab que el modelo no declara queda intacta
      después de una actualización.
    - El ensayo previo queda copiado en el SUMMARY antes de la carga real, con el residuo `pera`
      visible en él.
    - La segunda carga real reporta **cero columnas nuevas**.
    - `test "$(git diff --cached --name-only -- seo-tools/src/sheets/upsert.ts | wc -l | tr -d ' ')" -eq 0`
      en cada commit de este plan: el escritor de filas no se toca.
  </acceptance_criteria>

  <done>
    El tab transpuesto del cliente tiene a los cinco competidores con sus métricas, y recargarlo no
    duplica ninguno.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP de Ahrefs → sesión | Respuesta de un tercero que entra al repositorio como fixture commiteada |
| `seo-tools` → Google Sheets | Documento del cliente; primera escritura sobre un tab que nunca se tocó |
| Libro de cuota | Tercera fuente contada; el consumo de unidades de Ahrefs no lo reporta ningún endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-20 | Information disclosure | fixtures de Ahrefs | high | mitigate | Antes de commitear, criterio que busca en el texto crudo cualquier cosa con forma de credencial o de identificador de cuenta y falla si aparece. Es el mismo control que detectó la fuga en el plan 12-05 |
| T-13-21 | Tampering | `Competitor Analysis` | high | mitigate | Ensayo por defecto, validación de forma con diff de cuatro listas antes de escribir, y escritura acotada a las filas declaradas. Prueba dedicada de que una fila no declarada queda intacta |
| T-13-22 | Repudiation | consumo de Ahrefs | medium | mitigate | El proveedor no expone saldo, así que el control es el libro de cuota más `data/ahrefs-usage.json` con la estimación de unidades |
| T-13-23 | Tampering | `sheets/upsert.ts` | medium | mitigate | Este plan no lo edita; criterio de área de preparación vacía sobre ese archivo en cada commit. Corre en paralelo al plan 13-02 y un choque de archivos ahí rompería los dos |
| T-13-24 | Spoofing | métricas de competencia | low | accept | Se confía en lo que devuelve la fuente; la discrepancia entre fuentes ya está registrada como decisión de publicar las dos (D-08) |
| T-13-SC | Tampering | instalación de paquetes | high | accept | Esta fase no instala paquetes. Criterio: `git diff --cached --name-only -- seo-tools/package.json seo-tools/package-lock.json` vacío |
</threat_model>

<verification>
1. `cd seo-tools && npm run typecheck && env -u DINORANK_API_KEY -u SERPAPI_API_KEY -u SEO_SHEET_ID npm test` en verde.
2. `.cache/_quota.json` cuenta las tres fuentes, y el de SerpApi **no se movió**: este plan no gasta
   búsquedas.
3. `data/keywords.jsonl` conserva su SHA-256.
4. `test "$(git diff --cached --name-only -- src/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/workstreams/milestone/ .secrets seo-tools/src/cli.ts seo-tools/src/sheets/upsert.ts | wc -l | tr -d ' ')" -eq 0`
   antes de cada commit, desde la raíz del repositorio.
5. Segunda carga del tab transpuesto con cero columnas nuevas, medida contra el documento real.
</verification>

<success_criteria>
- Ahrefs es una fuente instrumentada: caché, contador, fixtures y contrato escrito.
- Los cinco competidores y la línea de base propia están perfilados con métricas trazadas a su fuente.
- Las páginas más enlazadas de cada competidor están registradas.
- El tab transpuesto del cliente está escrito y recargarlo no duplica nada.
</success_criteria>

<artifacts>
## Artefactos que produce este plan

| Archivo | Qué es | Lo consume |
|---|---|---|
| `seo-tools/src/phase13/ahrefs.ts` + los dos puntos de entrada | El camino instrumentado hacia Ahrefs | 13-04 (KD y traffic potential) |
| `seo-tools/data/fixtures/ahrefs-*.json` | El contrato real de la fuente | 13-04, fases futuras |
| `seo-tools/data/competitors.json` | Perfil de los cinco más la línea de base | 13-04 (gap), 13-05 (10 de Oro) |
| `seo-tools/src/sheets/column-upsert.ts` | Escritura por columna del tab transpuesto | 13-04 |
| `seo-tools/data/ahrefs-usage.json` | Consumo de unidades | 13-04 |
| `13-COMPETIDORES.md` | El entregable legible de COMP-01 y COMP-04 | Juan, fase 14 |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-03-SUMMARY.md`
al terminar, con la tabla comparativa real de los seis dominios, el ensayo del tab transpuesto
copiado antes de la carga y el consumo de unidades medido.
</output>
