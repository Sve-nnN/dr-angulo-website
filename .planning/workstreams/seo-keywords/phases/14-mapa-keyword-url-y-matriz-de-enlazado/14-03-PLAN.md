---
phase: 14-mapa-keyword-url-y-matriz-de-enlazado
workstream: seo-keywords
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - seo-tools/src/phase14/audit.ts
  - seo-tools/src/phase14/audit.test.ts
  - seo-tools/src/phase14/cannibal.ts
  - seo-tools/src/phase14/cannibal.test.ts
  - seo-tools/src/phase14/cannibal-run.ts
  - seo-tools/data/url-map.jsonl
  - seo-tools/data/cannibalization.json
  - .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md
  - .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CANIBALIZACION.md
autonomous: true
requirements: [MAP-01, MAP-02, MAP-04, SHEET-02]
must_haves:
  truths:
    - "Todas las URLs mapeables del inventario tienen una keyword primaria y de tres a cinco secundarias, no solo las nueve del handoff."
    - "Ninguna keyword primaria está asignada a dos URLs en todo el mapa."
    - "Cada URL viva declara si el contenido publicado sirve su keyword, si hay que reescribirlo o si hay que crear una URL nueva."
    - "La canibalización quedó detectada cruzando el mapa contra sí mismo y resuelta al asignar, con el desempate escrito."
    - "La revisión con datos reales de Search Console queda registrada como deuda con fecha, no como olvido."
  artifacts:
    - seo-tools/data/cannibalization.json
    - .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CANIBALIZACION.md
    - .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md
  key_links:
    - "El detector de canibalización usa `overlap.ts` par a par, nunca la pertenencia a cluster."
    - "El desempate consulta `data/golden-10.json` y deja constancia de la keyword de oro que ganó."
    - "El mapa completo se publica con el mismo `cm-push.ts` idempotente del plan 14-01."
---

<objective>
Completar el mapa sobre todas las URLs vivas —no solo las cuatro de servicio (D-02)— y cerrar
MAP-02 cruzando el mapa propuesto contra sí mismo (D-03), que es lo único medible hoy porque el
sitio es de agosto de 2026 y todavía no acumuló impresiones.

Propósito: la home, el hub `/servicios` y los cuatro posts del blog compiten entre sí desde el
día uno sin que nadie lo haya decidido. Este plan lo decide, y de paso audita qué publicó v1.1 el
2026-08-10 y qué de eso hay que reescribir (D-01).

Salida: `url-map.jsonl` completo, `cannibalization.json`, el mapa legible terminado y el
documento de canibalización con la deuda fechada.

Decisiones que gobiernan este plan: D-01, D-02, D-03, D-04, D-05, D-06, D-09, D-10, D-11, D-13.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-02-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md
@.planning/workstreams/seo-keywords/data/lectura-competencia-2026-08-11.md
@seo-tools/src/phase14/assign.ts
@seo-tools/src/phase14/overlap.ts
@seo-tools/data/url-inventory.json
@seo-tools/data/url-map.jsonl

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 14-01.
Presupuesto de SerpApi de este plan: cero (D-07).

**DinoRank no participa de MAP-02.** `/canibalizaciones` devuelve `has_data: false` y cero
keywords porque el sitio no tiene historial de impresiones. Ningún paso de este plan puede
depender de ese endpoint ni de `/auditoria`. Consultarlo para confirmar el estado es gratis y
está bien; construir sobre su respuesta, no.

**Lo que este plan lee de `src/` y lo que hace con eso.** Para decidir `dejar`, `reescribir` o
`crear` hay que abrir el contenido publicado: `src/content/service-pages.ts`,
`location-pages.ts`, `blog.ts`, `services.ts`, `locations.ts`, `faq.ts`, `cv.ts` y las páginas
estáticas de `src/app/`. Se leen. No se modifican. Ni un carácter.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auditar lo publicado y asignar el resto de las URLs vivas</name>
  <files>seo-tools/src/phase14/audit.ts, seo-tools/src/phase14/audit.test.ts, seo-tools/data/url-map.jsonl</files>
  <read_first>
    - `seo-tools/data/url-inventory.json`, en particular el bloque `reconciliacion` y la
      respuesta que Juan dio sobre él en el plan 14-02.
    - `seo-tools/src/phase14/assign.ts` y `model.ts`: se reutiliza el asignador, no se escribe
      uno nuevo.
    - `src/content/service-pages.ts`, `src/content/blog.ts`, `src/content/location-pages.ts`:
      los títulos, los encabezados y los primeros párrafos de cada entrada, que es lo que
      determina qué keyword sirve hoy la página.
    - `src/app/page.tsx`, `src/app/servicios/page.tsx`, `src/app/sedes/page.tsx`,
      `src/app/sobre-el-doctor/page.tsx`, `src/app/preguntas-frecuentes/page.tsx`,
      `src/app/testimonios/page.tsx`, `src/app/contacto/page.tsx`, `src/app/agendar/page.tsx`.
  </read_first>
  <behavior>
    - Una URL cuyo contenido publicado ya sirve la keyword asignada recibe `accion: dejar`.
    - Una URL que existe pero cuyo contenido apunta a otro tema recibe `accion: reescribir` con
      el motivo escrito.
    - Una keyword de oro sin URL que la pueda ganar produce una entrada nueva con
      `accion: crear` y `estado: planificada`, en vez de meterla a la fuerza en una URL que la
      SERP no premia.
    - `/privacidad` no aparece en el mapa (D-11).
    - Una URL de conversión —`/agendar`, `/contacto`— recibe primaria transaccional, nunca una
      keyword de captación.
    - El post del blog sobre pastillas para la ciática se mapea como captación y su acción nunca
      lo convierte en página de servicio (D-09).
    - Una URL sin candidata alcanzable falla explícitamente en vez de recibir una primaria débil
      sin justificar.
  </behavior>
  <action>
    Implementar `audit.ts`: por cada URL mapeable del inventario que todavía no esté en
    `url-map.jsonl`, deducir de qué trata el contenido publicado —título, encabezados y las
    primeras oraciones del cuerpo— y producir un `temaPublicado` con su evidencia citada, que es
    el texto literal del que se dedujo y el archivo de donde salió.

    Después correr el asignador de la fase sobre ese conjunto y resolver `accion` comparando la
    keyword asignada contra `temaPublicado` y `tipoExigidoPorSerp`. `reescribir` sin motivo
    escrito no se emite.

    Los cuatro posts del blog y las páginas institucionales entran al mapa igual que las demás:
    el objetivo de D-02 es precisamente saber si están peleando entre sí. Una página de conversión
    puede quedar marcada como `esPaginaSeo: false` y aun así llevar primaria: no ser objetivo de
    posicionamiento no la exime de declarar por qué keyword compite, porque es exactamente así
    como se canibaliza sin querer.

    Fundir el resultado dentro de `data/url-map.jsonl` sin tocar las líneas que el plan 14-02 ya
    dejó aprobadas por Juan. Si la auditoría propusiera cambiar alguna de esas nueve, no se
    cambia: se anota como observación en el SUMMARY y se decide en la verificación de la fase.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=audit && npm run typecheck && ./node_modules/.bin/tsx src/phase14/assign-run.ts --scope resto --out data/url-map.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const inv=require('./data/url-inventory.json'); const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); const nec=inv.urls.filter(u=>u.mapeable).map(u=>u.url); const tengo=new Set(m.map(r=>r.url)); process.stdout.write(String(nec.filter(u=>!tengo.has(u)).length))")" -eq 0`
      — ninguna URL mapeable del inventario quedó sin fila.
    - `test "$(cd seo-tools && node -e "const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); process.stdout.write(String(m.filter(r=>r.url==='/privacidad').length))")" -eq 0`
    - `test "$(cd seo-tools && node -e "const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); const ok=m.every(r=>r.keywordPrimaria&&r.secundarias.length>=3&&r.secundarias.length<=5&&['dejar','reescribir','crear'].includes(r.accion)&&r.tipoExigidoPorSerp&&r.justificacion); process.stdout.write(String(ok?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); const r=m.filter(x=>x.accion==='reescribir'); process.stdout.write(String(r.filter(x=>!x.motivoDeAccion||x.motivoDeAccion.length<30).length))")" -eq 0`
    - Las siete conductas declaradas tienen prueba propia y la suite completa queda en verde.
    - Las nueve filas aprobadas en el plan 14-02 conservan su `keywordPrimariaKey`, comprobado
      contra el SUMMARY de ese plan.
  </acceptance_criteria>
  <done>
    Todas las URLs mapeables tienen asignación y acción, y cada acción de reescritura dice por
    qué, citando el contenido publicado del que se dedujo.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canibalización por cruce del mapa contra sí mismo, y la deuda fechada</name>
  <files>seo-tools/src/phase14/cannibal.ts, seo-tools/src/phase14/cannibal.test.ts, seo-tools/src/phase14/cannibal-run.ts, seo-tools/data/cannibalization.json, .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CANIBALIZACION.md</files>
  <read_first>
    - `seo-tools/src/phase14/overlap.ts`: el veredicto par a par y su umbral.
    - `seo-tools/data/golden-10.json`: el criterio de desempate.
    - `14-CONTEXT.md`, sección `MAP-02, canibalización: cruce ahora, revisión después`.
    - `13-CLUSTERS.md`, la explicación de la transitividad del cluster de 41 cabezas.
  </read_first>
  <behavior>
    - Dos URLs con la misma `keywordPrimariaKey` producen un conflicto de severidad alta.
    - Dos URLs distintas cuyas primarias comparten URLs de SERP por encima del umbral producen un
      conflicto de severidad alta aunque las keywords se escriban distinto.
    - Dos URLs del mismo cluster cuyas primarias comparten cero URLs par a par NO producen
      conflicto, y el reporte deja escrito que el cluster se formó por transitividad (D-05).
    - Una secundaria de una URL que es primaria de otra produce un conflicto de severidad media.
    - Una keyword de distrito que aparece como primaria de dos URLs distintas produce conflicto
      alto (D-04).
    - Cuando hay conflicto, gana la URL que sirve a una keyword de oro y el reporte nombra cuál
      (D-06).
    - Un mapa sin conflictos produce un reporte con lista vacía y el conteo en cero, nunca un
      archivo ausente.
  </behavior>
  <action>
    Implementar `cannibal.ts` como función pura sobre `url-map.jsonl`. Emite
    `data/cannibalization.json` con: `umbralDeSolape` usado, `conflictos` —cada uno con las dos
    URLs, el tipo de choque, la severidad, las URLs de SERP compartidas como evidencia y la
    resolución aplicada—, `resueltos`, `sinResolver` y un `resumen` con los conteos.

    `cannibal-run.ts` corre el detector y, cuando encuentra conflictos resolubles, aplica la
    resolución sobre `url-map.jsonl`: la perdedora cede la primaria y recibe la siguiente
    candidata alcanzable de su propio cluster, con el cambio registrado. El detector se vuelve a
    correr después de aplicar, y la corrida final tiene que dar cero conflictos de severidad
    alta. Si algo queda sin resolver, se nombra en el reporte y en el SUMMARY en vez de bajarle
    la severidad.

    Escribir `14-CANIBALIZACION.md` con tres secciones:

    1. **Qué se midió y por qué así.** El cruce del mapa contra sí mismo es lo único medible hoy
       (D-03): `/canibalizaciones` de DinoRank devuelve `has_data: false` y cero keywords porque
       el sitio no acumuló impresiones, aunque Search Console ya esté vinculado desde el
       2026-08-09. La canibalización que importa en un sitio de agosto de 2026 es la que estamos
       por crear, no la heredada.
    2. **Los conflictos y su resolución**, uno por uno, con la evidencia de solape.
    3. **Deuda fechada.** La revisión con datos reales de Search Console, con la condición que la
       habilita —que `/canibalizaciones` deje de venir vacío— y la fecha de revisión propuesta.
       Registrarla además en el `deferred-items.md` del workstream si existe, o crearlo si no.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=cannibal && npm run typecheck && ./node_modules/.bin/tsx src/phase14/cannibal-run.ts --map data/url-map.jsonl --out data/cannibalization.json --apply && ./node_modules/.bin/tsx src/phase14/cannibal-run.ts --map data/url-map.jsonl --out data/cannibalization.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const c=require('./data/cannibalization.json'); process.stdout.write(String(c.conflictos.filter(x=>x.severidad==='alta'&&x.resolucion===undefined).length))")" -eq 0`
      — cero conflictos altos sin resolución en la corrida final.
    - `test "$(cd seo-tools && node -e "const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).map(JSON.parse); const k=m.map(r=>r.keywordPrimariaKey); process.stdout.write(String(k.length-new Set(k).size))")" -eq 0`
      — MAP-02 sobre el mapa completo: cero primarias repetidas.
    - `test "$(cd seo-tools && node -e "const c=require('./data/cannibalization.json'); const ok=c.conflictos.every(x=>Array.isArray(x.urlsCompartidas)&&typeof x.umbralAplicado==='number'); process.stdout.write(String(ok?1:0))")" -eq 1`
      — cada conflicto trae su evidencia de solape, no solo el veredicto.
    - Las siete conductas declaradas tienen prueba propia, incluida la del par del mismo cluster
      con solape cero que NO debe reportarse como conflicto.
    - `14-CANIBALIZACION.md` tiene la sección de deuda con la fecha de revisión escrita y la
      condición que la habilita.
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.serpapi))")" -eq 96`
  </acceptance_criteria>
  <done>
    El mapa completo no tiene dos URLs peleando la misma keyword, cada resolución dice quién ganó
    y con qué evidencia, y la revisión con Search Console quedó agendada con fecha.
  </done>
</task>

<task type="auto">
  <name>Task 3: Publicar el mapa completo en `Content Model` y cerrar el mapa legible</name>
  <files>seo-tools/data/url-map.jsonl, .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md</files>
  <read_first>
    - `seo-tools/src/phase14/cm-push.ts` y el resumen de la última corrida del plan 14-02.
    - `seo-tools/data/sheet-columns.json`, bloque `Content Model`.
    - `14-MAPA.md` tal como lo dejó el plan 14-02, para continuarlo y no reescribirlo.
  </read_first>
  <action>
    Correr `cm-push.ts` en ensayo sobre el mapa completo, leer el resumen —tiene que decir que
    las nueve del plan 14-02 se actualizan y el resto se inserta— y recién después con `--yes`.
    Repetir la escritura para volver a probar SHEET-06 con el dataset entero.

    Completar `14-MAPA.md` con el resto del mapa: la tabla completa de URLs con primaria,
    secundarias, intención, tipo exigido por la SERP, cluster, acción y el veredicto de
    `Leave, Update, or Bin?`. Agregar una sección de auditoría de lo publicado que responda la
    pregunta que motivó D-02: qué URLs vivas estaban compitiendo entre sí sin que nadie lo
    decidiera, y qué pasó con cada una.

    Agregar también la lista explícita de páginas publicadas por v1.1 el 2026-08-10 que quedan
    marcadas para reescribir, con el motivo, y actualizar `14-HANDOFF-V11.md` para que el otro
    workstream lea esa lista sin tener que abrir el mapa entero.
  </action>
  <verify>
    <automated>cd seo-tools && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/cm-push.ts --data data/url-map.jsonl --dry-run && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/cm-push.ts --data data/url-map.jsonl --yes && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/cm-push.ts --data data/url-map.jsonl --yes</automated>
  </verify>
  <acceptance_criteria>
    - La segunda corrida con `--yes` reporta `insertadas: 0` y `columnasAgregadas: 0`, con
      `actualizadas` igual al número de líneas de `url-map.jsonl`. Los números van al SUMMARY.
    - Lectura del documento vivo con scope de solo lectura: el conteo de filas con `Keyword` no
      vacía en `Content Model` coincide con el número de líneas del dataset.
    - Las tres columnas cuyo encabezado nombra a Ahrefs siguen vacías en todas las filas (D-13),
      verificado leyendo el documento vivo.
    - `14-MAPA.md` tiene una entrada por cada URL del mapa, con justificación en prosa.
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only | grep -c 'workstreams/milestone')" -eq 0`
  </acceptance_criteria>
  <done>
    El tab `Content Model` tiene una fila por URL mapeable con su keyword, intención, tipo,
    cluster y acción recomendada, recargarlo no duplica filas, y el mapa legible está completo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `audit.ts` → `src/` de la aplicación | Lectura del contenido publicado por el workstream paralelo. Escribir ahí rompe su deploy. |
| Resolución de conflictos → `url-map.jsonl` | Sobrescribe asignaciones que Juan ya aprobó en el plan 14-02. |
| Mapa completo → Sheet del cliente | Escritura masiva sobre un documento vivo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10 | Tampering | `cannibal-run.ts --apply` sobre las nueve aprobadas | high | mitigate | Las líneas del plan 14-02 no se reasignan; si el detector las señala, se anota como observación y se escala en la verificación de fase. |
| T-14-11 | Tampering | Lectura de `src/` | critical | mitigate | Solo lectura; criterio de staging vacío para `src/` antes de commitear. |
| T-14-12 | Information Disclosure | Contenido clínico citado como evidencia | medium | mitigate | Se cita texto ya publicado; no se genera ni se altera afirmación médica alguna. |
| T-14-13 | Repudiation | Conflicto rebajado de severidad para que el criterio pase | high | mitigate | El criterio cuenta conflictos altos SIN resolución, y lo sin resolver se nombra en el reporte y en el SUMMARY. |
| T-14-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si hiciera falta, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- `npx tsc --noEmit` en la raíz con estado 0.
- SHA-256 de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
- Cero primarias repetidas en el mapa completo.
</verification>

<success_criteria>
- MAP-01 cerrado: todas las URLs mapeables tienen exactamente una primaria y de tres a cinco
  secundarias.
- MAP-02 cerrado: cero primarias duplicadas, conflictos resueltos con evidencia y deuda de
  Search Console fechada.
- MAP-04 cerrado: cada asignación declara el tipo de página exigido y la acción.
- SHEET-02 cerrado: el tab `Content Model` tiene una fila por URL y recargarlo no duplica.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 14-03)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Mapa completo keyword → URL | `seo-tools/data/url-map.jsonl` | Dato |
| Reporte de canibalización | `seo-tools/data/cannibalization.json` | Dato medido |
| Mapa legible completo | `.../14-MAPA.md` | Entregable |
| Canibalización y deuda fechada | `.../14-CANIBALIZACION.md` | Entregable |
| Tab `Content Model` completo | Sheet del cliente | Entrega externa |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-03-SUMMARY.md` al terminar.
</output>