---
phase: 14-mapa-keyword-url-y-matriz-de-enlazado
workstream: seo-keywords
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seo-tools/src/phase14/model.ts
  - seo-tools/src/phase14/inventory.ts
  - seo-tools/src/phase14/inventory.test.ts
  - seo-tools/src/phase14/overlap.ts
  - seo-tools/src/phase14/overlap.test.ts
  - seo-tools/src/phase14/pagetype-map.ts
  - seo-tools/src/phase14/pagetype-map.test.ts
  - seo-tools/src/phase14/cm-push.ts
  - seo-tools/data/url-inventory.json
  - seo-tools/data/url-map.jsonl
  - seo-tools/data/page-type-map.json
autonomous: true
requirements: [MAP-01, MAP-04, SHEET-02]
must_haves:
  truths:
    - "Existe un inventario medido de las URLs del sitio, leído del código, no supuesto, con su estado viva o planificada."
    - "Una URL real recorre la tubería completa —inventario, asignación, escritura— y aparece en el tab `Content Model` del Sheet del cliente."
    - "Recargar esa misma URL actualiza su fila en vez de duplicarla."
    - "Cada cabeza con SERP medida tiene el tipo de página que la SERP exige, resuelto desde caché y sin gastar una sola búsqueda."
    - "Dos URLs solo pueden fusionarse si comparten URLs de SERP par a par por encima del umbral, nunca por pertenecer al mismo cluster."
  artifacts:
    - seo-tools/data/url-inventory.json
    - seo-tools/data/page-type-map.json
    - seo-tools/src/phase14/overlap.ts
    - seo-tools/src/phase14/cm-push.ts
  key_links:
    - "`inventory.ts` lee `src/app/` y `src/content/` en modo lectura y jamás escribe ahí."
    - "`cm-push.ts` usa `upsertRows` con `omitirCamposAusentes` activado: ninguna columna ajena a la fase se borra."
    - "`overlap.ts` consume el solape par a par de las capturas ya cacheadas, no la pertenencia a cluster."
---

<objective>
Fundar la fase midiendo la realidad antes de opinar sobre ella: el inventario de URLs sale del
código, no de un recuerdo; el tipo de página sale de las 96 capturas ya pagadas; y una URL real
recorre la tubería entera hasta el Sheet del cliente para probar que la tubería existe antes de
cargarle 18 filas encima.

Propósito: el resto de la fase se apoya en tres cosas que aquí quedan probadas —qué URLs hay,
qué tipo de página exige la SERP y cómo se escribe en `Content Model` sin romper nada.

Salida: `url-inventory.json`, `page-type-map.json`, el módulo de solape par a par y una fila
viva en el tab `Content Model`.

## Registro de decisiones de la fase (`14-CONTEXT.md`)

Estos identificadores se usan en los cuatro planes. Salen de `14-CONTEXT.md`, sección
`<decisions>`, y son inamovibles.

| ID | Decisión |
|---|---|
| D-01 | El handoff bloqueante ya se violó. La fase pasa de "asignar antes de escribir" a "asignar, auditar lo publicado y marcar qué reescribir". |
| D-02 | El alcance de la auditoría son todas las URLs vivas, no solo las cuatro páginas de servicio nuevas. |
| D-03 | MAP-02 se cierra cruzando el mapa contra sí mismo. La revisión con datos reales de Search Console queda como deuda fechada. |
| D-04 | Los modificadores de distrito son UNA página. Los nombres de clínica SÍ son páginas separadas. |
| D-05 | Pertenecer al mismo cluster NO implica misma URL: hay que mirar el solape par a par antes de fusionar. |
| D-06 | Las 10 de Oro son el criterio de desempate cuando dos URLs se pelean la misma keyword. |
| D-07 | MAP-04 sale de las 96 capturas cacheadas. Coste de SerpApi de esta fase: cero. |
| D-08 | Montefiori está fuera. Las cuatro sedes vigentes son consultorio Surco, Ricardo Palma, Sanna La Molina y Padre Luis Tezza. |
| D-09 | `las mejores pastillas para la ciática` es contenido de captación, no transaccional. |
| D-10 | Marcas de competidores y códigos CIE-10 quedan excluidos como objetivo. |
| D-11 | `/privacidad` queda fuera del mapa. |
| D-12 | Ruta rápida: MAP-03 primero, publicado en el Sheet antes de completar el resto del mapa. |
| D-13 | Las tres columnas `(Ahrefs)` de `Content Model` quedan vacías y no se agregan columnas de fuente al Sheet. |
| D-14 | La sede Ricardo Palma tiene prioridad de datos. |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/seo-keywords/ROADMAP.md
@.planning/workstreams/seo-keywords/STATE.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-VERIFICATION.md
@.planning/workstreams/seo-keywords/phases/13-clusters-competencia-y-las-10-de-oro/13-CLUSTERS.md
@seo-tools/data/sheet-columns.json
@seo-tools/src/phase13/kw-push.ts
@seo-tools/src/phase13/args.ts

**Restricciones duras del workstream, sin excepción:**

- Ningún archivo bajo `src/` de la aplicación se crea, se modifica ni se borra. Leerlo es lo
  que esta fase viene a hacer; escribirlo rompe el workstream paralelo.
- No se toca la exclusión de `seo-tools` en el `tsconfig.json` raíz.
- No se escribe nada en `.planning/workstreams/milestone/`, ni en `.planning/STATE.md`,
  `.planning/ROADMAP.md` o `.planning/REQUIREMENTS.md` de la raíz.
- `seo-tools/data/keywords.jsonl` es de solo lectura. SHA-256 esperado:
  `c59dad2ddaeaeff47395a8844a813b2f04a43f1a3f3ad490b7b43b98bbcd7eac`.
- `src/cli.ts` de `seo-tools` está CERRADO. Los ejecutables van bajo `src/phase14/` y se
  invocan con `./node_modules/.bin/tsx --env-file=../.secrets/.env`.
- Presupuesto de SerpApi de esta fase: **cero búsquedas** (D-07). Si algún paso pareciera
  necesitarlas, se detiene y se le pregunta a Juan; subir el techo no es decisión del ejecutor.

**Trampas ya conocidas, medidas en las fases 12 y 13:**

- `FORCE_COLOR` está activo: `console.log` de un número emite códigos ANSI y rompe `test -eq`.
  En los criterios usar `process.stdout.write(String(n))`.
- El shell es zsh: todo glob va entre comillas.
- `grep -c` sale con estado 1 cuando cuenta cero. Envolverlo en `test "$(...)" -eq N`.
- Después de `cd seo-tools`, las rutas relativas ya no llevan el prefijo `seo-tools/`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Una URL real del inventario al Sheet, de punta a punta</name>
  <files>seo-tools/src/phase14/model.ts, seo-tools/src/phase14/inventory.ts, seo-tools/src/phase14/cm-push.ts, seo-tools/data/url-inventory.json, seo-tools/data/url-map.jsonl</files>
  <read_first>
    - `seo-tools/data/sheet-columns.json`, bloque `Content Model`: `headerRow` 3, `mapBy` por
      nombre, `keyField` y `keyHeader` en `URL`, y la lista literal de columnas con su `status`.
    - `seo-tools/src/phase13/kw-push.ts` entero: es el patrón de punto de entrada de fase, con
      ensayo por defecto, `--yes` para escribir y defensa de dataset.
    - `seo-tools/src/sheets/upsert.ts` líneas 206 a 300: `UpsertOptions`, en particular
      `omitirCamposAusentes`, y la forma de `UpsertSummary`.
    - `seo-tools/src/phase13/args.ts`: `parseBanderas`, `texto`, `booleana`, `ejecutar`.
    - `src/content/location-pages.ts` y `src/app/sedes/[slug]/page.tsx`, solo para leer.
  </read_first>
  <action>
    Recorrer la tubería completa por UNA sola URL, elegida por prioridad de datos (D-14):
    `/sedes/clinica-ricardo-palma`.

    Definir en `model.ts` el tipo `AsignacionDeUrl` con los campos que el mapa va a usar en toda
    la fase: `url`, `titulo`, `estado` (`viva` o `planificada`), `origen` (el archivo del que se
    dedujo la URL), `esPaginaSeo`, `keywordPrimaria`, `keywordPrimariaKey`, `secundarias`
    (arreglo de 3 a 5), `intent`, `tipoDePagina`, `tipoExigidoPorSerp`, `cluster`,
    `clusterFuente`, `accion` (`dejar`, `reescribir` o `crear`), `dejarActualizarEliminar`,
    `canonical`, `topic` y `justificacion` en prosa. Un solo tipo para los cuatro planes.

    En `inventory.ts` implementar la lectura del sitio en modo solo lectura y dejarla operativa
    para la URL de la sede: parsear `src/content/location-pages.ts` y componer la ruta con el
    prefijo de `src/app/sedes/[slug]/`. La expansión al sitio entero es de la tarea 2.

    En `cm-push.ts` escribir el punto de entrada que carga `data/url-map.jsonl` en el tab
    `Content Model`. Reglas que no se negocian: ensayo por defecto y escritura solo con `--yes`;
    `upsertRows` invocado con `omitirCamposAusentes` en verdadero; los registros NO llevan campo
    para `Volume (Ahrefs)`, `Traffic Potential (Ahrefs)` ni `KD Difficulty (Ahrefs)`, que quedan
    intactas y vacías por D-13; y no se agrega ninguna columna nueva al tab, así que
    `addMissingColumns` queda en falso y el resumen debe reportar cero columnas agregadas.

    Emitir `url-map.jsonl` con una sola línea, la asignación real y definitiva de esa sede: la
    keyword primaria sale de `data/golden-10.json`, bloque `sedes`, entrada
    `clinica ricardo palma`. No es un valor de relleno: es la asignación que la fase entrega.

    Ejecutar el ensayo, leerlo, ejecutar la escritura, y volver a ejecutar la escritura para
    demostrar idempotencia. Registrar los tres resúmenes en el SUMMARY.
  </action>
  <verify>
    <automated>cd seo-tools && npm test && npm run typecheck && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/cm-push.ts --data data/url-map.jsonl --dry-run && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/cm-push.ts --data data/url-map.jsonl --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const l=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean); const r=JSON.parse(l[0]); process.stdout.write(String(l.length===1 && r.url.includes('clinica-ricardo-palma') && r.keywordPrimaria.length>0 && r.secundarias.length>=3 && r.secundarias.length<=5 ? 1 : 0))")" -eq 1`
    - **El tracer NO escribe en el documento del cliente.** Las dos corridas van con `--dry-run`
      y reportan `actualizadas: 1`, `insertadas: 0` y `columnasAgregadas: 0`, identicas entre si.
      Los tres numeros quedan copiados en el SUMMARY.
      <br>*Corregido el 2026-08-11 tras el chequeo de planes.* El `must_haves` del plan `14-02`
      afirma que Juan aprueba el mapa **antes** de que se escriba en el documento del cliente, y
      con este tracer escribiendo la fila de Ricardo Palma esa afirmacion ya era falsa para una
      de las nueve URLs. El ensayo prueba exactamente lo mismo —que el escritor mapea, respeta
      columnas ajenas y es idempotente— sin tocar el Sheet. La primera escritura real ocurre en
      `14-02`, despues del checkpoint.
    - `test "$(cd seo-tools && grep -c 'omitirCamposAusentes' src/phase14/cm-push.ts)" -ge 1`
    - `test "$(cd seo-tools && node -e "const c=require('./data/sheet-columns.json'); const s=new Set(c['Content Model'].columns.filter(x=>x.status==='sin-uso').map(x=>x.header)); const src=require('fs').readFileSync('src/phase14/cm-push.ts','utf8'); process.stdout.write(String([...s].filter(h=>src.includes(JSON.stringify(h))).length))")" -eq 0`
      — ninguna columna marcada `sin-uso` aparece como destino de escritura.
    - `test "$(shasum -a 256 seo-tools/data/keywords.jsonl | cut -d' ' -f1)" = "c59dad2ddaeaeff47395a8844a813b2f04a43f1a3f3ad490b7b43b98bbcd7eac"`
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96`
  </acceptance_criteria>
  <done>
    La fila de la sede Ricardo Palma existe en el tab `Content Model` del documento del cliente,
    con su keyword primaria y sus secundarias, y recargarla la actualiza sin duplicarla.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inventario medido del sitio y reconciliación con la cifra del roadmap</name>
  <files>seo-tools/src/phase14/inventory.ts, seo-tools/src/phase14/inventory.test.ts, seo-tools/data/url-inventory.json</files>
  <read_first>
    - `src/app/sitemap.xml/route.ts`: es la definición autoritativa de qué URLs son públicas y
      cuáles quedan fuera.
    - `src/content/service-pages.ts`, `src/content/location-pages.ts`, `src/content/blog.ts`:
      solo los `slug` y los títulos.
    - El listado de rutas estáticas bajo `src/app/`.
    - `14-CONTEXT.md`, sección `<specifics>`, sobre `/privacidad` y la cifra de mapeables.
  </read_first>
  <behavior>
    - Una ruta estática de `src/app/` con `page.tsx` aparece en el inventario con `estado: viva`.
    - Una ruta dinámica se expande a una URL por cada `slug` del módulo de contenido que la
      alimenta.
    - `/privacidad` aparece en el inventario con `mapeable: false` y el motivo escrito (D-11).
    - Las rutas de API, `llms.txt` y `sitemap.xml` no entran al inventario.
    - Una sede que el contenido ya no declara no aparece: Montefiori no debe aparecer (D-08).
  </behavior>
  <action>
    Extender `inventory.ts` a la lectura completa y determinista del sitio, siempre en modo
    lectura. Recorrer las rutas estáticas de `src/app/`, expandir las dinámicas con los `slug`
    de los tres módulos de contenido, y descartar rutas de API y de archivos generados.

    Emitir `data/url-inventory.json` con, por cada URL: `url`, `titulo`, `estado`, `mapeable`,
    `motivoNoMapeable` cuando corresponda, `origen` con el archivo y la línea de donde salió, y
    `familia` (`home`, `hub`, `servicio`, `sede`, `blog`, `institucional`, `conversion`, `legal`).
    Incluir un bloque `resumen` con el total, cuántas son mapeables y el reparto por familia.

    **Reconciliación, y esto es lo importante:** el roadmap declara 19 URLs y 18 mapeables. Ese
    número se escribió antes de que v1.1 publicara. El inventario reporta lo que mide, no lo que
    el roadmap esperaba. Si el conteo difiere, se registra la diferencia en el bloque
    `reconciliacion` del JSON con la lista nominal de las URLs que sobran o faltan respecto de
    esa expectativa, y se deja anotado para la aprobación de Juan del plan 14-02. Ajustar el
    inventario para que dé 18 sería falsear la medición.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=inventory && npm run typecheck && ./node_modules/.bin/tsx src/phase14/inventory.ts --out data/url-inventory.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const i=require('./data/url-inventory.json'); const u=i.urls; const ok = u.length>=19 && u.every(x=>x.url&&x.origen&&x.familia&&typeof x.mapeable==='boolean') && u.some(x=>x.url==='/privacidad'&&x.mapeable===false) && i.reconciliacion!==undefined; process.stdout.write(String(ok?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const i=require('./data/url-inventory.json'); process.stdout.write(String(i.urls.filter(x=>/montefiori/i.test(x.url)||/montefiori/i.test(x.titulo||'')).length))")" -eq 0`
    - Determinismo: dos corridas seguidas del generador producen el mismo SHA-256 de
      `data/url-inventory.json`.
    - El SUMMARY publica el conteo medido y, si difiere de 19 y 18, la lista nominal de la
      diferencia.
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0` al momento de preparar el commit.
  </acceptance_criteria>
  <done>
    Existe el inventario completo, medido del código, con procedencia por URL, y la diferencia
    con la cifra del roadmap está registrada en vez de disimulada.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Solape par a par y tipo de página exigido por la SERP, desde caché</name>
  <files>seo-tools/src/phase14/overlap.ts, seo-tools/src/phase14/overlap.test.ts, seo-tools/src/phase14/pagetype-map.ts, seo-tools/src/phase14/pagetype-map.test.ts, seo-tools/data/page-type-map.json</files>
  <read_first>
    - `seo-tools/src/phase13/cluster.ts` y `seo-tools/src/phase13/serp.ts`: de ahí sale el
      solape ya implementado y probado. Reutilizar, no reimplementar.
    - `seo-tools/src/phase13/pagetype.ts` y `seo-tools/data/serp-page-types.json`: la
      clasificación por tipo de página y su tabla de precedencia.
    - `seo-tools/data/clusters.json`: campos `umbralDeSolape`, `cabezas`, `uniones`,
      `tipoDePagina`, `repartoDeTipos`, `topResult`.
    - `13-CLUSTERS.md`, la parte que explica cómo se formó el cluster de 41 cabezas por
      transitividad.
  </read_first>
  <behavior>
    - Dos cabezas que comparten tres o más URLs en el top 10 devuelven `fusionable: true`.
    - Dos cabezas del mismo cluster que comparten cero URLs par a par devuelven
      `fusionable: false` y el motivo dice que el cluster se formó por transitividad.
    - El caso real de `ortopedia infantil lima`, `traumatología lima` y `cirujano de columna
      lima` da `fusionable: false` en los tres pares, pese a compartir cluster (D-05).
    - Una cabeza sin SERP medida devuelve `fusionable: false` con motivo de dato ausente, nunca
      un `true` optimista.
    - El tipo de página de una cabeza sale del reparto real de su top 10, no del nombre de la
      keyword.
  </behavior>
  <action>
    Implementar `overlap.ts` con una función que, dadas dos claves de keyword, devuelva el
    conjunto de URLs compartidas en el top 10, su cardinalidad y el veredicto de fusión contra un
    umbral configurable cuyo valor por defecto es 3, el mismo `umbralDeSolape` que la fase 13
    validó. El veredicto viaja siempre con el motivo en prosa y con la lista de URLs compartidas,
    para que quien lea el mapa pueda auditar la decisión sin abrir el caché.

    Este módulo es el guardarraíl de D-05 y lo van a llamar los planes 14-02 y 14-03 antes de
    fusionar cualquier par de URLs. La pertenencia a un cluster no es entrada de esta función.

    Implementar `pagetype-map.ts` para emitir `data/page-type-map.json`: por cada cabeza con SERP
    medida, el tipo dominante de su top 10, el reparto completo, el `topResult` y el nivel de
    confianza según cuántas posiciones se pudieron tipificar. Todo se resuelve leyendo
    `.cache/serpapi/` y `data/clusters.json` (D-07). El generador aborta si intenta salir a la
    red, y la cuota de SerpApi tiene que quedar en 96 al terminar.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern="overlap|pagetype-map" && npm run typecheck && ./node_modules/.bin/tsx src/phase14/pagetype-map.ts --out data/page-type-map.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const p=require('./data/page-type-map.json'); const c=p.cabezas||p.entradas; const ok = Array.isArray(c) && c.length>=91 && c.every(x=>x.tipoDePagina&&x.repartoDeTipos); process.stdout.write(String(ok?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const q=require('./.cache/_quota.json'); process.stdout.write(String(q.sources.serpapi.calls))")" -eq 96` — cero búsquedas gastadas.
    - Las pruebas cubren las cinco conductas declaradas, incluida la de las tres cabezas del
      cluster de 41 que dan `fusionable: false` par a par con datos reales del repositorio.
    - `test "$(cd seo-tools && node -e "const s=require('fs').readFileSync('src/phase14/overlap.ts','utf8').replace(/(['\"\`])(?:\\\\.|(?!\1)[^\\\\])*\1/g,'\"\"').replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm,''); process.stdout.write(String(/cluster/i.test(s)?1:0))")" -eq 0` — **borra literales de cadena ademas de comentarios**. El criterio prueba que el cluster no es ENTRADA del calculo de fusion, no que la palabra no se pueda escribir. Sin borrar los literales el criterio se contradice con la conducta que este mismo plan exige: el motivo obligatorio de `fusionable: false` dice que el cluster se formo por transitividad, y esa cadena reintroduciria la palabra. Cumplir la conducta romperia el criterio, y la salida barata seria aguar el motivo y perder la evidencia auditable.
      — el cluster no participa del cálculo de fusión ni siquiera como parámetro.
    - Determinismo: dos corridas producen el mismo SHA-256 de `data/page-type-map.json`.
  </acceptance_criteria>
  <done>
    Existe el veredicto de fusión par a par con su evidencia y el tipo de página exigido por la
    SERP para cada cabeza medida, todo desde caché y sin gastar cuota.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `seo-tools` → Sheet del cliente | Escritura sobre un documento vivo que el cliente lee. Un borrado silencioso es daño real. |
| `seo-tools` → `src/` de la aplicación | El workstream paralelo despliega desde ahí. Cualquier escritura rompe producción ajena. |
| `.cache/serpapi/` → generadores | Datos ya pagados. Una lectura que falle y salga a la red gasta cuota irrecuperable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `cm-push.ts` sobre `Content Model` | high | mitigate | `omitirCamposAusentes` activado y `addMissingColumns` en falso; criterio que prueba que ninguna columna `sin-uso` es destino de escritura. |
| T-14-02 | Tampering | `inventory.ts` leyendo `src/` | critical | mitigate | Solo lectura; criterio de diff en staging vacío para `src/` antes de commitear. |
| T-14-03 | Denial of Service | Cuota de SerpApi | high | mitigate | Generadores restringidos a caché; criterio que fija la cuota en 96 después de correr. |
| T-14-04 | Information Disclosure | `.secrets/.env` y service account | high | mitigate | Se pasan por `--env-file`, jamás se imprimen ni se versionan; `.secrets/` sigue ignorado. |
| T-14-05 | Repudiation | Procedencia del inventario | medium | mitigate | Cada URL lleva `origen` con archivo y línea. |
| T-14-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes nuevos: usa el `seo-tools` existente. Si algún paso lo requiriera, se detiene y escala. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- Guardarraíl de v1.1 intacto:
  `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- La app raíz sigue compilando: `npx tsc --noEmit` con estado 0.
- SHA-256 de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96.
- Al preparar el commit, `git diff --cached --name-only` no incluye ninguna ruta bajo `src/`
  de la aplicación ni bajo `.planning/workstreams/milestone/`.
- La fila de la sede Ricardo Palma es visible en el tab `Content Model` del documento vivo.
</verification>

<success_criteria>
- Existe `data/url-inventory.json` con todas las URLs del sitio medidas del código, su
  procedencia y la reconciliación contra la cifra del roadmap.
- Existe `data/page-type-map.json` con el tipo de página exigido por la SERP para las cabezas
  medidas, generado con cero búsquedas.
- `overlap.ts` decide fusiones por solape par a par y está probado contra el caso real que la
  fase 13 dejó documentado.
- `cm-push.ts` escribe en `Content Model` de forma idempotente y sin tocar columnas ajenas.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 14-01)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Inventario de URLs | `seo-tools/data/url-inventory.json` | Dato medido |
| Mapa keyword → URL, primera línea | `seo-tools/data/url-map.jsonl` | Dato, se completa en 14-02 y 14-03 |
| Tipo de página por cabeza | `seo-tools/data/page-type-map.json` | Dato medido desde caché |
| Módulo de solape par a par | `seo-tools/src/phase14/overlap.ts` | Código |
| Cargador de `Content Model` | `seo-tools/src/phase14/cm-push.ts` | Código |
| Fila viva de la sede Ricardo Palma | Tab `Content Model` del Sheet | Entrega externa |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-01-SUMMARY.md` al terminar.
</output>