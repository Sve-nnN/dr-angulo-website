---
phase: 14-mapa-keyword-url-y-matriz-de-enlazado
workstream: seo-keywords
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - seo-tools/src/phase14/canonical.ts
  - seo-tools/src/phase14/canonical.test.ts
  - seo-tools/src/phase14/links.ts
  - seo-tools/src/phase14/links.test.ts
  - seo-tools/src/phase14/ca-push.ts
  - seo-tools/src/phase14/il-push.ts
  - seo-tools/data/canonicals.json
  - seo-tools/data/internal-links.json
  - .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-ENLAZADO.md
autonomous: true
requirements: [MAP-05, SHEET-04, SHEET-05]
must_haves:
  truths:
    - "Cada URL del mapa tiene su topic y su canonical propuesto publicados en el tab `Canonical Audit`."
    - "Cada URL tiene hasta ocho enlaces salientes propuestos, cada uno con su anchor, publicados en el tab `Internal Linking Audit`."
    - "Los enlaces se mapean por posición dentro de cada bloque `Link N` / `Anchor N` / `Title with Link`, no por nombre de encabezado."
    - "Recargar los tres tabs de la fase no duplica filas ni agrega columnas."
    - "La matriz se propone; ningún enlace se implementa en el código de la aplicación."
  artifacts:
    - seo-tools/data/canonicals.json
    - seo-tools/data/internal-links.json
    - .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-ENLAZADO.md
  key_links:
    - "`il-push.ts` usa el modelo con `mapBy` por posición del tab `Internal Linking Audit`."
    - "Los anchors salen de las keywords secundarias del destino, no del título de la página de origen."
    - "Los tres cargadores corren con `omitirCamposAusentes` y sin agregar columnas."
---

<objective>
Cerrar la fase con las dos entregas que faltan: el canonical propuesto por URL y la matriz de
enlazado interno entre clusters, ambas publicadas en el Sheet del cliente.

Propósito: el mapa dice por qué pelea cada URL; la matriz dice cómo se sostienen entre sí. Sin
enlazado, cada URL rankea sola y el trabajo de clusters de la fase 13 no se capitaliza.

Salida: los tabs `Canonical Audit` e `Internal Linking Audit` llenos, `14-ENLAZADO.md` y los
datasets que los respaldan.

Decisiones que gobiernan este plan: D-04, D-05, D-06, D-11, D-13. La matriz **se propone, no se
implementa**: los enlaces los escribe v1.1 en el código, y este workstream no toca `src/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-CONTEXT.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-03-SUMMARY.md
@.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-MAPA.md
@seo-tools/data/url-map.jsonl
@seo-tools/data/sheet-columns.json
@seo-tools/src/phase14/cm-push.ts
@seo-tools/src/sheets/upsert.ts

Rigen las mismas restricciones duras y las mismas trampas de shell del plan 14-01.
Presupuesto de SerpApi de este plan: cero.

**Hechos medidos de los dos tabs, ya verificados, no hay que redescubrirlos:**

- `Canonical Audit`: encabezados en la fila **2**, `mapBy` por nombre, clave `URL`. Columnas:
  `URL`, `Keyword`, `Topic`, `Canonical`, `Approved?`, `Implemented?`.
- `Internal Linking Audit`: encabezados en la fila **2**, `mapBy` por **posición**, clave `URL`.
  Columnas: `URL`, `Title`, `Code`, `Action`, `Cluster`, y después ocho bloques de tres
  —`Link N`, `Anchor N`, `Title with Link`—. **El encabezado `Title with Link` se repite ocho
  veces**, así que mapear por nombre asigna mal siete de cada ocho. `Anchor 8` lleva un espacio
  final en el documento, a diferencia de los otros siete.
- El nombre del tab es exactamente `Canonical Audit`. El nombre largo que se lee en la fila 1 es
  un banner decorativo y pedirlo a la API devuelve HTTP 400.
- La fila 1 de ambos tabs es un banner decorativo, no la fila de encabezados.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonicals propuestos y el tab `Canonical Audit`</name>
  <files>seo-tools/src/phase14/canonical.ts, seo-tools/src/phase14/canonical.test.ts, seo-tools/src/phase14/ca-push.ts, seo-tools/data/canonicals.json</files>
  <read_first>
    - `seo-tools/data/sheet-columns.json`, bloque `Canonical Audit`: `headerRow`, `mapBy`,
      `keyField`, `keyHeader` y las seis columnas con su `status`.
    - `seo-tools/src/phase14/cm-push.ts`: el patrón de cargador de fase que hay que replicar.
    - `seo-tools/data/url-map.jsonl` completo.
    - `src/app/sitemap.xml/route.ts` y `src/config` o donde viva `siteConfig.url`, para el
      origen absoluto. Solo lectura.
  </read_first>
  <behavior>
    - Una URL sin variantes recibe como canonical su propia URL absoluta.
    - Dos URLs que el reporte de canibalización marcó como conflicto resuelto reciben canonicals
      distintos: resolver el conflicto separó los temas, no los unificó.
    - Una URL marcada para crear recibe canonical propuesto igual, porque el canonical es lo que
      v1.1 va a implementar cuando la escriba.
    - El `topic` de una URL es el nombre del cluster de su primaria, no la keyword.
    - `/privacidad` no aparece (D-11).
    - El canonical siempre es absoluto, con el mismo origen que el sitemap, y sin barra final
      salvo en la raíz.
  </behavior>
  <action>
    Implementar `canonical.ts` como función pura sobre `url-map.jsonl`, que emite
    `data/canonicals.json` con `url`, `keyword`, `topic`, `canonical`, `aprobado` e
    `implementado`. Los dos últimos son de seguimiento del cliente: se escriben con el valor
    inicial que corresponde a algo propuesto y aún no implementado, y quedan bajo control de Juan
    de ahí en adelante.

    Implementar `ca-push.ts` con la misma disciplina que `cm-push.ts`: ensayo por defecto,
    `--yes` para escribir, `omitirCamposAusentes` en verdadero, `addMissingColumns` en falso.

    Correr ensayo, escribir, y volver a escribir para probar idempotencia sobre este tab.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=canonical && npm run typecheck && ./node_modules/.bin/tsx src/phase14/canonical.ts --map data/url-map.jsonl --out data/canonicals.json && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/ca-push.ts --data data/canonicals.json --dry-run && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/ca-push.ts --data data/canonicals.json --yes && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/ca-push.ts --data data/canonicals.json --yes</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const c=require('./data/canonicals.json'); const m=require('fs').readFileSync('data/url-map.jsonl','utf8').split('\n').filter(Boolean).length; const f=c.filas||c; process.stdout.write(String(f.length===m?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const c=require('./data/canonicals.json'); const f=c.filas||c; const ok=f.every(x=>/^https?:\/\//.test(x.canonical)&&x.topic&&x.keyword); process.stdout.write(String(ok?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const c=require('./data/canonicals.json'); const f=c.filas||c; const s=f.map(x=>x.canonical); process.stdout.write(String(s.length-new Set(s).size))")" -eq 0`
      — ningún canonical repetido entre dos URLs.
    - La segunda corrida con `--yes` reporta `insertadas: 0` y `columnasAgregadas: 0`.
    - Las seis conductas declaradas tienen prueba propia y la suite completa queda en verde.
  </acceptance_criteria>
  <done>
    El tab `Canonical Audit` tiene una fila por URL con keyword, topic y canonical propuesto, y
    recargarlo no duplica filas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: La matriz de enlazado interno entre clusters</name>
  <files>seo-tools/src/phase14/links.ts, seo-tools/src/phase14/links.test.ts, seo-tools/data/internal-links.json, .planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-ENLAZADO.md</files>
  <read_first>
    - `seo-tools/data/url-map.jsonl` y `data/cannibalization.json`.
    - `seo-tools/data/clusters.json`: `id`, `nombre`, `familia`, `cabezas`, `uniones`.
    - `seo-tools/data/golden-10.json`, para saber qué URLs merecen recibir enlaces primero.
    - `seo-tools/src/phase14/overlap.ts`.
  </read_first>
  <behavior>
    - Ninguna URL se enlaza a sí misma.
    - Ninguna URL propone más de ocho enlaces salientes, que es lo que el tab acepta.
    - Una URL que sirve una keyword de oro recibe al menos un enlace entrante desde otra URL del
      mismo cluster (D-06).
    - Un post de blog de captación enlaza hacia la página de servicio de su tema, y no al revés
      como enlace principal (D-09 se respeta: la captación alimenta, no vende).
    - El anchor de un enlace sale de una keyword secundaria de la URL de destino, nunca del
      título de la URL de origen.
    - Dos URLs cuyo par dio conflicto en el reporte de canibalización no se enlazan con el mismo
      anchor.
    - Un anchor no se repite apuntando a dos destinos distintos en todo el sitio.
    - Toda URL mapeable tiene al menos un enlace entrante: ninguna queda huérfana.
  </behavior>
  <action>
    Implementar `links.ts` como función pura que emite `data/internal-links.json`: por cada URL
    de origen, `url`, `title`, `code`, `action`, `cluster` y un arreglo `enlaces` de hasta ocho
    elementos con `link`, `anchor` y `titleWithLink`, más el motivo del enlace en prosa.

    Criterio de construcción de la matriz, en este orden:

    1. Dentro del cluster primero: cada URL enlaza a las URLs cuyo tema comparte cluster y cuyo
       solape par a par lo respalda. La pertenencia a cluster ordena candidatos; el solape par a
       par decide (D-05).
    2. Hacia las de oro: las URLs que sirven keywords de oro son destino prioritario.
    3. De captación a servicio: los posts del blog enlazan hacia la página de servicio de su
       tema.
    4. Hacia las sedes: las páginas de servicio enlazan a las sedes donde se atiende ese
       servicio. Las cuatro sedes vigentes son consultorio Surco, Ricardo Palma, Sanna La Molina
       y Padre Luis Tezza (D-08).
    5. Hacia conversión: cada página de servicio enlaza a la URL de agendamiento.

    Escribir `14-ENLAZADO.md` con la matriz legible: una sección por cluster, la tabla de origen
    a destino con su anchor y el motivo, y arriba una nota que diga con todas las letras que esto
    **se propone y lo implementa v1.1 en el código**, más el conteo de enlaces entrantes por URL
    para que se vea de un vistazo qué páginas quedan mejor sostenidas.
  </action>
  <verify>
    <automated>cd seo-tools && npm test -- --test-name-pattern=links && npm run typecheck && ./node_modules/.bin/tsx src/phase14/links.ts --map data/url-map.jsonl --out data/internal-links.json</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(cd seo-tools && node -e "const d=require('./data/internal-links.json'); const f=d.filas||d; const ok=f.every(r=>r.enlaces.length<=8&&r.enlaces.every(e=>e.link!==r.url&&e.anchor&&e.anchor.length>0)); process.stdout.write(String(ok?1:0))")" -eq 1`
    - `test "$(cd seo-tools && node -e "const d=require('./data/internal-links.json'); const f=d.filas||d; const ent=new Map(); f.forEach(r=>ent.set(r.url,0)); f.forEach(r=>r.enlaces.forEach(e=>{if(ent.has(e.link))ent.set(e.link,ent.get(e.link)+1)})); process.stdout.write(String([...ent.values()].filter(v=>v===0).length))")" -eq 0`
      — cero URLs huérfanas de enlaces entrantes.
    - `test "$(cd seo-tools && node -e "const d=require('./data/internal-links.json'); const f=d.filas||d; const m=new Map(); let mal=0; f.forEach(r=>r.enlaces.forEach(e=>{const a=e.anchor.trim().toLowerCase(); if(m.has(a)&&m.get(a)!==e.link)mal++; else m.set(a,e.link)})); process.stdout.write(String(mal))")" -eq 0`
      — ningún anchor apunta a dos destinos distintos.
    - `test "$(cd seo-tools && node -e "const d=require('./data/internal-links.json'); const f=d.filas||d; process.stdout.write(String(f.every(r=>r.enlaces.every(e=>e.motivo&&e.motivo.length>20))?1:0))")" -eq 1`
    - Las ocho conductas declaradas tienen prueba propia y la suite completa queda en verde.
    - Determinismo: dos corridas producen el mismo SHA-256 de `data/internal-links.json`.
  </acceptance_criteria>
  <done>
    Existe la matriz completa de enlazado con anchor y motivo por enlace, sin huérfanas y sin
    anchors ambiguos, y el documento legible dice que se propone y no se implementa.
  </done>
</task>

<task type="auto">
  <name>Task 3: Publicar `Internal Linking Audit` mapeando por posición y cerrar los tres tabs</name>
  <files>seo-tools/src/phase14/il-push.ts</files>
  <read_first>
    - `seo-tools/data/sheet-columns.json`, bloque `Internal Linking Audit` completo: `mapBy` en
      posición, `headerRow` 2, y la lista de 29 columnas con sus `field`, incluidos los ocho
      `titleWithLink1` a `titleWithLink8` que comparten el mismo encabezado.
    - `seo-tools/src/sheets/schema.ts` y `src/sheets/upsert.ts`: cómo se resuelve un modelo cuyo
      mapeo es posicional y no nominal.
    - `seo-tools/src/phase14/ca-push.ts` de la tarea 1.
  </read_first>
  <action>
    Implementar `il-push.ts` para cargar `data/internal-links.json` en el tab
    `Internal Linking Audit`. Lo que decide si este cargador está bien o mal es una sola cosa: el
    mapeo es **posicional**. El encabezado que acompaña a cada bloque de enlace se repite ocho
    veces en el documento, así que resolver columnas por nombre asignaría el mismo destino ocho
    veces. La columna del octavo anchor lleva además un espacio final en el documento; el modelo
    guarda el literal y el recorte se hace al comparar, nunca editando el modelo.

    Misma disciplina que los otros dos cargadores: ensayo por defecto, `--yes` para escribir,
    `omitirCamposAusentes` en verdadero, `addMissingColumns` en falso.

    Al terminar, correr una pasada de cierre de los tres cargadores de la fase —`cm-push`,
    `ca-push`, `il-push`— por segunda vez, para dejar SHEET-06 probado sobre los tres tabs en la
    misma corrida, y copiar los tres resúmenes al SUMMARY.
  </action>
  <verify>
    <automated>cd seo-tools && npm run typecheck && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/il-push.ts --data data/internal-links.json --dry-run && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/il-push.ts --data data/internal-links.json --yes && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/il-push.ts --data data/internal-links.json --yes && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/cm-push.ts --data data/url-map.jsonl --yes && ./node_modules/.bin/tsx --env-file=../.secrets/.env src/phase14/ca-push.ts --data data/canonicals.json --yes</automated>
  </verify>
  <acceptance_criteria>
    - Las tres corridas finales reportan `insertadas: 0` y `columnasAgregadas: 0`. Los nueve
      números —tres tabs por tres campos— quedan copiados en el SUMMARY.
    - Lectura del documento vivo con scope de solo lectura sobre una URL de servicio elegida al
      azar: las columnas del primer bloque de enlace y las del octavo traen valores distintos y
      coinciden con `data/internal-links.json`. Si el mapeo hubiera sido por nombre, los ocho
      bloques traerían el mismo valor.
    - `test "$(cd seo-tools && node -e "const c=require('./data/sheet-columns.json'); process.stdout.write(String(c['Internal Linking Audit'].mapBy))")" = "posicion"`
    - `test "$(cd seo-tools && node -e "const s=require('fs').readFileSync('src/phase14/il-push.ts','utf8'); process.stdout.write(String(s.includes('addMissingColumns: false')&&s.includes('omitirCamposAusentes')?1:0))")" -eq 1`
    - `test "$(git -C "$(git rev-parse --show-toplevel)" diff --cached --name-only -- src/ | wc -l | tr -d ' ')" -eq 0`
  </acceptance_criteria>
  <done>
    Los tres tabs de la fase están llenos, el mapeo posicional está probado contra el documento
    vivo y una segunda carga de los tres no inserta ni agrega nada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `il-push.ts` → tab con encabezados repetidos | Un mapeo por nombre escribe el mismo valor ocho veces y destruye la matriz. |
| Cargadores → columnas de otras fases | Un campo ausente que se resuelva a cadena vacía borra celdas que este plan no escribió. |
| Este workstream → `src/` de la aplicación | La matriz se propone; implementarla sería escribir código ajeno. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-14 | Tampering | Mapeo nominal en `Internal Linking Audit` | critical | mitigate | El modelo declara mapeo posicional; criterio que compara el primer bloque contra el octavo en el documento vivo. |
| T-14-15 | Tampering | Borrado de columnas ajenas | high | mitigate | `omitirCamposAusentes` en verdadero y `addMissingColumns` en falso en los tres cargadores. |
| T-14-16 | Elevation of Privilege | Implementar los enlaces en el código | critical | mitigate | La matriz se entrega como propuesta; criterio de staging vacío para `src/`. |
| T-14-17 | Repudiation | Enlace sin motivo | medium | mitigate | Cada enlace exige `motivo`; criterio con longitud mínima. |
| T-14-SC | Tampering | Instalaciones de npm | high | accept | Este plan no instala paquetes. Si hiciera falta, se detiene y escala a Juan. |
</threat_model>

<verification>
- `cd seo-tools && npm test` en verde y `npm run typecheck` sin salida.
- `test "$(npx tsc --noEmit --listFiles | grep -c '/seo-tools/')" -eq 0`.
- `npx tsc --noEmit` en la raíz con estado 0.
- SHA-256 de `seo-tools/data/keywords.jsonl` sin cambios.
- Cuota de SerpApi en 96: la fase entera cerró sin gastar una búsqueda.
- `git diff --cached --name-only` sin rutas bajo `src/` de la app ni bajo
  `.planning/workstreams/milestone/`.
- Los tres tabs verificados por lectura del documento vivo.
</verification>

<success_criteria>
- MAP-05 cerrado: existe la matriz de enlazado con qué URL enlaza a cuál y con qué anchor.
- SHEET-04 cerrado: `Canonical Audit` con una fila por URL con keyword, topic y canonical.
- SHEET-05 cerrado: `Internal Linking Audit` con una fila por URL y hasta ocho enlaces salientes
  con su anchor, mapeados por posición.
- SHEET-06 sigue probado: segunda carga de los tres tabs con cero inserciones y cero columnas
  agregadas.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 14-04)

| Artefacto | Ruta | Naturaleza |
|---|---|---|
| Canonicals propuestos | `seo-tools/data/canonicals.json` | Dato |
| Matriz de enlazado | `seo-tools/data/internal-links.json` | Dato |
| Matriz legible | `.../14-ENLAZADO.md` | Entregable |
| Cargadores de los dos tabs | `seo-tools/src/phase14/ca-push.ts`, `il-push.ts` | Código |
| Tabs `Canonical Audit` e `Internal Linking Audit` | Sheet del cliente | Entrega externa |
</artifacts>

<output>
Crear `.planning/workstreams/seo-keywords/phases/14-mapa-keyword-url-y-matriz-de-enlazado/14-04-SUMMARY.md` al terminar.
</output>