---
phase: 09-p-ginas-por-sede-y-cobertura-local
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/components/layout/header.tsx
autonomous: false
requirements: [SEDE-01, SEDE-02, SEDE-03]
user_setup: []

must_haves:
  truths:
    - "El enlace Sedes del header lleva al hub /sedes y no a /agendar, en escritorio y en móvil (D-13, DC-03)."
    - "En el menú móvil, debajo de Sedes cuelgan las cuatro páginas de sede, con el mismo patrón anidado que ya usa Servicios (D-13, DC-03)."
    - "Agendar cita sigue siendo alcanzable desde el header en un clic, en escritorio y en móvil: el flujo de conversión no perdió superficie (D-02)."
    - "Un humano confirma en el navegador que las tres páginas de clínica no ofrecen el chat del doctor, que el consultorio privado sí, y que al llegar desde una sede a /agendar la tarjeta correcta queda resaltada (D-09, D-10)."
    - "El JSON-LD de una página de sede valida sin errores en el Rich Results Test, comprobado por un humano contra la URL desplegada o el HTML pegado (SEDE-03)."
    - "npm run build, npm run lint, npx tsc --noEmit, npm run content:check y npm run sedes:check terminan en 0 al cerrar la fase."
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "entrada Sedes apuntando al hub y submenú móvil con las cuatro sedes"
      contains: "locationPages"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/app/sedes/page.tsx"
      via: "la entrada Sedes de NAV_LINKS apunta al hub"
      pattern: "\"/sedes\""
    - from: "src/components/layout/header.tsx"
      to: "src/content/location-pages.ts"
      via: "el submenú móvil recorre locationPages, igual que ya recorre servicePages"
      pattern: "locationPages"
---

<decision_ids>
Los IDs `D-01` a `D-13` y `DC-01` a `DC-03` están definidos en la tabla de `09-01-PLAN.md`, derivada de `09-CONTEXT.md`. Este plan los cita, no los redefine.

Decisiones que este plan ejecuta y cierra: D-02, D-09, D-10, D-13, DC-03.
</decision_ids>

<objective>
Cerrar la fase por donde entra el tráfico: la navegación. El enlace "Sedes" del header apunta hoy a `/agendar`, decisión que quedó pendiente en `09-CONTEXT.md` y que este plan resuelve (D-13). Y después, la verificación humana de lo único que una puerta automática no puede confirmar: que el navegador se comporta como se diseñó, que el resaltado por ancla se ve y se entiende, y que el marcado valida en la herramienta de Google.

Se ejecuta en la tercera ola a propósito. El submenú móvil recorre las páginas de sede, así que solo tiene sentido con las cuatro publicadas, y el checkpoint humano no puede aprobar una fase a medio construir.

Purpose: que las páginas nuevas sean encontrables desde cualquier parte del sitio y que un humano firme la regla de negocio antes de desplegar.

Output: header apuntando al hub, submenú móvil con las cuatro sedes, y la fase verificada de punta a punta.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-CONTEXT.md
@.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-01-SUMMARY.md
@.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-02-SUMMARY.md

@src/components/layout/header.tsx
@src/content/location-pages.ts
@src/content/locations.ts
</context>

<interface_context>
**`src/components/layout/header.tsx`** (es `"use client"`, ya importa `servicePages`)
```
const NAV_LINKS = [
  { href: "/", label: "Inicio" },                                              // 0
  { href: "/sobre-el-doctor", label: "El doctor", longLabel: "Sobre el doctor" }, // 1
  { href: "/agendar", label: "Sedes", longLabel: "Sedes y horarios" },          // 2  ← D-13
  { href: "/testimonios", label: "Testimonios" },                              // 3
  { href: "/preguntas-frecuentes", label: "Preguntas", longLabel: "Preguntas frecuentes" },
  { href: "/blog", label: "Blog" },
  { href: "/contacto", label: "Contacto" },
]
```
Escritorio: `NAV_LINKS.slice(0, 2)`, luego `<ServicesMenu />` (megamenú), luego `NAV_LINKS.slice(2)`.
Móvil: `NAV_LINKS.slice(0, 2)`, luego el enlace `/servicios` con un `<ul>` anidado de `servicePages`, luego `NAV_LINKS.slice(2)`, luego un `<BookingCta>` a ancho completo.
`BookingCta` enlaza siempre a `/agendar`, en tamaño `sm` en la barra de escritorio y a ancho completo en el pie del menú móvil.

**`src/content/location-pages.ts`**: `locationPages: LocationPage[]` con las cuatro sedes; cada una trae `slug` y `navLabel`.
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarea 1: el header lleva al hub de sedes</name>
  <precondition>Las cuatro rutas de sede existen y `npm run sedes:check` pasa sobre las cuatro, tal como cerró el plan 02. Verificar con `npm run build && npm run sedes:check` antes de editar; si falla, detenerse.</precondition>
  <reversibility rating="reversible">Cambiar un `href` de la navegación y agregar una lista anidada. Se revierte en una edición.</reversibility>
  <read_first>
    - `src/components/layout/header.tsx` completo, en especial el comentario sobre `label` contra `longLabel` y el bloque de navegación móvil con la lista anidada de Servicios, que es el patrón a replicar.
    - `src/components/layout/services-menu.tsx`, solo para confirmar que el megamenú de escritorio vive ahí y no se toca en esta tarea.
    - `src/content/location-pages.ts`, los cuatro `navLabel`.
  </read_first>
  <files>src/components/layout/header.tsx</files>
  <action>
`src/components/layout/header.tsx`. Dos cambios y ninguno más.

Primero (D-13, DC-03): la entrada de índice 2 de `NAV_LINKS` cambia su `href` de `/agendar` a `/sedes`. El `label` "Sedes" y el `longLabel` "Sedes y horarios" se conservan: ya describían el destino correcto, solo que el destino no existía. Actualizar el comentario de cabecera de `NAV_LINKS` para dejar dicho que la entrada apunta al hub.

`/agendar` no queda huérfano: sigue alcanzable en un clic desde el header por el `BookingCta` "Agendar cita", que está en la barra de escritorio y al pie del menú móvil, y desde el propio hub, que enlaza al flujo de conversión (D-02). Son dos intenciones distintas con dos superficies distintas, que es exactamente la separación que pide `09-CONTEXT.md`: "dónde atiende" contra "quiero una cita".

Segundo (DC-03): en el bloque de navegación móvil, replicar para Sedes el mismo patrón anidado que ya usa Servicios. La barra de escritorio **no** estrena megamenú: el hub `/sedes` cumple ese papel con cuatro tarjetas, y agregar un segundo megamenú duplicaría el trabajo del hub y agregaría un componente de cliente sin necesidad. En escritorio, "Sedes" queda como enlace plano.

Para el submenú móvil, seguir el estilo de recortes que ya tiene el archivo en vez de meter condicionales dentro del `map`: renderizar `NAV_LINKS.slice(2, 3)` (la entrada de Sedes), inmediatamente después la lista anidada, y después `NAV_LINKS.slice(3)`. La lista anidada es un `<ul>` con las mismas clases que la de Servicios, recorriendo `locationPages` y con un `<Link>` a `/sedes/{slug}` que muestra el `navLabel`, `block min-h-11` como área táctil (A11Y-10) y `onClick={() => setOpen(false)}` para cerrar el menú al navegar, igual que todos los enlaces de ese bloque.

No se agregan clases de color nuevas: se reusan exactamente las del submenú de Servicios, que ya cumplen COLOR-01. No se agrega ningún estado ni ningún efecto: el submenú es estático y vive dentro del menú que ya se abre y se cierra.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm run content:check && npm run sedes:check && node -e "const h=require('fs').readFileSync('src/components/layout/header.tsx','utf8');if(/href:\s*\"\/agendar\"/.test(h))throw new Error('NAV_LINKS todavia apunta a /agendar');if(!/href:\s*\"\/sedes\"/.test(h))throw new Error('NAV_LINKS no apunta a /sedes');if(!h.includes('locationPages'))throw new Error('el submenu movil no recorre locationPages');console.log('header ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint` y `npm run build` terminan en 0.
    - El comando `<automated>` imprime `header ok`.
    - El header de cualquier página apunta al hub: `grep -q 'href="/sedes"' .next/server/app/contacto.html` termina en 0.
    - Las cuatro sedes están en el árbol del menú móvil: `for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do grep -q "/sedes/$s" .next/server/app/contacto.html || exit 1; done` termina en 0.
    - `/agendar` sigue alcanzable desde el header: `grep -q 'href="/agendar"' .next/server/app/contacto.html` termina en 0, aportado por `BookingCta` (D-02).
    - El megamenú de escritorio no se tocó: `git diff --name-only` no lista `src/components/layout/services-menu.tsx`.
    - `grep -c 'useState\|useEffect' src/components/layout/header.tsx` devuelve el mismo número que antes de esta tarea: el submenú no introdujo estado nuevo.
    - `grep -rn 'border-l-4' src/components/layout/header.tsx | wc -l` devuelve `0`.
    - `grep -c 'min-h-11' src/components/layout/header.tsx` devuelve 2 o más: el submenú de sedes cumple el objetivo táctil igual que el de servicios (A11Y-10).
    - `npm run content:check` y `npm run sedes:check` terminan en 0.
  </acceptance_criteria>
  <done>"Sedes" en el header lleva al hub en escritorio y en móvil, el menú móvil despliega las cuatro sedes debajo, y "Agendar cita" sigue a un clic desde el header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 2: verificación humana de la fase completa</name>
  <what-built>
La fase completa: cuatro páginas de sede bajo `/sedes/{slug}`, el hub `/sedes`, el JSON-LD de ubicación por sede, el enlazado en ambos sentidos con `/agendar` con resaltado por ancla, el sitemap en 21 URLs, la entrada "Sedes" del header apuntando al hub con submenú móvil, y dos puertas automáticas en verde (`npm run content:check` y `npm run sedes:check`).

Lo que las puertas ya garantizan y no hace falta volver a revisar a mano: que las cuatro rutas existen y prerenderizan, que cada `h1` es único y dice lo que debe, que cada página emite su JSON-LD con dirección, geo y horario, que ninguna página de clínica trae el chat del doctor en su cuerpo y que el consultorio privado sí lo trae, que las cuatro se enlazan desde `/agendar` y desde el hub, y que el sitemap declara 21 URLs.

Lo que queda para el ojo humano es lo que ninguna de esas comprobaciones puede firmar: cómo se ve, cómo se siente el recorrido y si Google acepta el marcado.
  </what-built>
  <how-to-verify>
Levantar el sitio en local:

```bash
npm run build && npm start
```

**1. Regla de negocio, que es lo que más importa (D-09).** Abrir las cuatro páginas y mirar la sección "Cómo se agenda en esta sede":

- http://localhost:3000/sedes/consultorio-privado → tiene que ofrecer el WhatsApp del doctor y decir con todas las letras que ahí él agenda directamente.
- http://localhost:3000/sedes/clinica-ricardo-palma → central de citas y web de la clínica. Ningún WhatsApp del doctor en el cuerpo de la página.
- http://localhost:3000/sedes/sanna-la-molina → app SANNA, central y agendamiento de Sanna. Ningún WhatsApp del doctor.
- http://localhost:3000/sedes/clinica-tezza → central de citas y web de Tezza. Ningún WhatsApp del doctor.

El botón flotante verde de la esquina y el del pie de página aparecen en todas, como en todo el sitio: esos no cuentan y no cambian en esta fase. Lo que se revisa es el cuerpo de la página. Además, leer si el texto deja claro **quién** saca la cita en cada sede. Si al leerlo queda la duda de si el doctor puede reservar en Ricardo Palma, el texto falló aunque el marcado esté bien.

**2. Recorrido de ancla (D-10).** Desde `/sedes/clinica-tezza`, hacer clic en el enlace de cierre que vuelve a `/agendar`. Al llegar: la tarjeta de Clínica Padre Luis Tezza tiene que quedar visible, resaltada y no tapada por el header. Repetir con las otras tres. Comprobar también que funciona con el JavaScript desactivado en el navegador: el resaltado es CSS, no debe depender de nada más.

**3. Navegación (D-13).** En escritorio, "Sedes" del header lleva a `/sedes`. En móvil, con el menú abierto, "Sedes y horarios" lleva al hub y debajo cuelgan las cuatro sedes. "Agendar cita" sigue llevando a `/agendar` desde los dos menús.

**4. Teclado y foco.** Recorrer `/sedes` solo con Tab: cada tarjeta recibe un anillo de foco visible sobre la tarjeta entera. En una página de sede, recorrer con Tab hasta el enlace de vuelta a `/agendar` y activarlo con Enter: el foco tiene que quedar en la tarjeta de destino, no volver arriba.

**5. Anchos.** Revisar `/sedes` y una página de sede a 320px, 375px, 768px y 1440px. Sin scroll horizontal en ninguno.

**6. Lenguaje visual.** Las páginas nuevas tienen que parecer del mismo sitio que `/servicios/hernia-discal`. En concreto: ninguna barra de color al borde izquierdo de ningún bloque, el dorado solo como superficie de botón y nunca como texto, y ningún patrón que no exista ya en el resto del sitio.

**7. Rich Results Test (SEDE-03, criterio 3 del roadmap).** Abrir https://search.google.com/test/rich-results, pegar el HTML de `/sedes/clinica-ricardo-palma` con la opción de código, y confirmar que el marcado se parsea sin errores y que aparece la entidad de la clínica con su dirección y su horario. Repetir con `/sedes/consultorio-privado`. Reportar cualquier error o advertencia que aparezca, con su texto literal.

**8. Lectura del contenido.** Leer las cuatro páginas de corrido. Buscar específicamente: datos de "cómo llegar" que no se puedan sostener (tiempos de viaje, paraderos, estacionamientos inventados), cifras de cirugías o de resultados, credenciales que no estén en el CV, y cualquier frase en primera persona sobre casos concretos. Si aparece algo así, hay que quitarlo antes de desplegar.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si los ocho puntos pasan, o describir qué falló indicando la URL y el punto.</resume-signal>
</task>

</tasks>

<deferred_verification>
Dos criterios de éxito del roadmap no se pueden cerrar dentro de la fase y quedan explícitamente pendientes al despliegue. No se dan por cumplidos acá.

- **Criterio 2 del roadmap (SEDE-02):** que `site:drangulocolumna.com ricardo palma` devuelva `/sedes/clinica-ricardo-palma` y no `/servicios` ni `/agendar`. Requiere que Google haya rastreado e indexado la URL, lo que ocurre días después del despliegue. Lo que esta fase sí garantiza y verifica es la condición necesaria: que existe una URL propia cuyo title, `h1` y cuerpo nombran la clínica y que está declarada en el sitemap. La comprobación de la SERP se hace después del despliegue y, si falla, es trabajo de la fase 10 o de la 11, no una regresión de esta.
- **Criterio 3 del roadmap (SEDE-03), la parte de "válido en el Rich Results Test":** se cubre en el punto 7 del checkpoint humano, contra el HTML local. La validación contra la URL desplegada se repite después del despliegue.
</deferred_verification>

<source_audit>
## Auditoría de cobertura de fuentes (conjunto de planes 09-01 a 09-03)

**GOAL**: ROADMAP, Phase 9: "Cada sede pelea su propia búsqueda con página propia, en vez de competir todas desde una página genérica."

| Item | Fuente | Estado | Plan |
|------|--------|--------|------|
| Cuatro sedes con URL propia, dirección, días, horario y canal de agenda | GOAL, criterio 1 | COVERED | 09-01 (Ricardo Palma), 09-02 (las otras tres) |
| Página de Ricardo Palma nombra la clínica en title, H1 y contenido | GOAL, criterio 2 | COVERED | 09-01 T1 |
| `site:` devuelve la página de sede | GOAL, criterio 2 | DEFERRED al despliegue, documentado en `<deferred_verification>` | 09-03 |
| JSON-LD de ubicación por sede, válido, con dirección y horario | GOAL, criterio 3 | COVERED | 09-01 T1, 09-02 T2, validación humana en 09-03 |
| `/agendar` enlaza a cada sede y cada sede vuelve con su sede seleccionada | GOAL, criterio 4 | COVERED | 09-01 T2, 09-02 T1 |

**REQ**: REQUIREMENTS.md

| ID | Estado | Plan |
|----|--------|------|
| SEDE-01 | COVERED | 09-01, 09-02, 09-03 |
| SEDE-02 | COVERED (condición necesaria; la SERP se verifica tras indexar) | 09-01, 09-02, 09-03 |
| SEDE-03 | COVERED | 09-01, 09-02, 09-03 |

**RESEARCH**: no hay `09-RESEARCH.md` en esta fase. El insumo competitivo vive en `09-CONTEXT.md`, sección `<specifics>`, y se audita abajo.

| Item | Fuente | Estado | Plan |
|------|--------|--------|------|
| Ricardo Palma es la sede prioritaria, related search verificada en la SERP del 2026-08-10 | specifics | COVERED: es el camino del trazador | 09-01 |
| Las páginas por sede son lo que permite competir por "traumatólogo en {clínica}" | specifics | COVERED: patrón de title y `h1` por clínica | 09-01 T1, 09-02 T1 |
| Las cuatro sedes ya están en `/agendar` desde v1.0; la fase reorganiza, no descubre | specifics | COVERED: `locations.ts` no se modifica en toda la fase | 09-01, 09-02 |

**CONTEXT**: decisiones de `09-CONTEXT.md`, con los IDs de la tabla de `09-01-PLAN.md`

| ID | Estado | Plan |
|----|--------|------|
| D-01 URLs bajo `/sedes/` con los slugs existentes | COVERED | 09-01 T1, 09-02 T1 |
| D-02 `/sedes` como hub, `/agendar` como conversión, enlazados | COVERED | 09-01 T2, 09-03 T1 |
| D-03 Una ruta dinámica alimentada por datos | COVERED | 09-01 T1 |
| D-04 Title y `h1` de Ricardo Palma | COVERED | 09-01 T1 |
| D-05 Extensión media, sin puerta de 900 palabras en `/sedes/*` | COVERED | 09-01 T1, 09-02 T1 |
| D-06 Dirección, cómo llegar, horarios, canal y enlace a las cuatro guías | COVERED | 09-01 T1, 09-02 T1 |
| D-07 `MedicalClinic` y `MedicalBusiness` con address, geo y horario | COVERED | 09-01 T1, 09-02 T2 |
| D-08 Extender `structured-data.tsx`, no duplicar | COVERED | 09-01 T1, 09-02 T2 |
| D-09 Regla del WhatsApp por sede | COVERED | 09-01 T1 y T3, 09-02 T1 y T3, 09-03 checkpoint |
| D-10 Ancla por slug con la tarjeta resaltada, sin JavaScript | COVERED | 09-01 T2, 09-03 checkpoint |
| D-11 Enlazado en ambos sentidos | COVERED | 09-01 T2, 09-02 T1 |
| D-12 Sitemap de 16 a 21 URLs | COVERED | 09-01 T1 y T2, 09-02 T1 |
| D-13 Destino del enlace "Sedes" del header | COVERED | 09-03 T1 |

**Exclusiones legítimas, no son huecos.** Las cuatro ideas de `<deferred>` de `09-CONTEXT.md` quedan fuera por decisión del usuario: Clínica Montefiori como quinta sede (pendiente de confirmación humana heredada de v1.0), breadcrumbs y titles de 60 caracteres e imágenes OG por sede (fase 10), Google Business Profile y citaciones NAP (fase 11), y páginas por distrito (no pedidas). El `<domain>` de `09-CONTEXT.md` excluye además contenido clínico nuevo, cerrado en la fase 8.

**Resultado: cero items sin plan.**
</source_audit>

<threat_model>
## Trust Boundaries

Sin superficie nueva respecto de los planes 01 y 02. Este plan modifica un componente de navegación que ya existe y no agrega entradas de usuario, endpoints, variables de entorno ni paquetes.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Spoofing | canal de agenda visto por un humano en el navegador | high | mitigate | El punto 1 del checkpoint revisa las cuatro páginas a ojo, incluida la claridad del texto sobre quién saca la cita, que es lo que la puerta automática no puede medir (D-09) |
| T-09-09 | Tampering | navegación del header hacia rutas de sede | low | mitigate | Los destinos se derivan de `locationPages`, el mismo array que genera las rutas; no se escriben `href` a mano. Criterio verificable: las cuatro rutas aparecen en el HTML del header de cualquier página (tarea 1) |
| T-09-10 | Elevation of Privilege | componente de cliente del header | low | accept | El submenú es estático dentro del menú que ya existe, no agrega estado, no lee ni escribe almacenamiento del navegador y no toca la sesión. Criterio verificable: la cuenta de hooks del archivo no cambia |
| T-09-SC | Tampering | cadena de suministro | low | accept | La fase no instala paquetes. La cuenta de dependencias de `package.json` no cambia en ninguno de los tres planes |
</threat_model>

<verification>
Cierre de la fase, con el repositorio limpio:

```bash
npx tsc --noEmit
npm run lint
npm run build
npm run content:check
npm run sedes:check
grep -c '<loc>' .next/server/app/sitemap.xml.body    # 21
for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do
  test -f ".next/server/app/sedes/$s.html" || echo "falta $s"
  grep -q "href=\"/sedes/$s\"" .next/server/app/sedes.html   || echo "el hub no enlaza $s"
  grep -q "href=\"/sedes/$s\"" .next/server/app/agendar.html || echo "/agendar no enlaza $s"
  grep -q "href=\"/agendar#$s\"" ".next/server/app/sedes/$s.html" || echo "$s no vuelve a su ancla"
done
grep -q 'href="/sedes"' .next/server/app/contacto.html || echo "el header no lleva al hub"
```

Todo en 0 y sin ninguna línea de error impresa. Después, el checkpoint humano.
</verification>

<success_criteria>
- El enlace "Sedes" del header lleva al hub en escritorio y en móvil, con las cuatro sedes desplegadas en el menú móvil, y `/agendar` sigue a un clic (D-02, D-13, DC-03).
- Un humano confirmó en el navegador la regla del canal de agenda en las cuatro sedes, el recorrido de ancla con y sin JavaScript, el foco de teclado, los cinco anchos y la coherencia visual con la fase 8 (D-09, D-10).
- El marcado de al menos dos páginas de sede se parseó sin errores en el Rich Results Test (SEDE-03).
- Las cinco comprobaciones automáticas del repositorio terminan en 0 y el sitemap declara 21 URLs.
- La verificación de la SERP queda registrada como pendiente al despliegue, no como cumplida (SEDE-02).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 03)

Archivos modificados:
- `src/components/layout/header.tsx`: `NAV_LINKS[2].href` pasa de `/agendar` a `/sedes`; submenú móvil anidado con las cuatro sedes

Sin archivos nuevos, sin componentes nuevos, sin tipos nuevos.

## Inventario completo de la fase 9

Archivos nuevos:
- `src/content/location-pages.ts`
- `src/app/sedes/[slug]/page.tsx`
- `src/app/sedes/page.tsx`
- `src/components/locations/sede-card.tsx`
- `scripts/check-sedes.mjs`

Archivos modificados:
- `src/components/structured-data.tsx`, `src/app/sitemap.ts`, `src/app/agendar/page.tsx`, `src/components/locations/location-card.tsx`, `src/app/globals.css`, `src/components/layout/header.tsx`, `scripts/check-content.mjs`, `package.json`

Tipos y componentes exportados: `LocationPage`, `locationPages`, `getLocationPage`, `SedeCard`, `SedeJsonLd`

Rutas nuevas (5): `/sedes`, `/sedes/consultorio-privado`, `/sedes/clinica-ricardo-palma`, `/sedes/sanna-la-molina`, `/sedes/clinica-tezza`

Script de npm nuevo: `sedes:check`

Sin dependencias nuevas y sin variables de entorno nuevas.
</artifacts>

<output>
Crear `.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-03-SUMMARY.md` al terminar.
</output>
</content>
