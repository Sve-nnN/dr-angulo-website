---
phase: 09-p-ginas-por-sede-y-cobertura-local
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/content/location-pages.ts
  - src/components/structured-data.tsx
  - scripts/check-sedes.mjs
  - scripts/check-content.mjs
autonomous: true
requirements: [SEDE-01, SEDE-02, SEDE-03]
user_setup: []

must_haves:
  truths:
    - "Las cuatro sedes tienen URL propia bajo /sedes/ con su dirección, sus días, sus horarios y el canal de agenda que corresponde a esa sede (SEDE-01, D-01, D-06)."
    - "El consultorio privado es la única de las cuatro páginas que ofrece el chat del doctor como canal de agenda; las tres clínicas muestran su central de citas o su web oficial y ninguna otra vía (SEDE-01, D-09)."
    - "Cada una de las cuatro páginas emite su propio JSON-LD de ubicación: MedicalBusiness para el consultorio y MedicalClinic para las tres clínicas, cada uno con su address, su geo y su openingHoursSpecification (SEDE-03, D-07)."
    - "El nodo de cada sede en el grafo del sitio apunta a su página de sede como url canónica y conserva la web oficial de la clínica en sameAs (D-07, D-08)."
    - "/agendar enlaza a las cuatro páginas de sede y cada una vuelve a /agendar con su propia ancla, que resalta la tarjeta correspondiente (SEDE-03, D-11)."
    - "El sitemap declara 21 URLs: las 16 de v1.0 más el hub y las cuatro sedes (D-12)."
    - "npm run build, npm run lint, npx tsc --noEmit, npm run content:check y npm run sedes:check terminan en 0 con las cuatro sedes en el manifiesto de la puerta."
  artifacts:
    - path: "src/content/location-pages.ts"
      provides: "las cuatro entradas editoriales, una por sede de locations.ts"
      contains: "consultorio-privado"
    - path: "scripts/check-sedes.mjs"
      provides: "manifiesto de cuatro sedes con la regla del canal de agenda diferenciada por tipo"
      contains: "consultorio"
  key_links:
    - from: "src/content/location-pages.ts"
      to: "src/content/locations.ts"
      via: "los cuatro slugs editoriales coinciden uno a uno con los de locations.ts; la puerta lo verifica leyendo los dos archivos"
      pattern: "clinica-tezza"
    - from: "src/components/structured-data.tsx"
      to: "src/app/sedes/[slug]/page.tsx"
      via: "locationNode apunta url a /sedes/{slug} ahora que las cuatro rutas existen, y mueve la web de la clínica a sameAs"
      pattern: "/sedes/"
    - from: "scripts/check-sedes.mjs"
      to: "src/app/sedes/[slug]/page.tsx"
      via: "el manifiesto declara por sede el h1 esperado, el tipo de schema y los canales, y mide el cuerpo recortado por data-sede-body"
      pattern: "MANIFEST"
---

<decision_ids>
Los IDs `D-01` a `D-13` y `DC-01` a `DC-03` están definidos en la tabla de `09-01-PLAN.md`, derivada de `09-CONTEXT.md`. Este plan los cita, no los redefine.

Decisiones que este plan ejecuta: D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-09, D-11, D-12.
</decision_ids>

<objective>
Expandir el camino que el plan 01 dejó probado a las tres sedes restantes: consultorio privado en Surco, Sanna sede La Molina y Clínica Padre Luis Tezza. La arquitectura no cambia: se agregan tres entradas a la capa editorial y las rutas, el schema, el sitemap, el hub y el enlazado con `/agendar` salen de los mismos recorridos que ya existen (D-03).

El trabajo real de este plan es de dos clases. Una es redacción por sede, que es lo único que no puede derivarse de datos. La otra es cerrar la regla de negocio en las cuatro páginas a la vez: el consultorio privado es la única sede donde el chat del doctor agenda de verdad, y con las cuatro páginas publicadas la puerta pasa de verificar una sede a verificar el contraste completo, que es donde la regla puede romperse (D-09).

Purpose: que cada sede pelee su propia búsqueda en vez de competir desde una página genérica.

Output: las cuatro rutas de sede publicadas, el sitemap en 21 URLs y la puerta con las cuatro sedes en su manifiesto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-CONTEXT.md
@.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-01-SUMMARY.md

@src/content/locations.ts
@src/content/location-pages.ts
@src/app/sedes/[slug]/page.tsx
@src/components/structured-data.tsx
@scripts/check-sedes.mjs
</context>

<interface_context>
Lo que el plan 01 dejó en pie y este plan consume. No hay que redescubrirlo.

**`src/content/location-pages.ts`**
```
export type LocationPage = {
  slug, navLabel, title, description, h1, heroLead, cardSummary,
  gettingThere: string[], conditionsLead: string,
}
export const locationPages: LocationPage[]                                  // una entrada: clinica-ricardo-palma
export function getLocationPage(slug): { page: LocationPage; location: Location } | undefined
```

**`src/components/structured-data.tsx`**
```
function locationNode(location: Location)   // @type MedicalBusiness | MedicalClinic segun kind (D-07)
                                            // url: website de la clinica, o /agendar si no tiene
export function SedeJsonLd({ page, location })   // JsonLdScript id="sede-jsonld", @graph de dos nodos
```

**`scripts/check-sedes.mjs`**
```
SEDE_BODY            marcador de region del cuerpo de la sede, con el ="" pegado
MANIFEST             array hardcodeado; una entrada: clinica-ricardo-palma
```

**Datos exactos de las tres sedes que faltan** (de `src/content/locations.ts`, no se modifican)
```
consultorio-privado
  name             Consultorio privado del Dr. Angulo
  kind             consultorio
  streetAddress    Av. El Derby 254, piso 24, oficina 2403
  building         Edificio Lima Central Tower
  reference        Cruce de Av. El Derby con Manuel Olguín
  addressLocality  Santiago de Surco
  geo              { latitude: -12.0977043, longitude: -76.9729404 }
  telephone        +51964305682          website: ninguna
  schedule         Viernes y sábados, 9:00 a. m. a 5:00 p. m.
  openingHours     [{ days: ["Friday","Saturday"], opens: "09:00", closes: "17:00" }]
  channels         WhatsApp +51 964 305 682, "Único canal donde el doctor agenda directamente"

sanna-la-molina
  name             Clínica Sanna, sede La Molina
  kind             clinica
  streetAddress    Av. Raúl Ferrero 1256
  addressLocality  La Molina
  geo              { latitude: -12.0902268, longitude: -76.9505892 }
  telephone        +5116355000
  website          https://www.sanna.pe/centros-clinicos/la-molina-lima/informacion-general/
  schedule         Martes 8:00 a. m. a 7:00 p. m.; Jueves 8:00 a. m. a 12:00 p. m.
  openingHours     [{ Tuesday 08:00-19:00 }, { Thursday 08:00-12:00 }]
  channels         App SANNA (sin href) · Central (01) 635 5000 → tel:+5116355000 · https://agendamiento.sanna.pe/

clinica-tezza
  name             Clínica Padre Luis Tezza
  kind             clinica
  streetAddress    Av. El Polo 570
  addressLocality  Santiago de Surco
  geo              { latitude: -12.1032942, longitude: -76.9718807 }
  telephone        +5116105050
  website          https://clinicatezza.com.pe/
  schedule         Jueves y viernes, 2:00 p. m. a 6:00 p. m.
  openingHours     [{ Thursday, Friday 14:00-18:00 }]
  channels         Central de citas (01) 610 5050 → tel:+5116105050 · https://clinicatezza.com.pe/
```

**`src/lib/site-config.ts`**: para el CTA del consultorio se usa la clave existente `booking_page`, cuyo mensaje ya dice "consultorio privado (Av. El Derby 254, Surco)". **No** se agrega ninguna clave nueva a `whatsappMessages`.
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarea 1: las tres entradas editoriales que faltan</name>
  <precondition>`/sedes/clinica-ricardo-palma` está publicada y `npm run sedes:check` pasa sobre ella, tal como cerró el plan 01. Verificar con `npm run build && npm run sedes:check` antes de escribir nada; si falla, detenerse.</precondition>
  <reversibility rating="costly">Los tres slugs restantes se vuelven URLs indexadas al desplegar. Ya estaban decididos en 09-CONTEXT.md (D-01) y vienen de v1.0 sin renombrar, así que se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `src/content/location-pages.ts` completo: la entrada de Ricardo Palma es la plantilla de redacción y de forma para las tres nuevas.
    - `src/content/locations.ts` completo, en especial `bookingSummary` y `channels` de cada sede, y el comentario de cabecera con la regla de negocio.
    - `src/app/sedes/[slug]/page.tsx` completo: para saber qué campos consume la plantilla y en qué sección cae cada uno.
    - `src/content/blog.ts` y `src/content/services.ts`, un fragmento de cada uno, para calibrar el registro de redacción.
  </read_first>
  <files>src/content/location-pages.ts, scripts/check-content.mjs</files>
  <action>
`src/content/location-pages.ts`. Agregar tres entradas a `locationPages`, sin tocar el tipo `LocationPage` ni `getLocationPage`. El orden del array define el orden de las tarjetas del hub: `consultorio-privado`, `clinica-ricardo-palma`, `sanna-la-molina`, `clinica-tezza`, que es el mismo orden de `locations` y el mismo que ya usa `/agendar`.

Valores fijos de `title` y `h1`, que las aserciones comprueban carácter por carácter. Para las tres clínicas se sigue el patrón que D-04 estableció en Ricardo Palma: el prefijo `Traumatólogo y cirujano de columna en ` seguido del `name` textual de esa sede en `locations.ts`. Se escriben como cadenas literales en el archivo de datos, no se componen en tiempo de render: son texto editorial y tienen que poder leerse y cambiarse sin rastrear una plantilla.

```
consultorio-privado
  navLabel  Consultorio privado en Surco
  title     Consultorio de traumatología y columna en Surco, Lima
  h1        Consultorio privado del Dr. Angulo en Surco

sanna-la-molina
  navLabel  Clínica Sanna, La Molina
  title     Traumatólogo y cirujano de columna en Clínica Sanna, sede La Molina
  h1        Traumatólogo y cirujano de columna en Clínica Sanna, sede La Molina

clinica-tezza
  navLabel  Clínica Padre Luis Tezza
  title     Traumatólogo y cirujano de columna en Clínica Padre Luis Tezza
  h1        Traumatólogo y cirujano de columna en Clínica Padre Luis Tezza
```

El consultorio privado es la excepción del patrón porque su `name` no es un nombre que un paciente escriba en el buscador. Su consulta es de distrito, no de institución, y por eso el title nombra Surco y Lima.

Para cada una de las tres entradas, redactar `description`, `heroLead`, `cardSummary`, `gettingThere` y `conditionsLead` con el mismo criterio que la entrada de Ricardo Palma:
- `description`: entre 140 y 155 caracteres, nombrando la sede, el distrito, los días de atención y quién saca la cita.
- `heroLead`: dos oraciones, sin repetir el `h1` palabra por palabra.
- `cardSummary`: una línea para la tarjeta del hub.
- `gettingThere`: dos o tres párrafos con referencias reales del barrio a partir de la dirección, el distrito y, cuando existe, el edificio y la referencia que ya trae `locations.ts`. Nada de instrucciones de tránsito inventadas, tiempos de viaje, nombres de paraderos ni indicaciones de estacionamiento que no se puedan afirmar desde los datos. Si un dato no se puede sostener, no se escribe. Para el consultorio privado, el edificio y el cruce de avenidas que ya están en los datos son el eje de esta sección.
- `conditionsLead`: un párrafo que introduzca qué se evalúa en esa sede antes de la lista de enlaces a las cuatro guías de servicio (D-06).

Redacción: segunda persona, español neutro, sin signos de admiración, sin emojis, sin guiones largos ni medios. Extensión media, sin mínimo de palabras (D-05). Prohibido afirmar credenciales fuera de `src/content/cv.ts`, cantidad de cirugías, tasas de éxito, promesas de resultado, precios y cualquier construcción en primera persona sobre casos concretos.

La distinción de canal se redacta explícitamente en cada texto, no se deja implícita (D-09). Para el consultorio privado, el texto dice que ahí el doctor agenda directamente. Para Sanna y Tezza, dice que la cita la gestiona la clínica con su propio sistema y que el doctor no puede reservarla por el paciente, y nombra el canal concreto de esa clínica. Ese contraste es la confusión más frecuente de los pacientes según el propio `locations.ts`, y el texto tiene que resolverla en la página, no solo en el marcado.

Ningún texto de las tres entradas de clínica menciona el número de teléfono personal del doctor, su chat ni el canal del consultorio privado. El literal que la puerta cuenta está en `<acceptance_criteria>`.

`scripts/check-content.mjs`. Subir `SITEMAP_TOTAL` de 18 a 21 y actualizar el comentario que lo acompaña: 16 URLs de v1.0 más el hub `/sedes` más las cuatro sedes (D-12). Esa es la última vez que esta constante cambia en la fase. Ninguna ruta de sede entra al `MANIFEST` de este script (D-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm run content:check && for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do test -f ".next/server/app/sedes/$s.html" || exit 1; done && test "$(grep -c '<loc>' .next/server/app/sitemap.xml.body)" = "21" && echo "cuatro sedes prerenderizadas"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint` y `npm run build` terminan en 0.
    - Los cuatro archivos existen: `for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do test -f ".next/server/app/sedes/$s.html" || exit 1; done` termina en 0.
    - Cada página tiene exactamente un `h1`: `for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do test "$(grep -c '<h1' .next/server/app/sedes/$s.html)" = "1" || exit 1; done` termina en 0.
    - Los `h1` son los textos fijados arriba: `grep -q 'Consultorio privado del Dr. Angulo en Surco' .next/server/app/sedes/consultorio-privado.html`, `grep -q 'Traumatólogo y cirujano de columna en Clínica Sanna, sede La Molina' .next/server/app/sedes/sanna-la-molina.html` y `grep -q 'Traumatólogo y cirujano de columna en Clínica Padre Luis Tezza' .next/server/app/sedes/clinica-tezza.html` terminan en 0.
    - <!-- planner-discipline-allow: wa.me --> **Regla de negocio (D-09).** El cuerpo delimitado por `data-sede-body` de las tres páginas de clínica tiene cero enlaces `wa.me`, y el del consultorio privado tiene uno o más. Se comprueba con `npm run sedes:check` una vez extendido el manifiesto en la tarea 3; en esta tarea se verifica el contraste crudo sobre el documento completo: `test "$(grep -c 'wa\.me' .next/server/app/sedes/consultorio-privado.html)" -gt "$(grep -c 'wa\.me' .next/server/app/sedes/clinica-ricardo-palma.html)"` termina en 0, y las tres páginas de clínica dan el mismo número entre sí, que es el piso del footer más el botón flotante.
    - Cada página de clínica muestra su propio canal: `grep -q 'tel:+5116355000' .next/server/app/sedes/sanna-la-molina.html`, `grep -q 'https://agendamiento.sanna.pe/' .next/server/app/sedes/sanna-la-molina.html`, `grep -q 'tel:+5116105050' .next/server/app/sedes/clinica-tezza.html` y `grep -q 'https://clinicatezza.com.pe/' .next/server/app/sedes/clinica-tezza.html` terminan en 0.
    - Cada página vuelve a su ancla: `for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do grep -q "href=\"/agendar#$s\"" ".next/server/app/sedes/$s.html" || exit 1; done` termina en 0 (D-11).
    - El hub indexa las cuatro: `for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do grep -q "href=\"/sedes/$s\"" .next/server/app/sedes.html || exit 1; done` termina en 0 (D-02).
    - `/agendar` enlaza a las cuatro: `for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do grep -q "href=\"/sedes/$s\"" .next/server/app/agendar.html || exit 1; done` termina en 0 (D-11).
    - `grep -c '<loc>' .next/server/app/sitemap.xml.body` devuelve `21` (D-12).
    - `npm run content:check` termina en 0.
    - <!-- planner-discipline-allow: /sedes --> La puerta de la fase 8 sigue sin cubrir rutas de sede (D-05), comprobado sobre código y no sobre comentarios: `grep -vE '^\s*(//|/\*|\*)' scripts/check-content.mjs | grep -c '/sedes'` devuelve `0`.
    - `grep -n 'SITEMAP_TOTAL = 21' scripts/check-content.mjs | wc -l` devuelve `1`.
    - Los cuatro slugs editoriales coinciden con los de `locations.ts`: `node -e "const s=require('fs').readFileSync('src/content/location-pages.ts','utf8');const l=require('fs').readFileSync('src/content/locations.ts','utf8');for(const g of ['consultorio-privado','clinica-ricardo-palma','sanna-la-molina','clinica-tezza']){if(!s.includes(g)||!l.includes(g))throw new Error('falta '+g)}console.log('slugs ok')"` imprime `slugs ok`.
    - Ningún texto de las tres entradas de clínica trae el número personal del doctor: `grep -c '964305682\|964 305 682' src/content/location-pages.ts` devuelve `0`.
    - Sin guiones largos ni medios en el contenido nuevo: `grep -c '—\|–' src/content/location-pages.ts` devuelve `0`.
  </acceptance_criteria>
  <done>Las cuatro sedes tienen página propia con su dirección, sus días, sus horarios y su canal correcto; el sitemap declara 21 URLs y las puertas del repositorio siguen en verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 2: el nodo de cada sede apunta a su página como url canónica</name>
  <reversibility rating="costly">Cambia la `url` de las cuatro sedes en el grafo que se emite en todas las páginas del sitio, así que altera lo que Google reindexa. Es una edición de una función y se revierte con otra, pero el efecto en el buscador tarda en asentarse. Se ejecuta ahora y no en el plan 01 justamente porque hasta ahora tres de las cuatro páginas no existían.</reversibility>
  <read_first>
    - `src/components/structured-data.tsx`, la función `locationNode` y la función `physicianNode` completas.
    - La comprobación 10 de `scripts/check-sedes.mjs`, para saber qué campos del nodo ya se verifican.
  </read_first>
  <files>src/components/structured-data.tsx</files>
  <action>
`src/components/structured-data.tsx`, solo la función `locationNode`. Ahora que las cuatro rutas de sede existen, la `url` del nodo de cada sede pasa a ser `abs("/sedes/" + location.slug)`: la página de sede es la URL canónica de esa ubicación dentro de este sitio, y era el destino natural del campo desde el principio, solo que hasta este plan no existía. La web oficial de la clínica, cuando la sede tiene una, se mueve a `sameAs` como array de un elemento, que es el campo correcto para "la misma entidad en otro dominio". Para el consultorio privado, que no tiene web propia, no se emite `sameAs` y desaparece el reenvío a `/agendar` que existía como sustituto.

Nada más de esta función cambia: `@type`, `@id`, `address`, `geo`, `telephone`, `hasMap`, `openingHoursSpecification`, `medicalSpecialty` y `branchOf` se conservan exactamente. `physicianNode` no se toca: su `hospitalAffiliation` y su `worksFor` referencian las sedes por `@id` y siguen resolviendo contra los mismos nodos.

Dejar un comentario corto explicando por qué la `url` apunta a la página de sede y no a la web de la clínica, para que un cambio futuro sepa qué está deshaciendo.

Como `locationNode` es la única función que produce marcado de ubicación, el cambio llega a la vez al grafo raíz que emite el layout y al `sede-jsonld` de cada página, sin ninguna edición adicional. Ese es exactamente el beneficio de haber reusado la función en vez de escribir un nodo paralelo (D-08).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && node -e "const fs=require('fs');for(const s of ['consultorio-privado','clinica-ricardo-palma','sanna-la-molina','clinica-tezza']){const h=fs.readFileSync('.next/server/app/sedes/'+s+'.html','utf8');const m=h.match(/<script id=\"sede-jsonld\" type=\"application\/ld\+json\">([\s\S]*?)<\/script>/);if(!m)throw new Error('falta sede-jsonld en '+s);const d=JSON.parse(m[1].replace(/\\\\u003c/g,'<'));const n=d['@graph'].find(x=>x['@id']==='https://drangulocolumna.com/#sede-'+s);if(!n)throw new Error('falta el nodo de '+s);if(n.url!=='https://drangulocolumna.com/sedes/'+s)throw new Error('url incorrecta en '+s+': '+n.url);if(!n.address.streetAddress)throw new Error('address vacia en '+s);if(!n.openingHoursSpecification.length)throw new Error('horario vacio en '+s);if(typeof n.geo.latitude!=='number')throw new Error('geo invalida en '+s);const t=s==='consultorio-privado'?'MedicalBusiness':'MedicalClinic';if(n['@type']!==t)throw new Error('tipo incorrecto en '+s+': '+n['@type'])}console.log('schema por sede ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint` y `npm run build` terminan en 0.
    - El comando `<automated>` imprime `schema por sede ok`: las cuatro sedes emiten su nodo con la `url` de su página, `address`, `geo`, `openingHoursSpecification` y el `@type` que fija D-07.
    - `grep -c '"@type":"MedicalClinic"' .next/server/app/sedes/clinica-tezza.html` devuelve 2 o más: el nodo aparece en el grafo raíz y en el script de la página, con el mismo `@id`.
    - `grep -c '"@type":"Hospital"' .next/server/app/sedes/clinica-tezza.html` devuelve `0`.
    - `grep -c '"@type":"MedicalBusiness"' .next/server/app/sedes/consultorio-privado.html` devuelve 2 o más.
    - Las webs oficiales siguen declaradas, ahora en `sameAs`: `grep -q '"sameAs":\["https://www.crp.com.pe/"\]' .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0.
    - El `Physician` no se rompió: `grep -c '"@id":"https://drangulocolumna.com/#physician"' .next/server/app/agendar.html` devuelve 3 o más, el mismo número que antes de esta tarea.
    - `grep -c 'hospitalAffiliation' src/components/structured-data.tsx` devuelve `1`: la propiedad se conservó tal cual.
    - `grep -c 'dangerouslySetInnerHTML' src/components/structured-data.tsx` devuelve `1`.
    - `npm run content:check` y `npm run sedes:check` terminan en 0.
  </acceptance_criteria>
  <done>Las cuatro sedes declaran su página de sede como url canónica en un único nodo por sede, con la web de la clínica en `sameAs`, y el grafo del doctor sigue intacto.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 3: la puerta cubre las cuatro sedes y el contraste de canal</name>
  <reversibility rating="reversible">Extensión del manifiesto de un script de verificación.</reversibility>
  <read_first>
    - `scripts/check-sedes.mjs` completo, en especial la forma de una entrada del `MANIFEST` y la comprobación de la regla de negocio.
    - `src/content/locations.ts`, los `channels` de las cuatro sedes: de ahí salen los literales que el manifiesto declara.
  </read_first>
  <files>scripts/check-sedes.mjs</files>
  <action>
`scripts/check-sedes.mjs`. Extender el `MANIFEST` de una a cuatro entradas, en el mismo orden que `locationPages`. Cada entrada declara, hardcodeados, el `slug`, el `kind`, el `h1` esperado carácter por carácter, la dirección esperada, el `@type` de schema esperado, la latitud y la longitud esperadas, y la lista de literales de canal que tienen que aparecer en el cuerpo. Se hardcodean a propósito: una puerta que deriva sus expectativas del mismo archivo que verifica no verifica nada.

Los valores por sede son los que están en `<interface_context>` de este plan.

La comprobación de la regla de negocio (D-09) pasa a ser bidireccional y explícita. Para `kind === "clinica"`, la cuenta del literal del chat del doctor dentro de la región recortada tiene que ser exactamente cero. Para `kind === "consultorio"`, tiene que ser uno o más. Las dos direcciones importan: una clínica que ofrece ese canal manda al paciente a agendar donde no se puede agendar, y un consultorio privado que **no** lo ofrece le esconde el único canal donde el doctor sí agenda. Hasta ahora solo la primera mitad estaba cubierta, porque no había ninguna sede de tipo consultorio en el manifiesto. El mensaje de falla nombra la sede y la consecuencia, no solo el número.

Agregar dos comprobaciones globales:
- El manifiesto tiene tantas entradas como sedes hay en `src/content/locations.ts`, contado sobre el texto del archivo. Si mañana entra Clínica Montefiori como quinta sede, la puerta falla hasta que alguien le escriba su página, en vez de aprobar en silencio una sede sin superficie propia.
- El sitemap prerenderizado declara exactamente 21 URLs (D-12), además de declarar el hub y las cuatro sedes que ya se verificaban.

Reejecutar la prueba de mutación del plan 01, ahora en las dos direcciones, y revertir en ambos casos: quitar temporalmente el canal del chat del cuerpo del consultorio privado y comprobar que la puerta falla; agregarlo temporalmente al cuerpo de Tezza y comprobar que la puerta falla. Sin las dos mutaciones, la comprobación bidireccional no está verificada, solo escrita.
  </action>
  <verify>
    <automated>npm run build && npm run sedes:check && npm run content:check && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run sedes:check` termina en 0 y su salida lista las cuatro rutas de sede en estado de pase.
    - `node -e "const s=require('fs').readFileSync('scripts/check-sedes.mjs','utf8');for(const g of ['consultorio-privado','clinica-ricardo-palma','sanna-la-molina','clinica-tezza']){if(!s.includes(g))throw new Error('el manifiesto no cubre '+g)}console.log('manifiesto completo')"` imprime `manifiesto completo`.
    - La puerta distingue por tipo de sede: `grep -c 'consultorio' scripts/check-sedes.mjs` devuelve 2 o más y `grep -c 'clinica' scripts/check-sedes.mjs` devuelve 2 o más.
    - **Prueba de mutación en las dos direcciones, ejecutada y revertida dentro de esta tarea.** (1) Quitar el canal del chat del cuerpo del consultorio privado, `npm run build && npm run sedes:check` termina en 1 y el mensaje nombra `consultorio-privado`; revertir. (2) Agregar un enlace a ese canal dentro del cuerpo de `/sedes/clinica-tezza`, `npm run build && npm run sedes:check` termina en 1 y el mensaje nombra `clinica-tezza`; revertir. Después de revertir las dos, `npm run build && npm run sedes:check` termina en 0.
    - La puerta cuenta el sitemap: `grep -c '21' scripts/check-sedes.mjs` devuelve 1 o más y, tras bajar temporalmente una entrada del sitemap, `npm run build && npm run sedes:check` termina en 1; revertir.
    - La puerta detecta una sede sin página: `grep -c 'locations.ts' scripts/check-sedes.mjs` devuelve 1 o más.
    - `npm run content:check`, `npx tsc --noEmit` y `npm run lint` terminan en 0.
    - No se instaló ningún paquete: la cuenta de `dependencies` más `devDependencies` de `package.json` es la misma que al inicio de la fase.
  </acceptance_criteria>
  <done>`npm run sedes:check` cubre las cuatro sedes, exige el canal del chat en el consultorio privado y lo prohíbe en las tres clínicas, y las dos mutaciones deliberadas la hacen fallar con mensajes que nombran la sede.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Las mismas del plan 01, sin superficie nueva: datos comprometidos en el repositorio hacia HTML renderizado, datos hacia JSON-LD inyectado como HTML crudo, sitio hacia destinos externos, y paciente hacia canal de agenda. Este plan no agrega entradas de usuario, ni endpoints, ni variables de entorno, ni paquetes.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Spoofing | canal de agenda de las cuatro páginas de sede | high | mitigate | La comprobación de la puerta pasa a ser bidireccional y cubre las cuatro sedes, con prueba de mutación en los dos sentidos (tarea 3, D-09). Es el riesgo central de la fase: un canal incorrecto envía al paciente a un sistema que no puede reservarle |
| T-09-06 | Tampering | `sameAs` y `hasMap` con dominios de terceros en el JSON-LD | low | mitigate | Los tres dominios salen de `locations.ts`, que está verificado contra la ficha oficial de cada clínica y solo cambia por pull request. `JsonLdScript` escapa el signo de menor que al serializar; no se agrega ningún punto de inyección |
| T-09-07 | Tampering | enlaces `tel:` de Sanna y Tezza renderizados desde datos | low | mitigate | Los `href` se toman literales de `locations.ts` y no se componen con interpolación en tiempo de render. La puerta verifica los literales exactos por sede (tarea 3) |
| T-09-08 | Repudiation | contenido editorial nuevo de tres sedes | medium | mitigate | La redacción no puede afirmar credenciales fuera de `cv.ts`, cifras de cirugías, tasas de éxito ni experiencia en primera persona, mismas salvaguardas que gobiernan la fase 8. `gettingThere` se limita a lo sostenible desde la dirección y el distrito, sin tiempos de viaje ni indicaciones inventadas |
| T-09-SC | Tampering | cadena de suministro | low | accept | La fase no instala paquetes. Criterio verificable en la tarea 3: la cuenta de dependencias no cambia |
</threat_model>

<verification>
```bash
npx tsc --noEmit
npm run lint
npm run build
npm run content:check
npm run sedes:check
grep -c '<loc>' .next/server/app/sitemap.xml.body    # 21
for s in consultorio-privado clinica-ricardo-palma sanna-la-molina clinica-tezza; do
  test -f ".next/server/app/sedes/$s.html" || echo "falta $s"
done
```
</verification>

<success_criteria>
- Las cuatro sedes tienen URL propia con dirección, días, horarios y su canal de agenda específico (SEDE-01, D-01, D-06).
- El consultorio privado es la única sede que ofrece el chat del doctor; las tres clínicas muestran su canal oficial y ninguna otra vía, verificado por región y con prueba de mutación en ambos sentidos (D-09).
- Cada página emite su JSON-LD de ubicación con el `@type` que fija D-07, su `address`, su `geo`, su `openingHoursSpecification` y su página de sede como `url` (SEDE-03).
- `/agendar` enlaza a las cuatro y cada una vuelve a su ancla (SEDE-03, D-11).
- El sitemap declara 21 URLs (D-12) y la puerta de contenido de la fase 8 sigue sin cubrir rutas de sede (D-05).
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 02)

Archivos modificados:
- `src/content/location-pages.ts`: tres entradas nuevas: `consultorio-privado`, `sanna-la-molina`, `clinica-tezza`
- `src/components/structured-data.tsx`: `locationNode` apunta `url` a `/sedes/{slug}` y mueve la web de la clínica a `sameAs`
- `scripts/check-sedes.mjs`: manifiesto de cuatro sedes, regla de canal bidireccional, cuenta del sitemap
- `scripts/check-content.mjs`: `SITEMAP_TOTAL` 18 → 21

Rutas nuevas: `/sedes/consultorio-privado`, `/sedes/sanna-la-molina`, `/sedes/clinica-tezza`

Sin componentes ni tipos nuevos: la arquitectura la fijó el plan 01.
</artifacts>

<output>
Crear `.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-02-SUMMARY.md` al terminar.
</output>
</content>
