---
phase: 09-p-ginas-por-sede-y-cobertura-local
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/location-pages.ts
  - src/app/sedes/[slug]/page.tsx
  - src/app/sedes/page.tsx
  - src/components/locations/sede-card.tsx
  - src/components/structured-data.tsx
  - src/app/sitemap.ts
  - src/app/agendar/page.tsx
  - src/components/locations/location-card.tsx
  - src/app/globals.css
  - scripts/check-sedes.mjs
  - scripts/check-content.mjs
  - package.json
autonomous: true
requirements: [SEDE-01, SEDE-02, SEDE-03]
user_setup: []

must_haves:
  truths:
    - "Un paciente abre https://drangulocolumna.com/sedes/clinica-ricardo-palma y lee la dirección completa, cómo llegar con referencias del barrio, los días y horarios de esa sede, y cómo se agenda ahí (D-01, D-06, SEDE-01)."
    - "El nombre Clínica Ricardo Palma aparece en el title, en el único h1 y en el cuerpo de esa página, y ninguna otra ruta del sitio reclama esa consulta (D-04, SEDE-02)."
    - "La página de Ricardo Palma muestra la central de citas y la web de la clínica como únicos canales, y no ofrece ningún camino hacia el chat del doctor dentro del cuerpo de la página (D-09, regla de negocio de locations.ts)."
    - "El HTML prerenderizado de esa ruta emite un JSON-LD propio de la sede con su address, su geo y su openingHoursSpecification, además del grafo raíz del sitio (D-07, D-08, SEDE-03)."
    - "Desde /agendar el paciente llega a la página de la sede con un clic, y desde la página de la sede vuelve a /agendar con la tarjeta de esa misma sede resaltada, sin JavaScript de cliente (D-10, D-11, SEDE-03)."
    - "El hub /sedes existe, indexa las páginas de sede publicadas y no reemplaza a /agendar, que sigue siendo el flujo de conversión (D-02)."
    - "El sitemap declara /sedes y /sedes/clinica-ricardo-palma, y la puerta de contenido de la fase 8 sigue en verde sin haber ganado ninguna ruta nueva bajo su manifiesto (D-05, D-12)."
    - "npm run build, npm run lint, npx tsc --noEmit, npm run content:check y npm run sedes:check terminan en 0."
  artifacts:
    - path: "src/content/location-pages.ts"
      provides: "capa editorial por sede: LocationPage, locationPages, getLocationPage y el join con locations.ts"
      contains: "getLocationPage"
    - path: "src/app/sedes/[slug]/page.tsx"
      provides: "plantilla única de las páginas de sede con generateStaticParams y generateMetadata"
      contains: "generateStaticParams"
    - path: "src/app/sedes/page.tsx"
      provides: "hub índice de sedes"
      contains: "SedeCard"
    - path: "src/components/locations/sede-card.tsx"
      provides: "tarjeta navegacional del hub hacia una página de sede, con enlace extendido"
      contains: "after:absolute"
    - path: "scripts/check-sedes.mjs"
      provides: "puerta automática de las páginas de sede: canal de agenda, encabezados, schema y enlazado bidireccional"
      contains: "SEDE_BODY"
    - path: "src/app/globals.css"
      provides: "regla :target que resalta la tarjeta de sede al llegar por ancla, sin JavaScript"
      contains: "data-location-card"
  key_links:
    - from: "src/app/sedes/[slug]/page.tsx"
      to: "src/content/location-pages.ts"
      via: "generateStaticParams recorre locationPages; una sede sin entrada editorial no genera ruta y no entra al sitemap"
      pattern: "locationPages"
    - from: "src/content/location-pages.ts"
      to: "src/content/locations.ts"
      via: "getLocationPage devuelve el par {page, location}; los datos de NAP nunca se duplican en la capa editorial"
      pattern: "from \"@/content/locations\""
    - from: "src/components/structured-data.tsx"
      to: "src/app/sedes/[slug]/page.tsx"
      via: "SedeJsonLd reusa locationNode y JsonLdScript; el nodo de la sede conserva el mismo @id que ya emite el grafo raíz"
      pattern: "SedeJsonLd"
    - from: "src/app/agendar/page.tsx"
      to: "src/app/sedes/[slug]/page.tsx"
      via: "LocationCard enlaza a /sedes/{slug} cuando esa sede tiene página publicada"
      pattern: "/sedes/"
    - from: "src/app/sedes/[slug]/page.tsx"
      to: "src/app/agendar/page.tsx"
      via: "cada página de sede vuelve a /agendar#{slug} y la regla :target de globals.css resalta esa tarjeta"
      pattern: "/agendar#"
    - from: "src/app/sitemap.ts"
      to: "src/content/location-pages.ts"
      via: "deriva una entrada por cada locationPage más la del hub, igual que ya hace con servicePages"
      pattern: "locationPages"
---

<decision_ids>
`09-CONTEXT.md` recoge las decisiones en prosa, sin identificadores. Esta tabla les pone ID para poder citarlas y auditarlas; el texto de referencia sigue siendo el de `09-CONTEXT.md`. Los tres planes de la fase citan estos mismos IDs.

| ID | Decisión bloqueada | Origen en 09-CONTEXT.md |
|----|--------------------|--------------------------|
| D-01 | URLs bajo `/sedes/`, reusando los cuatro slugs de `locations.ts` sin renombrar | Rutas y estructura |
| D-02 | `/sedes` existe como hub índice; `/agendar` se conserva como flujo de conversión y enlaza al hub | Rutas y estructura |
| D-03 | Una sola ruta dinámica `[slug]` alimentada por datos, mismo patrón que `/servicios/[slug]`; cero duplicación de datos | Rutas y estructura |
| D-04 | Title de Ricardo Palma: "Traumatólogo y cirujano de columna en Clínica Ricardo Palma"; el nombre de la clínica en title, `h1` y cuerpo | Rutas y estructura |
| D-05 | Extensión media, sin mínimo de palabras; la puerta de contenido de la fase 8 no aplica a `/sedes/*` | Contenido por sede |
| D-06 | Cada página lleva dirección completa, cómo llegar con referencias del barrio, días y horarios, canal de agenda de esa sede, y qué condiciones se atienden ahí con enlace a las cuatro páginas de servicio | Contenido por sede |
| D-07 | `MedicalClinic` para las tres clínicas y `MedicalBusiness` para el consultorio privado, cada uno con su `address`, `geo` y `openingHoursSpecification` derivados de `locations.ts` | Schema |
| D-08 | Extender `structured-data.tsx`, nunca duplicarlo; el schema por sede se agrega sin romper el `Physician` existente | Schema |
| D-09 | El WhatsApp del doctor agenda solo el consultorio privado; las tres clínicas muestran su canal oficial, nunca WhatsApp | Conversión y la regla del WhatsApp |
| D-10 | La vuelta a `/agendar` se resuelve con ancla por slug y la tarjeta destino se resalta al llegar, sin estado, sin query params y sin JavaScript de cliente | Conversión y la regla del WhatsApp |
| D-11 | `/agendar` enlaza a cada página de sede y cada página de sede vuelve a `/agendar` con su ancla | Conversión y la regla del WhatsApp |
| D-12 | El sitemap pasa de 16 a 21 URLs: cuatro sedes más el hub | Integration Points |
| D-13 | Decidir el destino del enlace "Sedes" del header, que hoy apunta a `/agendar` | Integration Points |

Decisiones tomadas bajo "Claude's Discretion" de `09-CONTEXT.md`, documentadas acá para que no se relitiguen en ejecución:

| ID | Decisión | Motivo |
|----|----------|--------|
| DC-01 | No se extrae un componente compartido entre `LocationCard` y la tarjeta del hub. El hub estrena `SedeCard`, modelada sobre `ServiceCard` de la fase 8 | Son dos trabajos distintos: `LocationCard` es una fila de agenda con canales; `SedeCard` es una tarjeta navegacional con enlace extendido. Unificarlas produciría un componente con props condicionales para dos usos que solo comparten el nombre de la sede |
| DC-02 | El resaltado por ancla se implementa con una regla `:target` en `globals.css` sobre un atributo de datos, no con la variante `target:` de Tailwind | Precedente documentado en el propio `globals.css`: el acordeón de preguntas frecuentes tuvo que bajar a CSS plano porque el compilador de Tailwind no conservaba la variante. Una regla plana no depende de qué variantes sobreviven a la compilación y es verificable con un grep sobre la hoja fuente |
| DC-03 | El enlace "Sedes" del header pasa a `/sedes`, sin megamenú de escritorio; el menú móvil replica el patrón anidado que ya usa Servicios (D-13) | Se ejecuta en el plan 03, cuando las cuatro rutas existen |
</decision_ids>

<objective>
Dejar viva de punta a punta la primera URL de sede: `/sedes/clinica-ricardo-palma`, con capa editorial, plantilla dinámica, schema propio, entrada de sitemap, hub, enlazado en ambos sentidos con `/agendar` y la puerta automática que va a gobernar el resto de la fase.

Este plan es el trazador. No entrega las otras tres sedes a propósito: prueba la arquitectura completa sobre un solo camino antes de expandir. Si el modelo de datos, el reparto entre `locations.ts` y la capa editorial, el tipo de schema o el resaltado por ancla están mal pensados, se descubre acá y no después de haber publicado cuatro páginas.

Se elige Ricardo Palma como camino del trazador porque es la sede prioritaria de la fase: la related search "traumatologo especialista en columna clínica ricardo palma" está verificada en la SERP del 2026-08-10 y hoy no hay ninguna página del sitio que la responda (SEDE-02).

Purpose: convertir una página genérica que compite consigo misma en superficies indexables por sede, empezando por la que tiene demanda verificada.

Output: `/sedes/clinica-ricardo-palma` y `/sedes` publicadas, schema por sede emitiéndose, sitemap en 18 URLs y `npm run sedes:check` en verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@AGENTS.md

@src/content/locations.ts
@src/app/servicios/[slug]/page.tsx
@src/app/servicios/page.tsx
@src/app/agendar/page.tsx
@src/components/locations/location-card.tsx
@src/components/services/service-card.tsx
@src/components/structured-data.tsx
@src/app/sitemap.ts
@src/app/globals.css
@scripts/check-content.mjs
</context>

<interface_context>
Firmas y valores que las tareas de este plan consumen y que ya existen en el repositorio. No hay que redescubrirlos.

**`src/content/locations.ts`** (fuente de verdad de NAP, no se modifica en esta fase)
```
type BookingChannel = { label: string; href?: string; detail?: string }
type SchemaDay = "Monday" | ... | "Sunday"
type OpeningHours = { days: SchemaDay[]; opens?: string; closes?: string }
type Location = {
  slug, name, kind: "consultorio" | "clinica",
  streetAddress, building?, reference?,
  addressLocality, addressRegion, postalCode, addressCountry,
  geo: { latitude: number; longitude: number },
  mapsUrl, telephone, website?,
  schedule: { days: string; hours: string }[],
  openingHours: OpeningHours[],
  bookingSummary: string,
  channels: BookingChannel[],
}
export const locations: Location[]
export const weeklySchedule            // bloques ordenados de lunes a sábado
export const primaryLocation = locations[0]      // consultorio privado
export const clinicLocations                     // las tres clínicas
```

Datos exactos de la sede del trazador, para las aserciones:
```
slug             clinica-ricardo-palma
name             Clínica Ricardo Palma
kind             clinica
streetAddress    Av. Javier Prado Este 1066
addressLocality  San Isidro
geo              { latitude: -12.090602, longitude: -77.018276 }
telephone        +5112242224
website          https://www.crp.com.pe/
schedule         [{ days: "Lunes y miércoles", hours: "9:00 a. m. a 6:00 p. m." }]
openingHours     [{ days: ["Monday","Wednesday"], opens: "09:00", closes: "18:00" }]
channels[0]      { label: "Central de citas (01) 224 2224", href: "tel:+5112242224" }
channels[1]      { label: "Agendar en crp.com.pe", href: "https://www.crp.com.pe/agenda-tu-cita/", detail: "Buscar al Dr. Juan Carlos Angulo Totesaut en Traumatología" }
```

**`src/components/structured-data.tsx`** (se extiende, no se duplica)
```
const ID = { website, physician, location: (slug) => `${siteConfig.url}/#sede-${slug}`, procedure }
function abs(path: string): string
function JsonLdScript({ data, id }): JSX          // único punto con dangerouslySetInnerHTML del repositorio
function postalAddress(location: Location)
function openingHours(location: Location)
function locationNode(location: Location)         // hoy: MedicalClinic para consultorio, Hospital para clínica
async function SiteJsonLd()                        // grafo raíz, se emite en el layout en todas las páginas
export BreadcrumbJsonLd, FaqJsonLd, ServicesJsonLd, MedicalWebPageJsonLd,
       ProfilePageJsonLd, BookingPageJsonLd, ContactPageJsonLd, BlogJsonLd, BlogPostingJsonLd
```

**`src/content/service-pages.ts`**
```
export const servicePages: ServicePage[]     // slug + navLabel + cardSummary, entre otros
slugs: hernia-discal, estenosis-espinal, escoliosis, ortopedia-infantil
navLabels: Hernia discal, Estenosis espinal, Escoliosis, Ortopedia infantil
```

**`src/lib/site-config.ts`**
```
siteConfig.url        https://drangulocolumna.com
siteConfig.name       Dr. Juan Carlos Angulo Totesaut
siteConfig.shortName  Dr. Juan Angulo
export type CtaLocation = keyof typeof whatsappMessages
// claves existentes: header, hero, floating_button, services, footer,
// contact_page, booking_page, service_page, blog_post
// `booking_page` ya dice "consultorio privado (Av. El Derby 254, Surco)":
// es la clave correcta para la página del consultorio. NO se agrega ninguna clave nueva.
```

**`src/components/ui/whatsapp-cta.tsx`**: `<WhatsAppCta location={CtaLocation} variant="accent"|"outline"|"outline-inverse">`, es `"use client"`.
**`src/components/ui/booking-cta.tsx`**: `<BookingCta size?="sm"|"md">`, siempre enlaza a `/agendar`.

**Forma real del HTML prerenderizado** (verificada contra el build actual, sirve para escribir las aserciones)
```
.next/server/app/sedes/{slug}.html          página de sede
.next/server/app/sedes.html                 hub
.next/server/app/agendar.html               flujo de conversión
.next/server/app/sitemap.xml.body           sitemap, una línea <loc> por URL
<link rel="canonical" href="https://drangulocolumna.com/servicios/hernia-discal"/>
<script id="servicio-jsonld" type="application/ld+json">
<title>Hernia discal: ... | Dr. Juan Angulo</title>       // plantilla `%s | Dr. Juan Angulo` del layout raíz
data-content-body=""                                      // marcador de región de la fase 8
```

**Presupuesto base de enlaces al chat del doctor.** El footer y el botón flotante del layout raíz emiten uno cada uno en **todas** las páginas del sitio, sin excepción. Una cuenta a documento completo nunca puede dar cero. Por eso la puerta de la regla de negocio se mide dentro de una región delimitada, igual que hace `data-content-body` en la fase 8.
</interface_context>

<tasks>

<task type="tracer" tdd="false">
  <name>Tarea 1: /sedes/clinica-ricardo-palma viva de punta a punta</name>
  <reversibility rating="costly">El slug de la URL se vuelve un enlace indexado en cuanto sale el deploy; cambiarlo después obliga a redirecciones permanentes. La decisión ya está tomada en 09-CONTEXT.md (D-01) y reusa un slug que existe desde v1.0, así que se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `src/content/locations.ts` completo, en especial el comentario de cabecera con la regla de negocio.
    - `src/app/servicios/[slug]/page.tsx`: es el análogo directo. Copiar de ahí el patrón de `generateStaticParams`, `generateMetadata` con `alternates.canonical`, `notFound()` y la banda de cabecera sobre el cuerpo.
    - `src/content/service-pages.ts` líneas 60 a 100: forma del tipo `ServicePage` y de `getServicePage`.
    - `src/app/sitemap.ts` completo.
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, secciones COLOR y A11Y.
    - `node_modules/next/dist/docs/` antes de usar cualquier API de metadata o de rutas dinámicas que no domines con certeza. AGENTS.md advierte que esta versión de Next diverge de lo que sabes de memoria y esa advertencia ya cazó dos errores reales en este proyecto.
  </read_first>
  <files>src/content/location-pages.ts, src/app/sedes/[slug]/page.tsx, src/components/structured-data.tsx, src/app/sitemap.ts, scripts/check-content.mjs</files>
  <action>
`src/content/location-pages.ts`, archivo nuevo. Es la capa editorial, separada a propósito de `locations.ts`, que se queda como fuente de verdad de NAP y no se toca en toda la fase (D-03). Mismo reparto que ya existe entre `services.ts` y `service-pages.ts`.

Declarar y exportar `type LocationPage` con: `slug: string`, `navLabel: string`, `title: string`, `description: string`, `h1: string`, `heroLead: string`, `cardSummary: string`, `gettingThere: string[]` y `conditionsLead: string`. Nada más: la dirección, el geo, el teléfono, la web, el horario y los canales se leen de `locations.ts` en tiempo de render y no se copian acá.

Exportar `locationPages: LocationPage[]` con **una sola entrada por ahora**, la de `clinica-ricardo-palma`. Exportar `getLocationPage(slug: string): { page: LocationPage; location: Location } | undefined`, que busca la entrada editorial, busca la `Location` del mismo slug en `locations` y devuelve las dos juntas, o `undefined` si falta cualquiera de las dos. Ese join en un solo lugar es lo que impide que una página quede sin sus datos de NAP o al revés.

Contenido de la entrada de Ricardo Palma, valores exactos que fija D-04:
- `slug`: `clinica-ricardo-palma`
- `navLabel`: `Clínica Ricardo Palma`
- `title`: `Traumatólogo y cirujano de columna en Clínica Ricardo Palma`
- `h1`: el mismo texto que el title. Es la consulta verificada de la SERP y el paciente tiene que reconocerla al llegar (SEDE-02).
- `description`: una frase que nombre la clínica, el distrito San Isidro, los días de atención y quién saca la cita. Entre 140 y 155 caracteres.
- `heroLead`: dos oraciones. Qué atiende el doctor en esa sede y en qué días, sin repetir el `h1` palabra por palabra.
- `cardSummary`: una línea para la tarjeta del hub, del estilo de las de `service-pages.ts`.
- `gettingThere`: dos o tres párrafos con referencias reales del barrio para llegar a Av. Javier Prado Este 1066, San Isidro. Referencias de ubicación urbana, nada de instrucciones de tránsito inventadas ni tiempos de viaje. Si un dato no se puede afirmar con certeza desde la dirección y el distrito, no se escribe.
- `conditionsLead`: un párrafo que introduzca qué condiciones de columna y traumatología se evalúan en esa sede, antes de la lista de enlaces a las cuatro guías de servicio.

Registro de redacción: segunda persona, español neutro, mismo tono que `src/content/blog.ts` y `src/content/services.ts`. Cero signos de admiración, cero emojis, cero guiones largos ni medios. Extensión media, sin mínimo de palabras (D-05): esto es intención local, no una guía clínica. Prohibido afirmar credenciales que no estén en `src/content/cv.ts`, cantidad de cirugías, tasas de éxito, precios y cualquier construcción en primera persona sobre casos concretos, exactamente igual que en la fase 8.

`src/app/sedes/[slug]/page.tsx`, archivo nuevo. Plantilla única (D-03). `generateStaticParams` recorre `locationPages`, no `locations`: una sede sin entrada editorial no genera ruta, que es lo que permite que este plan publique una sola sede sin dejar tres rutas a medio construir. `generateMetadata` devuelve `title`, `description` y `alternates.canonical` en `/sedes/{slug}`. El componente hace `const entry = getLocationPage(slug); if (!entry) notFound();`.

Estructura, replicando el lenguaje visual de la fase 8 y de v1.0 y nada más:
1. Todo el contenido de la página va envuelto en un `<div data-sede-body="">`. Es el límite que mide la puerta de la tarea 3, igual que `data-content-body` en la fase 8. Envuelve la banda de cabecera y el artículo como una sola unidad; si lo envolviera solo al artículo, el `h1` de la banda quedaría fuera de lo medido.
2. Banda de cabecera con `border-b border-border bg-muted`: enlace de vuelta a `/sedes` con `ArrowLeft` de Lucide, luego el `h1`, luego el `heroLead`.
3. `h2` "Dónde queda y cómo llegar": `streetAddress`, `building` si existe, `addressLocality`, `reference` si existe, enlace a `mapsUrl` con `target="_blank" rel="noopener noreferrer"` más un `<span className="sr-only">` que anuncie la pestaña nueva (A11Y-29), y debajo los párrafos de `gettingThere`.
4. `h2` "Días y horarios": los bloques de `location.schedule`.
5. `h2` "Cómo se agenda en esta sede": `location.bookingSummary`, la lista de `location.channels` con el mismo tratamiento de enlaces que ya usa `LocationCard`, y un aviso explícito de quién gestiona la agenda de esta sede. Para una sede de tipo `clinica`, el aviso dice que la cita la maneja la clínica con su propio sistema y que el doctor no puede reservarla por el paciente (D-09). Esta sección **no** ofrece ningún camino hacia el chat del doctor cuando la sede es una clínica: ni botón, ni enlace, ni mención del número. Ese es el punto entero de la regla de negocio que documenta `locations.ts`, y mandar al paciente a un canal que no puede agendarlo es un error de corrección, no de estilo. El literal exacto que la puerta cuenta está en `<acceptance_criteria>`.
6. `h2` "Qué se atiende en esta sede": `conditionsLead` y una lista con las cuatro guías de la fase 8, recorriendo `servicePages` e importando desde `@/content/service-pages`. Cada entrada es un `Link` a `/servicios/{slug}` con el `navLabel` textual, `block min-h-11 py-2.5` como área táctil (A11Y-10) y el color de marca en su variante oscura por COLOR-01.
7. `h2` de cierre "Agenda en esta sede": un `Link` a `/agendar#{slug}` que devuelve al flujo de conversión con esa sede resaltada (D-11), y para una sede de tipo `clinica`, la repetición del canal oficial de la clínica.

Reglas visuales que no se negocian, heredadas de `08-UI-SPEC.md`: nada de barras de acento al borde izquierdo, retiradas en el commit `c60f0c8` por ser un patrón reconocible de interfaz generada por IA (el literal exacto está en `<acceptance_criteria>`); el dorado es superficie de botón, nunca texto; el color de marca en texto por debajo de 24px va en la variante oscura del token y el token claro solo se usa en `h1`, `h2` y `h3` (COLOR-01, COLOR-02); `outline-offset: 2px` en los anillos de foco, que ya lo cubre la regla global de `globals.css` y está prohibido anularla; exactamente un `h1` por página y cero saltos de nivel de encabezado (A11Y-01, A11Y-04).

`src/components/structured-data.tsx`. Extender, nunca duplicar (D-08). Reusar `ID`, `abs`, `postalAddress`, `openingHours` y `JsonLdScript`, que es el único punto del repositorio que inyecta HTML crudo y ya serializa con `JSON.stringify` escapando el signo de menor que: todo schema nuevo pasa por ahí y no se agrega ningún otro punto de inyección.

Cambiar el mapeo de tipos de `locationNode` por D-07: una sede de tipo `consultorio` pasa a emitir `MedicalBusiness` y una de tipo `clinica` pasa a emitir `MedicalClinic`. Lo demás del nodo se conserva tal cual, incluido `hospitalAffiliation` en `physicianNode`, que referencia las clínicas por `@id` y sigue resolviendo contra el mismo grafo. Dejar un comentario corto en `locationNode` explicando de dónde sale el mapeo, para que nadie lo "arregle" de vuelta más adelante.

Exportar `SedeJsonLd({ page, location })`, que emite un `JsonLdScript` con `id="sede-jsonld"` y un `@graph` de dos nodos: un `MedicalWebPage` con `@id` en `{url}#page`, `url`, `name` desde `page.title`, `description`, `inLanguage: "es-PE"`, `isPartOf` al `@id` del sitio, `about` y `publisher` al `@id` del doctor y `mainEntity` al `@id` de la sede; y el nodo de la sede producido por la **misma** función `locationNode(location)` que usa el grafo raíz. Mismo `@id`, misma forma, un solo camino de código que produce marcado de ubicación: es lo que garantiza que la página y el grafo raíz nunca digan cosas distintas de la misma sede. Renderizar `SedeJsonLd` desde la plantilla, junto con `BreadcrumbJsonLd` con los items Sedes y el nombre de la sede.

`src/app/sitemap.ts`. Agregar `locationRoutes`, derivado de `locationPages` igual que ya se derivan `serviceRoutes` de `servicePages`: `${siteConfig.url}/sedes/${page.slug}`, `changeFrequency: "monthly"`, `priority: 0.8`. La entrada del hub `/sedes` llega en la tarea 2, cuando el hub exista; declarar en el sitemap una URL sin página detrás sería proponerle a Google un 404.

`scripts/check-content.mjs`. Subir `SITEMAP_TOTAL` de 16 a 17 y nada más. **No** agregar ninguna ruta de `/sedes` al `MANIFEST` de ese script: la puerta de 900 palabras de la fase 8 no aplica a estas páginas (D-05) y extenderla acá contradiría la decisión. La única razón legítima de tocar este archivo en toda la fase es que su comprobación global cuenta las URLs del sitemap y esa cuenta cambia; dejarla desactualizada rompería la puerta de la fase 8 sin que haya nada roto de verdad. El comentario que acompaña a la constante se actualiza para decir a qué corresponde el número nuevo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm run content:check && node -e "const h=require('fs').readFileSync('.next/server/app/sedes/clinica-ricardo-palma.html','utf8');const m=h.match(/<script id=\"sede-jsonld\" type=\"application\/ld\+json\">([\s\S]*?)<\/script>/);if(!m)throw new Error('no se emitio sede-jsonld');const d=JSON.parse(m[1].replace(/\\\\u003c/g,'<'));const n=d['@graph'].find(x=>x['@type']==='MedicalClinic');if(!n)throw new Error('el grafo de la sede no trae MedicalClinic');if(n.address.streetAddress!=='Av. Javier Prado Este 1066')throw new Error('address incorrecta');if(!Array.isArray(n.openingHoursSpecification)||n.openingHoursSpecification.length===0)throw new Error('falta openingHoursSpecification');if(n.geo.latitude!==-12.090602)throw new Error('geo incorrecta');console.log('sede-jsonld ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina en 0.
    - `npm run lint` termina en 0.
    - `npm run build` termina en 0 y el listado de rutas incluye `/sedes/[slug]` con `clinica-ricardo-palma` prerenderizada.
    - `test -f .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0.
    - `grep -c '<h1' .next/server/app/sedes/clinica-ricardo-palma.html` devuelve exactamente `1`.
    - `grep -o '<title>[^<]*</title>' .next/server/app/sedes/clinica-ricardo-palma.html` devuelve `<title>Traumatólogo y cirujano de columna en Clínica Ricardo Palma | Dr. Juan Angulo</title>`.
    - `grep -c 'Clínica Ricardo Palma' .next/server/app/sedes/clinica-ricardo-palma.html` devuelve 3 o más: el nombre está en title, en `h1` y en el cuerpo (D-04, SEDE-02).
    - `grep -q 'rel="canonical" href="https://drangulocolumna.com/sedes/clinica-ricardo-palma"' .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0.
    - `grep -q 'Av. Javier Prado Este 1066' .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0.
    - `grep -q '9:00 a. m. a 6:00 p. m.' .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0.
    - `grep -q 'tel:+5112242224' .next/server/app/sedes/clinica-ricardo-palma.html` y `grep -q 'https://www.crp.com.pe/agenda-tu-cita/' .next/server/app/sedes/clinica-ricardo-palma.html` terminan en 0: el canal oficial de la clínica está presente (D-09).
    - `grep -q 'href="/agendar#clinica-ricardo-palma"' .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0 (D-11).
    - Las cuatro guías de servicio están enlazadas: `for s in hernia-discal estenosis-espinal escoliosis ortopedia-infantil; do grep -q "href=\"/servicios/$s\"" .next/server/app/sedes/clinica-ricardo-palma.html || exit 1; done` termina en 0 (D-06).
    - `grep -q 'data-sede-body=""' .next/server/app/sedes/clinica-ricardo-palma.html` termina en 0.
    - El comando `<automated>` de esta tarea imprime `sede-jsonld ok` (D-07, D-08, SEDE-03).
    - `grep -c '"@type":"MedicalBusiness"' .next/server/app/agendar.html` devuelve 1: el consultorio privado cambió de tipo en el grafo raíz (D-07).
    - `grep -c '"@type":"Hospital"' .next/server/app/agendar.html` devuelve `0`: ninguna sede sigue emitiéndose como hospital.
    - `grep -c '<loc>' .next/server/app/sitemap.xml.body` devuelve `17` y `grep -q '<loc>https://drangulocolumna.com/sedes/clinica-ricardo-palma</loc>' .next/server/app/sitemap.xml.body` termina en 0.
    - `npm run content:check` termina en 0 y su salida dice `Sin fallas en 8 ruta(s).`
    - <!-- planner-discipline-allow: /sedes --> La puerta de la fase 8 no ganó ninguna ruta de sede (D-05). Se comprueba sobre código, no sobre comentarios, porque el comentario de la constante sí nombra las rutas nuevas: `grep -vE '^\s*(//|/\*|\*)' scripts/check-content.mjs | grep -c '/sedes'` devuelve `0`.
    - `grep -n 'SITEMAP_TOTAL = 17' scripts/check-content.mjs | wc -l` devuelve `1`.
    - Ningún archivo nuevo o modificado de esta tarea introduce una barra lateral de acento: `grep -rn 'border-l-4' src/app/sedes src/content/location-pages.ts | wc -l` devuelve `0`.
    - COLOR-01 en la plantilla: `grep -nE 'text-primary([^-a-z]|$)' src/app/sedes/'[slug]'/page.tsx | grep -vE '<h(1|2|3)' | wc -l` devuelve `0`. El token de marca claro solo aparece en encabezados.
    - `grep -c 'dangerouslySetInnerHTML' src/components/structured-data.tsx` devuelve `1`: no se agregó ningún segundo punto de inyección de HTML crudo.
    - `grep -c 'locationNode' src/components/structured-data.tsx` devuelve 3 o más: `SedeJsonLd` reusa la misma función que el grafo raíz en vez de escribir un nodo paralelo (D-08).
  </acceptance_criteria>
  <done>`/sedes/clinica-ricardo-palma` responde con la dirección, el horario, el canal oficial de la clínica, las cuatro guías enlazadas y su propio JSON-LD de ubicación, el sitemap la declara y las tres puertas del repositorio siguen en verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 2: hub /sedes y enlazado en ambos sentidos con /agendar</name>
  <reversibility rating="reversible">El hub y el resaltado por ancla son superficie nueva y una regla de CSS; revertirlos es borrar dos archivos y una regla.</reversibility>
  <read_first>
    - `src/app/servicios/page.tsx` líneas 1 a 50: patrón del hub de la fase 8, la grilla de tarjetas arriba del resto.
    - `src/components/services/service-card.tsx` completo: el enlace extendido con `after:absolute`, el anillo de foco trasladado al `article` y por qué no puede haber un segundo control dentro (A11Y-21, A11Y-23).
    - `src/app/agendar/page.tsx` completo.
    - `src/components/locations/location-card.tsx` completo.
    - `src/app/globals.css`, en especial el bloque del acordeón de preguntas frecuentes con su comentario sobre variantes de Tailwind que no sobreviven al compilador, y la regla global de foco visible.
  </read_first>
  <files>src/app/sedes/page.tsx, src/components/locations/sede-card.tsx, src/app/agendar/page.tsx, src/components/locations/location-card.tsx, src/app/globals.css, src/app/sitemap.ts, scripts/check-content.mjs</files>
  <action>
`src/components/locations/sede-card.tsx`, archivo nuevo. Tarjeta navegacional del hub hacia una página de sede (DC-01). Modelada sobre `ServiceCard`: `<article>` con `position: relative`, `h3` con el `navLabel` dentro de un `Link` a `/sedes/{slug}` que se extiende con `after:absolute after:inset-0`, `cardSummary` debajo, y un pie de texto que **no** es un control. Un solo elemento interactivo por tarjeta: un segundo enlace quedaría tapado por la capa extendida y sería inalcanzable con el mouse (A11Y-21). El anillo de foco se dibuja sobre la tarjeta completa con `focus-within:outline` en el `article` y `focus-visible:outline-none` limitado al enlace extendido (A11Y-23). Sumar, tomado de la `Location` correspondiente, el distrito y los días de atención como texto plano dentro de la tarjeta, que es la información que decide a qué sede va el paciente. La tarjeta recibe `page` y `location` por props; no vuelve a hacer el join.

`src/app/sedes/page.tsx`, archivo nuevo. Hub índice (D-02). `metadata` con `title`, `description` y `alternates.canonical` en `/sedes`. Un `h1` que nombre las sedes del doctor en Lima, un párrafo de orientación, y la grilla de `SedeCard` recorriendo `locationPages` y resolviendo cada `Location` con `getLocationPage`. Al cierre, un bloque que deja claro que el hub es el mapa de sedes y que la agenda vive en `/agendar`, con un `BookingCta`. El hub no repite los canales de agenda: para eso está `/agendar` y para eso está cada página de sede. Emitir `BreadcrumbJsonLd` con el item Sedes, igual que hace `/servicios`.

`src/components/locations/location-card.tsx`. Dos cambios, ninguno destructivo. Primero: el `<article>` recibe `id={location.slug}`, `tabIndex={-1}`, `scroll-mt-28` y el atributo de datos `data-location-card=""`. El `id` es el destino del ancla (D-10); el `tabIndex={-1}` hace que el navegador mueva el foco a la tarjeta al llegar por el ancla, en vez de solo desplazar la vista y dejar el foco atrás, que es el mismo razonamiento de A11Y-13 aplicado a una tarjeta en vez de a un encabezado. Segundo: una prop opcional `pageHref?: string`; cuando llega, la tarjeta agrega al final un `Link` a esa ruta con el texto "Ver la sede" o equivalente y `min-h-11`, que es el enlace de ida hacia la página de sede (D-11). Esta tarjeta no usa enlace extendido, así que sumar un enlace normal no rompe A11Y-21.

`src/app/agendar/page.tsx`. Pasar `pageHref` a cada `LocationCard` cuando esa sede tiene página publicada, resolviéndolo con `getLocationPage(location.slug)`. Mientras solo Ricardo Palma tenga entrada editorial, solo esa tarjeta muestra el enlace, y no queda ningún enlace apuntando a una ruta que no existe. Agregar además, cerca del encabezado, un enlace al hub `/sedes` para que el flujo de conversión y el mapa de sedes se conozcan (D-02). El bloque que ya explica la regla del WhatsApp se conserva tal cual: es la pieza de v1.0 que previene la confusión más frecuente de los pacientes.

`src/app/globals.css`. Agregar una regla `:target` sobre el atributo de datos de la tarjeta (DC-02), junto a las otras reglas de CSS plano del archivo y con un comentario que explique por qué no va como variante de Tailwind. El resaltado usa `background: var(--muted)` más `outline: 2px solid var(--primary-dark)` con `outline-offset: 2px`, y `scroll-margin-top` suficiente para no quedar debajo del header sticky. Se elige `--primary-dark` y no `--primary` por COLOR-01, y `outline-offset: 2px` para no contradecir A11Y-17. El resaltado no depende solo del color: agrega un contorno, que es un cambio de forma. No hay animación, así que el bloque de `prefers-reduced-motion` que ya existe no necesita cambios. Está prohibido anular con `outline-none` la regla global de foco visible.

`src/app/sitemap.ts`. Agregar la entrada del hub `/sedes` a `staticRoutes`, ahora que la página existe, con `changeFrequency: "monthly"` y `priority: 0.8`.

`scripts/check-content.mjs`. Subir `SITEMAP_TOTAL` de 17 a 18. Mismo motivo y mismo alcance que en la tarea 1: es la única línea de ese archivo que cambia en toda la fase, y cambia porque la cuenta global del sitemap es real. Ninguna ruta de sede entra al `MANIFEST`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm run content:check && grep -q 'href="/sedes/clinica-ricardo-palma"' .next/server/app/agendar.html && grep -q 'id="clinica-ricardo-palma"' .next/server/app/agendar.html && grep -q 'href="/sedes"' .next/server/app/agendar.html && grep -q 'href="/sedes/clinica-ricardo-palma"' .next/server/app/sedes.html && grep -q ':target' src/app/globals.css && test "$(grep -c '<loc>' .next/server/app/sitemap.xml.body)" = "18" && echo "enlazado bidireccional ok"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint` y `npm run build` terminan en 0.
    - `test -f .next/server/app/sedes.html` termina en 0.
    - `grep -c '<h1' .next/server/app/sedes.html` devuelve exactamente `1`.
    - `grep -q 'href="/sedes/clinica-ricardo-palma"' .next/server/app/sedes.html` termina en 0: el hub indexa la sede publicada (D-02).
    - `grep -q 'href="/sedes/clinica-ricardo-palma"' .next/server/app/agendar.html` termina en 0: `/agendar` enlaza a la página de sede (D-11).
    - `grep -q 'href="/sedes"' .next/server/app/agendar.html` termina en 0: `/agendar` enlaza al hub (D-02).
    - `grep -q 'id="clinica-ricardo-palma"' .next/server/app/agendar.html` termina en 0: la tarjeta tiene el destino del ancla (D-10).
    - `grep -c 'data-location-card=""' .next/server/app/agendar.html` devuelve `4`: las cuatro tarjetas llevan el marcador, no solo la que ya tiene página.
    - `grep -c 'tabindex="-1"' .next/server/app/agendar.html` devuelve 4 o más: el foco se mueve a la tarjeta al llegar por el ancla.
    - `grep -c ':target' src/app/globals.css` devuelve 1 o más y `grep -q 'data-location-card' src/app/globals.css` termina en 0: el resaltado es CSS plano y no depende de JavaScript de cliente (D-10, DC-02).
    - `grep -c 'outline-none' src/app/globals.css` devuelve `0` y `grep -rc 'outline-none' src/components/locations/location-card.tsx` devuelve `0`: la regla global de foco visible no se anula (A11Y-14).
    - `grep -c 'use client' src/app/sedes/page.tsx src/components/locations/sede-card.tsx` devuelve `0` en ambos archivos: el hub y su tarjeta se renderizan en el servidor (A11Y-25).
    - `grep -c '<loc>' .next/server/app/sitemap.xml.body` devuelve `18` y `grep -q '<loc>https://drangulocolumna.com/sedes</loc>' .next/server/app/sitemap.xml.body` termina en 0.
    - `npm run content:check` termina en 0.
    - `grep -rn 'border-l-4' src/app/sedes src/components/locations | wc -l` devuelve `0`.
    - `grep -nE 'text-primary([^-a-z]|$)' src/components/locations/sede-card.tsx src/app/sedes/page.tsx | grep -vE '<h(1|2|3)' | wc -l` devuelve `0` (COLOR-01).
    - `grep -c 'after:absolute' src/components/locations/sede-card.tsx` devuelve 1 o más y `grep -c '<Link' src/components/locations/sede-card.tsx` devuelve exactamente `1`: un solo control por tarjeta (A11Y-21).
  </acceptance_criteria>
  <done>`/sedes` indexa la sede publicada, `/agendar` enlaza al hub y a la página de sede, la página de sede vuelve a su ancla en `/agendar`, y al llegar por esa ancla la tarjeta correspondiente queda resaltada y con el foco encima sin ejecutar JavaScript de cliente.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 3: puerta automática de las páginas de sede</name>
  <reversibility rating="reversible">Un script de verificación y una línea de `package.json`.</reversibility>
  <read_first>
    - `scripts/check-content.mjs` completo. De ahí salen, sin modificarlas, las utilidades `elementRange`, `stripElementsWith`, `toText`, `normalize`, `occurrences` y `headings`, la forma del `MANIFEST`, la forma de `checkRoute` y el estilo de la salida por consola. La puerta nueva es un archivo aparte porque mide criterios distintos, no una extensión de la de la fase 8 (D-05).
    - `src/content/locations.ts`, el comentario de cabecera: es la fuente de la regla que esta puerta hace ejecutable.
    - El apartado "Presupuesto base de enlaces al chat del doctor" de `<interface_context>` de este plan.
  </read_first>
  <files>scripts/check-sedes.mjs, package.json</files>
  <action>
`scripts/check-sedes.mjs`, archivo nuevo, sin dependencias, mismo estilo que `scripts/check-content.mjs`: lee el HTML que `npm run build` deja en `.next/server/app/`, falla si el archivo esperado no existe, imprime una línea por ruta y sale con código 1 si hay fallas. Una puerta que aprueba por ausencia de evidencia es peor que no tener puerta.

Copiar tal cual, sin cambiarlas, las utilidades `elementRange`, `occurrences`, `toText` y `headings` de `check-content.mjs`. Se duplican a propósito en vez de extraerse a un módulo compartido: las dos puertas tienen que poder correr sueltas y una refactorización compartida acoplaría el gate de la fase 8 al de la fase 9 sin ganancia.

Declarar la constante `SEDE_BODY` con el valor del atributo marcador que emite la plantilla, incluido el `=""`. Buscar el atributo con el `=""` pegado importa: el HTML de Next también trae el árbol serializado del payload RSC, donde el mismo nombre de atributo aparece con comillas escapadas, y sin el `=""` la puerta mediría la región equivocada.

Declarar un `MANIFEST` explícito, hardcodeado, con una entrada por sede publicada. Por ahora una sola: `clinica-ricardo-palma`. Cada entrada declara `slug`, `kind` (`consultorio` o `clinica`), el `h1` esperado carácter por carácter, la dirección esperada, el `@type` de schema esperado y la lista de literales de canal que tienen que estar presentes. Hardcodear es deliberado: una puerta que deriva sus expectativas del mismo archivo que verifica no verifica nada.

Comprobaciones por ruta, todas sobre `.next/server/app/sedes/{slug}.html`:
1. El archivo existe.
2. Recortar la región del cuerpo de la sede con `elementRange` sobre el marcador. Si el marcador no aparece, o el elemento no cierra, es falla.
3. **Regla de negocio (D-09), la comprobación central de esta puerta.** Dentro de la región recortada se cuentan las ocurrencias del literal del dominio del chat del doctor. Para una sede de tipo `clinica` el resultado tiene que ser exactamente `0`. Para el consultorio privado tiene que ser 1 o más. El literal exacto está en `<acceptance_criteria>`. El mensaje de falla tiene que explicar la consecuencia real, no solo el número: una clínica que ofrece ese canal manda al paciente a agendar donde no se puede agendar. El recorte por región es obligatorio porque el pie de página y el botón flotante del layout emiten ese literal en todas las páginas del sitio; una cuenta a documento completo daría un piso de dos y la puerta no significaría nada.
4. Cada literal de canal declarado en el manifiesto aparece dentro de la región.
5. Exactamente un `h1` en la región, y su texto coincide carácter por carácter con el esperado.
6. La secuencia de encabezados de la región no salta niveles y no hay ningún `h2` antes del `h1` (A11Y-04).
7. El `<title>` del documento empieza con el `h1` esperado y termina con el sufijo de la plantilla del layout.
8. El documento declara `rel="canonical"` hacia `/sedes/{slug}`.
9. La dirección esperada aparece en la región.
10. El script `sede-jsonld` existe; se parsea su JSON revirtiendo el escape del signo de menor que; su `@graph` contiene un nodo del `@type` esperado con `address.streetAddress` igual al esperado, `geo.latitude` y `geo.longitude` numéricos y `openingHoursSpecification` no vacío (D-07, SEDE-03).
11. La región enlaza a `/agendar#{slug}` (D-11).
12. La región enlaza a las cuatro guías de servicio de la fase 8 (D-06).

Comprobaciones globales, una sola vez:
- `.next/server/app/agendar.html` enlaza a `/sedes` y, por cada sede del manifiesto, a `/sedes/{slug}` y trae un elemento con `id="{slug}"` (D-11).
- `.next/server/app/sedes.html` existe, tiene exactamente un `h1` y enlaza a cada sede del manifiesto (D-02).
- `src/app/globals.css` declara una regla `:target` sobre el atributo de la tarjeta de sede (D-10).
- Cada `slug` del manifiesto existe tanto en `src/content/locations.ts` como en `src/content/location-pages.ts`, comprobado leyendo el texto de los dos archivos. Es lo que impide que una entrada editorial quede huérfana de datos de NAP.
- El sitemap prerenderizado declara `/sedes` y cada `/sedes/{slug}` del manifiesto.

`package.json`. Agregar el script `"sedes:check": "node scripts/check-sedes.mjs"`, junto a `content:check`. No se agrega ninguna dependencia: esta fase no instala paquetes.
  </action>
  <verify>
    <automated>npm run build && npm run sedes:check && npm run content:check && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run sedes:check` termina en 0 y su salida incluye la línea de `/sedes/clinica-ricardo-palma` en estado de pase.
    - La puerta detecta de verdad la violación de la regla de negocio. Prueba de mutación obligatoria, ejecutada y revertida dentro de esta tarea: agregar temporalmente un enlace al chat del doctor dentro del cuerpo de la página de Ricardo Palma, correr `npm run build && npm run sedes:check`, comprobar que termina en 1 y que el mensaje nombra la sede, y revertir el cambio. Sin esta prueba la puerta no está verificada, solo escrita.
    - <!-- planner-discipline-allow: wa.me --> El literal que la puerta cuenta es `wa.me`, y `grep -c 'wa\.me' scripts/check-sedes.mjs` devuelve 1 o más.
    - La medición es por región y no a documento completo: `grep -c 'elementRange' scripts/check-sedes.mjs` devuelve 2 o más y `grep -c 'SEDE_BODY' scripts/check-sedes.mjs` devuelve 2 o más.
    - `grep -c 'data-sede-body=\"\"' scripts/check-sedes.mjs` devuelve 1 o más: el marcador se busca con el `=""` pegado, para no medir el payload RSC.
    - `node -e "const s=require('./package.json').scripts; if(s['sedes:check']!=='node scripts/check-sedes.mjs') process.exit(1)"` termina en 0.
    - `node -e "const p=require('./package.json'); const b=Object.keys(p.dependencies||{}).length+Object.keys(p.devDependencies||{}).length; console.log(b)"` devuelve el mismo número que antes de esta tarea: no se instaló ningún paquete.
    - `npm run content:check`, `npx tsc --noEmit` y `npm run lint` terminan en 0.
    - `grep -c 'MANIFEST' scripts/check-sedes.mjs` devuelve 2 o más y el manifiesto tiene exactamente una entrada en este plan.
    - `grep -c 'process.exit(1)' scripts/check-sedes.mjs` devuelve 1 o más: la puerta falla el proceso, no solo imprime.
  </acceptance_criteria>
  <done>`npm run sedes:check` existe, pasa sobre la sede publicada, y una violación deliberada de la regla del canal de agenda la hace fallar con un mensaje que nombra la sede.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos comprometidos en el repositorio → HTML renderizado | `locations.ts` y `location-pages.ts` son contenido como código, revisado en el pull request. No hay entrada de usuario en esta fase |
| datos comprometidos → JSON-LD inyectado como HTML crudo | `JsonLdScript` es el único punto del repositorio con `dangerouslySetInnerHTML` y la sede nueva pasa por ahí |
| sitio → destinos externos | `mapsUrl`, `website` y los `href` de `channels`, incluidos esquemas `tel:`, salen de `locations.ts` hacia dominios de terceros |
| paciente → canal de agenda | La página le dice al paciente por dónde reservar. Un canal incorrecto lo manda a un sistema que no puede atenderlo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `JsonLdScript` en `src/components/structured-data.tsx` | medium | mitigate | `SedeJsonLd` reusa el `JsonLdScript` existente, que serializa con `JSON.stringify` y escapa el signo de menor que. Criterio verificable: `dangerouslySetInnerHTML` aparece exactamente una vez en el archivo después del cambio (tarea 1) |
| T-09-02 | Tampering | enlaces externos de `mapsUrl`, `website` y `channels` en la plantilla de sede | low | mitigate | Todo destino externo va con `target="_blank" rel="noopener noreferrer"` más el aviso `sr-only`, replicando el patrón ya vigente en `LocationCard` (A11Y-29). Los `tel:` no abren pestaña y no llevan `target` |
| T-09-03 | Spoofing | sección "Cómo se agenda en esta sede" de las páginas de clínica | high | mitigate | Puerta ejecutable con recorte por región en `scripts/check-sedes.mjs`: cero enlaces al chat del doctor dentro del cuerpo de una sede de tipo clínica, verificado además con una prueba de mutación (tarea 3, D-09) |
| T-09-04 | Information Disclosure | variables de entorno y datos del build | low | accept | La fase no agrega variables de entorno, no consume `GOOGLE_PLACES_API_KEY` en superficies nuevas y no expone nada que no esté ya publicado en `/agendar` desde v1.0 |
| T-09-05 | Denial of Service | rutas dinámicas `/sedes/[slug]` | low | accept | `generateStaticParams` prerenderiza el conjunto cerrado de sedes y cualquier otro slug cae en `notFound()`. No hay render bajo demanda ni entrada de usuario que amplifique costo |
| T-09-SC | Tampering | cadena de suministro | low | accept | La fase no instala ningún paquete de npm. Criterio verificable en la tarea 3: la cuenta de dependencias de `package.json` no cambia. Si en ejecución apareciera la necesidad de instalar algo, se detiene el plan y se corre el protocolo de legitimidad de paquetes antes de instalar |
</threat_model>

<verification>
Al cerrar el plan, con el repositorio limpio:

```bash
npx tsc --noEmit
npm run lint
npm run build
npm run content:check
npm run sedes:check
grep -c '<loc>' .next/server/app/sitemap.xml.body        # 18
grep -c '"@type":"Hospital"' .next/server/app/agendar.html   # 0
```

Los cinco primeros comandos terminan en 0, el sitemap trae 18 URLs y ninguna sede se emite ya como `Hospital`.
</verification>

<success_criteria>
- `/sedes/clinica-ricardo-palma` publica dirección, cómo llegar, días, horarios y el canal oficial de la clínica, con el nombre de la clínica en title, `h1` y cuerpo (SEDE-01, SEDE-02, D-01, D-04, D-06).
- Esa página emite su propio JSON-LD de ubicación con `address`, `geo` y `openingHoursSpecification`, reusando el mismo `@id` y la misma función de nodo que el grafo raíz (SEDE-03, D-07, D-08).
- El cuerpo de la página de la clínica no ofrece ningún camino hacia el chat del doctor, y la puerta lo verifica por región con una prueba de mutación (D-09).
- `/agendar` enlaza al hub y a la página de sede; la página de sede vuelve a `/agendar#clinica-ricardo-palma` y esa tarjeta queda resaltada y enfocada al llegar, sin JavaScript de cliente (SEDE-03, D-10, D-11).
- El sitemap declara `/sedes` y `/sedes/clinica-ricardo-palma`, y la puerta de contenido de la fase 8 sigue en verde sin cubrir ninguna ruta de sede (D-05, D-12).
- `npm run sedes:check` existe y pasa.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 01)

Archivos nuevos:
- `src/content/location-pages.ts`: tipo `LocationPage`, `locationPages`, `getLocationPage`
- `src/app/sedes/[slug]/page.tsx`: plantilla de página de sede
- `src/app/sedes/page.tsx`: hub índice
- `src/components/locations/sede-card.tsx`: componente `SedeCard`
- `scripts/check-sedes.mjs`: puerta automática de las páginas de sede

Archivos modificados:
- `src/components/structured-data.tsx`: nuevo export `SedeJsonLd`; `locationNode` cambia el mapeo de tipos a `MedicalBusiness` y `MedicalClinic`
- `src/app/sitemap.ts`: `locationRoutes` más la entrada del hub
- `src/app/agendar/page.tsx`: enlace al hub y `pageHref` por tarjeta
- `src/components/locations/location-card.tsx`: `id`, `tabIndex`, `data-location-card` y prop `pageHref`
- `src/app/globals.css`: regla `:target` del resaltado por ancla
- `scripts/check-content.mjs`: `SITEMAP_TOTAL` 16 → 18
- `package.json`: script `sedes:check`

Tipos y componentes exportados:
- `LocationPage`, `locationPages`, `getLocationPage`
- `SedeCard`
- `SedeJsonLd`

Rutas nuevas: `/sedes`, `/sedes/clinica-ricardo-palma`
</artifacts>

<output>
Crear `.planning/workstreams/milestone/phases/09-p-ginas-por-sede-y-cobertura-local/09-01-SUMMARY.md` al terminar.
</output>
</content>
</invoke>
