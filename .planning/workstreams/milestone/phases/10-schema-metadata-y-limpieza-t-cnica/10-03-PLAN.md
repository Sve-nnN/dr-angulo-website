---
phase: 10-schema-metadata-y-limpieza-t-cnica
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - scripts/check-seo.mjs
  - .planning/workstreams/milestone/REQUIREMENTS.md
autonomous: false
requirements: [SEO-08, SEO-09]
must_haves:
  truths:
    - "`npm run seo:check` mide el largo del title y de la description de cada ruta y falla si alguna pasa de 60 o de 155."
    - "La puerta falla de verdad: alargar a proposito un title publicado la pone en rojo."
    - "La puerta deriva las rutas del sitemap, asi que una pagina nueva entra sola a la medicion."
    - "REQUIREMENTS.md refleja SEO-08 cerrado y corrige la fila de trazabilidad de SEO-09, que decia Pending sobre trabajo ya entregado."
  artifacts:
    - scripts/check-seo.mjs
    - .planning/workstreams/milestone/REQUIREMENTS.md
  key_links:
    - "La puerta lee el HTML prerenderizado de `.next/server/app/`, igual que las otras tres: mide lo que se sirve, no lo que dice el codigo fuente."
    - "El title medido incluye el sufijo del template cuando la ruta lo hereda, que es exactamente lo que ve Google."
---

<objective>
Hacer que la cuarta puerta ejecutable mida el largo de los titles y de las descriptions, y dejar la
documentacion de requisitos al dia.

Purpose: SEO-08 pide que ningun title pase de 60 caracteres ni ninguna description de 155. Los
planes 10-01 y 10-02 dejan el sitio dentro del contrato; sin puerta, la primera pagina nueva o la
primera edicion apurada lo rompe sin que nadie se entere. La corrida previa de esta fase construyo
`scripts/check-seo.mjs` con seis familias de comprobaciones y dejo esta afuera a proposito, porque
en ese momento habria nacido roja en 17 rutas y porque Juan iba a reescribir los textos el mismo.
Las dos razones caducaron: el paquete de v1.2 entrego los textos y los planes de esta ola los
aplicaron.

Output: la puerta mide longitud, la documentacion deja de mentir y Juan revisa la tabla final antes
de que esto salga a produccion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/10-schema-metadata-y-limpieza-t-cnica/10-SUMMARY.md
@scripts/check-seo.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medicion de longitud de title y description en la cuarta puerta</name>
  <files>scripts/check-seo.mjs</files>
  <precondition>Los planes 10-01 y 10-02 estan commiteados y `npm run build` esta corrido: sin ellos esta puerta nace en rojo y no se puede distinguir una falla real de la deuda previa.</precondition>
  <read_first>
    - `scripts/check-seo.mjs` completo: el comentario de cabecera que enumera las familias que cubre y la que deja afuera, `routesFromSitemap`, `htmlFor`, `NOINDEX_ROUTES`, `checkOgImages` (de ahi sale como se lee un `<meta>` del HTML del build) y `main`, donde se encadenan las comprobaciones y se arma el resumen.
    - `scripts/check-content.mjs`, para respetar el estilo de mensaje de falla de las otras puertas: una linea por falla, con la ruta y el dato medido.
    - `10-SUMMARY.md`, punto 7: por que esta medicion quedo fuera en su momento.
  </read_first>
  <action>
    Agregar a `scripts/check-seo.mjs` una septima familia de comprobaciones, `checkMetadataLength`,
    que recorra las mismas rutas que ya recorre el resto (las del sitemap mas las de
    `NOINDEX_ROUTES`) y, sobre el HTML prerenderizado de cada una, mida:

    - El contenido de `<title>`: falla si falta, si esta vacio o si pasa de 60 caracteres.
    - El `content` de `<meta name="description">`: falla si falta, si esta vacio o si pasa de 155.

    Medir sobre el texto ya desescapado: el HTML trae `&amp;`, `&#x27;` y compania, y contarlos como
    entidad infla el numero. Con desescapar las cinco entidades basicas alcanza.

    Dos limites como constantes con nombre arriba del archivo, junto a `SITEMAP_TOTAL`, para que se
    lean de un vistazo. El mensaje de falla trae la ruta, el largo medido, el limite y el texto
    recortado, que es lo que permite arreglarlo sin abrir el build.

    Sumar una nota al resumen con cuantas rutas se midieron y cual es el title y la description mas
    largos con su ruta: sirve para ver cuanto margen queda antes de que la puerta se ponga en rojo.

    Enganchar la funcion en `main`, despues de `checkOgImages`.

    Actualizar el comentario de cabecera del archivo para que describa las siete familias que
    verifica hoy. El parrafo que explica la exclusion de esta medicion ya no describe lo que hace el
    archivo y tiene que salir; en su lugar, una linea que diga desde donde salen los textos (el
    paquete on-page de v1.2) y donde estan los limites.

    No tocar ninguna de las seis familias existentes, ni `SITEMAP_TOTAL`, ni la logica de rutas.
  </action>
  <verify>
    <automated>npm run build && npm run seo:check && node -e "const{readFileSync,writeFileSync}=require('fs');const f='.next/server/app/testimonios.html';const o=readFileSync(f,'utf8');const t=/<title>([^<]*)<\/title>/.exec(o)[1];writeFileSync(f,o.replace('<title>'+t+'</title>','<title>'+t+' '+'x'.repeat(70)+'</title>'));const{status}=require('child_process').spawnSync('node',['scripts/check-seo.mjs'],{encoding:'utf8'});writeFileSync(f,o);if(status===0){console.log('la puerta aprobo un title de mas de 60: no mide nada');process.exit(1)}console.log('la puerta falla cuando debe')"</automated>
  </verify>
  <done>`npm run seo:check` pasa sobre el build real, reporta cuantas rutas midio y cual es el title y la description mas largos, y se pone en rojo cuando se le mete a la fuerza un title de mas de 60 caracteres.</done>
</task>

<task type="auto">
  <name>Task 2: Poner al dia REQUIREMENTS.md</name>
  <files>.planning/workstreams/milestone/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/workstreams/milestone/REQUIREMENTS.md`: la casilla de SEO-08 y de SEO-09 en el listado de requisitos, y las filas de SEO-08 y SEO-09 en la tabla de trazabilidad del final.
    - `10-SUMMARY.md`, punto 5: la imagen de Open Graph propia por ruta, que es lo que cierra SEO-09 y que ya esta en produccion.
  </read_first>
  <action>
    Marcar la casilla de SEO-08 como cumplida y poner su fila de la tabla de trazabilidad en
    `Complete`, apuntando a la fase 10.

    Corregir la fila de trazabilidad de SEO-09: la casilla del listado ya esta marcada porque el
    trabajo se entrego en la corrida previa de esta fase, pero la fila quedo en `Pending`. Las dos
    vistas del mismo requisito tienen que decir lo mismo.

    Editar solo esas cuatro lineas. No reescribir el archivo ni reordenar nada.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/workstreams/milestone/REQUIREMENTS.md','utf8');const err=[];if(!/- \[x\] \*\*SEO-08\*\*/.test(s))err.push('SEO-08 sin marcar en el listado');for(const id of ['SEO-08','SEO-09']){const fila=new RegExp('^\\\\| '+id+' \\\\|.*$','m').exec(s)?.[0]??'';if(!fila)err.push(id+' sin fila de trazabilidad');else if(/Pending/.test(fila))err.push(id+' sigue en Pending: '+fila)}if(err.length){console.log(err.join('\n'));process.exit(1)}console.log('SEO-08 y SEO-09 coherentes entre listado y trazabilidad')"</automated>
  </verify>
  <done>SEO-08 figura cumplido y SEO-09 dice lo mismo en el listado y en la tabla de trazabilidad.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Juan revisa la tabla final de titles y metas antes de desplegar</name>
  <what-built>
    Los tres planes de esta fase aplicaron a las 22 rutas con metadata el title y la meta que
    entrego el paquete on-page de v1.2, y la cuarta puerta ejecutable pasa a medir que ninguno se
    pase de 60 y 155 caracteres.
  </what-built>
  <how-to-verify>
    1. `npm run build` y despues `npm run seo:check`. Tiene que terminar sin fallas e imprimir
       cuantas rutas midio y cual es el title y la description mas largos.
    2. Correr esto para ver la tabla completa de lo que se va a publicar, ruta por ruta:
       `node -e "const{readFileSync}=require('fs');const sm=readFileSync('.next/server/app/sitemap.xml.body','utf8');const o=/<loc>(https?:\/\/[^/<]+)/.exec(sm)[1];for(const m of sm.matchAll(/<loc>([^<]+)<\/loc>/g)){const r=m[1].slice(o.length)||'/';const h=readFileSync('.next/server/app'+(r==='/'?'/index':r)+'.html','utf8');const t=/<title>([^<]*)<\/title>/.exec(h)[1];const d=/<meta name=\"description\" content=\"([^\"]*)\"/.exec(h)[1];console.log(r+'\n  '+t.length+'  '+t+'\n  '+d.length+'  '+d+'\n')}"`
    3. Leer esa tabla. Es la pasada de titles que quedo pendiente desde el 2026-08-10: lo que salga
       ahi es lo que Google va a mostrar. Comparar contra la seccion 2 de
       `15-HANDOFF-V11-ONPAGE.md` si quieres verlo al lado de los conteos del paquete.
    4. `npm start` y abrir dos o tres rutas para confirmar que ningun H1 ni ningun cuerpo de pagina
       se movio: esta fase solo cambia lo que se ve en la pestaña del navegador y en el resultado
       de busqueda.
    5. Nada se despliega hasta que apruebes.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o dime que filas de la tabla quieres cambiar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build -> puerta ejecutable | La puerta cree lo que encuentra en `.next/server/app/`. Si el build esta viejo o incompleto, aprueba sobre evidencia equivocada. |
| documentacion -> decisiones futuras | REQUIREMENTS.md es lo que se lee para saber que falta; una fila desactualizada manda a alguien a rehacer trabajo hecho o a dar por cerrado algo abierto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Spoofing | puerta que aprueba sin medir nada | high | mitigate | La verificacion incluye una prueba de mutacion: alarga un title del build a mano y exige que la puerta salga en rojo. Una puerta que solo sabe decir que si es peor que no tener puerta. |
| T-10-09 | Tampering | build ausente o parcial al correr la puerta | medium | mitigate | `check-seo.mjs` ya sale con error cuando falta `.next/server/app`; la familia nueva reutiliza esa misma entrada y falla si a una ruta del sitemap le falta el HTML. |
| T-10-10 | Repudiation | publicar titles nuevos sin que Juan los haya visto | high | mitigate | Checkpoint bloqueante con la tabla completa de las 22 rutas antes de cualquier despliegue. |
| T-10-SC | Tampering | instalaciones de paquetes | low | accept | Este plan no instala ni actualiza ninguna dependencia. |
</threat_model>

<verification>
- `npm run build` limpio y `npm run seo:check` sin fallas sobre ese build.
- Prueba de mutacion: con un title de mas de 60 caracteres inyectado en el HTML del build, la puerta sale con codigo distinto de cero y nombra la ruta.
- `npm run content:check` y `npm run sedes:check` siguen sin fallas.
- `npm run lint` sin errores ni avisos.
- La tabla de las 22 rutas revisada por Juan en el checkpoint.
</verification>

<success_criteria>
La cuarta puerta ejecutable mide el largo de los titles y de las descriptions de todas las rutas del
sitio, se comprobo que falla cuando debe, REQUIREMENTS.md refleja SEO-08 cerrado y SEO-09 coherente,
y Juan aprobo la tabla final antes de desplegar.
</success_criteria>

<output>
Crear `.planning/workstreams/milestone/phases/10-schema-metadata-y-limpieza-t-cnica/10-03-SUMMARY.md` al terminar.
</output>
