---
phase: 10-schema-metadata-y-limpieza-t-cnica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/service-pages.ts
  - src/content/location-pages.ts
  - src/content/blog.ts
autonomous: true
requirements: [SEO-08]
must_haves:
  truths:
    - "Las 13 rutas dinamicas vivas (4 sedes, 5 guias de servicio y 4 posts del blog) emiten el title y la meta exactos del paquete v1.2."
    - "Ninguna de esas rutas emite un <title> de mas de 60 caracteres ni una meta description de mas de 155."
    - "Una URL que la fase 8 apago con un 301 no queda con title ni meta propios."
    - "Las tarjetas de Open Graph de las rutas dinamicas se regeneran solas con el title nuevo."
  artifacts:
    - src/content/service-pages.ts
    - src/content/location-pages.ts
    - src/content/blog.ts
  key_links:
    - "`generateMetadata` de cada [slug]/page.tsx lee `page.title` y `page.description` de estos tres modulos: cambiar el dato cambia la metadata y la imagen OG a la vez, sin tocar ningun page.tsx."
    - "`opengraph-image.tsx` de las rutas dinamicas lee el mismo campo `title`, asi que no hay una segunda copia del texto."
---

<objective>
Aplicar el title y la meta description del paquete on-page de v1.2 a las rutas cuyo texto de
metadata vive en `src/content/`: las 4 sedes, las guias de servicio y los posts del blog.

Purpose: cierra la mitad restante de SEO-08. Los campos `title` y `description` de
`service-pages.ts`, `location-pages.ts` y `blog.ts` son la fuente unica que leen tanto
`generateMetadata` como `opengraph-image.tsx`, asi que un solo cambio por entrada mueve las dos
superficies.

Output: cada entrada de contenido con el par title/meta del paquete, dentro del contrato 60/155.

**Este plan depende de la fase 8 y el orden no se puede invertir.** La fase 8 crea
`/servicios/cirugia-minimamente-invasiva`, `/blog/artrosis` y `/blog/lumbalgia`, renombra el slug
`escoliosis` a `escoliosis-y-deformidades` y apaga con 301 los posts
`estenosis-espinal-que-es` y `hernia-discal-o-dolor-de-espalda-como-diferenciarlos`. Sobre las
entradas que la fase 8 todavia no creo no hay nada que editar, y sobre las que va a borrar seria
trabajo tirado. Antes de empezar, comprobar en `src/content/` que la forma del contenido ya es la
que dejo la fase 8; si no lo es, parar y avisar en vez de adivinar.

**No se toca ningun H1.** El campo `h1` de cada entrada es territorio de la fase 8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/phases/10-schema-metadata-y-limpieza-t-cnica/10-SUMMARY.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@src/content/service-pages.ts
@src/content/location-pages.ts
@src/content/blog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondicion, conciliar el inventario de rutas contra el paquete</name>
  <files>src/content/service-pages.ts, src/content/location-pages.ts, src/content/blog.ts</files>
  <precondition>La fase 8 ya esta commiteada: `src/content/service-pages.ts` trae el slug `escoliosis-y-deformidades` y la entrada `cirugia-minimamente-invasiva`, `src/content/blog.ts` trae `artrosis` y `lumbalgia` y ya no trae `estenosis-espinal-que-es` ni `hernia-discal-o-dolor-de-espalda-como-diferenciarlos`.</precondition>
  <read_first>
    - `15-HANDOFF-V11-ONPAGE.md`, secciones 2, 3 y 4: la tabla de las 22 URLs con metadata, las 3 URLs por crear, las 3 redirecciones 301 y por que la guia de destino se publica antes que el 301.
    - Los arreglos exportados por `src/content/service-pages.ts`, `src/content/location-pages.ts` y `src/content/blog.ts`, quedandose con el `slug` de cada entrada.
  </read_first>
  <action>
    Levantar la lista de slugs vivos de los tres modulos y cruzarla contra las filas del handoff que
    corresponden a rutas dinamicas: las 4 de `/sedes/`, las de `/servicios/` y las de `/blog/`.

    Tres resultados posibles y que hacer con cada uno:

    - Slug vivo con fila en el handoff: entra a la tarea 2.
    - Fila en el handoff sin slug vivo: la fase 8 no creo esa ruta todavia. Parar el plan y
      reportarlo. No crear la entrada aca: crear rutas es trabajo de la fase 8 y el copy va con ella.
    - Slug vivo sin fila en el handoff: o es una de las dos URLs que se apagan con 301, y entonces la
      fase 8 quedo a medias y hay que parar, o es una ruta que el mapa de v1.2 no cubrio, y entonces
      se reporta para que Juan decida. No inventarle title ni meta.

    Dejar el resultado del cruce escrito en el SUMMARY, con las filas resueltas y las que no.
  </action>
  <verify>
    <automated>node -e "const{readFileSync}=require('fs');const H=readFileSync('.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md','utf8');const filas=[...H.matchAll(/^\| \`(\/(?:servicios|sedes|blog)\/[a-z0-9-]+)\` \|/gm)].map(m=>m[1]);const src=['service-pages','location-pages','blog'].map(f=>readFileSync('src/content/'+f+'.ts','utf8')).join('\n');const slugs=[...src.matchAll(/^\s{4}slug: \"([a-z0-9-]+)\"/gm)].map(m=>m[1]);const falta=filas.filter(u=>!slugs.includes(u.split('/').pop()));if(falta.length){console.log('filas del paquete sin ruta viva:',falta.join(', '));process.exit(1)}console.log(filas.length+' rutas dinamicas del paquete tienen entrada viva')"</automated>
  </verify>
  <done>Toda fila de ruta dinamica del handoff tiene su entrada viva en `src/content/`, y cualquier desajuste quedo reportado en vez de resuelto a mano.</done>
</task>

<task type="auto">
  <name>Task 2: Title y meta del paquete en las entradas de contenido</name>
  <files>src/content/service-pages.ts, src/content/location-pages.ts, src/content/blog.ts</files>
  <read_first>
    - `15-HANDOFF-V11-ONPAGE.md`, seccion 2, las filas de `/servicios/*`, `/sedes/*` y `/blog/*`.
    - El documento individual de cada URL en `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/` (`servicios-hernia-discal.md`, `sedes-clinica-tezza.md`, `blog-lumbalgia.md` y los demas), tabla "Title, meta y H1". Si difiere del handoff, gana el handoff.
    - `src/app/servicios/[slug]/page.tsx`, funcion `generateMetadata`: lee `page.title` y `page.description` tal cual. Las de sedes y blog siguen el mismo patron.
    - `src/app/servicios/[slug]/opengraph-image.tsx`: lee el mismo campo `title`. Confirmar que sigue asi antes de editar; si sigue, no hay nada que tocar en ese archivo.
  </read_first>
  <action>
    Por cada entrada resuelta en la tarea 1, reemplazar sus campos `title` y `description` por las
    cadenas exactas de su fila del handoff. Copiar literal, con tildes y signos: reescribir al vuelo
    saca el texto del paquete que el doctor aprobo en `15-APROBACION-DOCTOR.md`.

    Lo que no se toca de cada entrada: `slug`, `navLabel`, `h1`, `heroLead`, `cardSummary`,
    `conditionName`, `sections`, `relatedPosts`, fechas y cualquier otro campo. Este plan mueve dos
    cadenas por entrada y nada mas.

    `cardSummary` de las guias de servicio se parece a una meta pero no lo es: alimenta la tarjeta
    del hub y tiene su propio limite de 120 caracteres. Dejarlo como esta.

    No editar ningun `page.tsx` ni ningun `opengraph-image.tsx`: la metadata y la imagen salen de
    estos tres modulos y se actualizan solas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && node -e "const{readFileSync,existsSync}=require('fs');const sm=readFileSync('.next/server/app/sitemap.xml.body','utf8');const o=/<loc>(https?:\/\/[^/<]+)/.exec(sm)[1];const R=[...sm.matchAll(/<loc>([^<]+)<\/loc>/g)].map(m=>m[1].slice(o.length)).filter(p=>/^\/(servicios|sedes|blog)\/.+/.test(p));let bad=0;for(const r of R){const f='.next/server/app'+r+'.html';if(!existsSync(f)){console.log('sin HTML',r);bad++;continue}const h=readFileSync(f,'utf8');const t=/<title>([^<]*)<\/title>/.exec(h)?.[1]??'';const d=/<meta name=\"description\" content=\"([^\"]*)\"/.exec(h)?.[1]??'';if(!t||t.length>60){console.log('TITLE',r,t.length,t);bad++}if(!d||d.length>155){console.log('DESC',r,d.length);bad++}}console.log(R.length+' rutas dinamicas medidas');process.exit(bad?1:0)"</automated>
  </verify>
  <done>Cada ruta dinamica viva sirve el title y la meta exactos de su fila del handoff, ninguna pasa de 60 ni de 155, y ningun otro campo de contenido cambio.</done>
  <reversibility rating="costly">Son los titles y metas publicados de las guias clinicas y las fichas de sede, que es lo que mas trafico organico recibe. Revertir es un commit, pero el reacomodo en la SERP tarda semanas. El texto viene aprobado por el doctor.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete v1.2 (Markdown) -> `src/content/` | Texto transcrito a mano de un documento de planificacion a datos de produccion. |
| fase 8 -> fase 10 | Este plan asume una forma del contenido que otra fase produce; si esa fase quedo a medias, este plan edita entradas equivocadas. |
| sitio -> Google y redes sociales | El title y la meta de las guias clinicas son la superficie de busqueda del sitio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | edicion de `src/content/*.ts` sobre un arbol que la fase 8 no termino | high | mitigate | La tarea 1 es una precondicion ejecutable: cruza slugs vivos contra el paquete y para el plan si falta o sobra una ruta. |
| T-10-05 | Tampering | transcripcion del handoff a los campos `title`/`description` | medium | mitigate | La medicion corre sobre el HTML del build y las rutas se derivan del sitemap, no de una lista escrita a mano; el plan 10-03 la vuelve puerta permanente. |
| T-10-06 | Information disclosure | texto de metadata con datos operativos de sede sin confirmar | high | mitigate | Los 10 datos pendientes de la seccion 6 del handoff son de cuerpo de pagina, no de metadata. No se copia al title ni a la meta ninguna direccion, piso, precio ni seguro. |
| T-10-07 | Repudiation | texto clinico sin aval del doctor | high | mitigate | Solo cadenas del paquete aprobado. Nada de credenciales, numero de cirugias, tasas de exito ni resultados. |
| T-10-SC | Tampering | instalaciones de paquetes | low | accept | Este plan no instala ni actualiza ninguna dependencia. |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- `npm run lint` sin errores ni avisos.
- `npm run build` limpio.
- `npm run content:check` y `npm run sedes:check` sin fallas: confirman que mover metadata no rompio las puertas de contenido de las fases 8 y 9.
- `npm run seo:check` sin fallas: cada ruta dinamica conserva su imagen OG propia y prerenderizada.
- Revision visual de dos tarjetas OG regeneradas, una guia clinica y una sede.
</verification>

<success_criteria>
Las rutas dinamicas vivas sirven el title y la meta del paquete on-page dentro de 60 y 155
caracteres, las tarjetas OG se regeneraron solas y ninguna entrada de contenido cambio fuera de
esos dos campos.
</success_criteria>

<output>
Crear `.planning/workstreams/milestone/phases/10-schema-metadata-y-limpieza-t-cnica/10-02-SUMMARY.md` al terminar.
</output>
