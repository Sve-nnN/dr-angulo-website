---
phase: 10-schema-metadata-y-limpieza-t-cnica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/agendar/page.tsx
  - src/app/blog/page.tsx
  - src/app/contacto/page.tsx
  - src/app/preguntas-frecuentes/page.tsx
  - src/app/sedes/page.tsx
  - src/app/servicios/page.tsx
  - src/app/sobre-el-doctor/page.tsx
  - src/app/testimonios/page.tsx
autonomous: true
requirements: [SEO-08]
must_haves:
  truths:
    - "Las 9 rutas base con metadata estatica emiten el title exacto del paquete v1.2, sin el sufijo del layout pegado atras."
    - "Ninguna de esas 9 rutas emite un <title> de mas de 60 caracteres ni una meta description de mas de 155."
    - "La imagen de Open Graph de cada una de esas rutas muestra el title nuevo, sin tocar ningun archivo de opengraph-image.tsx."
    - "El og:description de la portada deja de ser el texto largo de siteConfig."
  artifacts:
    - src/app/layout.tsx
    - src/app/page.tsx
    - src/app/agendar/page.tsx
    - src/app/blog/page.tsx
    - src/app/contacto/page.tsx
    - src/app/preguntas-frecuentes/page.tsx
    - src/app/sedes/page.tsx
    - src/app/servicios/page.tsx
    - src/app/sobre-el-doctor/page.tsx
    - src/app/testimonios/page.tsx
  key_links:
    - "metadataTitle() de src/lib/og-card.tsx ya lee la forma { absolute }: las tarjetas OG siguen sacando el title de la metadata de la ruta y no de una copia."
    - "El template `%s | Dr. Juan Angulo` del layout sigue vivo para /privacidad y /not-found, que no estan en el paquete y ya miden menos de 60 con el sufijo puesto."
---

<objective>
Aplicar el title y la meta description del paquete on-page de v1.2 a las 9 rutas base cuya metadata
vive dentro de `src/app/**/page.tsx` como export estatico.

Purpose: SEO-08 es lo unico que quedo abierto de la fase 10. El resto (SEO-05, 06, 07, 09, 10, 11)
ya se cerro en la corrida previa y esta descrito en `10-SUMMARY.md`. Aquel resumen dejo los titles
afuera por la decision de Juan del 2026-08-10 de reescribirlos el mismo; el workstream
`seo-keywords` (v1.2) entrego despues un paquete completo, aprobado por el doctor, para las 22 URLs
con metadata. Ese paquete es ahora la fuente y la decision de diferir ya no aplica.

Output: 9 rutas emitiendo el title y la meta exactos del handoff, dentro del contrato 60/155.

**Orden respecto de la fase 8.** Este plan toca solo rutas que ya existen hoy en produccion, asi
que no depende de que la fase 8 haya aterrizado. Lo que si conviene: si la fase 8 esta reescribiendo
`src/app/page.tsx`, `src/app/servicios/page.tsx` o `src/app/preguntas-frecuentes/page.tsx` en
paralelo, este plan corre despues de que esos cambios esten commiteados, para no pisar cuerpo de
pagina recien escrito. El bloque `metadata` es lo unico que este plan toca de cada archivo.

**No se toca ningun H1.** El handoff es explicito: las 6 URLs que solo reciben title y meta
conservan su H1 publicado, y las que reciben copy completo reciben el H1 desde la fase 8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/phases/10-schema-metadata-y-limpieza-t-cnica/10-SUMMARY.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@src/lib/og-card.tsx
@src/lib/site-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Title y meta del paquete en las 9 rutas base</name>
  <files>src/app/page.tsx, src/app/agendar/page.tsx, src/app/blog/page.tsx, src/app/contacto/page.tsx, src/app/preguntas-frecuentes/page.tsx, src/app/sedes/page.tsx, src/app/servicios/page.tsx, src/app/sobre-el-doctor/page.tsx, src/app/testimonios/page.tsx</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md`, seccion 2: la tabla de 22 filas con el title, la meta y su conteo de caracteres. Es la fuente. Las 9 filas de este plan son `/`, `/agendar`, `/blog`, `/contacto`, `/preguntas-frecuentes`, `/sedes`, `/servicios`, `/sobre-el-doctor`, `/testimonios`.
    - El documento individual de cada una en `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/` (`home.md`, `agendar.md`, `blog.md`, `contacto.md`, `preguntas-frecuentes.md`, `sedes.md`, `servicios.md`, `sobre-el-doctor.md`, `testimonios.md`), tabla "Title, meta y H1" cerca del inicio. Si el documento individual y el handoff difieren, gana el handoff: los dos se generan del mismo dataset y el handoff es la vista que la fase 10 recibio.
    - `src/lib/og-card.tsx`, funcion `metadataTitle`: ya acepta las tres formas del campo `title` de Next, incluida `{ absolute }`.
  </read_first>
  <action>
    En cada uno de los 9 `page.tsx`, reemplazar el valor de `title` y de `description` del export
    `metadata` por la cadena exacta de la fila correspondiente del handoff. Copiar literal, con
    tildes y signos tal como estan: cualquier reescritura al vuelo saca el texto del paquete que el
    doctor aprobo.

    El `title` va como `title: { absolute: "..." }`, no como cadena suelta. Motivo concreto: el
    layout raiz declara `title.template` con el sufijo ` | Dr. Juan Angulo`, 19 caracteres que Next
    pega detras de todo title de pagina hija. Los conteos del handoff (el mas largo mide 50) son del
    title final que ve Google, no del fragmento. Con `absolute` el sufijo no se aplica y el title
    servido es exactamente el del paquete.

    No quitar el `template` del layout: `/privacidad` y `/not-found` no estan en el paquete, siguen
    heredandolo y con el sufijo puesto miden 41 y 38 caracteres, dentro del contrato.

    Dejar intactos `alternates.canonical`, `robots`, `revalidate` y todo el cuerpo de cada pagina.
    No tocar ningun `<h1>`. En `/` sustituir tambien el uso de `siteConfig.name` y
    `siteConfig.description` dentro del export `metadata` por las cadenas del paquete, sin editar
    `src/lib/site-config.ts`: ese modulo alimenta el JSON-LD y `/llms.txt`, que no cambian en esta
    fase.

    Si alguna pagina declara su propio objeto `openGraph`, alinear ahi `title` y `description` con
    las mismas dos cadenas nuevas, para que la tarjeta compartida y el resultado de busqueda digan
    lo mismo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && node -e "const{readFileSync}=require('fs');const R=['/','/agendar','/blog','/contacto','/preguntas-frecuentes','/sedes','/servicios','/sobre-el-doctor','/testimonios'];let bad=0;for(const r of R){const f='.next/server/app'+(r==='/'?'/index':r)+'.html';const h=readFileSync(f,'utf8');const t=/<title>([^<]*)<\/title>/.exec(h)?.[1]??'';const d=/<meta name=\"description\" content=\"([^\"]*)\"/.exec(h)?.[1]??'';if(!t||t.length>60){console.log('TITLE',r,t.length,t);bad++}if(!d||d.length>155){console.log('DESC',r,d.length);bad++}if(t.includes('| Dr. Juan Angulo')){console.log('SUFIJO PEGADO',r);bad++}}process.exit(bad?1:0)"</automated>
  </verify>
  <done>Las 9 rutas emiten en el HTML prerenderizado el title y la meta exactos del handoff, ninguna pasa de 60 y 155, y ninguna arrastra el sufijo del template.</done>
  <reversibility rating="costly">Cambiar titles y metas de una web viva hace que Google reindexe y los snippets se muevan; volver atras es un commit, pero el ida y vuelta en la SERP cuesta semanas. El texto viene aprobado por el doctor en 15-APROBACION-DOCTOR.md, asi que el riesgo es de timing, no de contenido.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Alinear los valores por defecto de Open Graph del layout</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - `src/app/layout.tsx`, el export `metadata`: `title.template`, `title.default`, `description` y el objeto `openGraph`.
    - `10-SUMMARY.md`, punto 5 y la desviacion 1: por que `twitter.images` salio del layout y por que las imagenes OG se leen desde la metadata de cada ruta.
  </read_first>
  <action>
    El layout declara `openGraph.title` y `openGraph.description` armados con `siteConfig`. Ese par
    es el que hereda cualquier ruta que no declare el suyo, y hoy la description ronda el doble del
    contrato. Reemplazarlos por el title y la meta de la fila `/` del handoff, los mismos dos textos
    que la tarea 1 puso en la portada.

    Actualizar tambien `title.default` con el title de `/` del handoff: es lo que se sirve cuando una
    ruta no declara title propio.

    No tocar `title.template`: sigue cubriendo `/privacidad` y `/not-found`. No agregar
    `twitter.images`: se saco a proposito en la corrida previa para que cada ruta conserve su
    tarjeta propia. No tocar nada mas del layout.
  </action>
  <verify>
    <automated>npm run build && node -e "const{readFileSync}=require('fs');const h=readFileSync('.next/server/app/index.html','utf8');const og=/<meta property=\"og:description\" content=\"([^\"]*)\"/.exec(h)?.[1]??'';const ot=/<meta property=\"og:title\" content=\"([^\"]*)\"/.exec(h)?.[1]??'';const tw=/<meta name=\"twitter:image\"[^>]*>/.test(h);if(!og||og.length>155){console.log('og:description',og.length);process.exit(1)}if(!ot||ot.length>60){console.log('og:title',ot.length);process.exit(1)}if(!tw){console.log('la portada perdio twitter:image');process.exit(1)}"</automated>
  </verify>
  <done>El og:title y el og:description por defecto miden dentro del contrato y coinciden con la fila `/` del paquete; la portada conserva su twitter:image generada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete v1.2 (Markdown) -> codigo de `src/` | Texto transcrito a mano de un documento de planificacion a codigo de produccion. Nada lo valida en el traspaso salvo lo que este plan agregue. |
| sitio -> Google y redes sociales | El title y la meta son lo que se publica hacia afuera; una vez indexado, corregir cuesta tiempo de rastreo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | transcripcion del handoff a los 9 `page.tsx` | medium | mitigate | La verificacion mide el `<title>` y la `description` del HTML del build, no el codigo fuente; el plan 10-03 deja esa medicion como puerta permanente. |
| T-10-02 | Tampering | `title.template` del layout pegando el sufijo | medium | mitigate | `title: { absolute }` en las 9 rutas y una comprobacion explicita de que el sufijo no aparece en el `<title>` servido. |
| T-10-03 | Repudiation | texto clinico sin aval del doctor | high | mitigate | Solo se copian cadenas del paquete aprobado en `15-APROBACION-DOCTOR.md`. Ninguna redaccion nueva, ninguna credencial, cifra de cirugias ni tasa de exito. |
| T-10-SC | Tampering | instalaciones de paquetes | low | accept | Este plan no instala ni actualiza ninguna dependencia. |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- `npm run lint` sin errores ni avisos.
- `npm run build` limpio.
- `npm run seo:check` sigue sin fallas: las tarjetas OG se regeneran con el title nuevo y ninguna ruta pierde su imagen propia.
- Revision visual de al menos dos tarjetas OG regeneradas (portada y una ruta de title corto) para confirmar que el escalon tipografico de `og-card.tsx` sigue entrando bien con los titles nuevos, que son mas cortos que los anteriores.
</verification>

<success_criteria>
Las 9 rutas base con metadata estatica sirven el title y la meta description exactos del paquete
on-page, dentro de 60 y 155 caracteres, sin sufijo pegado, con sus tarjetas de Open Graph
regeneradas y sin ningun H1 modificado.
</success_criteria>

<output>
Crear `.planning/workstreams/milestone/phases/10-schema-metadata-y-limpieza-t-cnica/10-01-SUMMARY.md` al terminar.
</output>
