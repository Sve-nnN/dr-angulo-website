---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 10
type: execute
wave: 2
depends_on: ["08-05"]
files_modified:
  - src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts
autonomous: true
requirements: [BLOG-02, BLOG-03]
must_haves:
  truths:
    - "El post que hoy habla de síntomas de columna en general pasa a pelear `ciática`, que es la keyword que la SERP de Lima le asigna, con más de 1700 palabras"
    - "El post conserva su URL: nadie pierde un enlace entrante ni un ranking acumulado"
    - "El post sigue enlazando a `/servicios/hernia-discal` y ofreciendo CTA de agenda al cierre"
  artifacts:
    - src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts
  key_links:
    - "`relatedService` sigue en `hernia-discal`, que es el destino del enlace de salida que BLOG-03 exige y que la plantilla ya renderiza"
    - "El `h1` del post pasa a ser el del paquete, porque el cuerpo deja de hablar de cinco síntomas y pasa a hablar de ciática"
---

<objective>
Reescribir `/blog/5-sintomas-de-columna-que-no-debes-ignorar` con el copy aprobado del
paquete de v1.2.

El post publicado es un resumen corto sobre señales de columna en general. La fase 14 le
asignó `ciática` como keyword primaria, y el paquete lo redactó de nuevo entero: 1774
palabras con el esqueleto de guía clínica, ocho `h2` y los `h3` que cubren las preguntas
y las búsquedas relacionadas de la SERP medida en Lima.

La URL no cambia. Lo que cambia es todo lo que hay adentro.

Purpose: cerrar BLOG-02 sobre este post con el contenido que la SERP pide, en vez de
expandir el texto de v1.0 como proponía el plan 08-04 que quedó superado.
Output: `src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts` reescrito.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: el cuerpo completo del post de ciática</name>
  <files>src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts</files>
  <read_first>
    - .planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/blog-5-sintomas-de-columna-que-no-debes-ignorar.md **completo**, y en particular la tabla "Cómo entra este copy en la estructura de post", que mapea campo por campo
    - seo-tools/data/copy-blog.json, entrada de esta URL: trae el valor `clave` de cada sección
    - src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts tal como lo dejó 08-05
    - src/content/sections.ts
  </read_first>
  <action>
    Sigue la tabla "Cómo entra este copy en la estructura de post" del paquete, que ya
    dice de dónde sale cada campo. En concreto:

    `slug` no cambia: la URL se conserva. `format` queda en `guia-clinica` y las secciones
    pasan a ser las ocho canónicas del paquete, con los `id` del campo `clave` del
    dataset y sus `h3` colgando de la sección que les toca.

    `intro` se reemplaza por los dos primeros párrafos de la sección `que-es`, como
    indica el paquete: la intro publicada hablaba de síntomas de columna en general y el
    post ahora pelea ciática. Esos dos párrafos no se repiten después dentro de `que-es`.

    `h1` pasa al valor de la tabla del paquete, en el campo `h1` que el plan 08-05 sumó
    a `BlogPost`. **`title` y `description` no se tocan**: los reescribe la fase 10. Es
    justo la distinción que el modelo viejo no permitía, porque renderizaba `title` como
    encabezado: meter ahí el H1 habría hecho que la fase 10 lo borrara al escribir el
    title de buscador. `publishedAt` se
    conserva. `updatedAt` pasa a la fecha de ejecución. `relatedService` se queda en
    `hernia-discal`. `ctaBanner` conserva el encabezado y el cuerpo que el paquete
    transcribe, que son los ya publicados.

    El texto se transcribe **literal** del bloque entre `copy:inicio` y `copy:fin`,
    párrafo por párrafo. No se parafrasea, no se resume, no se reordena y no se conserva
    ninguna frase del post viejo por cariño: lo aprobado es el texto del paquete
    (aprobación del 2026-08-13, `15-APROBACION-DOCTOR.md`). Las líneas de cita que
    anuncian bloques pendientes son andamiaje del generador y no se transcriben.

    Los enlaces internos **sí se implementan acá**, no en un plan posterior: no existe
    ningún plan de enlazado en el set, y cada módulo de contenido es el dueño natural de
    sus propias salidas. Poblar `outboundLinks` con las 6 entradas de `enlacesPropuestos`
    del dataset, con su `href` y su `anchor` literales. Dos de ellas apuntan a
    `/blog/artrosis` y `/blog/lumbalgia`, que crean los planes 08-12 y 08-13 en olas
    posteriores: el enlace se escribe igual, porque la matriz es el compromiso de la fase,
    y quien verifica que las seis rutas resuelven es el plan 08-14, cuando ya están vivas
    todas las URLs. No se recorta un enlace de la matriz por llegar antes que su destino.

    El enlace de salida hacia la guía de servicio, que es el que BLOG-03 exige, ya lo
    renderiza la plantilla desde `relatedService` y no hace falta tocarlo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && node scripts/check-content.mjs /blog/5-sintomas-de-columna-que-no-debes-ignorar</automated>
  </verify>
  <acceptance_criteria>
    - La puerta pasa sobre la ruta y reporta 1700 palabras de cuerpo o más.
    - Los ocho `id` canónicos de `guia-clinica` aparecen como `h2` destino de ancla, en orden, y la comprobación de esqueleto lo confirma.
    - `grep -c 'relatedService: "hernia-discal"' src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts` devuelve 1.
    - `grep -c 'slug: "5-sintomas-de-columna-que-no-debes-ignorar"'` devuelve 1: la URL no cambió.
    - El HTML prerenderizado del post contiene el enlace hacia `/servicios/hernia-discal` y el CTA de agenda de cierre.
    - La puerta no reporta porcentaje, cifra en soles ni construcción en primera persona.
    - `git diff --name-only` devuelve exactamente un archivo.
  </acceptance_criteria>
  <done>El post publica las 1774 palabras aprobadas sobre ciática, conserva su URL y sigue empujando hacia la guía de hernia discal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy aprobado → sitio en producción | Texto clínico YMYL publicado bajo la firma del doctor |
| URL con historial → contenido nuevo | La URL conserva enlaces y señales acumuladas mientras el cuerpo se reemplaza entero |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-28 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal del paquete sellado; ninguna frase fuera del paquete |
| T-08-29 | Denial of service | pérdida de la URL publicada | high | mitigate | `slug` no se toca y el criterio de aceptación lo verifica con grep |
| T-08-30 | Information disclosure | cifras y credenciales | high | mitigate | La puerta bloquea porcentaje, soles y primera persona sobre el HTML y el archivo de datos |
| T-08-SC | Tampering | instalaciones npm | high | mitigate | Cero paquetes. Solo cambia un archivo de contenido |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` en 0.
- `node scripts/check-content.mjs /blog/5-sintomas-de-columna-que-no-debes-ignorar` en 0.
</verification>

<success_criteria>
El post publica las 1774 palabras aprobadas con el esqueleto de guía clínica, sin cambiar
de URL y sin perder el enlace a su página de servicio.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-10-SUMMARY.md` when done
</output>
