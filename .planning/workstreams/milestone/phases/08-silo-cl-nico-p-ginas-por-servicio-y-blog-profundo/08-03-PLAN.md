---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/content/blog.ts
  - src/app/blog/[slug]/page.tsx
  - src/app/blog/page.tsx
  - src/components/structured-data.tsx
  - src/app/sitemap.ts
autonomous: true
requirements: [BLOG-02, BLOG-03]
user_setup: []

must_haves:
  truths:
    - "Cada post del blog muestra un solo h1, secciones h2 con anclas que funcionan y ningún salto de nivel de encabezado (CTX-14)."
    - "Cada post muestra una tabla de contenidos construida desde sus propias secciones, con enlaces ancla que llevan al encabezado y mueven el foco (CTX-15, A11Y-07, A11Y-13)."
    - "Cada post muestra la firma del doctor con credenciales verificadas, fecha de publicación y fecha de actualización, y el aviso educativo pegado a la firma (CTX-17, SAFE-01, SAFE-07)."
    - "Cada post muestra un banner de agenda dentro del primer tercio del cuerpo y un CTA de cierre, que ahora es un h2 real y no un párrafo con aspecto de encabezado (CTX-16, POS-01)."
    - "Cada post enlaza a la página de servicio de su tema según el mapa acordado: los dos de hernia y el del miedo a operarse hacia hernia discal, y el de estenosis hacia estenosis espinal (BLOG-03, CTX-12)."
    - "Los dos posts de hernia discal superan las 900 palabras de prosa de cuerpo conservando su texto de v1.0 como núcleo (BLOG-02, CTX-11)."
    - "El marcado de artículo declara fecha de publicación y fecha de modificación reales, distintas entre sí cuando el post se actualizó (CTX-17)."
    - "El listado /blog y el sitemap siguen funcionando después del cambio de forma del tipo BlogPost."
  artifacts:
    - path: "src/content/blog.ts"
      provides: "BlogSection, BlogPost con intro y sections, y los cuatro posts migrados"
      contains: "relatedService"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "plantilla de post con firma, aviso, tabla de contenidos, banner, CTA de cierre y enlace al servicio"
      contains: "data-content-body"
  key_links:
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/content/service-pages.ts"
      via: "getServicePage(post.relatedService) alimenta el enlace de salida hacia la guía del tema"
      pattern: "getServicePage"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/ui/table-of-contents.tsx"
      via: "las entradas de la tabla salen del mismo array post.sections que se renderiza, así que no pueden desincronizarse"
      pattern: "TableOfContents"
    - from: "src/components/structured-data.tsx"
      to: "src/content/blog.ts"
      via: "BlogPostingJsonLd lee updatedAt para dateModified, en vez de repetir la fecha de publicación"
      pattern: "dateModified"
    - from: "src/app/sitemap.ts"
      to: "src/content/blog.ts"
      via: "lastModified de cada post pasa a salir de updatedAt"
      pattern: "updatedAt"
---

<objective>
Convertir los cuatro posts de v1.0 de resumen plano en documentos con estructura, y llevar los dos de hernia discal por encima de las 900 palabras.

El trabajo se ordena así: primero cambia la forma del dato y se reparte el texto existente en secciones sin escribir prosa nueva, después se reconstruye la plantilla de post con todo el mobiliario que el contrato exige, y solo al final se expande el contenido. Separar la migración de la redacción hace que, si el build se rompe, se sepa si fue por la forma o por el texto.

El texto actual de los cuatro posts es lo que ya se dio por bueno en v1.0 y se conserva como núcleo (CTX-11). No se reescribe: se reordena y se le suma profundidad.

Este plan no toca las páginas de servicio. Depende de que existan porque cada post enlaza a la suya, y la de estenosis espinal la entrega 08-02.

Purpose: cuatro posts de menos de 300 palabras no rankean ni empujan tráfico. Con estructura, anclas, firma y enlace de salida pasan a alimentar el silo.
Output: la nueva forma de `BlogPost`, la plantilla de post reconstruida, y los dos posts de hernia discal sobre 900 palabras.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-01-SUMMARY.md
@AGENTS.md
@src/content/blog.ts
@src/app/blog/[slug]/page.tsx
@src/app/blog/page.tsx
@src/app/servicios/[slug]/page.tsx
@src/content/service-pages.ts

<interfaces>
<!-- Contratos que entregó 08-01 y que este plan consume tal cual. -->

De `src/content/service-pages.ts`:
```typescript
export type ServicePage = { slug: string; navLabel: string; h1: string; /* ... */ };
export const servicePages: ServicePage[];
export function getServicePage(slug: string): ServicePage | undefined;
```

De los componentes de 08-01:
```typescript
export function AuthorByline(props: { publishedAt: string; updatedAt: string }): JSX.Element;
export function MedicalDisclaimer(): JSX.Element;
export function TableOfContents(props: { title: string; entries: { id: string; label: string }[] }): JSX.Element | null; // null con menos de 3 entradas
export function MidContentCta(props: { heading: string; body: string; location: "service_page" | "blog_post" }): JSX.Element;
```

Forma actual de `src/content/blog.ts`, la que se migra:
```typescript
export type BlogPost = { slug: string; title: string; description: string; date: string; paragraphs: string[] };
export const blogPosts: BlogPost[]; // 4 entradas
```

Consumidores actuales del campo de fecha, todos hay que actualizarlos en la misma pasada:
`src/app/blog/page.tsx`, `src/app/blog/[slug]/page.tsx`, `src/app/sitemap.ts`, y en `src/components/structured-data.tsx` el tipo `BlogPostJsonLdItem` con `BlogJsonLd` y `BlogPostingJsonLd`.
</interfaces>
</context>

## Decisiones de origen que este plan implementa

| Id | Decisión (08-CONTEXT.md) |
|----|--------------------------|
| CTX-09 | Tono de v1.0: segunda persona, español neutro peruano |
| CTX-10 | Mínimo 900 palabras |
| CTX-11 | Los cuatro posts se expanden, no se reescriben: el texto actual queda como núcleo |
| CTX-12 | Mapa de enlazado: los dos posts de hernia y el del miedo a operarse hacia hernia discal, el de estenosis hacia estenosis espinal |
| CTX-14 | Jerarquía de encabezados correcta y rica: un solo `h1`, `h2` por sección, `h3` para subtemas, sin saltos |
| CTX-15 | Tabla de contenidos en cada post, construida desde los encabezados y con anclas que funcionen |
| CTX-16 | Banner de CTA dentro del primer tercio, más el CTA de cierre |
| CTX-17 | E-E-A-T y schema: autoría con credenciales verificadas, fechas de publicación y de última actualización |
| CTX-21 | Prohibido afirmar credenciales fuera del CV, cifras de cirugías, tasas de éxito o tiempos garantizados |

<tasks>

<task type="auto" tdd="false">
  <name>Tarea 1: nueva forma de BlogPost y migración de los cuatro posts, sin prosa nueva</name>
  <precondition>Las cuatro rutas de servicio existen en el build: `.next/server/app/servicios/hernia-discal.html` y `.next/server/app/servicios/estenosis-espinal.html` están presentes tras `npm run build`.</precondition>
  <reversibility rating="costly">Cambiar la forma de `BlogPost` obliga a actualizar en la misma pasada el listado, la ruta de post, el sitemap y dos componentes de schema; deshacerlo después exige la misma cirugía coordinada. Los `id` de sección, en cambio, sí son un compromiso duro: pasan a ser anclas compartibles y no se renombran después.</reversibility>
  <read_first>
    - `src/content/blog.ts` completo, los cuatro posts
    - `src/content/service-pages.ts`, el tipo `ServicePage` y los slugs disponibles
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, sección "Modelo de datos que sostiene el contrato visual", último párrafo
  </read_first>
  <files>src/content/blog.ts</files>
  <action>
Cambiar la forma del tipo y repartir el texto que ya existe. En esta tarea no se escribe una sola frase nueva de contenido clínico: solo encabezados de sección, y el copy del banner de cada post.

Declarar `BlogSection` con `id: string`, `heading: string`, `paragraphs: string[]` y `subsections?: { heading: string; paragraphs: string[] }[]`. Declarar `BlogPost` con `slug`, `title`, `description`, `publishedAt: string`, `updatedAt: string`, `relatedService` tipado como el slug de una `ServicePage` mediante un `import type` desde `@/content/service-pages`, `ctaBanner: { heading: string; body: string }`, `intro: string[]` y `sections: BlogSection[]`. El arreglo plano de párrafos del tipo anterior desaparece del tipo.

Los `id` de sección van escritos en el archivo de datos, en kebab-case sin tildes, no generados por slug del título en tiempo de render: un cambio de redacción rompería anclas ya compartidas. Ese es el mismo criterio que A11Y-15 aplica a las páginas de servicio.

Migración post por post, conservando el texto literal de v1.0 (CTX-11):

- `5-sintomas-de-columna-que-no-debes-ignorar`: el primer párrafo pasa a `intro`. Los cinco párrafos numerados se convierten en secciones con encabezado propio, quitando el número del inicio del párrafo y llevándolo al `heading` de la sección. El párrafo final de cierre se convierte en una sección propia. `relatedService` hacia hernia discal.
- `hernia-discal-o-dolor-de-espalda-como-diferenciarlos`: el primer párrafo pasa a `intro`. Los tres restantes se convierten en tres secciones con encabezado. `relatedService` hacia hernia discal.
- `miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber`: el primer párrafo pasa a `intro`. Los cinco párrafos numerados se convierten en secciones, con el mismo tratamiento del número. `relatedService` hacia hernia discal, que es la cirugía que más miedo genera.
- `estenosis-espinal-que-es`: el primer párrafo pasa a `intro`. Los tres restantes se convierten en tres secciones con encabezado. `relatedService` hacia estenosis espinal.

Cada post queda con tres secciones como mínimo, para que la tabla de contenidos se renderice (A11Y-09). Los encabezados que se inventan son títulos de sección en el tono del sitio, en segunda persona, sin signos de admiración y sin promesa de resultado. Ningún encabezado de sección de blog contiene un número al inicio como si fuera una lista ordenada: el orden ya lo da la secuencia.

`publishedAt` de cada post conserva la fecha que hoy tiene el campo de fecha. `updatedAt` se fija en `2026-08-10` en los cuatro, que es la fecha real en la que el post cambia.

`ctaBanner` por post: un título contextual al tema del post, de una línea, y un cuerpo de una o dos líneas que empuje a agendar. Mismo registro que el banner de las páginas de servicio.

Este archivo también queda cubierto por la puerta de contenido: cero construcciones en primera persona sobre casos concretos, cero cifras de resultado, cero precio. La lista exacta que la puerta rechaza está en `<acceptance_criteria>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
1. `npx tsc --noEmit` reporta errores únicamente en los consumidores que todavía no se migraron (`src/app/blog/page.tsx`, `src/app/blog/[slug]/page.tsx`, `src/app/sitemap.ts`, `src/components/structured-data.tsx`), y ninguno dentro de `src/content/blog.ts`. Esos errores los cierra la tarea 2.
2. `grep -c 'publishedAt' src/content/blog.ts` devuelve 4 o más y `grep -c 'updatedAt' src/content/blog.ts` devuelve 4 o más.
3. `grep -c 'relatedService' src/content/blog.ts` devuelve 5 o más: la declaración del tipo más una por post.
4. `grep -c 'ctaBanner' src/content/blog.ts` devuelve 5 o más.
5. Cada uno de los cuatro posts declara `intro` y `sections`, y cada `sections` tiene 3 entradas o más, cada una con `id` y `heading`.
6. Los `id` de sección son kebab-case sin tildes y sin mayúsculas: `grep -oE 'id: "[^"]+"' src/content/blog.ts` devuelve solo cadenas que casan con `^[a-z0-9-]+$`.
7. `git diff src/content/blog.ts | grep -c '^-.*El dolor de espalda es tan comun'` sirve como sonda de que el texto de v1.0 se conserva: ninguna frase del cuerpo original desaparece, solo se reubica. Verificación equivalente: cada párrafo del archivo original sigue presente, con la única excepción del prefijo numérico movido al encabezado.
8. `src/content/blog.ts` no contiene ninguna de estas expresiones, comparadas en minúsculas y sin tildes:

```
en mi experiencia
mi experiencia
he operado
he tratado
he visto
mis pacientes
en los casos que atiendo
en mi consulta
en mi practica
yo opero
suelo operar
```

9. `git diff --stat` reporta un único archivo modificado: `src/content/blog.ts`.
  </acceptance_criteria>
  <done>`BlogPost` tiene intro, secciones con anclas estables, dos fechas y servicio relacionado, y los cuatro posts están migrados conservando su texto de v1.0.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 2: plantilla de post reconstruida, listado, schema y sitemap</name>
  <read_first>
    - `src/app/blog/[slug]/page.tsx` completo
    - `src/app/blog/page.tsx`
    - `src/app/servicios/[slug]/page.tsx`, la plantilla que 08-01 dejó, para reusar exactamente el mismo orden vertical y las mismas clases
    - `src/components/structured-data.tsx`, funciones `BlogJsonLd` y `BlogPostingJsonLd` y el tipo que comparten
    - `src/app/sitemap.ts`
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, "Superficies de la fase" seccion 3
  </read_first>
  <files>src/app/blog/[slug]/page.tsx, src/app/blog/page.tsx, src/components/structured-data.tsx, src/app/sitemap.ts</files>
  <action>
Reconstruir la superficie del post y cerrar todos los consumidores del tipo migrado.

`src/app/blog/[slug]/page.tsx`. El contenedor raíz pasa a ser `<article data-content-body className="mx-auto max-w-2xl px-4 py-14 sm:px-6 sm:py-20">`, el mismo anclaje que usa la puerta de contenido en las páginas de servicio. Orden vertical exacto: el enlace "Volver al blog", que pasa de `text-primary` a `text-primary-dark` porque a 14px en color de marca no llega al 4.5:1 (COLOR-01); el `h1` con el título; `AuthorByline` con las dos fechas del post, que reemplaza la línea de fecha suelta actual; `MedicalDisclaimer`; `TableOfContents` con el título "En este artículo" y las entradas derivadas de `post.sections`; los párrafos de `intro`; la primera sección; `MidContentCta` alimentado por `post.ctaBanner` con `location="blog_post"`; el resto de secciones; el `h2` del CTA de cierre; el enlace hacia la guía de servicio del tema; y un segundo `MedicalDisclaimer` cerrando.

Cada sección renderiza un `h2` con `id={section.id}`, `tabIndex={-1}` y `scroll-mt-28`, y sus párrafos como `<p>`. Las entradas de la tabla de contenidos salen del mismo array de secciones que se renderiza, y su texto es exactamente el `heading` de destino, sin abreviar (A11Y-11). El `h2` del CTA de cierre no lleva `id` ni `tabIndex`, así que no aparece en la tabla. El bloque de cierre que hoy usa un párrafo con estilo de encabezado pasa a ser un encabezado de verdad: es texto que se ve como encabezado y no lo es, justo lo que corrige la jerarquía que pidió el desarrollador (CTX-14).

El enlace hacia la guía de servicio se arma con `getServicePage(post.relatedService)` y usa el copy del contrato: "Leer la guía completa sobre {condición}", en `text-primary-dark`. Si el servicio no se encontrara, la página no debe romperse en build: se omite el bloque. El CTA de cierre conserva `BookingCta`, y se le suma `WhatsAppCta variant="accent" location="blog_post"` para igualar el cierre de las páginas de servicio.

`src/app/blog/page.tsx`. Actualizar el listado al campo de fecha de publicación migrado. El resto de la página no cambia.

`src/components/structured-data.tsx`. Actualizar el tipo que comparten `BlogJsonLd` y `BlogPostingJsonLd` para que lleve las dos fechas. `BlogJsonLd` sigue emitiendo la fecha de publicación por post. `BlogPostingJsonLd` emite la fecha de publicación desde el campo de publicación y la fecha de modificación desde el campo de actualización, que es lo que hoy no hace: hoy repite la misma fecha en las dos propiedades. Sumar además `about` apuntando al `@id` de la página de servicio relacionada, `/servicios/{relatedService}#page`, que es lo que le dice al buscador que el post y la guía son el mismo silo. Las fechas del marcado salen de los mismos campos que muestra `AuthorByline`: lo que dice el schema y lo que ve el paciente no pueden divergir. El marcado sigue sin afirmar que un médico revisó el texto; los nombres de propiedad prohibidos están en `<acceptance_criteria>`. No se agrega ningún punto de inyección de HTML crudo: todo pasa por el helper de script que ya existe.

`src/app/sitemap.ts`. La fecha de última modificación de cada post pasa a salir del campo de actualización. Las 16 URLs no cambian en cantidad.
  </action>
  <verify>
    <automated>npm run build && npm run lint && node -e "const fs=require('fs');const s=['5-sintomas-de-columna-que-no-debes-ignorar','hernia-discal-o-dolor-de-espalda-como-diferenciarlos','miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber','estenosis-espinal-que-es'];for(const x of s){const h=fs.readFileSync('.next/server/app/blog/'+x+'.html','utf8');for(const k of ['data-content-body','data-author-byline','data-toc','data-mid-cta'])if(!h.includes(k))throw new Error(x+' sin '+k);if((h.match(/data-medical-disclaimer/g)||[]).length!==2)throw new Error(x+' aviso educativo no aparece dos veces');if(!h.includes('href=\"/servicios/'))throw new Error(x+' sin enlace al servicio');}console.log('estructura de posts ok')"</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` y `npm run lint` terminan con código 0 y `npx tsc --noEmit` no reporta errores.
2. Los cuatro HTML de post contienen `data-content-body`, `data-author-byline`, `data-toc` y `data-mid-cta`, y exactamente dos apariciones de `data-medical-disclaimer`.
3. En los cuatro, la primera aparición de `data-medical-disclaimer` está antes de `data-toc`, que está antes del primer `<h2`.
4. Los cuatro tienen exactamente un `<h1` y ningún salto de nivel de encabezado: ningún `<h3` sin un `<h2` previo y ningún `<h4`.
5. La cantidad de entradas `<li>` dentro del `nav` con `aria-labelledby="toc-title"` coincide exactamente con la cantidad de `<h2` que llevan `id` y `tabindex="-1"`, en los cuatro posts.
6. Cada post contiene el enlace a su guía según el mapa: `5-sintomas...`, `hernia-discal-o-dolor...` y `miedo-a-operarte...` contienen `href="/servicios/hernia-discal"`, y `estenosis-espinal-que-es` contiene `href="/servicios/estenosis-espinal"`.
7. Cada post contiene al menos dos `href="/agendar"`.
8. `grep -c 'text-primary-dark' .next/server/app/blog/estenosis-espinal-que-es.html` devuelve 1 o más, y el enlace "Volver al blog" ya no usa `text-primary` a secas.
9. `grep -c 'post.paragraphs' src/app/blog/\[slug\]/page.tsx` devuelve 0.
10. `grep -c '"dateModified"' .next/server/app/blog/estenosis-espinal-que-es.html` devuelve 1, y su valor es `2026-08-10`, distinto del valor de `datePublished`.
11. Los cuatro HTML de post contienen `"@type":"BlogPosting"` con una propiedad `about` que apunta a `/servicios/` seguida del slug de su guía.
12. Ninguno de los cuatro HTML de post contiene `reviewedBy` ni `lastReviewed`.
13. `grep -c 'dangerouslySetInnerHTML' src/components/structured-data.tsx` sigue devolviendo exactamente 1, y `grep -c 'dangerouslySetInnerHTML' src/app/blog/\[slug\]/page.tsx` devuelve 0.
14. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` sigue devolviendo exactamente 16.
15. `node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal /servicios/escoliosis /servicios/ortopedia-infantil` sigue terminando con código 0: la reconstrucción del blog no rompió el silo.
  </acceptance_criteria>
  <done>Los cuatro posts se renderizan con firma, aviso educativo duplicado, tabla de contenidos funcional, banner, CTA de cierre como encabezado real y enlace de salida hacia su guía, y todos los consumidores del tipo migrado compilan.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 3: expandir los dos posts de hernia discal sobre 900 palabras</name>
  <reversibility rating="one-way">La prosa clínica ampliada sale publicada bajo la firma del doctor sin revisión previa suya; una vez indexada y compartida, retirarla no borra lo que ya se leyó ni lo que quedó en caché. Decisión ya tomada por el desarrollador el 2026-08-10 y registrada en 08-CONTEXT.md (CTX-21), se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `src/content/blog.ts`, los dos posts a expandir en su forma ya migrada
    - `src/content/service-pages.ts`, la entrada de hernia discal, para que el post y la guía no se contradigan ni se copien
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, secciones "Salvaguardas de contenido firmado" y "POS-01"
  </read_first>
  <files>src/content/blog.ts</files>
  <action>
Llevar `5-sintomas-de-columna-que-no-debes-ignorar` y `hernia-discal-o-dolor-de-espalda-como-diferenciarlos` de resumen a contenido de fondo, con 900 palabras o más de prosa de cuerpo cada uno.

Se expande, no se reescribe (CTX-11). El texto de v1.0 queda como núcleo de cada sección y se le suma profundidad alrededor: por qué ocurre, qué distingue un caso de otro, qué se evalúa en consulta, qué señales cambian la conducta, qué se hace primero. Donde una sección se vuelva larga, se le agregan `subsections` con `h3`, nunca un `h4`.

Presupuesto por post: `intro` entre 90 y 120 palabras, primera sección entre 130 y 180, y el resto repartido hasta llegar a un cuerpo total entre 950 y 1100 palabras. Ese presupuesto no es decorativo: el banner va después de la primera sección, y la puerta exige que las palabras previas al banner queden entre el 15% y el 35% del cuerpo. Si el conteo cae fuera de la ventana, se mueve el banner una sección, no se ajusta el rango.

Los dos posts pueden sumar secciones nuevas si el desarrollo lo pide, siempre con `id` en kebab-case sin tildes y estable.

Contenido y voz (CTX-06, CTX-09, CTX-21): consenso clínico general en voz neutra, sin atribuirle nada al doctor; lo que ya está en `src/content/` de v1.0; y el CV verificado para cualquier mención de credenciales. Prohibido afirmar credenciales fuera del CV, cantidad de cirugías, tasas de éxito, promesas de resultado, plazos garantizados y cualquier cifra de precio. Los plazos van en voz condicional dentro del párrafo, en rango cualitativo, nunca destacados. Segunda persona, español neutro peruano, cero signos de admiración, cero emojis. El repertorio de construcciones en primera persona sobre casos concretos que la puerta rechaza está en `<acceptance_criteria>`.

Los dos posts apuntan a la misma guía, así que no deben decir lo mismo: el de los cinco síntomas trabaja el reconocimiento de señales, y el de diferenciar dolor de espalda de hernia discal trabaja la distinción entre una contractura y un compromiso del disco o del nervio. Cada uno cierra empujando hacia la guía completa, que es donde vive el detalle del tratamiento.

`updatedAt` de los dos posts se mantiene en `2026-08-10`.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-content.mjs /blog/5-sintomas-de-columna-que-no-debes-ignorar /blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` y `npm run lint` terminan con código 0.
2. `node scripts/check-content.mjs` sobre las dos rutas de post termina con código 0, y las dos reportan 900 palabras de cuerpo o más.
3. El script confirma en los dos posts que las palabras previas al banner caen entre el 15% y el 35% del cuerpo.
4. Los dos posts conservan su `intro` y sus secciones con `id` estable, sin ningún salto de nivel de encabezado.
5. Los dos posts contienen `href="/servicios/hernia-discal"` y al menos dos `href="/agendar"`.
6. Ninguno de los dos HTML contiene un dígito seguido de `%` ni `S/` seguido de dígitos.
7. Ni los HTML ni `src/content/blog.ts` contienen ninguna de estas expresiones, comparadas en minúsculas y sin tildes:

```
en mi experiencia
mi experiencia
he operado
he tratado
he visto
mis pacientes
en los casos que atiendo
en mi consulta
en mi practica
yo opero
suelo operar
```

8. `node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal /servicios/escoliosis /servicios/ortopedia-infantil` sigue terminando con código 0.
9. `git diff --stat` reporta un único archivo modificado en esta tarea: `src/content/blog.ts`.
  </acceptance_criteria>
  <done>Los dos posts de hernia discal superan las 900 palabras de prosa conservando su texto de v1.0, con el banner dentro del primer tercio y enlace de salida hacia la guía.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/content/blog.ts` en build a HTML estático publicado | Prosa clínica ampliada que sale firmada por un médico colegiado sin revisión previa suya. Sin entrada de usuario. |
| `post.sections[].id` a atributo `id` y a `href="#id"` | Cadenas de datos que llegan al DOM como identificadores y como destinos de ancla. |
| Objetos de `BlogPostingJsonLd` a `JsonLdScript` a `dangerouslySetInnerHTML` | Único punto de inyección de HTML crudo del repositorio, ya existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Information disclosure | Prosa ampliada de los posts firmada por el doctor | high | mitigate | `scripts/check-content.mjs` corre sobre las dos rutas expandidas en el `<verify>` de la tarea 3 y verifica SAFE-05, SAFE-08 y SAFE-10 sobre el HTML renderizado y sobre `src/content/blog.ts`. |
| T-08-10 | Tampering | `post.sections[].id` usado como `id` de DOM y destino de ancla | low | mitigate | Los `id` se escriben a mano en el archivo de datos en kebab-case sin tildes y el criterio 6 de la tarea 1 lo verifica con una expresión regular. No hay generación en tiempo de render ni entrada de usuario. |
| T-08-11 | Spoofing | `BlogPostingJsonLd` con `author` y `publisher` del doctor | medium | mitigate | El marcado no declara revisión médica (criterio 12 de la tarea 2), las fechas del schema salen de los mismos campos que muestra la firma visible, y el aviso educativo viaja pegado a la firma. |
| T-08-12 | Tampering | `structured-data.tsx` modificado por segunda vez en la fase | medium | mitigate | El criterio 13 de la tarea 2 exige que siga habiendo exactamente un punto de inyección de HTML crudo en el archivo, el helper existente, y ninguno en la ruta de post. |
| T-08-SC | Tampering | Instalaciones de npm | high | mitigate | La fase no instala paquetes. Ninguna tarea de este plan toca `package.json`. |
</threat_model>

## Artifacts this phase produces

Aporte de este plan.

**Tipos nuevos en `src/content/blog.ts`**
- `BlogSection` (`id`, `heading`, `paragraphs`, `subsections?`)
- `BlogPost` reformado: `publishedAt`, `updatedAt`, `relatedService`, `ctaBanner`, `intro`, `sections`

**Superficies reconstruidas**
- `/blog/[slug]`: firma, aviso educativo duplicado, tabla de contenidos, banner del primer tercio, CTA de cierre como `h2` real, enlace de salida hacia la guía de servicio

**Schema ampliado en `src/components/structured-data.tsx`**
- `BlogPostJsonLdItem` con las dos fechas
- `BlogPostingJsonLd` con `dateModified` real y `about` apuntando al `@id` de la guía de servicio del tema

**Archivos nuevos:** ninguno. **Componentes nuevos:** ninguno. **Claves nuevas de `CtaLocation`:** ninguna, se consume `blog_post` que creó 08-01.

<verification>
1. `npm run build`, `npm run lint` y `npx tsc --noEmit` terminan con código 0.
2. `node scripts/check-content.mjs /blog/5-sintomas-de-columna-que-no-debes-ignorar /blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos` termina con código 0.
3. `node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal /servicios/escoliosis /servicios/ortopedia-infantil` termina con código 0.
4. Los cuatro HTML de post contienen tabla de contenidos, firma, dos avisos, un banner y enlace a su guía de servicio.
5. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve exactamente 16.
6. `.next/server/app/servicios.html` conserva `id="columna"`, `id="traumatologia"`, `id="ortopedia-infantil"` e `id="procedimientos"`.
7. `git diff --stat package.json` no reporta cambios.
</verification>

<success_criteria>
- `BlogPost` tiene intro, secciones con anclas estables, dos fechas y servicio relacionado, y los cuatro posts están migrados sin perder su texto de v1.0.
- Los cuatro posts se renderizan con jerarquía de encabezados correcta, tabla de contenidos funcional, firma con credenciales verificadas y aviso educativo pegado a la firma.
- Cada post enlaza a la guía de servicio de su tema y ofrece agendar dentro del primer tercio y al cierre.
- El marcado de artículo declara fecha de modificación real y enlaza el post con su guía.
- Los dos posts de hernia discal superan las 900 palabras de prosa de cuerpo.
- Las cuatro rutas del silo siguen pasando la puerta de contenido.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-03-SUMMARY.md` when done
</output>
