---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/service-pages.ts
  - src/app/servicios/[slug]/page.tsx
  - src/components/ui/author-byline.tsx
  - src/components/ui/medical-disclaimer.tsx
  - src/components/ui/table-of-contents.tsx
  - src/components/ui/mid-content-cta.tsx
  - src/components/services/service-card.tsx
  - src/components/ui/booking-cta.tsx
  - src/components/ui/whatsapp-cta.tsx
  - src/lib/site-config.ts
  - src/app/servicios/page.tsx
  - src/components/structured-data.tsx
  - src/app/sitemap.ts
  - scripts/check-content.mjs
  - package.json
autonomous: true
requirements: [SVC-01, SVC-05]
user_setup: []

must_haves:
  truths:
    - "Un paciente abre https://drangulocolumna.com/servicios/hernia-discal y lee una guía completa de 900 palabras o más de prosa, con las siete secciones en el orden fijo (CTX-04, CTX-10)."
    - "La firma del doctor y el aviso de contenido educativo se leen juntos, arriba de la tabla de contenidos, sin hacer scroll (SAFE-01, CTX-08)."
    - "Cada entrada de la tabla de contenidos lleva al h2 con el mismo texto, mueve el foco a ese encabezado y no queda tapada por el header sticky (A11Y-07, A11Y-11, A11Y-13, A11Y-16)."
    - "Desde /servicios el paciente llega a la guía de hernia discal con un clic, y desde la guía vuelve al hub sin pasar por el inicio (SVC-05, CTX-03, CTX-05)."
    - "Desde la guía se puede agendar dentro del primer tercio del texto y también al cierre, sin volver al inicio (SVC-05, CTX-16, POS-01)."
    - "Los cuatro anclajes vivos del hub (#columna, #traumatologia, #ortopedia-infantil, #procedimientos) siguen resolviendo, porque la home enlaza a los cuatro."
    - "El sitemap declara /servicios/hernia-discal y el HTML de esa ruta emite MedicalWebPage con about de tipo MedicalCondition (CTX-18)."
    - "Ninguna superficie nueva afirma credenciales fuera del CV, cifras de cirugías, tasas de éxito, precios ni experiencia clínica en primera persona (CTX-06, CTX-07, CTX-21, SAFE-05, SAFE-08, SAFE-10)."
  artifacts:
    - path: "src/content/service-pages.ts"
      provides: "SERVICE_SECTION_ORDER, ServiceSectionId, ServiceSection, ServicePage y la entrada de hernia discal"
      contains: "SERVICE_SECTION_ORDER"
    - path: "src/app/servicios/[slug]/page.tsx"
      provides: "plantilla única de las cuatro páginas del silo con generateStaticParams"
      contains: "generateStaticParams"
    - path: "src/components/ui/author-byline.tsx"
      provides: "firma del doctor con credenciales importadas de cv.ts y dos fechas"
      contains: "credentialsInfo"
    - path: "src/components/ui/medical-disclaimer.tsx"
      provides: "aviso de contenido educativo"
      contains: "data-medical-disclaimer"
    - path: "src/components/ui/table-of-contents.tsx"
      provides: "nav etiquetado con la lista de secciones y sus anclas"
      contains: "aria-labelledby"
    - path: "src/components/ui/mid-content-cta.tsx"
      provides: "banner de CTA del primer tercio"
      contains: "data-mid-cta"
    - path: "src/components/services/service-card.tsx"
      provides: "tarjeta del hub hacia una página hija con enlace extendido"
      contains: "after:absolute"
    - path: "scripts/check-content.mjs"
      provides: "puerta automática de contenido: palabras, jerarquía, anclas, posición del banner y salvaguardas SAFE"
      contains: "FIRST_PERSON_CLAIMS"
  key_links:
    - from: "src/app/servicios/[slug]/page.tsx"
      to: "src/content/service-pages.ts"
      via: "recorre SERVICE_SECTION_ORDER, nunca Object.keys, así la tabla de contenidos y lo renderizado salen de la misma lista"
      pattern: "SERVICE_SECTION_ORDER"
    - from: "src/components/ui/author-byline.tsx"
      to: "src/content/cv.ts"
      via: "importa credentialsInfo; el componente no acepta credenciales por props ni children"
      pattern: "from \"@/content/cv\""
    - from: "src/app/servicios/page.tsx"
      to: "src/app/servicios/[slug]/page.tsx"
      via: "ServiceCard enlaza a /servicios/{slug} desde la grilla del hub"
      pattern: "/servicios/\\$\\{"
    - from: "src/app/sitemap.ts"
      to: "src/content/service-pages.ts"
      via: "deriva una entrada de sitemap por cada servicePage, igual que ya hace con blogPosts"
      pattern: "servicePages"
    - from: "src/components/structured-data.tsx"
      to: "src/app/servicios/[slug]/page.tsx"
      via: "MedicalWebPageJsonLd y FaqJsonLd se emiten desde la plantilla reusando el helper JsonLdScript existente"
      pattern: "MedicalWebPageJsonLd"
---

<objective>
Dejar viva de punta a punta la primera URL del silo clínico: `/servicios/hernia-discal`, con datos, plantilla, firma, aviso, tabla de contenidos, banner, schema, sitemap y tarjeta en el hub, más la puerta automática que va a gobernar el contenido del resto de la fase.

Este plan es el trazador. No entrega las otras tres condiciones a propósito: prueba la arquitectura completa sobre un solo camino antes de expandir. Si algo del modelo de datos, del orden fijo de secciones o de la accesibilidad está mal pensado, se descubre acá y no después de haber escrito cuatro páginas.

La ruta es dinámica `[slug]` alimentada por datos (CTX-02), no cuatro archivos sueltos. El orden de las siete secciones se garantiza por construcción con una tupla y un `Record` (CTX-04): si falta una sección, TypeScript rompe el build.

`/servicios` se reconvierte en hub sumando una sección de tarjetas (CTX-03). Todo lo que ya tiene de v1.0 queda intacto, incluidos los cuatro `id` de sección a los que enlaza la home desde cuatro lugares.

Purpose: hoy una sola página `/servicios` compite contra dominios exact match en la SERP de Lima y pierde. La primera URL dedicada abre el silo.
Output: `/servicios/hernia-discal` publicable, el hub enlazando hacia ella, y `scripts/check-content.mjs` como contrato ejecutable del contenido de toda la fase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@AGENTS.md
@design-system/dr-angulo/MASTER.md
@src/app/servicios/page.tsx
@src/app/blog/[slug]/page.tsx
@src/content/cv.ts
@src/content/services.ts
@src/lib/site-config.ts
@src/app/sitemap.ts
@src/app/globals.css

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del repositorio, no hace falta explorar. -->

De `src/lib/site-config.ts`:
```typescript
export const siteConfig: { name, shortName, title, description, url, whatsapp: { number, displayNumber }, office, credentials: { cmp, rne }, specialties: readonly string[], social, email };
export function whatsappUrl(message: string): string;
export const whatsappMessages: { header, hero, floating_button, services, footer, contact_page, booking_page };
export type CtaLocation = keyof typeof whatsappMessages;
```

De `src/content/cv.ts`:
```typescript
export const credentialsInfo: {
  cmp: string;                    // colegiatura
  rne: string;                    // registro de especialista
  yearsOfExperience: number;
  internationalTrainings: number;
};
```

De `src/components/ui/booking-cta.tsx` y `src/components/ui/whatsapp-cta.tsx`:
```typescript
export function BookingCta(props: { children: React.ReactNode; size?: "sm" | "md"; className?: string; onClick?: () => void }): JSX.Element; // Link a /agendar
export function WhatsAppCta(props: { location: CtaLocation; children: React.ReactNode; variant?: "accent" | "outline" | "outline-inverse"; className?: string }): JSX.Element; // client component, llama trackWhatsAppClick(location)
```

De `src/components/structured-data.tsx` (403 líneas nuevas en `5387091`, se extiende, no se duplica, CTX-19):
```typescript
// Helpers internos ya existentes en el archivo, reusarlos tal cual:
const ID = { website, physician, location: (slug) => ..., procedure: (slug) => ... };
function abs(path: string): string;
function JsonLdScript({ data, id }: { data: unknown; id: string }): JSX.Element; // único punto con dangerouslySetInnerHTML, ya escapa "<"

export type BreadcrumbItem = { name: string; path: string };
export function BreadcrumbJsonLd({ items }: { items: BreadcrumbItem[] }): JSX.Element;
export type FaqJsonLdItem = { question: string; answer: string };
export function FaqJsonLd({ items }: { items: FaqJsonLdItem[] }): JSX.Element; // hoy con "@id" fijo en /preguntas-frecuentes#faq
export function ServicesJsonLd(): JSX.Element;
```

De `src/lib/utils.ts`:
```typescript
export function cn(...inputs: ClassValue[]): string; // clsx + tailwind-merge
```
</interfaces>
</context>

## Decisiones de origen que este plan implementa

| Id | Decisión (08-CONTEXT.md) |
|----|--------------------------|
| CTX-01 | URLs anidadas bajo el hub: `/servicios/hernia-discal` y sus tres hermanas |
| CTX-02 | Una ruta dinámica `[slug]` alimentada por datos en `src/content/`, no cuatro `page.tsx` sueltos |
| CTX-03 | `/servicios` se reconvierte en hub: conserva su contenido de v1.0 y suma tarjetas hacia las hijas |
| CTX-04 | Estructura interna fija e idéntica: qué es, síntomas, cuándo consultar, diagnóstico, tratamiento, recuperación, preguntas frecuentes, más CTA |
| CTX-05 | Cada página enlaza de vuelta al hub y ofrece CTA de agenda sin obligar a volver al inicio |
| CTX-06 | Consenso clínico general en voz neutra; las credenciales salen solo del CV verificado |
| CTX-07 | No se publica precio de consulta |
| CTX-08 | Aviso de contenido educativo visible |
| CTX-09 | Tono de v1.0: segunda persona, español neutro peruano |
| CTX-10 | Mínimo 900 palabras por página |
| CTX-16 | Banner de CTA dentro del primer tercio, más el CTA de cierre |
| CTX-17 | Autoría atribuida al doctor con credenciales verificadas, fecha de publicación y de actualización |
| CTX-18 | `MedicalWebPage` con `about` de tipo `MedicalCondition`; `MedicalProcedure` solo donde la página describe cirugía |
| CTX-19 | Extender `src/components/structured-data.tsx`, no duplicar lo que ya metió `5387091` |
| CTX-21 | Se publica sin revisión previa del doctor; prohibido afirmar credenciales fuera del CV, cifras de cirugías, tasas de éxito o tiempos garantizados |

Todas estas decisiones ya las tomó el desarrollador el 2026-08-10 y quedaron registradas en `08-CONTEXT.md`. Por eso este plan las ejecuta sin volver a preguntar, incluidas las que llevan calificación `one-way`.

<tasks>

<task type="tracer" tdd="false">
  <name>Tarea 1: /servicios/hernia-discal vivo de punta a punta</name>
  <reversibility rating="one-way">El slug de la URL y los siete `id` de ancla se vuelven enlaces compartibles e indexados en cuanto el deploy sale; cambiarlos después rompe enlaces ya repartidos y obliga a redirecciones permanentes. Decisión ya tomada en 08-CONTEXT.md (CTX-01, A11Y-15), se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md` completo, en particular "Modelo de datos que sostiene el contrato visual", "Contrato de accesibilidad", "Superficies de la fase" seccion 2 y "Salvaguardas de contenido firmado"
    - `src/app/blog/[slug]/page.tsx` como análogo directo de ruta dinámica ya en producción
    - `src/content/cv.ts`
    - `src/content/services.ts`
    - `src/app/globals.css` (regla global de foco y `scroll-padding-top`)
    - `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-static-params.md`
    - `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md`
  </read_first>
  <files>src/content/service-pages.ts, src/app/servicios/[slug]/page.tsx, src/components/ui/author-byline.tsx, src/components/ui/medical-disclaimer.tsx, src/components/ui/table-of-contents.tsx</files>
  <action>
Crear el modelo de datos, los tres componentes de "mobiliario del documento" y la plantilla de ruta, y escribir la guía completa de hernia discal.

`src/content/service-pages.ts`. Declarar `SERVICE_SECTION_ORDER` como tupla `as const` con exactamente los siete identificadores en kebab-case sin tildes que fija A11Y-15, en el orden de CTX-04, y derivar `export type ServiceSectionId = (typeof SERVICE_SECTION_ORDER)[number]`. Declarar `ServiceSubsection` con `heading: string` y `paragraphs: string[]`. Declarar `ServiceSection` con `heading: string`, `paragraphs: string[]` y `subsections?: ServiceSubsection[]`. Declarar `ServicePage` con: `slug`, `navLabel`, `h1`, `title`, `description`, `cardSummary`, `conditionName`, `alternateNames?: string[]`, `publishedAt: string`, `updatedAt: string`, `ctaBanner: { heading: string; body: string }`, `sections: Record<ServiceSectionId, ServiceSection>`, `relatedPosts: string[]` y `describesSurgery: boolean`. Exportar `servicePages: ServicePage[]` con una sola entrada por ahora y un helper `getServicePage(slug: string): ServicePage | undefined`. El `Record` sobre la tupla es lo que hace imposible una página incompleta: si falta una clave, el build falla. Los `paragraphs` son cadenas planas sin marcado, así que ninguna cifra puede quedar resaltada tipográficamente (SAFE-09). El tipo no expone ningún campo de vivencia personal ni de resultado (SAFE-04, SAFE-08): la lista exacta de nombres de campo prohibidos está en `<acceptance_criteria>` y la puerta de la tarea 3 la verifica sobre las declaraciones de tipo.

Contenido de la entrada `hernia-discal`. `navLabel` "Hernia discal", `h1` "Hernia discal", `conditionName` "Hernia discal", `alternateNames` con "Hernia de disco" y "Protrusión discal". `title` y `description` para la Metadata API, en el tono de v1.0. `cardSummary` de 120 caracteres como máximo. `publishedAt` y `updatedAt` en `2026-08-10`. `describesSurgery` en `true`. `relatedPosts` con los tres slugs de blog que apuntan a esta condición según el mapa de CTX-12: `5-sintomas-de-columna-que-no-debes-ignorar`, `hernia-discal-o-dolor-de-espalda-como-diferenciarlos` y `miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber`. `ctaBanner` contextualizado a la condición, con el ejemplo del UI-SPEC como referencia de tono.

Redacción (CTX-06, CTX-09, CTX-21). Segunda persona, español neutro peruano, sin tecnicismos innecesarios, el mismo registro que ya tienen `src/content/blog.ts` y `src/content/services.ts`. Voz explicativa y general, nunca testimonial: se describe cómo se aborda la condición, no lo que el doctor hizo con sus pacientes. Cero signos de admiración, cero emojis. Solo consenso clínico general más lo que ya está en `src/content/` de v1.0 y en `src/content/cv.ts`. Prohibido escribir credenciales que no estén en el CV, cantidad de cirugías, tasas de éxito, promesas de resultado, plazos de recuperación garantizados y cualquier cifra de precio. Los plazos de recuperación, si aparecen, van en voz condicional dentro del párrafo y en rango cualitativo (SAFE-09). El repertorio de construcciones en primera persona sobre casos concretos que la puerta rechaza está enumerado en `<acceptance_criteria>`; redacta de modo que ninguna aparezca.

Presupuesto de palabras por sección, para que la prosa quede sobre 900 con margen: qué es 150, síntomas 160, cuándo consultar 120, diagnóstico 150, tratamiento 280 repartidas entre sus dos subsecciones, recuperación 140, preguntas frecuentes 200 repartidas en cuatro preguntas. Total aproximado 1200. La sección `tratamiento` lleva exactamente dos `subsections`: manejo conservador y cuándo se plantea la cirugía. La sección `preguntas-frecuentes` lleva una `subsection` por pregunta, con la pregunta como `heading` y la respuesta en `paragraphs`. Ninguna otra sección lleva `subsections` (A11Y-03).

`src/components/ui/author-byline.tsx`. Server Component. Props exactamente `{ publishedAt: string; updatedAt: string }` y nada más: no acepta `children`, ni slot libre, ni credenciales por props (SAFE-04, SAFE-06). Importa `credentialsInfo` de `@/content/cv` y `siteConfig` de `@/lib/site-config`, y arma las cadenas de colegiatura y registro de especialista desde esos campos. Renderiza en el nodo raíz el atributo `data-author-byline`. Muestra el nombre del doctor, sus especialidades desde `siteConfig.specialties`, la línea de credenciales, las dos fechas en elementos `<time dateTime>` con el `Intl.DateTimeFormat("es-PE")` que ya usan `/blog` y `/blog/[slug]` y con el texto "Publicado el {fecha} · Actualizado el {fecha}" (SAFE-07, CTX-17), y un `Link` a `/sobre-el-doctor` con el texto "Ver trayectoria completa" en `text-primary-dark` (COLOR-01). Nada de dorado en esta superficie.

`src/components/ui/medical-disclaimer.tsx`. Server Component sin props. Nodo raíz con el atributo `data-medical-disclaimer`, clases `rounded-2xl border border-border bg-muted p-6`, ícono `Info` de `lucide-react` en `text-primary` con `aria-hidden="true"`, título "Información educativa" en `font-heading text-base font-bold text-foreground` y cuerpo en `text-foreground/80` a 16px con el texto literal del bloque de copia del UI-SPEC (SAFE-03). Sin `text-xs`, sin opacidad por debajo de `/70`, sin `details`.

`src/components/ui/table-of-contents.tsx`. Server Component. Props `{ title: string; entries: { id: string; label: string }[] }`. Si `entries.length < 3` devuelve `null` (A11Y-09). Marcado exacto del UI-SPEC A11Y-07: `nav` con `aria-labelledby="toc-title"` y el atributo `data-toc`, dentro un `<p id="toc-title">` con estilo de encabezado pequeño, y una `<ol>` con un `<li>` por entrada. Cada enlace es `block min-h-11 py-2.5 text-base font-semibold text-primary-dark hover:underline` (A11Y-10, COLOR-01) y su texto es `entry.label` sin abreviar (A11Y-11). Sin `truncate`, sin `line-clamp` (A11Y-12). El título de la tabla no es un encabezado, es un párrafo, para que el esquema de encabezados coincida uno a uno con las entradas (A11Y-05).

`src/app/servicios/[slug]/page.tsx`. Server Component con `params: Promise<{ slug: string }>`, siguiendo el patrón ya vigente en `src/app/blog/[slug]/page.tsx`. Exporta `generateStaticParams` derivado de `servicePages` y `generateMetadata` con `title`, `description` y `alternates.canonical` en `/servicios/${slug}`. Un slug desconocido llama `notFound()` y cae en el `src/app/not-found.tsx` de v1.0, sin copia nueva. El contenedor raíz es `<article data-content-body className="mx-auto max-w-2xl px-4 py-14 sm:px-6 sm:py-20">`: el atributo es el punto de anclaje de la puerta de la tarea 3, y `max-w-2xl` es deliberado aunque el hub use `max-w-4xl`, porque son documentos de lectura.

Orden vertical exacto dentro del `article`: `h1` con `text-pretty` en `font-heading text-3xl font-extrabold text-primary sm:text-4xl`; `AuthorByline`; `MedicalDisclaimer`; `TableOfContents` con `title` "En esta página"; las secciones visibles; el `h2` del CTA de cierre con `BookingCta` y `WhatsAppCta variant="accent" location="service_page"`; el enlace de vuelta al hub con `ArrowLeft` de Lucide, texto "Ver todas las especialidades" en `text-primary-dark`; el bloque de posts relacionados solo si `relatedPosts` no está vacío; y un segundo `MedicalDisclaimer` cerrando (SAFE-02). El primer aviso va pegado a la firma y arriba de la tabla de contenidos, no al pie (SAFE-01).

La lista de secciones visibles se calcula recorriendo `SERVICE_SECTION_ORDER`, jamás `Object.keys`, y filtrando las que no tienen ni `paragraphs` ni `subsections` con contenido. Esa misma lista alimenta la tabla de contenidos, así que no puede desincronizarse de lo renderizado. Cada sección renderiza un `h2` con `id={id}`, `tabIndex={-1}` y `scroll-mt-28` (A11Y-13, A11Y-16), clases `font-heading text-2xl font-bold text-primary sm:text-3xl`, sus párrafos como `<p>` en `text-lg text-foreground/80`, y sus subsecciones como `h3` en `font-heading text-lg font-bold text-primary` más párrafos. Las preguntas frecuentes se renderizan como `h3` más párrafo, sin acordeón. El `h2` del CTA de cierre no lleva `id` ni `tabIndex`, porque no es una sección de contenido y no entra en la tabla de contenidos. Ningún componente de esta tarea anula la regla global de foco de `globals.css` (A11Y-14).

En esta tarea el banner del primer tercio todavía no se inserta: llega en la tarea 2, junto con las claves de `CtaLocation` que necesita.
  </action>
  <verify>
    <automated>npm run build && npm run lint && test -f .next/server/app/servicios/hernia-discal.html</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` termina con código 0 y `npm run lint` termina con código 0.
2. `test -f .next/server/app/servicios/hernia-discal.html` devuelve 0.
3. `grep -c 'SERVICE_SECTION_ORDER' src/app/servicios/\[slug\]/page.tsx` devuelve 1 o más, y `grep -c 'Object.keys' src/app/servicios/\[slug\]/page.tsx` devuelve 0.
4. `grep -c 'data-content-body' src/app/servicios/\[slug\]/page.tsx` devuelve exactamente 1.
5. En `src/components/ui/author-byline.tsx`: `grep -c 'children' src/components/ui/author-byline.tsx` devuelve 0, `grep -Ec 'credential[A-Za-z]*\??:' src/components/ui/author-byline.tsx` devuelve 0, y `grep -c 'from "@/content/cv"' src/components/ui/author-byline.tsx` devuelve 1.
6. Ninguna cadena literal de colegiatura ni de registro de especialista está escrita a mano en JSX: `grep -c '83189' src/components/ui/author-byline.tsx` devuelve 0 y `grep -c '35310' src/components/ui/author-byline.tsx` devuelve 0. En cambio el HTML renderizado sí las muestra: `grep -c 'CMP 83189' .next/server/app/servicios/hernia-discal.html` devuelve 1 o más y `grep -c 'RNE 35310' .next/server/app/servicios/hernia-discal.html` devuelve 1 o más.
7. En el HTML renderizado hay exactamente un `<h1`, siete `<h2` con atributo `id` y `tabindex="-1"`, y un `<h2` más sin `id` (el del CTA de cierre).
8. `grep -c 'data-medical-disclaimer' .next/server/app/servicios/hernia-discal.html` devuelve exactamente 2, y la primera aparición ocurre antes de la primera aparición de `data-toc`, que a su vez ocurre antes del primer `<h2`.
9. `grep -c 'aria-labelledby="toc-title"' .next/server/app/servicios/hernia-discal.html` devuelve 1, y el `nav` contiene siete elementos `<li>`.
10. `grep -c 'outline-none' src/components/ui/table-of-contents.tsx src/components/ui/author-byline.tsx src/components/ui/medical-disclaimer.tsx` devuelve 0 en los tres archivos (A11Y-14).
11. La prosa del cuerpo (el contenido de `<article data-content-body>` descontando la firma, el aviso y la tabla de contenidos) tiene 900 palabras o más. Medición de referencia, la puerta formal llega en la tarea 3.
12. La declaración de `ServiceSection` y la de `ServicePage` en `src/content/service-pages.ts` no contienen ningún campo con estos nombres:

```
experiencia, miExperiencia, casos, testimonio, testimonios, garantia, sello,
stats, estadisticas, contador, cirugiasRealizadas, tasaExito, successRate,
porcentaje, rating, calificacion, precio, price, tarifa
```

13. Ni el HTML renderizado de la ruta ni `src/content/service-pages.ts` contienen ninguna de estas expresiones, comparadas en minúsculas y sin tildes:

```
en mi experiencia, mi experiencia, he operado, he tratado, he visto,
mis pacientes, en los casos que atiendo, en mi consulta, en mi practica,
yo opero, suelo operar
```

14. El HTML renderizado no contiene ninguna coincidencia de un dígito seguido de `%` (SAFE-08) ni de `S/` seguido de dígitos (SAFE-10), y ningún `h2` ni `h3` de la página contiene dígitos (SAFE-09).
15. `git diff --stat package.json` no reporta cambios en esta tarea.
  </acceptance_criteria>
  <done>`/servicios/hernia-discal` se prerenderiza como página estática, muestra las siete secciones en el orden fijo con 900 palabras o más de prosa, la firma y el aviso educativo se leen juntos arriba de la tabla de contenidos, y las siete anclas mueven el foco a su encabezado.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 2: banner del primer tercio, contraste de los CTA compartidos y tarjetas del hub</name>
  <read_first>
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, secciones "Color", "Superficies de la fase" seccion 1, "Banner del primer tercio", "POS-01" y "Claves nuevas de CtaLocation"
    - `src/components/ui/booking-cta.tsx`
    - `src/components/ui/whatsapp-cta.tsx`
    - `src/lib/site-config.ts`
    - `src/app/servicios/page.tsx`
  </read_first>
  <files>src/lib/site-config.ts, src/components/ui/booking-cta.tsx, src/components/ui/whatsapp-cta.tsx, src/components/ui/mid-content-cta.tsx, src/components/services/service-card.tsx, src/app/servicios/[slug]/page.tsx, src/app/servicios/page.tsx</files>
  <action>
Cerrar el circuito de conversión de la guía y engancharla al hub.

`src/lib/site-config.ts`. Sumar dos claves a `whatsappMessages`, `service_page` y `blog_post`, con los textos literales que fija el UI-SPEC en "Claves nuevas de CtaLocation". Dos claves y no ocho: una por condición fragmentaría las etiquetas de `trackWhatsAppClick(location)` en GA4 y volvería inútil comparar superficies. `CtaLocation` se amplía solo por derivación del objeto, sin tocar el tipo.

`src/components/ui/booking-cta.tsx`. Sumar `ring-1 ring-accent-dark` a la lista de clases base (COLOR-03). La superficie dorada da 2.27:1 contra el fondo de página, por debajo del 3:1 que exige un límite de control; el anillo de 1px en `--accent-dark` da 3.13:1 y lo resuelve. El cambio es de un componente compartido y aplica a todo el sitio de forma deliberada.

`src/components/ui/whatsapp-cta.tsx`. Sumar `ring-1 ring-accent-dark` únicamente al variante `accent`, por la misma razón. Además corregir el variante `outline`: su color de texto pasa a `text-primary-dark`, porque este plan lo estrena a 16px dentro del banner y COLOR-01 exige `--primary-dark` para todo texto de marca por debajo de 24px. El borde sigue en `border-primary`, que como límite de control necesita 3:1 y da 4.37:1. Los variantes `outline-inverse` y las clases de hover no se tocan.

`src/components/ui/mid-content-cta.tsx`. Server Component nuevo. Props `{ heading: string; body: string; location: Extract<CtaLocation, "service_page" | "blog_post"> }`. Nodo raíz `<aside data-mid-cta>` con superficie `bg-muted`, `border border-border`, la barra decorativa `border-l-4 border-l-accent`, `rounded-2xl p-6 sm:p-8` y `mt-12`. Dentro: el título como `<p className="font-heading text-lg font-bold text-foreground">`, nunca un encabezado, para que no aparezca en la tabla de contenidos ni contamine el esquema; el cuerpo en `text-foreground/80`; y una fila `flex flex-wrap items-center gap-4` con `BookingCta` cuyo texto es "Agendar consulta" y `WhatsAppCta variant="outline"` cuyo texto es "Escribir por WhatsApp", recibiendo `location`. El destino principal es `/agendar`, no el chat directo: tres de las cuatro sedes las agenda la clínica y no el doctor, que es la misma razón ya documentada dentro de `BookingCta`. La barra dorada es decorativa y no comunica estado (COLOR-04). El componente no declara animación propia (A11Y-27).

`src/components/services/service-card.tsx`. Server Component nuevo. Props `{ page: Pick<ServicePage, "slug" | "navLabel" | "cardSummary"> }`. Marcado exacto del UI-SPEC, sección "Superficies de la fase" 1: `<article>` con `position: relative`, borde, `rounded-2xl`, `p-6`, `shadow-sm`, `transition-shadow duration-200 hover:shadow-md` y `focus-within:outline focus-within:outline-[3px] focus-within:outline-offset-2 focus-within:outline-ring`. Dentro hay un único elemento interactivo: el `Link` que envuelve el `h3` y se extiende con `after:absolute after:inset-0`, con `focus-visible:outline-none` limitado a ese enlace porque el indicador se traslada a la tarjeta entera (A11Y-21, A11Y-23). Prohibido un segundo enlace o botón dentro de la tarjeta. El nombre accesible del enlace es el nombre de la condición, no "Leer más" (A11Y-22). El pie de la tarjeta es un `<p>` no interactivo con el texto "Leer la guía" y `ArrowRight` de Lucide con `aria-hidden="true"`, en `text-primary-dark`. Sin dorado en la tarjeta.

`src/app/servicios/[slug]/page.tsx`. Insertar `MidContentCta` inmediatamente después de la sección `sintomas`, que es la segunda de las siete (POS-01), alimentado por `page.ctaBanner` y con `location="service_page"`. Un solo banner por página, más el CTA de cierre que ya existe: nunca tres.

`src/app/servicios/page.tsx`. Insertar una sección nueva inmediatamente después del párrafo introductorio y antes del bloque que lista `serviceCategories`. Va arriba porque las URLs hijas son el motivo de existir de la fase. Contiene un `h2` con el texto "Condiciones que explico en detalle" y una grilla `grid items-stretch gap-6 sm:grid-cols-2` que mapea `servicePages` a `ServiceCard`. Una sola columna hasta `sm` (A11Y-26). La grilla tiene que verse bien con tres, cuatro o cinco tarjetas. Guardia de regresión dura: los `id` de sección del hub no se tocan ni se renombran, porque `src/app/page.tsx` enlaza a tres de ellos y a `#procedimientos` desde un cuarto lugar, y `src/app/sobre-el-doctor/page.tsx` enlaza a `#procedimientos`. Tampoco se toca `scroll-mt-28` ni ningún otro contenido heredado de v1.0.
  </action>
  <verify>
    <automated>npm run build && npm run lint && node -e "const h=require('fs').readFileSync('.next/server/app/servicios.html','utf8');for(const id of ['columna','traumatologia','ortopedia-infantil','procedimientos'])if(!h.includes('id=\"'+id+'\"'))throw new Error('ancla perdida: '+id);if(!h.includes('/servicios/hernia-discal'))throw new Error('falta la tarjeta del hub');console.log('hub ok')"</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` y `npm run lint` terminan con código 0.
2. `grep -c 'service_page' src/lib/site-config.ts` devuelve 1 y `grep -c 'blog_post' src/lib/site-config.ts` devuelve 1.
3. `grep -c 'ring-accent-dark' src/components/ui/booking-cta.tsx` devuelve 1 y `grep -c 'ring-accent-dark' src/components/ui/whatsapp-cta.tsx` devuelve 1.
4. En `src/components/ui/whatsapp-cta.tsx` el variante `outline` usa `text-primary-dark`: `grep -c 'text-primary-dark' src/components/ui/whatsapp-cta.tsx` devuelve 1 o más.
5. `grep -c 'outline-offset-0' src/components/ui/mid-content-cta.tsx src/components/services/service-card.tsx` devuelve 0 en ambos, y ningún archivo de la tarea reduce `outline-offset` a cero (A11Y-17).
6. `grep -c 'data-mid-cta' .next/server/app/servicios/hernia-discal.html` devuelve exactamente 1.
7. En el HTML de `/servicios/hernia-discal` la aparición de `data-mid-cta` está después de `id="sintomas"` y antes de `id="cuando-consultar"`.
8. `.next/server/app/servicios.html` contiene `id="columna"`, `id="traumatologia"`, `id="ortopedia-infantil"` e `id="procedimientos"`.
9. `.next/server/app/servicios.html` contiene `href="/servicios/hernia-discal"` exactamente una vez.
10. En `src/components/services/service-card.tsx` hay exactamente un elemento interactivo por tarjeta: `grep -Ec '<(Link|a|button)' src/components/services/service-card.tsx` devuelve 1.
11. `grep -c 'min-h-11' src/components/ui/table-of-contents.tsx` devuelve 1 o más, y `BookingCta` y `WhatsAppCta` en tamaño `md` conservan `py-3` sobre `text-base`, es decir 48px de alto (A11Y-18).
12. `grep -Ec 'bg-accent|text-accent' src/components/services/service-card.tsx` devuelve 0: el dorado no aparece en las tarjetas del hub.
13. `git diff --stat package.json` no reporta cambios en esta tarea.
  </acceptance_criteria>
  <done>Desde `/servicios` se llega a la guía de hernia discal con un clic, la guía ofrece agendar dentro del primer tercio y al cierre, los cuatro anclajes vivos del hub siguen resolviendo, y los CTA dorados de todo el sitio distinguen su superficie del fondo.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 3: schema MedicalWebPage, sitemap y puerta automática de contenido</name>
  <read_first>
    - `src/components/structured-data.tsx` completo, las 423 líneas, antes de escribir una sola línea de schema
    - `src/app/sitemap.ts`
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, sección "Datos estructurados" y sección "Salvaguardas de contenido firmado"
    - `src/app/preguntas-frecuentes/page.tsx` para ver cómo se invoca hoy `FaqJsonLd`
  </read_first>
  <files>src/components/structured-data.tsx, src/app/sitemap.ts, src/app/servicios/[slug]/page.tsx, scripts/check-content.mjs, package.json</files>
  <action>
Hacer la guía descubrible y convertir las salvaguardas del contrato en una puerta ejecutable.

`src/components/structured-data.tsx`. Extender, nunca duplicar (CTX-19). Reusar los helpers `ID`, `abs` y `JsonLdScript` que ya están en el archivo; `JsonLdScript` es el único punto del repositorio que inyecta HTML crudo y ya serializa con `JSON.stringify` escapando el signo de menor que, así que todo schema nuevo pasa por ahí y no se agrega ningún otro punto de inyección. Exportar `MedicalWebPageJsonLd` que recibe los campos de una `ServicePage` y emite `@type: "MedicalWebPage"` con `@id` en `/servicios/{slug}#page`, `url`, `name`, `description`, `inLanguage: "es-PE"`, `isPartOf` apuntando al `@id` del sitio, `author` y `publisher` apuntando al `@id` del doctor, `datePublished` desde `publishedAt`, `dateModified` desde `updatedAt`, `specialty`, `mainEntityOfPage` y `about` de tipo `MedicalCondition` con `name` desde `conditionName` y `alternateName` desde `alternateNames` cuando exista (CTX-18). Cuando `describesSurgery` es verdadero, y solo entonces, referenciar los procedimientos ya existentes por sus `@id` generados con `ID.procedure(slug)`, tanto en `about.possibleTreatment` como en `mentions`: no se declara ningún procedimiento nuevo. El marcado no puede afirmar que un médico revisó la página, porque a la fecha de publicación no la revisó; los nombres de propiedad concretos que quedan prohibidos están en `<acceptance_criteria>`. Las fechas del schema salen de los mismos campos que muestra `AuthorByline`, así que lo que dice el marcado y lo que ve el paciente no divergen. Parametrizar `FaqJsonLd` con una prop opcional para el path de la página, con valor por defecto igual al actual, de modo que `/preguntas-frecuentes` siga emitiendo exactamente el mismo `@id` que hoy y las páginas de servicio emitan el suyo.

`src/app/servicios/[slug]/page.tsx`. Emitir, antes del `article`, `MedicalWebPageJsonLd`, `BreadcrumbJsonLd` con la ruta anidada de dos niveles (Servicios y luego el `navLabel` de la página) y `FaqJsonLd` alimentado desde las subsecciones de `preguntas-frecuentes`, solo si esa sección tiene preguntas. Cada `JsonLdScript` de la página lleva un `id` de DOM distinto. Los breadcrumbs visibles siguen siendo de la fase 10 y no se agregan acá.

`src/app/sitemap.ts`. Derivar una entrada por cada `servicePage`, igual que ya se hace con `blogPosts`, con `lastModified` desde `updatedAt`, `changeFrequency` mensual y prioridad 0.8. La ruta `/privacidad` sigue fuera del sitemap por la decisión D-10 de la fase 7 y ese comentario no se toca.

`scripts/check-content.mjs`. Script de Node sin dependencias, ejecutable con `node scripts/check-content.mjs [ruta ...]`. Lee el HTML prerenderizado que deja `npm run build` en `.next/server/app/`, mapeando la ruta `/servicios/hernia-discal` al archivo `.next/server/app/servicios/hernia-discal.html`. Si un archivo esperado no existe, falla con un mensaje que pide correr `npm run build` primero: nunca se degrada a un resultado aprobado. Sin argumentos corre el manifiesto completo; con argumentos corre solo esas rutas. Sale con código 0 si todo pasa y 1 si algo falla, imprimiendo una tabla por ruta con el conteo de palabras y la lista de fallas.

Manifiesto interno: las cuatro rutas de servicio y los cuatro posts, cada una con su tipo, su mínimo de palabras en 900 y, en el caso de los posts, la ruta de servicio a la que debe enlazar según el mapa de CTX-12. Más el hub `/servicios`, que se verifica siempre aunque no se pase como argumento.

Definición del cuerpo: el script extrae desde `<article data-content-body` hasta el último `</article>` del documento, y de ahí descuenta el nodo con `data-author-byline`, los nodos con `data-medical-disclaimer` y el `nav` con `data-toc`. Lo que queda es el cuerpo. Las palabras se cuentan sobre ese texto ya sin etiquetas ni entidades. El conteo de 900 se mide sobre el cuerpo, no sobre el documento entero, para que el mínimo no se cumpla con texto de mobiliario.

Comprobaciones por ruta: cuerpo con 900 palabras o más; exactamente un `h1`; secuencia de encabezados sin saltos de nivel, es decir ningún `h3` sin un `h2` antes y ningún `h4`; si hay tres o más `h2` con `id` y `tabindex="-1"`, existe un `nav` con `aria-labelledby="toc-title"` y la cantidad de entradas de la lista coincide exactamente con la cantidad de esos `h2`, y el texto de cada entrada es idéntico carácter por carácter al del `h2` de destino; cada `href` de ancla de la tabla tiene un elemento con ese `id` y con `tabindex="-1"`; exactamente una aparición de `data-mid-cta`, con las palabras de cuerpo anteriores al banner entre el 15% y el 35% del total del cuerpo (POS-01); exactamente dos apariciones de `data-medical-disclaimer`, la primera antes del `nav` de la tabla de contenidos y antes del primer `h2` (SAFE-01, SAFE-02); exactamente una aparición de `data-author-byline`, antes del primer `data-medical-disclaimer`, con las cadenas de colegiatura y de registro de especialista y con dos elementos `time` que llevan atributo de fecha (SAFE-06, SAFE-07); al menos dos enlaces a `/agendar` en la página, uno del banner y otro del cierre (CTX-05, CTX-16); en las rutas de servicio, un enlace de vuelta a `/servicios`; en los posts, un enlace a la ruta de servicio que le asigna el manifiesto (CTX-12).

Comprobaciones de salvaguarda por ruta: cero coincidencias de un dígito seguido del signo de porcentaje y cero de la abreviatura de soles seguida de dígitos (SAFE-08, SAFE-10); en las rutas de servicio, ningún `h2` ni `h3` con dígitos y ninguna etiqueta de énfasis fuerte dentro del cuerpo (SAFE-09); cero coincidencias de la constante `FIRST_PERSON_CLAIMS` sobre el texto del cuerpo normalizado a minúsculas y sin tildes (SAFE-05).

Comprobaciones de código fuente, que corren siempre: sobre `src/content/service-pages.ts` y, cuando exista, `src/content/blog.ts`, cero coincidencias de `FIRST_PERSON_CLAIMS`; y sobre las declaraciones de tipo de esos archivos, cero campos cuyo nombre esté en la constante `FORBIDDEN_FIELDS`. La comprobación de campos se hace sobre los nombres de propiedad declarados dentro de los bloques de tipo, no sobre el texto libre, para que una palabra corriente dentro de la prosa nunca dispare un falso positivo.

Comprobaciones globales: `.next/server/app/sitemap.xml.body` contiene la URL de cada ruta pasada como argumento, y cuando el script corre sin argumentos el total de entradas del sitemap es exactamente 16; `.next/server/app/servicios.html` conserva los cuatro `id` de sección heredados de v1.0, y contiene el enlace de tarjeta hacia cada ruta de servicio verificada, y cuando el script corre sin argumentos contiene las cuatro.

Las dos constantes `FIRST_PERSON_CLAIMS` y `FORBIDDEN_FIELDS` se escriben con exactamente las expresiones y los nombres enumerados en `<acceptance_criteria>` de esta tarea.

`package.json`. Sumar el script `"content:check": "node scripts/check-content.mjs"`. Ni `dependencies` ni `devDependencies` cambian: esta fase no instala paquetes.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-content.mjs /servicios/hernia-discal</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` termina con código 0 y `node scripts/check-content.mjs /servicios/hernia-discal` termina con código 0.
2. `node scripts/check-content.mjs /servicios/hernia-discal` imprime un conteo de palabras de cuerpo de 900 o más para esa ruta.
3. Con el archivo `.next/server/app/servicios/hernia-discal.html` renombrado temporalmente, `node scripts/check-content.mjs /servicios/hernia-discal` termina con código 1 y un mensaje que menciona `npm run build`. El archivo se restituye después de la prueba. Esto verifica que la puerta no aprueba por ausencia de evidencia.
4. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve 13, es decir las 12 URLs previas más la ruta de hernia discal.
5. `.next/server/app/servicios/hernia-discal.html` contiene `"@type":"MedicalWebPage"` y `"@type":"MedicalCondition"`.
6. El HTML de `/servicios/hernia-discal` no contiene `reviewedBy` ni `lastReviewed`: el schema no afirma revisión médica.
7. `grep -c 'dangerouslySetInnerHTML' src/components/structured-data.tsx` devuelve exactamente 1, la que ya vive dentro del helper existente, y `grep -rc 'dangerouslySetInnerHTML' src/app/servicios src/components/ui src/components/services` devuelve 0 en todos los archivos.
8. `.next/server/app/preguntas-frecuentes.html` sigue conteniendo el `@id` de FAQ en `/preguntas-frecuentes#faq`, es decir la parametrización no cambió la página existente.
9. `scripts/check-content.mjs` declara la constante `FIRST_PERSON_CLAIMS` con exactamente estas expresiones:

```
en mi experiencia
mi experiencia
he operado
he tratado
he visto
mis pacientes
en los casos que atiendo
en mi consulta
en mi practica
yo opero
suelo operar
```

10. `scripts/check-content.mjs` declara la constante `FORBIDDEN_FIELDS` con exactamente estos nombres:

```
experiencia, miExperiencia, casos, testimonio, testimonios, garantia, sello,
stats, estadisticas, contador, cirugiasRealizadas, tasaExito, successRate,
porcentaje, rating, calificacion, precio, price, tarifa
```

11. `git diff package.json | grep -E '^\+' | grep -Ec '"(next|react|react-dom|resend|zod|lucide-react|clsx|tailwind-merge|typescript|eslint|tailwindcss)"'` devuelve 0: no se agregó ni cambió ninguna dependencia.
12. `npm run content:check /servicios/hernia-discal` termina con código 0.
  </acceptance_criteria>
  <done>`/servicios/hernia-discal` está en el sitemap, emite `MedicalWebPage` con `about` de tipo `MedicalCondition` sin afirmar revisión médica, y `scripts/check-content.mjs` verifica sola las ocho familias de criterios del contrato sobre esa ruta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/content/*.ts` en build a HTML estático publicado | Prosa clínica redactada por el agente que sale firmada por un médico colegiado. No hay entrada de usuario, pero sí riesgo de afirmación falsa sobre salud. |
| Objetos de schema a `JsonLdScript` a `dangerouslySetInnerHTML` | Único punto del repositorio que inyecta HTML crudo. Entrada: objetos construidos en tiempo de build. |
| Google Places API a `getGoogleReviews()` a nodo `Physician` del grafo raíz | Texto de terceros que ya viaja dentro del mismo mecanismo de script. Esta fase no lo modifica. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `JsonLdScript` en `src/components/structured-data.tsx` | medium | mitigate | Todo el schema nuevo pasa por el helper existente, que serializa con `JSON.stringify` y escapa el signo de menor que. Criterio 7 de la tarea 3 exige una sola aparición de inyección de HTML crudo en todo el archivo y cero en los componentes y rutas nuevos. |
| T-08-02 | Information disclosure | Prosa clínica firmada en `src/content/service-pages.ts` | high | mitigate | `scripts/check-content.mjs` verifica SAFE-05, SAFE-08, SAFE-09 y SAFE-10 sobre el HTML renderizado y sobre el archivo de datos. `AuthorByline` no acepta `children` ni credenciales por props, así que el hueco para una afirmación libre no existe. |
| T-08-03 | Spoofing | `MedicalWebPageJsonLd` con `author` y `publisher` del doctor | medium | mitigate | El marcado no declara revisión médica (criterio 6 de la tarea 3) y el aviso educativo viaja pegado a la firma, arriba del pliegue. |
| T-08-04 | Tampering | Anclas `href="#id"` de la tabla de contenidos | low | accept | Los `id` salen de una tupla cerrada declarada en código, nunca de entrada de usuario. No hay superficie de inyección. |
| T-08-05 | Information disclosure | Reseñas de Google en el grafo raíz del layout | low | accept | Superficie preexistente de v1.0, sin cambios en esta fase. Se declara porque comparte el mecanismo de script. |
| T-08-SC | Tampering | Instalaciones de npm | high | mitigate | La fase no instala paquetes. Criterio 11 de la tarea 3 exige que `dependencies` y `devDependencies` de `package.json` queden sin cambios, solo se agrega una entrada de `scripts`. No se requiere auditoría de legitimidad porque no hay paquete nuevo. |
</threat_model>

## Artifacts this phase produces

Aporte de este plan.

**Archivos nuevos**
- `src/content/service-pages.ts`
- `src/app/servicios/[slug]/page.tsx`
- `src/components/ui/author-byline.tsx`
- `src/components/ui/medical-disclaimer.tsx`
- `src/components/ui/table-of-contents.tsx`
- `src/components/ui/mid-content-cta.tsx`
- `src/components/services/service-card.tsx`
- `scripts/check-content.mjs`

**Rutas nuevas**
- `/servicios/hernia-discal`

**Componentes nuevos**
- `AuthorByline({ publishedAt, updatedAt })`
- `MedicalDisclaimer()`
- `TableOfContents({ title, entries })`
- `MidContentCta({ heading, body, location })`
- `ServiceCard({ page })`
- `MedicalWebPageJsonLd({ page })` en `src/components/structured-data.tsx`

**Tipos y constantes nuevas**
- `SERVICE_SECTION_ORDER` (tupla `as const` de siete identificadores)
- `ServiceSectionId`
- `ServiceSubsection`
- `ServiceSection`
- `ServicePage`
- `servicePages`
- `getServicePage(slug)`
- `FIRST_PERSON_CLAIMS` y `FORBIDDEN_FIELDS` en `scripts/check-content.mjs`

**Claves nuevas de `CtaLocation`**
- `service_page`
- `blog_post`

**Atributos de datos que la puerta usa como anclaje**
- `data-content-body`, `data-author-byline`, `data-medical-disclaimer`, `data-toc`, `data-mid-cta`

**Script de npm nuevo**
- `content:check`

<verification>
1. `npm run build` termina con código 0 y `npm run lint` termina con código 0.
2. `node scripts/check-content.mjs /servicios/hernia-discal` termina con código 0.
3. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve 13.
4. `.next/server/app/servicios.html` conserva `id="columna"`, `id="traumatologia"`, `id="ortopedia-infantil"` e `id="procedimientos"`, y suma `href="/servicios/hernia-discal"`.
5. `git diff package.json | grep -E '^\+' | grep -Ec '"(next|react|react-dom|resend|zod|lucide-react|clsx|tailwind-merge|typescript|eslint|tailwindcss)"'` devuelve 0.
6. `.next/server/app/preguntas-frecuentes.html` y `.next/server/app/blog.html` se siguen generando sin cambios de estructura.
</verification>

<success_criteria>
- `/servicios/hernia-discal` se prerenderiza estática, con 900 palabras o más de prosa de cuerpo y las siete secciones en el orden de `SERVICE_SECTION_ORDER`.
- La firma y el aviso educativo se leen juntos arriba de la tabla de contenidos, y el aviso se repite al cierre.
- Las siete anclas mueven el foco a su `h2` y ninguna queda debajo del header sticky.
- El hub enlaza a la guía y conserva sus cuatro `id` de sección vivos.
- El banner de agenda cae entre el 15% y el 35% del cuerpo y hay un CTA de cierre.
- El sitemap declara la ruta nueva y el HTML emite `MedicalWebPage` con `about` de tipo `MedicalCondition`.
- `scripts/check-content.mjs` pasa sobre la ruta nueva y falla si el build no existe.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-01-SUMMARY.md` when done
</output>
