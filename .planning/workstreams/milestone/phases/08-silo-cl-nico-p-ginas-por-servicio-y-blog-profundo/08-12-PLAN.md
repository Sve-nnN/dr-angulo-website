---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 12
type: execute
wave: 3
depends_on: ["08-05"]
files_modified:
  - src/content/service-pages/escoliosis-y-deformidades.ts
  - src/content/service-pages/escoliosis.ts
  - src/content/service-pages/index.ts
  - scripts/check-content.mjs
autonomous: true
requirements: [SVC-03]
must_haves:
  truths:
    - "Quien busca `escoliosis` en Lima encuentra una guía clínica de más de 2200 palabras con las ocho secciones del esqueleto de v1.2"
    - "La guía vive en `/servicios/escoliosis-y-deformidades`, que es el slug que el mapa de la fase 14 decidió, y el slug viejo queda listo para su 301"
    - "El sitemap, el hub y `llms.txt` nombran el slug nuevo sin que ninguno de ellos haya tenido que cambiar de código"
  artifacts:
    - src/content/service-pages/escoliosis-y-deformidades.ts
  key_links:
    - "Todo lo que enumera las páginas del silo lo hace recorriendo `servicePages`, así que renombrar el `slug` en el archivo de datos arrastra sitemap, hub, `llms.txt` y las imágenes OG sin tocarlos"
    - "El 301 desde `/servicios/escoliosis` lo pone el plan 08-13, junto con los otros dos, y por eso este plan va antes"
---

<objective>
Reescribir la guía de escoliosis con el copy aprobado y moverla al slug que el mapa de
v1.2 decidió.

Dos cambios en la misma página y por eso van juntos: el cuerpo pasa a las 2338 palabras
del paquete con el esqueleto de guía clínica, y la URL pasa de `/servicios/escoliosis` a
`/servicios/escoliosis-y-deformidades`. Separarlos obligaría a publicar dos veces la misma
página y a redirigir dos veces.

Purpose: cerrar SVC-03 y dejar el slug viejo listo para el 301 que pone 08-13.
Output: `src/content/service-pages/escoliosis-y-deformidades.ts` con las ocho secciones
canónicas, el módulo viejo retirado y la puerta apuntando a la ruta nueva.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: mover el módulo al slug nuevo y transcribir el copy aprobado</name>
  <files>src/content/service-pages/escoliosis-y-deformidades.ts, src/content/service-pages/escoliosis.ts, src/content/service-pages/index.ts</files>
  <read_first>
    - .planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/servicios-escoliosis-y-deformidades.md **completo**: jerarquía, entidades y el bloque entre `copy:inicio` y `copy:fin`
    - seo-tools/data/copy-guias.json, entrada de `/servicios/escoliosis-y-deformidades`: trae el valor `clave` de cada sección
    - src/content/service-pages/escoliosis.ts y src/content/service-pages/index.ts tal como los dejó 08-05
    - src/content/sections.ts
    - .planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md, la tabla de las tres redirecciones
  </read_first>
  <action>
    Renombra `src/content/service-pages/escoliosis.ts` a
    `src/content/service-pages/escoliosis-y-deformidades.ts` con `git mv`, para que el
    historial del archivo sobreviva, y actualiza el import de
    `src/content/service-pages/index.ts`. La posición de la entrada dentro de
    `servicePages` no cambia: el orden del arreglo es el orden de las tarjetas del hub.

    Dentro del módulo, `slug` pasa de `escoliosis` a `escoliosis-y-deformidades`. Ese es
    el único lugar donde la URL está escrita: el sitemap, el hub, `llms.txt` y las
    imágenes OG la derivan recorriendo `servicePages`, así que ninguno de esos archivos se
    toca. Si al terminar alguno de ellos aparece en el diff, algo se hizo a mano que no
    correspondía.

    Reemplaza el arreglo de secciones por el del paquete: ocho `h2` con los `id`
    canónicos `que-es`, `sintomas`, `causas`, `diagnostico`, `sin-operar`, `cirugia`,
    `preguntas-frecuentes`, `cuando-consultar`, más los `h3` cuyos `id` salen del campo
    `clave` del dataset.

    El texto se transcribe **literal** del bloque de copy. No se parafrasea, no se resume
    y no se reordena. Lo aprobado es ese texto (aprobación del 2026-08-13,
    `15-APROBACION-DOCTOR.md`). Las líneas de cita que anuncian bloques pendientes son
    andamiaje del generador y no se transcriben.

    `heroLead`, `cardSummary` y `ctaBanner` se conservan. `h1` se actualiza con el valor
    de la tabla del paquete, porque el campo `h1` es territorio de esta fase. `title` y
    `description` no se tocan: fase 10. `alternateNames` y `describesSurgery` no cambian.
    `updatedAt` pasa a la fecha de ejecución.

    Busca en `src/` cualquier referencia escrita a mano al slug viejo con
    `grep -rn "servicios/escoliosis\"" src/` y también sin comilla final, y corrígela. Si
    aparece dentro de una cadena de contenido, se corrige igual: un enlace interno que
    apunta a una URL que va a redirigir gasta un salto por cada visita.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` genera `.next/server/app/servicios/escoliosis-y-deformidades.html` y ya no genera el del slug viejo.
    - `grep -rn "servicios/escoliosis\b" src/ | grep -v "escoliosis-y-deformidades"` no devuelve nada.
    - El sitemap generado lista `/servicios/escoliosis-y-deformidades` y no lista el slug viejo.
    - `git diff --stat` toca solo los tres archivos declarados: el renombre queda registrado como renombre y no como borrado más alta.
    - `git log --follow src/content/service-pages/escoliosis-y-deformidades.ts` muestra la historia previa del archivo.
  </acceptance_criteria>
  <done>La guía vive en el slug nuevo con el copy aprobado completo y nada del resto del código tuvo que cambiar.</done>
</task>

<task type="auto">
  <name>Tarea 2: la puerta vigila la ruta nueva</name>
  <files>scripts/check-content.mjs</files>
  <read_first>
    - scripts/check-content.mjs, el manifiesto de rutas que dejó 08-05
  </read_first>
  <action>
    En el manifiesto de la puerta, cambia la entrada de `/servicios/escoliosis` por
    `/servicios/escoliosis-y-deformidades`, conservando `format: guia-clinica` y el
    mínimo de palabras que la entrada declare. No se agrega la ruta vieja como entrada
    aparte: deja de existir, y una entrada apuntando a un HTML que ya no se genera haría
    fallar la puerta por una razón que no es la que interesa.

    Sube el mínimo de palabras de esta entrada a 2200, que es lo que el paquete entrega.
    Un mínimo que quedó calibrado para el texto de v1.0 deja de proteger nada cuando el
    cuerpo triplicó su tamaño.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-content.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-content.mjs` sale en 0 sobre todas las rutas del manifiesto, incluida la nueva.
    - Prueba en rojo: se baja a mano una sección del HTML prerenderizado de la guía para dejarla bajo el mínimo, la puerta sale en 1 nombrando la ruta, se restaura y vuelve a verde. Las dos corridas se transcriben en el SUMMARY.
    - `grep -c "servicios/escoliosis\"" scripts/check-content.mjs` devuelve 0.
  </acceptance_criteria>
  <done>La puerta vigila la ruta nueva con el mínimo que corresponde al copy publicado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy aprobado → sitio en producción | Texto clínico YMYL publicado bajo la firma del doctor |
| slug publicado → enlaces existentes | Cambiar la URL rompe todo enlace entrante hasta que exista el 301 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-34 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal del paquete sellado |
| T-08-35 | Denial of service | URL vieja sin destino | high | mitigate | El 301 entra en 08-13, en la ola siguiente y antes de cualquier despliegue de la fase; el SUMMARY deja anotado que hasta entonces el slug viejo responde 404 en local |
| T-08-36 | Tampering | enlaces internos al slug viejo | medium | mitigate | Barrido con grep sobre `src/` y criterio de aceptación que exige cero coincidencias |
| T-08-SC | Tampering | instalaciones npm | high | mitigate | Cero paquetes nuevos |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` en 0.
- `node scripts/check-content.mjs` en 0 con la ruta nueva y el mínimo de 2200 palabras.
- Cero referencias al slug viejo dentro de `src/`.
</verification>

<success_criteria>
La guía de escoliosis y deformidades publica las 2338 palabras aprobadas en su slug
definitivo, y el slug viejo queda listo para recibir su 301.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-12-SUMMARY.md` when done
</output>
