---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 12
type: execute
wave: 5
depends_on: ["08-05", "08-16"]
files_modified:
  - src/content/blog/artrosis.ts
  - src/content/blog/index.ts
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
  - scripts/check-seo.mjs
autonomous: true
requirements: [BLOG-02, BLOG-03]
must_haves:
  truths:
    - "/blog/artrosis responde 200 con la guía clínica aprobada del paquete, más de 1700 palabras y las ocho secciones canónicas."
    - "La ruta entra al sitemap, al listado del blog y a su imagen de Open Graph sin que ninguno de esos archivos se toque a mano."
    - "El post no declara guía de destino, porque la matriz de enlazado de v1.2 no le asigna ninguna mención inversa."
  artifacts:
    - src/content/blog/artrosis.ts
  key_links:
    - "src/content/blog/index.ts es el único registro que hay que tocar: sitemap, listado y Open Graph derivan de blogPosts."
    - "SITEMAP_TOTAL sube de 22 a 23 en las tres puertas que lo declaran."
---

<objective>
Crear `/blog/artrosis`, que hoy no existe.

No es la expansión de nada. El mapa de keywords de v1.2 la creó porque `artrosis` es una
cabeza con SERP medida en Lima y ninguna URL del sitio podía ganarla. El copy viene
redactado entero: 1789 palabras, ocho secciones de nivel 2 y once de nivel 3.

Este plan reemplaza por completo el alcance que `08-12` tenía antes de la consolidación
del set. Aquella versión duplicaba el renombre y la reescritura de escoliosis, que ya
son la Task 1 y la Task 2 del plan 08-07; ejecutar las dos habría escrito dos veces el
mismo módulo y el mismo bloque de redirecciones.

Purpose: cerrar la mitad de BLOG-02 que corresponde a las URLs que el paquete manda
crear, no a las que manda reescribir.
Output: el módulo del post, el registro actualizado y el conteo de URLs corrido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-06-PLAN.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-05-SUMMARY.md
</context>

<protocolo_de_transcripcion>
Rige el mismo protocolo escrito en `08-06-PLAN.md`, con la fuente en
`seo-tools/data/copy-blog.json`. Los ids salen del campo `clave` copiado literal, el texto
del arreglo `parrafos`, y las líneas de sello de revisión que el generador intercala en la
versión Markdown no se transcriben. La autorización para publicar es
`15-APROBACION-DOCTOR.md`, 2026-08-13.

Con una diferencia que este plan sí tiene y los de reescritura no: al ser una URL nueva
necesita `title`, `description` y `h1` para poder existir, y los tres salen del mismo
objeto del dataset (`title`, `metaDescription`, `h1`), que es la misma fuente que va a leer
la fase 10. Así las dos fases no divergen.
</protocolo_de_transcripcion>

<tasks>

<task type="auto">
  <name>Task 1: el módulo del post de artrosis</name>
  <files>src/content/blog/artrosis.ts, src/content/blog/index.ts</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/blog-artrosis.md` entero, incluida la tabla "Title, meta y H1".
    - En `seo-tools/data/copy-blog.json`, el objeto cuya `url` es `/blog/artrosis`: sus 19 secciones, 8 de nivel 2 y 11 de nivel 3, y sus 5 `enlacesPropuestos`.
    - `src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts` como molde de un módulo de post ya migrado al modelo nuevo.
    - `src/content/blog/types.ts` y `src/content/blog/index.ts` tal como los dejó el plan 08-05.
  </read_first>
  <action>
Crear el módulo con las 19 secciones del paquete, 8 de nivel 2 (`que-es`, `sintomas`, `causas`,
`diagnostico`, `sin-operar`, `cirugia`, `preguntas-frecuentes`, `cuando-consultar`) y 11 de nivel
3, aplicando el protocolo de transcripción. `format` es `"guia-clinica"`, `publishedAt` y
`updatedAt` van a `2026-08-13`.

`relatedService` **no se declara**. La matriz de enlazado de v1.2 no le asigna a esta URL ninguna
mención inversa hacia una guía del silo, y forzarle una guía de destino sería inventar una
relación que nadie midió. El campo quedó opcional en el plan 08-05 justamente por este caso, y la
puerta dejó de exigir el enlace de salida cuando la entrada del MANIFEST no declara `linksTo`.
Sus salidas son las 5 de `enlacesPropuestos`: tres de vecindad temática hacia los otros posts y
dos de navegación hacia el blog y el hub. Van en `outboundLinks` con su `href` y su `anchor`
literales.

`intro` queda vacío. El paquete no redacta una entrada aparte: el primer nivel 2 abre la página.
Escribir una introducción propia sería publicar prosa clínica que el doctor no aprobó.

`ctaBanner` se arma con frases tomadas del cuerpo aprobado, recortadas y no reescritas, siguiendo
el contrato de copy de la UI-SPEC: segunda persona, verbo más sustantivo en el CTA, cero promesa
de resultado, cero signos de admiración.

Sumarlo a `blogPosts` en `index.ts`, después de los cuatro existentes.

Ojo con la sección de tratamiento: el paquete responde a las búsquedas de pastillas y de
vitaminas sin nombrar principio activo ni dosis. Esa contención es deliberada y se transcribe tal
cual.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-content.mjs /blog/artrosis</automated>
  </verify>
  <acceptance_criteria>
    - El módulo declara 19 secciones, 8 con `level: 2` y 11 con `level: 3`, y `format` es `"guia-clinica"`.
    - `npm run build` genera `.next/server/app/blog/artrosis.html` y su imagen de Open Graph.
    - El HTML supera las 1700 palabras de cuerpo y la puerta corre en verde sobre la ruta.
    - El módulo no declara `relatedService`, y el HTML no emite ningún enlace de salida hacia una guía ni un `about` vacío en el marcado de artículo.
    - `outboundLinks` declara 5 entradas con los anchors literales del dataset.
    - `grep -c "Pendiente de aprobación" src/content/blog/artrosis.ts` devuelve 0.
    - `.next/server/app/blog.html` lista el post y el sitemap prerenderizado declara la ruta.
  </acceptance_criteria>
  <done>El post de artrosis existe, publica el copy aprobado y no le inventa una guía de destino que la matriz no le dio.</done>
</task>

<task type="auto">
  <name>Task 2: el conteo de URLs sube a 23 en las tres puertas</name>
  <files>scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `scripts/check-content.mjs`, la constante `SITEMAP_TOTAL` con su comentario y el `MANIFEST`.
    - `scripts/check-sedes.mjs` y `scripts/check-seo.mjs`, cada una con su propia constante `SITEMAP_TOTAL`.
    - `08-16-SUMMARY.md`, que dejó las tres constantes en 22 al sumar la página de cirugía mínimamente invasiva.
  </read_first>
  <action>
Subir `SITEMAP_TOTAL` de 22 a 23 en las tres puertas. El valor está repetido a propósito en tres archivos, `check-content.mjs:35`,
`check-sedes.mjs:62` y `check-seo.mjs:35`, y una puerta que se olvida del otro sale roja sin que nadie haya roto nada: por eso las
dos se tocan en el mismo commit.

Sumar `/blog/artrosis` al `MANIFEST` de `scripts/check-content.mjs` con formato de guía clínica y
**sin** `linksTo`, que es lo que habilita al post a no empujar hacia el silo.

Actualizar el comentario que acompaña a cada constante para que diga de dónde sale el número, no
solo cuál es. En este punto de la fase son las 21 de la fase 9, más la página de cirugía
mínimamente invasiva del plan 08-16, más este post. El plan 08-13 lo sube a 24 y el 08-14 lo baja
a 22 al apagar los dos posts absorbidos.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "SITEMAP_TOTAL = 23" scripts/check-content.mjs scripts/check-sedes.mjs scripts/check-seo.mjs` devuelve 1 en los tres archivos.
    - El sitemap prerenderizado tiene exactamente 23 entradas.
    - Las tres puertas corren en verde, incluida `check-content.mjs` en corrida completa sobre todo el MANIFEST.
  </acceptance_criteria>
  <done>Las tres puertas están en verde con el conteo que corresponde a esta altura de la fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete de v1.2 → contenido publicado | Texto clínico YMYL que sale con el nombre del doctor en un sitio con tráfico real |
| microcopy compuesto a mano → post firmado | `ctaBanner` no viene redactado del paquete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-25 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal desde el dataset; `intro` queda vacío en vez de redactar una entrada sin aprobación |
| T-08-26 | Information disclosure | preguntas de medicación de la SERP | high | mitigate | El paquete responde sin principio activo ni dosis y se transcribe tal cual; la puerta bloquea porcentaje y soles sobre el cuerpo |
| T-08-27 | Spoofing | relación inventada entre post y guía | medium | mitigate | `relatedService` se declara solo cuando la matriz asigna una mención inversa; esta URL no la tiene y no la recibe |
| T-08-28 | Denial of service | conteo de URLs desincronizado entre puertas | medium | mitigate | Las tres constantes se mueven en el mismo commit y la corrida completa de las tres puertas es criterio de aceptación |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint` y `npm run build` en verde.
- Las tres puertas en verde, con `check-content.mjs` en corrida completa.
- El sitemap con 23 entradas y el listado del blog con el post nuevo.
</verification>

<success_criteria>
`/blog/artrosis` publica las 1789 palabras aprobadas con el esqueleto canónico, sin guía de
destino inventada, y el conteo de URLs quedó sincronizado en las tres puertas.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-12-SUMMARY.md` when done
</output>
