---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 15
type: execute
wave: 3
depends_on: ["08-05"]
files_modified:
  - src/content/service-pages/escoliosis.ts
  - src/content/service-pages/escoliosis-y-deformidades.ts
  - src/content/service-pages/index.ts
  - next.config.ts
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
autonomous: true
requirements: [SVC-03]
must_haves:
  truths:
    - "/servicios/escoliosis-y-deformidades responde 200 con la guía clínica aprobada del paquete, más de 2100 palabras y las ocho secciones canónicas."
    - "/servicios/escoliosis responde 301 permanente hacia el slug nuevo y no queda como página huérfana."
    - "Ningún archivo del sitio ni ninguna puerta sigue nombrando el slug viejo."
  artifacts:
    - src/content/service-pages/escoliosis-y-deformidades.ts
    - next.config.ts
  key_links:
    - "El sitemap, el hub, llms.txt y la imagen de Open Graph derivan de servicePages, así que el renombre se propaga solo; lo que no se propaga es el 301, que se escribe a mano."
    - "El slug viejo vive además en el MANIFEST de check-content.mjs y en la lista de slugs de check-sedes.mjs, y las dos se mueven en este commit."
---

<objective>
Publicar la guía de escoliosis con el copy aprobado y moverla al slug que decidió el
mapa de v1.2: de `/servicios/escoliosis` a `/servicios/escoliosis-y-deformidades`.

**Este plan cubre SVC-03, que había quedado sin ningún plan que lo reclamara.** En la
consolidación del set, la reescritura de escoliosis y su renombre quedaron descritos en
dos planes distintos que después se reasignaron a otras URLs, y el requisito se quedó sin
dueño. Se detectó auditando la cobertura de requisitos contra los `files_modified` de los
doce planes: ningún plan declaraba el módulo de escoliosis.

La SERP de Lima para `escoliosis` premia guía clínica y no página de servicio, igual que
la de hernia discal, y el paquete ya la redactó como guía: 2338 palabras, ocho secciones
de nivel 2 y once de nivel 3.

Purpose: cerrar SVC-03 con el contenido medido y en la URL que recibe cinco anchors de la
matriz de enlazado.
Output: el módulo en su slug definitivo, la redirección puesta y las dos puertas alineadas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-06-PLAN.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-05-SUMMARY.md
</context>

<protocolo_de_transcripcion>
Rige el protocolo escrito en `08-06-PLAN.md`, con la fuente en `seo-tools/data/copy-guias.json`,
entrada `/servicios/escoliosis-y-deformidades`. Los ids salen del campo `clave` copiado literal, el
texto del arreglo `parrafos`, y las líneas de sello de revisión del Markdown no se transcriben.
Incluye el punto 6, que reclama el `h1` para esta fase, y el punto 7, que saca los enlaces de
`seo-tools/data/internal-links.json` y no de `copy-guias.json`, que los trae vacíos para las guías
de servicio. La autorización es `15-APROBACION-DOCTOR.md`, 2026-08-13.
</protocolo_de_transcripcion>

<tasks>

<task type="auto">
  <name>Task 1: el renombre del módulo y su redirección permanente</name>
  <files>src/content/service-pages/escoliosis.ts, src/content/service-pages/escoliosis-y-deformidades.ts, src/content/service-pages/index.ts, next.config.ts</files>
  <read_first>
    - `next.config.ts` completo. Hoy solo declara `images.remotePatterns` y no tiene bloque de redirecciones: este plan lo crea, y el plan 08-14 le suma después los dos 301 del blog.
    - `node_modules/next/dist/docs/` — la guía de `redirects` de la versión instalada. Este proyecto corre una versión de Next con cambios de API respecto de lo que suele asumirse, así que la forma del bloque se lee ahí y no de memoria.
    - `src/content/service-pages/escoliosis.ts` y `src/content/service-pages/index.ts` tal como los dejó el plan 08-05.
    - `15-HANDOFF-V11-ONPAGE.md`, sección 3, la fila del renombre en la tabla de redirecciones.
    - El resultado de buscar el slug viejo en `src/` y en `scripts/`.
  </read_first>
  <action>
Renombrar el módulo a `escoliosis-y-deformidades.ts`, cambiar el valor de `slug` y actualizar el
import en `index.ts`. Conservar la posición de la página dentro de `servicePages`: el orden de ese
arreglo es el de las tarjetas del hub y el del sitemap, y un renombre no tiene por qué moverlo.

Esta tarea **no toca el cuerpo**. Solo mueve el módulo y deja la redirección puesta, para que si
algo sale mal el diagnóstico sea inmediato: o falló el renombre o falló la transcripción, nunca
las dos mezcladas.

Sumar a `next.config.ts` la función de redirecciones con una entrada permanente del slug viejo
hacia el nuevo, con un comentario de una línea que explique de dónde sale: el renombre de la fase
14 de v1.2.

El sitemap, el hub, `llms.txt`, la imagen de Open Graph y el JSON-LD derivan la ruta de
`servicePages`, así que se propagan sin tocarlos. Verificarlo en el build, no darlo por supuesto.
  </action>
  <reversibility rating="costly">
    Cambiar la URL de una página indexada mueve su historial de posicionamiento. Se puede volver
    atrás, pero cada ida y vuelta cuesta rastreo y consolidación de señales, así que se hace una
    sola vez y con la redirección en el mismo commit. La decisión está tomada en la fase 14 de
    v1.2; este plan la ejecuta y no la vuelve a abrir.
  </reversibility>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` genera `.next/server/app/servicios/escoliosis-y-deformidades.html` y ya no genera el HTML del slug viejo.
    - Con `npm run start` sirviendo, una petición a `/servicios/escoliosis` devuelve 301 o 308 con `location` hacia el slug nuevo. La respuesta literal se transcribe en el SUMMARY.
    - `grep -rn '"escoliosis"' src/` no devuelve ninguna línea.
    - El sitemap prerenderizado declara la ruta nueva, no la vieja, y sigue teniendo 21 entradas: el renombre no suma ni resta URLs.
    - `.next/server/app/servicios.html` enlaza a la ruta nueva.
  </acceptance_criteria>
  <done>La guía vive en su slug definitivo y la URL anterior redirige de forma permanente.</done>
</task>

<task type="auto">
  <name>Task 2: el cuerpo aprobado de la guía de escoliosis</name>
  <files>src/content/service-pages/escoliosis-y-deformidades.ts</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/servicios-escoliosis-y-deformidades.md` entero: la tabla de jerarquía, la de secundarias, la de entidades obligatorias y el copy.
    - En `seo-tools/data/copy-guias.json`, el objeto cuya `url` es `/servicios/escoliosis-y-deformidades`: sus 19 secciones, 8 de nivel 2 y 11 de nivel 3.
    - En `seo-tools/data/internal-links.json`, la fila de esa misma URL: 8 entradas con `link`, `anchor` y `regla`.
  </read_first>
  <action>
Reescribir `sections` con las 19 secciones del paquete, 8 de nivel 2 (`que-es`, `sintomas`,
`causas`, `diagnostico`, `sin-operar`, `cirugia`, `preguntas-frecuentes`, `cuando-consultar`) y 11
de nivel 3, aplicando el protocolo de transcripción.

`format` queda en `"guia-clinica"`. `updatedAt` pasa a `2026-08-13`. `describesSurgery` se decide
leyendo si la sección `cirugia` del paquete describe efectivamente el acto quirúrgico, que es lo
que ese campo declara. `heroLead`, `cardSummary`, `conditionName`, `alternateNames`, `ctaBanner` y
`relatedPosts` se conservan.

`h1` pasa al valor del dataset, por el punto 6 del protocolo. `title` y `description` no se tocan:
son de la fase 10.

`outboundLinks` se puebla con las 8 entradas de `internal-links.json` para esta URL: dos hacia
guías hermanas, cuatro hacia fichas de sede, una hacia `/agendar` y una hacia el hub, con sus
`href` y `anchor` literales.

Ojo con la sección que explica cómo se mide la curva: es la que más tienta a destacar un número.
El paquete ya resolvió cómo decirlo sin convertirlo en una cifra en grande, y esa es la redacción
que va. La puerta bloquea cifras destacadas en encabezado sobre las rutas de servicio, así que un
desliz ahí sale en rojo.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-content.mjs /servicios/escoliosis-y-deformidades</automated>
  </verify>
  <acceptance_criteria>
    - El módulo declara 19 secciones, 8 con `level: 2` y 11 con `level: 3`.
    - Los ocho ids canónicos aparecen como destino de ancla, en orden, y la comprobación de esqueleto de la puerta lo confirma.
    - El HTML prerenderizado supera las 2100 palabras de cuerpo.
    - `grep -c "Pendiente de aprobación" src/content/service-pages/escoliosis-y-deformidades.ts` devuelve 0.
    - `h1` es el valor del dataset, y `title` y `description` quedaron sin tocar respecto de lo que había antes del plan.
    - `outboundLinks` declara 8 entradas con los anchors literales de `internal-links.json`.
    - La puerta no reporta porcentaje, cifra en soles, primera persona ni cifra destacada en encabezado.
  </acceptance_criteria>
  <done>La guía de escoliosis publica el copy aprobado completo en su URL definitiva.</done>
</task>

<task type="auto">
  <name>Task 3: las dos puertas dejan de nombrar el slug viejo</name>
  <files>scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `scripts/check-content.mjs`, la entrada del `MANIFEST` que nombra la ruta de escoliosis.
    - `scripts/check-sedes.mjs` líneas 52 a 60, el arreglo de slugs de servicio.
  </read_first>
  <action>
Apuntar al slug nuevo la entrada del `MANIFEST` de `scripts/check-content.mjs` y el arreglo de
slugs de servicio de `scripts/check-sedes.mjs`. Son las dos únicas apariciones del slug viejo
fuera del archivo de datos.

`SITEMAP_TOTAL` no se toca en este plan: el renombre no suma ni resta URLs y el conteo sigue en 21.
Lo mueven los planes 08-16, 08-12, 08-13 y 08-14, cada uno con su delta.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "servicios/escoliosis\"" src/ scripts/` no devuelve ninguna línea.
    - `grep -c "SITEMAP_TOTAL = 21" scripts/check-content.mjs` devuelve 1: este plan no cambia el conteo.
    - Las tres puertas corren en verde.
  </acceptance_criteria>
  <done>Ninguna puerta ni ningún archivo del sitio sigue nombrando el slug anterior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL indexada → URL nueva | El renombre mueve señales de posicionamiento de una página viva |
| paquete de v1.2 → contenido publicado | Texto clínico YMYL que sale con el nombre del doctor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-36 | Denial of service | `/servicios/escoliosis` sin redirección | high | mitigate | La redirección entra en la misma tarea que el renombre y se comprueba con una petición real, no leyendo la configuración |
| T-08-37 | Tampering | slug viejo persistente en puertas y datos | medium | mitigate | Búsqueda del literal en `src/` y `scripts/` con cero coincidencias como criterio de aceptación |
| T-08-38 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal desde el dataset sellado; ninguna frase fuera del paquete |
| T-08-39 | Information disclosure | grados de curva presentados como cifra | high | mitigate | La puerta bloquea cifra destacada en encabezado y énfasis fuerte sobre las rutas de servicio |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint` y `npm run build` en verde.
- Las tres puertas en verde, con el conteo del sitemap todavía en 21.
- Petición real al slug viejo devolviendo redirección permanente, transcrita en el SUMMARY.
</verification>

<success_criteria>
La guía de escoliosis publica las 2338 palabras aprobadas en su URL definitiva, la anterior
redirige de forma permanente, y SVC-03 deja de ser el requisito sin dueño del set.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-15-SUMMARY.md` when done
</output>
