---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 18
type: execute
wave: 9
depends_on: ["08-17"]
files_modified:
  - src/content/location-pages/consultorio-privado.ts
  - src/content/location-pages/sanna-la-molina.ts
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
autonomous: true
requirements: [SVC-05]
must_haves:
  truths:
    - "/sedes/consultorio-privado y /sedes/sanna-la-molina publican su copy aprobado completo, más de 1200 palabras cada una."
    - "El consultorio sigue siendo la única sede que ofrece el WhatsApp del doctor como canal de agenda."
    - "Ninguna de las dos páginas afirma un dato operativo que el paquete dejó sin confirmar, y son las dos que más pendientes acumulan."
    - "Las cuatro fichas de sede están en el manifiesto de la puerta de contenido y la corrida completa sale en verde."
  artifacts:
    - src/content/location-pages/consultorio-privado.ts
    - src/content/location-pages/sanna-la-molina.ts
  key_links:
    - "El consultorio es la excepción del patrón: es la única sede cuya agenda maneja el doctor, y el copy lo repite porque es la confusión más frecuente."
    - "Con estas dos, las cuatro rutas de sede entran al manifiesto de check-content.mjs."
---

<objective>
Las otras dos fichas de sede: el consultorio privado de Surco y Sanna La Molina.

Son las dos más largas del paquete, 1238 y 1263 palabras, y las dos que más datos
operativos sin confirmar acumulan: cinco de los diez. El consultorio además es la
excepción de todo el conjunto, porque es la única sede cuya agenda maneja el doctor
directamente, y su copy lo dice varias veces a propósito.

El modelo, la plantilla y el esqueleto de la puerta ya los abrió el plan 08-17, así que
acá solo entra contenido y el cierre de las dos puertas.

Purpose: completar las cuatro fichas de sede, que eran cuatro de las cinco URLs del
paquete sin fase que las recogiera.
Output: los dos módulos con su cuerpo y las cuatro sedes bajo la puerta de contenido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-17-PLAN.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-17-SUMMARY.md
</context>

<protocolo_de_transcripcion>
El de `08-06-PLAN.md`, con la fuente en `seo-tools/data/copy-sedes.json`, y **la regla de datos
operativos sin confirmar del plan 08-17, que vale igual acá y con más peso**: el copy aprobado no
afirma ninguno de los pendientes, y este plan tiene prohibido completarlos, deducirlos de otra
sede o redondearlos. La tabla de pendientes de cada documento se transcribe al SUMMARY y nunca al
sitio.

El punto 6 vale igual: el `h1` es de esta fase, `title` y `description` son de la fase 10.
</protocolo_de_transcripcion>

<tasks>

<task type="auto">
  <name>Task 1: el cuerpo aprobado del consultorio privado de Surco</name>
  <files>src/content/location-pages/consultorio-privado.ts</files>
  <read_first>
    - `.planning/.../paquetes/sedes-consultorio-privado.md` entero, incluidas su tabla de datos operativos respaldados, su tabla "Pendientes de confirmación antes de publicar" con 3 filas, y su nota "Qué queda pendiente del doctor", que señala cuál es el bloque más delicado.
    - En `seo-tools/data/copy-sedes.json`, el objeto de `/sedes/consultorio-privado`: 20 secciones, 5 de nivel 2 y 15 de nivel 3, sus 6 `enlacesPropuestos` y su arreglo `datosOperativos`.
    - `src/content/locations.ts`, entrada `consultorio-privado`: la regla de negocio del canal de agenda está declarada en la cabecera del archivo.
    - `src/content/location-pages/clinica-ricardo-palma.ts` tal como lo dejó el plan 08-17, como molde de una sede ya transcrita.
  </read_first>
  <action>
Escribir `sections` con las 20 secciones del paquete, aplicando el protocolo, y `outboundLinks`
con sus 6 entradas. `h1` pasa a `Consultorio de Surco: la consulta sin intermediarios`, el valor
del dataset. `title` y `description` no se tocan.

Esta es **la única sede que sí ofrece el WhatsApp del doctor**, porque es la única cuya agenda
maneja él. El copy lo repite varias veces y eso es deliberado: según la nota del paquete, es la
confusión más frecuente de los pacientes. No se recorta esa repetición por parecer redundante.

Tres datos quedan pendientes y ninguno se completa: el estacionamiento del edificio y sus tarifas,
los seguros y convenios que el consultorio acepta, y el precio de la consulta. Los tres están
listados en la tabla del documento. El de precio además está doblemente bloqueado, porque D-10
prohíbe publicar cifras de consulta: el paquete responde las tres preguntas de precio de la SERP
explicando de qué depende el costo, sin dar un número, y esa es la redacción que va.

Ojo con el bloque que el propio paquete señala como el más delicado: el de hernia discal sin
cirugía, que dice que la mayoría mejora sin operar y a la vez advierte contra los sitios que
prometen resolver toda hernia sin cirugía. Está aprobado tal como está y se transcribe literal. No
se suaviza ninguno de los dos lados de esa frase.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-sedes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - El módulo declara 20 secciones, 5 de nivel 2 con las claves del esqueleto en orden y 15 de nivel 3.
    - El HTML de la ruta supera las 1200 palabras de cuerpo.
    - `h1` es el valor del dataset y `title` y `description` quedaron sin tocar.
    - El HTML no muestra ninguna cifra en soles y `check-sedes.mjs` sigue reconociendo el canal de agenda del consultorio.
    - Los tres datos pendientes siguen sin afirmarse en el HTML y su tabla se transcribe en el SUMMARY.
    - `grep -c "Pendiente de aprobación" src/content/location-pages/consultorio-privado.ts` devuelve 0.
  </acceptance_criteria>
  <done>El consultorio publica su copy aprobado, con su canal propio y sin ninguna cifra de precio.</done>
</task>

<task type="auto">
  <name>Task 2: el cuerpo aprobado de Sanna La Molina</name>
  <files>src/content/location-pages/sanna-la-molina.ts</files>
  <read_first>
    - `.planning/.../paquetes/sedes-sanna-la-molina.md` entero, incluidas sus dos tablas de datos.
    - En `seo-tools/data/copy-sedes.json`, el objeto de `/sedes/sanna-la-molina`: 20 secciones, 5 de nivel 2 y 15 de nivel 3, y sus 6 `enlacesPropuestos`.
    - `src/content/location-pages/consultorio-privado.ts` tal como quedó en la Task 1.
  </read_first>
  <action>
Mismo trabajo que la Task 1 sobre la cuarta sede: 20 secciones, `outboundLinks` con sus 6
entradas, `h1` a `Atención de columna en Sanna La Molina`, `title` y `description` intactos.

Es una sede de tipo clínica, así que **no ofrece el WhatsApp del doctor ni menciona su número**:
su agenda va por la central de citas de la clínica, que la plantilla ya emite desde
`locations.ts`.

Quedan pendientes el piso y el consultorio dentro del centro clínico, los seguros y convenios, y
el precio de la consulta. Ninguno se completa ni se deduce de las otras sedes.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-sedes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - El módulo declara 20 secciones, 5 de nivel 2 con las claves del esqueleto en orden y 15 de nivel 3.
    - El HTML de la ruta supera las 1200 palabras de cuerpo.
    - `grep -c "964305682\|964 305 682" src/content/location-pages/sanna-la-molina.ts` devuelve 0.
    - `h1` es el valor del dataset y `title` y `description` quedaron sin tocar.
    - Los tres datos pendientes siguen sin afirmarse en el HTML y su tabla se transcribe en el SUMMARY.
  </acceptance_criteria>
  <done>La cuarta sede publica su copy aprobado y respeta la regla del canal de agenda.</done>
</task>

<task type="auto">
  <name>Task 3: las cuatro sedes entran al manifiesto y las puertas cierran en verde</name>
  <files>scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `scripts/check-content.mjs`, el `MANIFEST` tal como lo dejó el plan 08-17, con dos rutas de sede.
    - `scripts/check-sedes.mjs`, su `MANIFEST` con el `h1` esperado de cada sede.
  </read_first>
  <action>
Sumar al `MANIFEST` de `scripts/check-content.mjs` las dos rutas de sede que faltan, con formato
`ficha-de-sede`. Con estas dos, la puerta de contenido cubre las cuatro.

Actualizar en `scripts/check-sedes.mjs` el `h1` esperado de estas dos sedes. El `title` no se
toca: lo reescribe la fase 10.

`SITEMAP_TOTAL` no se toca: las cuatro rutas de sede ya existían y ya estaban en el sitemap. El
número de cierre de la fase sigue siendo 22, fijado por el plan 08-14.

Al cerrar, correr las tres puertas en corrida completa. Es la primera vez que
`scripts/check-content.mjs` mide las cuatro sedes, así que si alguna salvaguarda salta, salta acá.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - El `MANIFEST` de la puerta de contenido lista las cuatro rutas de sede con formato `ficha-de-sede`.
    - Las tres puertas corren en verde en corrida completa.
    - `grep -c "SITEMAP_TOTAL = 22" scripts/check-content.mjs` devuelve 1.
    - Ninguna de las cuatro sedes reporta porcentaje, cifra en soles ni construcción en primera persona.
  </acceptance_criteria>
  <done>Las cuatro fichas de sede están publicadas y bajo puerta, con las tres puertas en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato operativo sin confirmar → paciente | Cinco de los diez pendientes viven en estas dos sedes |
| canal de agenda → regla de negocio | El consultorio es la única sede que ofrece el WhatsApp del doctor |
| paquete de v1.2 → contenido publicado | Texto clínico YMYL que sale con el nombre del doctor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-49 | Tampering | los cinco datos pendientes de estas dos sedes | critical | mitigate | El copy no los afirma y el plan prohíbe completarlos o deducirlos de otra sede; la tabla va al SUMMARY y no al sitio |
| T-08-50 | Information disclosure | precio de consulta | high | mitigate | D-10 lo prohíbe y el paquete responde las preguntas de precio sin cifra; la puerta bloquea soles sobre el cuerpo |
| T-08-51 | Spoofing | canal de agenda del doctor en una sede de clínica | high | mitigate | Criterio de aceptación con búsqueda del número en el módulo de Sanna, con cero coincidencias |
| T-08-52 | Spoofing | el bloque de hernia discal sin cirugía del consultorio | high | mitigate | Se transcribe literal el texto aprobado, que equilibra los dos lados de la afirmación; no se suaviza ninguno |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint` y `npm run build` en verde.
- Las tres puertas en verde en corrida completa, con las cuatro sedes dentro del manifiesto.
- Las dos tablas de datos pendientes transcritas en el SUMMARY.
</verification>

<success_criteria>
Las cuatro fichas de sede publican el copy aprobado del paquete, ninguna afirma un dato que
nadie confirmó, y el consultorio sigue siendo la única que ofrece la agenda directa del doctor.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-18-SUMMARY.md` when done
</output>
