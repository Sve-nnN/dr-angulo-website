---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 08
type: execute
wave: 2
depends_on: ["08-05"]
files_modified:
  - src/content/service-pages/ortopedia-infantil.ts
autonomous: true
requirements: [SVC-04]
must_haves:
  truths:
    - "Un padre que busca `ortopedia infantil lima` encuentra una página de servicio de más de 1400 palabras con las cinco secciones del esqueleto de v1.2"
    - "La página declara el formato `pagina-de-servicio` y ya no el de guía clínica, y la puerta verifica el orden canónico de ese esqueleto y no del otro"
    - "El texto publicado es literal el del paquete aprobado por el doctor"
  artifacts:
    - src/content/service-pages/ortopedia-infantil.ts
  key_links:
    - "Es la única de las cuatro páginas del silo que no cambia a guía clínica: la SERP de Lima para esta keyword pide página de servicio, y el esqueleto de cinco secciones sale de `sectionOrderFor('pagina-de-servicio')` que creó 08-05"
---

<objective>
Reescribir `/servicios/ortopedia-infantil` con el copy aprobado del paquete de v1.2 y
cambiarla al esqueleto de página de servicio.

Las otras tres páginas del silo pasan a guía clínica porque la SERP medida de Lima está
llena de guías. Esta no: para `ortopedia infantil lima` la intención es encontrar a quién
llevar al niño, no leer sobre una condición. El paquete la redactó con las cinco
secciones de página de servicio y 1520 palabras, con trece `h3` que cubren las preguntas
y las búsquedas relacionadas de la SERP.

Purpose: cerrar SVC-04 con el contenido que la SERP pide, no con el formato que el resto
del silo usa por inercia.
Output: `src/content/service-pages/ortopedia-infantil.ts` con las cinco secciones
canónicas del formato `pagina-de-servicio`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: el cuerpo completo de la página de ortopedia infantil</name>
  <files>src/content/service-pages/ortopedia-infantil.ts</files>
  <read_first>
    - .planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/servicios-ortopedia-infantil.md **completo**: jerarquía, entidades obligatorias y el bloque entre `copy:inicio` y `copy:fin`
    - seo-tools/data/copy-servicios.json, entrada de `/servicios/ortopedia-infantil`: trae el valor `clave` de cada sección, que es el `id` a usar
    - src/content/service-pages/ortopedia-infantil.ts tal como lo dejó 08-05
    - src/content/sections.ts, en particular la tupla del formato `pagina-de-servicio`
  </read_first>
  <action>
    Cambia `format` de `guia-clinica` a `pagina-de-servicio` y reemplaza el arreglo de
    secciones por el del paquete: cinco `h2` y trece `h3`. Los `id` de los `h2` son
    `que-se-atiende`, `como-es-la-consulta`, `condiciones`, `donde-se-atiende`,
    `como-agendar`, en ese orden, y los de los `h3` salen del campo `clave` del dataset.

    El texto se transcribe **literal** del bloque de copy del paquete. Cada párrafo del
    paquete es un elemento de `paragraphs`. No se parafrasea, no se resume, no se reordena
    y no se agrega una frase de puente. Lo aprobado es ese texto (aprobación del
    2026-08-13, `15-APROBACION-DOCTOR.md`).

    Las líneas de cita del Markdown que anuncian que un bloque está pendiente de
    aprobación son andamiaje del generador y no se transcriben.

    Ojo con dos cosas que este copy roza y que siguen prohibidas: el paquete nombra sedes
    y distritos, y eso está permitido porque sale de `src/content/locations.ts`; pero
    ninguna afirmación sobre piso, consultorio, seguro o precio entra acá aunque la
    prosa la insinúe, porque son los diez datos que el handoff dejó sin confirmar. Si un
    párrafo del copy contiene uno de esos datos, se detiene el plan y se reporta en vez de
    publicarlo.

    `heroLead`, `cardSummary` y `ctaBanner` se conservan. `h1` **sí se actualiza acá**,
    con el valor de la tabla "Title, meta y H1" del paquete, porque los planes de la fase
    10 declaran que ese campo es territorio de la fase 8. `title` y `description` no se
    tocan: son de la fase 10. `slug`, `relatedPosts`, `alternateNames` y
    `describesSurgery` no cambian. `updatedAt` pasa a la fecha de ejecución.

    Los enlaces internos que el paquete propone al pie **no se implementan en este plan**:
    los escribe 08-18 sobre las 16 URLs de una sola vez, porque el modelo de contenido no
    admite marcado dentro de la prosa (SAFE-08) y hace falta un campo nuevo para llevarlos.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && node scripts/check-content.mjs /servicios/ortopedia-infantil</automated>
  </verify>
  <acceptance_criteria>
    - La puerta pasa sobre `/servicios/ortopedia-infantil` y reporta 1400 palabras de cuerpo o más.
    - `grep -c "format: \"pagina-de-servicio\"" src/content/service-pages/ortopedia-infantil.ts` devuelve 1.
    - Los cinco `id` canónicos del formato aparecen como `h2` destino de ancla en el orden de la tupla, y la comprobación de esqueleto de la puerta lo confirma contra `pagina-de-servicio` y no contra `guia-clinica`.
    - El banner del primer tercio queda después de la primera sección, que es donde POS-01 lo pide para este formato, y el porcentaje que reporta la puerta cae entre 15 y 35.
    - La puerta no reporta porcentaje, cifra en soles ni construcción en primera persona.
    - `git diff --name-only` devuelve exactamente un archivo.
  </acceptance_criteria>
  <done>`/servicios/ortopedia-infantil` publica el copy aprobado con el esqueleto de página de servicio y la puerta lo verifica contra la tupla correcta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy aprobado → sitio en producción | Texto clínico YMYL publicado bajo la firma del doctor |
| dato operativo de sede → página publicada | Un piso, un seguro o un precio equivocado manda a un padre con su hijo a un lugar donde no lo atienden |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-22 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal del paquete sellado; ninguna frase fuera del paquete |
| T-08-23 | Tampering | dato operativo de sede sin confirmar | high | mitigate | Los diez datos pendientes del handoff no se publican; si aparecen en un párrafo, el plan se detiene y lo reporta |
| T-08-24 | Information disclosure | cifras y credenciales | high | mitigate | La puerta bloquea porcentaje, soles y primera persona sobre el HTML y sobre el archivo de datos |
| T-08-SC | Tampering | instalaciones npm | high | mitigate | Cero paquetes. Solo cambia un archivo de contenido |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` en 0.
- `node scripts/check-content.mjs /servicios/ortopedia-infantil` en 0, verificando el esqueleto de `pagina-de-servicio`.
</verification>

<success_criteria>
La página de ortopedia infantil publica las 1520 palabras aprobadas con las cinco
secciones del formato que la SERP de Lima pide.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-08-SUMMARY.md` when done
</output>
