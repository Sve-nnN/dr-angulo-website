---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 14
type: execute
wave: 7
depends_on: ["08-06", "08-10", "08-11", "08-13"]
files_modified:
  - src/content/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos.ts
  - src/content/blog/estenosis-espinal-que-es.ts
  - src/content/blog/index.ts
  - next.config.ts
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
autonomous: false
requirements: [BLOG-03]
must_haves:
  truths:
    - "/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos responde 301 permanente hacia /servicios/hernia-discal."
    - "/blog/estenosis-espinal-que-es responde 301 permanente hacia /servicios/estenosis-espinal."
    - "Las dos guías de destino ya publicaban el material absorbido antes de que la redirección exista."
    - "Ninguna de las dos URLs apagadas queda en el sitemap, en el listado del blog ni en llms.txt."
  artifacts:
    - next.config.ts
  key_links:
    - "El orden publicar antes que redirigir es la única razón por la que este plan está en la ola 7 y no antes."
    - "SITEMAP_TOTAL baja de 24 a 22 en las dos puertas: es el número con el que cierra la fase."
---

<objective>
Apagar los dos posts que las guías absorbieron, con 301 permanente hacia su destino.

`hernia-discal-o-dolor-de-espalda-como-diferenciarlos` se funde en
`/servicios/hernia-discal` y `estenosis-espinal-que-es` en `/servicios/estenosis-espinal`.
Los dos documentos del paquete traen la tabla de qué bloque suyo quedó en qué sección de
la guía, y los planes 08-06 verificaron esas tablas bloque por bloque al publicar.

**El orden no se puede invertir y es la razón de ser de esta ola.** La guía de destino se
publica primero, con el contenido ya fundido, y recién entonces se pone el 301. Al revés,
la redirección entierra material que todavía no vive en ningún otro lado. Por eso este
plan depende de 08-06, que publicó las dos guías, y no se ejecuta antes.

Purpose: cerrar BLOG-03 consolidando el silo en las dos URLs que sí pueden ganar la
búsqueda, sin perder el contenido que las alimenta.
Output: dos rutas apagadas con redirección permanente y el conteo final de la fase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-06-SUMMARY.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: comprobar que el material absorbido ya está publicado</name>
  <files>src/content/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos.ts, src/content/blog/estenosis-espinal-que-es.ts</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/blog-hernia-discal-o-dolor-de-espalda-como-diferenciarlos.md`, tabla "Qué se absorbió y dónde quedó": 7 filas.
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/blog-estenosis-espinal-que-es.md`, misma tabla: 9 filas, de las cuales la de las remisiones a la guía completa no se transcribe a propósito.
    - `08-06-SUMMARY.md`, donde quedó anotada la verificación bloque por bloque hecha al publicar las dos guías.
    - Los dos módulos de post que se van a apagar, para tener a la vista el texto que se está dando por absorbido.
  </read_first>
  <precondition>
    `.next/server/app/servicios/hernia-discal.html` y `.next/server/app/servicios/estenosis-espinal.html`
    existen y contienen los ids de destino que declaran las dos tablas de absorción. Si el build no
    está o falta un id, la tarea se detiene: redirigir sin destino publicado es exactamente el daño
    que esta ola existe para evitar.
  </precondition>
  <action>
Antes de tocar una sola línea de configuración, verificar sobre el HTML **ya construido** que cada
fila de las dos tablas de absorción tiene destino vivo.

Para el post de hernia discal son 7 filas y sus destinos son tres ids:
`sintomas--diferencia-entre-lumbalgia-y-hernia-discal`, `sintomas` y `cuando-consultar`. Para el
de estenosis son 8 filas con destino, sobre 9: la novena son las dos remisiones que el post hace
hacia "la guía completa", que el propio dataset declara no transcribibles porque la guía pasa a
ser esa página y la remisión pierde destino.

La comprobación se hace sobre el HTML prerenderizado y no sobre el archivo de datos: lo que
importa es que el lector que llegue por el 301 encuentre el material, no que exista una cadena en
un módulo de TypeScript.

Esta tarea no modifica ningún archivo. Su salida es la evidencia que autoriza a la siguiente, y se
transcribe entera en el SUMMARY: fila por fila, con el id de destino y si se encontró. Si falta
una sola, el plan se detiene acá y se reporta, sin escribir la redirección.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal</automated>
  </verify>
  <acceptance_criteria>
    - Las 7 filas del post de hernia discal tienen su id de destino presente en `.next/server/app/servicios/hernia-discal.html`.
    - Las 8 filas con destino del post de estenosis lo tienen en `.next/server/app/servicios/estenosis-espinal.html`, y la novena queda anotada como no transcrita a propósito.
    - La tabla completa de la comprobación, 16 filas, queda transcrita en el SUMMARY.
    - `git diff --name-only` no devuelve ningún archivo: la tarea solo lee.
  </acceptance_criteria>
  <done>Está probado, sobre el HTML publicado, que apagar los dos posts no entierra nada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Las dos guías de destino ya publican el material de los posts que se van a apagar, y la
    comprobación de la Task 1 lo confirmó fila por fila sobre el HTML construido.
  </what-built>
  <how-to-verify>
    Este es el punto de no retorno de la fase sobre un sitio en producción. Antes de escribir el
    301, revisar a mano:

    1. `npm run build &amp;&amp; npm run start`, y abrir `http://localhost:3000/servicios/hernia-discal`
       y `http://localhost:3000/servicios/estenosis-espinal`.
    2. Leer las secciones de destino de las dos tablas de absorción y confirmar que el material del
       post viejo se lee completo y con sentido dentro de la guía, no cortado a la mitad.
    3. Abrir `http://localhost:3000/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos` y
       `http://localhost:3000/blog/estenosis-espinal-que-es` y confirmar que no queda en ellos
       ningún párrafo que no esté en su guía de destino.
    4. Confirmar la decisión: las dos URLs dejan de existir de forma permanente. Un 301 se puede
       revertir, pero el historial de posicionamiento que se transfiere no vuelve intacto.
  </how-to-verify>
  <resume-signal>Escribir "aprobado" para continuar con la redirección, o describir qué material falta en la guía</resume-signal>
</task>

<task type="auto">
  <name>Task 2: las dos redirecciones permanentes y el conteo final</name>
  <files>src/content/blog/hernia-discal-o-dolor-de-espalda-como-diferenciarlos.ts, src/content/blog/estenosis-espinal-que-es.ts, src/content/blog/index.ts, next.config.ts, scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `next.config.ts` tal como lo dejó el plan 08-07, que creó el bloque de redirecciones con la entrada del renombre de escoliosis.
    - `src/content/blog/index.ts` tal como lo dejó el plan 08-13.
    - `scripts/check-content.mjs`, `SITEMAP_TOTAL` y el `MANIFEST`; `scripts/check-sedes.mjs`, su constante.
    - `src/lib/llms-txt.ts`, para confirmar que la sección de artículos deriva de `blogPosts` y no lista slugs a mano.
  </read_first>
  <action>
Sacar los dos posts de `blogPosts` en `src/content/blog/index.ts` y borrar sus dos módulos. Al
derivar de ese registro, el sitemap, el listado `/blog`, las imágenes de Open Graph y `llms.txt`
dejan de nombrarlos sin que ninguno de esos archivos se toque.

Sumar a `next.config.ts` las dos entradas permanentes, junto a la del renombre de escoliosis que
ya está: el post de hernia discal hacia `/servicios/hernia-discal` y el de estenosis hacia
`/servicios/estenosis-espinal`. Con estas dos, el bloque queda con las tres redirecciones que el
handoff declara para v1.1 y no hay ninguna más pendiente.

Bajar `SITEMAP_TOTAL` de 24 a 22 en las dos puertas, en el mismo commit. Este es el número con el
que cierra la fase: las 21 de la fase 9, más la página de cirugía mínimamente invasiva, más los
dos posts nuevos, menos los dos que se apagan acá. Dejar el comentario de cada constante contando
esa cuenta, para que quien la lea el mes que viene no tenga que reconstruirla.

Sacar las dos rutas del `MANIFEST` de `scripts/check-content.mjs`. Dejarlas listadas haría que la
puerta exija el HTML de una página que ya no se prerenderiza, y la puerta falla cuando falta el
archivo, que es el comportamiento correcto y por eso mismo hay que retirarlas.

Comprobar además que ninguna otra página del sitio enlaza a las dos URLs apagadas. La matriz de
enlazado de v1.2 no las tiene como destino en ninguna fila, así que el resultado esperado de la
búsqueda es cero; si aparece alguna, es un enlace escrito a mano en v1.0 y se repunta a la guía de
destino.
  </action>
  <reversibility rating="one-way">
    Un 301 permanente sobre dos URLs indexadas de un sitio en producción transfiere su historial a
    la guía de destino. Se puede quitar la regla, pero las señales ya consolidadas no vuelven
    intactas y el contenido original habría dejado de existir. La decisión está tomada en la fase
    14 de v1.2 (D-07) y verificada por el checkpoint anterior; este plan la ejecuta.
  </reversibility>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Con `npm run start` sirviendo, una petición a cada una de las dos rutas apagadas devuelve 301 o 308 con `location` hacia su guía. Las dos respuestas literales se transcriben en el SUMMARY.
    - `next.config.ts` declara exactamente tres redirecciones permanentes y las tres son las del handoff.
    - El sitemap prerenderizado tiene exactamente 22 entradas y ninguna es una de las dos URLs apagadas.
    - `.next/server/app/blog.html` lista cuatro posts y `/llms.txt` prerenderizado tampoco nombra las dos apagadas.
    - `grep -rn "hernia-discal-o-dolor-de-espalda-como-diferenciarlos\|estenosis-espinal-que-es" src/` no devuelve ninguna línea.
    - Las tres puertas corren en verde en corrida completa.
  </acceptance_criteria>
  <done>Las dos URLs absorbidas redirigen de forma permanente, el sitio queda en 22 URLs y las tres redirecciones del handoff están puestas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL indexada → redirección permanente | Dos URLs con historial propio dejan de existir en un sitio con tráfico real |
| registro de posts → artefactos derivados | Sitemap, listado, Open Graph y llms.txt dependen del mismo arreglo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-32 | Denial of service | contenido enterrado por el 301 | critical | mitigate | La Task 1 verifica las 16 filas de absorción sobre el HTML construido y el checkpoint humano bloquea la escritura de la regla hasta que alguien lo confirme leyendo las guías |
| T-08-33 | Denial of service | regla de redirección mal escrita | high | mitigate | Se comprueba con una petición real a cada ruta, no leyendo la configuración |
| T-08-34 | Tampering | enlace interno huérfano hacia una URL apagada | medium | mitigate | Búsqueda sobre `src/` con cero coincidencias como criterio de aceptación |
| T-08-35 | Denial of service | conteo de URLs desincronizado entre puertas | medium | mitigate | Las dos constantes bajan en el mismo commit y las tres puertas corren en completa |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- Las 16 filas de absorción verificadas sobre HTML antes de escribir una sola redirección.
- Checkpoint humano aprobado antes de la Task 2.
- Petición real a las dos rutas apagadas devolviendo redirección permanente.
- Sitemap en 22, listado del blog en cuatro posts, tres puertas en verde en corrida completa.
</verification>

<success_criteria>
Los dos posts absorbidos dejan de existir con 301 permanente hacia su guía, sin haber enterrado
un solo bloque de contenido, y el sitio cierra la fase en 22 URLs.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-14-SUMMARY.md` when done
</output>
