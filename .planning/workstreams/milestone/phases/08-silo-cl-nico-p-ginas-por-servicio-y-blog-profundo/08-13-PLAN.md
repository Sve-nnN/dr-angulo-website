---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 13
type: execute
wave: 6
depends_on: ["08-05", "08-12"]
files_modified:
  - src/content/blog/lumbalgia.ts
  - src/content/blog/index.ts
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
autonomous: true
requirements: [BLOG-02, BLOG-03]
must_haves:
  truths:
    - "/blog/lumbalgia responde 200 con la guía clínica aprobada del paquete, más de 1400 palabras y las ocho secciones canónicas."
    - "El post empuja hacia /servicios/hernia-discal con el anchor exacto que le asigna la matriz de enlazado."
    - "La ruta entra al sitemap, al listado del blog y a su imagen de Open Graph sin que ninguno de esos archivos se toque a mano."
  artifacts:
    - src/content/blog/lumbalgia.ts
  key_links:
    - "La mención inversa hacia la guía de hernia discal es lo que convierte a este post en tráfico para el silo y no en una hoja suelta."
    - "SITEMAP_TOTAL sube de 23 a 24 en las dos puertas que lo declaran."
---

<objective>
Crear `/blog/lumbalgia`, la segunda de las dos URLs de blog que el paquete manda crear.

`lumbalgia` es una cabeza con SERP medida en Lima y hoy la única página del sitio que la
roza es la guía de hernia discal, que la usa como término de contraste. El copy viene
redactado entero: 1532 palabras, ocho secciones de nivel 2 y siete de nivel 3.

Purpose: cerrar la otra mitad de BLOG-02 que corresponde a URLs nuevas, y sumar la
segunda mención inversa hacia la guía de hernia discal.
Output: el módulo del post, el registro actualizado y el conteo de URLs corrido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-12-PLAN.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-12-SUMMARY.md
</context>

<protocolo_de_transcripcion>
El mismo del plan 08-12, incluida la parte de URL nueva: `title`, `description` y `h1` salen
del objeto del dataset en `seo-tools/data/copy-blog.json`, que es la misma fuente que va a
leer la fase 10. Los ids salen del campo `clave` literal, el texto del arreglo `parrafos`, y
las líneas de sello de revisión del Markdown no se transcriben. La autorización es
`15-APROBACION-DOCTOR.md`, 2026-08-13.
</protocolo_de_transcripcion>

<tasks>

<task type="auto">
  <name>Task 1: el módulo del post de lumbalgia</name>
  <files>src/content/blog/lumbalgia.ts, src/content/blog/index.ts</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/blog-lumbalgia.md` entero, incluida la tabla "Title, meta y H1".
    - En `seo-tools/data/copy-blog.json`, el objeto cuya `url` es `/blog/lumbalgia`: sus 15 secciones, 8 de nivel 2 y 7 de nivel 3, su `derivaA` y sus 6 `enlacesPropuestos`.
    - `src/content/blog/artrosis.ts` tal como lo dejó el plan 08-12, que es el molde más cercano.
    - `src/content/blog/index.ts` tal como lo dejó el plan 08-12.
  </read_first>
  <action>
Crear el módulo con las 15 secciones del paquete, 8 de nivel 2 (`que-es`, `sintomas`, `causas`,
`diagnostico`, `sin-operar`, `cirugia`, `preguntas-frecuentes`, `cuando-consultar`) y 7 de nivel
3, aplicando el protocolo de transcripción. `format` es `"guia-clinica"`, `publishedAt` y
`updatedAt` van a `2026-08-13`.

`relatedService` va en `hernia-discal`, que es lo que declara `derivaA`, y el enlace de salida usa
el anchor `hernia discal lumbosacra tratamiento` que la matriz especifica, no la frase genérica
que la plantilla arma desde `conditionName`. A diferencia de artrosis, esta URL sí tiene mención
inversa medida y por eso sí la declara.

Poblar `outboundLinks` con las 6 entradas de `enlacesPropuestos`. Todas sus rutas de destino ya
existen a esta altura de la fase salvo ninguna: artrosis entró en el plan 08-12 y los otros dos
posts son de v1.0 reescritos en la ola 2. Comprobarlo en el build.

`intro` queda vacío, por la misma razón que en artrosis: el paquete no redacta entrada aparte y
escribir una sería publicar prosa clínica sin aprobación.

`ctaBanner` se arma con frases tomadas del cuerpo aprobado, recortadas y no reescritas, según el
contrato de copy de la UI-SPEC.

Sumarlo a `blogPosts` en `index.ts`, después de artrosis.

Ojo con la sección de cirugía: la lumbalgia común rara vez se opera, y el paquete lo dice sin
ambigüedad. No se suaviza esa redacción para que la página parezca más quirúrgica.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-content.mjs /blog/lumbalgia</automated>
  </verify>
  <acceptance_criteria>
    - El módulo declara 15 secciones, 8 con `level: 2` y 7 con `level: 3`, y `format` es `"guia-clinica"`.
    - `npm run build` genera `.next/server/app/blog/lumbalgia.html` y su imagen de Open Graph.
    - El HTML supera las 1400 palabras de cuerpo y la puerta corre en verde sobre la ruta.
    - El HTML contiene un enlace a `/servicios/hernia-discal` cuyo texto es el anchor de la matriz.
    - `outboundLinks` declara 6 entradas con los anchors literales del dataset y las 6 rutas resuelven en el build.
    - `grep -c "Pendiente de aprobación" src/content/blog/lumbalgia.ts` devuelve 0.
    - `.next/server/app/blog.html` lista el post y el sitemap prerenderizado declara la ruta.
  </acceptance_criteria>
  <done>El post de lumbalgia existe, publica el copy aprobado y empuja al silo con el anchor que le toca.</done>
</task>

<task type="auto">
  <name>Task 2: el conteo de URLs sube a 24 en las dos puertas</name>
  <files>scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `scripts/check-content.mjs`, la constante `SITEMAP_TOTAL` y el `MANIFEST` tal como los dejó el plan 08-12.
    - `scripts/check-sedes.mjs`, su propia constante.
  </read_first>
  <action>
Subir `SITEMAP_TOTAL` de 23 a 24 en las dos puertas, en el mismo commit.

Sumar `/blog/lumbalgia` al `MANIFEST` con formato de guía clínica y con `linksTo` apuntando a
`/servicios/hernia-discal`, que es la mención inversa que la matriz le asigna y que la puerta
tiene que exigir.

Actualizar el comentario de cada constante: a esta altura son las 21 de la fase 9, más la página
de cirugía mínimamente invasiva, más los dos posts nuevos. Este es el máximo que alcanza la fase;
el plan 08-14 lo baja a 22 al apagar los dos posts absorbidos.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SITEMAP_TOTAL = 24" scripts/check-content.mjs` devuelve 1 y lo mismo en `scripts/check-sedes.mjs`.
    - El sitemap prerenderizado tiene exactamente 24 entradas.
    - Las tres puertas corren en verde, incluida `check-content.mjs` en corrida completa.
  </acceptance_criteria>
  <done>Las tres puertas están en verde con el conteo máximo de la fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete de v1.2 → contenido publicado | Texto clínico YMYL que sale con el nombre del doctor |
| microcopy compuesto a mano → post firmado | `ctaBanner` no viene redactado del paquete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-29 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal desde el dataset; `intro` vacío en vez de redactar entrada sin aprobación |
| T-08-30 | Tampering | sesgo quirúrgico en la sección de cirugía | high | mitigate | La redacción del paquete se transcribe sin suavizar; la puerta bloquea cifras y promesas de resultado |
| T-08-31 | Denial of service | conteo de URLs desincronizado entre puertas | medium | mitigate | Las dos constantes se mueven en el mismo commit y la corrida completa de las tres puertas es criterio de aceptación |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint` y `npm run build` en verde.
- Las tres puertas en verde, con `check-content.mjs` en corrida completa.
- El sitemap con 24 entradas y el listado del blog con los seis posts.
</verification>

<success_criteria>
`/blog/lumbalgia` publica las 1532 palabras aprobadas con el esqueleto canónico y su mención
inversa hacia la guía de hernia discal, y el blog queda en su máximo de seis posts antes de que
la fase apague dos.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-13-SUMMARY.md` when done
</output>
