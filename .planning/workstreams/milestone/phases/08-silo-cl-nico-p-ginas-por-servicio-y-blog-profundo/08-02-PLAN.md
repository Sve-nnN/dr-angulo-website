---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/content/service-pages.ts
autonomous: true
requirements: [SVC-02, SVC-03, SVC-04, SVC-05]
user_setup: []

must_haves:
  truths:
    - "Un paciente abre /servicios/estenosis-espinal y lee una guía completa de 900 palabras o más de prosa, con las siete secciones en el orden fijo (SVC-02, CTX-04, CTX-10)."
    - "Lo mismo en /servicios/escoliosis (SVC-03) y en /servicios/ortopedia-infantil (SVC-04)."
    - "El hub /servicios muestra las cuatro tarjetas y desde cualquiera de las cuatro guías se vuelve al hub y se puede agendar sin pasar por el inicio (SVC-05, CTX-03, CTX-05)."
    - "El sitemap declara 16 URLs, las 12 de v1.0 más las cuatro rutas del silo."
    - "Escoliosis y ortopedia infantil no muestran bloque de posts relacionados, porque no tienen post que las alimente, y no muestran un vacío (CTX-13)."
    - "Las cuatro páginas emiten MedicalWebPage con about de tipo MedicalCondition, y MedicalProcedure solo donde la página describe efectivamente cirugía (CTX-18)."
    - "Ninguna de las tres páginas nuevas afirma credenciales fuera del CV, cifras de cirugías, tasas de éxito, precios ni experiencia clínica en primera persona (CTX-06, CTX-07, CTX-21)."
  artifacts:
    - path: "src/content/service-pages.ts"
      provides: "las cuatro entradas de ServicePage del silo"
      contains: "estenosis-espinal"
  key_links:
    - from: "src/content/service-pages.ts"
      to: "src/app/servicios/[slug]/page.tsx"
      via: "generateStaticParams deriva las cuatro rutas del array servicePages, sin tocar la plantilla"
      pattern: "generateStaticParams"
    - from: "src/content/service-pages.ts"
      to: "src/app/sitemap.ts"
      via: "el sitemap mapea servicePages, así que pasar de una a cuatro entradas lo lleva de 13 a 16 URLs sin cambiar el archivo"
      pattern: "servicePages"
    - from: "src/content/service-pages.ts"
      to: "src/app/servicios/page.tsx"
      via: "la grilla del hub mapea servicePages a ServiceCard, así que las tres tarjetas nuevas aparecen sin tocar el hub"
      pattern: "ServiceCard"
---

<objective>
Completar el silo con las tres condiciones que faltan: estenosis espinal, escoliosis y deformidades, y ortopedia infantil.

Este plan es puramente de expansión sobre el trazador. La arquitectura ya quedó probada en 08-01: ruta dinámica, orden fijo por tupla, firma, aviso, tabla de contenidos, banner, schema, sitemap y tarjeta del hub. Acá no se toca ni la plantilla, ni el hub, ni el sitemap, ni el schema, ni ningún componente. Se agregan tres entradas al archivo de datos y todo lo demás sale derivado.

Si algo de este plan exigiera modificar `src/app/servicios/[slug]/page.tsx`, es señal de que el modelo de datos de 08-01 quedó corto y hay que corregirlo ahí, no meter una excepción en la plantilla.

Purpose: la SERP de Lima está segmentada por condición. Cuatro URLs dedicadas es lo que separa competir de no competir contra los dominios exact match que hoy ocupan las posiciones 1 y 3.
Output: las cuatro rutas del silo publicables, el hub con sus cuatro tarjetas y el sitemap en 16 URLs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-01-SUMMARY.md
@AGENTS.md
@src/content/service-pages.ts
@src/content/services.ts
@src/content/cv.ts
</context>

## Decisiones de origen que este plan implementa

| Id | Decisión (08-CONTEXT.md) |
|----|--------------------------|
| CTX-01 | URLs anidadas: `/servicios/estenosis-espinal`, `/servicios/escoliosis`, `/servicios/ortopedia-infantil` |
| CTX-02 | Una ruta dinámica alimentada por datos, no cuatro `page.tsx` sueltos |
| CTX-03 | `/servicios` como hub que enlaza a las cuatro hijas |
| CTX-04 | Estructura interna fija e idéntica en las cuatro páginas |
| CTX-05 | Cada página enlaza de vuelta al hub y ofrece CTA de agenda |
| CTX-06 | Consenso clínico general en voz neutra; credenciales solo del CV |
| CTX-07 | No se publica precio de consulta |
| CTX-09 | Tono de v1.0: segunda persona, español neutro peruano |
| CTX-10 | Mínimo 900 palabras por página |
| CTX-13 | Escoliosis y ortopedia infantil quedan sin post que las alimente, y es aceptado |
| CTX-18 | `MedicalWebPage` con `about` `MedicalCondition`; `MedicalProcedure` solo donde se describe cirugía |
| CTX-21 | Se publica sin revisión previa del doctor, con las prohibiciones de contenido listadas |

Estas decisiones ya las tomó el desarrollador el 2026-08-10. Se ejecutan sin volver a preguntar.

<tasks>

<task type="auto" tdd="false">
  <name>Tarea 1: guía de estenosis espinal</name>
  <precondition>`src/content/service-pages.ts` exporta `SERVICE_SECTION_ORDER` y `servicePages`, y `node scripts/check-content.mjs /servicios/hernia-discal` termina con código 0 sobre el build actual.</precondition>
  <reversibility rating="one-way">El slug `/servicios/estenosis-espinal` y sus siete anclas quedan indexados y compartibles apenas sale el deploy; cambiarlos después obliga a redirecciones permanentes. Decisión ya tomada en 08-CONTEXT.md (CTX-01), se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `src/content/service-pages.ts`, la entrada de hernia discal completa, para copiar su forma y su registro de voz
    - `src/content/services.ts`, categoría `columna`
    - `src/content/blog.ts`, el post `estenosis-espinal-que-es`, que es la fuente ya aprobada en v1.0 sobre esta condición
    - `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md`, sección "Salvaguardas de contenido firmado"
  </read_first>
  <files>src/content/service-pages.ts</files>
  <action>
Agregar la entrada `estenosis-espinal` al array `servicePages`, con la misma forma exacta que la entrada de hernia discal.

Campos: `navLabel` "Estenosis espinal", `h1` "Estenosis espinal (canal estrecho)", `conditionName` "Estenosis espinal", `alternateNames` con "Canal estrecho lumbar" y "Estenosis de canal". `title` y `description` para la Metadata API. `cardSummary` de 120 caracteres como máximo. `publishedAt` y `updatedAt` en `2026-08-10`. `describesSurgery` en `true`. `relatedPosts` con el único post que alimenta esta condición según el mapa de CTX-12: `estenosis-espinal-que-es`. `ctaBanner` contextualizado a la condición, con un título que recoja el síntoma que el paciente reconoce (la molestia que aumenta al caminar y cede al sentarse o inclinarse hacia adelante) y un cuerpo de una o dos líneas.

Las siete secciones en el orden de la tupla, con este presupuesto de palabras para que la prosa quede sobre 900 con margen: qué es 150, síntomas 160, cuándo consultar 120, diagnóstico 150, tratamiento 280 repartidas entre sus dos subsecciones, recuperación 140, preguntas frecuentes 200 en cuatro preguntas. La sección `tratamiento` lleva exactamente dos subsecciones, manejo conservador y cuándo se plantea la cirugía. La sección `preguntas-frecuentes` lleva una subsección por pregunta.

Base de contenido admitida (CTX-06): el consenso clínico general que ya recoge el post `estenosis-espinal-que-es` de v1.0 (estrechamiento del canal, forma degenerativa más frecuente después de los 60 y más común en mujeres, claudicación que mejora al sentarse o inclinarse hacia adelante), lo que ya lista `src/content/services.ts` en la categoría de columna, y el CV verificado para cualquier mención de credenciales. Nada más.

Redacción (CTX-09, CTX-21): segunda persona, español neutro peruano, voz explicativa y general, nunca testimonial. Cero signos de admiración, cero emojis. Prohibido afirmar credenciales fuera del CV, cantidad de cirugías, tasas de éxito, promesas de resultado, plazos garantizados y cualquier cifra de precio. Los plazos de recuperación van en voz condicional dentro del párrafo y en rango cualitativo, nunca destacados. El repertorio de construcciones en primera persona sobre casos concretos que la puerta rechaza está en `<acceptance_criteria>`; redacta de modo que ninguna aparezca.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` y `npm run lint` terminan con código 0.
2. `node scripts/check-content.mjs /servicios/estenosis-espinal` termina con código 0 e imprime un conteo de palabras de cuerpo de 900 o más.
3. `test -f .next/server/app/servicios/estenosis-espinal.html` devuelve 0.
4. El HTML de la ruta tiene exactamente un `<h1`, siete `<h2` con `id` y `tabindex="-1"`, y exactamente dos `<h3` dentro de la sección con `id="tratamiento"`.
5. `grep -c 'data-mid-cta' .next/server/app/servicios/estenosis-espinal.html` devuelve exactamente 1, y el script reporta que las palabras previas al banner caen entre el 15% y el 35% del cuerpo.
6. El HTML contiene `href="/servicios"` (vuelta al hub) y al menos dos `href="/agendar"`.
7. `.next/server/app/servicios.html` contiene `href="/servicios/estenosis-espinal"` exactamente una vez, y conserva `id="columna"`, `id="traumatologia"`, `id="ortopedia-infantil"` e `id="procedimientos"`.
8. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve 14.
9. El HTML no contiene ninguna coincidencia de un dígito seguido de `%` ni de `S/` seguido de dígitos, y ningún `h2` ni `h3` contiene dígitos.
10. Ni el HTML ni `src/content/service-pages.ts` contienen ninguna de estas expresiones, comparadas en minúsculas y sin tildes:

```
en mi experiencia
mi experiencia
he operado
he tratado
he visto
mis pacientes
en los casos que atiendo
en mi consulta
en mi practica
yo opero
suelo operar
```

11. `git diff --stat` reporta cambios únicamente en `src/content/service-pages.ts`.
  </acceptance_criteria>
  <done>`/servicios/estenosis-espinal` se prerenderiza estática con 900 palabras o más de prosa, aparece en el hub y en el sitemap, y pasa la puerta de contenido sin tocar plantilla, hub, sitemap ni schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 2: guía de escoliosis y deformidades de columna</name>
  <reversibility rating="one-way">El slug `/servicios/escoliosis` y sus anclas quedan indexados y compartibles apenas sale el deploy. Decisión ya tomada en 08-CONTEXT.md (CTX-01), se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `src/content/service-pages.ts`, las dos entradas ya escritas
    - `src/content/services.ts`, categorías `columna` y `ortopedia-infantil`
    - `src/content/cv.ts`, formación y entrenamientos relacionados con corrección de deformidades
  </read_first>
  <files>src/content/service-pages.ts</files>
  <action>
Agregar la entrada `escoliosis` al array `servicePages`, con la misma forma exacta que las anteriores.

Campos: `navLabel` "Escoliosis", `h1` "Escoliosis y deformidades de columna", `conditionName` "Escoliosis", `alternateNames` con "Deformidad de columna" y "Cifosis". `title` y `description` para la Metadata API. `cardSummary` de 120 caracteres como máximo. `publishedAt` y `updatedAt` en `2026-08-10`. `describesSurgery` en `true`. `relatedPosts` como array vacío: ningún post de v1.0 alimenta esta condición y así lo aceptó el desarrollador (CTX-13). Con el array vacío, la plantilla de 08-01 no renderiza el bloque de posts relacionados y no muestra un vacío. `ctaBanner` contextualizado a la condición.

Las siete secciones en el orden de la tupla, con el mismo presupuesto de palabras del plan anterior, prosa total sobre 900. La sección `tratamiento` cubre el manejo conservador (observación con controles, corsé en curvas en crecimiento, fisioterapia) y cuándo se plantea la cirugía (corrección y artrodesis en varios niveles), que son exactamente los dos abordajes que ya declara `src/content/services.ts` en `procedureApproaches`. Escribir la sección de recuperación reconociendo que en deformidad el plan es individual y depende de la magnitud de la curva y de la edad, siempre en voz condicional y sin plazos destacados.

Base de contenido admitida: consenso clínico general en voz neutra, lo que ya lista `src/content/services.ts` (escoliosis y otras deformidades, escoliosis en niños y adolescentes, corrección de escoliosis y deformidades, artrodesis en varios niveles) y el CV verificado. La formación en corrección de deformidades que aparece en `src/content/cv.ts` se puede mencionar como formación, nunca como volumen de casos ni como resultado.

Redacción: idénticas reglas que la tarea 1. Voz explicativa y general, nunca testimonial, cero cifras de resultado, cero precio, plazos en rango cualitativo dentro del párrafo.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-content.mjs /servicios/escoliosis</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` y `npm run lint` terminan con código 0.
2. `node scripts/check-content.mjs /servicios/escoliosis` termina con código 0 e imprime un conteo de palabras de cuerpo de 900 o más.
3. El HTML de la ruta tiene exactamente un `<h1`, siete `<h2` con `id` y `tabindex="-1"`, y exactamente dos `<h3` dentro de `id="tratamiento"`.
4. `grep -c 'data-mid-cta' .next/server/app/servicios/escoliosis.html` devuelve exactamente 1 y el script confirma la ventana del 15% al 35%.
5. El HTML de la ruta no contiene la cadena `/blog/`: sin `relatedPosts`, el bloque de posts relacionados no se renderiza (CTX-13).
6. El HTML contiene `href="/servicios"` y al menos dos `href="/agendar"`.
7. `.next/server/app/servicios.html` contiene `href="/servicios/escoliosis"` exactamente una vez y conserva sus cuatro `id` de sección heredados.
8. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve 15.
9. El HTML no contiene ninguna coincidencia de un dígito seguido de `%` ni de `S/` seguido de dígitos, y ningún `h2` ni `h3` contiene dígitos.
10. `node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal /servicios/escoliosis` termina con código 0: la puerta de primera persona y de campos prohibidos sigue verde sobre el archivo de datos completo.
11. `git diff --stat` reporta cambios únicamente en `src/content/service-pages.ts`.
  </acceptance_criteria>
  <done>`/servicios/escoliosis` se prerenderiza estática con 900 palabras o más de prosa, sin bloque de posts relacionados, y pasa la puerta de contenido.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 3: guía de ortopedia infantil y cierre del silo</name>
  <reversibility rating="one-way">El slug `/servicios/ortopedia-infantil` y sus anclas quedan indexados y compartibles apenas sale el deploy. Decisión ya tomada en 08-CONTEXT.md (CTX-01), se ejecuta sin checkpoint.</reversibility>
  <read_first>
    - `src/content/service-pages.ts`, las tres entradas ya escritas
    - `src/content/services.ts`, categoría `ortopedia-infantil`
    - `src/app/servicios/page.tsx`, para confirmar que el anclaje `#ortopedia-infantil` del hub sigue intacto y es distinto de la ruta nueva
  </read_first>
  <files>src/content/service-pages.ts</files>
  <action>
Agregar la entrada `ortopedia-infantil` al array `servicePages` y dejar el silo completo en cuatro.

Campos: `navLabel` "Ortopedia infantil", `h1` "Ortopedia infantil", `conditionName` "Condiciones ortopédicas en niños y adolescentes", `alternateNames` con las condiciones concretas que ya lista `src/content/services.ts`: "Displasia congénita de cadera", "Alteraciones de la marcha", "Escoliosis en niños y adolescentes" y "Deformidades de postura". `title` y `description` para la Metadata API. `cardSummary` de 120 caracteres como máximo. `publishedAt` y `updatedAt` en `2026-08-10`. `relatedPosts` como array vacío (CTX-13). `ctaBanner` contextualizado, dirigido a padres y madres, en segunda persona.

`describesSurgery`: fijarlo en `true` solo si la sección `tratamiento` de esta página describe efectivamente un abordaje quirúrgico. Si el contenido que se escribe se queda en observación, ortesis y seguimiento del crecimiento, fijarlo en `false` y no referenciar ningún procedimiento en el schema (CTX-18). El valor tiene que corresponder con lo que el texto realmente dice, no fijarse por inercia.

Las siete secciones en el orden de la tupla, con el mismo presupuesto de palabras, prosa total sobre 900. Registro particular de esta página: la persona que lee es el padre o la madre, no el paciente. Segunda persona igual, pero hablando del hijo o la hija. La sección `cuando-consultar` es la más importante de esta página, porque el motivo real de consulta suele ser una duda sobre el desarrollo, no un dolor. La sección `tratamiento` lleva exactamente dos subsecciones, igual que las otras tres páginas, para que el orden sea idéntico por construcción.

Base de contenido admitida: consenso clínico general en voz neutra sobre desarrollo músculo esquelético en niños y adolescentes, lo que ya lista `src/content/services.ts` en la categoría de ortopedia infantil, y el CV verificado. Nada más. Prohibido sugerir urgencia alarmista: el tono es el mismo del sitio, explicativo y tranquilo.

Redacción: idénticas reglas que las tareas 1 y 2.
  </action>
  <verify>
    <automated>npm run build && npm run lint && node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal /servicios/escoliosis /servicios/ortopedia-infantil</automated>
  </verify>
  <acceptance_criteria>
1. `npm run build` y `npm run lint` terminan con código 0.
2. `node scripts/check-content.mjs` con las cuatro rutas de servicio termina con código 0, y las cuatro reportan 900 palabras de cuerpo o más.
3. Los cuatro archivos existen: `.next/server/app/servicios/hernia-discal.html`, `estenosis-espinal.html`, `escoliosis.html` y `ortopedia-infantil.html`.
4. Las cuatro rutas tienen exactamente un `<h1`, siete `<h2` con `id` y `tabindex="-1"`, y exactamente dos `<h3` dentro de `id="tratamiento"`.
5. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve exactamente 16.
6. `.next/server/app/servicios.html` contiene los cuatro `href="/servicios/{slug}"`, uno por condición, y conserva `id="columna"`, `id="traumatologia"`, `id="ortopedia-infantil"` e `id="procedimientos"`.
7. `/servicios#ortopedia-infantil` y `/servicios/ortopedia-infantil` son destinos distintos y ambos resuelven: el ancla sigue en el hub y la ruta tiene su propio archivo HTML.
8. Si `describesSurgery` de esta entrada es `false`, el HTML de `/servicios/ortopedia-infantil` no contiene `"@type":"MedicalProcedure"` ni referencias a `#procedimiento-`; si es `true`, sí las contiene. Las dos ramas son válidas, lo que no es válido es que el valor no corresponda con lo que dice el texto de la sección de tratamiento.
9. Las cuatro rutas emiten `"@type":"MedicalWebPage"` y `"@type":"MedicalCondition"`, y ninguna contiene `reviewedBy` ni `lastReviewed`.
10. Ninguna de las cuatro rutas contiene un dígito seguido de `%` ni `S/` seguido de dígitos, y ningún `h2` ni `h3` de las cuatro contiene dígitos.
11. `git diff --stat` reporta cambios únicamente en `src/content/service-pages.ts`.
  </acceptance_criteria>
  <done>Las cuatro URLs del silo existen, cada una con 900 palabras o más de prosa, el hub las enlaza a las cuatro, el sitemap declara 16 URLs y los cuatro anclajes vivos del hub siguen resolviendo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/content/service-pages.ts` en build a HTML estático publicado | Prosa clínica de tres condiciones más, firmada por un médico colegiado que no la revisó antes de publicar. Sin entrada de usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Information disclosure | Prosa clínica de estenosis, escoliosis y ortopedia infantil | high | mitigate | `scripts/check-content.mjs` corre sobre cada ruta nueva en el `<verify>` de su tarea y verifica SAFE-05, SAFE-08, SAFE-09 y SAFE-10 sobre el HTML renderizado y sobre el archivo de datos. El tipo `ServiceSection` no expone ningún campo de vivencia ni de resultado, así que la salvaguarda es estructural además de textual. |
| T-08-07 | Spoofing | `MedicalProcedure` referenciado en una página que no describe cirugía | low | mitigate | `describesSurgery` se fija según lo que el texto dice, y el criterio 8 de la tarea 3 exige que schema y prosa concuerden. Se reusan los `@id` de procedimiento ya existentes, no se declara ninguno nuevo. |
| T-08-08 | Denial of service | Ruta dinámica `[slug]` con slug arbitrario | low | accept | Las cuatro rutas se prerenderizan estáticas desde `generateStaticParams` y cualquier slug fuera de esa lista cae en `notFound()`. No hay cómputo por petición. |
| T-08-SC | Tampering | Instalaciones de npm | high | mitigate | La fase no instala paquetes. Criterio 11 de cada tarea exige que el único archivo modificado sea `src/content/service-pages.ts`, así que `package.json` no cambia. |
</threat_model>

## Artifacts this phase produces

Aporte de este plan.

**Rutas nuevas**
- `/servicios/estenosis-espinal`
- `/servicios/escoliosis`
- `/servicios/ortopedia-infantil`

**Entradas de datos nuevas en `src/content/service-pages.ts`**
- `estenosis-espinal` (`describesSurgery: true`, un post relacionado)
- `escoliosis` (`describesSurgery: true`, sin posts relacionados)
- `ortopedia-infantil` (`describesSurgery` según lo que describa su sección de tratamiento, sin posts relacionados)

**Archivos nuevos:** ninguno. **Componentes nuevos:** ninguno. **Tipos nuevos:** ninguno. **Claves nuevas de `CtaLocation`:** ninguna.

<verification>
1. `npm run build` y `npm run lint` terminan con código 0.
2. `node scripts/check-content.mjs /servicios/hernia-discal /servicios/estenosis-espinal /servicios/escoliosis /servicios/ortopedia-infantil` termina con código 0.
3. `grep -o '<loc>' .next/server/app/sitemap.xml.body | wc -l` devuelve exactamente 16.
4. `.next/server/app/servicios.html` contiene cuatro enlaces de tarjeta, uno por ruta hija, y conserva sus cuatro `id` de sección de v1.0.
5. `git diff --stat` reporta un único archivo modificado en toda la fase de este plan: `src/content/service-pages.ts`.
6. `.next/server/app/blog.html` y los cuatro HTML de posts siguen generándose sin cambios: este plan no toca el blog.
</verification>

<success_criteria>
- Las cuatro URLs del silo existen y se prerenderizan estáticas.
- Cada una supera las 900 palabras de prosa de cuerpo, con las siete secciones en el mismo orden.
- El hub muestra las cuatro tarjetas y cada guía vuelve al hub y ofrece agendar sin pasar por el inicio.
- El sitemap declara 16 URLs.
- Escoliosis y ortopedia infantil no muestran bloque de posts relacionados ni un estado vacío.
- Las cuatro emiten `MedicalWebPage` con `about` de tipo `MedicalCondition`, y `MedicalProcedure` solo donde la prosa describe cirugía.
- La puerta de contenido pasa sobre las cuatro rutas.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-02-SUMMARY.md` when done
</output>
