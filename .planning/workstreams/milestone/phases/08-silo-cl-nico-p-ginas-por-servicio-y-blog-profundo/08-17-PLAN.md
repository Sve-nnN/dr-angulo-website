---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 17
type: execute
wave: 8
depends_on: ["08-05", "08-09", "08-16"]
files_modified:
  - src/content/location-pages.ts
  - src/content/location-pages/index.ts
  - src/content/location-pages/types.ts
  - src/content/location-pages/consultorio-privado.ts
  - src/content/location-pages/clinica-ricardo-palma.ts
  - src/content/location-pages/sanna-la-molina.ts
  - src/content/location-pages/clinica-tezza.ts
  - src/app/sedes/[slug]/page.tsx
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
autonomous: true
requirements: [SVC-05]
must_haves:
  truths:
    - "Las cuatro fichas de sede admiten cuerpo largo con el esqueleto de cinco secciones del formato ficha-de-sede."
    - "/sedes/clinica-ricardo-palma y /sedes/clinica-tezza publican su copy aprobado completo, más de 1000 palabras cada una."
    - "Ninguna de las dos páginas afirma un dato operativo que el paquete dejó sin confirmar."
    - "check-sedes.mjs sigue en verde: el NAP, el schema y los canales de agenda de las cuatro sedes no cambiaron."
  artifacts:
    - src/content/location-pages/types.ts
    - src/content/location-pages/clinica-ricardo-palma.ts
    - src/content/location-pages/clinica-tezza.ts
  key_links:
    - "La plantilla de sede reutiliza el renderizador de cuerpo largo del plan 08-09: es la cuarta superficie que lo necesita y no se vuelve a escribir la lógica de anclas y foco."
    - "src/content/locations.ts sigue siendo la única fuente del NAP; el cuerpo nuevo no copia direcciones, horarios ni teléfonos."
---

<objective>
Abrir las fichas de sede al cuerpo largo del paquete y publicar las dos primeras.

Las cuatro páginas de sede salieron en la fase 9, antes de que existiera el paquete de
v1.2, y hoy son fichas cortas: `heroLead`, cómo llegar y una lista de guías. El paquete
las redactó enteras con el esqueleto `ficha-de-sede`, cinco secciones de nivel 2
(`donde-queda`, `como-llegar`, `que-se-atiende`, `horarios`, `como-agendar`) y entre
1095 y 1263 palabras cada una.

Purpose: es una de las cinco URLs que el paquete entregó y que ninguna fase de v1.1
recogía. Juan decidió el 2026-08-13 sumarlas a la fase 8.
Output: el modelo de sede abierto a secciones, la plantilla renderizándolas y dos sedes
publicadas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-HANDOFF-V11-ONPAGE.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-06-PLAN.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-09-SUMMARY.md
</context>

<protocolo_de_transcripcion>
Rige el protocolo de `08-06-PLAN.md`, con la fuente en `seo-tools/data/copy-sedes.json`. Ids del
campo `clave` literal, texto del arreglo `parrafos`, sin las líneas de sello de revisión del
Markdown. Autorización: `15-APROBACION-DOCTOR.md`, 2026-08-13, que cubre las fichas de sede.

El punto 6 vale igual: el `h1` es de esta fase, `title` y `description` son de la fase 10.
</protocolo_de_transcripcion>

<datos_operativos_sin_confirmar>
El handoff declara **10 datos operativos sin confirmar** repartidos en las cuatro sedes: piso y
consultorio en tres, seguros y convenios en cuatro, estacionamiento en Surco y dos de precio que
D-10 prohíbe publicar de todos modos.

**El paquete ya los resolvió por omisión y este plan no los resuelve de nuevo.** El copy aprobado
no afirma ninguno: donde la SERP pregunta por precio, la prosa explica de qué depende sin dar
cifra, y donde faltaría un piso o un convenio, sencillamente no se menciona. Cada documento los
lista aparte, bajo el encabezado "Pendientes de confirmación antes de publicar", precisamente
para que publicarlos sin confirmarlos obligue a saltearse un encabezado que dice que faltan.

La regla operativa de este plan, y del 08-18:

- Se transcribe el copy tal como está. Al no afirmar el dato pendiente, la página se puede
  publicar sin inventar nada.
- **Prohibido completar un dato pendiente con un valor plausible**, deducirlo de otra sede o
  redondear un horario. Un piso equivocado manda a un paciente a otro consultorio.
- La tabla de pendientes **no se transcribe al sitio**: es material de planificación, no copy.
  Se transcribe al SUMMARY, para que quede el registro de qué sigue faltando.
- Si al transcribir aparece un párrafo que sí afirma uno de los diez, el plan se detiene y lo
  reporta en vez de publicarlo.
</datos_operativos_sin_confirmar>

<tasks>

<task type="auto">
  <name>Task 1: el modelo de sede admite secciones y la plantilla las renderiza</name>
  <files>src/content/location-pages.ts, src/content/location-pages/index.ts, src/content/location-pages/types.ts, src/content/location-pages/consultorio-privado.ts, src/content/location-pages/clinica-ricardo-palma.ts, src/content/location-pages/sanna-la-molina.ts, src/content/location-pages/clinica-tezza.ts, src/app/sedes/[slug]/page.tsx</files>
  <read_first>
    - `src/content/location-pages.ts` completo: el tipo `LocationPage` y las cuatro entradas, con la cabecera que explica el reparto con `locations.ts`.
    - `src/app/sedes/[slug]/page.tsx` completo.
    - `src/content/sections.ts` y `src/components/content/content-body.tsx` tal como los dejaron los planes 08-05 y 08-09.
    - `seo-tools/src/phase15/serp-onpage.ts`, el esqueleto `FICHA_DE_SEDE` y sus cinco claves.
    - `scripts/check-sedes.mjs` entero: su MANIFEST hardcodea el `h1` y el `title` de cada sede, el NAP, el tipo de schema, las coordenadas y los canales de agenda.
  </read_first>
  <action>
Partir `src/content/location-pages.ts` en el directorio `src/content/location-pages/`, con
`types.ts`, `index.ts` y un módulo por sede, dejando el archivo suelto como barril delgado para
que ninguno de sus consumidores cambie el import. Es el mismo movimiento que el plan 08-05 hizo
con las guías y los posts, y por la misma razón: dos planes hermanos no pueden escribir el mismo
archivo.

En el tipo `LocationPage`, sumar `sections: ContentSection[]` y
`outboundLinks?: { href: string; anchor: string }[]`, reutilizando el modelo de
`src/content/sections.ts`. **No se suma `format`**: las cuatro sedes son `ficha-de-sede` y no hay
otra opción; el esqueleto se declara en la puerta, que es donde vive la expectativa. Se conservan
sin cambios `slug`, `navLabel`, `title`, `description`, `h1`, `heroLead`, `cardSummary`,
`gettingThere` y `conditionsLead`.

Ojo con `gettingThere`: el paquete trae una sección `como-llegar` que cubre lo mismo. **No se
borra el campo en este plan.** Cada sede decide en su propia tarea si su `como-llegar` reemplaza
al bloque heredado o convive con él, y esa decisión se toma leyendo los dos textos, no por regla
general. Lo que no puede pasar es que la página diga dos veces cómo llegar con palabras
distintas.

En `src/app/sedes/[slug]/page.tsx`: renderizar `page.sections` con el renderizador compartido de
`content-body.tsx`, envolver la superficie medible con `data-content-body`, y sumar la firma, el
aviso educativo único de cierre, el índice y el banner del primer tercio con los mismos criterios
que las otras tres superficies. Es la cuarta que los necesita y por eso el 08-09 sacó el
renderizador a un componente: **no se vuelve a escribir la lógica de anclas, foco y jerarquía**.

Un solo `h1` por página, el que ya tiene. Las cinco secciones del paquete son nivel 2 con su `id`
y `tabIndex={-1}`, y los bloques heredados que se conserven no pueden competir por ese nivel sin
respetar la jerarquía del contrato de accesibilidad.

Las cuatro entradas se migran **sin tocar una palabra de su texto**: en esta tarea `sections`
queda vacío en las cuatro y el contenido llega en las tareas siguientes y en el plan 08-18. Una
sede con `sections` vacío tiene que seguir renderizando exactamente lo que renderiza hoy.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/content/location-pages/*.ts | wc -l` devuelve 6.
    - Ningún consumidor cambió su import: `grep -rc "@/content/location-pages" src/ | grep -v ":0"` lista los mismos archivos que antes del plan.
    - Con `sections` vacío en las cuatro, el HTML de las cuatro rutas de sede tiene el mismo conteo de palabras que antes del plan. Se anota el antes y el después.
    - `node scripts/check-sedes.mjs` y `node scripts/check-seo.mjs` en 0.
    - `src/app/sedes/[slug]/page.tsx` importa el renderizador compartido y no vuelve a declarar el recorrido de secciones.
  </acceptance_criteria>
  <done>Las fichas de sede admiten cuerpo largo y las cuatro siguen renderizando lo mismo que antes.</done>
</task>

<task type="auto">
  <name>Task 2: el cuerpo aprobado de la Clínica Ricardo Palma y de la Clínica Tezza</name>
  <files>src/content/location-pages/clinica-ricardo-palma.ts, src/content/location-pages/clinica-tezza.ts</files>
  <read_first>
    - `.planning/.../paquetes/sedes-clinica-ricardo-palma.md` y `paquetes/sedes-clinica-tezza.md` enteros, incluidas su tabla de datos operativos respaldados y su tabla "Pendientes de confirmación antes de publicar".
    - En `seo-tools/data/copy-sedes.json`, los objetos de esas dos URLs: 16 secciones cada uno, 5 de nivel 2 y 11 de nivel 3, más sus 6 `enlacesPropuestos` y su arreglo `datosOperativos`.
    - `src/content/locations.ts`, para confirmar qué datos del NAP ya están publicados y verificados.
  </read_first>
  <action>
Escribir `sections` de las dos sedes con las 16 secciones de su paquete, aplicando el protocolo de
transcripción, y `outboundLinks` con sus 6 entradas de `enlacesPropuestos`.

`h1` pasa al valor del dataset. Para Ricardo Palma es `Atención de columna en la Clínica Ricardo
Palma` y para Tezza `Ortopedia infantil en la Clínica Padre Luis Tezza`, los dos distintos de los
que la página publica hoy. `title` y `description` no se tocan: son de la fase 10.

**El cuerpo no repite el NAP.** Dirección, geo, teléfono, horario y canales de agenda se siguen
leyendo de `src/content/locations.ts` en tiempo de render, que es la regla que la cabecera de
`location-pages.ts` declara desde la fase 9. Si un párrafo del paquete repite un dato que la
plantilla ya emite desde `locations.ts`, se transcribe igual: el paquete lo escribió como prosa
de lectura, no como ficha, y las dos superficies conviven. Lo que no se hace es copiar el dato al
archivo de contenido como campo propio, porque entonces habría dos fuentes que pueden divergir.

Regla de negocio que sigue viva y que estas dos sedes tocan de lleno: **el WhatsApp del doctor
agenda solo el consultorio privado.** Ninguna sede de tipo clínica ofrece ese canal ni menciona su
número. El copy del paquete la respeta; si al transcribir apareciera una frase que la rompe, se
detiene y se reporta.

Aplicar la regla de datos sin confirmar de este plan: para Ricardo Palma quedan pendientes el piso
y el consultorio, y los seguros y convenios; para Tezza, los planes y convenios vigentes y el piso
y consultorio. Ninguno se completa.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-sedes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Cada módulo declara 16 secciones, 5 de nivel 2 con las claves del esqueleto `ficha-de-sede` en orden y 11 de nivel 3.
    - El HTML de cada ruta supera las 1000 palabras de cuerpo.
    - `h1` es el valor del dataset en las dos, y `title` y `description` quedaron sin tocar.
    - `outboundLinks` declara 6 entradas por sede con los anchors literales del dataset.
    - `grep -c "Pendiente de aprobación"` sobre los dos módulos devuelve 0.
    - Ninguno de los dos módulos menciona el número de WhatsApp del doctor: `grep -c "964305682\|964 305 682"` devuelve 0 en ambos.
    - Los cuatro datos pendientes de estas dos sedes siguen sin afirmarse en el HTML, y la tabla de pendientes se transcribe en el SUMMARY.
  </acceptance_criteria>
  <done>Las dos sedes publican su copy aprobado sin afirmar ningún dato operativo sin confirmar.</done>
</task>

<task type="auto">
  <name>Task 3: las dos puertas aprenden las cuatro fichas de sede</name>
  <files>scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `scripts/check-content.mjs`, el `MANIFEST` y la comprobación de esqueleto que sumó el plan 08-05.
    - `scripts/check-sedes.mjs`, su `MANIFEST`, que hardcodea el `h1` y el `title` de cada sede.
  </read_first>
  <action>
Sumar el esqueleto `ficha-de-sede` a la comprobación de orden canónico de
`scripts/check-content.mjs`, con sus cinco claves literales copiadas de `serp-onpage.ts`, y sumar
al `MANIFEST` las **dos** rutas de sede que ya publican cuerpo, con ese formato. Las otras dos
las suma el plan 08-18 cuando su contenido exista: la puerta no lista una ruta cuyo cuerpo
todavía no está escrito, porque exigiría 900 palabras a una página que aún no las tiene.

Actualizar en `scripts/check-sedes.mjs` el `h1` esperado de las dos sedes que este plan cambia. El
`title` **no se toca**: sigue siendo el de hoy hasta que la fase 10 lo reescriba, y esa puerta lo
verifica contra el valor publicado.

`SITEMAP_TOTAL` no se toca en este plan ni en el 08-18 ni en el 08-19: las cinco URLs que estos
tres planes recogen ya existen y ya están en el sitemap. La fase cierra en 22 y ese número lo fijó
el plan 08-14.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - La puerta de contenido corre en verde en corrida completa, con las dos rutas de sede nuevas dentro del manifiesto.
    - La comprobación de esqueleto verifica esas dos rutas contra la tupla de `ficha-de-sede` y no contra otra.
    - `node scripts/check-sedes.mjs` en 0 con los `h1` nuevos.
    - `grep -c "SITEMAP_TOTAL = 22" scripts/check-content.mjs` devuelve 1: este plan no mueve el conteo.
  </acceptance_criteria>
  <done>Las puertas cubren las dos sedes publicadas sin exigirle cuerpo a las que todavía no lo tienen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dato operativo sin confirmar → paciente | Un piso, un seguro o un horario equivocado manda a alguien a un lugar donde no lo atienden |
| paquete de v1.2 → contenido publicado | Texto clínico YMYL que sale con el nombre del doctor |
| canal de agenda → regla de negocio | El WhatsApp del doctor agenda solo el consultorio privado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-45 | Tampering | los 10 datos operativos sin confirmar | critical | mitigate | El copy aprobado no los afirma y este plan prohíbe completarlos, deducirlos de otra sede o redondearlos; la tabla de pendientes va al SUMMARY y no al sitio |
| T-08-46 | Spoofing | canal de agenda en una sede de clínica | high | mitigate | Criterio de aceptación con búsqueda del número del doctor en los dos módulos, con cero coincidencias |
| T-08-47 | Tampering | NAP duplicado entre contenido y `locations.ts` | medium | mitigate | El cuerpo no copia el NAP como campo propio; sigue derivándose de `locations.ts` en render |
| T-08-48 | Denial of service | puerta de sedes rota por el h1 nuevo | medium | mitigate | El `h1` esperado se actualiza en el mismo plan que lo cambia, y el `title` se deja para la fase 10 |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint` y `npm run build` en verde.
- Las tres puertas en verde, con el conteo del sitemap intacto en 22.
- Conteo de palabras de las cuatro sedes idéntico antes y después de la Task 1.
- Tabla de datos pendientes de las dos sedes transcrita en el SUMMARY.
</verification>

<success_criteria>
Las fichas de sede admiten cuerpo largo, dos de ellas publican su copy aprobado completo, y
ninguna afirma un dato operativo que nadie confirmó.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-17-SUMMARY.md` when done
</output>
