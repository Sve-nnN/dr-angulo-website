---
phase: 08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo
plan: 16
type: execute
wave: 4
depends_on: ["08-05", "08-09", "08-15"]
files_modified:
  - src/content/service-pages/cirugia-minimamente-invasiva.ts
  - src/content/service-pages/index.ts
  - src/content/static-pages/hub-servicios.ts
  - src/app/servicios/page.tsx
  - scripts/check-content.mjs
  - scripts/check-sedes.mjs
autonomous: true
requirements: [SVC-05]
must_haves:
  truths:
    - "/servicios/cirugia-minimamente-invasiva existe, se prerenderiza, entra al sitemap y aparece como quinta tarjeta del hub."
    - "El hub /servicios publica el cuerpo aprobado del paquete sin perder sus cuatro anclas de sección de v1.0, a las que el inicio enlaza."
    - "Cada una de las cinco páginas del silo enlaza de vuelta al hub y ofrece CTA de agenda sin obligar a volver al inicio."
  artifacts:
    - src/content/service-pages/cirugia-minimamente-invasiva.ts
    - src/content/static-pages/hub-servicios.ts
  key_links:
    - "La página nueva entra al sitemap, al hub, a llms.txt y a su imagen de Open Graph sin tocar ninguno de esos archivos, porque los cuatro derivan de servicePages."
    - "El hub reutiliza el renderizador de cuerpo largo que extrajo el plan 08-09, en vez de duplicar por tercera vez la lógica de anclas y foco."
---

<objective>
Cerrar el silo: entra la quinta página, que es nueva, y el hub `/servicios` publica su
cuerpo aprobado.

`/servicios/cirugia-minimamente-invasiva` no existe hoy. La creó el mapa de v1.2 porque
`cirugía mínimamente invasiva en lima` es una keyword de oro y ninguna URL del sitio podía
ganarla. El hub, por su parte, es hoy un índice sin cuerpo, y el paquete le asigna 1092
palabras con el esqueleto de cinco secciones.

**Este plan cubre la parte de SVC-05 que había quedado sin dueño.** En la consolidación
del set, la página nueva y el hub estaban descritos en un plan que después se reasignó a
ortopedia infantil, y ningún `files_modified` volvía a declararlos.

Purpose: SVC-05 pide que `/servicios` funcione como hub del silo. Con cuatro hijas y sin
cuerpo propio se cumple a medias; con las cinco y el copy medido, se cumple.
Output: el módulo de la página nueva, el cuerpo del hub y el conteo de URLs en 22.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-UI-SPEC.md
@.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/15-APROBACION-DOCTOR.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-06-PLAN.md
@.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-09-SUMMARY.md
</context>

<protocolo_de_transcripcion>
Rige el protocolo escrito en `08-06-PLAN.md`, con la fuente en
`seo-tools/data/copy-servicios.json`. Ids del campo `clave` literal, texto del arreglo
`parrafos`, sin las líneas de sello de revisión del Markdown. Autorización:
`15-APROBACION-DOCTOR.md`, 2026-08-13.

Con la diferencia que ya tienen los planes de URL nueva: la página de cirugía mínimamente
invasiva necesita `title`, `description` y `h1` para poder existir, y los tres salen del mismo
objeto del dataset, que es la fuente que va a leer la fase 10. El hub, en cambio, ya existe:
su `h1` sí lo escribe esta fase por el punto 6, y su `title` y su `description` no se tocan.
</protocolo_de_transcripcion>

<tasks>

<task type="auto">
  <name>Task 1: la página nueva de cirugía mínimamente invasiva</name>
  <files>src/content/service-pages/cirugia-minimamente-invasiva.ts, src/content/service-pages/index.ts</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/servicios-cirugia-minimamente-invasiva.md` entero, incluida la tabla "Title, meta y H1".
    - En `seo-tools/data/copy-servicios.json`, el objeto cuya `url` es `/servicios/cirugia-minimamente-invasiva`: sus 17 secciones, 5 de nivel 2 y 12 de nivel 3, y sus 8 `enlacesPropuestos`.
    - `src/content/service-pages/ortopedia-infantil.ts` tal como lo dejó el plan 08-08: es el molde de una página del mismo formato ya migrada.
    - `src/components/services/service-card.tsx`, que consume `cardSummary` con un máximo de 120 caracteres.
  </read_first>
  <action>
Crear el módulo con las 17 secciones del paquete: 5 de nivel 2 (`que-se-atiende`,
`como-es-la-consulta`, `condiciones`, `donde-se-atiende`, `como-agendar`) y 12 de nivel 3,
aplicando el protocolo de transcripción. `format` es `"pagina-de-servicio"`, `publishedAt` y
`updatedAt` van a `2026-08-13`, `describesSurgery` en `true`: la página describe la cirugía de
punta a punta.

Sumarlo a `servicePages` en `index.ts`, al final del arreglo, para no mover el orden de las
tarjetas que ya existen.

Los campos que el paquete no redacta se componen, no se inventan. `title`, `description` y `h1`
salen del objeto del dataset. `heroLead`, `cardSummary` y `ctaBanner` se arman **con frases
tomadas del cuerpo aprobado**, recortadas y no reescritas, siguiendo el contrato de copy de la
UI-SPEC: segunda persona, verbo más sustantivo en los CTA, cero promesa de resultado, cero signos
de admiración. `cardSummary` no pasa de 120 caracteres. `navLabel` y `conditionName` describen el
procedimiento. `relatedPosts` lista el post de cirugía de columna, que es el que la matriz apunta
hacia acá con una mención inversa.

`outboundLinks` se puebla con las 8 entradas de `enlacesPropuestos`, con sus `href` y `anchor`
literales.

Ojo con la sección de cómo agendar: la SERP de esta keyword está llena de preguntas de precio, y
el paquete las responde sin publicar una cifra. Esa contención es deliberada y se transcribe tal
cual (D-10). La puerta bloquea cifras en soles sobre el cuerpo, así que un desliz sale en rojo.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node scripts/check-content.mjs /servicios/cirugia-minimamente-invasiva</automated>
  </verify>
  <acceptance_criteria>
    - El módulo declara 17 secciones, 5 con `level: 2` y 12 con `level: 3`, y `format` es `"pagina-de-servicio"`.
    - `npm run build` genera `.next/server/app/servicios/cirugia-minimamente-invasiva.html` y su imagen de Open Graph.
    - El HTML supera las 1300 palabras de cuerpo y la comprobación de esqueleto lo verifica contra la tupla de `pagina-de-servicio`.
    - `.next/server/app/servicios.html` enlaza a la ruta nueva: la quinta tarjeta sale sola de `servicePages` y se verifica en el HTML, no en el código.
    - `/llms.txt` prerenderizado nombra la página nueva.
    - `cardSummary` mide 120 caracteres o menos y `grep -c "Pendiente de aprobación"` sobre el módulo devuelve 0.
  </acceptance_criteria>
  <done>La quinta URL del silo existe, publica el copy aprobado y entra sola al sitemap, al hub y a llms.txt.</done>
</task>

<task type="auto">
  <name>Task 2: el cuerpo aprobado del hub /servicios</name>
  <files>src/content/static-pages/hub-servicios.ts, src/app/servicios/page.tsx</files>
  <read_first>
    - `.planning/workstreams/seo-keywords/milestones/v1.2-phases/15-paquete-on-page-por-url/paquetes/servicios.md` entero.
    - En `seo-tools/data/copy-servicios.json`, el objeto cuya `url` es `/servicios`: sus 15 secciones, 5 de nivel 2 y 10 de nivel 3, y sus 8 `enlacesPropuestos`.
    - `src/app/servicios/page.tsx` completo: hoy tiene el h1, la rejilla de tarjetas hacia las páginas del silo, y las secciones de `serviceCategories` con sus cuatro `id` de v1.0.
    - `src/content/static-pages/home.ts` y `src/components/content/content-body.tsx` tal como los dejó el plan 08-09: el molde del modelo de página estática y el renderizador compartido.
    - `scripts/check-content.mjs`, la lista `HUB_ANCHORS`, que exige que las cuatro anclas de v1.0 sigan existiendo porque el inicio enlaza a ellas.
  </read_first>
  <action>
Crear `src/content/static-pages/hub-servicios.ts` con las 15 secciones del paquete y sus 8
`enlacesPropuestos`, con la misma forma que `home.ts`, y renderizarlas en `src/app/servicios/page.tsx`
con el renderizador que extrajo el plan 08-09. **No se duplica la lógica de anclas, foco y
jerarquía**: es la tercera superficie que la necesita y por eso el 08-09 la sacó a un componente.

El cuerpo nuevo **se suma**, no reemplaza lo que la página ya tiene. Se conservan tal cual el
`h1`, la rejilla de tarjetas hacia las cinco hijas y las secciones de `serviceCategories` con sus
cuatro `id` de v1.0 (`columna`, `traumatologia`, `ortopedia-infantil`, `procedimientos`): el
inicio enlaza a esas cuatro anclas y la puerta las verifica. Perderlas rompe enlaces vivos.

Dónde cae el cuerpo: después de la rejilla de tarjetas y antes de las secciones heredadas. La
rejilla es la razón de ser del hub y no se entierra bajo mil palabras de prosa; las secciones
heredadas son el detalle de catálogo y quedan al pie.

Envolver la superficie que mide la puerta con `data-content-body`, y respetar la jerarquía: un
solo `h1`, las cinco secciones del paquete como nivel 2 con su `id` y `tabIndex={-1}`, y las
cuatro secciones heredadas conservando el nivel que ya tienen, sin saltos. Si al sumar el cuerpo
quedaran dos bloques compitiendo por el mismo nivel, manda la jerarquía del contrato de
accesibilidad y se ajusta el heredado, nunca borrando sus `id`.

`h1` pasa al valor del dataset, por el punto 6 del protocolo. `title` y `description` de la
página no se tocan: son de la fase 10.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; node scripts/check-content.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `.next/server/app/servicios.html` conserva los cuatro `id` de v1.0 y la puerta no reporta el fallo de ancla perdida del hub.
    - El HTML del hub declara un solo `h1` y las cinco secciones del paquete como destino de ancla.
    - El hub enlaza a las cinco páginas hijas y la puerta lo confirma en sus comprobaciones globales.
    - `grep -c "Pendiente de aprobación" src/content/static-pages/hub-servicios.ts` devuelve 0.
    - `src/app/servicios/page.tsx` importa el renderizador compartido y no vuelve a declarar el recorrido de secciones: `grep -c "content-body" src/app/servicios/page.tsx` devuelve 1 o más.
  </acceptance_criteria>
  <done>El hub publica su cuerpo aprobado sin perder ninguna de las anclas que el sitio ya enlazaba.</done>
</task>

<task type="auto">
  <name>Task 3: el conteo de URLs sube a 22 en las dos puertas</name>
  <files>scripts/check-content.mjs, scripts/check-sedes.mjs</files>
  <read_first>
    - `scripts/check-content.mjs`, la constante `SITEMAP_TOTAL` con su comentario y el `MANIFEST`.
    - `scripts/check-sedes.mjs`, su propia constante.
  </read_first>
  <action>
Subir `SITEMAP_TOTAL` de 21 a 22 en las dos puertas, en el mismo commit. El valor está duplicado a
propósito en dos archivos, y una puerta que se olvida de la otra sale roja sin que nadie haya roto
nada.

Sumar `/servicios/cirugia-minimamente-invasiva` al `MANIFEST` con formato de página de servicio.

Dejar en el comentario de cada constante la cuenta de la que sale el número, no solo el número: a
esta altura son las 21 de la fase 9 más esta página. Los planes 08-12 y 08-13 lo suben a 23 y 24
al sumar los posts nuevos, y el 08-14 lo baja a 22, que es el número con el que cierra la fase.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-content.mjs &amp;&amp; node scripts/check-sedes.mjs &amp;&amp; node scripts/check-seo.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SITEMAP_TOTAL = 22" scripts/check-content.mjs` devuelve 1 y lo mismo en `scripts/check-sedes.mjs`.
    - El sitemap prerenderizado tiene exactamente 22 entradas.
    - Las tres puertas corren en verde, incluida `check-content.mjs` en corrida completa.
  </acceptance_criteria>
  <done>Las tres puertas están en verde con el conteo que corresponde a esta altura de la fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete de v1.2 → contenido publicado | Texto clínico YMYL que sale con el nombre del doctor |
| página viva del sitio → cuerpo nuevo | El hub tiene anclas que el inicio enlaza desde v1.0 |
| microcopy compuesto a mano → página firmada | `heroLead`, `cardSummary` y `ctaBanner` de la página nueva no vienen del paquete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-40 | Spoofing | prosa clínica firmada | critical | mitigate | Transcripción literal desde el dataset sellado en las dos superficies |
| T-08-41 | Denial of service | anclas del hub perdidas al sumar el cuerpo | high | mitigate | Los cuatro `id` de v1.0 se conservan y la puerta ya los vigila en sus comprobaciones globales |
| T-08-42 | Spoofing | microcopy compuesto de la página nueva | high | mitigate | Los tres campos se recortan de frases del cuerpo aprobado y quedan sujetos al contrato de copy de la UI-SPEC |
| T-08-43 | Information disclosure | preguntas de precio de la SERP | high | mitigate | El paquete responde sin cifra y se transcribe tal cual; la puerta bloquea soles y porcentaje sobre el cuerpo |
| T-08-44 | Denial of service | conteo de URLs desincronizado entre puertas | medium | mitigate | Las dos constantes se mueven en el mismo commit y la corrida completa de las tres puertas es criterio de aceptación |
| T-08-SC | Tampering | instalaciones de paquetes | high | mitigate | Plan sin dependencias nuevas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint` y `npm run build` en verde.
- Las tres puertas en verde en corrida completa, con el sitemap en 22.
- Las cuatro anclas heredadas del hub verificadas sobre el HTML prerenderizado.

**Verificación humana pendiente, no automatizable:** el hub y el inicio son las dos páginas de
más tráfico del sitio y las dos suman cuerpo largo en esta fase. Que la prosa nueva no entierre la
rejilla de tarjetas ni descoloque el catálogo heredado se confirma mirándolo a 375px y a 1440px,
no con una puerta. Queda para la ronda de verificación de la fase.
</verification>

<success_criteria>
El silo cierra con cinco hijas, el hub publica su cuerpo aprobado sin perder ninguna de sus
anclas, y el conteo de URLs queda sincronizado en las dos puertas.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/08-silo-cl-nico-p-ginas-por-servicio-y-blog-profundo/08-16-SUMMARY.md` when done
</output>
