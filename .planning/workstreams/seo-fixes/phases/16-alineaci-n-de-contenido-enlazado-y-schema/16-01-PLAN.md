---
phase: 16-alineaci-n-de-contenido-enlazado-y-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/blog/ciatica.ts
  - src/content/blog/cirugia-de-columna.ts
  - src/content/blog/index.ts
  - src/content/blog/lumbalgia.ts
  - src/content/blog/artrosis.ts
  - src/content/service-pages/hernia-discal.ts
  - src/content/service-pages/cirugia-minimamente-invasiva.ts
  - next.config.ts
  - scripts/check-content.mjs
autonomous: true
requirements: [SLUG-01, SLUG-02]
user_setup: []

must_haves:
  truths:
    - "Un paciente que ve /blog/ciatica en la SERP encuentra un artículo sobre ciática, y quien ve /blog/cirugia-de-columna encuentra uno sobre cirugía de columna (SLUG-01, D-01)."
    - "Los dos slugs viejos responden un solo salto de redirección permanente al slug nuevo, sin cadena intermedia y sin 404 (SLUG-02, D-02)."
    - "Ni el sitemap ni un solo enlace interno del sitio apunta a un slug viejo (SLUG-02, D-03)."
    - "Los dos posts declaran updatedAt en la fecha del cambio, porque su contenido se tocó de verdad (D-04)."
  artifacts:
    - src/content/blog/ciatica.ts
    - src/content/blog/cirugia-de-columna.ts
    - next.config.ts
  key_links:
    - "src/content/blog/index.ts importa cada post por nombre de export: si el import no se renombra, el build cae."
    - "src/app/sitemap.xml/route.ts deriva las URLs del blog de blogPosts, así que el renombre se propaga solo; hay que verificarlo, no suponerlo."
    - "src/content/service-pages/*.ts referencian posts por slug en relatedPosts: un slug huérfano deja la guía sin bloque de artículos relacionados y sin aviso."
    - "scripts/check-content.mjs lista las dos rutas viejas en su MANIFEST: si no se actualiza, la puerta falla contra rutas que ya no existen."
---

<objective>
Renombrar los dos slugs del blog que anuncian un tema distinto al que tratan, y apagar los viejos con una redirección permanente en un solo salto.

`/blog/5-sintomas-de-columna-que-no-debes-ignorar` habla de ciática y `/blog/miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber` habla de cirugía de columna. Ninguna de las dos está indexada, así que el renombre hoy no cuesta nada de posicionamiento; en seis meses sí.

Este plan es el desbloqueante de toda la fase: el plan 16-03 referencia los `@id` que estos slugs generan, y el plan 16-06 reescribe estas mismas dos páginas.

Purpose: que la URL, el title, el H1 y el cuerpo de cada post digan el mismo tema.
Output: dos módulos de contenido renombrados, dos entradas nuevas en `redirects()` y el MANIFEST de la puerta de contenido al día.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/STATE.md
@.planning/workstreams/seo-fixes/phases/16-alineaci-n-de-contenido-enlazado-y-schema/16-CONTEXT.md

@AGENTS.md
@src/content/blog/index.ts
@next.config.ts
</context>

<decision_map>
Las decisiones de `16-CONTEXT.md` no traen numeración propia. Este plan las cita con las etiquetas de abajo, y las mismas etiquetas se usan en los seis planes de la fase.

| ID | Decisión de 16-CONTEXT.md |
|---|---|
| D-01 | Slugs nuevos: `/blog/ciatica` y `/blog/cirugia-de-columna`. Nombran el tema real, cortos, sin relleno. |
| D-02 | El 301 va en `next.config.ts` dentro de `redirects()`, donde ya hay tres precedentes de la fase 14 de v1.2. |
| D-03 | El renombre es completo y en un solo commit por post: archivo de contenido, campo `slug`, nombre del export, import en `src/content/blog/index.ts` y toda referencia interna. |
| D-04 | `updatedAt` se actualiza a la fecha del cambio, porque el contenido se toca de verdad en esta fase. |
</decision_map>

<tasks>

<!-- planner-discipline-allow: 5-sintomas-de-columna-que-no-debes-ignorar -->
<!-- planner-discipline-allow: miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber -->

<task type="auto">
  <name>Task 1: Renombrar el post de ciática a /blog/ciatica</name>
  <files>src/content/blog/ciatica.ts, src/content/blog/index.ts, src/content/blog/lumbalgia.ts, src/content/blog/artrosis.ts, src/content/service-pages/hernia-discal.ts</files>
  <read_first>
    - `src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts` — el módulo que se renombra. Su `slug`, su `title` ("Ciática: por qué duele la pierna y qué hacer") y su `h1` ya hablan de ciática; lo único desalineado es el nombre del archivo, el `slug` y el nombre del export.
    - `src/content/blog/index.ts` — el arreglo `blogPosts` importa cada post por nombre de export. Es el único lugar donde se registran.
    - `src/content/blog/lumbalgia.ts` y `src/content/blog/artrosis.ts` — sus `outboundLinks` apuntan al slug viejo.
    - `src/content/service-pages/hernia-discal.ts` — su campo `relatedPosts` lista el slug viejo.
  </read_first>
  <action>
    Mover el archivo con `git mv src/content/blog/5-sintomas-de-columna-que-no-debes-ignorar.ts src/content/blog/ciatica.ts`, para que el historial siga al contenido en vez de aparecer como borrado más alta.

    Dentro del archivo movido: cambiar el campo `slug` a `ciatica` y renombrar el export de `post5SintomasDeColumnaQueNoDebesIgnorar` a `postCiatica` (D-01, D-03). Poner `updatedAt: "2026-08-24"` (D-04). `title`, `h1`, `description`, `publishedAt`, `sections` e `intro` no se tocan: ya hablan de ciática y este plan no reescribe contenido clínico.

    En `src/content/blog/index.ts`: actualizar el import a `./ciatica` con el nombre nuevo del export y su posición dentro del arreglo `blogPosts`, que no cambia (sigue siendo el primer elemento).

    Actualizar toda referencia interna al slug viejo (D-03): el `href` del `outboundLink` en `src/content/blog/lumbalgia.ts`, el `href` del `outboundLink` en `src/content/blog/artrosis.ts` y la entrada de `relatedPosts` en `src/content/service-pages/hernia-discal.ts`. Los textos de `anchor` no se tocan en este plan: los reescribe el plan 16-05 completo y en una sola pasada.

    No agregar todavía la redirección: va en la tarea 3, después de que el destino exista.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'slug: "ciatica"' src/content/blog/ciatica.ts` devuelve 1.
    - `grep -rn "5-sintomas-de-columna-que-no-debes-ignorar" src/` no devuelve ninguna línea.
    - `grep -rn "post5SintomasDeColumnaQueNoDebesIgnorar" src/` no devuelve ninguna línea.
    - `grep -c 'updatedAt: "2026-08-24"' src/content/blog/ciatica.ts` devuelve 1.
    - `npx tsc --noEmit` termina con código 0.
  </acceptance_criteria>
  <done>El post de ciática vive en `src/content/blog/ciatica.ts` con `slug: "ciatica"`, el proyecto compila y ninguna referencia bajo `src/` menciona el slug anterior.</done>
</task>

<task type="auto">
  <name>Task 2: Renombrar el post de cirugía a /blog/cirugia-de-columna</name>
  <files>src/content/blog/cirugia-de-columna.ts, src/content/blog/index.ts, src/content/blog/lumbalgia.ts, src/content/blog/artrosis.ts, src/content/blog/ciatica.ts, src/content/service-pages/hernia-discal.ts, src/content/service-pages/cirugia-minimamente-invasiva.ts</files>
  <read_first>
    - `src/content/blog/miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber.ts` — el módulo que se renombra. Su `title` es "Cirugía de columna: cuándo se plantea y cómo es" y su `h1` "Operarse de la columna: lo que conviene saber antes".
    - `src/content/blog/index.ts` — tal como lo dejó la tarea 1.
    - `src/content/blog/ciatica.ts`, `src/content/blog/lumbalgia.ts` y `src/content/blog/artrosis.ts` — sus `outboundLinks` apuntan al slug viejo.
    - `src/content/service-pages/hernia-discal.ts` y `src/content/service-pages/cirugia-minimamente-invasiva.ts` — sus campos `relatedPosts` listan el slug viejo.
  </read_first>
  <action>
    Mover el archivo con `git mv src/content/blog/miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber.ts src/content/blog/cirugia-de-columna.ts`.

    Dentro del archivo movido: cambiar el campo `slug` a `cirugia-de-columna` y renombrar el export de `postMiedoAOperarteDeLaColumna5CosasQueDebesSaber` a `postCirugiaDeColumna` (D-01, D-03). Poner `updatedAt: "2026-08-24"` (D-04). El resto del módulo queda como está.

    En `src/content/blog/index.ts`: actualizar el import a `./cirugia-de-columna` con el nombre nuevo del export, conservando la posición dentro de `blogPosts`.

    Actualizar toda referencia interna al slug viejo (D-03): los `href` de `outboundLinks` en `src/content/blog/ciatica.ts`, `src/content/blog/lumbalgia.ts` y `src/content/blog/artrosis.ts`, y las entradas de `relatedPosts` en `src/content/service-pages/hernia-discal.ts` y `src/content/service-pages/cirugia-minimamente-invasiva.ts`. Otra vez: los textos de `anchor` no se tocan acá.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'slug: "cirugia-de-columna"' src/content/blog/cirugia-de-columna.ts` devuelve 1.
    - `grep -rn "miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber" src/` no devuelve ninguna línea.
    - `grep -rn "postMiedoAOperarteDeLaColumna5CosasQueDebesSaber" src/` no devuelve ninguna línea.
    - `grep -c 'updatedAt: "2026-08-24"' src/content/blog/cirugia-de-columna.ts` devuelve 1.
    - `npx tsc --noEmit` termina con código 0.
  </acceptance_criteria>
  <done>El post de cirugía vive en `src/content/blog/cirugia-de-columna.ts` con `slug: "cirugia-de-columna"`, el proyecto compila y ninguna referencia bajo `src/` menciona el slug anterior.</done>
</task>

<task type="auto">
  <name>Task 3: Redirección permanente de los dos slugs viejos y MANIFEST de la puerta al día</name>
  <files>next.config.ts, scripts/check-content.mjs</files>
  <read_first>
    - `next.config.ts` — el bloque `redirects()` ya trae tres entradas permanentes de la fase 14 de v1.2, cada una con su comentario de por qué existe. Es el lugar y el formato exactos (D-02).
    - `node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/redirects.md` — obligatorio antes de escribir: esta versión de Next.js no emite 301. `permanent: true` emite **308** y `permanent: false` emite 307, y la propia doc explica el motivo (preservar el método de la petición). AGENTS.md advierte que las APIs de esta versión difieren de lo que suene familiar; leer la doc antes de tocar la config no es opcional.
    - `scripts/check-content.mjs` líneas 120 a 165 — el arreglo `MANIFEST` lista las rutas del sitio con su tipo y su `linksTo`. Dos entradas usan los slugs viejos.
  </read_first>
  <action>
    Agregar dos entradas nuevas al arreglo que devuelve `redirects()` en `next.config.ts`, con la misma forma que las tres existentes: `source` el slug viejo, `destination` el nuevo, `permanent: true` (D-02).

    - de `/blog/5-sintomas-de-columna-que-no-debes-ignorar` a `/blog/ciatica`
    - de `/blog/miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber` a `/blog/cirugia-de-columna`

    Escribir encima un comentario corto que explique por qué existen: el contenido era ciática y cirugía de columna, la URL anunciaba otra cosa, y ninguna de las dos estaba indexada cuando se renombraron. Que el comentario no repita la tabla del audit, solo el motivo.

    Sobre el código de estado: el requisito SLUG-02 pide "un solo 301". Esta versión de Next.js devuelve 308 con `permanent: true`, y Google consolida 308 igual que 301 desde 2016. Se usa `permanent: true` y no `statusCode: 301` para no partir el formato de las tres redirecciones que ya viven en el archivo. Dejar esa razón escrita en el mismo comentario, en una línea.

    Ninguna de las dos entradas puede apuntar a una URL que a su vez redirija: los destinos son los slugs finales de las tareas 1 y 2, así que el salto es uno solo.

    En `scripts/check-content.mjs`: actualizar las dos entradas del `MANIFEST` a las rutas nuevas `/blog/ciatica` y `/blog/cirugia-de-columna`, conservando su `type: "post"` y su `linksTo: "/servicios/hernia-discal"`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run content:check &amp;&amp; npm run seo:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "permanent: true" next.config.ts` devuelve 5.
    - `grep -c 'destination: "/blog/ciatica"' next.config.ts` devuelve 1 y `grep -c 'destination: "/blog/cirugia-de-columna"' next.config.ts` devuelve 1.
    - `grep -rn "5-sintomas-de-columna-que-no-debes-ignorar\|miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber" scripts/` no devuelve ninguna línea.
    - Tras `npm run build`, `grep -c "/blog/ciatica<" .next/server/app/sitemap.xml.body` devuelve 1 y `grep -c "5-sintomas" .next/server/app/sitemap.xml.body` devuelve 0.
    - `npm run content:check` termina con código 0.
    - `npm run seo:check` termina con código 0 y reporta 22 rutas revisadas (el total no cambia en este plan: dos URLs se renombran, ninguna se agrega).
    - Con el servidor de producción levantado (`npm run start`), `curl -sI http://localhost:3000/blog/5-sintomas-de-columna-que-no-debes-ignorar` responde `308` con `location: /blog/ciatica`, y una segunda petición a esa `location` responde `200`, no otra redirección.
    - Misma comprobación para el segundo par: `curl -sI http://localhost:3000/blog/miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber` responde `308` con `location: /blog/cirugia-de-columna`, y esa `location` responde `200`.
  </acceptance_criteria>
  <done>Las dos URLs viejas responden un único salto permanente a su URL nueva, el sitemap solo lista las nuevas y las dos puertas de contenido y de SEO pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rastreador o navegador → origen Next.js | La única entrada nueva es el `source` de dos redirecciones estáticas. No hay parámetros, ni comodines, ni entrada de usuario que llegue al `destination`. |
| repositorio → build de producción | El renombre de módulos cambia qué URLs se prerenderizan y qué lista el sitemap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | `redirects()` de `next.config.ts` | low | mitigate | `source` y `destination` son cadenas literales sin comodín ni captura, así que no hay redirección abierta posible. La revisión de la tarea 3 comprueba que ningún `destination` sea absoluto ni externo. |
| T-16-02 | Denial of Service | cadena de redirecciones | medium | mitigate | Un `destination` que a su vez redirija produce un bucle o una cadena que gasta presupuesto de rastreo. El criterio de aceptación exige que la segunda petición devuelva 200, no otra redirección. |
| T-16-03 | Information Disclosure | sitemap prerenderizado | low | accept | El sitemap publica solo URLs ya públicas. El criterio verifica que las viejas salgan de la lista. |
| T-16-SC | Tampering | instalaciones de npm/pip/cargo | high | accept | Este plan no instala ningún paquete: no hay superficie de cadena de suministro que abrir. `package.json` no está en `files_modified`. |
</threat_model>

<verification>
1. `npx tsc --noEmit` compila sin errores.
2. `npm run build` termina con código 0 y prerenderiza `/blog/ciatica` y `/blog/cirugia-de-columna`.
3. `npm run content:check` y `npm run seo:check` pasan las dos.
4. `grep -rn "5-sintomas-de-columna-que-no-debes-ignorar\|miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber" src/ scripts/` no devuelve nada. Las dos únicas apariciones que quedan en el repo son los dos `source` de `next.config.ts`, que es donde tienen que estar.
5. Con `npm run start`, las cuatro peticiones de `curl -sI` del criterio de la tarea 3 dan el resultado esperado.
</verification>

<success_criteria>
- SLUG-01: las dos URLs del blog anuncian el tema que tratan.
- SLUG-02: los dos slugs viejos responden un solo salto permanente, sin cadena ni 404, y ningún enlace interno ni el sitemap apuntan a ellos.
- El total de URLs del sitemap sigue en 22.
</success_criteria>

<artifacts>
## Artifacts this phase produces

Producidos por este plan:

| Símbolo o ruta | Tipo | Nota |
|---|---|---|
| `src/content/blog/ciatica.ts` | módulo de contenido (renombrado) | export `postCiatica`, `slug: "ciatica"` |
| `src/content/blog/cirugia-de-columna.ts` | módulo de contenido (renombrado) | export `postCirugiaDeColumna`, `slug: "cirugia-de-columna"` |
| `postCiatica` | export nombrado | reemplaza a `post5SintomasDeColumnaQueNoDebesIgnorar` |
| `postCirugiaDeColumna` | export nombrado | reemplaza a `postMiedoAOperarteDeLaColumna5CosasQueDebesSaber` |
| `/blog/ciatica` | ruta pública | generada por `generateStaticParams` desde `blogPosts` |
| `/blog/cirugia-de-columna` | ruta pública | igual |
| entrada de `redirects()` → `/blog/ciatica` | redirección 308 | cuarta del arreglo de `next.config.ts` |
| entrada de `redirects()` → `/blog/cirugia-de-columna` | redirección 308 | quinta del arreglo |

Consumidos aguas abajo: el plan 16-03 construye el `@id` de cada `BlogPosting` a partir de estos dos slugs; el plan 16-04 varía los H2 de estos dos módulos; el plan 16-06 reescribe las secciones de `src/content/blog/ciatica.ts`.
</artifacts>

<output>
Crear `.planning/workstreams/seo-fixes/phases/16-alineaci-n-de-contenido-enlazado-y-schema/16-01-SUMMARY.md` al terminar.
</output>
