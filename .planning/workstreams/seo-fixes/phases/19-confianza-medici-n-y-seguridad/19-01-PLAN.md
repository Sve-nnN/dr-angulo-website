---
phase: 19-confianza-medicion-y-seguridad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/static-pages/hub-blog.ts
  - src/content/static-pages/hub-sedes.ts
  - src/app/blog/page.tsx
  - src/app/sedes/page.tsx
autonomous: false
requirements: [TRUST-01]
user_setup: []

must_haves:
  truths:
    - "Un paciente que abre /blog lee, además del listado, una explicación propia de qué se publica ahí y con qué criterio, en la voz del doctor."
    - "Un paciente que abre /sedes lee cómo elegir sede por zona, por días de atención y por cobertura, sin que la página repita lo que ya dice /agendar."
    - "El recuento de palabras del main de /blog pasa de 196 a 400 o más, y el de /sedes pasa de 239 a 500 o más. Medido como palabras de prosa dentro de data-content-body: 210 o más en /blog, 265 o más en /sedes."
    - "El resultado visual de todo lo que ya existía en las dos rutas queda idéntico: ninguna className preexistente cambia."
    - "Las cinco compuertas siguen en 0 después del cambio."
  artifacts:
    - src/content/static-pages/hub-blog.ts
    - src/content/static-pages/hub-sedes.ts
  key_links:
    - "src/app/blog/page.tsx importa hubBlog y lo renderiza dentro de <ContentBodyBoundary as=\"section\" className=\"mt-16\"> con <ContentBody sections={hubBlog.sections} flushFirstSection />, entre el listado de posts y el bloque 'Sigue leyendo'."
    - "src/app/sedes/page.tsx importa hubSedes con el mismo envoltorio, entre la rejilla de SedeCard y la sección '¿Prefieres comparar los días antes de elegir?'."
    - "Los dos módulos tipan contra StaticPage de src/content/static-pages/types.ts, cuyo campo sections es ContentSection[], alias de ServiceSection."
  ui_considerations:
    - "loading — citation-list, hub-blog, hub-sedes: no hay estado de carga. Las superficies son server components estáticos, sin fetch y sin JS de cliente. Es lo mismo que protege el CLS 0."
    - statement: "long-text / prosa de hub a 375px: el ancho de línea y el ritmo vertical de la prosa nueva se comportan igual que en /servicios, que usa el mismo envoltorio mt-16 y el mismo ContentBody."
      verification: "Captura de página completa a 375, 768 y 1440 px antes y después en las dos rutas. La franja desde el borde superior hasta el último elemento preexistente anterior al punto de inserción es idéntica; debajo solo se admite desplazamiento vertical."
    - statement: "jerarquía de encabezados en /sedes — SUPUESTO DEL PLANIFICADOR, la fila sigue ⚠ unresolved en 19-UI-SPEC.md."
      verification: "En esta rama los <h3> de SedeCard preceden a todo <h2> en /sedes. El arreglo vive en el PR #20, sin mergear. Al ejecutar, leer src/components/locations/sede-card.tsx: si ya emite <h2>, se respeta y no se toca; si todavía emite <h3>, la fase 19 tampoco lo arregla. La prosa nueva va después de la rejilla a propósito y no lo corrige. No editar sede-card.tsx en ningún caso."
---

<objective>
Dar a `/blog` y `/sedes` contenido propio que las sostenga como páginas indexables y no solo como índices de enlaces (TRUST-01), alineando de paso los dos hubs con el patrón de módulo de contenido que `/servicios` ya usa.

Purpose: hoy los dos hubs son listados. Un listado no compite por ninguna consulta y no le da al paciente nada que no obtenga entrando a un enlace. El texto propio le da a cada hub una razón de existir en el índice.
Output: dos módulos de contenido nuevos y dos `page.tsx` que los montan sin tocar nada de lo que ya renderizaban.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-CONTEXT.md
@.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-UI-SPEC.md
@src/content/static-pages/hub-servicios.ts
@src/content/static-pages/types.ts
@src/app/servicios/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: preparar la sesión de diseño y escribir el cuerpo de /blog</name>
  <files>src/content/static-pages/hub-blog.ts, src/app/blog/page.tsx</files>
  <read_first>
    - `.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-UI-SPEC.md`, secciones `## Superficie 2 — Módulos de contenido de /blog y /sedes` y `## Copywriting Contract`.
    - `src/content/static-pages/hub-servicios.ts` (encabezado del archivo y las primeras tres secciones) y `src/content/static-pages/types.ts` completo.
    - `src/app/servicios/page.tsx` líneas 55 a 80: el envoltorio exacto que se replica.
    - `src/app/blog/page.tsx` completo.
    - `design-system/dr-angulo/MASTER.md` y `PRODUCT.md`, que la skill carga sola.
  </read_first>
  <action>
    Antes de tocar cualquier archivo de interfaz, y una sola vez en la sesión, con cwd en la raíz del repo, correr `node ~/.claude/skills/impeccable/scripts/context.mjs --target src/app/blog/page.tsx`. La skill es global; la ruta `.claude/skills/impeccable/` no existe en este repo. El verbo de esta tarea es `clarify`: se agrega prosa, no se mueve estructura existente. Los hallazgos del hook detector se atienden, no se ignoran.

    Crear `src/content/static-pages/hub-blog.ts` copiando la forma de `hub-servicios.ts`: comentario de cabecera que explica de dónde sale el texto y por qué reusa `ContentSection`, `import type { StaticPage } from "./types";`, y `export const hubBlog: StaticPage` con `slug: "/blog"` y `format: "pagina-de-servicio"`. No declarar `outboundLinks`: el bloque "Sigue leyendo" ya está escrito a mano en el `page.tsx` y moverlo cambiaría el marcado servido. Exportar además `export const hubBlogH1` con el h1 publicado actual, `Artículos sobre columna y traumatología`, para que el módulo quede completo aunque la página siga escribiendo el h1 a mano en esta fase.

    Escribir tres o cuatro secciones de `level: 2` con `id` en kebab-case sin tildes, escritos a mano y permanentes. Contenido: qué clase de artículo se publica ahí, con qué criterio se elige el tema, cómo se relaciona cada artículo con la guía completa del silo, y qué hacer si lo que el paciente busca no está. En la voz explicativa del doctor. **Sin ninguna promesa de frecuencia de publicación** (D de `19-CONTEXT.md`, `## Contenido propio en los hubs`): una promesa que el consultorio no sostenga envejece mal y queda visible. Prohibido, como en todo el repo: cifras de cirugías, tasas de éxito, plazos garantizados, precios, y voz testimonial. `paragraphs` son cadenas planas sin marcado.

    Extensión: la prosa tiene que sumar como mínimo 210 palabras renderizadas, porque el `<main>` de `/blog` mide hoy 196 palabras y el objetivo de la fase es superar 400. Apuntar a entre 260 y 320 palabras de prosa deja el total entre 456 y 516, con margen.

    En `src/app/blog/page.tsx`: agregar el import de `hubBlog` y los de `ContentBody` y `ContentBodyBoundary` desde `@/components/content/content-body`, e insertar el envoltorio `<ContentBodyBoundary as="section" className="mt-16">` con `<ContentBody sections={hubBlog.sections} flushFirstSection />` dentro, colocado **después** del `</div>` que cierra el contenedor de la lista de artículos y **antes** del `<section className="mt-14 border-t border-border pt-10">` de "Sigue leyendo". `headingSize` se deja en su valor por defecto. No se agrega tabla de contenidos: `/servicios` publica quince secciones ancladas y no la tiene, y la puerta solo la exige en rutas del `MANIFEST`.

    No agregar `/blog` al `MANIFEST` de `scripts/check-content.mjs`: ese manifiesto impone `MIN_WORDS = 900` y el objetivo de esta ruta es 400. No tocar `scripts/check-content.mjs` en ninguna de sus constantes.

    Todo el copy pasa por el humanizador antes de quedar en el archivo: sin guiones largos, sin muletillas de IA, voz variada.
  </action>
  <verify>
    <automated>npm run build && node -e "const h=require('fs').readFileSync('.next/server/app/blog.html','utf8');const i=h.indexOf('data-content-body');const j=h.indexOf('/main',i);const t=h.slice(i,j).replace(/[<][^>]*[>]/g,' ').replace(/&[a-zA-Z#0-9]*;/g,' ');const w=t.split(/\s+/).filter(Boolean).length;console.log('blog prose words',w);if(i===-1||w<210)process.exit(1)" && npm run content:check && npm run seo:check && npm run sedes:check && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El comando de arriba imprime `blog prose words` con un valor de 210 o más y sale con estado 0. El `<main>` de `/blog` medía 196 palabras el 2026-08-25 sobre esta rama, así que 210 de prosa nueva dejan el total en 406 o más, por encima del objetivo de 400 de la fase. Objetivo cómodo: entre 260 y 320.
    - El mismo comando falla si `data-content-body` no aparece en `blog.html`, que es la prueba de que el envoltorio quedó montado.
    - Las cinco compuertas salen con estado 0: `content:check`, `seo:check`, `sedes:check`, `tsc --noEmit`, `build`.
    - `git diff src/app/blog/page.tsx` muestra únicamente imports nuevos y el bloque `<ContentBodyBoundary>` nuevo. `grep -c 'mx-auto max-w-3xl px-4 py-14 sm:px-6 sm:py-20' src/app/blog/page.tsx` sigue devolviendo 1, y las cadenas `mt-5 text-lg text-foreground/70`, `mt-12 divide-y divide-border border-y border-border`, `py-8` y `mt-14 border-t border-border pt-10` siguen presentes sin cambios.
    - `grep -vE '^\s*(//|\*|/\*)' src/content/static-pages/hub-blog.ts | grep -cE 'utm_|https?://'` devuelve 0: la prosa del hub no lleva enlaces ni marcas de campaña.
    - Ningún `id` de sección del módulo nuevo contiene tildes ni mayúsculas.
    - `grep -n 'MIN_WORDS\|SITEMAP_TOTAL\|SKELETONS\|MANIFEST' scripts/check-content.mjs` devuelve exactamente lo mismo que antes de la tarea: el script no se toca.
  </acceptance_criteria>
  <done>`/blog` publica cuerpo propio de 400 palabras o más, el listado y el bloque "Sigue leyendo" se ven exactamente igual que antes, y las cinco compuertas están en 0.</done>
</task>

<task type="auto">
  <name>Tarea 2: escribir el cuerpo de /sedes</name>
  <files>src/content/static-pages/hub-sedes.ts, src/app/sedes/page.tsx</files>
  <read_first>
    - `19-UI-SPEC.md`, `## Superficie 2` y la subsección `## Qué significa "el resultado visual no cambia", en concreto`.
    - `src/app/sedes/page.tsx` completo.
    - `src/components/locations/sede-card.tsx`: leerlo para saber si ya emite `<h2>` o todavía `<h3>`. Es lectura de diagnóstico, no de edición.
    - `src/app/agendar/page.tsx`, para saber qué ya dice esa página y no repetirlo.
    - El módulo `hub-blog.ts` que dejó la tarea 1, como referencia de forma.
  </read_first>
  <action>
    Correr `node ~/.claude/skills/impeccable/scripts/context.mjs --target src/app/sedes/page.tsx` si la sesión cambió desde la tarea 1. Verbo `clarify`.

    Crear `src/content/static-pages/hub-sedes.ts` con `export const hubSedes: StaticPage`, `slug: "/sedes"`, `format: "pagina-de-servicio"`, sin `outboundLinks`, y `export const hubSedesH1` con el h1 publicado actual, `Sedes donde atiende el Dr. Angulo en Lima`.

    Tres o cuatro secciones de `level: 2` sobre **cómo elegir sede**: por zona de Lima y tiempo de traslado, por los días en que el doctor atiende en cada una, y por cobertura o forma de pago según sea consultorio privado o clínica. Una sección puede cerrar diciendo qué mirar antes de decidir. Lo que no entra: repetir horarios concretos, canales de contacto ni la mecánica de agendar, porque eso ya lo resuelven `/agendar` y la ficha de cada sede. Voz explicativa, cadenas planas, sin cifras de cirugías ni tasas de éxito ni precios.

    Extensión: mínimo 265 palabras de prosa renderizada, porque el `<main>` de `/sedes` mide hoy 239 palabras y el objetivo es superar 500. Apuntar a entre 300 y 360 palabras deja el total entre 539 y 599.

    En `src/app/sedes/page.tsx`: imports nuevos más el bloque `<ContentBodyBoundary as="section" className="mt-16"><ContentBody sections={hubSedes.sections} flushFirstSection /></ContentBodyBoundary>`, insertado **después** del `</div>` que cierra la rejilla `mt-10 grid items-stretch gap-6 sm:grid-cols-2` y **antes** del comentario y la `<section className="mt-16 border-t border-border pt-12">` de "¿Prefieres comparar los días antes de elegir?". La prosa va antes de ese bloque porque ese bloque es el cierre de conversión de la página y los cierres van al final.

    No tocar `src/components/locations/sede-card.tsx` bajo ninguna circunstancia. Si todavía emite `<h3>`, el `<h3>` antes del primer `<h2>` sigue ahí y **esta fase no lo arregla**: el arreglo vive en el PR #20 y hacerlo también acá dejaría dos encabezados donde va uno. Anotar en el SUMMARY qué nivel emitía la tarjeta al ejecutar.

    No agregar `/sedes` al `MANIFEST` de `check-content.mjs`. No tocar `scripts/check-sedes.mjs`: `SITEMAP_TOTAL` sigue en 23 porque esta fase no publica ninguna URL nueva.

    Copy humanizado antes de quedar en el archivo.
  </action>
  <verify>
    <automated>npm run build && node -e "const h=require('fs').readFileSync('.next/server/app/sedes.html','utf8');const i=h.indexOf('data-content-body');const j=h.indexOf('/main',i);const t=h.slice(i,j).replace(/[<][^>]*[>]/g,' ').replace(/&[a-zA-Z#0-9]*;/g,' ');const w=t.split(/\s+/).filter(Boolean).length;console.log('sedes prose words',w);if(i===-1||w<265)process.exit(1)" && npm run content:check && npm run seo:check && npm run sedes:check && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El comando imprime `sedes prose words` con un valor de 265 o más y sale con estado 0. El `<main>` de `/sedes` medía 239 palabras el 2026-08-25 sobre esta rama, así que 265 de prosa nueva dejan el total en 504 o más, por encima del objetivo de 500. Objetivo cómodo: entre 300 y 360.
    - Las cinco compuertas en 0. `sedes:check` incluido, que es el que verifica que el hub siga teniendo exactamente un `<h1>` y siga enlazando a las cuatro sedes.
    - `git status --porcelain src/components/locations/sede-card.tsx` no devuelve nada: el archivo no se tocó.
    - `git diff src/app/sedes/page.tsx` muestra solo imports nuevos y el bloque nuevo. Siguen presentes sin cambios: `mx-auto max-w-4xl px-4 py-14 sm:px-6 sm:py-20`, `mt-5 max-w-2xl text-lg text-foreground/70`, `mt-10 grid items-stretch gap-6 sm:grid-cols-2`, `mt-16 border-t border-border pt-12` y `mt-14 border-t border-border pt-10`.
    - `grep -c 'SITEMAP_TOTAL = 23' scripts/check-sedes.mjs scripts/check-seo.mjs scripts/check-content.mjs` devuelve 1 en cada uno de los tres archivos.
    - Ningún párrafo de `hub-sedes.ts` repite un horario concreto ni un canal de contacto. Se comprueba sobre el código, filtrando comentarios, porque un comentario que nombre el patrón invalidaría la compuerta: `grep -vE '^\s*(//|\*|/\*)' src/content/static-pages/hub-sedes.ts | grep -cE 'wa\.me|https?://|[0-9]{1,2}:[0-9]{2}'` devuelve 0.
  </acceptance_criteria>
  <done>`/sedes` publica cuerpo propio de 500 palabras o más, la rejilla y el bloque de agenda se ven exactamente igual, `sede-card.tsx` sigue intacto y las cinco compuertas están en 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: verificación visual y de métricas de los dos hubs</name>
  <read_first>
    - `19-UI-SPEC.md`, `## Qué significa "el resultado visual no cambia", en concreto`: las tres pruebas son obligatorias.
  </read_first>
  <what-built>
    Dos módulos de contenido nuevos, `hub-blog.ts` y `hub-sedes.ts`, montados en `/blog` y `/sedes` con el mismo envoltorio que `/servicios` ya usa. Nada preexistente se movió ni se restiló.
  </what-built>
  <how-to-verify>
    1. `npm run build &amp;&amp; npm run start`, y abrir `http://localhost:3000/blog` y `http://localhost:3000/sedes`.
    2. Capturas de página completa a 375, 768 y 1440 px de las dos rutas, contra las mismas capturas del commit anterior. La franja que va del borde superior al último elemento anterior al punto de inserción tiene que ser idéntica: mismo ancho, mismo color, misma tipografía, mismo orden. Debajo del punto de inserción solo se admite el desplazamiento vertical que introduce la prosa.
    3. Unlighthouse o Lighthouse sobre `/blog` y `/sedes`, **mediana de tres corridas** por ruta. Una sola corrida no distingue señal de ruido. Criterios: accesibilidad 1.00 y CLS 0 en las dos rutas, igual que antes de esta fase.
    4. Leer la prosa nueva completa en las dos rutas y confirmar: se lee en la voz del doctor, no promete frecuencia de publicación en `/blog`, y en `/sedes` no repite horarios ni canales que ya viven en `/agendar`.
  </how-to-verify>
  <acceptance_criteria>
    - Accesibilidad 1.00 y CLS 0 en `/blog` y `/sedes`, como mediana de tres corridas por ruta. Si baja alguna de las dos, el cambio no está terminado y vuelve a la tarea que lo introdujo.
    - Las capturas por encima del punto de inserción son idénticas en las tres anchuras.
    - Juan confirma que el texto se lee en su voz y no promete nada que el consultorio no sostenga.
  </acceptance_criteria>
  <resume-signal>Escribir "aprobado" o describir qué falla y en qué ruta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → HTML servido | El texto de los dos módulos se prerenderiza y se publica firmado por un médico real. No hay entrada de usuario en esta superficie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01-01 | Tampering | prosa de `hub-blog.ts` y `hub-sedes.ts` | high | mitigate | Contenido YMYL firmado por un profesional. Prohibido en los criterios de aceptación: cifras de cirugías, tasas de éxito, plazos garantizados, precios y voz testimonial. La tarea 3 es un checkpoint bloqueante donde Juan lee el texto completo antes de que se publique. |
| T-19-01-02 | Repudiation | promesa de frecuencia en `/blog` | medium | mitigate | Una promesa de publicación que el consultorio no sostenga queda visible y es indefendible. Se prohíbe explícitamente en la acción de la tarea 1 y se revisa en el checkpoint. |
| T-19-01-03 | Denial of Service | CLS y accesibilidad de las dos rutas | medium | mitigate | Las dos superficies son server components estáticos sin fetch, sin JS de cliente, sin imágenes y sin fuentes nuevas. Verificación por mediana de tres corridas en la tarea 3. |
| T-19-01-04 | Tampering | invariantes de las compuertas | high | accept | Esta fase no toca `scripts/check-content.mjs`, `check-seo.mjs` ni `check-sedes.mjs`, y no publica URLs nuevas, así que `SITEMAP_TOTAL = 23` no se mueve. Se acepta con el criterio de grep que lo comprueba en los tres archivos. |
| T-19-01-SC | Tampering | instalaciones de npm | high | mitigate | Esta fase instala cero dependencias. `lucide-react` ya está en el bundle. `git diff package.json package-lock.json` tiene que salir vacío. |
</threat_model>

<verification>
- Las cinco compuertas en 0 en cada commit del plan: `npm run content:check`, `npm run seo:check`, `npm run sedes:check`, `npx tsc --noEmit`, `npm run build`.
- `<main>` de `/blog` con 400 palabras o más y `<main>` de `/sedes` con 500 o más, con el comando de conteo de las tareas 1 y 2.
- Accesibilidad 1.00 y CLS 0 en `/blog` y `/sedes`, mediana de tres corridas.
- `git diff package.json package-lock.json` vacío.
- `git status --porcelain src/components/locations/sede-card.tsx scripts/` vacío.
</verification>

<success_criteria>
- TRUST-01 cumplido: `/blog` y `/sedes` tienen texto propio suficiente para sostenerse como páginas indexables y no solo como índices de enlaces.
- Los dos hubs quedan alineados con el patrón de `hub-servicios.ts`, que era la deuda de forma que arrastraban.
- Ningún elemento preexistente de las dos rutas cambió de aspecto, de posición relativa ni de clase.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 01)

| Artifact | Tipo | Qué contiene |
|---|---|---|
| `src/content/static-pages/hub-blog.ts` | nuevo | `hubBlog: StaticPage` y `hubBlogH1`. Prosa de 260 a 320 palabras sobre qué se publica en el blog y con qué criterio. |
| `src/content/static-pages/hub-sedes.ts` | nuevo | `hubSedes: StaticPage` y `hubSedesH1`. Prosa de 300 a 360 palabras sobre cómo elegir sede. |
| `src/app/blog/page.tsx` | modificado | Imports nuevos y el envoltorio `ContentBodyBoundary` entre el listado y "Sigue leyendo". |
| `src/app/sedes/page.tsx` | modificado | Imports nuevos y el envoltorio `ContentBodyBoundary` entre la rejilla y el bloque de agenda. |
</artifacts>

<output>
Crear `.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-01-SUMMARY.md` al terminar. Anotar el recuento final de palabras de las dos rutas, la mediana de las tres corridas de Lighthouse, y qué nivel de encabezado emitía `sede-card.tsx` al momento de ejecutar.
</output>
