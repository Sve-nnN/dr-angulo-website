---
phase: 19-confianza-medicion-y-seguridad
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/google-business-profile.md
  - docs/preguntas-yuly-y-nap.md
  - audit/baselines/2026-08-25-baseline-utm-gbp.md
  - seo-tools/data/url-map.jsonl
  - seo-tools/data/internal-links.json
  - seo-tools/data/copy-blog.json
  - seo-tools/data/onpage.json
  - seo-tools/data/canonicals.json
autonomous: true
requirements: [MEAS-01]
user_setup:
  - service: google-business-profile
    why: "El enlace del sitio web y el enlace de citas de la ficha son campos del panel de Google Business. Solo Juan puede editarlos."
    dashboard_config:
      - task: "Reemplazar el enlace de Sitio web por la URL limpia, sin parámetros utm"
        location: "Google Business Profile → Editar perfil → Contacto → Sitio web"
      - task: "Reemplazar el enlace de citas por /agendar sin parámetros utm"
        location: "Google Business Profile → Editar perfil → Contacto → Enlace de citas"
      - task: "Anotar la fecha del cambio, que es la que arranca la ventana de 30 días de la verificación de consolidación"
        location: "Al pie de audit/baselines/2026-08-25-baseline-utm-gbp.md"

must_haves:
  truths:
    - "El repo dice, por escrito y con su motivo, que el enlace del perfil de Google Business va sin parámetros utm."
    - "Queda registrada la línea base de impresiones partidas, para poder comprobar la consolidación 30 días después de que Juan aplique el cambio."
    - "Ningún documento del repo sigue prescribiendo un enlace con utm_campaign=gbp hacia el sitio."
    - "Los cinco archivos de seo-tools/data/ dejan de referirse a los slugs viejos del blog."
    - "La verificación de consolidación a 30 días está escrita como un paso comprobable con fecha, no como una intención."
  artifacts:
    - audit/baselines/2026-08-25-baseline-utm-gbp.md
    - "docs/google-business-profile.md con la sección de decisión sobre los utm"
  key_links:
    - "docs/google-business-profile.md, secciones '### Sitio web' y '### Enlace de citas', son las que Juan copia al panel: si ahí queda la URL con utm, el cambio se revierte solo la próxima vez que alguien siga el documento."
    - "El canal de Google Business se pasa a medir por el informe propio del perfil, que ya cuenta clics al sitio. La decisión no deja el canal sin medición, la cambia de fuente."
    - "MEAS-01 es el único punto de contacto con el workstream milestone: GBP-05 de su fase 11 es dueño de la ficha. Este workstream decide y mide, no configura."
  ui_considerations:
    - "No aplica. MEAS-01 no tiene superficie visual y 19-UI-SPEC.md lo excluye explícitamente de su alcance."
---

<objective>
Cerrar MEAS-01: dejar por escrito la decisión de quitar los parámetros `utm` del enlace del perfil de Google Business, registrar la línea base de impresiones partidas y dejar preparada la verificación de consolidación a 30 días. De paso, saldar la limpieza pendiente de los cinco archivos de `seo-tools/data/` que quedaron con los slugs viejos del blog (IN-04 del code review de la fase 16).

Purpose: 778 de las 892 impresiones del sitio están hoy en URLs con `utm`, que Search Console lista como páginas separadas. El canonical ya apunta a la versión limpia, así que no hay riesgo de contenido duplicado: lo que se arregla es el informe partido, no la indexación. Sin el informe entero no se puede leer el rendimiento de la portada de un vistazo.
Output: la decisión escrita donde Juan la va a leer, la línea base medida, y el dataset de `seo-tools/` coherente con los slugs publicados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-CONTEXT.md
@audit/findings/2026-08-23-auditoria-seo.md
@docs/google-business-profile.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: decisión escrita sobre los utm y línea base de impresiones</name>
  <files>docs/google-business-profile.md, docs/preguntas-yuly-y-nap.md, audit/baselines/2026-08-25-baseline-utm-gbp.md</files>
  <read_first>
    - `audit/findings/2026-08-23-auditoria-seo.md`, líneas 20 a 30 y la línea 185, que es donde está el hallazgo.
    - `docs/google-business-profile.md` completo, en particular `### Sitio web` (línea 113), `### Enlace de citas` (línea 118) y la mención de `gbp_post` en la sección de publicaciones (línea 188).
    - `docs/preguntas-yuly-y-nap.md`, `## Parte 4. Cómo saber si esto funcionó`, líneas 171 a 195.
    - `audit/baselines/2026-08-24-baseline-anchors.md`, como modelo de formato de un archivo de línea base de este repo.
  </read_first>
  <action>
    Crear `audit/baselines/2026-08-25-baseline-utm-gbp.md` con la línea base medida el 2026-08-23 en Search Console: 892 impresiones totales del sitio, de las cuales 778 caen en URLs con `?utm_source=google&utm_medium=organic&utm_campaign=gbp` — 673 en la portada, 75 en `/servicios` y 30 en `/agendar`. Registrar también qué se espera después del cambio y cómo se comprueba: a 30 días de que Juan edite la ficha, las tres filas con parámetros dejan de recibir impresiones nuevas y el total se lee en las filas limpias. El archivo deja un campo vacío `Fecha en que Juan aplicó el cambio en la ficha: ____` y una línea `Verificación de consolidación: 30 días después de esa fecha`. Sin ese campo, la ventana de 30 días no arranca y la verificación no es comprobable.

    En `docs/google-business-profile.md`, reemplazar el valor de `### Sitio web` por `https://[dominio]/` y el de `### Enlace de citas` por `https://[dominio]/agendar`, y agregar debajo de cada uno una línea corta que explique por qué van sin parámetros. Agregar además una subsección propia — por ejemplo `### Por qué el enlace va sin utm` — que diga las tres cosas que hay que poder repetir: que los `utm` partían el informe de Search Console en dos porque Google los trata como páginas separadas; que el canonical ya apuntaba a la versión limpia, así que nunca hubo riesgo de duplicado y lo que se arregla es el reporte; y que el canal de Google Business se sigue midiendo, por el informe propio del perfil, que ya cuenta clics al sitio. Dejar dicho también quién aplica el cambio: lo hace Juan en el panel, este workstream decide y mide, y la configuración de la ficha en sí es GBP-05 de la fase 11 del workstream `milestone`.

    Tratar igual la mención de `gbp_post` de la sección de publicaciones: el botón "Más información" de las publicaciones apunta al artículo del blog, y ese parámetro parte las filas de esos posts por el mismo mecanismo. Quitarlo y dejar el enlace limpio.

    En `docs/preguntas-yuly-y-nap.md`, en `## Parte 4`, cambiar **solo** el bloque "Desde la ficha de Google" por la URL limpia y agregar una frase que remita a `docs/google-business-profile.md` para el motivo. Los bloques de Instagram y de Doctoralia **no se tocan**: esos perfiles no alimentan el informe de Search Console y sus `utm` no parten nada. Dejar esa distinción escrita en el propio documento, para que nadie los quite después "por consistencia".

    No inventar cifras. Todo número que entre a estos archivos sale de la auditoría del 2026-08-23 o de la medición del 2026-08-25 que está en `19-CONTEXT.md`. Texto humanizado antes de entregarse.
  </action>
  <verify>
    <automated>test -f audit/baselines/2026-08-25-baseline-utm-gbp.md && grep -q '892' audit/baselines/2026-08-25-baseline-utm-gbp.md && grep -q '778' audit/baselines/2026-08-25-baseline-utm-gbp.md && grep -q '673' audit/baselines/2026-08-25-baseline-utm-gbp.md && ! grep -qE '^https?://.*utm_' docs/google-business-profile.md && ! grep -qE '^https?://.*utm_campaign=gbp' docs/preguntas-yuly-y-nap.md && grep -q 'utm_source=instagram' docs/preguntas-yuly-y-nap.md && grep -q 'utm_source=doctoralia' docs/preguntas-yuly-y-nap.md && echo "MEAS-01 docs ok"</automated>
  </verify>
  <acceptance_criteria>
    - `audit/baselines/2026-08-25-baseline-utm-gbp.md` existe y contiene los cuatro números de la línea base: 892, 778, 673, 75 y 30.
    - El comando automatizado imprime `MEAS-01 docs ok`. Lo que comprueba, por partes:
      - Ninguna línea de `docs/google-business-profile.md` que empiece por una URL lleva parámetros de campaña: `grep -nE '^https?://.*utm_' docs/google-business-profile.md` no devuelve nada. El ancla al inicio de línea es lo que distingue las URLs prescriptivas, que viven solas dentro de bloques de código, de la prosa que explica la decisión y sí puede nombrar el parámetro.
      - Lo mismo en `docs/preguntas-yuly-y-nap.md` para el bloque de la ficha de Google.
      - Instagram y Doctoralia siguen presentes en `docs/preguntas-yuly-y-nap.md`: esos dos canales no se tocaron.
    - El archivo de línea base contiene el campo vacío para la fecha en que Juan aplique el cambio y la instrucción de verificar a 30 días.
    - `git status --porcelain src/ scripts/` está vacío: esta tarea no toca código.
  </acceptance_criteria>
  <done>La decisión sobre los `utm` está escrita donde Juan la va a leer, con su motivo y con quién la aplica, y la línea base queda registrada con la ventana de verificación definida.</done>
</task>

<task type="auto">
  <name>Tarea 2: limpieza de los slugs viejos del blog en seo-tools/data</name>
  <files>seo-tools/data/url-map.jsonl, seo-tools/data/internal-links.json, seo-tools/data/copy-blog.json, seo-tools/data/onpage.json, seo-tools/data/canonicals.json</files>
  <read_first>
    - `19-CONTEXT.md`, `## Limpieza pendiente`: los cinco archivos vienen de IN-04, del code review de la fase 16, y quedaron sin dueño.
    - `next.config.ts`, el bloque `redirects()`: ahí está la correspondencia exacta entre slug viejo y slug nuevo, que es la fuente de verdad de este renombre.
    - Los cinco archivos, para ver en qué campo aparece cada slug antes de sustituir nada.
  </read_first>
  <action>
    Sustituir los slugs viejos por los publicados en los cinco archivos de `seo-tools/data/`. Las correspondencias salen de `redirects()` de `next.config.ts`, que es donde ya están declaradas: `5-sintomas-de-columna-que-no-debes-ignorar` pasa a `ciatica`, `miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber` pasa a `cirugia-de-columna`, y los dos posts que las guías absorbieron —`hernia-discal-o-dolor-de-espalda-como-diferenciarlos` y `estenosis-espinal-que-es`— pasan a `/servicios/hernia-discal` y `/servicios/estenosis-espinal` respectivamente, que es un cambio de sección, no solo de slug: la fila deja de ser un post.

    Recuento actual de apariciones, medido el 2026-08-25: `canonicals.json` 8, `copy-blog.json` 19, `internal-links.json` 16, `onpage.json` 4, `url-map.jsonl` 8.

    En `copy-blog.json`, que es el que más apariciones tiene, revisar si las entradas de los dos posts absorbidos siguen teniendo sentido como filas de blog. Si ya no existen como posts, la decisión correcta es quitarlas, no renombrarlas hacia una guía: el copy de blog no describe una página de servicio. Anotar en el SUMMARY qué se hizo con esas entradas y por qué.

    Validar que los archivos siguen siendo parseables después de la sustitución: `.jsonl` una línea igual a un objeto JSON, `.json` un documento entero válido.

    **Fuera de alcance de esta tarea, y por qué.** El mismo `grep` encuentra los slugs viejos también en `seo-tools/data/onpage-serp.json` (2 apariciones) y `seo-tools/data/url-inventory.json` (8). `19-CONTEXT.md` nombra cinco archivos, no siete. Inspeccionar esos dos y **no reescribirlos si son capturas fechadas de una SERP o un inventario de un momento dado**: reescribir una captura histórica la convierte en un dato falso. Dejar en el SUMMARY qué son los dos archivos y la recomendación, para que el lead decida. Si resultan ser mapeos vivos y no capturas, decirlo también, sin tocarlos en esta tarea.
  </action>
  <verify>
    <automated>! grep -qE '5-sintomas-de-columna-que-no-debes-ignorar|miedo-a-operarte-de-la-columna-5-cosas-que-debes-saber|hernia-discal-o-dolor-de-espalda-como-diferenciarlos|estenosis-espinal-que-es' seo-tools/data/url-map.jsonl seo-tools/data/internal-links.json seo-tools/data/copy-blog.json seo-tools/data/onpage.json seo-tools/data/canonicals.json && node -e "const fs=require('fs');for(const f of ['internal-links.json','copy-blog.json','onpage.json','canonicals.json'])JSON.parse(fs.readFileSync('seo-tools/data/'+f,'utf8'));for(const l of fs.readFileSync('seo-tools/data/url-map.jsonl','utf8').split('\n'))if(l.trim())JSON.parse(l);console.log('json ok')" && npm run content:check && npm run seo:check && npm run sedes:check && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - El `grep` devuelve 0 en los cinco archivos. Los recuentos de partida eran 8, 19, 16, 4 y 8.
    - El comando de node imprime `json ok`: los cuatro `.json` parsean como documento y cada línea no vacía del `.jsonl` parsea como objeto.
    - Las cinco compuertas siguen en 0. `seo-tools/data/` no entra en ninguna de ellas, pero se corren igual: una compuerta que ningún plan corre es la que se rompe en silencio.
    - El SUMMARY dice qué se hizo con las entradas de `copy-blog.json` de los dos posts absorbidos, y qué son `onpage-serp.json` y `url-inventory.json`, con la recomendación de tocarlos o no.
    - `git status --porcelain src/ scripts/` está vacío.
  </acceptance_criteria>
  <done>Ninguno de los cinco archivos de `seo-tools/data/` se refiere ya a un slug que el sitio no publica, todos siguen parseando, y los dos archivos extra quedan inspeccionados y documentados sin tocarse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → panel de Google Business | El documento del repo es la fuente que Juan copia al panel. Un valor obsoleto ahí se reaplica solo la próxima vez que alguien siga el documento. |
| dataset de `seo-tools/` → decisiones de SEO | Un slug obsoleto en el dataset produce recomendaciones sobre URLs que ya no existen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03-01 | Repudiation | `docs/google-business-profile.md` | high | mitigate | Si la decisión se aplica en el panel pero el documento sigue prescribiendo la URL con `utm`, el cambio se revierte la próxima vez que alguien siga el documento. El criterio de aceptación exige que ningún bloque de código prescriptivo contenga `utm_`. |
| T-19-03-02 | Information Disclosure | pérdida de medición del canal GBP | medium | accept | Quitar los `utm` deja al canal sin medición en GA4. Se acepta a propósito: el informe propio de Google Business ya cuenta clics al sitio, y el documento lo deja escrito para que nadie lo lea como un descuido. |
| T-19-03-03 | Tampering | `seo-tools/data/onpage-serp.json` y `url-inventory.json` | medium | mitigate | Reescribir una captura histórica de SERP la convierte en un dato falso. Se inspeccionan y se documentan, y no se tocan en esta tarea. |
| T-19-03-04 | Tampering | integridad de los cinco datasets | low | mitigate | Una sustitución de texto puede romper el JSON. Se valida el parseo de los cuatro `.json` y de cada línea del `.jsonl` antes de cerrar la tarea. |
| T-19-03-SC | Tampering | instalaciones de npm | high | mitigate | Este plan instala cero dependencias. `git diff package.json package-lock.json` tiene que salir vacío. |
</threat_model>

<verification>
- `grep -c` de los cuatro slugs viejos devuelve 0 en los cinco archivos nombrados.
- Los cinco archivos de `seo-tools/data/` siguen parseando.
- Ningún bloque de código prescriptivo de `docs/google-business-profile.md` contiene `utm_`, y el de `docs/preguntas-yuly-y-nap.md` conserva Instagram y Doctoralia.
- `audit/baselines/2026-08-25-baseline-utm-gbp.md` existe con los cinco números de la línea base y con el campo de fecha en blanco que arranca la ventana de 30 días.
- Las cinco compuertas en 0.
- `git status --porcelain src/ scripts/` vacío.

**Verificación diferida, con fecha.** 30 días después de que Juan aplique el cambio en la ficha: abrir Search Console, informe de Rendimiento, filtrar por URL que contenga `utm_campaign=gbp` y confirmar que no hay impresiones nuevas en los últimos 28 días, y que las filas limpias de `/`, `/servicios` y `/agendar` absorbieron el volumen. Esta comprobación no bloquea el cierre de la fase; se registra como pendiente con fecha en el SUMMARY y en el archivo de línea base.
</verification>

<success_criteria>
- MEAS-01 cumplido en lo que este workstream puede cumplir: la decisión está tomada, escrita y justificada, la línea base está medida, y la verificación de consolidación está definida como un paso comprobable con fecha.
- Ningún documento del repo prescribe ya un enlace con `utm` hacia el sitio desde una superficie de Google.
- La limpieza pendiente de `seo-tools/data/` queda saldada y los dos archivos fuera de la lista quedan documentados en vez de silenciados.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 03)

| Artifact | Tipo | Qué contiene |
|---|---|---|
| `audit/baselines/2026-08-25-baseline-utm-gbp.md` | nuevo | Línea base de impresiones partidas del 2026-08-23, expectativa post-cambio, campo de fecha de aplicación y ventana de verificación de 30 días. |
| `docs/google-business-profile.md` | modificado | Enlaces sin `utm` en Sitio web, Enlace de citas y publicaciones, más la subsección que explica por qué. |
| `docs/preguntas-yuly-y-nap.md` | modificado | Solo el bloque de la ficha de Google, con la distinción escrita de por qué Instagram y Doctoralia se quedan como están. |
| 5 archivos de `seo-tools/data/` | modificado | Slugs de blog alineados con los publicados. |
| Inspección de `onpage-serp.json` y `url-inventory.json` | en el SUMMARY | Qué son, y si corresponde tocarlos en una fase futura. |
</artifacts>

<output>
Crear `.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-03-SUMMARY.md` al terminar. Anotar: la fecha objetivo de la verificación de consolidación una vez que Juan informe cuándo aplicó el cambio, qué se hizo con las entradas de `copy-blog.json` de los dos posts absorbidos, y la recomendación sobre los dos archivos fuera de la lista de cinco.
</output>
