---
phase: 19-confianza-medicion-y-seguridad
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/api/csp-report/route.ts
  - docs/content-security-policy.md
autonomous: true
requirements: [MEAS-02]
user_setup:
  - service: cloudflare
    why: "Cloudflare está delante del origen y puede filtrar o reescribir cabeceras. Que la CSP salga del origen no garantiza que llegue al navegador."
    dashboard_config:
      - task: "Después del deploy, confirmar con curl que la cabecera Content-Security-Policy-Report-Only llega desde producción. Si no llega, revisar Transform Rules y Managed Transforms del zone."
        location: "Cloudflare → el zone del sitio → Rules → Transform Rules → Modify Response Header"

must_haves:
  truths:
    - "El sitio sirve una cabecera Content-Security-Policy-Report-Only en todas sus respuestas, con un endpoint propio que recibe los reportes."
    - "Ninguna de las 23 URLs del sitemap cambia de comportamiento: report-only no bloquea nada, solo reporta."
    - "Queda escrito de dónde salieron las directivas: un inventario de los orígenes que el sitio carga de verdad, no una plantilla copiada."
    - "Queda investigado y escrito qué eran los issues de CSP que Lighthouse reportaba en /agendar, /servicios/escoliosis-y-deformidades y /servicios/estenosis-espinal antes de este cambio."
    - "El procedimiento para pasar a enforce está escrito, con sus pasos, su criterio de salida y una fecha."
  artifacts:
    - src/app/api/csp-report/route.ts
    - docs/content-security-policy.md
    - "next.config.ts con Content-Security-Policy-Report-Only y Reporting-Endpoints junto a las cinco cabeceras que ya declaraba"
  key_links:
    - "La directiva report-to de la política nombra el endpoint que declara la cabecera Reporting-Endpoints, y esa URL resuelve al route handler de /api/csp-report. Si el nombre no coincide, la política se sirve pero nunca llega un reporte."
    - "El comentario largo sobre headers() en next.config.ts es la especificación de MEAS-02: describe el plan de tres pasos, report-only, observar, enforce. Este plan cumple los pasos 1 y 3 y documenta el 2, que necesita días de tráfico real."
    - "La política NO usa nonce: la guía de CSP de Next 16 exige renderizado dinámico para el nonce, y eso apagaría el prerenderizado estático de las 23 rutas y con él la Cache Rule de Cloudflare de la fase 18."
  ui_considerations:
    - "No aplica. MEAS-02 no es diseño y 19-UI-SPEC.md lo excluye explícitamente de su alcance. La skill impeccable no interviene en este plan."
---

<objective>
Cerrar MEAS-02 en el alcance que la fase declara: desplegar `Content-Security-Policy-Report-Only` con un endpoint que recoja los reportes, y dejar escrito el procedimiento para pasarla a enforce. **El enforce no entra en esta fase.**

Purpose: el comentario largo sobre `headers()` en `next.config.ts` describe desde antes de esta fase un plan de tres pasos —report-only, observar tráfico real, enforce— y su paso 2 no se puede acelerar: observar necesita días de tráfico. El sitio carga la fuente y los mapas de Google, el pixel de Meta, GA4 y las portadas de los reels desde el CDN de Instagram; una política mal calibrada rompe producción en silencio, que es peor que no tenerla.
Output: cabecera report-only en producción, endpoint de reportes, y el procedimiento de enforce con fecha.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-CONTEXT.md
@next.config.ts
@src/components/analytics/analytics-scripts.tsx
@src/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: investigar los issues de CSP y levantar el inventario de orígenes reales</name>
  <files>docs/content-security-policy.md</files>
  <read_first>
    - `node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md` completo. Esta versión de Next tiene cambios de ruptura frente a lo que puedas recordar: el archivo de middleware se llama `proxy.ts`, y el nonce **exige renderizado dinámico**.
    - `node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md`.
    - `next.config.ts` completo, en particular el comentario largo sobre `headers()`, que es la especificación de este requisito, y `images.remotePatterns`.
    - `src/components/analytics/analytics-scripts.tsx` completo: GA4 por `@next/third-parties/google` y el pixel de Meta como `<Script>` inline, los dos condicionados al consentimiento.
    - `src/components/structured-data.tsx`, la función `JsonLdScript`: emite `<script type="application/ld+json">` inline en casi todas las rutas.
    - `src/proxy.ts`, para saber qué ya corre en el borde de la aplicación y no duplicarlo.
    - `docs/cloudflare-robots.md`, como modelo del formato con el que este repo documenta algo que se sirve en parte desde Cloudflare y en parte desde el origen.
  </read_first>
  <action>
    **Investigación previa, obligatoria antes de escribir una sola directiva.** La auditoría del 2026-08-23 registra issues de CSP en devtools en `/agendar`, `/servicios/escoliosis-y-deformidades` y `/servicios/estenosis-espinal`. Averiguar qué son antes de proponer nada: si ya existe una CSP parcial en algún lado —un `<meta http-equiv>` en alguna plantilla, una regla de Cloudflare, una cabecera de un proveedor intermedio— puede estar bloqueando chunks legítimos, y una política nueva encima empeoraría el diagnóstico. Comprobar tres cosas y escribir el resultado de las tres: (1) `grep -rn 'Content-Security-Policy' src/ scripts/ public/ next.config.ts` sobre el repo, sabiendo que hoy solo aparece dentro del comentario de `next.config.ts`; (2) `curl -sI https://drangulocolumna.com/agendar` contra producción, buscando cualquier cabecera de política que no salga de este repo; (3) abrir las tres rutas en devtools con la pestaña Issues y anotar el texto literal del issue. Si el issue resulta ser el aviso genérico de Lighthouse de "no se encontró CSP", decirlo con esas palabras: es un hallazgo válido y cambia la lectura del requisito.

    **Inventario de orígenes.** Levantar la lista de lo que el sitio carga de verdad, cada entrada con la evidencia de dónde sale. Punto de partida verificado sobre el árbol:
    - Scripts de terceros: `www.googletagmanager.com` (GA4 vía `@next/third-parties/google`) y `connect.facebook.net` (pixel de Meta). Los dos solo se montan si hay consentimiento, pero la política tiene que permitirlos porque el consentimiento existe.
    - Conexiones salientes: los dominios de recolección de GA4, incluido el subdominio regional, y los de Meta.
    - Imágenes remotas: `**.cdninstagram.com` y `**.fbcdn.net`, ya declarados en `images.remotePatterns` de `next.config.ts` para las portadas de los reels.
    - Scripts inline propios: los `<script type="application/ld+json">` de `structured-data.tsx`, el bootstrap del pixel y la carga de datos de Next. Todos inline, ninguno con nonce.
    - Fuentes: Poppins e Inter están self-hosted por `next/font/google`, así que **no hay origen externo de fuentes**. Confirmarlo en el HTML servido antes de escribirlo.
    - Mapas de Google: según el comentario de `Permissions-Policy` en `next.config.ts`, van como enlace externo y no embebidos. Confirmar que no hay ningún `<iframe>` de Maps en el árbol antes de decidir si hace falta `frame-src`.

    **Decisión de arquitectura, y hay que dejarla escrita con su motivo.** La política **no usa nonce**. La guía de CSP de esta versión de Next dice que el nonce obliga a renderizado dinámico; las 23 rutas del sitio se prerenderizan estáticas y la fase 18 apoya una Cache Rule de Cloudflare sobre ese hecho. Cambiar a dinámico para ganar un nonce costaría el rendimiento que la fase anterior acaba de comprar. La consecuencia honesta, y va escrita: sin nonce, `script-src` necesita `'unsafe-inline'`, y eso significa que la política de esta etapa protege contra inyección de orígenes externos pero no contra XSS inline. Es una mejora real, no una completa, y el documento no puede dar a entender otra cosa.

    Crear `docs/content-security-policy.md` con: qué se encontró en la investigación de los tres issues, el inventario de orígenes con su evidencia, la decisión sobre el nonce con su costo, y la política report-only propuesta directiva por directiva, cada una justificada por una entrada del inventario. Ninguna directiva entra "por si acaso": si no hay una entrada del inventario que la pida, no va.

    No tocar `next.config.ts` en esta tarea. Acá solo se investiga y se documenta.
  </action>
  <verify>
    <automated>test -f docs/content-security-policy.md && grep -q 'cdninstagram' docs/content-security-policy.md && grep -q 'googletagmanager' docs/content-security-policy.md && grep -q 'connect.facebook.net' docs/content-security-policy.md && grep -q 'agendar' docs/content-security-policy.md && grep -qi 'nonce' docs/content-security-policy.md && echo "inventario ok"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/content-security-policy.md` existe y el comando imprime `inventario ok`.
    - El documento nombra las tres rutas de la auditoría y dice, para cada una, qué era el issue que Lighthouse reportaba. Si el issue era el aviso genérico de ausencia de CSP, lo dice con esas palabras.
    - El documento registra el resultado de `grep -rn 'Content-Security-Policy' src/ scripts/ public/ next.config.ts` y de `curl -sI` contra las tres rutas de producción, pegado literal.
    - Cada directiva propuesta está justificada por una entrada del inventario. No hay directivas sin origen que las pida.
    - El documento dice explícitamente que la política no usa nonce, por qué, y qué protección deja fuera esa decisión.
    - `git status --porcelain next.config.ts src/` está vacío: esta tarea no toca código.
  </acceptance_criteria>
  <done>Está escrito qué eran los issues de CSP, qué orígenes carga el sitio de verdad, y qué política se va a servir, con su justificación directiva por directiva.</done>
</task>

<task type="auto">
  <name>Tarea 2: endpoint de reportes y cabecera Content-Security-Policy-Report-Only</name>
  <files>src/app/api/csp-report/route.ts, next.config.ts</files>
  <read_first>
    - `docs/content-security-policy.md`, que dejó la tarea 1: la política se implementa tal como quedó justificada ahí.
    - `src/app/api/reviews/status/route.ts`: el patrón de route handler de este repo, con `export const dynamic = "force-dynamic"` y respuestas por `Response.json`.
    - `next.config.ts`, el bloque `headers()` con sus cinco cabeceras actuales y el comentario que las precede.
    - `node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md`.
  </read_first>
  <action>
    **Endpoint.** Crear `src/app/api/csp-report/route.ts` con un handler `POST` y `export const dynamic = "force-dynamic"`. Acepta los dos formatos que mandan los navegadores: el cuerpo `application/csp-report` del mecanismo antiguo y el `application/reports+json` del mecanismo `Reporting-Endpoints`. El endpoint es público y sin autenticación, porque el navegador que reporta no puede autenticarse; eso obliga a tres cuidados que van en el código y son criterio de aceptación: leer el cuerpo con un tope de tamaño y descartar lo que lo exceda, responder **siempre** `204` sin cuerpo —también ante un JSON inválido, para no darle a nadie una señal de sondeo—, y no persistir nada en disco. Los reportes se emiten por `console.warn` con un prefijo estable y fácil de filtrar en los logs, por ejemplo `csp-report`, y con los campos que sirven para calibrar: la directiva violada, el URI bloqueado y el documento donde ocurrió. Un handler `GET` que devuelva `405` deja claro que la ruta no es navegable.

    **Cabeceras.** En el bloque `headers()` de `next.config.ts`, junto a las cinco cabeceras que ya declara y bajo el mismo `source: "/:path*"`, agregar dos:
    - `Content-Security-Policy-Report-Only` con el valor que la tarea 1 justificó, en una sola línea, terminando con `report-uri /api/csp-report; report-to csp-endpoint`. Se declaran las dos formas de reporte a propósito: `report-to` es la moderna y `report-uri` es la que todavía entienden navegadores que no implementan Reporting API. Una sola de las dos deja huecos de cobertura justo en la etapa en que la cobertura es todo el objetivo.
    - `Reporting-Endpoints` con `csp-endpoint="https://drangulocolumna.com/api/csp-report"`. El nombre `csp-endpoint` tiene que ser **idéntico** al que nombra la directiva `report-to`. Si no coinciden, la política se sirve, todo parece correcto y no llega ni un reporte.

    Coherencia con lo que ya está: el sitio sirve `X-Frame-Options: SAMEORIGIN`, así que `frame-ancestors` va en `'self'` y no en `'none'`. Dos cabeceras que dicen cosas distintas sobre lo mismo es una contradicción que alguien va a resolver mal más adelante.

    **Actualizar el comentario largo** que precede a `headers()`. Hoy dice que falta la CSP y describe el plan de tres pasos. Después de esta tarea el paso 1 está hecho: el comentario tiene que decir dónde está el paso 2, que es `docs/content-security-policy.md`, y que el enforce sigue pendiente. Un comentario que describe un plan ya ejecutado es peor que ningún comentario. **Este cambio va en esta misma tarea, no en una posterior**: el comentario declara el estado del requisito y su invariante se rompe en el momento en que la cabecera se agrega.

    No convertir ninguna ruta a dinámica. No agregar nonce. No tocar `src/proxy.ts`: la canonicalización de host que vive ahí es de otro requisito y la CSP no la necesita.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run content:check && npm run seo:check && npm run sedes:check && grep -q 'Content-Security-Policy-Report-Only' next.config.ts && grep -q 'Reporting-Endpoints' next.config.ts && grep -q 'csp-endpoint' next.config.ts && test -f src/app/api/csp-report/route.ts && node -e "const s=require('fs').readFileSync('next.config.ts','utf8');const m=s.match(/report-to ([a-z-]+)/);const r=s.match(/([a-z-]+)=\"https:\/\/[^\"]*csp-report\"/);if(!m||!r||m[1]!==r[1]){console.error('el nombre del endpoint no coincide entre report-to y Reporting-Endpoints');process.exit(1)}console.log('endpoint name ok:',m[1])"</automated>
  </verify>
  <acceptance_criteria>
    - Las cinco compuertas en 0: `content:check`, `seo:check`, `sedes:check`, `tsc --noEmit`, `build`.
    - El comando imprime `endpoint name ok:` seguido del nombre, lo que prueba que la directiva `report-to` y la cabecera `Reporting-Endpoints` nombran el mismo endpoint.
    - `next.config.ts` declara siete cabeceras bajo `/:path*`: las cinco anteriores más `Content-Security-Policy-Report-Only` y `Reporting-Endpoints`. Las cinco anteriores quedan con su valor idéntico.
    - La política contiene `frame-ancestors 'self'` y **no** `frame-ancestors 'none'`, para no contradecir el `X-Frame-Options: SAMEORIGIN` que ya se sirve.
    - La política contiene `Content-Security-Policy-Report-Only` y **no** una cabecera `Content-Security-Policy` a secas: `grep -cE '^\s*key: "Content-Security-Policy"' next.config.ts` devuelve 0. El enforce no es de esta fase.
    - `src/app/api/csp-report/route.ts` exporta `POST` y `dynamic = "force-dynamic"`, responde 204 en todos los caminos del `POST`, incluido el de JSON inválido, y no escribe en disco: `grep -vE '^\s*(//|\*|/\*)' src/app/api/csp-report/route.ts | grep -cE 'writeFile|appendFile|createWriteStream'` devuelve 0.
    - En build local: `npm run start` y `curl -sI http://localhost:3000/` incluye la línea `content-security-policy-report-only`, y `curl -s -o /dev/null -w "%{http_code}" -X POST -H "Content-Type: application/csp-report" -d '{"csp-report":{}}' http://localhost:3000/api/csp-report` devuelve `204`. Con un cuerpo que no sea JSON, también `204`.
    - El comentario que precede a `headers()` ya no dice que falta la CSP en report-only, y remite a `docs/content-security-policy.md`.
    - `git diff package.json package-lock.json` está vacío.
  </acceptance_criteria>
  <done>El sitio sirve la política en modo report-only con un endpoint que recibe y registra los reportes, el nombre del endpoint coincide en las dos cabeceras, y ninguna ruta cambió de comportamiento ni de modo de renderizado.</done>
</task>

<task type="auto">
  <name>Tarea 3: procedimiento escrito para pasar a enforce, con fecha</name>
  <files>docs/content-security-policy.md</files>
  <read_first>
    - `docs/content-security-policy.md` tal como quedó tras las tareas 1 y 2.
    - `docs/cloudflare-robots.md`, sección `## Cómo comprobar que quedó`, como modelo del nivel de detalle que este repo espera de un procedimiento manual.
    - El comentario de `next.config.ts` sobre Cloudflare delante del origen.
  </read_first>
  <action>
    Agregar a `docs/content-security-policy.md` dos secciones.

    **Verificación después del deploy.** Que la cabecera salga del origen no garantiza que llegue al navegador: Cloudflare está delante y puede filtrar o reescribir cabeceras. El procedimiento: `curl -sI https://drangulocolumna.com/` y confirmar que aparece `content-security-policy-report-only`; repetirlo contra las tres rutas que la auditoría señaló, `/agendar`, `/servicios/escoliosis-y-deformidades` y `/servicios/estenosis-espinal`; y confirmar que el valor que llega es idéntico al que declara `next.config.ts`, no una versión recortada. Si la cabecera no llega o llega distinta, el lugar donde mirar es Transform Rules y Managed Transforms del zone de Cloudflare, y eso lo revisa Juan en el panel.

    **Procedimiento de enforce, paso a paso y con criterio de salida.** Escribir los pasos concretos: durante cuántos días se observan los reportes en los logs, qué se hace con cada violación reportada —distinguir la que denuncia un origen legítimo que falta en la política, que se agrega, de la que denuncia una extensión del navegador del visitante, que se ignora, de la que denuncia una inyección real, que es el caso que justifica todo esto—, y cuál es el criterio de salida: cero violaciones atribuibles al sitio durante una ventana completa de observación con tráfico real. Recién con ese criterio cumplido se cambia el nombre de la cabecera de `Content-Security-Policy-Report-Only` a `Content-Security-Policy`, se conservan `report-uri` y `report-to` para no quedarse ciego después del cambio, y se vuelve a verificar contra las tres rutas.

    **Fecha.** La ventana de observación no se puede acortar: necesita días de tráfico real. Fijar la revisión para el **2026-09-15**, tres semanas después de esta fase, y decir de qué depende que esa fecha se mueva: si en la fecha no hubo tráfico suficiente para llenar la ventana, se corre la revisión, no se hace el enforce a medias. Dejar escrito que el enforce es una tarea de seguimiento con esa fecha y que **no es alcance de la fase 19**, y anotar la relación con el issue #17: se cierra con la report-only desplegada y el procedimiento documentado.

    Texto humanizado antes de entregarse.
  </action>
  <verify>
    <automated>grep -q '2026-09-15' docs/content-security-policy.md && grep -q 'curl -sI' docs/content-security-policy.md && grep -q 'escoliosis-y-deformidades' docs/content-security-policy.md && grep -qi 'cloudflare' docs/content-security-policy.md && echo "procedimiento ok"</automated>
  </verify>
  <acceptance_criteria>
    - El comando imprime `procedimiento ok`.
    - El documento contiene el comando `curl -sI` exacto y las cuatro URLs contra las que se corre: la portada y las tres rutas de la auditoría.
    - El documento contiene la fecha `2026-09-15` como revisión de enforce, y dice de qué depende que se mueva.
    - El documento distingue los tres tipos de violación reportada y qué se hace con cada uno.
    - El criterio de salida hacia enforce está escrito como una condición comprobable, no como una impresión.
    - El documento dice que el enforce no es alcance de la fase 19 y qué cierra el issue #17.
  </acceptance_criteria>
  <done>Quien tome la tarea de enforce el 2026-09-15 puede ejecutarla leyendo un solo documento, sin reconstruir el razonamiento de esta fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `/api/csp-report` | Endpoint público sin autenticación, porque el navegador que reporta no puede autenticarse. Cualquiera puede enviarle cuerpos arbitrarios. |
| origen → Cloudflare → navegador | Cloudflare puede filtrar o reescribir la cabecera. La política puede salir correcta del origen y no llegar. |
| terceros → página | GA4, el pixel de Meta y el CDN de Instagram cargan código y recursos de fuera del sitio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04-01 | Denial of Service | `/api/csp-report` | high | mitigate | Endpoint público: un atacante puede inundarlo y llenar los logs. Tope de tamaño del cuerpo, nada persistido en disco, respuesta 204 constante y sin cuerpo, y prefijo estable en el log para poder filtrar o silenciar sin desplegar. |
| T-19-04-02 | Information Disclosure | respuestas de `/api/csp-report` | medium | mitigate | Una respuesta distinta ante JSON válido e inválido convierte al endpoint en un oráculo de sondeo. Se responde 204 en todos los caminos, incluido el error de parseo. `GET` devuelve 405 y nada más. |
| T-19-04-03 | Tampering | despliegue de una CSP mal calibrada | critical | mitigate | Una política en enforce mal calibrada rompe producción en silencio. Por eso esta fase entrega **solo** report-only, y el criterio de aceptación prohíbe explícitamente que exista una cabecera `Content-Security-Policy` a secas. |
| T-19-04-04 | Elevation of Privilege | XSS inline | high | accept | Sin nonce, `script-src` necesita `'unsafe-inline'` y la política no protege contra XSS inline. Se acepta con motivo escrito: el nonce exige renderizado dinámico en esta versión de Next, lo que apagaría el prerenderizado de las 23 rutas y la Cache Rule de la fase 18. La aceptación y su costo van en `docs/content-security-policy.md`, no ocultos en un commit. |
| T-19-04-05 | Spoofing | desajuste entre `report-to` y `Reporting-Endpoints` | high | mitigate | Si los nombres no coinciden, todo parece bien y no llega ningún reporte, y la etapa de observación se completa sobre datos vacíos. Comprobación automatizada que compara los dos nombres en `next.config.ts`. |
| T-19-04-06 | Tampering | Cloudflare filtrando la cabecera | high | mitigate | Verificación con `curl -sI` contra producción después del deploy, contra la portada y las tres rutas de la auditoría, con el punto exacto del panel donde mirar si no llega. |
| T-19-04-07 | Repudiation | contradicción entre `frame-ancestors` y `X-Frame-Options` | medium | mitigate | `frame-ancestors 'self'` para no contradecir el `SAMEORIGIN` que ya se sirve. Criterio de aceptación explícito. |
| T-19-04-SC | Tampering | instalaciones de npm | high | mitigate | Este plan instala cero dependencias: el endpoint usa solo la API de `Request` y `Response` del runtime. `git diff package.json package-lock.json` tiene que salir vacío. |
</threat_model>

<verification>
- Las cinco compuertas en 0: `content:check`, `seo:check`, `sedes:check`, `tsc --noEmit`, `build`.
- `next.config.ts` sirve siete cabeceras bajo `/:path*`, con `Content-Security-Policy-Report-Only` y `Reporting-Endpoints` entre ellas, y ninguna `Content-Security-Policy` en enforce.
- El nombre del endpoint coincide entre la directiva `report-to` y la cabecera `Reporting-Endpoints`, comprobado por comando.
- En local: `curl -sI http://localhost:3000/` muestra la cabecera report-only, y un `POST` a `/api/csp-report` devuelve 204 tanto con JSON válido como inválido.
- `docs/content-security-policy.md` contiene la investigación de los tres issues, el inventario de orígenes con evidencia, la decisión sobre el nonce con su costo, el procedimiento de verificación post-deploy y el de enforce con fecha 2026-09-15.
- `git diff package.json package-lock.json` vacío; `src/proxy.ts` sin tocar.

**Verificación post-deploy, a cargo de Juan.** `curl -sI` contra `https://drangulocolumna.com/`, `/agendar`, `/servicios/escoliosis-y-deformidades` y `/servicios/estenosis-espinal`, confirmando que la cabecera llega desde producción con el mismo valor que declara el origen. No bloquea el cierre de la fase; se registra en el SUMMARY.
</verification>

<success_criteria>
- MEAS-02 cumplido en el alcance declarado: el sitio sirve una Content-Security-Policy en modo report-only, las 23 URLs siguen respondiendo igual que antes porque report-only no bloquea nada, y hay un endpoint recibiendo los reportes.
- Los issues de CSP que Lighthouse reportaba en las tres rutas quedan investigados y explicados por escrito, con lo que se encontró de verdad y no con una suposición.
- El enforce queda como tarea de seguimiento con fecha y procedimiento, exactamente como el comentario de `next.config.ts` lo pedía desde antes de esta fase.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 04)

| Artifact | Tipo | Qué contiene |
|---|---|---|
| `docs/content-security-policy.md` | nuevo | Investigación de los issues de las tres rutas, inventario de orígenes con evidencia, decisión sobre el nonce y su costo, política directiva por directiva, verificación post-deploy y procedimiento de enforce con fecha 2026-09-15. |
| `src/app/api/csp-report/route.ts` | nuevo | Route handler `POST` público, con tope de cuerpo, respuesta 204 constante, sin persistencia y con log filtrable. |
| `next.config.ts` | modificado | `Content-Security-Policy-Report-Only` y `Reporting-Endpoints` junto a las cinco cabeceras existentes, más el comentario largo actualizado al estado real del plan de tres pasos. |
| Tarea de seguimiento: enforce de la CSP | pendiente con fecha | 2026-09-15, con su criterio de salida escrito. No es alcance de la fase 19. |
</artifacts>

<output>
Crear `.planning/workstreams/seo-fixes/phases/19-confianza-medici-n-y-seguridad/19-04-SUMMARY.md` al terminar. Anotar: qué eran los issues de CSP de las tres rutas, la política final servida en una línea, la salida de `curl -sI` en local, y la fecha de la revisión de enforce.
</output>
