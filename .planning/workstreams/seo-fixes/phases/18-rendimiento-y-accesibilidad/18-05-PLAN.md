---
phase: 18-rendimiento-y-accesibilidad
plan: 05
type: execute
wave: 2
depends_on: ["18-01", "18-03"]
files_modified:
  - package.json
  - next.config.ts
  - src/components/instagram/instagram-reels-section.tsx
  - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-BUNDLE-ANALYSIS.md
autonomous: false
requirements: [CWV-04]
must_haves:
  truths:
    - "El chunk compartido arrastra menos de 15 KB de JavaScript sin usar, contra los 28 KB de la auditoría del 2026-08-23."
    - "Ninguna ruta que no puede renderizar el carrusel de reels descarga el código del carrusel."
    - "El sitio no sirve polyfills legacy a navegadores que no los necesitan, y el rango de navegadores soportado está declarado explícitamente en vez de heredado por defecto."
    - "Ninguna de las 23 rutas cambia el HTML que sirve ni el aspecto de una sola superficie."
    - "El CLS de las rutas medidas sigue en 0. Este plan toca `instagram-reels-section.tsx` y reorganiza el grafo de módulos: es un cambio con presupuesto de CLS y sin nadie que lo mida, el CLS se gasta en silencio. Línea base del 2026-08-24: 0 en las 24 rutas."
  artifacts:
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-BUNDLE-ANALYSIS.md
    - package.json
  key_links:
    - "Medición del build del 2026-08-24, tomada con `find` y no con un glob de un solo nivel: el build produce **26 documentos HTML**, no 12. Trece están anidados (`blog/*` ×5, `sedes/*` ×4, `servicios/*` ×5) y un glob `.next/server/app/*.html` no los ve. **Dos chunks** cargan el código del carrusel: `0krsqwhv3zry_.js` (29.656 bytes), referenciado por 25 de los 26 documentos, y `420mtziom5wrd.js` (3.498 bytes), referenciado solo por `index.html` y `testimonios.html`. El único documento que no arrastra el chunk grande es `_global-error.html`. Solo `/` y `/testimonios` montan `InstagramReelsSection`."
    - "Los 70.799 bytes que la auditoría del 2026-08-23 atribuye al chunk compartido **no corresponden a ningún archivo de este build**: el chunk más grande de `.next/static/chunks/` pesa 226.356 bytes y el del carrusel 29.656. Casi seguro es tamaño de transferencia comprimido, o la suma de varios chunks. No salir a buscar un archivo de ese peso: no existe. Los 28 KB de JavaScript sin usar sí se miden, pero con Coverage y sobre los archivos reales."
    - "`node_modules/next/dist/docs/03-architecture/supported-browsers.md`: el browserslist por defecto de esta versión ya es `chrome 111`, `edge 111`, `firefox 111`, `safari 16.4`, y Next solo carga sus polyfills en los navegadores que los necesitan, por la vía `nomodule`. El proyecto no declara `browserslist` en `package.json`."
    - "La salida de `next build` de esta versión NO trae columna de tamaños: solo ruta, `Revalidate` y `Expire`. El peso del bundle se mide sobre los archivos de `.next/static/chunks/` y con el panel de Coverage, nunca leyendo la tabla del build."
    - statement: "Las 23 rutas siguen sirviendo el mismo HTML y la navegación del router sigue funcionando después de reorganizar el punto de importación."
      verification: backstop
---

<objective>
Cerrar CWV-04: sacar del JavaScript que se descarga en todas las rutas el código que solo dos rutas pueden llegar a usar, y declarar explícitamente el rango de navegadores que el sitio soporta.

Purpose: hoy un paciente que abre `/privacidad` descarga, analiza y compila el código de un carrusel de videos de Instagram que esa página no tiene y no puede tener. Ese costo se paga en cada carga de cada ruta, en móviles de gama media, antes de que la página responda a un toque.

Output: `18-BUNDLE-ANALYSIS.md` con el mapa del bundle antes y después, y el bundle reorganizado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-CONTEXT.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Tarea 1: Compuerta de legitimidad de `@next/bundle-analyzer`</name>
  <read_first>
    - package.json — la lista de dependencias actual, donde ya vive `@next/third-parties` de la misma organización
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-CONTEXT.md, sección "Assets y bundle"
  </read_first>
  <what-built>Todavía nada. Esta tarea es la compuerta previa a la única instalación de paquete de toda la fase 18.

La fase 18 no tiene `RESEARCH.md` con tabla de auditoría de legitimidad de paquetes, así que la política de respaldo aplica y `@next/bundle-analyzer` se trata como `[ASSUMED]`: hay que verificarlo a mano antes de instalarlo. Es la única instalación de la fase; los otros cuatro planes no tocan `package.json`.</what-built>
  <how-to-verify>
1. Abrir `https://www.npmjs.com/package/@next/bundle-analyzer` y confirmar tres cosas: que el paquete está publicado bajo el alcance `@next`, que su repositorio apunta a `vercel/next.js`, y que su última versión es coherente con la versión de Next instalada en el proyecto, que es la 16.2.12.
2. Confirmar que el alcance `@next` es el mismo del que el proyecto ya depende hoy: `@next/third-parties` está en `dependencies` desde antes de esta fase.
3. Confirmar que se instala como dependencia de desarrollo, no de producción: es una herramienta de análisis de build y no debe entrar en el árbol que se despliega.
  </how-to-verify>
  <acceptance_criteria>
    - El paquete está publicado bajo el alcance `@next` y su repositorio es `vercel/next.js`.
    - Se instala en `devDependencies`, no en `dependencies`.
    - Sin esta aprobación, la tarea 2 no arranca. Esta compuerta no es auto-aprobable, independientemente de la configuración de avance automático.
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" para autorizar la instalación, o "rechazado" con el motivo</resume-signal>
</task>

<task type="auto">
  <name>Tarea 2: Mapear el bundle y sacar el código muerto de las rutas que no lo usan</name>
  <precondition>La compuerta de legitimidad de la tarea 1 está aprobada. Sin esa aprobación no se instala `@next/bundle-analyzer` ni ningún otro paquete.</precondition>
  <files>package.json, next.config.ts, src/components/instagram/instagram-reels-section.tsx, .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-BUNDLE-ANALYSIS.md</files>
  <read_first>
    - AGENTS.md — esta versión de Next tiene cambios de ruptura respecto de lo que recuerdas
    - node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md — sobre todo la advertencia de que cuando un Server Component importa dinámicamente un Client Component, la división automática de código no está soportada
    - node_modules/next/dist/docs/03-architecture/supported-browsers.md — el browserslist por defecto de esta versión y cómo carga sus polyfills
    - src/components/instagram/instagram-reels-section.tsx — el `import` estático de `ReelsCarousel` en la línea 5, dentro de un Server Component `async`
    - src/app/testimonios/page.tsx y src/app/page.tsx — las dos únicas rutas que montan `InstagramReelsSection`
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-03-SUMMARY.md — si el plan 18-03 ya reorganizó el punto de importación, esta tarea mide sobre ese resultado y no sobre la línea base vieja
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md, sección `## Contrato del montaje diferido`
  </read_first>
  <action>
Arranque de la skill, una vez por sesión, con cwd en la raíz del repo: `node ~/.claude/skills/impeccable/scripts/context.mjs --target src/components/instagram/instagram-reels-section.tsx`. La skill es global, en `~/.claude/skills/impeccable/`, y no hay copia local en el proyecto. El verbo es `optimize`: la regla de diseño obligatoria de `18-CONTEXT.md` manda el bundle por ese verbo, igual que `/testimonios` y la imagen del LCP. Si la skill ya se arrancó en la sesión, no se vuelve a arrancar.

Instalar `@next/bundle-analyzer` como dependencia de desarrollo y envolver la configuración de `next.config.ts` con él, activado por variable de entorno para que un build normal no cambie de comportamiento. Correr el análisis y guardar el mapa.

Advertencia sobre el método de medición, porque la doc de tu memoria no aplica: la salida de `next build` de esta versión no trae columna de tamaños. Sus columnas son ruta, `Revalidate` y `Expire`, nada más. El peso se mide de dos formas y las dos van al archivo: sobre los archivos de `.next/static/chunks/` referenciados por cada documento de `.next/server/app/`, y con el panel de Coverage de devtools contra producción, que es lo que dice qué porcentaje de cada chunk se ejecuta de verdad.

El hallazgo que esta tarea implementa está medido sobre el build del 2026-08-24, con `find` y no con un glob de un solo nivel. **El build produce 26 documentos HTML, no 12**: trece están anidados bajo `blog/`, `sedes/` y `servicios/`, y un glob `.next/server/app/*.html` no los ve. El código del carrusel vive en **dos** chunks, no en uno: `0krsqwhv3zry_.js` (29.656 bytes), referenciado por 25 de los 26 documentos, y `420mtziom5wrd.js` (3.498 bytes), referenciado solo por `index.html` y `testimonios.html`. La tabla de antes y después nombra los dos.

`InstagramReelsSection` importa `ReelsCarousel` de forma estática desde un Server Component, así que el código entra en el grafo de cliente y termina en un chunk que casi todas las rutas descargan. Solo `/` y `/testimonios` montan esa sección: **las otras 23 rutas descargan código que no pueden ejecutar**, `/privacidad` incluida, que nunca va a renderizar un carrusel. Las 23 son las nueve de primer nivel (`/_not-found`, `/agendar`, `/blog`, `/contacto`, `/preguntas-frecuentes`, `/privacidad`, `/sedes`, `/servicios`, `/sobre-el-doctor`) más los cinco posts de `blog/`, las cuatro sedes y los cinco servicios.

Y un dato para no perder tiempo: **los 70.799 bytes que la auditoría atribuye al chunk compartido no corresponden a ningún archivo de este build.** El más grande de `.next/static/chunks/` pesa 226.356 bytes. Es casi seguro tamaño de transferencia comprimido, o una suma. No salgas a buscar ese archivo; medí sobre los reales y con Coverage.

Reorganizar el punto de importación para que el código del carrusel deje de estar en el chunk que todas las rutas cargan. Cuál es la forma concreta la decide el mapa del análisis: puede ser aislar el componente de cliente detrás de un límite propio, o mover el punto de importación, o partir la sección. La restricción no es el mecanismo sino el resultado, y el resultado se mide con la sonda del criterio de aceptación.

Tres restricciones que el arreglo no puede violar:

1. **`dynamic(..., { ssr: false })` está prohibido en toda la fase.** Borra el HTML del servidor y obliga a un placeholder, que es el único camino por el que el CLS puede dejar de ser 0. Y `InstagramReelsSection` es un Server Component `async`: la doc de la versión instalada dice que `ssr: false` no está permitido con `next/dynamic` en Server Components.
2. **`dynamic()` sin `ssr: false` tampoco compra bundle acá.** La misma doc advierte que cuando un Server Component importa dinámicamente un Client Component, la división automática de código no está soportada. Si el mapa demuestra lo contrario en esta versión, adjuntar la medición; si no, el camino es otro.
3. **El HTML servido de las 23 rutas no cambia.** Esto es reorganización del grafo de módulos, no del marcado.

Cerrar con los polyfills legacy. El browserslist por defecto de esta versión de Next ya es moderno (`chrome 111`, `edge 111`, `firefox 111`, `safari 16.4`) y Next solo carga sus polyfills en los navegadores que los necesitan, por la vía `nomodule`, así que la mayoría del tráfico no los descarga. El proyecto hoy no declara `browserslist` en `package.json`: se hereda el default sin dejar constancia. Declararlo explícitamente en `package.json` con el rango que el sitio realmente soporta, y comprobar en el análisis si sigue apareciendo un chunk de polyfills en la carga de las rutas. Si la auditoría marcó polyfills que en realidad viajan por `nomodule` y ningún navegador moderno descarga, decirlo en el archivo con la evidencia: un hallazgo cerrado por medición vale más que un arreglo cosmético.

Un cuidado con `next.config.ts` que no cubre ninguna compuerta: ese archivo declara cinco cabeceras de seguridad y cinco redirecciones permanentes, y `scripts/check-seo.mjs` no lo referencia ni una vez en sus 747 líneas (verificado el 2026-08-24). Envolverlo con el analizador es un cambio sobre código que nadie vigila. Leer el `git diff next.config.ts` entero antes de commitear, no tocar las claves `headers()` ni `redirects()`, y dejar que las dos aserciones de conteo del `<verify>` lo respalden. La comprobación de que las cinco cabeceras siguen llegando al navegador de verdad la hace el plan 18-02 contra producción; acá se protege el archivo.

Escribir `18-BUNDLE-ANALYSIS.md` con: la tabla de chunks por ruta antes y después con sus pesos, nombrando **los dos** chunks que hoy cargan el código del carrusel (`0krsqwhv3zry_.js` de 29.656 bytes en 25 de los 26 documentos, y `420mtziom5wrd.js` de 3.498 bytes solo en `index.html` y `testimonios.html`); el porcentaje de código ejecutado según Coverage antes y después; la salida de la sonda de código muerto con su línea base de 23; el veredicto sobre los polyfills con su evidencia; la nota de que los 70.799 bytes de la auditoría no corresponden a ningún archivo de este build; y el rango de `browserslist` declarado con su motivo.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test "$(for f in $(find .next/server/app -name '*.html'); do case "$f" in .next/server/app/index.html|.next/server/app/testimonios.html) continue;; esac; for c in $(grep -o '/_next/static/chunks/[A-Za-z0-9_-]*\.js' "$f" | sort -u); do p=".next${c#/_next}"; [ -f "$p" ] &amp;&amp; grep -q 'Ver reels anteriores' "$p" &amp;&amp; { echo "$f"; break; }; done; done | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -o '&lt;img' .next/server/app/testimonios.html | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(grep -o '&lt;h2' .next/server/app/testimonios.html | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(grep -o '&lt;h2' .next/server/app/sedes.html | wc -l | tr -d ' ')" = "7" &amp;&amp; grep -q 'browserslist' package.json &amp;&amp; test "$(grep -rn 'ssr: false' src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -cE 'Strict-Transport-Security|X-Content-Type-Options|X-Frame-Options|Referrer-Policy|Permissions-Policy' next.config.ts)" = "5" &amp;&amp; test "$(grep -c 'permanent: true' next.config.ts)" = "5" &amp;&amp; npm run content:check &amp;&amp; npm run seo:check &amp;&amp; npm run sedes:check &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Sonda de código muerto: recorriendo con `find .next/server/app -name '*.html'` y excluyendo por ruta completa `index.html` y `testimonios.html`, ninguno de los chunks que esos documentos referencian contiene la cadena `Ver reels anteriores`. La sonda devuelve 0. **Línea base medida el 2026-08-24 con la sonda corregida: 23 documentos con coincidencia, sobre 24 sondeados.** El recorrido tiene que ser `find` y no `.next/server/app/*.html`: el glob solo ve un nivel y se pierde los 13 documentos anidados, con lo que la sonda podía salir en verde con 13 rutas todavía arrastrando los 29.656 bytes.
    - Los 23 documentos que hoy dan coincidencia son: `_not-found`, `agendar`, `blog`, `contacto`, `preguntas-frecuentes`, `privacidad`, `sedes`, `servicios`, `sobre-el-doctor`, los cinco de `blog/` (`artrosis`, `ciatica`, `cirugia-de-columna`, `lumbalgia`, `reumatologo-o-traumatologo`), los cuatro de `sedes/` (`clinica-ricardo-palma`, `clinica-tezza`, `consultorio-privado`, `sanna-la-molina`) y los cinco de `servicios/` (`cirugia-minimamente-invasiva`, `escoliosis-y-deformidades`, `estenosis-espinal`, `hernia-discal`, `ortopedia-infantil`). El único documento sondeado que no da coincidencia es `_global-error.html`.
    - `@next/bundle-analyzer` figura en `devDependencies` de `package.json` y no en `dependencies`.
    - `package.json` declara una clave `browserslist`. Línea base medida hoy: no existe.
    - `next.config.ts` conserva sus cinco cabeceras de seguridad y sus cinco redirecciones permanentes después de envolverse con el analizador. Líneas base medidas el 2026-08-24: 5 y 5. Esta aserción existe porque ninguna de las cinco compuertas del proyecto mira ese archivo: `scripts/check-seo.mjs` no lo referencia ni una vez en sus 747 líneas.
    - `grep -rn 'ssr: false' src/` devuelve 0 líneas. Línea base medida hoy: 0.
    - El HTML servido no cambió: en `/testimonios`, `<img` = 2 y `<h2` = 4; en `/sedes`, `<h2` = 7 después del plan 18-01. Líneas base: 2 y 4 medidas hoy, y 7 es el valor que deja el plan 18-01 (hoy son 3, con cuatro `h3` que ese plan promueve).
    - `18-BUNDLE-ANALYSIS.md` existe y trae la tabla de chunks por ruta antes y después, con pesos en bytes.
    - El archivo trae el porcentaje de código ejecutado según Coverage antes y después, medido contra producción.
    - El archivo emite un veredicto sobre los polyfills legacy con su evidencia: o se eliminaron, o se demuestra que viajan por `nomodule` y ningún navegador del rango soportado los descarga.
    - El archivo justifica el rango de `browserslist` declarado.
    - Ninguno de los archivos que solo pertenecen a los planes 18-01 y 18-04 aparece en el diff.
    - Las cinco compuertas salen en 0. Línea base verificada hoy: las cinco en 0.
  </acceptance_criteria>
  <done>Ninguna de las 23 rutas que no pueden renderizar el carrusel descarga su código, el rango de navegadores está declarado explícitamente, el veredicto de los polyfills está escrito con evidencia, y el HTML servido de las 23 rutas no cambió.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: Verificación de cierre del bundle</name>
  <read_first>
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-BUNDLE-ANALYSIS.md
  </read_first>
  <what-built>El código del carrusel salió del chunk que todas las rutas descargan, el rango de navegadores quedó declarado en `package.json`, y el mapa del bundle antes y después quedó escrito en `18-BUNDLE-ANALYSIS.md`.</what-built>
  <how-to-verify>
1. Correr Lighthouse móvil sobre al menos tres rutas del sitio, entre ellas `/privacidad` y `/testimonios`. En la auditoría "Reduce unused JavaScript", el ahorro reportado tiene que ser menor a 15 KB. Línea base de la auditoría del 2026-08-23: 28 KB de JavaScript sin usar en el chunk compartido, el 41% de sus 70.799 bytes. (Ese peso de 70.799 bytes no corresponde a ningún archivo de este build: es casi seguro tamaño comprimido de transferencia. Los 28 KB de ahorro sí son la cifra a mover.)
2. **En cada una de esas rutas, mirar el CLS. Tiene que ser 0 en todas.** Línea base del 2026-08-24: 0 en las 24 rutas del sitio, y es el mejor número que tiene el sitio en Core Web Vitals. Este plan toca `instagram-reels-section.tsx` y reorganiza el grafo de módulos, que es exactamente la clase de cambio que puede introducir un salto de layout sin que ninguna de las cinco compuertas lo note: las aserciones de HTML idéntico de la tarea 2 cubren `/testimonios` y `/sedes`, dos de las 23 rutas, y no dicen nada de las otras 21. Un ahorro de 20 KB de JavaScript que suba el CLS de 0 a 0,05 es un retroceso y se rechaza, igual que en el plan 18-03.
3. En la misma corrida, mirar si sigue apareciendo la auditoría de polyfills legacy. Si aparece, contrastarla con el veredicto escrito en `18-BUNDLE-ANALYSIS.md`.
4. Navegar el sitio a mano con JavaScript activado: entrar a la portada, abrir el menú de especialidades del encabezado, ir a `/testimonios`, volver con el botón atrás del navegador y abrir el botón flotante de WhatsApp. Reorganizar el grafo de módulos es el tipo de cambio que rompe la hidratación en silencio y no lo detecta ninguna compuerta.
5. Confirmar que el aviso de cookies sigue apareciendo en una ventana de incógnito y que aceptar y rechazar siguen funcionando.
6. Confirmar que ninguna superficie se ve distinta.
  </how-to-verify>
  <acceptance_criteria>
    - El ahorro reportado por "Reduce unused JavaScript" es menor a 15 KB en las rutas probadas.
    - **El CLS es 0 en todas las rutas probadas con Lighthouse.** Sin esta cifra escrita, el verificador no da pase: el UI-SPEC exige rechazar un cambio que suba el CLS, y no se puede rechazar lo que nadie midió.
    - El menú de especialidades, la navegación con el botón atrás, el botón flotante de WhatsApp y el aviso de cookies siguen funcionando.
    - Ninguna superficie del sitio cambió de aspecto.
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" o describe qué dejó de funcionar o qué número no llegó</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro de npm → árbol de dependencias | Única instalación de paquete de toda la fase 18. Sin tabla de auditoría de legitimidad en la fase, aplica la política de respaldo y el paquete se trata como `[ASSUMED]`. |
| grafo de módulos de cliente → navegador | Reorganizar el punto de importación cambia qué código se hidrata en cada ruta. Una frontera mal puesta rompe la hidratación sin que ninguna compuerta lo note. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-SC | Tampering | instalación de `@next/bundle-analyzer` | high | mitigate | Compuerta humana bloqueante en la tarea 1, con verificación manual en `npmjs.com/package`: alcance `@next`, repositorio `vercel/next.js`, versión coherente con Next 16.2.12, e instalación en `devDependencies`. No es auto-aprobable. |
| T-18-16 | Denial of Service | hidratación de los componentes de cliente del armazón | high | mitigate | Recorrido manual del checkpoint de la tarea 3 sobre menú, navegación hacia atrás, botón flotante y aviso de cookies. Ninguna de las cinco compuertas del proyecto mide hidratación. |
| T-18-17 | Tampering | HTML servido de las 23 rutas | high | mitigate | Aserciones de conteo sobre `/testimonios` y `/sedes` en el `<verify>`: esto es reorganización del grafo de módulos, no del marcado. |
| T-18-18 | Denial of Service | `browserslist` demasiado estrecho | medium | mitigate | El rango se justifica por escrito en `18-BUNDLE-ANALYSIS.md`. Un rango más estrecho que el default de Next dejaría fuera navegadores que hoy sí funcionan, y los pacientes de este sitio no son un público de navegadores recientes. |
| T-18-19 | Tampering | envoltorio del analizador en `next.config.ts` | high | mitigate | **Ninguna compuerta del proyecto cubre este archivo.** `scripts/check-seo.mjs` no tiene una sola referencia a `next.config.ts` en sus 747 líneas (verificado el 2026-08-24: `grep -c 'next.config' scripts/check-seo.mjs` devuelve 0), así que las cinco cabeceras de seguridad y las cinco redirecciones permanentes que ese archivo declara no están protegidas por `npm run seo:check` ni por ninguna otra de las cinco. Mitigación real, en esta misma tarea: (1) el analizador se activa por variable de entorno, de modo que un build normal no cambia de comportamiento; (2) `git diff next.config.ts` se lee entero antes de commitear y el envoltorio no puede tocar las claves `headers()` ni `redirects()`; (3) aserción de conteo en el `<verify>` sobre las cinco cabeceras y las cinco redirecciones declaradas (baseline medida el 2026-08-24: `grep -cE 'Strict-Transport-Security\|X-Content-Type-Options\|X-Frame-Options\|Referrer-Policy\|Permissions-Policy' next.config.ts` = 5 y `grep -c 'permanent: true' next.config.ts` = 5); (4) la comprobación en producción de que las cinco cabeceras siguen llegando la hace el plan 18-02 en su tarea 3, contra el sitio desplegado, que es donde importa. |
</threat_model>

<verification>
1. Sonda de código muerto sobre el build: ninguna ruta distinta de `/` y `/testimonios` referencia un chunk que contenga el código del carrusel. Recorrido con `find`, no con un glob de un nivel. Línea base del 2026-08-24: 23 documentos con coincidencia sobre 24 sondeados.
2. Lighthouse móvil sobre al menos tres rutas: "Reduce unused JavaScript" por debajo de 15 KB.
3. CLS en 0 en cada una de esas rutas, escrito en el checkpoint de la tarea 3. Línea base del 2026-08-24: 0 en las 24 rutas.
4. Veredicto escrito sobre polyfills legacy, con evidencia.
5. `package.json` declara `browserslist` y `@next/bundle-analyzer` está en `devDependencies`.
6. Recorrido manual de hidratación: menú de especialidades, botón atrás, botón flotante de WhatsApp y aviso de cookies.
7. HTML servido sin cambios en las 23 rutas.
8. Las cinco compuertas del proyecto en 0 en cada commit.
</verification>

<success_criteria>
- El chunk compartido arrastra menos de 15 KB de JavaScript sin usar, contra los 28 KB de la auditoría.
- Ninguna de las 23 rutas que no pueden renderizar el carrusel descarga su código.
- El rango de navegadores soportado está declarado y el veredicto sobre polyfills legacy está escrito con evidencia.
- El CLS de las rutas medidas sigue en 0.
- La hidratación sigue intacta y ninguna superficie cambió de aspecto.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Quién lo produce | Qué contiene |
|---|---|---|
| `18-BUNDLE-ANALYSIS.md` | este plan, tarea 2 | Tabla de chunks por ruta antes y después con pesos, cobertura ejecutada según Coverage, salida de la sonda de código muerto, veredicto de polyfills y justificación del rango de `browserslist`. |
| `18-A11Y-EVIDENCE.md` | plan 18-01 | Evidencia de A11Y-01. |
| `18-CLOUDFLARE-CACHE.md` y `18-CWV01-MEASUREMENT.md` | plan 18-02 | Caché de borde y TTFB. |
| `18-TESTIMONIOS-PROFILE.md` | plan 18-03 | Atribución del costo de `/testimonios`; es la entrada de este plan. |
| `18-LCP-EVIDENCE.md` | plan 18-04 | Evidencia del LCP de `/sobre-el-doctor`. |

<output>
Create `.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-05-SUMMARY.md` when done
</output>
