---
phase: 18-rendimiento-y-accesibilidad
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/sobre-el-doctor/page.tsx
  - src/app/page.tsx
  - src/components/layout/header.tsx
  - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-LCP-EVIDENCE.md
autonomous: false
requirements: [CWV-03]
must_haves:
  truths:
    - "La imagen del doctor en `/sobre-el-doctor` se precarga con prioridad alta: el navegador la pide desde el `<head>`, con prioridad High en el panel de red, y deja de ser un LCP diferido."
    - "La imagen se sirve en formato moderno y en una variante de tamaño acorde a los 220px que se renderizan en móvil y los 260px desde 640px."
    - "El LCP de `/sobre-el-doctor` baja respecto de la línea base viva que la tarea 1 mide, como mediana de tres corridas en la misma sesión. **Los 2,64 s del 2026-08-23 no sirven de umbral:** la corrida del 2026-08-24 midió 2,73 s en esa ruta sin que nadie la tocara, así que un criterio de «menor a 2,64 s» arranca fallando su propia línea base. Los dos números quedan anotados como históricos."
    - "El LCP de esta ruta también está dominado por el TTFB, como el del resto del sitio: en la corrida del 2026-08-24 `/sobre-el-doctor` puntúa 0,96 con **TBT de 0 ms** y LCP de 2,73 s. Con el hilo principal desocupado, ese tiempo es primer byte. La medición de cierre de la tarea 3 tiene como precondición que la Cache Rule del plan 18-02 esté aplicada."
    - "Ninguna de las tres imágenes involucradas cambia de aspecto, de recorte ni de posición."
    - "El proyecto deja de usar la prop `priority`, que Next 16 declara obsoleta en favor de `preload`."
  artifacts:
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-LCP-EVIDENCE.md
  key_links:
    - "En Next 16 la prop `priority` está obsoleta en favor de `preload` (`node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md`, secciones `#### preload` y `#### priority`, y la tabla de versiones en `v16.0.0`). El proyecto la usa en tres lugares: `src/app/page.tsx:81`, `src/app/sobre-el-doctor/page.tsx:35` y `src/components/layout/header.tsx:89`."
    - "La misma doc advierte que `preload` no se combina con `fetchPriority` ni con `loading`: son caminos alternativos, no acumulativos. Elegir uno."
    - "`node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md`, línea 289: «In most cases, you should use `loading=\"eager\"` or `fetchPriority=\"high\"` instead of `preload`». Este plan elige `preload` igual, y la razón está escrita en la tarea 2: la recomendación apunta a los casos que las tres viñetas anteriores de esa misma sección enumeran (varias candidatas a LCP según el viewport, uso de `loading`, uso de `fetchPriority`), y ninguno aplica a una imagen única, arriba del pliegue, que es el elemento LCP de la ruta. El camino queda abierto igual: si la prioridad de red no da High, se cambia."
    - statement: "El LCP medido en producción con throttling móvil baja respecto de la línea base viva de la tarea 1, y el elemento LCP sigue siendo la imagen del doctor y no otro."
      verification: backstop
---

<objective>
Cerrar CWV-03: que la imagen del doctor de `/sobre-el-doctor` se descubra temprano, se pida con prioridad alta y se sirva en el tamaño que realmente se pinta, y que el proyecto deje de apoyarse en una prop que Next 16 declara obsoleta.

Purpose: `/sobre-el-doctor` es la página donde el paciente decide si el doctor le inspira confianza, y su LCP es la cara del doctor. Tarda entre 2,6 y 2,8 s en aparecer: 2,64 s el 2026-08-23 y 2,73 s el 2026-08-24, sin que nadie tocara la ruta entre las dos. La auditoría del 2026-08-23 registró además 21 KB de ahorro disponible en esa imagen.

Output: las tres declaraciones de `Image` migradas de la prop obsoleta, y `18-LCP-EVIDENCE.md` con lo que producción sirve de verdad antes y después.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-CONTEXT.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Medir lo que producción sirve de verdad para el LCP</name>
  <files>.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-LCP-EVIDENCE.md</files>
  <read_first>
    - AGENTS.md — esta versión de Next tiene cambios de ruptura respecto de lo que recuerdas. La doc de la versión instalada manda.
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md, secciones `#### preload`, `#### priority`, `#### loading`, `#### fetchPriority` y `#### sizes`
    - src/app/sobre-el-doctor/page.tsx (líneas 30-38): la declaración del `Image` del LCP
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-CONTEXT.md, sección "Assets y bundle"
    - ~/.claude/skills/impeccable/reference/optimize.md
  </read_first>
  <action>
Arranque de la skill, una vez por sesión, con cwd en la raíz del repo: `node ~/.claude/skills/impeccable/scripts/context.mjs --target src/app/sobre-el-doctor/page.tsx`. La skill es global, en `~/.claude/skills/impeccable/`, y no hay copia local. El verbo es `optimize`.

Medir antes de tocar, contra `https://drangulocolumna.com/sobre-el-doctor`, con throttling móvil y CPU 4x. Registrar seis cosas:

**Antes de las seis mediciones, establecer la línea base viva.** Correr Lighthouse tres veces sobre `/sobre-el-doctor` **en la misma sesión**, anotar las tres corridas y su mediana en `18-LCP-EVIDENCE.md` con fecha y hora, bajo el rótulo "línea base viva". Ese es el número contra el que se juzga el delta al cerrar, y existe porque el umbral que este plan traía estaba mal calibrado: la auditoría del 2026-08-23 dio 2,64 s de LCP y la corrida del 2026-08-24 dio **2,73 s** sin que nadie tocara la ruta, con el puntaje subiendo de 0,91 a 0,96. Un criterio de "menor a 2,64 s" arranca fallando contra la realidad de hoy. Los dos números quedan anotados como históricos, no como umbral.

Un dato que conviene tener a la vista antes de perfilar: **el LCP de esta ruta probablemente sea TTFB.** `/sobre-el-doctor` puntúa 0,96 con TBT de 0 ms, y el patrón se repite en todo el sitio (TBT de 0 ms en 21 de las 24 rutas con el LCP entre 2,1 y 3,6 s). Eso no invalida el trabajo de esta tarea, que es real y está bien fundado: la prop obsoleta hay que migrarla igual, y la variante que se descarga y el formato hay que medirlos igual. Lo que sí implica es que la mejora de LCP puede venir del plan 18-02 y no de acá, y que hay que poder distinguirlo. Por eso la medición de cierre declara la Cache Rule como precondición.

Las seis mediciones:

1. Qué elemento identifica Lighthouse como LCP, y su tiempo mediano. La auditoría del 2026-08-23 supuso que era la imagen del doctor: confirmarlo, no heredarlo.
2. Si el `<head>` de la respuesta trae el `<link rel="preload" as="image">` para `dr-angulo-implante-disco.avif`. Estado del build local de hoy: sí lo trae, con su `imageSrcSet` completo. Lo que hay que confirmar es que sobrevive en producción, donde Cloudflare está delante del origen.
3. La prioridad que el navegador le asigna a esa petición en el panel de red. Es el punto que decide si "prioridad alta" se cumple o no: hoy el documento no emite ningún `fetchpriority="high"`, así que la prioridad la fija el navegador a partir del preload y hay que leerla, no suponerla.
4. Qué variante de `srcset` se descarga realmente en un viewport de 375px con DPR 2, y cuántos bytes pesa. Contexto para juzgarlo: la imagen se renderiza a 220px bajo 640px y a 260px desde ahí, `sizes` ya declara `(min-width: 640px) 260px, 220px`, el original es `dr-angulo-implante-disco.avif` de 56 KB a 933×1400, y el `srcset` que Next emite va de 32w a 3840w.
5. El `Content-Type` de la respuesta de la imagen: hay que confirmar que llega en formato moderno y que Cloudflare no la está reescribiendo.
6. El atributo `src` de respaldo del `<img>`, que hoy apunta a la variante de 3840w, y si algún agente lo descarga además del `srcset`.

Escribir `18-LCP-EVIDENCE.md` con esas seis mediciones y un veredicto: cuál de ellas explica el LCP de la línea base viva y los 21 KB de ahorro que la auditoría reportó, y cuánto de ese tiempo es TTFB y por lo tanto trabajo del plan 18-02. Si el elemento LCP resulta no ser la imagen, decirlo y replantear la tarea 2 sobre el elemento que sí lo es.

No tocar código en esta tarea.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-LCP-EVIDENCE.md &amp;&amp; npm run content:check &amp;&amp; npm run seo:check &amp;&amp; npm run sedes:check &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `18-LCP-EVIDENCE.md` existe y registra las seis mediciones enumeradas, con la fecha y las condiciones de throttling.
    - El archivo trae la **línea base viva**: tres corridas de Lighthouse en la misma sesión con su mediana, fecha y hora. Es el umbral contra el que se juzga el cierre.
    - El archivo anota los 2,64 s del 2026-08-23 y los 2,73 s del 2026-08-24 como datos históricos rotulados, no como umbral, con la observación de que la ruta subió de 0,91 a 0,96 sin que nadie la tocara.
    - El archivo nombra el elemento LCP identificado por Lighthouse y su tiempo mediano en segundos.
    - El archivo registra la prioridad de red asignada a la petición de la imagen, con el valor literal que muestra el panel.
    - El archivo registra el ancho de la variante descargada y su peso en bytes, para un viewport de 375px con DPR 2.
    - El archivo registra el `Content-Type` con el que producción sirve la imagen.
    - El archivo emite un veredicto sobre qué explica el LCP de la línea base viva y los 21 KB de ahorro reportados, con la parte atribuible al TTFB separada del resto.
    - `git diff --name-only` de esta tarea no incluye ningún archivo bajo `src/`.
    - Las cinco compuertas salen en 0. Línea base verificada hoy: las cinco en 0.
  </acceptance_criteria>
  <done>Está medido qué elemento es el LCP, con qué prioridad se pide la imagen, qué variante y cuántos bytes se descargan en móvil, y qué explica el LCP de la línea base viva, con la parte de TTFB separada.</done>
</task>

<task type="auto">
  <name>Tarea 2: Migrar de la prop obsoleta y cerrar la brecha de prioridad</name>
  <files>src/app/sobre-el-doctor/page.tsx, src/app/page.tsx, src/components/layout/header.tsx</files>
  <read_first>
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-LCP-EVIDENCE.md — el veredicto que esta tarea implementa
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md, secciones `#### preload`, `#### priority`, `#### loading` y `#### fetchPriority`
    - src/app/sobre-el-doctor/page.tsx (líneas 30-38 y 149-156): la segunda imagen NO se precarga
    - src/app/page.tsx (líneas 76-84): la imagen del hero de la portada
    - src/components/layout/header.tsx (líneas 83-91): el logo del encabezado
  </read_first>
  <action>
Next 16 declaró obsoleta la prop `priority` del componente `Image` en favor de `preload`, para que el comportamiento quede explícito. El proyecto la usa en tres lugares y los tres se migran en este mismo commit: `src/app/sobre-el-doctor/page.tsx:35`, `src/app/page.tsx:81` y `src/components/layout/header.tsx:89`. Migrar dos y dejar uno reintroduce la deuda que esta tarea salda.

La migración es sustituir la prop obsoleta por `preload`, sin tocar ninguna otra prop de esas tres declaraciones: `src`, `alt`, `width`, `height`, `sizes` y `className` quedan carácter por carácter iguales en las tres. Esta tarea no cambia recortes, ni proporciones, ni posiciones de objeto, ni tamaños renderizados.

La segunda imagen de `/sobre-el-doctor`, la de fluoroscopia de la línea 149, no lleva `preload` y no se le agrega. Está por debajo del pliegue y precargar dos imágenes compite con el LCP en vez de ayudarlo. La doc lo dice expresamente: no usar `preload` cuando hay varias candidatas a LCP según el viewport.

Antes de elegir camino, un párrafo de la doc que conviene tener leído para no dudar a mitad de la tarea. `image.md` línea 289 dice: «In most cases, you should use `loading="eager"` or `fetchPriority="high"` instead of `preload`». Suena a que este plan eligió mal, y no es así: esa frase cierra una lista de tres casos en los que `preload` no corresponde (varias candidatas a LCP según el viewport, uso de `loading`, uso de `fetchPriority`), y ninguno de los tres aplica acá. La imagen del doctor es una sola, está arriba del pliegue y es el elemento LCP de la ruta, que es exactamente el caso para el que `preload` existe. **`preload` es el camino por defecto de esta tarea, con esa justificación.** Lo que decide si se queda no es la doc sino la medición.

Cerrar entonces la brecha que la tarea 1 haya medido, con esta regla de decisión, y anotar el camino elegido en `18-LCP-EVIDENCE.md` con el valor de prioridad que lo motivó:

- **Camino A, el previsto:** la petición de la imagen del LCP sale con prioridad High en el panel de red. `preload` bastó, no se agrega nada más, y la declaración de `/sobre-el-doctor` queda con `preload` y sin `fetchPriority` ni `loading`.
- **Camino B:** la prioridad sale por debajo de High. Se quita `preload` de esa declaración y se ponen `loading="eager"` y `fetchPriority="high"`. Los dos caminos son alternativos y no se acumulan: la doc advierte que `preload` no se usa cuando se usa `fetchPriority` ni cuando se usa `loading`.

**El camino B tiene una consecuencia medible en el HTML servido, y hay que tenerla presente porque cambia lo que se asserta:** sin `preload`, Next deja de emitir el `<link rel="preload" as="image">` de la imagen del doctor, así que las apariciones de `imageSrcSet` en `/sobre-el-doctor` bajan de 2 a 1, la que queda es la del logo del encabezado, y la mención de `dr-angulo-implante-disco` desaparece de los enlaces de precarga. Eso es el resultado correcto del camino B, no una regresión. El `<verify>` de abajo lo contempla; si tomás el camino B y una aserción escrita para el camino A te falla, la aserción está mal, no tu trabajo.

Si la tarea 1 midió que la variante descargada en móvil es más ancha de lo que hace falta, corregir `sizes` para que declare el ancho real. El valor de hoy, `(min-width: 640px) 260px, 220px`, ya coincide con lo que dibuja el `className`, así que un ajuste acá solo procede si la medición muestra otra cosa, y en ese caso el ancho nuevo tiene que salir de la medición, no de una estimación.

Prohibido cambiar `width` y `height` del `Image`: son la relación de aspecto intrínseca del archivo (933×1400) y tocarlas mueve el espacio reservado, que es la vía directa a introducir CLS en una página que hoy está en 0.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test "$(grep -rn '^\s*priority\s*$' src/ | wc -l | tr -d ' ')" = "0" &amp;&amp; P=$(grep -c 'preload' src/app/sobre-el-doctor/page.tsx) &amp;&amp; S=$(grep -o 'imageSrcSet' .next/server/app/sobre-el-doctor.html | wc -l | tr -d ' ') &amp;&amp; if [ "$P" = "1" ]; then test "$S" = "2" &amp;&amp; grep -q 'dr-angulo-implante-disco' .next/server/app/sobre-el-doctor.html; else test "$P" = "0" &amp;&amp; test "$(grep -c 'fetchPriority="high"' src/app/sobre-el-doctor/page.tsx)" = "1" &amp;&amp; test "$(grep -c 'loading="eager"' src/app/sobre-el-doctor/page.tsx)" = "1" &amp;&amp; test "$S" = "1"; fi &amp;&amp; test "$(grep -o 'imageSrcSet' .next/server/app/index.html | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(grep -c 'width={933}' src/app/sobre-el-doctor/page.tsx)" = "1" &amp;&amp; test "$(grep -c 'height={1400}' src/app/sobre-el-doctor/page.tsx)" = "1" &amp;&amp; npm run content:check &amp;&amp; npm run seo:check &amp;&amp; npm run sedes:check &amp;&amp; npx tsc --noEmit</automated>
    <note>El bloque condicional es deliberado. La aserción de `preload` y la de `imageSrcSet` dependen del camino que la medición de la tarea 1 haya elegido, y una aserción fija sobre `preload = 1` e `imageSrcSet = 2` reprobaría el camino B, que este mismo plan sanciona. Ese fue el defecto de la fase 16 y no se repite: un criterio que falla contra trabajo correcto es un criterio roto. La aserción de la portada (`imageSrcSet = 2`) sí es fija, porque la portada conserva `preload` en los dos caminos.</note>
  </verify>
  <acceptance_criteria>
    - `grep -rn '^\s*priority\s*$' src/ | wc -l` devuelve 0. Línea base medida hoy: 3, en `src/app/page.tsx:81`, `src/app/sobre-el-doctor/page.tsx:35` y `src/components/layout/header.tsx:89`.
    - **Camino A (`preload`, el previsto):** `grep -c 'preload' src/app/sobre-el-doctor/page.tsx` devuelve 1, solo la imagen de arriba del pliegue y nunca la de fluoroscopia; y en el HTML prerenderizado de `/sobre-el-doctor` hay exactamente 2 apariciones de `imageSrcSet` en enlaces de precarga (el logo del encabezado y la imagen del doctor), una de ellas con `dr-angulo-implante-disco`. Línea base medida el 2026-08-24: `preload` en 0 (todavía usa la prop obsoleta) e `imageSrcSet` en 2.
    - **Camino B (prioridad explícita):** `grep -c 'preload' src/app/sobre-el-doctor/page.tsx` devuelve 0, `grep -c 'fetchPriority="high"'` devuelve 1, `grep -c 'loading="eager"'` devuelve 1, y las apariciones de `imageSrcSet` en `/sobre-el-doctor` bajan a 1, la del logo. Que baje a 1 es el resultado correcto de este camino: sin `preload`, Next no emite el enlace de precarga de la imagen del doctor.
    - Los dos caminos son excluyentes y el `<verify>` los evalúa condicionalmente. Ningún criterio de este plan puede reprobar el camino B por cumplir el contrato del camino A.
    - `npm run build` no emite ninguna advertencia de prop obsoleta sobre `Image`.
    - En el HTML prerenderizado de la portada siguen habiendo exactamente 2 apariciones de `imageSrcSet`. Línea base medida hoy: 2, el logo y `dr-angulo-consulta`.
    - `grep -c 'width={933}' src/app/sobre-el-doctor/page.tsx` devuelve 1 y `grep -c 'height={1400}' ...` devuelve 1: las dimensiones intrínsecas no se movieron. Línea base medida hoy: 1 y 1.
    - Las cadenas `object-cover`, `w-[220px]` y `sm:w-[260px]` siguen presentes en `src/app/sobre-el-doctor/page.tsx`: el recorte y el tamaño renderizado no cambiaron.
    - `18-LCP-EVIDENCE.md` registra qué camino se eligió (precarga o prioridad explícita) y el valor de prioridad de red que lo motivó.
    - Ninguno de los archivos que solo pertenecen al plan 18-05 (`package.json`, `next.config.ts`, `src/components/instagram/instagram-reels-section.tsx`) aparece en el diff.
    - Las cinco compuertas salen en 0.
  </acceptance_criteria>
  <done>Las tres declaraciones de `Image` dejaron atrás la prop obsoleta, la imagen del LCP se pide con prioridad alta comprobada en el panel de red, las dos precargas de imagen por página siguen siendo dos, y ninguna imagen cambió de aspecto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: Medición de cierre del LCP contra producción</name>
  <read_first>
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-LCP-EVIDENCE.md — las seis mediciones de la tarea 1 y el camino elegido en la tarea 2, contra los que se compara
  </read_first>
  <what-built>Las tres declaraciones de `Image` del proyecto dejaron atrás la prop `priority`, obsoleta en Next 16, y la del LCP de `/sobre-el-doctor` quedó por el camino A (`preload`) o por el camino B (`loading="eager"` más `fetchPriority="high"`), según la prioridad de red que la tarea 1 midió. `18-LCP-EVIDENCE.md` registra cuál se eligió y qué valor lo motivó.

Este checkpoint existe porque lo que falta no lo puede hacer un agente. Necesita tres cosas que solo hay del lado de Juan: que el cambio esté desplegado en producción, un panel de red donde leer la prioridad de la petición, y un ojo sobre la cara del doctor. Medir el LCP contra el origen local no cierra CWV-03: la imagen pasa por el optimizador de Next y por Cloudflare, y las dos capas están en el camino crítico.</what-built>
  <how-to-verify>
0. **Dos precondiciones, no una.** La primera: el cambio de la tarea 2 está desplegado en producción. Si no lo está, este checkpoint no arranca; medir contra la versión vieja solo produce evidencia de que nada cambió. **La segunda: la Cache Rule del plan 18-02 está aplicada en Cloudflare.** El LCP de esta ruta está dominado por el TTFB —0,96 de puntaje con TBT de 0 ms y LCP de 2,73 s— y el TTFB de la página entra en el camino crítico del LCP. Medir el cierre de CWV-03 antes de la regla de borde mezcla los dos efectos y no permite atribuir la mejora. Si hay que medir igual, se anota con todas las letras que la medición es previa a la regla.
1. Repetir la línea base viva: tres corridas en la misma sesión, mediana, fecha y hora, para que el delta se juzgue contra la línea de la tarea 1 y no contra un número de hace dos días.
2. Repetir contra `https://drangulocolumna.com/sobre-el-doctor` el mismo protocolo de la tarea 1, con el mismo throttling, para que las dos tablas sean comparables renglón por renglón: elemento LCP y su tiempo, presencia del enlace de precarga en el `<head>`, prioridad de red asignada, ancho y peso de la variante descargada a 375px con DPR 2, y `Content-Type` de la respuesta.
3. Leer la prioridad de red en el panel. Es el número que decide si el camino elegido en la tarea 2 fue el correcto: si se tomó el camino A y la prioridad sigue por debajo de High, el trabajo vuelve a la tarea 2 para tomar el camino B.
4. Comprobar la no regresión de CLS: el CLS de `/sobre-el-doctor` y el de la portada tienen que seguir en 0. Las dos rutas tocaron su declaración de `Image` y el espacio reservado de una imagen es exactamente el lugar donde el CLS se rompe. Línea base del 2026-08-24: 0 en las 24 rutas.
5. Comparación visual: capturas de `/sobre-el-doctor` y de la portada a 375px y a 1440px, contra las de antes. Las dos imágenes tienen recortes con `object-position` afinado (`object-[45%_20%]` en fluoroscopia, `object-[60%_30%]` en el hero de la portada) y un cambio ahí se nota en la cara.
6. Ampliar `18-LCP-EVIDENCE.md` con la tabla de después y el veredicto de cierre, contrastado **contra la línea base viva** y no contra los números históricos. Si el LCP no baja, registrar el número real y qué queda por atribuir. Y separar los dos efectos con todas las letras: cuánto del delta es de esta tarea (prioridad de red, formato, variante servida) y cuánto es de la Cache Rule del plan 18-02. Si el LCP mejoró solo por el borde, decirlo: es un resultado legítimo y saberlo evita que la próxima persona atribuya a `preload` una mejora que era de caché.
  </how-to-verify>
  <acceptance_criteria>
    - `18-LCP-EVIDENCE.md` trae las dos tablas enfrentadas, antes y después, sobre la misma ruta y con el mismo protocolo.
    - La Cache Rule del plan 18-02 está aplicada antes de esta medición, o queda anotado que el número es previo a la regla y no es definitivo.
    - **El LCP registrado después es menor que la línea base viva de la tarea 1**, como mediana de tres corridas en la misma sesión. Los 2,64 s del 2026-08-23 y los 2,73 s del 2026-08-24 quedan escritos como históricos y no se usan de umbral: la ruta se movió 0,09 s y subió de 0,91 a 0,96 sin que nadie la tocara, así que ninguno de los dos es una referencia estable.
    - El veredicto separa cuánto del delta es de esta tarea y cuánto de la Cache Rule.
    - El elemento LCP registrado después sigue siendo la imagen del doctor.
    - La prioridad de red registrada para esa petición es High. Si no lo es, el trabajo vuelve a la tarea 2 para cambiar de camino.
    - El `Content-Type` registrado corresponde a un formato moderno de imagen.
    - El CLS registrado para `/sobre-el-doctor` y para la portada es 0 en las dos.
    - El archivo registra el resultado de la comparación de capturas a 375px y a 1440px de las dos rutas.
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" o describe qué número no llegó o qué se ve distinto</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → optimizador de imágenes de Next | La ruta `/_next/image` toma `url`, `w` y `q` de la petición. Los patrones remotos permitidos están acotados en `next.config.ts` y esta fase no los amplía. |
| borde de Cloudflare → respuesta de imagen | Cloudflare puede reescribir o recomprimir la respuesta. Lo que sale del optimizador no es necesariamente lo que llega al navegador. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-12 | Denial of Service | CLS de `/sobre-el-doctor` y de la portada | high | mitigate | Prohibición explícita de tocar `width` y `height`, con aserción `grep` de los dos valores intrínsecos, más la comprobación de CLS = 0 en la tarea 3. |
| T-18-13 | Denial of Service | competencia de precargas | medium | mitigate | Aserción de que las apariciones de `imageSrcSet` en enlaces de precarga siguen siendo 2 por página: precargar la imagen de fluoroscopia le robaría ancho de banda al LCP. |
| T-18-14 | Tampering | recorte y posición de las tres imágenes | medium | mitigate | Aserciones de que `object-cover`, `w-[220px]` y `sm:w-[260px]` siguen presentes, más la comparación de capturas de la tarea 3. Un `object-position` movido se ve en la cara del doctor. |
| T-18-15 | Spoofing | `remotePatterns` de `next.config.ts` | low | accept | Este plan no toca `next.config.ts` y no amplía los orígenes remotos permitidos. Los dos declarados hoy son los CDN de Instagram que alimentan las portadas de los reels. |
| T-18-SC | Tampering | instalaciones npm/pip/cargo | high | accept | Este plan no instala ningún paquete ni toca `package.json`. Sin superficie que vetar. |
</threat_model>

<verification>
1. `grep -rn '^\s*priority\s*$' src/` devuelve cero líneas: la prop obsoleta desapareció de los tres usos.
2. `npm run build` sin advertencias de prop obsoleta sobre `Image`.
3. Lighthouse móvil sobre `https://drangulocolumna.com/sobre-el-doctor`, tres corridas en la misma sesión: LCP mediano por debajo de la línea base viva de la tarea 1, con la imagen del doctor como elemento LCP. Los 2,64 s y los 2,73 s son históricos, no umbral.
4. Panel de red: la petición de la imagen sale con prioridad High y con `Content-Type` de formato moderno.
5. CLS de `/sobre-el-doctor` y de la portada en 0.
6. Capturas de las dos rutas a 375px y 1440px sin diferencia visible.
7. Las cinco compuertas del proyecto en 0 en cada commit.
</verification>

<success_criteria>
- La imagen LCP de `/sobre-el-doctor` se precarga con prioridad alta comprobada y se sirve en formato moderno.
- El LCP baja respecto de la línea base viva medida en la misma sesión, y el veredicto separa lo que aportó esta tarea de lo que aportó la Cache Rule del plan 18-02.
- El proyecto no usa ninguna prop obsoleta del componente `Image`.
- El CLS de las dos rutas tocadas sigue en 0 y ninguna imagen cambió de aspecto.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Quién lo produce | Qué contiene |
|---|---|---|
| `18-LCP-EVIDENCE.md` | este plan, tareas 1 y 3 | Las seis mediciones de lo que producción sirve para el LCP, antes y después; el camino elegido entre precarga y prioridad explícita, con el valor que lo motivó; CLS y comparación de capturas. |
| `18-A11Y-EVIDENCE.md` | plan 18-01 | Evidencia de A11Y-01. |
| `18-CLOUDFLARE-CACHE.md` y `18-CWV01-MEASUREMENT.md` | plan 18-02 | Caché de borde y TTFB, que entra en el camino crítico del LCP. |
| `18-TESTIMONIOS-PROFILE.md` | plan 18-03 | Atribución del costo de `/testimonios`. |
| `18-BUNDLE-ANALYSIS.md` | plan 18-05 | Mapa del bundle antes y después. |

<output>
Create `.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-04-SUMMARY.md` when done
</output>
