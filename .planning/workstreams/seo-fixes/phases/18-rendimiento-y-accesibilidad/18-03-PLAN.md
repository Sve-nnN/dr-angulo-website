---
phase: 18-rendimiento-y-accesibilidad
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-TESTIMONIOS-PROFILE.md
  - src/components/instagram/reels-carousel.tsx
  - src/components/instagram/instagram-reels-section.tsx
  - src/app/layout.tsx
  - src/components/layout/whatsapp-float-button.tsx
  - src/components/cookie-consent-banner.tsx
  - src/components/analytics/analytics-scripts.tsx
autonomous: false
requirements: [CWV-02]
must_haves:
  truths:
    - "`/testimonios` puntúa por encima de 0,90 en performance de Lighthouse móvil, sobre la mediana de al menos tres corridas. Rango de partida medido: 0,60 el 2026-08-23 y 0,69 el 2026-08-24, sin que nadie tocara la página entre las dos."
    - "El TBT de `/testimonios` queda por debajo de 200 ms sobre la mediana de al menos tres corridas. Rango de partida medido: 850 ms el 2026-08-23 y 361 ms el 2026-08-24."
    - "El LCP de `/testimonios` queda nombrado y medido: 4,25 s el 2026-08-24, el peor del sitio. La auditoría del 2026-08-23 no lo marcó porque entonces estaba en 3,47 s."
    - "Los 2,45 s de Style & Layout y los 630 ms de forced reflow tienen un culpable nombrado y atribuido con evidencia, o quedan declarados como no reproducibles con la evidencia de las tres corridas que lo demuestra. Repetir la sospecha sin medirla no cierra el requisito; declarar que el fenómeno desapareció, con números, sí."
    - "El HTML servido de `/testimonios` sale idéntico antes y después: ni un nodo más, ni un nodo menos, ni una clase distinta."
    - "El CLS de `/testimonios` sigue en 0."
  artifacts:
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-TESTIMONIOS-PROFILE.md
  key_links:
    - "El carrusel de reels NO renderiza en producción: `cdninstagram` aparece 0 veces en el HTML servido y la página trae 2 elementos `<img>`. Verificado el 2026-08-24 contra producción y confirmado en el build local. La auditoría midió producción, así que el carrusel no estaba en el marcado cuando se registraron los 2,45 s: queda descartado como culpable por evidencia."
    - "`GoogleReviewsSection` (`src/components/reviews/google-reviews.tsx`) no declara `\"use client\"`, no tiene hooks ni handlers: es un Server Component puro y no emite un byte de JavaScript de cliente. Puede aportar a Style & Layout por volumen de DOM, pero no puede producir forced reflow."
    - statement: "El CLS de `/testimonios` medido con throttling 4x sigue en 0 después del cambio."
      verification: backstop
    - "Una sola corrida de Lighthouse no distingue señal de ruido. Entre el 2026-08-23 y el 2026-08-24, sin ningún cambio en la página, el TBT de `/testimonios` cayó de 850 ms a 361 ms y el LCP subió de 3,47 s a 4,25 s (`audit/baselines/2026-08-24-lighthouse-post-fase16.md`, commit `b9726fc`). Todo número de cierre de este plan es la mediana de al menos tres corridas."
    - "El HTML prerenderizado de `/testimonios` es comparable de forma determinista antes y después con una huella normalizada que descarta los `<script>` y las referencias a chunks. Medida el 2026-08-24 sobre dos builds consecutivos: 21.014 bytes, `md5` `69e0ed17e7e1c5724de8c4e194c871b2`, idéntica entre builds."
---

<objective>
Cerrar CWV-02: establecer con mediciones repetidas qué le cuesta a `/testimonios` su puntaje, arreglarlo, y no gastar el CLS = 0 en el intento.

Purpose: `/testimonios` es la página donde el paciente decide si confía. La auditoría culpó al carrusel de reels; se comprobó contra producción y el carrusel no renderiza. Este plan existe porque perfilar antes de tocar era la decisión correcta: optimizar el carrusel habría consumido la fase entera sin mover el número.

Y existe con una segunda advertencia, que llegó después de que estos planes se escribieran: **los números de partida de `/testimonios` no reproducen.** La corrida del 2026-08-24 contra producción, sin que nadie tocara la página, dio TBT de 361 ms donde la auditoría del 2026-08-23 había dado 850 ms, y LCP de 4,25 s donde había dado 3,47 s. Un criterio de cierre calibrado contra 850 ms se puede dar por cumplido por variación de la medición y no por el arreglo. Por eso todo número de este plan es la mediana de al menos tres corridas, y por eso "el fenómeno no se reproduce" es un resultado válido de la tarea 1 y no un fracaso.

Output: `18-TESTIMONIOS-PROFILE.md` con la atribución y la evidencia, más el arreglo que esa atribución señale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/seo-fixes/ROADMAP.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-CONTEXT.md
@.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Perfilar `/testimonios` y nombrar al culpable</name>
  <files>.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-TESTIMONIOS-PROFILE.md</files>
  <read_first>
    - audit/baselines/2026-08-24-lighthouse-post-fase16.md — la corrida de Unlighthouse contra producción del 2026-08-24, posterior a la auditoría que originó esta fase. Es la evidencia de que los números de `/testimonios` no reproducen, y trae la tabla completa de las 24 rutas para comparar contra el resto del sitio.
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-CONTEXT.md, el Addendum del 2026-08-24 al final del archivo: es la evidencia que descarta al carrusel
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md, sección `## Contrato del montaje diferido`, subsección "Lectura del código, previa al perfilado"
    - src/app/testimonios/page.tsx
    - src/components/instagram/instagram-reels-section.tsx — el `import` estático de `ReelsCarousel` en la línea 5, dentro de un Server Component
    - src/components/reviews/google-reviews.tsx
    - src/app/layout.tsx — los cuatro componentes de cliente del armazón compartido
    - ~/.claude/skills/impeccable/reference/optimize.md
  </read_first>
  <action>
Arranque de la skill, una vez por sesión, con cwd en la raíz del repo: `node ~/.claude/skills/impeccable/scripts/context.mjs --target src/app/testimonios/page.tsx`. La skill es global, en `~/.claude/skills/impeccable/`, y no hay copia local. El verbo es `optimize`.

Perfilar contra producción (`https://drangulocolumna.com/testimonios`) con devtools y CPU throttling 4x, que es la condición en la que la auditoría tomó sus números. No perfilar solo en local: el bloque de reseñas de Google responde 403 en el build local (visible en la salida de `npm run build`) y por eso no renderiza acá, mientras que en producción sí renderiza. Un perfilado local mide una página distinta de la que la auditoría midió.

**Antes que nada, establecer la línea base de verdad, porque la que traía este plan no reproduce.** La corrida del 2026-08-24 contra producción (`audit/baselines/2026-08-24-lighthouse-post-fase16.md`, commit `b9726fc`) midió, sin que nadie tocara la página entre una medición y la otra:

| Métrica | 2026-08-23 | 2026-08-24 |
|---|---|---|
| Performance | 0,60 | 0,69 |
| TBT | 850 ms | 361 ms |
| LCP | 3,47 s | 4,25 s |

De ahí salen tres reglas para esta tarea:

- **Toda cifra va como mediana de al menos tres corridas**, y las tres corridas individuales quedan escritas en el archivo junto a la mediana. Una sola corrida no distingue señal de ruido, y con un TBT que se movió de 850 a 361 ms por su cuenta, la diferencia entre "el arreglo funcionó" y "tuve suerte" es exactamente esa mediana.
- **"No se reproduce" es un resultado sancionado.** Puede que los 2,45 s de Style & Layout y los 630 ms de forced reflow no estén presentes hoy. Si las tres corridas lo demuestran, el archivo lo declara con los números en la mano, la tarea 2 no busca culpable para un fenómeno que no existe, y CWV-02 se cierra por esa vía. Lo prohibido es lo contrario: inventar un culpable para justificar un arreglo, o arreglar algo para perseguir un número que ya se movió solo.
- **El LCP de 4,25 s entra en el alcance de este plan.** No estaba en la auditoría del 2026-08-23 porque entonces medía 3,47 s, pero hoy es el peor LCP del sitio y el criterio de cierre de CWV-02 es un puntaje de performance por encima de 0,90: con un LCP de 4,25 s ese puntaje es aritméticamente inalcanzable, así que el LCP no es un hallazgo aparte sino la restricción que decide el requisito. Medirlo como cuarta cifra: qué elemento es el LCP de `/testimonios`, su tiempo mediano sobre tres corridas, y qué lo retrasa. La decisión de alcance está escrita abajo, en la tarea 2.

Empezar por confirmar el estado del marcado servido, que es lo primero que cualquier atribución necesita: contar apariciones de `cdninstagram` y de `<img` en el HTML de producción, y listar los `<h2>`. Lo esperado, ya verificado el 2026-08-24: 0 apariciones de `cdninstagram`, 2 elementos `<img>`, y los H2 "Reseñas en Google", "Otras reseñas", "Videos del consultorio", "Consultorio privado" y "Contacto rápido". Si el conteo difiere, el carrusel volvió a renderizar porque alguien vinculó la cuenta de Instagram, y todo el análisis se rehace desde cero antes de tocar nada.

Con eso confirmado, hay tres candidatos y ninguno es el carrusel renderizado. Recorrerlos en este orden, tomando evidencia de cada uno antes de pasar al siguiente:

1. **JavaScript muerto en la ruta.** `instagram-reels-section.tsx` importa `ReelsCarousel` de forma estática desde un Server Component. Medición tomada del build de hoy: el chunk que contiene el código del carrusel pesa 29.656 bytes y se carga en 11 de los 12 documentos prerenderizados, incluido `/privacidad`, que nunca puede renderizar un carrusel. Ese código se descarga, se analiza y se compila en cada carga de cada ruta. Confirmar en producción con el panel de Coverage de devtools qué porcentaje de cada chunk se ejecuta, y anotar el número.

2. **El armazón compartido.** `src/app/layout.tsx` monta cuatro componentes de cliente en todas las rutas: `Header`, `WhatsAppFloatButton`, `CookieConsentBanner` y `AnalyticsScripts`. Un dato que acota la búsqueda antes de perfilar: `AnalyticsScripts` devuelve `null` mientras el consentimiento no esté en `granted`, y Lighthouse corre con perfil limpio, así que ni GA4 ni el pixel de Meta se montan durante la medición y no pueden explicar los 850 ms de TBT que la auditoría reportó. Si el TBT aparece igual en otras rutas del sitio, el culpable es el armazón y no `/testimonios`: medir el TBT de al menos otras dos rutas para poder afirmarlo o descartarlo.

3. **Volumen de DOM y costo de estilo del bloque de reseñas.** `GoogleReviewsSection` es un Server Component puro: no emite JavaScript de cliente y por lo tanto no puede producir forced reflow, porque un reflow forzado exige que haya JavaScript leyendo geometría de layout de forma síncrona. Sí puede aportar a los 2,45 s de Style & Layout por volumen de nodos y por costo de recálculo de estilo. Medirlo, no suponerlo.

Escribir `18-TESTIMONIOS-PROFILE.md` con: el estado del marcado servido confirmado; las tres corridas individuales y su mediana para performance, TBT y LCP; la tabla de chunks con su peso y su porcentaje de cobertura ejecutada; el TBT comparado entre `/testimonios` y al menos otras dos rutas; el elemento LCP nombrado con lo que lo retrasa; y la atribución final de Style & Layout y forced reflow, cada uno con el marco de llamada o el recurso que lo produce.

La atribución tiene que terminar en uno de estos tres estados, y decir cuál con todas las letras:

1. **Culpable nombrado**, con el marco de llamada o el recurso que produce cada número.
2. **No reproduce**, con las tres corridas que lo demuestran y la comparación contra las cifras del 2026-08-23 y del 2026-08-24.
3. **No encontrado**, con lo que falta para encontrarlo.

Lo que no cierra esta tarea es un archivo que repita la sospecha de la auditoría sin medirla: eso es exactamente lo que este plan existe para no repetir.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-TESTIMONIOS-PROFILE.md &amp;&amp; npm run content:check &amp;&amp; npm run seo:check &amp;&amp; npm run sedes:check &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `18-TESTIMONIOS-PROFILE.md` existe y registra el conteo de `cdninstagram` y de `<img` en el HTML de producción de `/testimonios`, con la fecha de la medición.
    - Ese conteo es 0 y 2 respectivamente, o el archivo explica qué cambió y rehace la atribución desde cero.
    - El archivo trae una tabla de chunks de JavaScript cargados por la ruta, con peso en bytes y porcentaje de código ejecutado según Coverage.
    - El archivo registra tres corridas individuales y su mediana para performance, TBT y LCP. Un número suelto sin sus tres corridas no cuenta como medición en este plan.
    - El archivo enfrenta esas medianas contra las dos líneas base conocidas: 0,60 / 850 ms / 3,47 s del 2026-08-23, y 0,69 / 361 ms / 4,25 s del 2026-08-24.
    - El archivo compara el TBT de `/testimonios` con el de al menos otras dos rutas del sitio, bajo el mismo throttling 4x, y de esa comparación sale un veredicto escrito sobre si el costo es de la ruta o del armazón compartido.
    - El archivo nombra el elemento LCP de `/testimonios`, su tiempo mediano y qué lo retrasa.
    - El archivo termina en uno de los tres estados declarados: culpable nombrado, no reproduce, o no encontrado con lo que falta. El estado queda escrito con esas palabras, no inferido de la prosa.
    - El archivo NO atribuye nada al carrusel de reels renderizado: esa hipótesis está descartada por evidencia desde el 2026-08-24.
    - `git diff --name-only` de esta tarea no incluye ningún archivo bajo `src/`: perfilar no es tocar.
    - Las cinco compuertas salen en 0. Línea base verificada hoy: las cinco en 0.
  </acceptance_criteria>
  <done>Hay un culpable nombrado con evidencia medida bajo throttling 4x contra producción, o la declaración de que el fenómeno no reproduce, sostenida por las tres corridas. En los dos casos el archivo sostiene su conclusión con números, no con sospechas, y el LCP de la ruta queda nombrado y medido.</done>
</task>

<task type="auto">
  <name>Tarea 2: Aplicar el arreglo que el perfilado señaló</name>
  <precondition>`18-TESTIMONIOS-PROFILE.md` existe y nombra un culpable atribuido con evidencia. Si el perfilado no encontró culpable, esta tarea no arranca: se vuelve a la tarea 1 con el método que el propio archivo dejó anotado.</precondition>
  <files>src/components/instagram/reels-carousel.tsx, src/components/instagram/instagram-reels-section.tsx, src/app/layout.tsx, src/components/layout/whatsapp-float-button.tsx, src/components/cookie-consent-banner.tsx, src/components/analytics/analytics-scripts.tsx</files>
  <read_first>
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-TESTIMONIOS-PROFILE.md — la atribución que esta tarea implementa
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md, sección `## Contrato del montaje diferido`: la forma del arreglo y las tres prohibiciones
    - src/components/instagram/reels-carousel.tsx — `updateEdges` en la línea 20, el `ResizeObserver` en las líneas 28-35, el `onScroll` de la línea 72 y `scrollByCards` en la línea 37 (verificado sobre el archivo el 2026-08-24; el UI-SPEC decía 41 y se corrigió)
    - src/app/layout.tsx y los tres componentes de cliente del armazón que esta tarea puede tocar: `src/components/layout/whatsapp-float-button.tsx`, `src/components/cookie-consent-banner.tsx`, `src/components/analytics/analytics-scripts.tsx`
    - node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md — la guía de la versión instalada, no la de tu memoria
  </read_first>
  <action>
Implementar lo que la atribución del perfilado señaló. El contrato de abajo aplica sea quien sea el culpable.

**Regla dura, sin excepción: el HTML servido de `/testimonios` sale idéntico antes y después.** Ni un nodo más, ni un nodo menos, ni una clase distinta. Con esa regla el problema del placeholder desaparece: si el marcado servido no cambia, el navegador pinta todo en el primer frame y no existe ningún momento en que algo ocupe el espacio de otra cosa. El CLS = 0 deja de ser algo que hay que cuidar y pasa a ser estructuralmente imposible de romper.

**Primer paso de esta tarea, antes de editar una línea: capturar la huella del marcado actual.** El `<verify>` de abajo la compara, así que sin ella la tarea no puede terminar. Con el build actual en `.next/`, correr:

`perl -0pe 's{<script\b.*?</script>}{}gs; s{<link[^>]*static/chunks[^>]*>}{}gs; s{static/chunks/[A-Za-z0-9_.\-]+\.js}{CHUNK}gs' .next/server/app/testimonios.html > /tmp/testimonios-antes.norm`

Qué hace y por qué así: quita los `<script>` y los `<link>` que apuntan a chunks, y normaliza cualquier nombre de chunk que quede suelto en el árbol RSC serializado. Eso deja solo el marcado, que es lo que la regla dura protege, y descarta justo lo que esta tarea sí tiene permitido cambiar, que es qué JavaScript carga la ruta. Medido el 2026-08-24 sobre dos builds consecutivos: 21.014 bytes y `md5` `69e0ed17e7e1c5724de8c4e194c871b2`, idéntico entre builds, así que la comparación es determinista y no un "parece igual". Si tu huella de partida no da ese `md5`, no es un error: significa que el bloque de reseñas de Google respondió distinto en tu build (en local suele dar 403) o que 18-01 ya está mergeado. Anota la tuya y comparala contra sí misma; lo que se asserta es la igualdad antes/después, no ese hash concreto.

Lo que se difiere es trabajo de cliente, nunca marcado.

**Si el culpable es el JavaScript muerto de la ruta:** el objetivo es que el código del carrusel deje de descargarse y compilarse en rutas que no pueden renderizarlo, sin cambiar un solo nodo del HTML servido. Este trabajo se coordina con el plan 18-05, que es dueño del análisis del bundle: si el arreglo pasa por reorganizar el punto de importación de `ReelsCarousel`, hacerlo acá y anotarlo en el SUMMARY para que 18-05 mida sobre el resultado y no sobre la línea base vieja.

**Si el culpable es el trabajo de cliente del carrusel** (posible solo si la cuenta de Instagram se vinculó entre medio y el carrusel volvió a renderizar): el `ResizeObserver` y la primera corrida de `updateEdges()` se montan cuando la sección entra en viewport, con `IntersectionObserver`, en vez de en la hidratación; y el handler de `onScroll` agrupa sus lecturas en `requestAnimationFrame` descartando las corridas pendientes, para que un gesto de scroll produzca una lectura de layout por frame y no una por evento. El estado inicial de los botones se mantiene en `atStart` verdadero y `atEnd` falso, que es exactamente el estado que ya tienen hoy antes del primer efecto: sin cambio visual en el primer frame.

**Si el culpable es el bloque de reseñas:** el arreglo no es diferir el montaje, porque es un Server Component y no hay montaje de cliente que diferir. Sería reducir el volumen de DOM o el costo de estilo, con el mismo contrato de HTML idéntico.

**Si el culpable es el armazón compartido**, que es el segundo candidato que la tarea 1 mide y que hasta ahora este plan no tenía cómo implementar: el alcance de archivos de esta tarea lo cubre. `src/app/layout.tsx`, `src/components/layout/whatsapp-float-button.tsx`, `src/components/cookie-consent-banner.tsx` y `src/components/analytics/analytics-scripts.tsx` están en `files_modified` y se pueden tocar acá, con el mismo contrato de HTML servido idéntico: lo que se difiere es trabajo de cliente, nunca marcado, y en las 23 rutas, no solo en `/testimonios`.

Con una excepción de propiedad que no se negocia: **`src/components/layout/header.tsx` es del plan 18-04** y no se toca desde acá. Dos planes no pueden ser dueños del mismo archivo. Si la atribución cae sobre el `Header` o sobre `services-menu.tsx`, esta tarea se detiene, lo anota en `18-TESTIMONIOS-PROFILE.md` y lo sube al checkpoint de la tarea 3 para que Juan decida entre ampliar 18-04 o abrir un plan nuevo. Es una decisión de alcance, no un problema técnico, y la toma él.

**Si la tarea 1 concluyó que el fenómeno no reproduce:** esta tarea no busca a quién arreglar. No se toca nada por tocar. Se pasa directo al checkpoint de la tarea 3, que mide las tres corridas de cierre y decide si CWV-02 se cierra con la evidencia de que Style & Layout y forced reflow ya no están presentes. Un cambio de código sin un número que lo justifique es riesgo puro sobre una página que hoy tiene el CLS en 0.

**El LCP de 4,25 s: decisión de alcance, tomada acá y no dejada sin nombre.** Es de la fase 18 y de este plan, no un hallazgo suelto para la fase 19, por una razón aritmética: el criterio de cierre de CWV-02 es un puntaje de performance por encima de 0,90, y con el peor LCP del sitio ese puntaje no se alcanza aunque el TBT baje a cero. Tratarlo como hallazgo de la fase 19 sería cerrar CWV-02 con un número que no se cumplió.

Ahora bien, el arreglo tiene que caber en el contrato de esta fase, y eso acota lo que se puede hacer:

- Si el elemento LCP resulta ser una imagen del bloque de reseñas o de la sección de reels, y el arreglo es de carga (precarga, prioridad de red, `sizes`, formato), se hace acá, con el mismo criterio que el plan 18-04 aplica en `/sobre-el-doctor` y citando la misma doc: `node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md`, sección `#### preload`.
- Si el elemento LCP resulta ser un bloque de texto y el arreglo es de fuentes o de TTFB, se anota que depende del plan 18-02 y se mide después de que la Cache Rule esté aplicada, sin duplicar acá el trabajo de caché.
- Si el arreglo exige un cambio visual, un archivo fuera de `files_modified`, o tocar `header.tsx`, se detiene y sube al checkpoint de la tarea 3 con la propuesta escrita. Lo que no se hace es dejar el LCP sin nombrar.

Tres prohibiciones, cada una con su motivo:

1. **`dynamic(..., { ssr: false })` está prohibido.** Borra el HTML del servidor y obliga a un placeholder, que es el único camino por el que el CLS puede dejar de ser 0. Y además `InstagramReelsSection` es un Server Component `async`: la guía de lazy loading de la versión instalada de Next dice que `ssr: false` no está permitido con `next/dynamic` en Server Components. Habría que introducir un envoltorio de cliente solo para poder degradar el resultado.
2. **`content-visibility: auto` sin `contain-intrinsic-size` está prohibido.** Es la vía rápida para bajar Style & Layout y también la vía rápida para introducir CLS. Si se usa, hay que declarar el `contain-intrinsic-size` exacto por breakpoint, y ese tamaño tiene que coincidir con las alturas de la tabla del UI-SPEC.
3. **Ningún cambio visual.** El trabajo de rendimiento no tiene presupuesto visual en esta fase: los tres únicos cambios de interfaz sancionados están en el plan 18-01. Si el arreglo necesita cambiar el aspecto de algo, el arreglo está mal planteado.

**Parada obligatoria:** si el arreglo que el perfilado exige requiere `ssr: false`, o un placeholder, o el cuarto cambio visual que arrastra (fijar el título del reel a dos líneas exactas para volver determinista el alto de la tarjeta), la tarea se detiene y se sube la decisión a Juan antes de escribir una línea. El UI-SPEC deja las dimensiones del placeholder calculadas por si ese camino se aprueba: 601px de alto reservado bajo 640px y 637px desde 640px. Ese camino no está aprobado hoy.
  </action>
  <verify>
    <automated>test -f /tmp/testimonios-antes.norm &amp;&amp; npm run build &amp;&amp; perl -0pe 's{&lt;script\b.*?&lt;/script>}{}gs; s{&lt;link[^>]*static/chunks[^>]*>}{}gs; s{static/chunks/[A-Za-z0-9_.\-]+\.js}{CHUNK}gs' .next/server/app/testimonios.html > /tmp/testimonios-despues.norm &amp;&amp; diff -q /tmp/testimonios-antes.norm /tmp/testimonios-despues.norm &amp;&amp; test "$(grep -o '&lt;img' .next/server/app/testimonios.html | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(grep -o 'cdninstagram' .next/server/app/testimonios.html | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(grep -o '&lt;h2' .next/server/app/testimonios.html | wc -l | tr -d ' ')" = "4" &amp;&amp; test "$(grep -c 'ssr: false' src/components/instagram/instagram-reels-section.tsx src/components/instagram/reels-carousel.tsx | grep -v ':0' | wc -l | tr -d ' ')" = "0" &amp;&amp; npm run content:check &amp;&amp; npm run seo:check &amp;&amp; npm run sedes:check &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - En el HTML prerenderizado de `/testimonios`: `<img` = 2, `cdninstagram` = 0, `<h2` = 4. Línea base medida hoy en el build local: 2, 0 y 4. Cualquier desviación significa que el marcado servido cambió, y eso viola la regla dura del contrato.
    - `diff -q /tmp/testimonios-antes.norm /tmp/testimonios-despues.norm` sale en 0: el marcado servido es idéntico byte a byte una vez descartados los `<script>` y las referencias a chunks. Esta aserción está en el `<verify>` y no queda librada al criterio de quien ejecuta. Huella medida el 2026-08-24: 21.014 bytes, `md5` `69e0ed17e7e1c5724de8c4e194c871b2`, estable entre dos builds consecutivos.
    - Si el `diff` falla, la salida se lee antes de tocar nada más: la única causa legítima es que 18-01 haya entrado al build entre las dos capturas, y `/testimonios` no está entre los archivos de 18-01, así que ni siquiera esa aplica.
    - `grep -rn 'ssr: false' src/` devuelve 0 líneas. Línea base medida hoy: 0.
    - Si el diff toca `src/components/instagram/reels-carousel.tsx`, el archivo sigue inicializando su estado con `atStart` en verdadero y `atEnd` en falso, sin cambio en el primer frame.
    - Si se usó `content-visibility`, en el mismo archivo y en la misma declaración aparece un `contain-intrinsic-size` explícito.
    - Ningún archivo del plan 18-01 (`src/app/page.tsx`, `src/components/locations/location-card.tsx`, `src/components/locations/sede-card.tsx`) aparece en el diff.
    - Ningún archivo del plan 18-04 aparece en el diff, `src/components/layout/header.tsx` incluido. Si la atribución cayó sobre el `Header`, el diff está vacío en ese punto y la decisión de alcance quedó anotada para el checkpoint.
    - Si el diff toca `src/app/layout.tsx` o alguno de los tres componentes de cliente del armazón, la huella normalizada de `/testimonios` sigue siendo idéntica y además se comprueba lo mismo sobre una segunda ruta, `/privacidad`, que es la más simple del sitio: el armazón se sirve en las 23 rutas y un cambio ahí no puede alterar el marcado de ninguna.
    - Las cinco compuertas salen en 0.
  </acceptance_criteria>
  <done>El arreglo que el perfilado señaló está aplicado, el HTML servido de `/testimonios` no cambió en un solo nodo, y ninguna de las tres prohibiciones del contrato se violó.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3: Verificación de cierre de `/testimonios`</name>
  <read_first>
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-TESTIMONIOS-PROFILE.md
    - .planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-UI-SPEC.md, sección "Lo que hay que proteger"
  </read_first>
  <what-built>`18-TESTIMONIOS-PROFILE.md` cierra la atribución en uno de tres estados declarados: culpable nombrado con evidencia, fenómeno que no reproduce con las tres corridas que lo demuestran, o no encontrado con lo que falta. Si hubo culpable, el arreglo está aplicado bajo la regla de HTML servido idéntico, comprobada con una huella normalizada que el `<verify>` compara byte a byte.

Lo que este checkpoint tiene que resolver y ningún agente puede: las tres corridas de Lighthouse contra producción, el veredicto sobre el LCP de 4,25 s, y cualquier decisión de alcance que la tarea 2 haya subido.</what-built>
  <how-to-verify>
1. Abrir `https://drangulocolumna.com/testimonios` con devtools, throttling móvil y CPU 4x, y correr Lighthouse **tres veces**. Anotar las tres corridas y quedarse con la mediana. Una sola corrida no sirve acá: entre el 2026-08-23 y el 2026-08-24, sin que nadie tocara la página, el TBT se movió de 850 ms a 361 ms por su cuenta. Contra ese ruido, un número suelto no distingue el arreglo de la suerte.
2. Contrastar la mediana contra las dos líneas base, no contra una: 0,60 / 850 ms / 3,47 s el 2026-08-23, y 0,69 / 361 ms / 4,25 s el 2026-08-24. El objetivo sigue siendo performance por encima de 0,90 y TBT por debajo de 200 ms.
3. Mirar el LCP, que es la cifra que hoy decide el puntaje: 4,25 s el 2026-08-24, el peor del sitio. Si sigue por encima de 2,5 s, el puntaje no va a llegar a 0,90 aunque el TBT esté en cero, y lo que corresponde es leer qué dice `18-TESTIMONIOS-PROFILE.md` sobre el elemento LCP y decidir: se arregla acá dentro del alcance de archivos del plan, o se abre como plan nuevo. Lo que no se hace es cerrar CWV-02 con un puntaje que no llegó.
4. En el mismo informe, mirar el CLS. Tiene que seguir en 0. Un cambio que baje el TBT a 150 ms pero suba el CLS de 0 a 0,05 es un retroceso, no una mejora, y se rechaza. Línea base del 2026-08-24: 0 en las 24 rutas.
5. Recorrer la página a ojo, en móvil y en escritorio: el bloque de reseñas de Google, la lista de "Otras reseñas", el estado vacío de "Videos del consultorio" con el enlace al perfil, y los dos enlaces de Instagram y Doctoralia. Nada puede verse distinto de como se veía antes.
6. Si la tarea 2 tocó el armazón compartido, repetir el punto 5 en `/privacidad` y en la portada: un cambio en `layout.tsx` se sirve en las 23 rutas.
7. Confirmar que la conclusión escrita en `18-TESTIMONIOS-PROFILE.md` te resulta convincente, sea "culpable nombrado" o "no reproduce". Es el archivo que evita que la próxima persona vuelva a sospechar del carrusel.
8. Si la tarea 2 dejó anotada una decisión de alcance (atribución que cae sobre `header.tsx`, o un arreglo de LCP que se sale del contrato de la fase), resolverla acá: ampliar 18-04, abrir un plan nuevo, o dejarlo como hallazgo con nombre para la fase 19.
  </how-to-verify>
  <acceptance_criteria>
    - El puntaje de performance de Lighthouse móvil sobre `/testimonios`, como mediana de tres corridas, es mayor a 0,90.
    - El TBT, como mediana de tres corridas, es menor a 200 ms.
    - El LCP quedó nombrado y con decisión escrita: arreglado acá, o abierto como trabajo con dueño y plan.
    - El CLS es 0.
    - Ninguna superficie visible de la página cambió de aspecto, ni en `/testimonios` ni en las otras rutas si se tocó el armazón.
    - Toda decisión de alcance que la tarea 2 haya subido queda resuelta acá, no pendiente.
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" o describe qué se ve distinto o qué número no llegó</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API de Instagram → build | El feed de reels entra en tiempo de build y hoy cae en el estado de fallback. Si la cuenta se vincula, el marcado de la ruta cambia y la atribución de este plan deja de valer. |
| API de Google Places → build | El bloque de reseñas se resuelve en el servidor. Responde 403 en el entorno local, así que el build local no reproduce el marcado de producción. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Denial of Service | CLS de `/testimonios` | high | mitigate | Regla de HTML servido idéntico más la aserción de conteos (`<img` 2, `cdninstagram` 0, `<h2` 4) en el `<verify>` de la tarea 2, y la comprobación de CLS = 0 en el checkpoint de la tarea 3. |
| T-18-09 | Repudiation | atribución del perfilado | high | mitigate | La tarea 1 exige que el archivo nombre el marco de llamada o el recurso que produce cada número, y prohíbe explícitamente repetir la hipótesis descartada del carrusel. Una atribución sin evidencia deja el requisito abierto disfrazado de cerrado. |
| T-18-10 | Tampering | `reels-carousel.tsx`, estado inicial de los botones | medium | mitigate | Aserción de que el estado inicial se conserva: cambiarlo altera el primer frame renderizado y con él la posibilidad de introducir un salto de layout. |
| T-18-11 | Information Disclosure | perfilado local en vez de producción | medium | mitigate | La tarea 1 obliga a perfilar contra producción y explica por qué: el 403 de Places hace que el build local rinda una página distinta de la que la auditoría midió. |
| T-18-21 | Repudiation | variación de la medición de Lighthouse | high | mitigate | Entre el 2026-08-23 y el 2026-08-24 el TBT de `/testimonios` cayó de 850 ms a 361 ms sin ningún cambio en la página. Un criterio de cierre contra un solo número se satisface por ruido. Mitigación: mediana de al menos tres corridas en la tarea 1 y en el checkpoint de la tarea 3, con las corridas individuales escritas, y las dos líneas base declaradas como rango. |
| T-18-22 | Tampering | armazón compartido servido en las 23 rutas | high | mitigate | La cuarta rama de la tarea 2 puede tocar `layout.tsx` y tres componentes de cliente. Mitigación: misma regla de HTML idéntico, comprobada con la huella normalizada sobre `/testimonios` y sobre `/privacidad`, más el recorrido manual del checkpoint sobre una segunda ruta. `header.tsx` queda fuera por pertenecer al plan 18-04. |
| T-18-SC | Tampering | instalaciones npm/pip/cargo | high | accept | Este plan no instala ningún paquete. El análisis de bundle, que sí instala una herramienta, vive en el plan 18-05 con su propia compuerta de legitimidad. |
</threat_model>

<verification>
1. Lighthouse móvil sobre `https://drangulocolumna.com/testimonios` con throttling 4x, tres corridas: la mediana de performance es mayor a 0,90 y la mediana de TBT es menor a 200 ms. Las tres corridas quedan escritas junto a la mediana.
2. LCP de `/testimonios` nombrado y con decisión escrita. Línea base del 2026-08-24: 4,25 s, el peor del sitio.
3. CLS de `/testimonios` en 0, medido bajo el mismo throttling. Sin esta evidencia explícita el verificador no da pase.
4. `diff -q /tmp/testimonios-antes.norm /tmp/testimonios-despues.norm` en 0: el marcado servido es idéntico byte a byte descartando `<script>` y referencias a chunks.
5. `18-TESTIMONIOS-PROFILE.md` cierra en uno de los tres estados declarados, no en una sospecha.
6. Las cinco compuertas del proyecto en 0 en cada commit.
</verification>

<success_criteria>
- `/testimonios` puntúa por encima de 0,90 con un TBT por debajo de 200 ms, sobre la mediana de tres corridas y no sobre una.
- El LCP de 4,25 s tiene dueño, decisión y número escrito.
- El CLS sigue en 0.
- El HTML servido no cambió en un solo nodo, comprobado con una huella determinista.
- La conclusión queda escrita con evidencia, sea un culpable nombrado o la demostración de que el fenómeno no reproduce, de forma que nadie vuelva a sospechar del carrusel.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Quién lo produce | Qué contiene |
|---|---|---|
| `18-TESTIMONIOS-PROFILE.md` | este plan, tarea 1 | Estado del marcado servido, tabla de chunks con cobertura ejecutada, TBT comparado entre rutas y atribución final de los tres números de la auditoría. |
| `18-A11Y-EVIDENCE.md` | plan 18-01 | Evidencia de A11Y-01. |
| `18-CLOUDFLARE-CACHE.md` y `18-CWV01-MEASUREMENT.md` | plan 18-02 | Caché de borde y TTFB. |
| `18-LCP-EVIDENCE.md` | plan 18-04 | Evidencia del LCP de `/sobre-el-doctor`. |
| `18-BUNDLE-ANALYSIS.md` | plan 18-05 | Mapa del bundle antes y después. Consume el SUMMARY de este plan si acá se reorganizó el punto de importación. |

<output>
Create `.planning/workstreams/seo-fixes/phases/18-rendimiento-y-accesibilidad/18-03-SUMMARY.md` when done
</output>
