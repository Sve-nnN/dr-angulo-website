---
phase: 01-fundaci-n-t-cnica-y-de-marca
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, next.config.ts, src/app/globals.css, .env.example]
autonomous: true
requirements: [INFRA-01, INFRA-04, BRAND-01, BRAND-02]
user_setup: []

must_haves:
  truths:
    - "npm run dev levanta el sitio sin errores"
    - "npm run build y npx tsc --noEmit corren limpios"
    - "Paleta teal (#0E7C7E) + dorado-mostaza (#E8971F) disponible como tokens de Tailwind"
    - "Tipografía Poppins (headings) + Inter (body) self-hosted vía next/font"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens de color/tipografía vía @theme inline (Tailwind v4)"
    - path: ".env.example"
      provides: "Variables documentadas: WhatsApp, GA4, Meta Pixel, Resend"
    - path: "package.json"
      provides: "Next.js 16 + React 19 + Tailwind v4 + deps de marca (lucide-react, clsx, tailwind-merge)"
  key_links: []
---

<objective>
Scaffold del proyecto Next.js 16 (App Router) + TypeScript + Tailwind CSS v4, con la paleta e identidad tipográfica real del doctor (extraída de `.planning/research/BRAND.md`) aplicada como design tokens desde el día uno — no una plantilla genérica.

Purpose: base técnica sobre la que se construyen las páginas de contenido (Phase 2) y todo lo demás.
Output: proyecto Next.js corriendo, tokens de marca en `globals.css`, `.env.example` documentado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/BRAND.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + Tailwind v4 + estructura de carpetas</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/globals.css</files>
  <action>`create-next-app` con App Router + TypeScript + Tailwind v4 (`@tailwindcss/postcss`). Estructura `src/app`, `src/components`, `src/lib`, `src/content`. Sin CMS (pedido explícito del cliente).</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - "package.json contiene next@16, react@19, tailwindcss@4"
    - "npm run build termina sin errores"
  </acceptance_criteria>
  <done>Build de producción limpio</done>
</task>

<task type="auto">
  <name>Task 2: Design tokens de marca real (color + tipografía)</name>
  <files>src/app/globals.css</files>
  <read_first>.planning/research/BRAND.md</read_first>
  <action>Tokens vía `@theme inline`: paleta teal (#0E7C7E) + dorado-mostaza (#E8971F) extraída del Instagram real del doctor, verde WhatsApp (#25D366) reservado solo para el botón flotante. Fuentes Poppins (headings) + Inter (body) self-hosted con `next/font/google`. Sin modo oscuro automático (decisión deliberada de marca única).</action>
  <verify>Inspección visual — colores/tipografía coinciden con capturas reales de Instagram</verify>
  <acceptance_criteria>
    - "globals.css define variables de color teal y dorado"
    - "layout.tsx importa Poppins e Inter de next/font/google"
  </acceptance_criteria>
  <done>Tokens aplicados y visibles en navegador</done>
</task>

<task type="auto">
  <name>Task 3: .env.example documentado</name>
  <files>.env.example</files>
  <action>Documentar todas las variables que fases posteriores necesitarán: número de WhatsApp, GA4 measurement ID, Meta Pixel ID, Resend API key + email from, site URL.</action>
  <verify>cat .env.example</verify>
  <acceptance_criteria>
    - "Archivo presente con WHATSAPP/GA/META_PIXEL/RESEND documentados"
  </acceptance_criteria>
  <done>.env.example completo</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npm run build limpio
- [x] npx tsc --noEmit limpio
- [x] Colores/tipografía verificados visualmente contra Instagram real del doctor
</verification>

<success_criteria>
- Scaffold corriendo, tokens de marca reales aplicados, .env.example documentado
- Sin errores ni warnings de build
</success_criteria>

---
**Estado:** Completado — ver `01-SUMMARY.md` y commit `88b397d`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
