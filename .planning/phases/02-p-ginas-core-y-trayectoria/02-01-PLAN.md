---
phase: 02-p-ginas-core-y-trayectoria
plan: 02-01
type: execute
wave: 2
depends_on: ["02-02"]
files_modified: [src/app/page.tsx]
autonomous: true
requirements: [CONTENT-01]
user_setup: []

must_haves:
  truths:
    - "La Home comunica las 3 especialidades (traumatología, ortopedia infantil, cirugía de columna) en la sección hero, visible sin hacer scroll"
    - "La Home tiene un CTA de WhatsApp visible sin hacer scroll, dentro de la sección hero"
    - "La Home incluye preview de testimonios reales y preview de FAQ, cada uno con link a su página completa"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home: hero (CTA WhatsApp + 3 especialidades) + sección de especialidades con link a /servicios#slug + sección 'por qué elegirme' + preview de testimonios + preview de FAQ + CTA final"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/content/services.ts"
      via: "import { serviceCategories } — tarjetas de especialidad con link a /servicios#slug"
      pattern: "serviceCategories"
    - from: "src/app/page.tsx"
      to: "src/content/testimonials.ts"
      via: "import { testimonials, reviewLinks } — preview de testimonios + link a Doctoralia"
      pattern: "testimonials"
    - from: "src/app/page.tsx"
      to: "src/content/faq.ts"
      via: "import { faqItems } — preview de las primeras 3 preguntas"
      pattern: "faqItems\\.slice"
    - from: "src/app/page.tsx"
      to: "src/components/ui/whatsapp-cta.tsx"
      via: "<WhatsAppCta location=\"hero\" /> en hero y en CTA final (componente ya existente desde Fase 1)"
      pattern: "WhatsAppCta location=\"hero\""
---

<objective>
Home del sitio: hero con propuesta de valor y CTA de WhatsApp visible sin scroll, las 3 especialidades del doctor, sección de credibilidad, y previews de testimonios/FAQ que enlazan a sus páginas completas.

Purpose: primera impresión del sitio — debe comunicar en segundos qué hace el doctor y dar una vía de contacto inmediata, sin depender de que el visitante haga scroll.
Output: `src/app/page.tsx` completo con copy real (sin lorem ipsum).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/content/services.ts
@src/content/testimonials.ts
@src/content/faq.ts
@src/lib/site-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero con especialidades y CTA de WhatsApp sin scroll</name>
  <files>src/app/page.tsx</files>
  <read_first>src/lib/site-config.ts, src/components/ui/whatsapp-cta.tsx</read_first>
  <action>Sección hero (`bg-primary`, primer elemento de la página) con: badge de colegiatura CMP + sede, headline ("Recupera tu movilidad sin miedo a la columna"), subheadline con propuesta de valor, `WhatsAppCta location="hero"` + link secundario a `/servicios`, y grid de 3 tarjetas iterando `siteConfig.specialties` (Traumatología, Ortopedia infantil, Cirugía de columna) con íconos de lucide-react (Bone, Baby, Sparkles). Todo dentro de la primera `<section>`, sin necesidad de scroll.</action>
  <verify>Verificado visualmente en navegador — hero con CTA y las 3 especialidades visible sin hacer scroll (desktop y mobile)</verify>
  <acceptance_criteria>
    - "page.tsx contiene 'WhatsAppCta location=\"hero\"' dentro de la primera <section>"
    - "page.tsx itera siteConfig.specialties dentro del hero"
  </acceptance_criteria>
  <done>Hero con CTA WhatsApp y 3 especialidades renderiza como primera sección de la página</done>
</task>

<task type="auto">
  <name>Task 2: Secciones de especialidades, credibilidad y CTA final</name>
  <files>src/app/page.tsx</files>
  <read_first>src/content/services.ts</read_first>
  <action>Sección "Especialidades" iterando `serviceCategories` (3 tarjetas con nombre, descripción, primeras 4 condiciones, link a `/servicios#${cat.slug}`). Sección "Por qué elegirme" con colegiatura verificada, sede fija, 3 especialidades y consulta directa por WhatsApp. Sección CTA final con `WhatsAppCta location="hero"` repetido antes del footer.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - "page.tsx mapea serviceCategories con Link a `/servicios#${cat.slug}`"
  </acceptance_criteria>
  <done>Secciones de especialidades y credibilidad completas con copy real</done>
</task>

<task type="auto">
  <name>Task 3: Preview de testimonios y FAQ con link a página completa</name>
  <files>src/app/page.tsx</files>
  <read_first>src/content/testimonials.ts, src/content/faq.ts</read_first>
  <action>Sección de testimonios iterando `testimonials` (cita + autor + fuente) más tarjeta de link a Doctoralia (`reviewLinks.doctoralia`). Sección de FAQ mostrando `faqItems.slice(0, 3)` con link "Ver todas las preguntas frecuentes" hacia `/preguntas-frecuentes`.</action>
  <verify>Verificado visualmente — ambas secciones enlazan a sus páginas completas sin romper el layout</verify>
  <acceptance_criteria>
    - "page.tsx contiene faqItems.slice(0, 3)"
    - "page.tsx enlaza a /preguntas-frecuentes y a reviewLinks.doctoralia"
  </acceptance_criteria>
  <done>Previews de testimonios y FAQ completos, cada uno enlazando a su página dedicada</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npm run build limpio
- [x] Hero con CTA WhatsApp + 3 especialidades visible sin scroll (verificado en navegador, desktop y mobile)
- [x] Previews de testimonios y FAQ enlazan correctamente a sus páginas
</verification>

<success_criteria>
- Home comunica las 3 especialidades y tiene CTA de WhatsApp visible sin hacer scroll
- Todas las secciones usan copy real (sin lorem ipsum) desde `src/content/*.ts`
</success_criteria>

---
**Estado:** Completado — ver `02-SUMMARY.md`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
