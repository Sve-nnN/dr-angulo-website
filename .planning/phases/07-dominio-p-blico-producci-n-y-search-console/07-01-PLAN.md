---
phase: 07-dominio-p-blico-producci-n-y-search-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proxy.ts
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03]
user_setup: []

must_haves:
  truths:
    - "Con el build de produccion corriendo local, una peticion con cabecera Host de www responde 301 y location en el apex (D-04)."
    - "En produccion, https://www.drangulocolumna.com responde 301 hacia https://drangulocolumna.com conservando path y query (D-01, D-04)."
    - "https://drangulocolumna.com responde 200 con certificado valido y el apex por http llega a https (DOM-01, ya cumplido, se verifica)."
    - "El fallback de siteConfig.url en HEAD apunta al dominio propio: ningun archivo bajo src/ menciona vercel.app. Esta es la prueba real de DOM-03 y es de codigo, no de red."
    - "Despues del deploy de la tarea 2, el HTML servido emite canonical y og:url sobre el apex y sitemap.xml lista solo URLs del apex (DOM-03)."
  artifacts:
    - "src/proxy.ts"
    - ".planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-01-SUMMARY.md"
  key_links:
    - "src/proxy.ts lee la cabecera Host entrante y construye el destino sobre el mismo host sin el prefijo www, sin depender de NEXT_PUBLIC_SITE_URL."
    - "El commit de src/proxy.ts empujado a main dispara el webhook de GitHub configurado en Dokploy (triggerType push) y despliega la aplicacion 29ZFzVVwEczNI733DodMp."
---

<objective>
Dejar en el origen la canonicalizacion de www hacia el apex y levantar evidencia medida de lo que DOM-01 y DOM-03 ya cumplen en produccion.

Este plan entrega el respaldo de defensa en profundidad que exige D-04: un archivo de proxy en el repo que responde 301 de `www.drangulocolumna.com` hacia `https://drangulocolumna.com`. Es el unico archivo fuente nuevo de la fase. Cubre el caso de que alguien apague el proxy de Cloudflare o mueva el DNS, y de paso deja DOM-02 funcionando desde el origen antes de que el plan 07-02 lo mueva al borde.

DOM-01 ya pasa en produccion segun la medicion del 2026-08-09 registrada en 07-CONTEXT.md. Aqui NO se reconstruye: se verifica con comandos y se deja la evidencia en el SUMMARY.

**Correccion sobre DOM-03, 2026-08-10.** La tabla de 07-CONTEXT.md daba DOM-03 por cumplido. Era falso para el repositorio. El `siteConfig.url` que se habia leido era trabajo local sin commitear; lo que estaba en `HEAD`, y por lo tanto lo que corre hoy en produccion, tenia el fallback apuntando a `https://dr-angulo.vercel.app`. El canonical en vivo salia correcto unicamente porque `NEXT_PUBLIC_SITE_URL` esta cargada en Dokploy, que es exactamente la muleta que DOM-03 existe para eliminar. Juan commiteo el trabajo local el 2026-08-10 en `5387091`, asi que `HEAD` ya trae el fallback corregido a `https://drangulocolumna.com`.

Consecuencia para este plan: DOM-03 dejo de ser puramente verificable y pasa a depender de un deploy. La prueba honesta del requisito es de codigo, no de red, y se hace contra `HEAD`. Las mediciones en vivo se corren DESPUES del deploy de la tarea 2, nunca antes, porque antes se estaria midiendo el binario viejo. No se pide quitar `NEXT_PUBLIC_SITE_URL` de produccion para probar el fallback.

Queda fuera por D-09: GA4, Meta Pixel, variables del feed de Instagram, contenido nuevo, rutas nuevas y schema.

**Nota de alcance, no es trabajo de esta fase.** El commit `5387091` del 2026-08-10 entro con mucho mas que el fallback: 843 lineas que adelantan alcance de las fases 9, 10 y 11. Concretamente `src/components/structured-data.tsx` crecio 403 lineas con breadcrumbs, `hasCredential` y `openingHoursSpecification`, mas marcado de resenas; aparecieron `src/lib/google-reviews.ts` y `src/components/reviews/google-reviews.tsx`; y `src/content/locations.ts` sumo 43 lineas. Nada de eso se toca ni se verifica en la fase 7. Queda anotado aca para que cuando se planifiquen SEO-05, SEO-06, SEO-07 y las fases de sedes nadie lo reimplemente desde cero: primero hay que leer lo que ya existe en el repositorio y planificar sobre eso. La seccion de resenas depende de `GOOGLE_PLACES_API_KEY`, que no esta cargada en Dokploy, asi que se degrada sin romper nada.

Purpose: sin canonicalizacion de host el sitio se sirve identico en dos direcciones, que es exactamente lo que hoy pasa, y Google reparte la autoridad entre las dos.
Output: `src/proxy.ts` desplegado en produccion mas evidencia medida de DOM-01 y DOM-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-CONTEXT.md
@AGENTS.md
@src/lib/site-config.ts
@next.config.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Redireccion www a apex desde el origen, extremo a extremo</name>
  <files>src/proxy.ts</files>
  <read_first>
    - `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` (referencia canonica de la version instalada)
    - `node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md`
    - `AGENTS.md`
    - `next.config.ts`
    - `src/lib/site-config.ts`
    - `package.json` (confirmar que la version de next sigue siendo 16.2.12)
  </read_first>
  <action>
    Crear `src/proxy.ts`. Va dentro de `src/`, al mismo nivel que `src/app`, porque el proyecto usa carpeta `src`. NO crear un archivo llamado middleware: en Next.js 16 esa convencion esta deprecada y renombrada, el archivo se llama `proxy.ts` y la funcion exportada se llama `proxy`. Exportarla como export nombrado `proxy` con firma `(request: NextRequest)`, importando `NextResponse` y el tipo `NextRequest` desde `next/server`.

    Logica: leer el host entrante desde `request.headers.get("host")`, no desde `request.nextUrl.host`, porque detras de Cloudflare y Traefik la cabecera cruda es la fuente confiable. Normalizar a minusculas y quitar un eventual sufijo de puerto. Si el host no empieza con el prefijo de tres letras w seguido de punto, devolver `NextResponse.next()` y no hacer nada mas.

    Si empieza con ese prefijo, construir el destino con esquema `https`, host igual al host entrante sin ese prefijo, y conservar `request.nextUrl.pathname` y `request.nextUrl.search` tal cual. No leer `siteConfig.url` ni `NEXT_PUBLIC_SITE_URL`: derivar el apex del propio host entrante deja el archivo sin configuracion y funcionando en cualquier entorno, y la documentacion de Next.js 16 desaconseja depender de modulos compartidos dentro de proxy.

    Devolver `NextResponse.redirect(destino, 301)` con el 301 explicito como segundo argumento. `NextResponse.redirect` sin estado devuelve 307 y eso no cumple DOM-02 (D-01).

    Exportar tambien `export const config` con `matcher` como arreglo de un solo patron que excluya unicamente `_next/static` y `_next/image`. Las rutas de API, `robots.txt` y `sitemap.xml` tienen que quedar DENTRO del matcher: una peticion a esas rutas por www tambien debe canonicalizar. El valor del matcher tiene que ser una constante literal para que Next lo analice en build.

    No agregar la opcion `runtime` en el objeto config: en Next.js 16 proxy corre en runtime Node por defecto y declarar `runtime` lanza error.

    Comentar en el archivo, en espanol neutro, que este 301 es el respaldo de origen y que la redireccion primaria vive en una Redirect Rule de Cloudflare (D-02).
  </action>
  <behavior>
    - Host de www, ruta `/servicios`: responde 301 con location `https://drangulocolumna.com/servicios`.
    - Host de www, ruta `/sitemap.xml`: responde 301 con location `https://drangulocolumna.com/sitemap.xml`.
    - Host de www, ruta `/contacto?utm_source=gbp`: la query se conserva en el location.
    - Host `drangulocolumna.com`, ruta `/`: responde 200, la pagina se sirve normal.
  </behavior>
  <reversibility rating="reversible">Un archivo fuente nuevo que se puede borrar en un commit; la redireccion primaria es la de Cloudflare.</reversibility>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit && npm run lint && npm run build && { npx next start -p 3111 & echo $! > /tmp/gsd-p7.pid; }; sleep 10; echo "A:"; curl -sS -o /dev/null -w '%{http_code} %{redirect_url}\n' -H 'Host: www.drangulocolumna.com' http://127.0.0.1:3111/servicios; echo "B:"; curl -sS -o /dev/null -w '%{http_code} %{redirect_url}\n' -H 'Host: www.drangulocolumna.com' http://127.0.0.1:3111/sitemap.xml; echo "C:"; curl -sS -o /dev/null -w '%{http_code}\n' -H 'Host: drangulocolumna.com' http://127.0.0.1:3111/; kill "$(cat /tmp/gsd-p7.pid)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina con codigo 0.
    - `npm run lint` termina con codigo 0.
    - `npm run build` termina con codigo 0.
    - Linea A imprime exactamente `301 https://drangulocolumna.com/servicios`.
    - Linea B imprime exactamente `301 https://drangulocolumna.com/sitemap.xml`.
    - Linea C imprime exactamente `200`.
    - `test -f src/proxy.ts` devuelve 0.
    - `ls src/middleware.ts middleware.ts 2>/dev/null | wc -l` devuelve 0.
    - `grep -c 'export function proxy\|export const proxy\|export default function proxy' src/proxy.ts` devuelve 1 o mas.
    - `grep -v '^\s*//' src/proxy.ts | grep -c '301'` devuelve 1 o mas: el estado va explicito, no implicito.
  </acceptance_criteria>
  <done>El build de produccion, corriendo localmente, canonicaliza cualquier ruta de www hacia el apex con un 301 y sirve el apex con 200.</done>
</task>

<task type="auto">
  <name>Task 2: Desplegar el respaldo de origen y confirmar el 301 en produccion</name>
  <files>src/proxy.ts</files>
  <read_first>
    - `src/proxy.ts` (el archivo creado en la tarea 1)
    - `/Users/juan/Documents/Codigo/Personal/hosting/infra/API-DEPLOY-GUIDE.md` (paso 7 del flujo estandar y la seccion Debugging playbook)
    - `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-CONTEXT.md` (bloque de code_context, seccion Integration Points)
  </read_first>
  <precondition>El archivo `/Users/juan/Documents/Codigo/Personal/hosting/infra/dokploy/.env` existe y define `DOKPLOY_URL` y `DOKPLOY_TOKEN`. Cargarlo con `set -a; . <ruta>; set +a` y nunca imprimir su contenido.</precondition>
  <action>
    Commitear unicamente `src/proxy.ts` con mensaje `feat(07-01): redirige www al apex con 301 desde el origen` y empujar a `main` en `origin`, que es `https://github.com/Sve-nnN/dr-angulo-website.git`. Antes de commitear, correr `git diff --cached` y confirmar que el diff no contiene ninguna cadena que parezca una credencial.

    ATENCION con el alcance del push. El commit es de un solo archivo, pero `main` local viene adelantado de `origin/main` con commits previos que todavia no estan publicados, entre ellos `5387091`, que trae el fallback corregido de `siteConfig.url` mas 843 lineas de schema ampliado, cliente de Google Places y componente de resenas. Empujar `main` publica todo eso de una vez, no solo el proxy. Es lo correcto y esperado: ese mismo deploy es el que hace pasar DOM-03 en vivo. Pero significa que el build que va a correr es mucho mas grande que un archivo, asi que si falla puede fallar por algo ajeno a la redireccion. Antes de empujar, correr `git log --oneline origin/main..HEAD` y anotar en el SUMMARY la lista completa de commits que se van a publicar. Si aparece algun commit inesperado, detener y preguntar.

    Existe un archivo sin trackear, `src/app/api/reviews/status/route.ts`, que nadie referencia en el codigo commiteado. No commitearlo ni borrarlo: no pertenece a esta fase. Solo anotar su existencia en el SUMMARY.

    La aplicacion de Dokploy tiene `sourceType` github y `triggerType` push, asi que el push a `main` dispara el webhook y despliega solo. Esperar hasta 120 segundos consultando `application.one?applicationId=29ZFzVVwEczNI733DodMp` hasta que `applicationStatus` llegue a `done`. Si a los 120 segundos no arranco ningun despliegue nuevo, disparar `application.deploy` con body `{"applicationId":"29ZFzVVwEczNI733DodMp"}` y volver a esperar.

    Segun la guia de Dokploy, `done` significa que el build salio bien, no que el contenedor siguio en pie. Cruzar con `docker.getContainers` filtrando por `appName` `dr-angulo-website-nqscdc` y confirmar que no hay entradas `Exited` recientes. Si las hay, leer `application.readLogs?applicationId=29ZFzVVwEczNI733DodMp&tail=200&since=all` y corregir antes de dar la tarea por terminada.

    Verificar el 301 en produccion. La maquina local de Juan puede no resolver el host de www por cache del resolver, no por un problema del sitio, asi que resolver primero contra un resolver publico y usar la opcion `--resolve` de curl. En este momento el 301 viene del origen porque ese hostname sigue registrado en Dokploy: eso es correcto y esperado, el plan 07-02 lo mueve al borde.

    No tocar la lista de dominios de la aplicacion en este plan.
  </action>
  <reversibility rating="reversible">Revertir el commit y redesplegar restaura el estado anterior en minutos.</reversibility>
  <verify>
    <automated>IP=$(dig +short A www.drangulocolumna.com @1.1.1.1 | head -1); echo "IP=$IP"; echo "WWW:"; curl -sS -o /dev/null -w '%{http_code} %{redirect_url}\n' --resolve "www.drangulocolumna.com:443:$IP" https://www.drangulocolumna.com/servicios --max-time 25; echo "WWW_QS:"; curl -sS -o /dev/null -w '%{http_code} %{redirect_url}\n' --resolve "www.drangulocolumna.com:443:$IP" 'https://www.drangulocolumna.com/contacto?utm_source=gbp' --max-time 25; echo "APEX:"; curl -sS -o /dev/null -w '%{http_code}\n' https://drangulocolumna.com/ --max-time 25</automated>
  </verify>
  <acceptance_criteria>
    - `applicationStatus` de `application.one?applicationId=29ZFzVVwEczNI733DodMp` es `done` despues del despliegue.
    - `docker.getContainers` no muestra contenedores de `dr-angulo-website-nqscdc` en estado `Exited` posteriores al despliegue.
    - La linea `WWW:` imprime `301 https://drangulocolumna.com/servicios`.
    - La linea `WWW_QS:` imprime `301 https://drangulocolumna.com/contacto?utm_source=gbp`.
    - La linea `APEX:` imprime `200`.
    - `git log --oneline -1 -- src/proxy.ts` muestra el commit y `git status --porcelain src/proxy.ts` sale vacio.
  </acceptance_criteria>
  <done>El respaldo de origen esta desplegado y cualquier ruta pedida por www en produccion devuelve 301 hacia el apex conservando path y query.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar DOM-01 y DOM-03 contra produccion y dejar la evidencia</name>
  <files>.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-CONTEXT.md` (tabla "Estado medido el 2026-08-09")
    - `src/lib/site-config.ts`
    - `src/app/sitemap.ts`
    - `src/app/robots.ts`
    - `.planning/REQUIREMENTS.md` (DOM-01 y DOM-03)
  </read_first>
  <action>
    Tarea de verificacion, no de reconstruccion. Se corre DESPUES de la tarea 2, sin excepcion: la tarea 2 publica el commit que corrige el fallback, asi que medir produccion antes seria medir el binario viejo y dar un falso resultado sobre DOM-03.

    La verificacion tiene dos mitades que prueban cosas distintas y no se sustituyen entre si.

    **Mitad de codigo, que es la prueba real de DOM-03.** El requisito pide que el sitio no pueda caer a un dominio muerto si falta la variable de entorno. Eso solo se prueba leyendo el codigo: que el fallback de `siteConfig.url` en `HEAD` sea el dominio propio y que ningun archivo bajo `src/` mencione el dominio de despliegue de Vercel. El canonical en vivo NO prueba esto, porque `NEXT_PUBLIC_SITE_URL` esta cargada en Dokploy y tapa cualquier fallback, bueno o malo. No se pide quitar la variable de produccion para probarlo: seria arriesgar el sitio para verificar algo que el codigo ya responde.

    **Mitad de red, que prueba DOM-01 y que el deploy quedo consistente.** Confirma que lo que el sitio publica de si mismo sigue sobre el apex despues del despliegue.

    Medir y anotar en el SUMMARY, con el valor observado literal:
    1. Codigo HTTP y validez del certificado del apex, y que `http://drangulocolumna.com` llega a `https://` (D-01).
    2. La etiqueta canonical y la etiqueta og:url del HTML servido en la home.
    3. La lista completa de URLs del sitemap con su conteo. Al momento de planificar son 13: las 9 rutas base mas los 4 posts del blog. Anotar el conteo real, no el esperado.
    4. Que `robots.txt` declara `Allow: /`, `Disallow: /api/` y apunta al sitemap del apex.
    5. El valor literal del fallback de `siteConfig.url` tal como esta en `HEAD`, leido con `git grep` y no del archivo de trabajo, para que un cambio local sin commitear no pueda volver a dar un falso positivo como paso el 2026-08-09.
    6. Que `HEAD` no tiene ninguna referencia al dominio de despliegue de Vercel. El grep va acotado a `src/`, `next.config.ts` y `package.json`; el directorio `.planning/` queda fuera a proposito porque ahi si se menciona por razones historicas, incluida esta misma correccion.

    Si alguna medicion contradice la tabla de 07-CONTEXT.md, NO seguir adelante: anotarlo como bloqueo en el SUMMARY y detener el plan. Unica excepcion ya conocida y resuelta: la fila de DOM-03 de esa tabla esta desactualizada, y la explicacion vive en el objetivo de este plan.
  </action>
  <reversibility rating="reversible">Solo lee y documenta, no modifica nada.</reversibility>
  <verify>
    <automated>echo "APEX:"; curl -sS -o /dev/null -w '%{http_code} ssl=%{ssl_verify_result}\n' https://drangulocolumna.com/ --max-time 25; echo "APEX_HTTP:"; curl -sS -o /dev/null -w '%{http_code} %{redirect_url}\n' http://drangulocolumna.com/ --max-time 25; echo "CANONICAL:"; curl -sS https://drangulocolumna.com/ --max-time 25 | grep -o '<link rel="canonical" href="[^"]*"' | head -1; echo "OGURL:"; curl -sS https://drangulocolumna.com/ --max-time 25 | grep -o '<meta property="og:url" content="[^"]*"' | head -1; echo "SITEMAP_TOTAL:"; curl -sS https://drangulocolumna.com/sitemap.xml --max-time 25 | grep -c '<loc>'; echo "SITEMAP_APEX:"; curl -sS https://drangulocolumna.com/sitemap.xml --max-time 25 | grep -c '<loc>https://drangulocolumna.com'; echo "ROBOTS:"; curl -sS https://drangulocolumna.com/robots.txt --max-time 25; echo "FALLBACK_HEAD:"; git grep -h 'NEXT_PUBLIC_SITE_URL' HEAD -- src/lib/site-config.ts; echo "VERCEL_REFS_HEAD:"; git grep -c 'vercel\.app' HEAD -- src/ next.config.ts package.json | wc -l; echo "VERCEL_REFS_TREE:"; grep -rn 'vercel\.app' src/ next.config.ts package.json | wc -l; echo "DEPLOYED_HEAD:"; git rev-parse --short HEAD; git log --oneline origin/main..HEAD | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `APEX:` imprime `200 ssl=0`; `ssl=0` significa cadena de certificado valida.
    - `APEX_HTTP:` imprime un codigo 301 o 308 y una `redirect_url` que empieza con `https://drangulocolumna.com`.
    - `CANONICAL:` imprime `<link rel="canonical" href="https://drangulocolumna.com"`.
    - `OGURL:` imprime `<meta property="og:url" content="https://drangulocolumna.com"`.
    - `SITEMAP_TOTAL:` y `SITEMAP_APEX:` imprimen el mismo numero: toda URL del sitemap vive en el apex.
    - `ROBOTS:` contiene las lineas `Allow: /`, `Disallow: /api/` y `Sitemap: https://drangulocolumna.com/sitemap.xml`.
    - `FALLBACK_HEAD:` imprime una linea que contiene `|| "https://drangulocolumna.com"`. Este es el criterio que cierra DOM-03 de verdad: prueba que si `NEXT_PUBLIC_SITE_URL` faltara, el sitio caeria al dominio propio y no a uno muerto.
    - `VERCEL_REFS_HEAD:` imprime `0` y `VERCEL_REFS_TREE:` imprime `0`. Se comprueban los dos a proposito: `HEAD` es lo que se despliega y el arbol de trabajo es lo que se compila local. Que difieran fue justamente la causa del falso positivo del 2026-08-09.
    - `DEPLOYED_HEAD:` imprime el hash y luego `0`, es decir cero commits locales por delante de `origin/main`. Un numero distinto de cero significa que la tarea 2 no publico todo y que las mediciones de red de arriba estan describiendo un binario que no corresponde a `HEAD`; en ese caso el resultado de esta tarea no es valido.
    - El SUMMARY registra los valores observados literales, no una descripcion.
  </acceptance_criteria>
  <done>DOM-01 queda verificado en vivo y DOM-03 queda probado en el codigo de `HEAD` mas confirmado en vivo sobre el binario ya desplegado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> Cloudflare -> Traefik -> contenedor Next | Toda peticion publica cruza aqui; la cabecera Host entrante es dato no confiable |
| laptop de Juan -> API de Dokploy | Un token con permisos de despliegue viaja en cada llamada |
| repositorio local -> GitHub publico | Cualquier cosa commiteada queda publica de forma permanente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `DOKPLOY_TOKEN` leido de `infra/dokploy/.env` | high | mitigate | Cargar con `set -a; . archivo; set +a`. Nunca imprimir el archivo ni el valor. Pasar el token solo como cabecera `x-api-key`, jamas como parametro de query, porque las query strings quedan en logs de proxy |
| T-07-02 | Information Disclosure | commit de `src/proxy.ts` hacia GitHub publico | high | mitigate | `git diff --cached` obligatorio antes del commit; la tarea 2 commitea un solo archivo por ruta explicita, nunca `git add -A` |
| T-07-03 | Spoofing | cabecera `Host` entrante usada por `src/proxy.ts` | low | accept | El unico uso es decidir si redirigir. Un Host falsificado solo consigue un 301 hacia si mismo. No se usa para autenticacion, ni para construir enlaces persistidos, ni para elegir tenant |
| T-07-04 | Denial of Service | `matcher` de proxy demasiado amplio | low | mitigate | El matcher excluye `_next/static` y `_next/image` para no interponer logica delante de los assets estaticos, que es el error clasico que rompe CSS y JS |
| T-07-SC | Tampering | cadena de suministro de paquetes | high | accept | Este plan no instala ningun paquete y no modifica `package.json` ni `package-lock.json`. El gate de legitimidad de paquetes no aplica. Verificable: `git diff --name-only HEAD~1 HEAD` no debe listar `package.json` ni `package-lock.json` |
</threat_model>

<verification>
1. `src/proxy.ts` existe, compila, pasa lint y build.
2. En produccion, cualquier ruta pedida por www devuelve 301 hacia la misma ruta en el apex, con query conservada.
3. El apex sigue devolviendo 200 con certificado valido despues del despliegue.
4. El fallback de `siteConfig.url` en `HEAD` apunta al dominio propio y ni `HEAD` ni el arbol de trabajo referencian el dominio de despliegue de Vercel.
5. Medido despues del despliegue, canonical, og:url y sitemap siguen sobre el apex.
6. Ningun contenedor de `dr-angulo-website-nqscdc` quedo en crash loop tras el despliegue.
7. `origin/main` quedo al dia con `HEAD`, asi que lo medido en vivo corresponde al codigo verificado.
</verification>

<success_criteria>
- DOM-01 verificado con evidencia medida, sin reconstruir nada.
- DOM-03 cerrado: el fallback corregido llego a produccion en el mismo despliegue y queda probado por codigo, no por una variable de entorno que lo tape.
- DOM-02 cubierto desde el origen; el plan 07-02 lo mueve al borde y lo cierra.
- Un solo archivo fuente nuevo en el repo: `src/proxy.ts`.
</success_criteria>

## Artifacts this phase produces

Inventario completo de la fase 07. Este plan produce las filas marcadas con `[07-01]`.

**Archivos fuente nuevos**
- `src/proxy.ts` `[07-01]`: exporta `proxy(request: NextRequest)` y `config` con `matcher`. Unico archivo fuente nuevo de la fase (D-04).

**Archivos fuente modificados**
- `src/app/actions/contact.ts` `[07-03]`: trazas de fallo con prefijo filtrable y escapado del HTML del correo.
- `src/app/sitemap.ts` `[07-03]`: la ruta de privacidad sale del arreglo de rutas.
- `.env.example` `[07-03]`: documenta `RESEND_API_KEY`, `EMAIL_FROM` y `CONTACT_EMAIL_TO`.

**Artefactos de infraestructura, fuera del repositorio**
- Redirect Rule `www a apex 301` en la zona de Cloudflare `[07-02]`.
- Aplicacion `29ZFzVVwEczNI733DodMp` con un solo dominio registrado `[07-02]`.
- Dominio `drangulocolumna.com` verificado en Resend con sus registros DNS `[07-04]`.
- API key de Resend `dr-angulo-website-prod` con permiso de envio `[07-04]`.
- Entorno de produccion con `NEXT_PUBLIC_SITE_URL`, `RESEND_API_KEY`, `EMAIL_FROM` y `CONTACT_EMAIL_TO` `[07-04]`.
- Propiedad de tipo Dominio verificada en Google Search Console con su TXT `[07-05]`.
- `sitemap.xml` enviado desde esa propiedad `[07-05]`.

**Documentos de planificacion**
- `07-01-SUMMARY.md`, `07-02-SUMMARY.md`, `07-03-SUMMARY.md`, `07-04-SUMMARY.md`, `07-05-SUMMARY.md`
- `COVERAGE.md`

**Simbolos nuevos**
- `proxy` (funcion exportada de `src/proxy.ts`) `[07-01]`
- `config` (objeto exportado de `src/proxy.ts`) `[07-01]`
- Constante de prefijo de log a nivel de modulo y funcion local de escapado en `src/app/actions/contact.ts` `[07-03]`

<output>
Create `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-01-SUMMARY.md` when done
</output>
