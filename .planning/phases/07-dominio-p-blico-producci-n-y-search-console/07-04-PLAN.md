---
phase: 07-dominio-p-blico-producci-n-y-search-console
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified: []
autonomous: false
requirements: [DOM-04]

user_setup:
  - service: resend
    why: "El formulario de contacto tiene que entregar correo real desde el dominio propio y verificado (D-05). No hay cuenta creada y el agente no puede crearla"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key, permiso Sending access sobre drangulocolumna.com"
      - name: EMAIL_FROM
        source: "Valor fijo acordado en D-05: Dr. Juan Angulo <web@drangulocolumna.com>"
      - name: CONTACT_EMAIL_TO
        source: "Dato pendiente de Juan: la casilla existente donde el doctor quiere recibir las consultas. Es el unico bloqueante de este plan"
    dashboard_config:
      - task: "Agregar y verificar el dominio drangulocolumna.com"
        location: "Resend Dashboard -> Domains -> Add Domain"
      - task: "Cargar en Cloudflare los registros DNS que entregue Resend, todos en modo DNS only"
        location: "Cloudflare Dashboard -> zona drangulocolumna.com -> DNS -> Records"

must_haves:
  truths:
    - "El dominio drangulocolumna.com esta verificado en Resend y sus registros DKIM y SPF resuelven desde resolvers publicos (D-05)."
    - "La aplicacion 29ZFzVVwEczNI733DodMp tiene en su entorno de produccion las cuatro claves: NEXT_PUBLIC_SITE_URL, RESEND_API_KEY, EMAIL_FROM y CONTACT_EMAIL_TO (D-05)."
    - "El valor de EMAIL_FROM leido de vuelta desde Dokploy es exactamente Dr. Juan Angulo <web@drangulocolumna.com>, sin truncado ni comillas de mas (D-05)."
    - "Un envio de prueba por la API de Resend desde Dr. Juan Angulo <web@drangulocolumna.com> hacia la casilla del doctor devuelve 200 con identificador de mensaje."
    - "Un envio real del formulario en produccion llega a la casilla del doctor."
    - "Despues de ese envio, el log del contenedor no tiene ninguna linea con el prefijo de error de contacto (D-06)."
    - "NEXT_PUBLIC_SITE_URL sigue en el entorno y en buildArgs con el valor https://drangulocolumna.com; ninguna credencial quedo en buildArgs."
  artifacts:
    - "Dominio verificado en Resend con sus registros DNS en Cloudflare"
    - "Entorno de produccion de Dokploy con las tres variables de correo"
    - ".planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-04-SUMMARY.md"
  key_links:
    - "src/lib/resend.ts instancia el cliente solo si RESEND_API_KEY existe; sin esa clave el formulario no envia nada."
    - "siteConfig.email sale de CONTACT_EMAIL_TO; sin esa variable el envio al doctor se salta por completo."
    - "application.saveEnvironment reemplaza el bloque de entorno entero, asi que hay que releer y preservar lo que ya estaba."
---

<objective>
Cerrar DOM-04: que un paciente que envia el formulario genere un correo real que llegue a la casilla del doctor, salido de Resend con el dominio propio verificado.

Hoy el entorno de produccion de la aplicacion tiene una sola variable, `NEXT_PUBLIC_SITE_URL`. Faltan las tres de correo. Con el plan 07-03 ya desplegado, cualquier fallo de envio deja rastro en el log, asi que este plan se puede verificar de verdad en vez de suponer.

El bloqueante conocido es la casilla destino: no existe todavia y la decide Juan. Por eso la primera tarea es un checkpoint humano que la recoge junto con la cuenta de Resend y la API key (D-05).

Queda fuera por D-09: GA4, Meta Pixel, variables del feed de Instagram y el cron semanal, y la clave de Google Places. Este plan carga tres variables, ni una mas.

Purpose: el formulario es el unico canal de contacto del sitio que no pasa por WhatsApp; si no entrega correo, no existe.
Output: dominio verificado en Resend y produccion enviando correo real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-CONTEXT.md
@.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-03-SUMMARY.md
@src/lib/resend.ts
@src/lib/site-config.ts
@src/app/actions/contact.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Juan crea la cuenta de Resend, verifica el dominio y define la casilla destino</name>
  <read_first>
    - `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-CONTEXT.md` (seccion "Correo transaccional")
    - `src/lib/resend.ts`
    - `src/lib/site-config.ts`
  </read_first>
  <what-built>
    El codigo del formulario esta completo desde v1.0 y el plan 07-03 le agrego trazas de fallo. Lo que falta es infraestructura de correo: una cuenta de Resend, el dominio verificado, una API key y, sobre todo, la direccion donde el doctor quiere recibir las consultas. Ese ultimo dato no lo tiene nadie todavia y es el unico bloqueante de la fase.
  </what-built>
  <how-to-verify>
    Pasos exactos, en orden.

    **A. Cuenta y dominio en Resend**

    1. Abrir `https://resend.com` y crear la cuenta, o iniciar sesion si ya existe. Anotar con que correo quedo registrada: esa cuenta es la duena del envio del sitio.
    2. En el menu lateral, entrar a **Domains**.
    3. Hacer clic en **Add Domain**.
    4. En el campo de dominio escribir exactamente: `drangulocolumna.com`
       Sin `www`, sin `https://`, sin subdominio. Se verifica el apex (D-05).
    5. En **Region**, elegir la mas cercana a Peru de las que ofrezca el panel; `us-east-1` sirve.
    6. Hacer clic en **Add**.
    7. Resend muestra una tabla de registros DNS por cargar. Tipicamente un MX y un TXT de SPF sobre el subdominio `send`, y un TXT de DKIM sobre `resend._domainkey`. Dejar esa pantalla abierta: se copia registro por registro.

    **B. Cargar los registros en Cloudflare**

    8. En otra pestana abrir `https://dash.cloudflare.com`, entrar a la zona `drangulocolumna.com` y luego a **DNS** y **Records**.
    9. Para cada fila que muestre Resend, hacer clic en **Add record** y copiar tal cual el **Type**, el **Name** y el **Content** (y la **Priority** cuando sea MX). No reescribir ni traducir nada.
    10. En el campo **Name**, pegar solo la parte de subdominio que muestra Resend (por ejemplo `send`, o `resend._domainkey`), no el hostname completo. Cloudflare agrega la zona sola. Despues de guardar, confirmar que el nombre resultante quedo como `send.drangulocolumna.com` y `resend._domainkey.drangulocolumna.com`, sin el dominio repetido dos veces.
    11. Si algun registro es de tipo CNAME, dejarlo en **DNS only**, la nube gris. Un CNAME proxeado por Cloudflare rompe la validacion de Resend. Los MX y TXT no se pueden proxear, asi que esos no tienen la opcion.
    12. NO borrar ni editar ningun registro existente. En particular hay ya un TXT en el apex que empieza con `google-site-verification=`; ese se queda donde esta, lo usa el plan 07-05.
    13. Volver a Resend y hacer clic en **Verify DNS Records**. Esperar hasta que el dominio quede en estado **Verified**. Puede tardar algunos minutos.

    **C. API key**

    14. En Resend, entrar a **API Keys** y hacer clic en **Create API Key**.
    15. Nombre: `dr-angulo-website-prod`
    16. Permission: **Sending access**.
    17. Domain: `drangulocolumna.com`
    18. Hacer clic en **Add**. Resend muestra el valor, que empieza con `re_`, una sola vez. Copiarlo.

    **D. Casilla destino, el dato bloqueante**

    19. Decidir a que direccion de correo deben llegar las consultas del formulario. Segun 07-CONTEXT.md se descarto crear un buzon nuevo en el dominio: se usa una casilla que ya exista, del doctor o de Juan.

    **E. Entregar los dos secretos al agente**

    20. Pasar por el chat, en el mismo mensaje: el valor de la API key (`re_...`) y la direccion de la casilla destino. El agente los carga en Dokploy y no los escribe en ningun archivo del repositorio.
  </how-to-verify>
  <acceptance_criteria>
    Comandos que corre el agente despues de que Juan confirme.

    - `dig +short TXT resend._domainkey.drangulocolumna.com @1.1.1.1` devuelve al menos un registro y su contenido incluye `p=`.
    - `dig +short TXT send.drangulocolumna.com @1.1.1.1` devuelve un registro que incluye `v=spf1`.
    - `dig +short MX send.drangulocolumna.com @1.1.1.1` devuelve al menos un host de entrega.
    - Cada registro que Resend haya listado resuelve por `dig` sobre su nombre exacto con el contenido que muestra el panel. Si Resend pidio registros distintos a los tres de arriba, la comprobacion se hace sobre los que realmente pidio, no sobre estos.
    - `dig +short TXT drangulocolumna.com @1.1.1.1 | grep -c 'google-site-verification='` devuelve 1 o mas: el TXT preexistente sigue vivo.
    - Juan entrego un valor que empieza con `re_` y una direccion de correo con formato valido.
    - El SUMMARY registra con que cuenta quedo Resend y cual es la casilla destino, sin escribir la API key en ningun lado.
  </acceptance_criteria>
  <resume-signal>Pega la API key y la direccion de la casilla destino, o describe donde se atasco la verificacion del dominio</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Cargar las tres variables en Dokploy y redesplegar</name>
  <files></files>
  <read_first>
    - `/Users/juan/Documents/Codigo/Personal/hosting/infra/API-DEPLOY-GUIDE.md` (pasos 6 y 7 del flujo estandar)
    - `src/lib/resend.ts`
    - `src/lib/site-config.ts`
    - `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-01-SUMMARY.md`
  </read_first>
  <precondition>Juan entrego en la tarea 1 el valor de `RESEND_API_KEY` y el valor de `CONTACT_EMAIL_TO`, y el dominio figura como verificado en Resend. Sin esos dos valores no se puede correr esta tarea; detener el plan y esperar.</precondition>
  <action>
    Cargar credenciales de Dokploy con `set -a; . /Users/juan/Documents/Codigo/Personal/hosting/infra/dokploy/.env; set +a` sin imprimir nada.

    Leer primero el entorno actual con `application.one?applicationId=29ZFzVVwEczNI733DodMp` y quedarse con los campos `env`, `buildArgs`, `buildSecrets` y `createEnvFile`. Al momento de planificar valen: `env` igual a `NEXT_PUBLIC_SITE_URL=https://drangulocolumna.com`, `buildArgs` con esa misma linea, `buildSecrets` vacio y `createEnvFile` en true. `application.saveEnvironment` REEMPLAZA el bloque entero, asi que hay que reconstruirlo con lo que ya estaba mas lo nuevo. Perder `NEXT_PUBLIC_SITE_URL` en ese paso rompe el canonical del sitio.

    Componer el nuevo bloque `env` con cuatro lineas, en este orden y separadas por salto de linea:
    - `NEXT_PUBLIC_SITE_URL` con el valor que ya tenia, `https://drangulocolumna.com`
    - `RESEND_API_KEY` con el valor que entrego Juan
    - `EMAIL_FROM` con exactamente `Dr. Juan Angulo <web@drangulocolumna.com>` (D-05)
    - `CONTACT_EMAIL_TO` con la casilla que entrego Juan

    Cuidado especifico con `EMAIL_FROM`: su valor lleva espacios, un punto y los signos de menor y mayor que, dentro de un bloque de entorno que Dokploy materializa como archivo. Escribirlo primero sin comillas, que es como lo interpretan los parsers de dotenv, y despues LEERLO DE VUELTA con `application.one` y compararlo caracter por caracter con el valor acordado. Si vuelve truncado, con los signos comidos o con comillas de mas, reescribirlo entre comillas dobles y volver a leer hasta que la lectura coincida exacto. El valor no es secreto, asi que se puede imprimir para comparar. No dar la variable por cargada sin haberla leido de vuelta: el envio de prueba usa el valor que escribio el agente, no el que quedo en el contenedor, asi que un valor mal materializado pasaria esa prueba y fallaria en produccion.

    `buildArgs` se manda tal cual estaba, con la unica linea de `NEXT_PUBLIC_SITE_URL`. Las tres variables nuevas NO van a `buildArgs`: son de servidor en tiempo de ejecucion, y meterlas ahi hornea la API key en las capas de la imagen y en los logs de build. `buildSecrets` va como cadena vacia y `createEnvFile` en true. Los cuatro campos son obligatorios en el body o la API responde 400.

    POST a `$DOKPLOY_URL/api/application.saveEnvironment` con cabecera `x-api-key` y body con `applicationId`, `env`, `buildArgs`, `buildSecrets` y `createEnvFile`. Construir el JSON con un archivo temporal fuera del repositorio o con here-doc, nunca con la clave interpolada dentro de un comando que quede en el historial del shell.

    Disparar `application.deploy` con body `{"applicationId":"29ZFzVVwEczNI733DodMp"}` y esperar a `applicationStatus` igual a `done`, hasta 180 segundos. Cruzar con `docker.getContainers` sobre `appName` `dr-angulo-website-nqscdc`.

    Comprobar el camino de correo de extremo a extremo sin depender del formulario: POST directo a `https://api.resend.com/emails` con `Authorization: Bearer` y la API key, `from` igual al `EMAIL_FROM` acordado y `to` igual a la casilla destino. Un 200 con identificador prueba tres cosas de una: la clave sirve, el dominio esta verificado y el remitente esta autorizado. Un 403 significa dominio sin verificar y hay que volver a la tarea 1.
  </action>
  <reversibility rating="reversible">Se puede volver al bloque de entorno anterior con otra llamada a `saveEnvironment` y un redespliegue.</reversibility>
  <verify>
    <automated>set -a; . /Users/juan/Documents/Codigo/Personal/hosting/infra/dokploy/.env; set +a; echo "ENV_KEYS:"; curl -sS -H "x-api-key: $DOKPLOY_TOKEN" "$DOKPLOY_URL/api/application.one?applicationId=29ZFzVVwEczNI733DodMp" --max-time 30 | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s);const lines=(a.env||'').split('\n').filter(Boolean);const k=lines.map(l=>l.split('=')[0]).sort();console.log('env='+k.join(','));const b=(a.buildArgs||'').split('\n').filter(Boolean).map(l=>l.split('=')[0]).sort();console.log('buildArgs='+b.join(','));const ef=lines.find(l=>l.startsWith('EMAIL_FROM='));console.log('EMAIL_FROM_RAW=<'+(ef===undefined?'AUSENTE':ef.slice('EMAIL_FROM='.length))+'>');console.log('EMAIL_FROM_OK='+(ef!==undefined&&ef.slice('EMAIL_FROM='.length)==='Dr. Juan Angulo <web@drangulocolumna.com>'));console.log('status='+a.applicationStatus);});"; echo "APEX:"; curl -sS -o /dev/null -w '%{http_code}\n' https://drangulocolumna.com/ --max-time 25; echo "CONTACTO:"; curl -sS -o /dev/null -w '%{http_code}\n' https://drangulocolumna.com/contacto --max-time 25; echo "CANONICAL:"; curl -sS https://drangulocolumna.com/ --max-time 25 | grep -o '<link rel="canonical" href="[^"]*"' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `ENV_KEYS:` imprime `env=CONTACT_EMAIL_TO,EMAIL_FROM,NEXT_PUBLIC_SITE_URL,RESEND_API_KEY`.
    - La misma salida imprime `buildArgs=NEXT_PUBLIC_SITE_URL`: ninguna credencial llego a los argumentos de build.
    - `EMAIL_FROM_OK=true`. El valor que quedo materializado en el entorno del contenedor coincide caracter por caracter con `Dr. Juan Angulo <web@drangulocolumna.com>` (D-05). Si sale `false`, `EMAIL_FROM_RAW` muestra entre signos de menor y mayor que lo que realmente se guardo: comparar y corregir el escapado antes de seguir. Este gate existe porque los espacios y los signos de menor y mayor que del valor pueden sobrevivir mal al archivo de entorno, y el envio de prueba a la API de Resend no lo detectaria porque usa el valor que escribio el agente, no el del contenedor.
    - `status=done`.
    - `docker.getContainers` no muestra contenedores de `dr-angulo-website-nqscdc` en `Exited` posteriores al despliegue.
    - `APEX:` y `CONTACTO:` imprimen `200`.
    - `CANONICAL:` imprime `<link rel="canonical" href="https://drangulocolumna.com"`: `NEXT_PUBLIC_SITE_URL` sobrevivio al reemplazo del entorno.
    - El POST de prueba a `https://api.resend.com/emails` devuelve HTTP 200 y un cuerpo con campo `id`.
    - Ninguna salida de terminal de esta tarea contiene el valor de la API key.
  </acceptance_criteria>
  <done>Produccion tiene las cuatro variables, el contenedor esta sano y la API de Resend acepta un envio desde el remitente acordado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Envio real del formulario y confirmacion de llegada</name>
  <read_first>
    - `src/app/actions/contact.ts`
    - `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-03-SUMMARY.md` (que prefijo lleva la linea de error)
  </read_first>
  <what-built>
    Las tres variables ya estan cargadas y la API de Resend acepta envios desde el dominio verificado. Falta la unica prueba que cierra DOM-04 tal como esta escrito: que un envio hecho desde el formulario del sitio, por el camino real del paciente, llegue a la casilla del doctor.
  </what-built>
  <how-to-verify>
    1. Abrir `https://drangulocolumna.com/contacto` en un navegador, en ventana normal.
    2. Llenar el formulario con datos de prueba reconocibles. En el campo de nombre escribir algo como `Prueba fase 7`, un telefono cualquiera y un motivo de una linea.
    3. En el campo de correo, poner una direccion tuya a la que tengas acceso: el sitio manda ademas un acuse de recibo al paciente y conviene comprobar los dos correos.
    4. Enviar.
    5. Confirmar que la pagina muestra el mensaje de exito con el enlace de WhatsApp ya armado.
    6. Revisar la casilla destino que definiste en la tarea 1. Debe llegar un correo con asunto que empieza con `Nueva consulta desde la web`, remitido por `Dr. Juan Angulo <web@drangulocolumna.com>`. Revisar tambien la carpeta de correo no deseado: si llego ahi, anotarlo, porque significa que falta alineacion de DMARC y es seguimiento posterior.
    7. Revisar la direccion que pusiste en el paso 3. Debe llegar el acuse de recibo.
    8. Avisar por el chat: si llegaron los dos correos, a que carpeta, y a que hora aproximada enviaste.
  </how-to-verify>
  <acceptance_criteria>
    Comandos que corre el agente despues de que Juan confirme.

    - `application.readLogs?applicationId=29ZFzVVwEczNI733DodMp&tail=300&since=all` no contiene ninguna linea con el prefijo de error de contacto posterior a la hora del envio. Si aparece alguna, leerla: dice exactamente cual de los cuatro modos de fallo se disparo (D-06).
    - El mismo volcado de log no contiene el nombre, el telefono ni el motivo que se escribieron en el formulario. Verifica la regla de privacidad de T-07-09 sobre datos reales.
    - Juan confirma por escrito que el correo llego a la casilla destino y anota en que carpeta.
    - Juan confirma que el acuse de recibo llego a la direccion del remitente de prueba.
    - `curl -sS -o /dev/null -w '%{http_code}\n' https://drangulocolumna.com/contacto` sigue devolviendo `200`.
    - El SUMMARY registra la hora del envio, las dos llegadas y la carpeta de destino.
  </acceptance_criteria>
  <resume-signal>Escribe "llego" indicando la carpeta, o pega el mensaje de error que aparezca</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat con Juan -> agente | Una API key de envio viaja por el canal de conversacion |
| agente -> API de Dokploy | Se escribe un secreto en el entorno de un proveedor de hosting |
| contenedor de produccion -> API de Resend | Salida autenticada disparada por entrada anonima de internet |
| DNS de la zona -> validadores de correo | SPF y DKIM determinan quien puede firmar correo del dominio |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-14 | Information Disclosure | `RESEND_API_KEY` en `buildArgs` o en logs de build | high | mitigate | La clave va solo en `env`, nunca en `buildArgs` ni en `buildSecrets`. Gate automatizado: la salida de `buildArgs` debe listar unicamente `NEXT_PUBLIC_SITE_URL` |
| T-07-15 | Information Disclosure | la clave en el historial del shell o en la salida de terminal | high | mitigate | Construir el body JSON con here-doc o archivo temporal fuera del repositorio, filtrar toda respuesta de `application.one` para imprimir solo nombres de clave, nunca valores |
| T-07-16 | Information Disclosure | la clave escrita por accidente en un archivo del repositorio | high | mitigate | Este plan no modifica ningun archivo: `files_modified` va vacio. Verificable con `git status --porcelain` vacio al terminar |
| T-07-17 | Spoofing | correo del dominio falsificable sin SPF ni DKIM | high | mitigate | La verificacion del dominio en Resend carga SPF y DKIM. DMARC no lo pide Resend para verificar y queda como seguimiento posterior, anotado en el SUMMARY si algun correo cae en no deseado |
| T-07-18 | Elevation of Privilege | alcance excesivo de la API key | medium | mitigate | La key se crea con permiso de envio unicamente y acotada al dominio `drangulocolumna.com`, no con acceso completo a la cuenta |
| T-07-19 | Denial of Service | cuota de Resend consumida por envios automatizados contra el formulario | medium | accept | Ninguna fuente de esta fase pide limitacion de tasa. Quedan el honeypot, el proxy de Cloudflare y la cuota propia de Resend. Se anota como seguimiento posterior |
| T-07-20 | Information Disclosure | datos personales del paciente en el log del contenedor | high | mitigate | El plan 07-03 prohibio registrarlos; la tarea 3 lo comprueba contra un envio con datos reales grepeando el volcado de log |
| T-07-SC | Tampering | cadena de suministro de paquetes | high | accept | Este plan no instala paquetes y no toca `package.json`. El gate de legitimidad de paquetes no aplica |
</threat_model>

<verification>
1. Los registros DNS que pidio Resend resuelven desde resolvers publicos con el contenido que muestra el panel.
2. El TXT de verificacion de Google preexistente en el apex sigue intacto.
3. El entorno de produccion declara las cuatro claves y `buildArgs` sigue con una sola.
4. La API de Resend acepta un envio desde el remitente acordado con HTTP 200.
5. Un envio real del formulario llega a la casilla del doctor y el acuse de recibo llega al remitente.
6. El log del contenedor no tiene lineas de error de contacto ni datos personales del paciente.
7. El canonical del sitio sigue sobre el apex despues del reemplazo del entorno.
</verification>

<success_criteria>
- DOM-04 cerrado con un correo real recibido, no con una suposicion.
- Ninguna credencial en el repositorio, en `buildArgs` ni en la salida de terminal.
- El fallo silencioso queda descartado por evidencia de log, no por confianza.
</success_criteria>

## Artifacts this phase produces

Inventario completo de la fase 07. Este plan produce las filas marcadas con `[07-04]`.

**Archivos fuente nuevos**
- `src/proxy.ts` `[07-01]`: exporta `proxy(request: NextRequest)` y `config` con `matcher`. Unico archivo fuente nuevo de la fase (D-04).

**Archivos fuente modificados**
- `src/app/actions/contact.ts` `[07-03]`: trazas de fallo con prefijo filtrable y escapado del HTML del correo.
- `src/app/sitemap.ts` `[07-03]`: la ruta de privacidad sale del arreglo de rutas.
- `.env.example` `[07-03]`: documenta `RESEND_API_KEY`, `EMAIL_FROM` y `CONTACT_EMAIL_TO`.

**Artefactos de infraestructura, fuera del repositorio**
- Redirect Rule `www a apex 301` en la zona de Cloudflare `[07-02]`.
- Aplicacion `29ZFzVVwEczNI733DodMp` con un solo dominio registrado `[07-02]`.
- Dominio `drangulocolumna.com` verificado en Resend con sus registros DNS `[07-04]`.
- API key de Resend `dr-angulo-website-prod` con permiso de envio `[07-04]`.
- Entorno de produccion con `NEXT_PUBLIC_SITE_URL`, `RESEND_API_KEY`, `EMAIL_FROM` y `CONTACT_EMAIL_TO` `[07-04]`.
- Propiedad de tipo Dominio verificada en Google Search Console con su TXT `[07-05]`.
- `sitemap.xml` enviado desde esa propiedad `[07-05]`.

**Documentos de planificacion**
- `07-01-SUMMARY.md`, `07-02-SUMMARY.md`, `07-03-SUMMARY.md`, `07-04-SUMMARY.md`, `07-05-SUMMARY.md`
- `COVERAGE.md`

**Simbolos nuevos**
- `proxy` (funcion exportada de `src/proxy.ts`) `[07-01]`
- `config` (objeto exportado de `src/proxy.ts`) `[07-01]`
- Constante de prefijo de log a nivel de modulo y funcion local de escapado en `src/app/actions/contact.ts` `[07-03]`

<output>
Create `.planning/phases/07-dominio-p-blico-producci-n-y-search-console/07-04-SUMMARY.md` when done
</output>
