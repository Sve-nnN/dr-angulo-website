---
phase: 05-material-real-y-feed-de-instagram
plan: 05-02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/lib/instagram.ts, src/lib/instagram-shared.ts, src/components/instagram/, src/app/api/instagram/refresh/route.ts, src/lib/tracking.ts, src/app/page.tsx, src/app/testimonios/page.tsx, next.config.ts, docs/instagram-reels.md]
autonomous: true
requirements: [SOCIAL-01, SOCIAL-02]
user_setup:
  - "Cuenta de Instagram del doctor en modo Profesional (Business o Creator)"
  - "App en Meta for Developers con el producto Instagram (API con inicio de sesión de Instagram) y token de larga duración"
  - "Variables INSTAGRAM_ACCESS_TOKEN, INSTAGRAM_TOKEN_FILE y CRON_SECRET + volumen persistente en Dokploy"
  - "Cron semanal contra /api/instagram/refresh"

must_haves:
  truths:
    - "Home y Testimonios muestran un carrusel con los últimos reels del Instagram del doctor, que se actualiza solo"
    - "Cada tarjeta muestra portada y caption y abre el reel en Instagram, sin cargar scripts de terceros en el sitio"
    - "Sin token configurado o con la API caída, la sección cae en una tarjeta al perfil y el resto de la página sigue intacta"
    - "El token de larga duración se puede renovar sin redeploy, mediante un endpoint protegido llamable por cron"
    - "El carrusel es operable con teclado y anuncia correctamente sus controles a un lector de pantalla"
  artifacts:
    - path: "src/lib/instagram.ts"
      provides: "getInstagramReels (feed cacheado 1h), getAccessToken y refreshAccessToken con persistencia en disco — solo servidor"
    - path: "src/lib/instagram-shared.ts"
      provides: "Tipo InstagramReel y helper reelTitle, importables desde el cliente sin arrastrar node:fs"
    - path: "src/components/instagram/reels-carousel.tsx"
      provides: "Carrusel accesible con scroll-snap, botones prev/next y estados de borde"
    - path: "src/components/instagram/instagram-reels-section.tsx"
      provides: "Server component con encabezado, carrusel y fallback al perfil"
    - path: "src/app/api/instagram/refresh/route.ts"
      provides: "Endpoint protegido con CRON_SECRET que renueva el token e invalida la caché del feed"
    - path: "docs/instagram-reels.md"
      provides: "Paso a paso de vinculación: app de Meta, token, variables, volumen y cron"
  key_links:
    - from: "src/components/instagram/instagram-reels-section.tsx"
      to: "src/lib/instagram.ts"
      via: "await getInstagramReels(limit)"
      pattern: "getInstagramReels"
    - from: "src/components/instagram/reels-carousel.tsx"
      to: "src/lib/tracking.ts"
      via: "trackReelClick(reel.id) en el onClick de cada tarjeta"
      pattern: "trackReelClick"
    - from: "src/app/api/instagram/refresh/route.ts"
      to: "src/lib/instagram.ts"
      via: "refreshAccessToken() + revalidateTag(REELS_CACHE_TAG, 'max')"
      pattern: "refreshAccessToken|REELS_CACHE_TAG"
    - from: "next.config.ts"
      to: "CDN de Instagram"
      via: "images.remotePatterns para **.cdninstagram.com y **.fbcdn.net"
      pattern: "cdninstagram"
---

<objective>
Carrusel con los últimos reels del Instagram del doctor en Home y Testimonios, alimentado por la API oficial de Instagram y actualizado solo, con renovación automática del token y degradación limpia cuando no hay credenciales.

Purpose: el doctor publica contenido educativo en video de forma constante y hasta ahora el sitio no lo mostraba. Es prueba social y contenido fresco sin trabajo manual de nadie.
Output: módulo de Instagram + componentes + endpoint de refresh + documentación de vinculación.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Módulo de datos y token</name>
  <files>src/lib/instagram.ts, src/lib/instagram-shared.ts, next.config.ts</files>
  <action>`getInstagramReels(limit)` contra `graph.instagram.com/v25.0/me/media` pidiendo id, caption, media_type, media_product_type, permalink, thumbnail_url, media_url y timestamp; filtrar `media_product_type === "REELS"` (o `media_type === "VIDEO"` para publicaciones viejas), cachear con `next: { revalidate: 3600, tags: [REELS_CACHE_TAG] }` y devolver `[]` ante cualquier fallo. Token: leer primero el archivo de `INSTAGRAM_TOKEN_FILE`, si no existe caer a `INSTAGRAM_ACCESS_TOKEN`; `refreshAccessToken()` llama a `refresh_access_token?grant_type=ig_refresh_token` y persiste el token nuevo con su fecha de vencimiento. Mover tipos y `reelTitle` a `instagram-shared.ts` para que el cliente no importe `node:fs`. Habilitar los dominios del CDN de Instagram en `images.remotePatterns`.</action>
  <verify>El build no arrastra node:fs al bundle de cliente</verify>
  <acceptance_criteria>
    - "Sin token, getInstagramReels devuelve [] sin lanzar"
    - "next.config.ts permite **.cdninstagram.com y **.fbcdn.net"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Carrusel accesible y sección</name>
  <files>src/components/instagram/reels-carousel.tsx, src/components/instagram/instagram-reels-section.tsx, src/lib/tracking.ts</files>
  <action>Carrusel cliente: lista con scroll-snap horizontal, contenedor enfocable con `aria-labelledby` apuntando al título de la sección, botones prev/next con `aria-label` y `disabled` en los bordes (calculado con scroll + ResizeObserver), suavizado por CSS (`scroll-smooth` + `motion-reduce:scroll-auto`) en vez de `behavior: "smooth"` en JS para respetar la preferencia del sistema y no pelearse con el scroll-snap. Portadas con `alt=""` porque el caption ya está en el texto del enlace, y aviso `sr-only` de que abre en pestaña nueva. Sección servidor con encabezado, link al perfil y fallback. Evento `instagram_reel_click` nuevo en `tracking.ts`.</action>
  <verify>Navegación por teclado y estados disabled verificados en navegador</verify>
  <acceptance_criteria>
    - "Los botones se deshabilitan al llegar a cada extremo"
    - "La lista es alcanzable y desplazable con teclado"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Endpoint de refresh, montaje y documentación</name>
  <files>src/app/api/instagram/refresh/route.ts, src/app/page.tsx, src/app/testimonios/page.tsx, docs/instagram-reels.md</files>
  <action>Endpoint que valida `CRON_SECRET` con comparación de tiempo constante, renueva el token, invalida `REELS_CACHE_TAG` y responde con `refreshedAt`/`expiresAt` sin filtrar el token. Montar la sección en Home (8 reels) y Testimonios (12) y poner `export const revalidate = 3600` en ambas para que ISR las regenere aunque en el build no hubiera token. Documentar el paso a paso de vinculación en `docs/instagram-reels.md`.</action>
  <verify>El endpoint responde 401 sin el header correcto</verify>
  <acceptance_criteria>
    - "npm run build genera /api/instagram/refresh como ruta dinámica"
    - "docs/instagram-reels.md cubre app de Meta, token, variables, volumen y cron"
  </acceptance_criteria>
</task>

</tasks>
