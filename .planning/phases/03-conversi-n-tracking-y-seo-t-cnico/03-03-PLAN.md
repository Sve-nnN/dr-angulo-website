---
phase: 03-conversi-n-tracking-y-seo-t-cnico
plan: 03-03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/structured-data.tsx, src/app/sitemap.ts, src/app/robots.ts]
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03]
user_setup:
  - service: "Dominio de producción"
    why: "sitemap.ts, robots.ts y el JSON-LD usan siteConfig.url (NEXT_PUBLIC_SITE_URL) para construir URLs absolutas; hoy cae al fallback https://dr-angulo.vercel.app"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "URL real de producción una vez desplegado en Vercel (o dominio propio cuando exista) — actualizar en las env vars de Vercel"

must_haves:
  truths:
    - "El JSON-LD Physician se renderiza en todas las páginas (layout raíz) con nombre, teléfono, dirección y especialidades reales"
    - "El JSON-LD FAQPage se renderiza en /preguntas-frecuentes con las preguntas reales del sitio, no contenido de relleno"
    - "sitemap.xml incluye todas las páginas estáticas más una entrada por cada artículo de blog publicado"
    - "robots.txt permite el rastreo de todo el sitio salvo /api/, y referencia el sitemap"
    - "Cada página principal define su propio title/description/canonical vía la Metadata API de Next.js"
  artifacts:
    - path: "src/components/structured-data.tsx"
      provides: "PhysicianJsonLd (sitewide) y FaqJsonLd (recibe items reales por prop) — dos scripts JSON-LD independientes, no un @graph combinado"
    - path: "src/app/sitemap.ts"
      provides: "Metadata API sitemap — rutas estáticas + una ruta por post de src/content/blog"
    - path: "src/app/robots.ts"
      provides: "Metadata API robots — allow /, disallow /api/, referencia a sitemap.xml"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/structured-data.tsx"
      via: "<PhysicianJsonLd /> renderizado en el <body>, antes del Header"
      pattern: "PhysicianJsonLd"
    - from: "src/app/preguntas-frecuentes/page.tsx"
      to: "src/components/structured-data.tsx"
      via: "<FaqJsonLd items={faqItems} /> con contenido real de src/content/faq.ts"
      pattern: "FaqJsonLd items="
    - from: "src/app/sitemap.ts"
      to: "src/content/blog"
      via: "blogPosts.map(post => ({ url: .../blog/${post.slug}, ... }))"
      pattern: "blogPosts\\.map"
---

<objective>
El sitio es técnicamente indexable: cada página principal tiene metadata propia, existen sitemap.xml/robots.txt generados dinámicamente, y hay datos estructurados JSON-LD (Physician sitewide + FAQPage) con información real, no de relleno.

Purpose: sin esto Google no entiende qué es el sitio (médico, especialidad, ubicación) ni qué páginas debe indexar — es la base del SEO orgánico de las fases siguientes.
Output: `structured-data.tsx`, `sitemap.ts`, `robots.ts`, y confirmación de `metadata` propia en cada página.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/site-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON-LD Physician + FAQPage</name>
  <files>src/components/structured-data.tsx</files>
  <read_first>src/lib/site-config.ts</read_first>
  <action>`PhysicianJsonLd`: objeto `@type: "Physician"` con `name`, `url`, `telephone`, `medicalSpecialty` (`Musculoskeletal`, `Surgical`), `knowsAbout` (lista de condiciones tratadas), `address` (`PostalAddress` desde `siteConfig.clinic`), `geo` (`GeoCoordinates`), `hospitalAffiliation` (`MedicalOrganization` = Clínica Montefiori), `areaServed: Lima`, `sameAs` (Instagram + Doctoralia). `FaqJsonLd`: recibe `items: FaqJsonLdItem[]` como prop y arma `@type: "FAQPage"` con `mainEntity` mapeando cada item a `Question`/`acceptedAnswer`. Ambos se inyectan con `dangerouslySetInnerHTML`, escapando `<` a `<` para que el script no se rompa si algún dato contuviera ese carácter.</action>
  <verify>Inspección del HTML renderizado — confirmar que el script type="application/ld+json" parsea como JSON válido y contiene los campos requeridos/recomendados por schema.org para Physician y FAQPage</verify>
  <acceptance_criteria>
    - "structured-data.tsx exporta PhysicianJsonLd y FaqJsonLd"
    - "PhysicianJsonLd incluye address, geo y hospitalAffiliation con datos de siteConfig.clinic"
  </acceptance_criteria>
  <done>Ambos componentes exportados y consumidos (layout.tsx y preguntas-frecuentes/page.tsx respectivamente)</done>
</task>

<task type="auto">
  <name>Task 2: sitemap.ts + robots.ts</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <read_first>src/lib/site-config.ts, src/content/blog</read_first>
  <action>`sitemap.ts` exporta una función default que retorna `MetadataRoute.Sitemap`: rutas estáticas (home, sobre-el-doctor, servicios, testimonios, preguntas-frecuentes, contacto, blog, privacidad) con `changeFrequency`/`priority` diferenciados, más `blogPosts.map(...)` para generar una entrada por artículo con su `lastModified`. `robots.ts` exporta `MetadataRoute.Robots`: `userAgent: "*"`, `allow: "/"`, `disallow: ["/api/"]`, `sitemap: ${siteConfig.url}/sitemap.xml`.</action>
  <verify>npm run build — confirmar que /sitemap.xml y /robots.txt aparecen como rutas generadas</verify>
  <acceptance_criteria>
    - "sitemap.ts importa blogPosts de @/content/blog y genera una entrada por post"
    - "robots.ts referencia siteConfig.url en el campo sitemap"
  </acceptance_criteria>
  <done>Build genera /sitemap.xml y /robots.txt sin errores</done>
</task>

<task type="auto">
  <name>Task 3: Confirmar metadata por página vía Metadata API</name>
  <read_first>src/app/layout.tsx</read_first>
  <action>El layout raíz (Phase 1) define `metadataBase`, `title.template` (`%s | Dr. Juan Angulo`), Open Graph y Twitter card por default. Cada página de contenido — construida en su fase correspondiente: Phase 1 (layout), Phase 2 (páginas core), Phase 3 (`/contacto`), Phase 4 (`/blog`, `/privacidad`) — exporta su propio `metadata: Metadata` con `title`/`description`/`alternates.canonical` específicos, heredando OG/Twitter del layout. Esta tarea confirma que el patrón está aplicado sin huecos; no crea páginas nuevas, ya que las páginas en sí pertenecen a otras fases.</action>
  <verify>grep -rln "export const metadata" src/app --include="*.tsx" | wc -l</verify>
  <acceptance_criteria>
    - "Todas las páginas de contenido (home, sobre-el-doctor, servicios, testimonios, preguntas-frecuentes, contacto, blog, privacidad) exportan metadata con alternates.canonical propio"
    - "Ningún title se repite entre páginas"
  </acceptance_criteria>
  <done>9 archivos (layout + 8 rutas) con metadata propia confirmada</done>
</task>

</tasks>

<verification>
Antes de declarar el plan completo:
- [x] npm run build limpio — /sitemap.xml y /robots.txt generados
- [x] JSON-LD inspeccionado manualmente contra los campos requeridos/recomendados de schema.org para Physician y FAQPage
- [x] Metadata por página confirmada (title/description/canonical distintos por ruta)
</verification>

<success_criteria>
- sitemap.xml, robots.txt y el JSON-LD Physician/FAQPage son válidos
- Cada página principal tiene metadata propia (title/description/OG/canonical)
</success_criteria>

---
**Estado:** Completado — ver `03-SUMMARY.md`.
**Nota:** PLAN.md retroactivo (backfill de tracking GSD); el trabajo real se construyó y verificó en la sesión original del 2026-07-31, antes de que existiera este archivo.
